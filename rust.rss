<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-03-12T16:19:02.599Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[kora-lib] kora-lib: Token-2022 Transfer Fee Not Deducted During Payment Verification]]></title>
        <id>https://github.com/advisories/GHSA-725g-w329-g7qr</id>
        <link href="https://github.com/advisories/GHSA-725g-w329-g7qr"/>
        <updated>2026-03-12T14:50:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When a user pays transaction fees using a Token-2022 token with a <code>TransferFeeConfig</code> extension, Kora's <code>verify_token_payment()</code> credits the full raw transfer <code>amount</code> as the payment value. However, the on-chain SPL Token-2022 program withholds a portion of that amount as a transfer fee, so the paymaster's destination account only receives <code>amount - transfer_fee</code>. This means the paymaster consistently credits more value than it actually receives, resulting in systematic financial loss.</p>
<h2 id="severity">Severity</h2>
<p><strong>High</strong></p>
<h2 id="affected-component">Affected Component</h2>
<ul>
<li><strong>File:</strong> <code>crates/lib/src/token/token.rs</code></li>
<li><strong>Function:</strong> <code>verify_token_payment()</code></li>
<li><strong>Lines:</strong> 529–654 (specifically 633–639)</li>
</ul>
<h2 id="root-cause">Root Cause</h2>
<p>In <code>verify_token_payment()</code>, the <code>amount</code> extracted from the parsed SPL transfer instruction is the <strong>pre-fee</strong> amount (what the sender specifies in the <code>transfer_checked</code> instruction). The function passes this raw amount to <code>calculate_token_value_in_lamports()</code> to determine how many lamports the payment is worth. It never subtracts the Token-2022 transfer fee.</p>
<p>The fee estimation path (<code>fee.rs:analyze_payment_instructions</code>) correctly accounts for transfer fees by calculating them and adding them to the total fee. But the verification path does not perform the inverse subtraction, creating an asymmetry.</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<pre><code class="language-rust">// crates/lib/src/token/token.rs:529-654
pub async fn verify_token_payment(
    transaction_resolved: &amp;mut VersionedTransactionResolved,
    rpc_client: &amp;RpcClient,
    required_lamports: u64,
    expected_destination_owner: &amp;Pubkey,
) -&gt; Result&lt;bool, KoraError&gt; {
    let config = get_config()?;
    let mut total_lamport_value = 0u64;

    // ...

    for instruction in transaction_resolved
        .get_or_parse_spl_instructions()?
        .get(&amp;ParsedSPLInstructionType::SplTokenTransfer)
        .unwrap_or(&amp;vec![])
    {
        if let ParsedSPLInstructionData::SplTokenTransfer {
            source_address,
            destination_address,
            mint,
            amount,      // &lt;-- This is the PRE-FEE amount from the instruction
            is_2022,
            ..
        } = instruction
        {
            // ... destination validation ...

            // LINE 633-639: Uses raw *amount without deducting transfer fee
            let lamport_value = TokenUtil::calculate_token_value_in_lamports(
                *amount,        // &lt;-- BUG: Should be (amount - transfer_fee)
                &amp;token_mint,
                config.validation.price_source.clone(),
                rpc_client,
            )
            .await?;

            total_lamport_value = total_lamport_value
                .checked_add(lamport_value)
                .ok_or_else(|| {
                    KoraError::ValidationError("Payment accumulation overflow".to_string())
                })?;
        }
    }

    Ok(total_lamport_value &gt;= required_lamports)
}
</code></pre>
<p>For comparison, the transfer fee calculation exists elsewhere in the codebase and is used during fee estimation:</p>
<pre><code class="language-rust">// crates/lib/src/token/spl_token_2022.rs:165-198
pub fn calculate_transfer_fee(
    &amp;self,
    amount: u64,
    current_epoch: u64,
) -&gt; Result&lt;Option&lt;u64&gt;, KoraError&gt; {
    if let Some(fee_config) = self.get_transfer_fee() {
        let transfer_fee = if current_epoch &gt;= u64::from(fee_config.newer_transfer_fee.epoch) {
            &amp;fee_config.newer_transfer_fee
        } else {
            &amp;fee_config.older_transfer_fee
        };
        let basis_points = u16::from(transfer_fee.transfer_fee_basis_points);
        let maximum_fee = u64::from(transfer_fee.maximum_fee);
        let fee_amount = (amount as u128)
            .checked_mul(basis_points as u128)
            .and_then(|product| product.checked_div(10_000))
            // ...
        Ok(Some(std::cmp::min(fee_amount, maximum_fee)))
    } else {
        Ok(None)
    }
}
</code></pre>
<p>This function exists but is <strong>never called</strong> in <code>verify_token_payment()</code>.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="arithmetic-demonstration">Arithmetic Demonstration</h3>
<p>Given:</p>
<ul>
<li>Token-2022 token with 5% transfer fee (500 basis points), whitelisted in <code>allowed_spl_paid_tokens</code></li>
<li>Transaction fee cost: 5000 lamports equivalent</li>
<li>Token price: 1 token = 5 lamports</li>
</ul>
<p><strong>What should happen:</strong></p>
<ul>
<li>User needs to pay 5000 lamports worth → 1000 tokens</li>
<li>Transfer fee on 1000 tokens at 5% = 50 tokens</li>
<li>Paymaster destination receives: 1000 - 50 = 950 tokens (worth 4750 lamports)</li>
<li>User should be required to pay MORE to cover the fee</li>
</ul>
<p><strong>What actually happens:</strong></p>
<ul>
<li>User sends <code>transfer_checked</code> for <code>amount = 1000</code> tokens</li>
<li><code>verify_token_payment()</code> calculates: 1000 tokens * 5 lamports/token = 5000 lamports</li>
<li>5000 &gt;= 5000 required → <strong>payment verified as sufficient</strong></li>
<li>But paymaster only received 950 tokens (worth 4750 lamports)</li>
<li><strong>Paymaster lost 250 lamports on this transaction</strong></li>
</ul>
<p><strong>Over 1000 transactions:</strong> Paymaster loses 250,000 lamports (0.25 SOL)</p>
<h3 id="runnable-test-using-existing-test-infrastructure">Runnable Test (using existing test infrastructure)</h3>
<pre><code class="language-rust">#[tokio::test]
async fn test_token2022_transfer_fee_not_deducted_in_verification() {
    // Setup: Token-2022 mint with 10% transfer fee (1000 bps)
    let transfer_fee_config = create_transfer_fee_config(
        1000,      // 10% basis points
        u64::MAX,  // no maximum fee cap
    );

    let mint_pubkey = Pubkey::new_unique();
    let mint_account = MintAccountMockBuilder::new()
        .with_decimals(6)
        .with_supply(1_000_000_000_000)
        .with_extension(ExtensionType::TransferFeeConfig)
        .build_token2022();

    // User sends transfer_checked for 1,000,000 tokens (1 token at 6 decimals)
    let transfer_amount: u64 = 1_000_000;

    // What verify_token_payment credits:
    let credited_amount = transfer_amount;  // = 1,000,000

    // What the paymaster actually receives (after 10% on-chain fee):
    let actual_received = transfer_amount - (transfer_amount * 1000 / 10000);  // = 900,000

    // BUG: credited_amount (1,000,000) &gt; actual_received (900,000)
    // Paymaster is credited 11.1% MORE than it actually receives
    assert!(credited_amount &gt; actual_received);
    assert_eq!(credited_amount - actual_received, 100_000); // 100,000 token units lost

    // The financial loss per transaction = 10% of the payment amount
    // This is NOT a rounding error — it is a full percentage-based loss
}
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Systematic Financial Loss:</strong> The paymaster consistently credits more token value than it receives for every transaction paid with a transfer-fee-bearing Token-2022 token.</li>
<li><strong>Loss Scale:</strong> Proportional to <code>transfer_fee_basis_points / 10000 * payment_amount</code> per transaction. For a token with 5% fee and 100 transactions/day at $1 each, that is $5/day or $1,825/year in losses.</li>
<li><strong>Precondition:</strong> Requires a Token-2022 token with <code>TransferFeeConfig</code> extension to be whitelisted in <code>allowed_spl_paid_tokens</code>. The existing test infrastructure already creates such tokens (<code>TestAccountSetup::create_usdc_mint_2022()</code> with 100 bps / 1% fee).</li>
</ul>
<h2 id="recommendation">Recommendation</h2>
<p>Deduct the Token-2022 transfer fee before calculating the lamport value of the payment:</p>
<pre><code class="language-rust">// In verify_token_payment(), after extracting amount:
let effective_amount = if *is_2022 {
    // Fetch the mint to check for TransferFeeConfig
    let mint_account = CacheUtil::get_account(
        rpc_client,
        &amp;token_mint,
        false,
    ).await?;
    let mint_info = Token2022MintInfo::from_account_data(&amp;mint_account.data)?;

    if let Ok(Some(fee)) = mint_info.calculate_transfer_fee(
        *amount,
        rpc_client.get_epoch_info().await?.epoch,
    ) {
        amount.saturating_sub(fee)
    } else {
        *amount
    }
} else {
    *amount
};

let lamport_value = TokenUtil::calculate_token_value_in_lamports(
    effective_amount,  // Use post-fee amount
    &amp;token_mint,
    config.validation.price_source.clone(),
    rpc_client,
)
.await?;
</code></pre>
<h2 id="references">References</h2>
<ul>
<li><code>crates/lib/src/token/token.rs:529-654</code> — <code>verify_token_payment()</code> using raw amount</li>
<li><code>crates/lib/src/token/spl_token_2022.rs:165-198</code> — <code>calculate_transfer_fee()</code> (exists but not called in verification)</li>
<li><code>crates/lib/src/fee/fee.rs:174-204</code> — <code>analyze_payment_instructions()</code> (correctly accounts for transfer fee in estimation)</li>
<li>SPL Token-2022 specification: transfer fees are deducted from the transfer amount by the on-chain program</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/solana-foundation/kora/security/advisories/GHSA-725g-w329-g7qr">https://github.com/solana-foundation/kora/security/advisories/GHSA-725g-w329-g7qr</a></li>
<li><a href="https://github.com/solana-foundation/kora/commit/8cbd8217ee505e6b37c63ef835ff095cfa8ab318">https://github.com/solana-foundation/kora/commit/8cbd8217ee505e6b37c63ef835ff095cfa8ab318</a></li>
<li><a href="https://github.com/advisories/GHSA-725g-w329-g7qr">https://github.com/advisories/GHSA-725g-w329-g7qr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-725g-w329-g7qr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-12T14:50:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kora-lib] kora-lib: Unrecognized Instruction Types Create Empty Stubs That Bypass Fee Payer Policy]]></title>
        <id>https://github.com/advisories/GHSA-x442-m7cc-hr92</id>
        <link href="https://github.com/advisories/GHSA-x442-m7cc-hr92"/>
        <updated>2026-03-12T14:50:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When inner CPI instructions use instruction types not recognized by Kora's parser (including Token-2022 extension instructions like <code>ConfidentialTransfer</code>, <code>TransferFeeExtension::WithdrawWithheldTokens</code>, etc.), they are reconstructed as stub instructions with empty accounts and empty data. These stubs fail deserialization during fee payer policy validation and are silently skipped, meaning any fee payer usage within those instructions goes completely unchecked.</p>
<h2 id="severity">Severity</h2>
<p><strong>Medium</strong></p>
<h2 id="affected-component">Affected Component</h2>
<ul>
<li><strong>File:</strong> <code>crates/lib/src/transaction/instruction_util.rs</code></li>
<li><strong>Functions:</strong> <code>reconstruct_system_instruction()</code>, <code>reconstruct_spl_token_instruction()</code></li>
<li><strong>Lines:</strong> 750–753, 1187–1189</li>
</ul>
<h2 id="root-cause">Root Cause</h2>
<p>The instruction reconstruction functions have a catch-all <code>_ =&gt;</code> arm for unrecognized instruction types that creates a stub <code>CompiledInstruction</code> with the correct <code>program_id_index</code> but <strong>empty <code>accounts</code> and empty <code>data</code></strong>. When this stub reaches the fee payer policy parsing (<code>parse_system_instructions</code> / <code>parse_token_instructions</code>), deserialization of empty data fails. The parsing functions also have a catch-all <code>_ =&gt; {}</code> that silently skips the failed instruction. The result: the instruction exists in <code>all_instructions</code> (so program allowlist checks pass), but fee payer policy is never enforced on it.</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<h3 id="stub-creation">Stub Creation</h3>
<pre><code class="language-rust">// crates/lib/src/transaction/instruction_util.rs:750-753
// System program — unrecognized instruction type:
_ =&gt; {
    log::error!("Unsupported system instruction type: {}", instruction_type);
    Ok(Self::build_default_compiled_instruction(program_id_index))
}

// crates/lib/src/transaction/instruction_util.rs:1187-1189
// SPL Token program — unrecognized instruction type:
_ =&gt; {
    log::error!("Unsupported token instruction type: {}", instruction_type);
    Ok(Self::build_default_compiled_instruction(program_id_index))
}
</code></pre>
<p>The stub builder:</p>
<pre><code class="language-rust">pub fn build_default_compiled_instruction(program_id_index: u8) -&gt; CompiledInstruction {
    CompiledInstruction {
        program_id_index,
        accounts: vec![],  // &lt;-- No accounts
        data: vec![],       // &lt;-- No data
    }
}
</code></pre>
<h3 id="silent-skip-during-policy-parsing">Silent Skip During Policy Parsing</h3>
<pre><code class="language-rust">// In parse_system_instructions:
if let Ok(system_instruction) = bincode::deserialize::&lt;SystemInstruction&gt;(&amp;instruction.data) {
    match system_instruction {
        // ... known types handled ...
        _ =&gt; {}  // &lt;-- Unrecognized: silently skipped
    }
}
// If deserialize fails (empty data), the entire `if let Ok` block is skipped.
// The instruction is not added to any policy check map.

// In parse_token_instructions:
if let Ok(token_instruction) = TokenInstruction::unpack(&amp;instruction.data) {
    match token_instruction {
        // ... known types handled ...
        _ =&gt; {}  // &lt;-- Unrecognized: silently skipped
    }
}
// Same: empty data causes unpack to fail, instruction completely invisible to policy.
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="affected-token-2022-extension-instructions">Affected Token-2022 Extension Instructions</h3>
<p>The following Token-2022 extension instruction types are NOT handled by Kora's parser and would produce empty stubs:</p>
<table>
<thead>
<tr>
<th>Extension</th>
<th>Instruction</th>
<th>Risk if Fee Payer is Authority</th>
</tr>
</thead>
<tbody><tr>
<td><code>TransferFeeExtension</code></td>
<td><code>WithdrawWithheldTokensFromMint</code></td>
<td>Fee payer as withdraw authority can drain withheld fees</td>
</tr>
<tr>
<td><code>TransferFeeExtension</code></td>
<td><code>WithdrawWithheldTokensFromAccounts</code></td>
<td>Same</td>
</tr>
<tr>
<td><code>TransferFeeExtension</code></td>
<td><code>HarvestWithheldTokensToMint</code></td>
<td>Fee collection manipulation</td>
</tr>
<tr>
<td><code>ConfidentialTransfer</code></td>
<td><code>Transfer</code></td>
<td>Hidden transfer amounts bypass fee tracking</td>
</tr>
<tr>
<td><code>ConfidentialTransfer</code></td>
<td><code>Withdraw</code></td>
<td>Hidden withdrawals</td>
</tr>
<tr>
<td><code>InterestBearingMint</code></td>
<td><code>UpdateRate</code></td>
<td>Fee payer as rate authority can manipulate interest</td>
</tr>
<tr>
<td><code>TransferHook</code></td>
<td><code>Execute</code></td>
<td>Arbitrary hook execution</td>
</tr>
<tr>
<td><code>GroupMemberPointer</code></td>
<td><code>Update</code></td>
<td>Metadata manipulation</td>
</tr>
<tr>
<td><code>MetadataPointer</code></td>
<td><code>Update</code></td>
<td>Metadata manipulation</td>
</tr>
<tr>
<td><code>PermanentDelegate</code></td>
<td><code>Transfer</code> (via delegate)</td>
<td>Delegate-based unauthorized transfers</td>
</tr>
</tbody></table>
<h3 id="code-path-trace">Code Path Trace</h3>
<pre><code>1. Transaction contains an inner CPI instruction:
   Program: Token-2022
   Type: "withdrawWithheldTokensFromMint" (TransferFeeExtension)
   Accounts: [fee_payer (as withdraw_withheld_authority), mint, destination]

2. RPC returns this as a Parsed inner instruction

3. reconstruct_spl_token_instruction() is called:
   - instruction_type = "withdrawWithheldTokensFromMint"
   - No match in the known types (transfer, transferChecked, burn, etc.)
   - Falls through to _ =&gt; arm
   - Returns: CompiledInstruction { program_id_index, accounts: [], data: [] }

4. Stub is added to all_instructions
   → validate_programs() sees Token-2022 program ID → PASS (allowed)
   → validate_disallowed_accounts() sees no accounts in the stub → PASS

5. parse_token_instructions() processes the stub:
   - TokenInstruction::unpack(&amp;[]) → Err (empty data)
   - if let Ok(...) block skipped entirely
   - Instruction not added to any ParsedSPLInstructionType map

6. validate_fee_payer_usage() iterates parsed SPL instructions:
   - No entry for "withdrawWithheldTokensFromMint"
   - Fee payer's usage as withdraw_withheld_authority is NEVER checked

7. Transaction is signed by Kora

8. On-chain: fee_payer (as withdraw authority) withdraws withheld
   transfer fees from the mint to attacker's account
</code></pre>
<h3 id="verifiable-test">Verifiable Test</h3>
<pre><code class="language-rust">#[test]
fn test_unrecognized_instruction_produces_empty_stub() {
    // Simulate what happens for an unrecognized Token-2022 instruction
    let program_id_index: u8 = 3; // Token-2022 at index 3

    // This is what the catch-all arm produces:
    let stub = IxUtils::build_default_compiled_instruction(program_id_index);

    assert_eq!(stub.accounts.len(), 0);  // No accounts
    assert_eq!(stub.data.len(), 0);      // No data

    // Attempt to parse it:
    let result = TokenInstruction::unpack(&amp;stub.data);
    assert!(result.is_err());  // Cannot parse empty data

    // Therefore: fee payer policy is never applied to this instruction
    // The fee payer could be the withdraw_withheld_authority in the
    // REAL instruction, but the stub has zero accounts — invisible.
}
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Fee Payer Policy Bypass:</strong> Token-2022 extension instructions that use the fee payer as an authority are invisible to policy enforcement.</li>
<li><strong>Forward-Looking Risk:</strong> As Solana and SPL Token-2022 add new instruction types, they will automatically bypass all fee payer policy checks in Kora.</li>
<li><strong>Precondition:</strong> Requires the fee payer to hold some authority role (e.g., <code>withdraw_withheld_authority</code>, <code>permanent_delegate</code>) for Token-2022 accounts. This is unlikely in typical deployments but possible in misconfigured setups.</li>
</ul>
<h2 id="recommendation">Recommendation</h2>
<p>Reject transactions containing inner instructions with unrecognized types (fail-secure):</p>
<pre><code class="language-rust">// In reconstruct_system_instruction:
_ =&gt; {
    return Err(KoraError::InvalidTransaction(format!(
        "Unrecognized system instruction type '{}' in CPI — \
         cannot validate fee payer policy. Transaction rejected.",
        instruction_type
    )));
}

// In reconstruct_spl_token_instruction:
_ =&gt; {
    return Err(KoraError::InvalidTransaction(format!(
        "Unrecognized SPL Token instruction type '{}' in CPI — \
         cannot validate fee payer policy. Transaction rejected.",
        instruction_type
    )));
}
</code></pre>
<p>Alternatively, maintain a list of known-safe instruction types that don't involve authority checks, and only reject truly unknown types.</p>
<h2 id="references">References</h2>
<ul>
<li><code>crates/lib/src/transaction/instruction_util.rs:750-753</code> — system instruction catch-all</li>
<li><code>crates/lib/src/transaction/instruction_util.rs:1187-1189</code> — SPL token instruction catch-all</li>
<li><code>crates/lib/src/transaction/instruction_util.rs:316-319</code> — <code>build_default_compiled_instruction</code></li>
<li><a href="https://github.com/solana-labs/solana-program-library/tree/master/token/program-2022/src/extension">SPL Token-2022 instruction types</a> — full list of extension instructions</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/solana-foundation/kora/security/advisories/GHSA-x442-m7cc-hr92">https://github.com/solana-foundation/kora/security/advisories/GHSA-x442-m7cc-hr92</a></li>
<li><a href="https://github.com/advisories/GHSA-x442-m7cc-hr92">https://github.com/advisories/GHSA-x442-m7cc-hr92</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x442-m7cc-hr92</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-12T14:50:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[actix-web-lab] actix-web-lab has host header poisoning in redirect middleware can generate attacker-controlled absolute redirects]]></title>
        <id>https://github.com/advisories/GHSA-vhj5-x93p-67jw</id>
        <link href="https://github.com/advisories/GHSA-vhj5-x93p-67jw"/>
        <updated>2026-03-11T00:29:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>actix-web-lab</code> redirect middleware uses request-derived host information to construct absolute redirect URLs (for example, <code>https://{hostname}{path}</code>). In deployments without strict host allowlisting, an attacker can supply a malicious Host header and poison the <code>Location</code> response header, causing open redirect/phishing behavior.</p>
<h3 id="cve">CVE</h3>
<p>Assigned CVE ID:  CVE-2025-63762</p>
<h3 id="details">Details</h3>
<p>The issue is in redirect middleware paths that construct absolute URLs from <code>req.connection_info()</code>:</p>
<ol>
<li><p><code>actix-web-lab/src/redirect_to_https.rs</code> (around lines 119-132)</p>
<ul>
<li><code>let host = conn_info.host();</code></li>
<li><code>format!("https://{hostname}{path}")</code></li>
<li><code>format!("https://{hostname}:{port}{path}")</code></li>
</ul>
</li>
<li><p><code>actix-web-lab/src/redirect_to_www.rs</code> (around lines 30-35)</p>
<ul>
<li><code>format!("{scheme}://www.{host}{path}")</code></li>
</ul>
</li>
<li><p><code>actix-web-lab/src/redirect_to_non_www.rs</code> (around lines 30-34)</p>
<ul>
<li><code>format!("{scheme}://{host_no_www}{path}")</code></li>
</ul>
</li>
</ol>
<p>Because host values come from request connection metadata, untrusted Host input can influence redirect targets when deployment-side host validation is missing.</p>
<h3 id="poc">PoC</h3>
<p>Environment used for validation:</p>
<ul>
<li>Local minimal Actix apps using <code>actix-web-lab</code> middleware</li>
<li>RedirectHttps: <code>http://127.0.0.1:18080</code></li>
<li>redirect_to_www: <code>http://127.0.0.1:18081</code></li>
<li>redirect_to_non_www: <code>http://127.0.0.1:18082</code></li>
</ul>
<p>Reproduction (RedirectHttps):</p>
<pre><code class="language-bash">curl.exe -i -s "http://127.0.0.1:18080/test" -H "Host: attacker.example"
</code></pre>
<p>Observed response:</p>
<pre><code class="language-http">HTTP/1.1 307 Temporary Redirect
location: https://attacker.example/test
</code></pre>
<p>Additional verification:</p>
<pre><code class="language-bash">curl.exe -i -s "http://127.0.0.1:18080/abc/def" -H "Host: evil.example:9999"
</code></pre>
<p>Observed response:</p>
<pre><code class="language-http">HTTP/1.1 307 Temporary Redirect
location: https://evil.example/abc/def
</code></pre>
<p>Reproduction (redirect_to_www):</p>
<pre><code class="language-bash">curl.exe -i -s "http://127.0.0.1:18081/hello" -H "Host: attacker.example"
</code></pre>
<p>Observed response:</p>
<pre><code class="language-http">HTTP/1.1 307 Temporary Redirect
location: http://www.attacker.example/hello
</code></pre>
<p>Reproduction (redirect_to_non_www):</p>
<pre><code class="language-bash">curl.exe -i -s "http://127.0.0.1:18082/path" -H "Host: www.attacker.example"
</code></pre>
<p>Observed response:</p>
<pre><code class="language-http">HTTP/1.1 307 Temporary Redirect
location: http://attacker.example/path
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a Host header poisoning / open redirect issue. Users can be redirected to attacker-controlled domains, enabling phishing and trust-boundary abuse. Any application using these middleware paths without strict host validation (proxy/app allowlisting) is impacted.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/robjtede/actix-web-lab/security/advisories/GHSA-vhj5-x93p-67jw">https://github.com/robjtede/actix-web-lab/security/advisories/GHSA-vhj5-x93p-67jw</a></li>
<li><a href="https://github.com/robjtede/actix-web-lab/pull/292">https://github.com/robjtede/actix-web-lab/pull/292</a></li>
<li><a href="https://github.com/robjtede/actix-web-lab/commit/142c28b82eb59b67445a859a2a9b75e01a9964ee">https://github.com/robjtede/actix-web-lab/commit/142c28b82eb59b67445a859a2a9b75e01a9964ee</a></li>
<li><a href="https://github.com/advisories/GHSA-vhj5-x93p-67jw">https://github.com/advisories/GHSA-vhj5-x93p-67jw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vhj5-x93p-67jw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-11T00:29:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[quinn-proto] Quinn affected by unauthenticated remote DoS via panic in QUIC transport parameter parsing]]></title>
        <id>https://github.com/advisories/GHSA-6xvm-j4wr-6v98</id>
        <link href="https://github.com/advisories/GHSA-6xvm-j4wr-6v98"/>
        <updated>2026-03-11T05:46:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A remote, unauthenticated attacker can trigger a denial of service in applications using vulnerable <code>quinn</code> versions by sending a crafted QUIC Initial packet containing malformed <code>quic_transport_parameters</code>. <code>In quinn-proto</code> parsing logic, attacker-controlled varints are decoded with <code>unwrap()</code>, so truncated encodings cause <code>Err(UnexpectedEnd)</code> and <code>panic</code>. This is reachable over the network with a single packet and no prior trust or authentication.</p>
<h3 id="details">Details</h3>
<p>The issue is panic-on-untrusted-input in QUIC transport parameter parsing.
In <code>quinn-proto</code> (observed in <code>quinn-proto 0.11.13</code>), parsing of some transport parameters uses a fallible varint decode followed by <code>unwrap()</code>. For malformed/truncated parameter values, decode returns <code>UnexpectedEnd</code>, and <code>unwrap()</code> panics.</p>
<h4 id="observed-output">Observed output:</h4>
<pre><code>thread 'tokio-rt-worker' (2366474) panicked at quinn-proto/src/transport_parameters.rs:473:67:
called `Result::unwrap()` on an `Err` value: UnexpectedEnd
</code></pre>
<h3 id="poc">PoC</h3>
<h4 id="reproduces-against-the-upstream-quinn-server-example">Reproduces against the upstream Quinn server example.</h4>
<ol>
<li>Start server:</li>
</ol>
<pre><code>cargo run --example server -- ./
</code></pre>
<ol start="2">
<li>Prepare PoC client environment:</li>
</ol>
<pre><code>python3 -m venv .venv
source .venv/bin/activate
pip install aioquic
</code></pre>
<ol start="3">
<li>Run PoC script <a href="https://github.com/user-attachments/files/25741713/attack.py">attack.py</a> against server QUIC listener (default example target shown):</li>
</ol>
<pre><code>python attack.py
</code></pre>
<h4 id="observed-output-1">Observed output</h4>
<pre><code>thread 'tokio-rt-worker' (2366903) panicked at quinn-proto/src/transport_parameters.rs:473:67:
called `Result::unwrap()` on an `Err` value: UnexpectedEnd
</code></pre>
<h3 id="impact">Impact</h3>
<p>Vulnerability type: Remote Denial of Service (panic/crash)
Attack requirements:  Network reachability to UDP QUIC listener
Authentication/privileges: None
Who is impacted: Any server/application using affected <code>quinn/quinn-proto</code> versions where this parse path is reachable; process-level impact depends on integration panic handling policy</p>
<p>This vulnerability was originally submitted by @revofusion to the Ethereum Foundation bug bounty program</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/quinn-rs/quinn/security/advisories/GHSA-6xvm-j4wr-6v98">https://github.com/quinn-rs/quinn/security/advisories/GHSA-6xvm-j4wr-6v98</a></li>
<li><a href="https://github.com/quinn-rs/quinn/pull/2559">https://github.com/quinn-rs/quinn/pull/2559</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0037.html">https://rustsec.org/advisories/RUSTSEC-2026-0037.html</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-31812">https://nvd.nist.gov/vuln/detail/CVE-2026-31812</a></li>
<li><a href="https://github.com/advisories/GHSA-6xvm-j4wr-6v98">https://github.com/advisories/GHSA-6xvm-j4wr-6v98</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6xvm-j4wr-6v98</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-11T00:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rssn] RSSN has Arbitrary Code Execution via Unvalidated JIT Instruction Generation in C-FFI Interface]]></title>
        <id>https://github.com/advisories/GHSA-9c4h-pwmf-m6fj</id>
        <link href="https://github.com/advisories/GHSA-9c4h-pwmf-m6fj"/>
        <updated>2026-03-10T18:40:25.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p><strong>Vulnerability Type</strong>: 
Improper Control of Generation of Code ('Code Injection') (CWE-94) / Improper Check for Unusual or Exceptional Conditions (CWE-754) / Improper Input Validation (CWE-20) / Use of Low-Level Functionality (CWE-695) / Improper Privilege Management (CWE-269) / External Control of System or Configuration Setting (CWE-15).</p>
<p><strong>Technical Details</strong>:
The vulnerability exists in the JIT (Just-In-Time) compilation engine, which is fully exposed via the CFFI (Foreign Function Interface). Due to Improper Input Validation and External Control of Code Generation, an attacker can supply malicious parameters or instruction sequences through the CFFI layer. Since the library often operates with elevated privileges or within high-performance computing contexts, this allows for Arbitrary Code Execution (ACE) at the privilege level of the host process.</p>
<h2 id="who-is-impacted">Who is Impacted?</h2>
<ul>
<li>Developers using the library as a dynamic linked library (.so, .dll, .dylib) in multi-language environments (e.g., Python, Node.js, C++).</li>
<li>Cloud Service Providers running the library in multi-tenant environments or automated model-training pipelines.</li>
<li>Users processing untrusted or third-party datasets/models that may trigger malicious JIT instruction generation.
Patches</li>
<li>Affected versions: &lt; 0.2.8</li>
<li>Patched version: 0.2.9</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>If you cannot upgrade immediately, please consider the following mitigations:</p>
<ul>
<li>Strict Sandboxing: Run the library within a restricted sandbox (e.g., WebAssembly, Docker with non-root user, or seccomp profiles) to limit system call access.</li>
<li>Principle of Least Privilege: Ensure the process calling the library does not have administrative or root privileges.</li>
<li>Input Filtering: If possible, implement an application-level validation layer to sanitize any data passed to the CFFI interfaces.</li>
<li>Disable JIT (if applicable): If your workload allows, use the interpreter-only mode (if provided by the library) to bypass the JIT engine entirely.
CVSS Score</li>
<li>Vector: CVSS:4.0/AV:L/AC:L/AT:N/PR:N/UI:N/VC:H/VI:H/VA:H/SC:H/SI:H/SA:H</li>
<li>Base Score: 9.4 (Critical)</li>
</ul>
<h2 id="references">References</h2>
<p><a href="https://security.apich.org/">Apich Organization Security Team Homepage</a></p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/Apich-Organization/rssn/security/advisories/GHSA-9c4h-pwmf-m6fj">https://github.com/Apich-Organization/rssn/security/advisories/GHSA-9c4h-pwmf-m6fj</a></li>
<li><a href="https://github.com/Apich-Organization/rssn/releases/tag/v0.2.9">https://github.com/Apich-Organization/rssn/releases/tag/v0.2.9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30960">https://nvd.nist.gov/vuln/detail/CVE-2026-30960</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0038.html">https://rustsec.org/advisories/RUSTSEC-2026-0038.html</a></li>
<li><a href="https://github.com/advisories/GHSA-9c4h-pwmf-m6fj">https://github.com/advisories/GHSA-9c4h-pwmf-m6fj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9c4h-pwmf-m6fj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-10T01:19:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[soroban-env-host] Soroban: Muxed address<->ScVal conversions may break after a conversion failure]]></title>
        <id>https://github.com/advisories/GHSA-pm4j-7r4q-ccg8</id>
        <link href="https://github.com/advisories/GHSA-pm4j-7r4q-ccg8"/>
        <updated>2026-03-07T02:39:45.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Soroban host ensures that <code>MuxedAddress</code> objects can't be used as storage keys in order to proactively prevent the contract logic bugs. However, due to a bug in Soroban host implementation, a failure in <code>Val</code>-&gt;<code>ScVal</code> conversion during the storage key computation will have the flag indicating that storage conversion is happening stuck in the <code>true</code> state until the next storage access. While the flag is stuck in <code>true</code> state, any <code>MuxedAddress</code> object conversions to <code>ScVal</code> will fail, i.e. a failure will occur if a <code>MuxedAddress</code> is emitted in the event or is serialized to XDR via a host function.</p>
<h3 id="impact">Impact</h3>
<p>The bug may cause unexpected contract failures in the rare edge case scenarios. In the worst case scenario the whole transaction will fail and the changes will be rolled back. Because the contract call is simply rolled back, there is no risk of the state corruption.</p>
<p>An example scenario that would be affected by the bug is as follows:</p>
<ul>
<li>Contract A calls contract B via <code>try_call</code></li>
<li>Contract B calls a storage function (e.g. <code>put_contract_data</code>) with a non-convertible <code>Val</code> as a key (e.g. a <code>MuxedAddress</code> object, or a deeply nested vector)</li>
<li>Contract B fails</li>
<li>Contract A handles the failure gracefully and proceeds without accessing any storage methods</li>
<li>Contract A tries to emit an event with a <code>MuxedAddress</code> argument. That should be allowed, but instead of succeeding, contract A fails.</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The bug will be fixed in protocol 26.</p>
<h3 id="workarounds">Workarounds</h3>
<p>We believe that the bug is highly unlikely to occur in practice, as it involves three rare events happening simultaneously: <code>Val</code> conversion failure (these should normally not occur for the audited protocols), graceful handling of a cross-contract call failure (most protocols need cross-contract calls to succeed, or fail with a contract error), and <code>MuxedAddress</code> write (most of the contracts don't support <code>MuxedAddress</code> at all).</p>
<p>In the case if the bug does occur, the mitigation depends on the reason of the value conversion failure:</p>
<ul>
<li>If the conversion failure has been caused by a malicious contract, then either no action is necessary (because the whole interaction is malicious and has been correctly rolled back), or the contract invocation should be replaced by a non-malicious contract</li>
<li>If the conversion failure has been caused by a bad user input for a non-malicious contract (e.g. a bad user input passed to a legitimate protocol), then the user input has to be fixed</li>
</ul>
<p>In both scenarios the mitigation is to basically retry the transaction with proper arguments.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/stellar/rs-soroban-env/security/advisories/GHSA-pm4j-7r4q-ccg8">https://github.com/stellar/rs-soroban-env/security/advisories/GHSA-pm4j-7r4q-ccg8</a></li>
<li><a href="https://github.com/advisories/GHSA-pm4j-7r4q-ccg8">https://github.com/advisories/GHSA-pm4j-7r4q-ccg8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pm4j-7r4q-ccg8</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-07T02:39:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[time-sync] `time-sync` was removed from crates.io due to malicious code]]></title>
        <id>https://github.com/advisories/GHSA-mh23-rw7f-v5pq</id>
        <link href="https://github.com/advisories/GHSA-mh23-rw7f-v5pq"/>
        <updated>2026-03-05T21:15:45.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>time-sync</code> crate attempted to exfiltrate <code>.env</code> files to a server that was in turn impersonating the legitimate <code>timeapi.io</code> service. This the same attack that we've seen three times in the last few days.</p>
<p>The malicious crate had 1 version published on 2026-03-04 approximately 50 minutes before removal and had no evidence of actual downloads. There were no crates depending on this crate on crates.io.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0036.html">https://rustsec.org/advisories/RUSTSEC-2026-0036.html</a></li>
<li><a href="https://github.com/advisories/GHSA-mh23-rw7f-v5pq">https://github.com/advisories/GHSA-mh23-rw7f-v5pq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mh23-rw7f-v5pq</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-05T21:15:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pingora-cache] Pingora vulnerable to cache poisoning via insecure-by-default cache key]]></title>
        <id>https://github.com/advisories/GHSA-f93w-pcj3-rggc</id>
        <link href="https://github.com/advisories/GHSA-f93w-pcj3-rggc"/>
        <updated>2026-03-09T20:46:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Pingora versions prior to 0.8.0 generated cache keys using only the URI path, excluding critical factors such as the host header. This allows an attacker to poison the cache and serve cross-origin responses to users.</p>
<p>This vulnerability affects users of Pingora's alpha proxy caching feature who relied on the default CacheKey implementation. An attacker could exploit this for cross-tenant data leakage in multi-tenant deployments, or serve malicious content to legitimate users by poisoning shared cache entries.</p>
<p>Note: Cloudflare customers and Cloudflare's CDN infrastructure were not affected by this vulnerability, as Cloudflare's default cache key implementation uses multiple factors to prevent cache key poisoning and never made use of the previously provided default.</p>
<h3 id="patches">Patches</h3>
<p>We strongly suggest users should upgrade to Pingora v.0.8.0 or higher, which removes the default CacheKey implementation.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not rely on the provided CacheKey default, and at minimum use the host / :authority and the upstream peer TLS scheme as part of building the CacheKey, as well as other factors that may apply to the deployment e.g. HTTP method. </p>
<h3 id="references">References</h3>
<p>See <a href="https://cve.org/CVERecord?id=CVE-2026-2836">CVE-2026-2836</a> and the <a href="https://blog.cloudflare.com/pingora-oss-smuggling-vulnerabilities/">Cloudflare blog post</a> for more details.</p>
<h3 id="credits">Credits</h3>
<p>Disclosed responsibly by Rajat Raghav (@xclow3n) through the Cloudflare <a href="https://www.cloudflare.com/disclosure/">Bug Bounty Program</a>.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/cloudflare/pingora/security/advisories/GHSA-f93w-pcj3-rggc">https://github.com/cloudflare/pingora/security/advisories/GHSA-f93w-pcj3-rggc</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2836">https://nvd.nist.gov/vuln/detail/CVE-2026-2836</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0035.html">https://rustsec.org/advisories/RUSTSEC-2026-0035.html</a></li>
<li><a href="https://github.com/advisories/GHSA-f93w-pcj3-rggc">https://github.com/advisories/GHSA-f93w-pcj3-rggc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f93w-pcj3-rggc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-05T20:57:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pingora-cache] Duplicate Advisory: Cache poisoning via insecure-by-default cache key]]></title>
        <id>https://github.com/advisories/GHSA-2m8c-2374-465f</id>
        <link href="https://github.com/advisories/GHSA-2m8c-2374-465f"/>
        <updated>2026-03-05T20:57:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-f93w-pcj3-rggc. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>A cache poisoning vulnerability has been found in the Pingora HTTP proxy framework’s default cache key construction. The issue occurs because the default HTTP cache key implementation generates cache keys using only the URI path, excluding critical factors such as the host header (authority). Operators relying on the default are vulnerable to cache poisoning, and cross-origin responses may be improperly served to users.</p>
<p>Impact</p>
<p>This vulnerability affects users of Pingora's alpha proxy caching feature who relied on the default CacheKey implementation. An attacker could exploit this for:</p>
<ul>
<li><p>Cross-tenant data leakage: In multi-tenant deployments, poison the cache so that users from one tenant receive cached responses from another tenant</p>
</li>
<li><p>Cache poisoning attacks: Serve malicious content to legitimate users by poisoning shared cache entries</p>
</li>
</ul>
<p>Cloudflare's CDN infrastructure was not affected by this vulnerability, as Cloudflare's default cache key implementation uses multiple factors to prevent cache key poisoning and never made use of the previously provided default.</p>
<p>Mitigation:</p>
<p>We strongly recommend Pingora users to upgrade to Pingora v0.8.0 or higher, which removes the insecure default cache key implementation. Users must now explicitly implement their own callback that includes appropriate factors such as Host header, origin server HTTP scheme, and other attributes their cache should vary on.</p>
<p>Pingora users on previous versions may also remove any of their default CacheKey usage and implement their own that should at minimum include the host header / authority and upstream peer’s HTTP scheme.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2836">https://nvd.nist.gov/vuln/detail/CVE-2026-2836</a></li>
<li><a href="https://github.com/cloudflare/pingora">https://github.com/cloudflare/pingora</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0035.html">https://rustsec.org/advisories/RUSTSEC-2026-0035.html</a></li>
<li><a href="https://github.com/advisories/GHSA-2m8c-2374-465f">https://github.com/advisories/GHSA-2m8c-2374-465f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2m8c-2374-465f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-05T00:31:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pingora-core] Pingora has HTTP Request Smuggling via HTTP/1.0 and Transfer-Encoding Misparsing]]></title>
        <id>https://github.com/advisories/GHSA-hj7x-879w-vrp7</id>
        <link href="https://github.com/advisories/GHSA-hj7x-879w-vrp7"/>
        <updated>2026-03-09T20:45:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Pingora versions prior to 0.8.0 improperly allowed HTTP/1.0 request bodies to be close-delimited and incorrectly handled multiple Transfer-Encoding values. This allows an attacker to desync Pingora's request framing from backend servers and smuggle requests to the backend.</p>
<p>This vulnerability primarily affects standalone Pingora deployments in front of certain backends that accept HTTP/1.0 requests. An attacker could exploit this to bypass proxy-level ACL controls and WAF logic, poison caches and upstream connections, or perform cross-user attacks by hijacking sessions.</p>
<p>Note: Cloudflare customers and Cloudflare's CDN infrastructure were not affected by this vulnerability, as its ingress proxy layers rejected ambiguous framing such as invalid Content-Length values and internally forwarded non-ambiguous message length framing headers.</p>
<h3 id="patches">Patches</h3>
<p>Pingora users should upgrade to Pingora v0.8.0 or higher that fixes this issue by correctly parsing message length headers per RFC 9112 and strictly adhering to more RFC guidelines, including that HTTP request bodies are never close-delimited (commits 7f7166d62fa916b9f11b2eb8f9e3c4999e8b9023, 40c3c1e9a43a86b38adeab8da7a2f6eba68b83ad, and 87e2e2fb37edf9be33e3b1d04726293ae6bf2052).</p>
<h3 id="workarounds">Workarounds</h3>
<p>As a workaround, users can reject certain requests with an error in the request filter logic in order to stop processing bytes on the connection and disable downstream connection reuse. The user should reject any non-HTTP/1.1 request, or a request that has invalid Content-Length, multiple Transfer-Encoding headers, or Transfer-Encoding header that is not an exact “chunked” string match.</p>
<h3 id="references">References</h3>
<p>See <a href="https://www.cve.org/CVERecord?id=CVE-2026-2835">CVE-2026-2835</a> and the <a href="https://blog.cloudflare.com/pingora-oss-smuggling-vulnerabilities/">Cloudflare blog post</a> for more details.</p>
<h3 id="credits">Credits</h3>
<p>Disclosed responsibly by Rajat Raghav (@xclow3n) through the Cloudflare <a href="https://www.cloudflare.com/disclosure/">Bug Bounty Program</a>.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/cloudflare/pingora/security/advisories/GHSA-hj7x-879w-vrp7">https://github.com/cloudflare/pingora/security/advisories/GHSA-hj7x-879w-vrp7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2835">https://nvd.nist.gov/vuln/detail/CVE-2026-2835</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0034.html">https://rustsec.org/advisories/RUSTSEC-2026-0034.html</a></li>
<li><a href="https://github.com/advisories/GHSA-hj7x-879w-vrp7">https://github.com/advisories/GHSA-hj7x-879w-vrp7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hj7x-879w-vrp7</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-05T20:56:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pingora-core] Duplicate Advisory: HTTP Request Smuggling via HTTP/1.0 and Transfer-Encoding Misparsing]]></title>
        <id>https://github.com/advisories/GHSA-262p-vjx5-45xh</id>
        <link href="https://github.com/advisories/GHSA-262p-vjx5-45xh"/>
        <updated>2026-03-05T20:55:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-hj7x-879w-vrp7. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>An HTTP Request Smuggling vulnerability (CWE-444) has been found in Pingora's parsing of HTTP/1.0 and Transfer-Encoding requests. The issue occurs due to improperly allowing HTTP/1.0 request bodies to be close-delimited and incorrect handling of multiple Transfer-Encoding values, allowing attackers to send HTTP/1.0 requests in a way that would desync Pingora’s request framing from backend servers’.</p>
<p>Impact</p>
<p>This vulnerability primarily affects standalone Pingora deployments in front of certain backends that accept HTTP/1.0 requests. An attacker could craft a malicious payload following this request that Pingora forwards to the backend in order to:</p>
<ul>
<li><p>Bypass proxy-level ACL controls and WAF logic</p>
</li>
<li><p>Poison caches and upstream connections, causing subsequent requests from legitimate users to receive responses intended for smuggled requests</p>
</li>
<li><p>Perform cross-user attacks by hijacking sessions or smuggling requests that appear to originate from the trusted proxy IP</p>
</li>
</ul>
<p>Cloudflare's CDN infrastructure was not affected by this vulnerability, as its ingress proxy layers forwarded HTTP/1.1 requests only, rejected ambiguous framing such as invalid Content-Length values, and forwarded a single Transfer-Encoding: chunked header for chunked requests.</p>
<p>Mitigation:</p>
<p>Pingora users should upgrade to Pingora v0.8.0 or higher that fixes this issue by correctly parsing message length headers per RFC 9112 and strictly adhering to more RFC guidelines, including that HTTP request bodies are never close-delimited.</p>
<p>As a workaround, users can reject certain requests with an error in the request filter logic in order to stop processing bytes on the connection and disable downstream connection reuse. The user should reject any non-HTTP/1.1 request, or a request that has invalid Content-Length, multiple Transfer-Encoding headers, or Transfer-Encoding header that is not an exact “chunked” string match.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2835">https://nvd.nist.gov/vuln/detail/CVE-2026-2835</a></li>
<li><a href="https://github.com/cloudflare/pingora">https://github.com/cloudflare/pingora</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0034.html">https://rustsec.org/advisories/RUSTSEC-2026-0034.html</a></li>
<li><a href="https://github.com/advisories/GHSA-262p-vjx5-45xh">https://github.com/advisories/GHSA-262p-vjx5-45xh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-262p-vjx5-45xh</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-05T00:31:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pingora-core] Pingora vulnerable to HTTP Request Smuggling via Premature Upgrade]]></title>
        <id>https://github.com/advisories/GHSA-xq2h-p299-vjwv</id>
        <link href="https://github.com/advisories/GHSA-xq2h-p299-vjwv"/>
        <updated>2026-03-09T20:45:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Pingora versions prior to 0.8.0 would immediately forward bytes following a request with an Upgrade header to the backend, without waiting for a 101 Switching Protocols response. This allows an attacker to smuggle requests to the backend and bypass proxy-level security controls.</p>
<p>This vulnerability primarily affects standalone Pingora deployments where a Pingora proxy is exposed to external traffic. An attacker could exploit this to bypass proxy-level ACL controls and WAF logic, poison caches and upstream connections, or perform cross-user attacks by hijacking sessions.</p>
<p>Note: Cloudflare customers and Cloudflare's CDN infrastructure were not affected by this vulnerability, as ingress proxies in the CDN stack maintain proper HTTP parsing boundaries and do not prematurely switch to upgraded connection forwarding mode.</p>
<h3 id="patches">Patches</h3>
<p>Pingora users should upgrade to Pingora v0.8.0 or higher, which fixes this issue by only switching connection modes after receiving a 101 Switching Protocols response from the backend (hash 824bdeefc61e121cc8861de1b35e8e8f39026ecd). Without a 101 response, subsequent bytes continue to be parsed as HTTP requests.</p>
<h3 id="workarounds">Workarounds</h3>
<p>As a workaround, users may return an error on requests with the Upgrade header present in their request filter logic in order to stop processing bytes beyond the request header and disable downstream connection reuse.</p>
<h3 id="references">References</h3>
<p>See <a href="https://www.cve.org/cverecord?id=CVE-2026-2833">CVE-2026-2833</a> and the <a href="https://blog.cloudflare.com/pingora-oss-smuggling-vulnerabilities/">Cloudflare blog post</a> for more details.</p>
<h3 id="credits">Credits</h3>
<p>Disclosed responsibly by Rajat Raghav (@xclow3n) through the Cloudflare <a href="https://www.cloudflare.com/disclosure/">Bug Bounty Program</a>.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/cloudflare/pingora/security/advisories/GHSA-xq2h-p299-vjwv">https://github.com/cloudflare/pingora/security/advisories/GHSA-xq2h-p299-vjwv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2833">https://nvd.nist.gov/vuln/detail/CVE-2026-2833</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0033.html">https://rustsec.org/advisories/RUSTSEC-2026-0033.html</a></li>
<li><a href="https://github.com/advisories/GHSA-xq2h-p299-vjwv">https://github.com/advisories/GHSA-xq2h-p299-vjwv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xq2h-p299-vjwv</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-05T20:55:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pingora-core] Duplicate Advisory: HTTP Request Smuggling via Premature Upgrade]]></title>
        <id>https://github.com/advisories/GHSA-f9v3-j2m7-4hpg</id>
        <link href="https://github.com/advisories/GHSA-f9v3-j2m7-4hpg"/>
        <updated>2026-03-05T20:55:00.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-xq2h-p299-vjwv. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>An HTTP request smuggling vulnerability (CWE-444) was found in Pingora's handling of HTTP/1.1 connection upgrades. The issue occurs when a Pingora proxy reads a request containing an Upgrade header, causing the proxy to pass through the rest of the bytes on the connection to a backend before the backend has accepted the upgrade. An attacker can thus directly forward a malicious payload after a request with an Upgrade header to that backend in a way that may be interpreted as a subsequent request header, bypassing proxy-level security controls and enabling cross-user session hijacking.</p>
<p>Impact</p>
<p>This vulnerability primarily affects standalone Pingora deployments where a Pingora proxy is exposed to external traffic. An attacker could exploit this to:</p>
<ul>
<li><p>Bypass proxy-level ACL controls and WAF logic</p>
</li>
<li><p>Poison caches and upstream connections, causing subsequent requests from legitimate users to receive responses intended for smuggled requests</p>
</li>
<li><p>Perform cross-user attacks by hijacking sessions or smuggling requests that appear to originate from the trusted proxy IP</p>
</li>
</ul>
<p>Cloudflare's CDN infrastructure was not affected by this vulnerability, as ingress proxies in the CDN stack maintain proper HTTP parsing boundaries and do not prematurely switch to upgraded connection forwarding mode.</p>
<p>Mitigation:</p>
<p>Pingora users should upgrade to Pingora v0.8.0 or higher</p>
<p>As a workaround, users may return an error on requests with the Upgrade header present in their request filter logic in order to stop processing bytes beyond the request header and disable downstream connection reuse.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2833">https://nvd.nist.gov/vuln/detail/CVE-2026-2833</a></li>
<li><a href="https://github.com/cloudflare/pingora">https://github.com/cloudflare/pingora</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0033.html">https://rustsec.org/advisories/RUSTSEC-2026-0033.html</a></li>
<li><a href="https://github.com/advisories/GHSA-f9v3-j2m7-4hpg">https://github.com/advisories/GHSA-f9v3-j2m7-4hpg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f9v3-j2m7-4hpg</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-05T00:31:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[stellar-xdr] stellar-xdr's StringM::from_str bypasses max length validation]]></title>
        <id>https://github.com/advisories/GHSA-x57h-xx53-v53w</id>
        <link href="https://github.com/advisories/GHSA-x57h-xx53-v53w"/>
        <updated>2026-03-06T22:51:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>StringM::from_str</code> does not validate that the input length is within the declared maximum (<code>MAX</code>). Calling <code>StringM::&lt;N&gt;::from_str(s)</code> where <code>s</code> is longer than <code>N</code> bytes succeeds and returns an <code>Ok</code> value instead of <code>Err(Error::LengthExceedsMax)</code>, producing a <code>StringM</code> that violates its length invariant.</p>
<p>This affects any code that constructs <code>StringM</code> values from string input using <code>FromStr</code> (including <code>str::parse</code>), and relies on the type's maximum length constraint being enforced. An oversized <code>StringM</code> could propagate through serialization, validation, or other logic that assumes the invariant holds.</p>
<p>All published versions of the <code>stellar-xdr</code> crate up to and including <code>v25.0.0</code> are affected.</p>
<h3 id="patches">Patches</h3>
<p>The fix is merged in <a href="https://github.com/stellar/rs-stellar-xdr/pull/500">#500</a>. It replaces the direct <code>Ok(Self(b))</code> construction with <code>b.try_into()</code>, which routes through <code>TryFrom&lt;Vec&lt;u8&gt;&gt;</code> and properly validates the length — matching the pattern already used by <code>BytesM::from_str</code>.</p>
<p>Users should upgrade to the first release containing this fix once published (the next release after <code>v25.0.0</code>).</p>
<h3 id="workarounds">Workarounds</h3>
<p>Validate the byte length of string input before calling <code>StringM::from_str</code>, or construct <code>StringM</code> values via <code>StringM::try_from(s.as_bytes().to_vec())</code> which correctly enforces the length constraint.</p>
<h3 id="references">References</h3>
<ul>
<li>Issue: <a href="https://github.com/stellar/rs-stellar-xdr/issues/499">https://github.com/stellar/rs-stellar-xdr/issues/499</a></li>
<li>Fix: <a href="https://github.com/stellar/rs-stellar-xdr/pull/500">https://github.com/stellar/rs-stellar-xdr/pull/500</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/stellar/rs-stellar-xdr/security/advisories/GHSA-x57h-xx53-v53w">https://github.com/stellar/rs-stellar-xdr/security/advisories/GHSA-x57h-xx53-v53w</a></li>
<li><a href="https://github.com/stellar/rs-stellar-xdr/issues/499">https://github.com/stellar/rs-stellar-xdr/issues/499</a></li>
<li><a href="https://github.com/stellar/rs-stellar-xdr/pull/500">https://github.com/stellar/rs-stellar-xdr/pull/500</a></li>
<li><a href="https://github.com/stellar/rs-stellar-xdr/commit/1f840013c3e2fca0321fb844b048afa01d10dda6">https://github.com/stellar/rs-stellar-xdr/commit/1f840013c3e2fca0321fb844b048afa01d10dda6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-29795">https://nvd.nist.gov/vuln/detail/CVE-2026-29795</a></li>
<li><a href="https://github.com/advisories/GHSA-x57h-xx53-v53w">https://github.com/advisories/GHSA-x57h-xx53-v53w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x57h-xx53-v53w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-05T20:45:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dnp3times] `dnp3times` was removed from crates.io due to malicious code]]></title>
        <id>https://github.com/advisories/GHSA-xhw7-jhmp-j62j</id>
        <link href="https://github.com/advisories/GHSA-xhw7-jhmp-j62j"/>
        <updated>2026-03-05T00:43:57.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>dnp3times</code> crate attempted to exfiltrate <code>.env</code> files to a server that was in turn impersonating the legitimate <code>timeapi.io</code> service. It was loosely trying to typosquat the <code>dnp3time</code> crate, but otherwise was the same attack as the recent <code>time_calibrator</code> and <code>time_calibrators</code> malware.</p>
<p>The malicious crate had 1 version published on 2026-03-04 approximately 6 hours before removal and had no evidence of actual downloads. There were no crates depending on this crate on crates.io.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0032.html">https://rustsec.org/advisories/RUSTSEC-2026-0032.html</a></li>
<li><a href="https://github.com/advisories/GHSA-xhw7-jhmp-j62j">https://github.com/advisories/GHSA-xhw7-jhmp-j62j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xhw7-jhmp-j62j</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-05T00:43:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zeptoclaw] zeptoclaw has Shell allowlist-blocklist bypass via command/argument injection and file name wildcards]]></title>
        <id>https://github.com/advisories/GHSA-5wp8-q9mx-8jx8</id>
        <link href="https://github.com/advisories/GHSA-5wp8-q9mx-8jx8"/>
        <updated>2026-03-05T00:38:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><a href="https://github.com/qhkm/zeptoclaw">zeptoclaw</a> implements a allowlist combined with a blocklist to prevent malicious shell commands in <a href="https://github.com/qhkm/zeptoclaw/blob/v0.5.8/src/security/shell.rs">src/security/shell.rs</a>. However, even in the <code>Strict</code> mode, attackers can completely bypass all the guards from allowlist and blocklist:</p>
<ul>
<li>to bypass the <code>allowlist</code>, command injection is enough, such as <code>;</code>, <code>$()</code> etc.</li>
<li>to bypass the <code>REGEX_BLOCKED_PATTERNS</code>, argument injection is enough, such as the <code>python3 -P -c "..."</code></li>
<li>to bypass the <code>LITERAL_BLOCKED_PATTERNS</code>, file name wildcards can do the work, such as <code>cat /etc/pass[w]d</code></li>
</ul>
<h3 id="details">Details</h3>
<p>In code <a href="https://github.com/qhkm/zeptoclaw/blob/fe2ef07cfec5bb46b42cdd65f52b9230c03e9270/src/security/shell.rs#L218-L243">src/security/shell.rs#L218-L243</a>, one can see the allowlist only checks the first token and thus makes command injection possible. </p>
<pre><code class="language-rust">        // Allowlist check (runs after blocklist)
        if self.allowlist_mode != ShellAllowlistMode::Off &amp;&amp; !self.allowlist.is_empty() {
            let first_token = command
                .split_whitespace()
                .next()
                .unwrap_or("")
                .to_lowercase();
            // Strip path prefix (e.g. /usr/bin/git -&gt; git)
            let executable = first_token.rsplit('/').next().unwrap_or(&amp;first_token);
            if !self.allowlist.iter().any(|a| a == executable) {
                match self.allowlist_mode {
                    ShellAllowlistMode::Strict =&gt; {
                        return Err(ZeptoError::SecurityViolation(format!(
                            "Command '{}' not in allowlist",
                            executable
                        )));
                    }
                    ShellAllowlistMode::Warn =&gt; {
                        tracing::warn!(
                            command = %command,
                            executable = %executable,
                            "Command not in allowlist"
                        );
                    }
                    ShellAllowlistMode::Off =&gt; {} // unreachable
                }
</code></pre>
<p> <code>!self.allowlist.is_empty()</code> makes the empty allowlist overlook the allowlist check, if it is in <code>ShellAllowlistMode::Strict</code> mode, empty allowlist should direct reject all the commands.</p>
<p>As the code in <a href="https://github.com/qhkm/zeptoclaw/blob/fe2ef07cfec5bb46b42cdd65f52b9230c03e9270/src/security/shell.rs#L18-L70">src/security/shell.rs#L18-L70</a>, we can find the <code>REGEX_BLOCKED_PATTERNS</code> only apply <code>\s+</code> in between the command and arguments, making argument injection possible, and the <code>LITERAL_BLOCKED_PATTERNS</code> just uses specific file name, totally overlooking the file name wildcards:</p>
<pre><code class="language-rust">const REGEX_BLOCKED_PATTERNS: &amp;[&amp;str] = &amp;[
    // Piped shell execution (curl/wget to sh/bash)
    r"curl\s+.*\|\s*(sh|bash|zsh)",
    r"wget\s+.*\|\s*(sh|bash|zsh)",
    r"\|\s*(sh|bash|zsh)\s*$",
    // Reverse shells
    r"bash\s+-i\s+&gt;&amp;\s*/dev/tcp",
    r"nc\s+.*-e\s+(sh|bash|/bin)",
    r"/dev/tcp/",
    r"/dev/udp/",
    // Destructive root operations (various flag orderings)
    r"rm\s+(-[rf]{1,2}\s+)*(-[rf]{1,2}\s+)*/\s*($|;|\||&amp;)",
    r"rm\s+(-[rf]{1,2}\s+)*(-[rf]{1,2}\s+)*/\*\s*($|;|\||&amp;)",
    // Format/overwrite disk
    r"mkfs(\.[a-z0-9]+)?\s",
    r"dd\s+.*if=/dev/(zero|random|urandom).*of=/dev/[sh]d",
    r"&gt;\s*/dev/[sh]d[a-z]",
    // System-wide permission changes
    r"chmod\s+(-R\s+)?777\s+/\s*$",
    r"chmod\s+(-R\s+)?777\s+/[a-z]",
    // Fork bombs
    r":\(\)\s*\{\s*:\|:&amp;\s*\}\s*;:",
    r"fork\s*\(\s*\)",
    // Encoded/indirect execution (common blocklist bypasses)
    r"base64\s+(-d|--decode)",
    r"python[23]?\s+-c\s+",
    r"perl\s+-e\s+",
    r"ruby\s+-e\s+",
    r"node\s+-e\s+",
    r"\beval\s+",
    r"xargs\s+.*sh\b",
    r"xargs\s+.*bash\b",
    // Environment variable exfiltration
    r"\benv\b.*&gt;\s*/",
    r"\bprintenv\b.*&gt;\s*/",
];

/// Literal substring patterns (credentials, sensitive paths)
const LITERAL_BLOCKED_PATTERNS: &amp;[&amp;str] = &amp;[
    "/etc/shadow",
    "/etc/passwd",
    "~/.ssh/",
    ".ssh/id_rsa",
    ".ssh/id_ed25519",
    ".ssh/id_ecdsa",
    ".ssh/id_dsa",
    ".ssh/authorized_keys",
    ".aws/credentials",
    ".kube/config",
    // ZeptoClaw's own config (contains API keys and channel tokens)
    ".zeptoclaw/config.json",
    ".zeptoclaw/config.yaml",
];
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-rust">    #[test]
    fn test_allowlist_bypass() {
        let config =
            ShellSecurityConfig::new().with_allowlist(vec!["git"], ShellAllowlistMode::Strict);
        assert!(config.validate_command("/usr/bin/git status; python -P -c 'import os; os.system(\"rm -rf /\")'; cat /etc/pass[w]d").is_ok());
    }
</code></pre>
<h3 id="impact">Impact</h3>
<p>Unauthorized command execution.</p>
<h3 id="credit">Credit</h3>
<p><a href="https://github.com/zpbrent">@zpbrent</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/qhkm/zeptoclaw/security/advisories/GHSA-5wp8-q9mx-8jx8">https://github.com/qhkm/zeptoclaw/security/advisories/GHSA-5wp8-q9mx-8jx8</a></li>
<li><a href="https://github.com/qhkm/zeptoclaw/commit/68916c3e4f3af107f11940b27854fc7ef517058b">https://github.com/qhkm/zeptoclaw/commit/68916c3e4f3af107f11940b27854fc7ef517058b</a></li>
<li><a href="https://github.com/qhkm/zeptoclaw/blob/fe2ef07cfec5bb46b42cdd65f52b9230c03e9270/src/security/shell.rs#L218-L243">https://github.com/qhkm/zeptoclaw/blob/fe2ef07cfec5bb46b42cdd65f52b9230c03e9270/src/security/shell.rs#L218-L243</a></li>
<li><a href="https://github.com/advisories/GHSA-5wp8-q9mx-8jx8">https://github.com/advisories/GHSA-5wp8-q9mx-8jx8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5wp8-q9mx-8jx8</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-05T00:38:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zeptoclaw] zeptoclaw has Android device shell blocklist bypass via argument permutation]]></title>
        <id>https://github.com/advisories/GHSA-hhjv-jq77-cmvx</id>
        <link href="https://github.com/advisories/GHSA-hhjv-jq77-cmvx"/>
        <updated>2026-03-05T00:35:31.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><a href="https://github.com/qhkm/zeptoclaw">zeptoclaw</a> implements a <a href="https://github.com/qhkm/zeptoclaw/blob/fe2ef07cfec5bb46b42cdd65f52b9230c03e9270/src/tools/android/actions.rs#L413-L424">blocklist</a> to prevent dangerous commands running in android device shell, but this blocklist has several blocked commands with argements in the pattern literal, such as <code>rm -f</code> and <code>rm -rf</code>, this can be simply bypassed by using different orders for these arguments, such as <code>rm -r -f</code> or <code>rm -fr</code> etc.</p>
<h3 id="details">Details</h3>
<p>As in code <a href="https://github.com/qhkm/zeptoclaw/blob/fe2ef07cfec5bb46b42cdd65f52b9230c03e9270/src/tools/android/actions.rs#L413-L424">src/tools/android/actions.rs#L413-L424</a>, we can see the <code>rm -f</code> and <code>rm -rf</code> are hard coded and thus can be simply bypassed via <code>rm -r -f</code> or <code>rm -fr</code> etc.</p>
<pre><code class="language-rust">pub async fn device_shell(adb: &amp;AdbExecutor, cmd: &amp;str) -&gt; Result&lt;String&gt; {
    // Normalize whitespace for blocklist check
    let normalized: String = cmd.split_whitespace().collect::&lt;Vec&lt;_&gt;&gt;().join(" ");
    let lower = normalized.to_lowercase();

    let blocked = [
        "rm -rf",
        "rm -r",
        "reboot",
        "factory_reset",
        "wipe",
        "format",
        "dd if=",
        "mkfs",
        "flash",
        "fastboot",
    ];
    for pattern in &amp;blocked {
        if lower.contains(pattern) {
            return Err(ZeptoError::Tool(format!(
                "Blocked dangerous command containing '{}'",
                pattern
            )));
        }
    }
</code></pre>
<h3 id="poc">PoC</h3>
<p>Set up <a href="https://github.com/qhkm/zeptoclaw">zeptoclaw</a> with an Android tool and then run the command <code>rm -f -r</code> etc.</p>
<h3 id="impact">Impact</h3>
<p>Unauthorized command executed in Android device.</p>
<h3 id="credit">Credit</h3>
<p><a href="https://github.com/zpbrent">@zpbrent</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/qhkm/zeptoclaw/security/advisories/GHSA-hhjv-jq77-cmvx">https://github.com/qhkm/zeptoclaw/security/advisories/GHSA-hhjv-jq77-cmvx</a></li>
<li><a href="https://github.com/qhkm/zeptoclaw/commit/68916c3e4f3af107f11940b27854fc7ef517058b">https://github.com/qhkm/zeptoclaw/commit/68916c3e4f3af107f11940b27854fc7ef517058b</a></li>
<li><a href="https://github.com/qhkm/zeptoclaw/blob/fe2ef07cfec5bb46b42cdd65f52b9230c03e9270/src/tools/android/actions.rs#L413-L424">https://github.com/qhkm/zeptoclaw/blob/fe2ef07cfec5bb46b42cdd65f52b9230c03e9270/src/tools/android/actions.rs#L413-L424</a></li>
<li><a href="https://github.com/advisories/GHSA-hhjv-jq77-cmvx">https://github.com/advisories/GHSA-hhjv-jq77-cmvx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hhjv-jq77-cmvx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-05T00:35:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[time_calibrators] `time_calibrators` was removed from crates.io due to malicious code]]></title>
        <id>https://github.com/advisories/GHSA-wf45-3gpw-vrqv</id>
        <link href="https://github.com/advisories/GHSA-wf45-3gpw-vrqv"/>
        <updated>2026-03-04T21:07:41.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>time_calibrators</code> crate attempted to exfiltrate <code>.env</code> files to a server that was in turn impersonating the legitimate <code>timeapi.io</code> service.</p>
<p>The malicious crate had 1 version published on 2026-03-03 approximately 3 hours before removal and had no evidence of actual downloads. There were no crates depending on this crate on crates.io.</p>
<p>Rust security response working group thanks cybergeek for finding and reporting this, and thanks to Emily Albini for co-ordinating with the crates.io team.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0031.html">https://rustsec.org/advisories/RUSTSEC-2026-0031.html</a></li>
<li><a href="https://github.com/advisories/GHSA-wf45-3gpw-vrqv">https://github.com/advisories/GHSA-wf45-3gpw-vrqv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wf45-3gpw-vrqv</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-04T21:07:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lemmy_routes] Lemmy has unauthenticated SSRF via file_type query parameter injection in image endpoint]]></title>
        <id>https://github.com/advisories/GHSA-jvxv-2jjp-jxc3</id>
        <link href="https://github.com/advisories/GHSA-jvxv-2jjp-jxc3"/>
        <updated>2026-03-06T22:44:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>GET /api/v4/image/{filename}</code> endpoint is vulnerable to unauthenticated SSRF through parameter injection in the <code>file_type</code> query parameter. An attacker can inject arbitrary query parameters into the internal request to pict-rs, including the <code>proxy</code> parameter which causes pict-rs to fetch arbitrary URLs.</p>
<h2 id="affected-code">Affected code</h2>
<p><code>crates/routes/src/images/download.rs</code>, lines 17-40 (<code>get_image</code> function):</p>
<pre><code class="language-rust">pub async fn get_image(
  filename: Path&lt;String&gt;,
  Query(params): Query&lt;ImageGetParams&gt;,
  req: HttpRequest,
  context: Data&lt;LemmyContext&gt;,
) -&gt; LemmyResult&lt;HttpResponse&gt; {
  let name = &amp;filename.into_inner();
  let pictrs_url = context.settings().pictrs()?.url;
  let processed_url = if params.file_type.is_none() &amp;&amp; params.max_size.is_none() {
    format!("{}image/original/{}", pictrs_url, name)
  } else {
    let file_type = file_type(params.file_type, name);
    let mut url = format!("{}image/process.{}?src={}", pictrs_url, file_type, name);
    // ...
  };
  do_get_image(processed_url, req, &amp;context).await
}
</code></pre>
<p>The <code>file_type</code> parameter (<code>ImageGetParams.file_type: Option&lt;String&gt;</code>) is directly interpolated into the URL string without any validation or encoding. Since pict-rs's <code>/image/process.{ext}</code> endpoint supports a <code>?proxy={url}</code> parameter for fetching remote images, an attacker can inject <code>?proxy=...</code> via <code>file_type</code> to make pict-rs fetch arbitrary URLs.</p>
<p>This endpoint does not require authentication (no <code>LocalUserView</code> extractor).</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-bash"># Basic SSRF - make pict-rs fetch AWS metadata endpoint
# The file_type value is: jpg?proxy=http://169.254.169.254/latest/meta-data&amp;x=
# This constructs: http://pictrs:8080/image/process.jpg?proxy=http://169.254.169.254/latest/meta-data&amp;x=?src=anything

curl -v 'https://TARGET/api/v4/image/anything?file_type=jpg%3Fproxy%3Dhttp%3A%2F%2F169.254.169.254%2Flatest%2Fmeta-data%26x%3D'

# Scan internal services on the Docker network
curl -v 'https://TARGET/api/v4/image/anything?file_type=jpg%3Fproxy%3Dhttp%3A%2F%2Flemmy%3A8536%2Fapi%2Fv4%2Fsite%26x%3D'

# The same issue exists in the image_proxy endpoint, but it requires the
# proxy URL to exist in the remote_image table (RemoteImage::validate check),
# making it harder to exploit.
</code></pre>
<p>The response from the internal URL is streamed back to the attacker through pict-rs and Lemmy.</p>
<h2 id="impact">Impact</h2>
<p>An unauthenticated attacker can:</p>
<ul>
<li>Access cloud metadata services (AWS/GCP/Azure instance metadata) from the pict-rs service</li>
<li>Scan and interact with internal services on the Docker network (pict-rs is typically co-located with Lemmy, PostgreSQL, etc.)</li>
<li>Bypass the <code>RemoteImage::validate()</code> check that protects the <code>image_proxy</code> endpoint</li>
</ul>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Validate the <code>file_type</code> parameter to only allow alphanumeric characters:</p>
<pre><code class="language-rust">fn file_type(file_type: Option&lt;String&gt;, name: &amp;str) -&gt; String {
  let ft = file_type
    .unwrap_or_else(|| name.split('.').next_back().unwrap_or("jpg").to_string());
  if ft.chars().all(|c| c.is_alphanumeric()) {
    ft
  } else {
    "jpg".to_string()
  }
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/LemmyNet/lemmy/security/advisories/GHSA-jvxv-2jjp-jxc3">https://github.com/LemmyNet/lemmy/security/advisories/GHSA-jvxv-2jjp-jxc3</a></li>
<li><a href="https://github.com/LemmyNet/lemmy/commit/f47a03f56d1797bceab5f34b6f624c91cecd5871">https://github.com/LemmyNet/lemmy/commit/f47a03f56d1797bceab5f34b6f624c91cecd5871</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-29178">https://nvd.nist.gov/vuln/detail/CVE-2026-29178</a></li>
<li><a href="https://github.com/advisories/GHSA-jvxv-2jjp-jxc3">https://github.com/advisories/GHSA-jvxv-2jjp-jxc3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jvxv-2jjp-jxc3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-04T20:55:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[time_calibrator] `time_calibrator` was removed from crates.io due to malicious code]]></title>
        <id>https://github.com/advisories/GHSA-77xj-rrh3-wx3v</id>
        <link href="https://github.com/advisories/GHSA-77xj-rrh3-wx3v"/>
        <updated>2026-03-04T20:44:35.000Z</updated>
        <content type="html"><![CDATA[<p>It was reported <code>time_calibrator</code> contained malicious code, that would try to upload <code>.env</code> files to a server.</p>
<p>The malicious crate had only 1 version published at 2026-02-28 and no evidence of actual usage. The crate was removed from crates.io and the user account was locked. There were no crates depending on this crate on crates.io.</p>
<p>Rust security response working group thanks Gabriel Silva for finding and reporting this, and thanks to Emily Albini for co-ordinating with the crates.io and infra-admin teams.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0030.html">https://rustsec.org/advisories/RUSTSEC-2026-0030.html</a></li>
<li><a href="https://github.com/advisories/GHSA-77xj-rrh3-wx3v">https://github.com/advisories/GHSA-77xj-rrh3-wx3v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-77xj-rrh3-wx3v</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-04T20:44:35.000Z</published>
    </entry>
</feed>