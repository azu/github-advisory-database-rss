<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-04-22T15:25:00.829Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[actix-http] actix-http has HTTP/1.1 CL.TE Request Smuggling ]]></title>
        <id>https://github.com/advisories/GHSA-xhj4-vrgc-hr34</id>
        <link href="https://github.com/advisories/GHSA-xhj4-vrgc-hr34"/>
        <updated>2026-04-22T14:37:31.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability in <code>actix-http</code>'s HTTP/1.1 request parser allows an unauthenticated remote client to smuggle requests in deployments where a front-end HTTP intermediary and the Actix backend disagree about whether <code>Content-Length</code> or <code>Transfer-Encoding: chunked</code> defines the request body length.</p>
<h2 id="severity">Severity</h2>
<p><strong>Medium</strong>.
This is an HTTP request smuggling vulnerability that can be triggered over the network without application-level credentials. Exploitation requires a specific proxy topology: an upstream proxy, WAF, load balancer, or similar intermediary must use <code>Content-Length</code> framing while forwarding the conflicting <code>Transfer-Encoding: chunked</code> request to an Actix backend over a reused HTTP/1.1 connection.</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li><code>actix-http</code>: versions up to and including <strong>3.12.0</strong></li>
</ul>
<h2 id="description">Description</h2>
<p>HTTP/1.1 requests that contain both <code>Content-Length</code> and <code>Transfer-Encoding: chunked</code> are ambiguous and must be rejected by recipients to avoid request smuggling.</p>
<p>Affected versions of <code>actix-http</code> accepted a request with a syntactically valid <code>Content-Length</code> header and <code>Transfer-Encoding: chunked</code> on the same HTTP/1.1 message. The parser then selected chunked decoding instead of rejecting the conflicting framing signals.</p>
<p>In a CL.TE proxy topology, an intermediary may treat bytes after the declared <code>Content-Length</code> body as part of the first request, while the Actix backend stops at the terminating chunk marker and parses the remaining bytes on the backend connection as a second HTTP request. This creates a backend-side request desynchronization primitive.</p>
<p>The issue is limited to HTTP/1.1 request parsing.</p>
<h2 id="impact">Impact</h2>
<p><strong>HTTP request smuggling</strong></p>
<ul>
<li><strong>Attack Vector:</strong> Network, unauthenticated.</li>
<li><strong>Effect:</strong> Backend request desynchronization with low integrity impact to requests processed by the vulnerable Actix service.</li>
<li><strong>Scope:</strong> Actix services using affected <code>actix-http</code> versions behind an HTTP/1.1 intermediary that forwards ambiguous <code>Content-Length</code> plus <code>Transfer-Encoding: chunked</code> requests and reuses backend connections.</li>
</ul>
<p>No direct confidentiality, availability, or subsequent-system impact is scored for this advisory.</p>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in <strong>actix-http 3.12.1</strong>.</p>
<p>The fix rejects HTTP/1.1 requests that contain both <code>Content-Length</code> and <code>Transfer-Encoding: chunked</code> instead of choosing one framing interpretation.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to <strong>actix-http 3.12.1</strong> or later.</p>
<p>Applications that depend on <code>actix-http</code> through <code>actix-web</code>, <code>awc</code>, or another Actix crate should ensure dependency resolution selects <code>actix-http</code> 3.12.1 or later. For example:</p>
<pre><code class="language-bash">cargo update -p actix-http
</code></pre>
<p>If an immediate upgrade is not possible, configure all upstream HTTP intermediaries to reject HTTP/1.1 requests that contain both <code>Content-Length</code> and <code>Transfer-Encoding</code>, and avoid forwarding ambiguous request framing to Actix backends.</p>
<h2 id="credits">Credits</h2>
<p>Actix thanks <a href="https://github.com/mufeedvh">mufeedvh</a> who disclosed this issue through coordinated disclosure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/actix/actix-web/security/advisories/GHSA-xhj4-vrgc-hr34">https://github.com/actix/actix-web/security/advisories/GHSA-xhj4-vrgc-hr34</a></li>
<li><a href="https://github.com/actix/actix-web/releases/tag/http-v3.12.1">https://github.com/actix/actix-web/releases/tag/http-v3.12.1</a></li>
<li><a href="https://www.rfc-editor.org/rfc/rfc9112.html#name-message-body-length">https://www.rfc-editor.org/rfc/rfc9112.html#name-message-body-length</a></li>
<li><a href="https://github.com/advisories/GHSA-xhj4-vrgc-hr34">https://github.com/advisories/GHSA-xhj4-vrgc-hr34</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xhj4-vrgc-hr34</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T14:37:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[brillig] Brillig: Heap corruption in foreign call results with nested tuple arrays]]></title>
        <id>https://github.com/advisories/GHSA-jj7c-x25r-r8r3</id>
        <link href="https://github.com/advisories/GHSA-jj7c-x25r-r8r3"/>
        <updated>2026-04-21T20:16:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<p>Noir programs can invoke external functions through foreign calls. When compiling to Brillig bytecode, the SSA instructions are processed block-by-block in <code>BrilligBlock::compile_block()</code>. When the compiler encounters an <code>Instruction::Call</code> with a <code>Value::ForeignFunction</code> target, it invokes <code>codegen_call()</code> in <code>brillig_call/code_gen_call.rs</code>, which dispatches to <code>convert_ssa_foreign_call()</code>.</p>
<p>Before emitting the foreign call opcode, the compiler must pre-allocate memory for any array results the call will return. This happens through <code>allocate_external_call_results()</code>, which iterates over the result types. For <code>Type::Array</code> results, it delegates to <code>allocate_foreign_call_result_array()</code> to recursively allocate memory on the heap for nested arrays.</p>
<p>The <code>BrilligArray</code> struct is the internal representation of a Noir array in Brillig IR. Its <code>size</code> field represents the <strong>semi-flattened size</strong>, the total number of memory slots the array occupies, accounting for the fact that composite types like tuples consume multiple slots per element. This size is computed by <code>compute_array_length()</code> in <code>brillig_block_variables.rs</code>:</p>
<pre><code class="language-rust">pub(crate) fn compute_array_length(item_typ: &amp;CompositeType, elem_count: usize) -&gt; usize {
    item_typ.len() * elem_count
}
</code></pre>
<p>For the <strong>outer</strong> array, <code>allocate_external_call_results()</code> correctly uses <code>define_variable()</code>, which internally calls <code>allocate_value_with_type()</code>. This function applies the formula above, producing the correct semi-flattened size.</p>
<p>However, for <strong>nested</strong> arrays, <code>allocate_foreign_call_result_array()</code> contains a bug. When it encounters a nested <code>Type::Array(types, nested_size)</code>, it calls:</p>
<pre><code class="language-rust">Type::Array(_, nested_size) =&gt; { 
    let inner_array = self.brillig_context.allocate_brillig_array(*nested_size as usize);
    // ....
}
</code></pre>
<p>The pattern <code>Type::Array(_, nested_size)</code> discards the inner types with <code>_</code> and uses only <code>nested_size</code>, the <strong>semantic length</strong> of the nested array (the number of logical elements), not the semi-flattened size. For simple element types this works correctly, but for composite element types it under-allocates. Consider a nested array of type <code>[(u32, u32); 3]</code>:</p>
<ul>
<li>Semantic length: 3 (three tuples)</li>
<li>Element size: 2 (each tuple has two fields)</li>
<li>Required semi-flattened size: 6 memory slots</li>
</ul>
<p>The current code passes <code>3</code> to <code>allocate_brillig_array()</code>, which then calls <code>codegen_initialize_array()</code>. This function allocates <code>array.size + ARRAY_META_COUNT</code> slots, only 4 slots instead of the required 7 (6 data + 1 metadata). When the VM executes the foreign call and writes 6 values plus metadata, it overwrites adjacent heap memory.</p>
<h2 id="impact">Impact</h2>
<p>Foreign calls returning nested arrays of tuples or other composite types corrupt the Brillig VM heap.</p>
<h2 id="recommendation">Recommendation</h2>
<p>Multiply the semantic length by the number of element types when allocating nested arrays. Extract the inner types from the pattern and replace the <code>nested_size</code> argument to <code>allocate_brillig_array()</code> with <code>types.len() * nested_size</code> to compute the semi-flattened size. Alternatively, reuse the existing <code>compute_array_length()</code> helper function to maintain consistency with outer array allocation.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/noir-lang/noir/security/advisories/GHSA-jj7c-x25r-r8r3">https://github.com/noir-lang/noir/security/advisories/GHSA-jj7c-x25r-r8r3</a></li>
<li><a href="https://github.com/noir-lang/noir/releases/tag/v1.0.0-beta.19">https://github.com/noir-lang/noir/releases/tag/v1.0.0-beta.19</a></li>
<li><a href="https://github.com/advisories/GHSA-jj7c-x25r-r8r3">https://github.com/advisories/GHSA-jj7c-x25r-r8r3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jj7c-x25r-r8r3</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-21T20:16:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zebra-script] Zebra Vulnerable to Consensus Divergence in Transparent Sighash Hash-Type Handling]]></title>
        <id>https://github.com/advisories/GHSA-8m29-fpq5-89jj</id>
        <link href="https://github.com/advisories/GHSA-8m29-fpq5-89jj"/>
        <updated>2026-04-18T01:15:25.000Z</updated>
        <content type="html"><![CDATA[<h1 id="consensus-divergence-in-transparent-sighash-hash-type-handling">Consensus Divergence in Transparent Sighash Hash-Type Handling</h1>
<h2 id="summary">Summary</h2>
<p>After a refactoring, Zebra failed to validate a consensus rule that restricted the possible values of sighash hash types for V5 transactions which were enabled in the NU5 network upgrade. Zebra nodes could thus accept and eventually mine a block that would be considered invalid by zcashd nodes, creating a consensus split between Zebra and zcashd nodes.</p>
<p>In a similar vein, for V4 transactions, Zebra mistakenly used the "canonical" hash type when computing the sighash while zcashd (correctly per the spec) uses the raw value, which could also crate a consensus split.</p>
<h2 id="severity">Severity</h2>
<p><strong>Critical</strong> - This is a Consensus Vulnerability that could allow a malicious party to induce network partitioning, service disruption, and potential double-spend attacks against affected nodes.</p>
<p>Note that the impact is currently alleviated by the fact that currently most miners run <code>zcashd</code>.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>All Zebra versions prior to version 4.3.1. (Some older versions are not impacted but are no longer supported by the network.)</p>
<h2 id="description">Description</h2>
<p>Verification of transparent transactions inherits the Bitcoin Script verification code in C++. Since it is consensus-critical, this code was called from Zebra through foreign function interface (FFI). That interface was clunky because it required parsing the whole transaction in C++ code, which would then pull Rust libraries which could get in conflict with Zebra code. A refactoring was done so that only the verification itself was done in C++, and the rest done by Rust code, using a callback. However, in this refactoring, it was not noticed that a particular consensus rule - only accepting known hash types in transparent transaction signatures - was being enforced in C++ code and thus had to be enforced by the Rust caller.</p>
<p>An attacker could exploit this by:</p>
<ul>
<li>Submitting a V4 or V5 transaction with an invalid hash type</li>
<li>The V5 transaction would be accepted by Zebra nodes but not by <code>zcashd</code> nodes (and vice-versa for V4), creating a consensus split in the network.</li>
</ul>
<h2 id="impact">Impact</h2>
<p><strong>Consensus Failure</strong></p>
<ul>
<li><strong>Attack Vector:</strong> Network.</li>
<li><strong>Effect:</strong> Network partition/consensus split.</li>
<li><strong>Scope:</strong> Any Zebra affected Zebra node</li>
</ul>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in Zebra 4.3.1.</p>
<p>The fix adds the consensus check in the caller of the C++ verification code. It also uses the raw hash type for V4 sighash computations.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to Zebra 4.3.1 or later immediately.</p>
<p>There are no known workarounds for this issue. Immediate upgrade is the only way to ensure the node remains on the correct consensus path and is protected against malicious chain forks.</p>
<h2 id="credits">Credits</h2>
<p>Thanks Alex “Scalar” Sol for finding and reporting the issue, and to @sangsoo-osec who independently found the same issue and demonstrated that the V4 variant was also exploitable.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-8m29-fpq5-89jj">https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-8m29-fpq5-89jj</a></li>
<li><a href="https://github.com/advisories/GHSA-8m29-fpq5-89jj">https://github.com/advisories/GHSA-8m29-fpq5-89jj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8m29-fpq5-89jj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-18T01:15:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zebrad] Zebra Vulnerable to Consensus Divergence in Transparent Sighash Hash-Type Handling]]></title>
        <id>https://github.com/advisories/GHSA-8m29-fpq5-89jj</id>
        <link href="https://github.com/advisories/GHSA-8m29-fpq5-89jj"/>
        <updated>2026-04-18T01:15:25.000Z</updated>
        <content type="html"><![CDATA[<h1 id="consensus-divergence-in-transparent-sighash-hash-type-handling">Consensus Divergence in Transparent Sighash Hash-Type Handling</h1>
<h2 id="summary">Summary</h2>
<p>After a refactoring, Zebra failed to validate a consensus rule that restricted the possible values of sighash hash types for V5 transactions which were enabled in the NU5 network upgrade. Zebra nodes could thus accept and eventually mine a block that would be considered invalid by zcashd nodes, creating a consensus split between Zebra and zcashd nodes.</p>
<p>In a similar vein, for V4 transactions, Zebra mistakenly used the "canonical" hash type when computing the sighash while zcashd (correctly per the spec) uses the raw value, which could also crate a consensus split.</p>
<h2 id="severity">Severity</h2>
<p><strong>Critical</strong> - This is a Consensus Vulnerability that could allow a malicious party to induce network partitioning, service disruption, and potential double-spend attacks against affected nodes.</p>
<p>Note that the impact is currently alleviated by the fact that currently most miners run <code>zcashd</code>.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>All Zebra versions prior to version 4.3.1. (Some older versions are not impacted but are no longer supported by the network.)</p>
<h2 id="description">Description</h2>
<p>Verification of transparent transactions inherits the Bitcoin Script verification code in C++. Since it is consensus-critical, this code was called from Zebra through foreign function interface (FFI). That interface was clunky because it required parsing the whole transaction in C++ code, which would then pull Rust libraries which could get in conflict with Zebra code. A refactoring was done so that only the verification itself was done in C++, and the rest done by Rust code, using a callback. However, in this refactoring, it was not noticed that a particular consensus rule - only accepting known hash types in transparent transaction signatures - was being enforced in C++ code and thus had to be enforced by the Rust caller.</p>
<p>An attacker could exploit this by:</p>
<ul>
<li>Submitting a V4 or V5 transaction with an invalid hash type</li>
<li>The V5 transaction would be accepted by Zebra nodes but not by <code>zcashd</code> nodes (and vice-versa for V4), creating a consensus split in the network.</li>
</ul>
<h2 id="impact">Impact</h2>
<p><strong>Consensus Failure</strong></p>
<ul>
<li><strong>Attack Vector:</strong> Network.</li>
<li><strong>Effect:</strong> Network partition/consensus split.</li>
<li><strong>Scope:</strong> Any Zebra affected Zebra node</li>
</ul>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in Zebra 4.3.1.</p>
<p>The fix adds the consensus check in the caller of the C++ verification code. It also uses the raw hash type for V4 sighash computations.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to Zebra 4.3.1 or later immediately.</p>
<p>There are no known workarounds for this issue. Immediate upgrade is the only way to ensure the node remains on the correct consensus path and is protected against malicious chain forks.</p>
<h2 id="credits">Credits</h2>
<p>Thanks Alex “Scalar” Sol for finding and reporting the issue, and to @sangsoo-osec who independently found the same issue and demonstrated that the V4 variant was also exploitable.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-8m29-fpq5-89jj">https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-8m29-fpq5-89jj</a></li>
<li><a href="https://github.com/advisories/GHSA-8m29-fpq5-89jj">https://github.com/advisories/GHSA-8m29-fpq5-89jj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8m29-fpq5-89jj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-18T01:15:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zebrad] Zebra Vulnerable to Denial of Service via Interrupted JSON-RPC Requests from Authenticated Clients]]></title>
        <id>https://github.com/advisories/GHSA-29x4-r6jv-ff4w</id>
        <link href="https://github.com/advisories/GHSA-29x4-r6jv-ff4w"/>
        <updated>2026-04-18T01:15:14.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability in Zebra's JSON-RPC HTTP middleware allows an authenticated RPC client to cause a Zebra node to crash by disconnecting before the request body is fully received. The node treats the failure to read the HTTP request body as an unrecoverable error and aborts the process instead of returning an error response.</p>
<h2 id="severity">Severity</h2>
<p><strong>Moderate</strong> - This is a Denial of Service (DoS) that requires a client capable of passing Zebra's cookie authentication, which is enabled by default.</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li><code>zebrad</code>: versions from <strong>2.2.0</strong> up to (but not including) <strong>4.3.1</strong></li>
<li><code>zebra-rpc</code>: versions from <strong>1.0.0-beta.45</strong> up to (but not including) <strong>6.0.2</strong></li>
</ul>
<h2 id="description">Description</h2>
<p>Zebra's JSON-RPC HTTP middleware treated a failure to read the incoming HTTP request body as an unrecoverable error, aborting the process rather than returning an error response. A client that disconnected after sending only part of a request body, for example, by resetting the TCP connection mid-transfer, was sufficient to trigger the crash. The vulnerability could be exploited only by authenticated RPC clients. Nodes running the shipped defaults, with RPC bound to localhost and cookie authentication on, were not vulnerable.</p>
<h2 id="impact">Impact</h2>
<p><strong>Denial of Service</strong></p>
<ul>
<li><strong>Attack Vector:</strong> Network, authenticated (requires a valid RPC cookie when cookie authentication is enabled).</li>
<li><strong>Effect:</strong> Immediate crash of the Zebra node.</li>
<li><strong>Scope:</strong> Any node whose RPC interface is reachable by a client with valid credentials, or any node with cookie authentication disabled and an exposed RPC interface.</li>
</ul>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in <strong>Zebra 4.3.1</strong> (crate <code>zebra-rpc</code> 6.0.2).</p>
<p>The fix propagates failures to read the HTTP request body as ordinary error responses, so Zebra now rejects truncated or interrupted requests rather than crashing.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to <strong>Zebra 4.3.1</strong> or later.</p>
<p>If an immediate upgrade is not possible, users should ensure their RPC port is not exposed to untrusted networks and that cookie authentication remains enabled (the default).</p>
<h2 id="references">References</h2>
<ul>
<li><a href="https://zfnd.org/">Zebra 4.3.1 Release Announcement</a></li>
</ul>
<h2 id="credits">Credits</h2>
<p>Thanks to <a href="https://github.com/shieldedonly">shieldedonly</a> who discovered this issue and reported it via our coordinated disclosure process.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-29x4-r6jv-ff4w">https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-29x4-r6jv-ff4w</a></li>
<li><a href="https://github.com/advisories/GHSA-29x4-r6jv-ff4w">https://github.com/advisories/GHSA-29x4-r6jv-ff4w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-29x4-r6jv-ff4w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-18T01:15:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zebra-rpc] Zebra Vulnerable to Denial of Service via Interrupted JSON-RPC Requests from Authenticated Clients]]></title>
        <id>https://github.com/advisories/GHSA-29x4-r6jv-ff4w</id>
        <link href="https://github.com/advisories/GHSA-29x4-r6jv-ff4w"/>
        <updated>2026-04-18T01:15:14.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability in Zebra's JSON-RPC HTTP middleware allows an authenticated RPC client to cause a Zebra node to crash by disconnecting before the request body is fully received. The node treats the failure to read the HTTP request body as an unrecoverable error and aborts the process instead of returning an error response.</p>
<h2 id="severity">Severity</h2>
<p><strong>Moderate</strong> - This is a Denial of Service (DoS) that requires a client capable of passing Zebra's cookie authentication, which is enabled by default.</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li><code>zebrad</code>: versions from <strong>2.2.0</strong> up to (but not including) <strong>4.3.1</strong></li>
<li><code>zebra-rpc</code>: versions from <strong>1.0.0-beta.45</strong> up to (but not including) <strong>6.0.2</strong></li>
</ul>
<h2 id="description">Description</h2>
<p>Zebra's JSON-RPC HTTP middleware treated a failure to read the incoming HTTP request body as an unrecoverable error, aborting the process rather than returning an error response. A client that disconnected after sending only part of a request body, for example, by resetting the TCP connection mid-transfer, was sufficient to trigger the crash. The vulnerability could be exploited only by authenticated RPC clients. Nodes running the shipped defaults, with RPC bound to localhost and cookie authentication on, were not vulnerable.</p>
<h2 id="impact">Impact</h2>
<p><strong>Denial of Service</strong></p>
<ul>
<li><strong>Attack Vector:</strong> Network, authenticated (requires a valid RPC cookie when cookie authentication is enabled).</li>
<li><strong>Effect:</strong> Immediate crash of the Zebra node.</li>
<li><strong>Scope:</strong> Any node whose RPC interface is reachable by a client with valid credentials, or any node with cookie authentication disabled and an exposed RPC interface.</li>
</ul>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in <strong>Zebra 4.3.1</strong> (crate <code>zebra-rpc</code> 6.0.2).</p>
<p>The fix propagates failures to read the HTTP request body as ordinary error responses, so Zebra now rejects truncated or interrupted requests rather than crashing.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to <strong>Zebra 4.3.1</strong> or later.</p>
<p>If an immediate upgrade is not possible, users should ensure their RPC port is not exposed to untrusted networks and that cookie authentication remains enabled (the default).</p>
<h2 id="references">References</h2>
<ul>
<li><a href="https://zfnd.org/">Zebra 4.3.1 Release Announcement</a></li>
</ul>
<h2 id="credits">Credits</h2>
<p>Thanks to <a href="https://github.com/shieldedonly">shieldedonly</a> who discovered this issue and reported it via our coordinated disclosure process.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-29x4-r6jv-ff4w">https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-29x4-r6jv-ff4w</a></li>
<li><a href="https://github.com/advisories/GHSA-29x4-r6jv-ff4w">https://github.com/advisories/GHSA-29x4-r6jv-ff4w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-29x4-r6jv-ff4w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-18T01:15:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zebra-chain] Zebra has rk Identity Point Panic in Transaction Verification]]></title>
        <id>https://github.com/advisories/GHSA-452v-w3gx-72wg</id>
        <link href="https://github.com/advisories/GHSA-452v-w3gx-72wg"/>
        <updated>2026-04-18T01:14:59.000Z</updated>
        <content type="html"><![CDATA[<h1 id="rk-identity-point-panic-in-transaction-verification">rk Identity Point Panic in Transaction Verification</h1>
<h2 id="summary">Summary</h2>
<p>Orchard transactions contain a <code>rk</code> field which is a randomized validating key and also an elliptic curve point. The Zcash specification allows the field to be the identity (a "zero" value), however, the <code>orchard</code> crate which is used to verify Orchard proofs would panic when fed a <code>rk</code> with the identity value. Thus an attacker could send a crafted transaction that would make a Zebra node crash.</p>
<h2 id="severity">Severity</h2>
<p><strong>Critical</strong> - This is a Denial of Service Vulnerability that could allow an attacker to crash Zebra nodes.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>All Zebra versions prior to <strong>version 4.3.1</strong>.</p>
<h2 id="description">Description</h2>
<p>The vulnerability exists in the <code>circuits.rs</code> file of the <code>orchard</code> crate; it attempts to get the coordinates of the <code>rk</code> value and calls <code>unwrap()</code> on the results, which causes a panic if <code>rk</code> is the identity.</p>
<p>Zebra parses <code>rk</code> as a byte vector; it creates an Orchard "bundle" using the <code>orchard</code> crate and then calls the same crate to verify it, triggering the panic.</p>
<p>An attacker could exploit this by:</p>
<ol>
<li>Creating a transaction with a identity <code>rk</code></li>
<li>Submitting it to a Zebra node, making it crash</li>
</ol>
<h2 id="impact">Impact</h2>
<p><strong>Denial of Service</strong></p>
<ul>
<li><strong>Attack Vector:</strong> Network.</li>
<li><strong>Effect:</strong> Node crash.</li>
<li><strong>Scope:</strong> Any impacted Zebra node.</li>
</ul>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in <strong>Zebra 4.3.1</strong>. </p>
<p>The fix was agreed with <code>zcashd</code> developers (which has the same issue) to not allow the identity <code>rk</code> anymore and change the specification as such. Zebra now does this when parsing a transaction. This was deemed easier than fixing the issue in <code>orchard</code>, which would make the bug public before the nodes could be patched.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to <strong>Zebra 4.3.1</strong> or later immediately. </p>
<p>There are no known workarounds for this issue. Immediate upgrade is the only way to ensure the node remains not vulnerable to denial of service.</p>
<h2 id="credits">Credits</h2>
<p>Thanks to Alex “Scalar” Sol for finding and reporting the issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-452v-w3gx-72wg">https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-452v-w3gx-72wg</a></li>
<li><a href="https://github.com/advisories/GHSA-452v-w3gx-72wg">https://github.com/advisories/GHSA-452v-w3gx-72wg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-452v-w3gx-72wg</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-18T01:14:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zebrad] Zebra has rk Identity Point Panic in Transaction Verification]]></title>
        <id>https://github.com/advisories/GHSA-452v-w3gx-72wg</id>
        <link href="https://github.com/advisories/GHSA-452v-w3gx-72wg"/>
        <updated>2026-04-18T01:14:59.000Z</updated>
        <content type="html"><![CDATA[<h1 id="rk-identity-point-panic-in-transaction-verification">rk Identity Point Panic in Transaction Verification</h1>
<h2 id="summary">Summary</h2>
<p>Orchard transactions contain a <code>rk</code> field which is a randomized validating key and also an elliptic curve point. The Zcash specification allows the field to be the identity (a "zero" value), however, the <code>orchard</code> crate which is used to verify Orchard proofs would panic when fed a <code>rk</code> with the identity value. Thus an attacker could send a crafted transaction that would make a Zebra node crash.</p>
<h2 id="severity">Severity</h2>
<p><strong>Critical</strong> - This is a Denial of Service Vulnerability that could allow an attacker to crash Zebra nodes.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>All Zebra versions prior to <strong>version 4.3.1</strong>.</p>
<h2 id="description">Description</h2>
<p>The vulnerability exists in the <code>circuits.rs</code> file of the <code>orchard</code> crate; it attempts to get the coordinates of the <code>rk</code> value and calls <code>unwrap()</code> on the results, which causes a panic if <code>rk</code> is the identity.</p>
<p>Zebra parses <code>rk</code> as a byte vector; it creates an Orchard "bundle" using the <code>orchard</code> crate and then calls the same crate to verify it, triggering the panic.</p>
<p>An attacker could exploit this by:</p>
<ol>
<li>Creating a transaction with a identity <code>rk</code></li>
<li>Submitting it to a Zebra node, making it crash</li>
</ol>
<h2 id="impact">Impact</h2>
<p><strong>Denial of Service</strong></p>
<ul>
<li><strong>Attack Vector:</strong> Network.</li>
<li><strong>Effect:</strong> Node crash.</li>
<li><strong>Scope:</strong> Any impacted Zebra node.</li>
</ul>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in <strong>Zebra 4.3.1</strong>. </p>
<p>The fix was agreed with <code>zcashd</code> developers (which has the same issue) to not allow the identity <code>rk</code> anymore and change the specification as such. Zebra now does this when parsing a transaction. This was deemed easier than fixing the issue in <code>orchard</code>, which would make the bug public before the nodes could be patched.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to <strong>Zebra 4.3.1</strong> or later immediately. </p>
<p>There are no known workarounds for this issue. Immediate upgrade is the only way to ensure the node remains not vulnerable to denial of service.</p>
<h2 id="credits">Credits</h2>
<p>Thanks to Alex “Scalar” Sol for finding and reporting the issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-452v-w3gx-72wg">https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-452v-w3gx-72wg</a></li>
<li><a href="https://github.com/advisories/GHSA-452v-w3gx-72wg">https://github.com/advisories/GHSA-452v-w3gx-72wg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-452v-w3gx-72wg</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-18T01:14:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zebra-network] Zebra: addr/addrv2 Deserialization Resource Exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-xr93-pcq3-pxf8</id>
        <link href="https://github.com/advisories/GHSA-xr93-pcq3-pxf8"/>
        <updated>2026-04-18T00:42:06.000Z</updated>
        <content type="html"><![CDATA[<h1 id="cve-2026-40881-addraddrv2-deserialization-resource-exhaustion">CVE-2026-40881: addr/addrv2 Deserialization Resource Exhaustion</h1>
<h2 id="summary">Summary</h2>
<p>When deserializing <code>addr</code> or <code>addrv2</code> messages, which contain vectors of addresses, Zebra would fully deserialize them up to a maximum length (over 233,000) that was derived from the 2 MiB message size limit. This is much larger than the actual limit of 1,000 messages from the specification. Zebra would eventually check that limit but, at that point, the memory for the larger vector was already allocated. An attacker could cause out-of-memory aborts in Zebra by sending multiple such messages over different connections.</p>
<h2 id="severity">Severity</h2>
<p><strong>Moderate</strong> - This is a Denial of Service Vulnerability that could allow an attacker to crash a Zebra node.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>All Zebra versions prior to <strong>version 4.3.1</strong>.</p>
<h2 id="description">Description</h2>
<p>The vulnerability exists in the <code>read_addr/addrv2</code> functions in <code>codec.rs</code>. It deserializes a vector of addresses with the <code>zcash_deserialize()</code> trait method, which uses as a upper bound the result of <code>T::max_allocation()</code>. For theses types, it was derived from dividing the max message size (2 MiB) by the minimum serialized size of one entry. For AddrV1: 2_097_152 / 30 = 69,904. For AddrV2: 2_097_152 / 9 = 233,016. Only after deserialization was the <code>MAX_ADDRS_IN_MESSAGE = 1000</code> limit checked.</p>
<p>An attacker could exploit this by:</p>
<ol>
<li>Creating <code>addr</code> or <code>addrv2</code> messages with a large number of entries.</li>
<li>Submitting them to a Zebra node, possibly through multiple connections, to attempt to get Zebra into an out-of-memory state.</li>
</ol>
<h2 id="impact">Impact</h2>
<p><strong>Denial of Service</strong></p>
<ul>
<li><strong>Attack Vector:</strong> Network.</li>
<li><strong>Effect:</strong> Zebra node crash.</li>
<li><strong>Scope:</strong> Any impacted Zebra node.</li>
</ul>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in <strong>Zebra 4.3.1</strong>. </p>
<p>The fix changes the <code>max_allocation()</code> method for the relevant types to return 1,000, thus blocking larger values prior to deserialization.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to <strong>Zebra 4.3.1</strong> or later immediately. </p>
<p>There are no known workarounds for this issue. Immediate upgrade is the only way to ensure the node remains not vulnerable to the denial of service attack.</p>
<h2 id="credits">Credits</h2>
<p>Thanks @Zk-nd3r for finding and reporting the issue, and suggesting the fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-xr93-pcq3-pxf8">https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-xr93-pcq3-pxf8</a></li>
<li><a href="https://github.com/advisories/GHSA-xr93-pcq3-pxf8">https://github.com/advisories/GHSA-xr93-pcq3-pxf8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xr93-pcq3-pxf8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-18T00:42:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zebrad] Zebra: addr/addrv2 Deserialization Resource Exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-xr93-pcq3-pxf8</id>
        <link href="https://github.com/advisories/GHSA-xr93-pcq3-pxf8"/>
        <updated>2026-04-18T00:42:06.000Z</updated>
        <content type="html"><![CDATA[<h1 id="cve-2026-40881-addraddrv2-deserialization-resource-exhaustion">CVE-2026-40881: addr/addrv2 Deserialization Resource Exhaustion</h1>
<h2 id="summary">Summary</h2>
<p>When deserializing <code>addr</code> or <code>addrv2</code> messages, which contain vectors of addresses, Zebra would fully deserialize them up to a maximum length (over 233,000) that was derived from the 2 MiB message size limit. This is much larger than the actual limit of 1,000 messages from the specification. Zebra would eventually check that limit but, at that point, the memory for the larger vector was already allocated. An attacker could cause out-of-memory aborts in Zebra by sending multiple such messages over different connections.</p>
<h2 id="severity">Severity</h2>
<p><strong>Moderate</strong> - This is a Denial of Service Vulnerability that could allow an attacker to crash a Zebra node.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>All Zebra versions prior to <strong>version 4.3.1</strong>.</p>
<h2 id="description">Description</h2>
<p>The vulnerability exists in the <code>read_addr/addrv2</code> functions in <code>codec.rs</code>. It deserializes a vector of addresses with the <code>zcash_deserialize()</code> trait method, which uses as a upper bound the result of <code>T::max_allocation()</code>. For theses types, it was derived from dividing the max message size (2 MiB) by the minimum serialized size of one entry. For AddrV1: 2_097_152 / 30 = 69,904. For AddrV2: 2_097_152 / 9 = 233,016. Only after deserialization was the <code>MAX_ADDRS_IN_MESSAGE = 1000</code> limit checked.</p>
<p>An attacker could exploit this by:</p>
<ol>
<li>Creating <code>addr</code> or <code>addrv2</code> messages with a large number of entries.</li>
<li>Submitting them to a Zebra node, possibly through multiple connections, to attempt to get Zebra into an out-of-memory state.</li>
</ol>
<h2 id="impact">Impact</h2>
<p><strong>Denial of Service</strong></p>
<ul>
<li><strong>Attack Vector:</strong> Network.</li>
<li><strong>Effect:</strong> Zebra node crash.</li>
<li><strong>Scope:</strong> Any impacted Zebra node.</li>
</ul>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in <strong>Zebra 4.3.1</strong>. </p>
<p>The fix changes the <code>max_allocation()</code> method for the relevant types to return 1,000, thus blocking larger values prior to deserialization.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to <strong>Zebra 4.3.1</strong> or later immediately. </p>
<p>There are no known workarounds for this issue. Immediate upgrade is the only way to ensure the node remains not vulnerable to the denial of service attack.</p>
<h2 id="credits">Credits</h2>
<p>Thanks @Zk-nd3r for finding and reporting the issue, and suggesting the fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-xr93-pcq3-pxf8">https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-xr93-pcq3-pxf8</a></li>
<li><a href="https://github.com/advisories/GHSA-xr93-pcq3-pxf8">https://github.com/advisories/GHSA-xr93-pcq3-pxf8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xr93-pcq3-pxf8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-18T00:42:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zebrad] Zebra: Cached Mempool Verification Bypasses Consensus Rules for Ahead-of-Tip Blocks]]></title>
        <id>https://github.com/advisories/GHSA-xvj8-ph7x-65gf</id>
        <link href="https://github.com/advisories/GHSA-xvj8-ph7x-65gf"/>
        <updated>2026-04-18T00:41:56.000Z</updated>
        <content type="html"><![CDATA[<h1 id="cve-2026-40880-cached-mempool-verification-bypasses-consensus-rules-for-ahead-of-tip-blocks">CVE-2026-40880: Cached Mempool Verification Bypasses Consensus Rules for Ahead-of-Tip Blocks</h1>
<h2 id="summary">Summary</h2>
<p>A logic error in Zebra's transaction verification cache could allow a malicious miner to induce a consensus split. By carefully submitting a transaction that is valid for height <code>H+1</code> but invalid for <code>H+2</code> and then mining that transaction in a block at height <code>H+2</code>, a miner could cause vulnerable Zebra nodes to accept an invalid block, leading to a consensus split from the rest of the Zcash network.</p>
<h2 id="severity">Severity</h2>
<p>High - This is a Consensus Vulnerability that could allow a malicious miner to induce network partitioning, service disruption, and potential double-spend attacks against affected nodes.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>All Zebra versions prior to version 4.3.1. (Some older versions are not affected but are no longer supported by the network)</p>
<h2 id="description">Description</h2>
<p>The vulnerability exists due to a performance optimization whose goal is to improve performance of transaction validation if the transaction was previously accepted into the mempool (and thus was verified as valid). That however did not take into account that a transaction can be valid for a specific height, but invalid at higher heights; for example, it can contain an expiry height, a lock time, and it is always bound to a network upgrade, all of which are height-dependant.</p>
<p>An attacker (specifically a malicious miner) could exploit this by (e.g. in the expiry height case):</p>
<ul>
<li>Submitting a transaction with expiry height <code>H+1</code> (where <code>H</code> is the height of the current tip)</li>
<li>Mining a block <code>H+1</code>, and a block <code>H+2</code> that contains that same transaction, and submitting block <code>H+2</code> before <code>H+1</code></li>
<li>Zebra nodes would accept <code>H+2</code> as valid (pending contextual verification) and wait for block <code>H+1</code>; when it arrives, Zebra would commit both blocks even if <code>H+2</code> contains an expired transaction</li>
<li>Other nodes (like <code>zcashd</code> or <code>zebrad</code> nodes without that transaction in their mempool) reject the block, resulting in a chain fork where the poisoned Zebra node is isolated.</li>
</ul>
<h2 id="impact">Impact</h2>
<p><strong>Consensus Failure</strong></p>
<p><strong>Attack Vector</strong>: Network (specifically via a malicious miner).
<strong>Effect</strong>: Network partition/consensus split.
<strong>Scope</strong>: Any Zebra node utilizing the transaction verification cache optimization for V5 transactions.</p>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in <strong>Zebra 4.3.1</strong>.</p>
<p>We removed the performance optimization altogether, since we deemed it too risky.</p>
<p>The fix ensures that verification is only skipped if the transaction's full integrity—including authorization data—is validated against the mempool entry.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to Zebra 4.3.0 or later immediately.</p>
<p>There are no known workarounds for this issue. Immediate upgrade is the only way to ensure the node remains on the correct consensus path and is protected against malicious chain forks.</p>
<h2 id="credits">Credits</h2>
<p>Thanks to @sangsoo-osec for a thorough advisory submission that noticed the lock time issue, and to @shieldedonly with an also thorough advisory (that was submitted while we were working on the first one) who noticed that the issue applied to other aspects of the transaction validation.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-xvj8-ph7x-65gf">https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-xvj8-ph7x-65gf</a></li>
<li><a href="https://github.com/advisories/GHSA-xvj8-ph7x-65gf">https://github.com/advisories/GHSA-xvj8-ph7x-65gf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xvj8-ph7x-65gf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-18T00:41:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zebra-consensus] Zebra: Cached Mempool Verification Bypasses Consensus Rules for Ahead-of-Tip Blocks]]></title>
        <id>https://github.com/advisories/GHSA-xvj8-ph7x-65gf</id>
        <link href="https://github.com/advisories/GHSA-xvj8-ph7x-65gf"/>
        <updated>2026-04-18T00:41:56.000Z</updated>
        <content type="html"><![CDATA[<h1 id="cve-2026-40880-cached-mempool-verification-bypasses-consensus-rules-for-ahead-of-tip-blocks">CVE-2026-40880: Cached Mempool Verification Bypasses Consensus Rules for Ahead-of-Tip Blocks</h1>
<h2 id="summary">Summary</h2>
<p>A logic error in Zebra's transaction verification cache could allow a malicious miner to induce a consensus split. By carefully submitting a transaction that is valid for height <code>H+1</code> but invalid for <code>H+2</code> and then mining that transaction in a block at height <code>H+2</code>, a miner could cause vulnerable Zebra nodes to accept an invalid block, leading to a consensus split from the rest of the Zcash network.</p>
<h2 id="severity">Severity</h2>
<p>High - This is a Consensus Vulnerability that could allow a malicious miner to induce network partitioning, service disruption, and potential double-spend attacks against affected nodes.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>All Zebra versions prior to version 4.3.1. (Some older versions are not affected but are no longer supported by the network)</p>
<h2 id="description">Description</h2>
<p>The vulnerability exists due to a performance optimization whose goal is to improve performance of transaction validation if the transaction was previously accepted into the mempool (and thus was verified as valid). That however did not take into account that a transaction can be valid for a specific height, but invalid at higher heights; for example, it can contain an expiry height, a lock time, and it is always bound to a network upgrade, all of which are height-dependant.</p>
<p>An attacker (specifically a malicious miner) could exploit this by (e.g. in the expiry height case):</p>
<ul>
<li>Submitting a transaction with expiry height <code>H+1</code> (where <code>H</code> is the height of the current tip)</li>
<li>Mining a block <code>H+1</code>, and a block <code>H+2</code> that contains that same transaction, and submitting block <code>H+2</code> before <code>H+1</code></li>
<li>Zebra nodes would accept <code>H+2</code> as valid (pending contextual verification) and wait for block <code>H+1</code>; when it arrives, Zebra would commit both blocks even if <code>H+2</code> contains an expired transaction</li>
<li>Other nodes (like <code>zcashd</code> or <code>zebrad</code> nodes without that transaction in their mempool) reject the block, resulting in a chain fork where the poisoned Zebra node is isolated.</li>
</ul>
<h2 id="impact">Impact</h2>
<p><strong>Consensus Failure</strong></p>
<p><strong>Attack Vector</strong>: Network (specifically via a malicious miner).
<strong>Effect</strong>: Network partition/consensus split.
<strong>Scope</strong>: Any Zebra node utilizing the transaction verification cache optimization for V5 transactions.</p>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in <strong>Zebra 4.3.1</strong>.</p>
<p>We removed the performance optimization altogether, since we deemed it too risky.</p>
<p>The fix ensures that verification is only skipped if the transaction's full integrity—including authorization data—is validated against the mempool entry.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to Zebra 4.3.0 or later immediately.</p>
<p>There are no known workarounds for this issue. Immediate upgrade is the only way to ensure the node remains on the correct consensus path and is protected against malicious chain forks.</p>
<h2 id="credits">Credits</h2>
<p>Thanks to @sangsoo-osec for a thorough advisory submission that noticed the lock time issue, and to @shieldedonly with an also thorough advisory (that was submitted while we were working on the first one) who noticed that the issue applied to other aspects of the transaction validation.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-xvj8-ph7x-65gf">https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-xvj8-ph7x-65gf</a></li>
<li><a href="https://github.com/advisories/GHSA-xvj8-ph7x-65gf">https://github.com/advisories/GHSA-xvj8-ph7x-65gf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xvj8-ph7x-65gf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-18T00:41:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sp1_prover] SP1 V6 Recursion Circuit Row-Count Binding Gap]]></title>
        <id>https://github.com/advisories/GHSA-63x8-x938-vx33</id>
        <link href="https://github.com/advisories/GHSA-63x8-x938-vx33"/>
        <updated>2026-04-17T22:58:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A soundness vulnerability in the SP1 V6 recursive shard verifier allows a malicious prover to construct a recursive proof from a shard proof that the native verifier would reject.</p>
<ul>
<li><strong>Affected versions:</strong> <code>&gt;= 6.0.0, &lt;= 6.0.2</code></li>
<li><strong>Not affected:</strong> SP1 V5 (all versions)</li>
<li><strong>Severity:</strong> High</li>
</ul>
<h2 id="details">Details</h2>
<h3 id="background">Background</h3>
<p>The recursive shard verifier circuit verifies shard proofs inside a recursive proof. Each shard proof includes a jagged PCS opening, which binds trace-shape metadata into a modified commitment and uses that same shape to evaluate the committed polynomials. These two operations must agree on the committed table heights.</p>
<h3 id="the-bug">The Bug</h3>
<p>In the V6 recursion circuit's jagged verifier, the two checks above are served by separate witnesses: a vector of row counts hashed into the modified commitment (commitment side), and a separate witness of prefix sums derived from row and column counts that drives the jagged polynomial evaluator (evaluation side). The prefix sums are observed within the shard verifier.</p>
<p>The consistency check between these two witnesses was missing in the recursion sub-circuit describing the jagged PCS verifier. A malicious prover can therefore supply one trace shape for commitment binding and a different shape for polynomial evaluation.</p>
<h3 id="potential-impact">Potential Impact</h3>
<p>The vulnerability applies to both main trace and preprocessed trace metadata. Because preprocessed traces encode circuit structure (selectors, fixed columns, permutation layout), the potential impact extends beyond data forgery to misrepresentation of the circuit itself.</p>
<p>While a demonstration of a full exploit proving arbitrary statements has not been created — since modifying one table's layout incidentally constrains changes to related tables — this barrier is not by design and should not be relied upon. This is considered a soundness violation that is unacceptable regardless of current exploitability.</p>
<h3 id="why-the-native-verifier-is-not-affected">Why the Native Verifier Is Not Affected</h3>
<p>The native shard verifier uses a single jagged PCS verifier object where row counts and evaluation layout are derived from the same data, so the split-witness divergence cannot occur. The recursion circuit's shard-level checks (prefix-sum and total-area assertions) only constrain the evaluation-side parameters, not the commitment-side row counts, so they do not catch the gap.</p>
<h2 id="mitigation">Mitigation</h2>
<p>The fix adds a post-evaluation consistency constraint in the recursive jagged verifier. After the jagged evaluation returns the prefix-sum values derived from the evaluation layout, the circuit reconstructs expected prefix sums from the commitment-side row counts (repeating each row count by its corresponding column count and accumulating). It then asserts element-wise equality between the reconstructed and returned prefix sums, and verifies that the final accumulated area matches the total area from the evaluation parameters.</p>
<p>This forces both witnesses to describe the same trace geometry. Any divergence is now a constraint failure.</p>
<h2 id="credit">Credit</h2>
<p>This vulnerability was identified through the SP1 bug bounty program on Code4rena.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33">https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33</a></li>
<li><a href="https://github.com/succinctlabs/sp1/releases/tag/v6.1.0">https://github.com/succinctlabs/sp1/releases/tag/v6.1.0</a></li>
<li><a href="https://github.com/advisories/GHSA-63x8-x938-vx33">https://github.com/advisories/GHSA-63x8-x938-vx33</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63x8-x938-vx33</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T00:05:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sp1_recursion_circuit] SP1 V6 Recursion Circuit Row-Count Binding Gap]]></title>
        <id>https://github.com/advisories/GHSA-63x8-x938-vx33</id>
        <link href="https://github.com/advisories/GHSA-63x8-x938-vx33"/>
        <updated>2026-04-17T22:58:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A soundness vulnerability in the SP1 V6 recursive shard verifier allows a malicious prover to construct a recursive proof from a shard proof that the native verifier would reject.</p>
<ul>
<li><strong>Affected versions:</strong> <code>&gt;= 6.0.0, &lt;= 6.0.2</code></li>
<li><strong>Not affected:</strong> SP1 V5 (all versions)</li>
<li><strong>Severity:</strong> High</li>
</ul>
<h2 id="details">Details</h2>
<h3 id="background">Background</h3>
<p>The recursive shard verifier circuit verifies shard proofs inside a recursive proof. Each shard proof includes a jagged PCS opening, which binds trace-shape metadata into a modified commitment and uses that same shape to evaluate the committed polynomials. These two operations must agree on the committed table heights.</p>
<h3 id="the-bug">The Bug</h3>
<p>In the V6 recursion circuit's jagged verifier, the two checks above are served by separate witnesses: a vector of row counts hashed into the modified commitment (commitment side), and a separate witness of prefix sums derived from row and column counts that drives the jagged polynomial evaluator (evaluation side). The prefix sums are observed within the shard verifier.</p>
<p>The consistency check between these two witnesses was missing in the recursion sub-circuit describing the jagged PCS verifier. A malicious prover can therefore supply one trace shape for commitment binding and a different shape for polynomial evaluation.</p>
<h3 id="potential-impact">Potential Impact</h3>
<p>The vulnerability applies to both main trace and preprocessed trace metadata. Because preprocessed traces encode circuit structure (selectors, fixed columns, permutation layout), the potential impact extends beyond data forgery to misrepresentation of the circuit itself.</p>
<p>While a demonstration of a full exploit proving arbitrary statements has not been created — since modifying one table's layout incidentally constrains changes to related tables — this barrier is not by design and should not be relied upon. This is considered a soundness violation that is unacceptable regardless of current exploitability.</p>
<h3 id="why-the-native-verifier-is-not-affected">Why the Native Verifier Is Not Affected</h3>
<p>The native shard verifier uses a single jagged PCS verifier object where row counts and evaluation layout are derived from the same data, so the split-witness divergence cannot occur. The recursion circuit's shard-level checks (prefix-sum and total-area assertions) only constrain the evaluation-side parameters, not the commitment-side row counts, so they do not catch the gap.</p>
<h2 id="mitigation">Mitigation</h2>
<p>The fix adds a post-evaluation consistency constraint in the recursive jagged verifier. After the jagged evaluation returns the prefix-sum values derived from the evaluation layout, the circuit reconstructs expected prefix sums from the commitment-side row counts (repeating each row count by its corresponding column count and accumulating). It then asserts element-wise equality between the reconstructed and returned prefix sums, and verifies that the final accumulated area matches the total area from the evaluation parameters.</p>
<p>This forces both witnesses to describe the same trace geometry. Any divergence is now a constraint failure.</p>
<h2 id="credit">Credit</h2>
<p>This vulnerability was identified through the SP1 bug bounty program on Code4rena.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33">https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33</a></li>
<li><a href="https://github.com/succinctlabs/sp1/releases/tag/v6.1.0">https://github.com/succinctlabs/sp1/releases/tag/v6.1.0</a></li>
<li><a href="https://github.com/advisories/GHSA-63x8-x938-vx33">https://github.com/advisories/GHSA-63x8-x938-vx33</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63x8-x938-vx33</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T00:05:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sp1_sdk] SP1 V6 Recursion Circuit Row-Count Binding Gap]]></title>
        <id>https://github.com/advisories/GHSA-63x8-x938-vx33</id>
        <link href="https://github.com/advisories/GHSA-63x8-x938-vx33"/>
        <updated>2026-04-17T22:58:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A soundness vulnerability in the SP1 V6 recursive shard verifier allows a malicious prover to construct a recursive proof from a shard proof that the native verifier would reject.</p>
<ul>
<li><strong>Affected versions:</strong> <code>&gt;= 6.0.0, &lt;= 6.0.2</code></li>
<li><strong>Not affected:</strong> SP1 V5 (all versions)</li>
<li><strong>Severity:</strong> High</li>
</ul>
<h2 id="details">Details</h2>
<h3 id="background">Background</h3>
<p>The recursive shard verifier circuit verifies shard proofs inside a recursive proof. Each shard proof includes a jagged PCS opening, which binds trace-shape metadata into a modified commitment and uses that same shape to evaluate the committed polynomials. These two operations must agree on the committed table heights.</p>
<h3 id="the-bug">The Bug</h3>
<p>In the V6 recursion circuit's jagged verifier, the two checks above are served by separate witnesses: a vector of row counts hashed into the modified commitment (commitment side), and a separate witness of prefix sums derived from row and column counts that drives the jagged polynomial evaluator (evaluation side). The prefix sums are observed within the shard verifier.</p>
<p>The consistency check between these two witnesses was missing in the recursion sub-circuit describing the jagged PCS verifier. A malicious prover can therefore supply one trace shape for commitment binding and a different shape for polynomial evaluation.</p>
<h3 id="potential-impact">Potential Impact</h3>
<p>The vulnerability applies to both main trace and preprocessed trace metadata. Because preprocessed traces encode circuit structure (selectors, fixed columns, permutation layout), the potential impact extends beyond data forgery to misrepresentation of the circuit itself.</p>
<p>While a demonstration of a full exploit proving arbitrary statements has not been created — since modifying one table's layout incidentally constrains changes to related tables — this barrier is not by design and should not be relied upon. This is considered a soundness violation that is unacceptable regardless of current exploitability.</p>
<h3 id="why-the-native-verifier-is-not-affected">Why the Native Verifier Is Not Affected</h3>
<p>The native shard verifier uses a single jagged PCS verifier object where row counts and evaluation layout are derived from the same data, so the split-witness divergence cannot occur. The recursion circuit's shard-level checks (prefix-sum and total-area assertions) only constrain the evaluation-side parameters, not the commitment-side row counts, so they do not catch the gap.</p>
<h2 id="mitigation">Mitigation</h2>
<p>The fix adds a post-evaluation consistency constraint in the recursive jagged verifier. After the jagged evaluation returns the prefix-sum values derived from the evaluation layout, the circuit reconstructs expected prefix sums from the commitment-side row counts (repeating each row count by its corresponding column count and accumulating). It then asserts element-wise equality between the reconstructed and returned prefix sums, and verifies that the final accumulated area matches the total area from the evaluation parameters.</p>
<p>This forces both witnesses to describe the same trace geometry. Any divergence is now a constraint failure.</p>
<h2 id="credit">Credit</h2>
<p>This vulnerability was identified through the SP1 bug bounty program on Code4rena.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33">https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33</a></li>
<li><a href="https://github.com/succinctlabs/sp1/releases/tag/v6.1.0">https://github.com/succinctlabs/sp1/releases/tag/v6.1.0</a></li>
<li><a href="https://github.com/advisories/GHSA-63x8-x938-vx33">https://github.com/advisories/GHSA-63x8-x938-vx33</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63x8-x938-vx33</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T00:05:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[p3-symmetric] Plonky3: The sponge construction used to get a hash function from a cryptographic permutation is not collision resistant for inputs of different lengths]]></title>
        <id>https://github.com/advisories/GHSA-3g92-f9ch-qjcm</id>
        <link href="https://github.com/advisories/GHSA-3g92-f9ch-qjcm"/>
        <updated>2026-04-16T22:52:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="vulnerability">Vulnerability</h3>
<p>Currently, when hashing, if the number of elements to hash is not a multiple of the rate, <code>hash_iter</code> pads by elements of
the current state. This means that it is possible to create iterators of different lengths which lead to an identical hashed state.</p>
<p>Given a simple example using a <code>PaddingFreeSponge</code> with width 8 and rate 4.
Start with the zero state: [0, 0, 0, 0, 0, 0, 0, 0]
Take the first 4 elements to hash and insert into the first 4 elements of the state: [h0, h1, h2, h3, 0, 0, 0, 0]
Run the cryptographic permutation on the state: [p00, p10, p20, p30, p40, p50, p60, p70]</p>
<p>Take the next 4 elements to hash and insert into the first 4 elements of the state: [h4, h5, h6, h7, p40, p50, p60, p70]
Run the cryptographic permutation: [p01, p11, p21, p31, p41, p51, p61, p71]</p>
<p>Repeat the above two steps until all elements of the iterator have been consumed.</p>
<p>If the number of elements in the iterator is not a multiple of 4 (say there are 10 elements) then, in the final round,
the first 2 elements are overwritten and so our final hash would be of: [h8, h9, p21, p31, p41, p51, p61, p71]</p>
<p>This means that the iterators over the elements [h0, h1, h2, h3, h4, h5, h6, h7, h8, h9] and [h0, h1, h2, h3, h4, h5, h6, h7, h8, h9, p21] would lead to the same final state of the hasher.</p>
<h3 id="impact">Impact</h3>
<p>The impact of this vulnerability is a little difficult to estimate. It is important to note that, in circumstances where the number of elements to be hashed is known and fixed in advance, (as is the case for most STARKS), the method is collision resistant. This vulnerability only applies if a malicious user is able to manipulate the number of elements to be hashed.</p>
<p>That being said, there are theoretically situations where this could allow for an amortising of grinding costs (if a prover can manipulate things to get the same hasher state across multiple proofs).</p>
<h3 id="patches">Patches</h3>
<p>The fix comes in two parts. The documentation on the current struct <code>PaddingFreeSponge</code> has been improved to clarify its intended use case and highlight that it is not collision resistant if an attacker can modify the number of elements being hashed.</p>
<p>In addition we add a new struct <code>Pad10Sponge</code> which is slightly less efficient but safe in all cases. The padding strategy of the new struct is as follows:</p>
<p>If the number of elements in the iterator is not a multiple of the rate, use a 10 padding scheme. If it is a multiple of the rate add 1 to the first secret state element. In the above example, for hashes of length 9, 10, 11, 12, the final state to be permuted would be
[h8, 1, 0, 0, p41, p51, p61, p71]
[h8, h9, 1, 0, p41, p51, p61, p71]
[h8, h9, h10, 1, p41, p51, p61, p71]
[h8, h9, h10, h11, p41 + 1, p51, p61, p71]</p>
<p>As can be seen, it is now impossible for iterators of different lengths to produce the same "final state" to be hashed which restores collision resistance. (See the following for more details <a href="https://github.com/user-attachments/files/24465342/padding-in-sponge.pdf">padding-in-sponge.pdf</a>)</p>
<h3 id="thanks">Thanks</h3>
<p>Many thanks to Benedikt Wagner, Dmitry Khovratovich and Bart Mennink for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Plonky3/Plonky3/security/advisories/GHSA-3g92-f9ch-qjcm">https://github.com/Plonky3/Plonky3/security/advisories/GHSA-3g92-f9ch-qjcm</a></li>
<li><a href="https://github.com/Plonky3/Plonky3/commit/5c1dc1d64c0516a8911bbf3ea40f173c21d6ae47">https://github.com/Plonky3/Plonky3/commit/5c1dc1d64c0516a8911bbf3ea40f173c21d6ae47</a></li>
<li><a href="https://github.com/advisories/GHSA-3g92-f9ch-qjcm">https://github.com/advisories/GHSA-3g92-f9ch-qjcm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3g92-f9ch-qjcm</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-16T22:52:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rustls-webpki] webpki: Name constraints were accepted for certificates asserting a wildcard name]]></title>
        <id>https://github.com/advisories/GHSA-xgp8-3hg3-c2mh</id>
        <link href="https://github.com/advisories/GHSA-xgp8-3hg3-c2mh"/>
        <updated>2026-04-16T21:17:13.000Z</updated>
        <content type="html"><![CDATA[<p>Permitted subtree name constraints for DNS names were accepted for certificates asserting a wildcard name.</p>
<p>This was incorrect because, given a name constraint of <code>accept.example.com</code>, <code>*.example.com</code> could feasibly allow a name of <code>reject.example.com</code> which is outside the constraint.
This is very similar to <a href="https://go.dev/issue/76442">CVE-2025-61727</a>.</p>
<p>Since name constraints are restrictions on otherwise properly-issued certificates, this bug is reachable only after signature verification and requires misissuance to exploit.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rustls/webpki/security/advisories/GHSA-xgp8-3hg3-c2mh">https://github.com/rustls/webpki/security/advisories/GHSA-xgp8-3hg3-c2mh</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0099.html">https://rustsec.org/advisories/RUSTSEC-2026-0099.html</a></li>
<li><a href="https://github.com/advisories/GHSA-xgp8-3hg3-c2mh">https://github.com/advisories/GHSA-xgp8-3hg3-c2mh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xgp8-3hg3-c2mh</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-16T21:17:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rustls-webpki] webpki: Name constraints were accepted for certificates asserting a wildcard name]]></title>
        <id>https://github.com/advisories/GHSA-xgp8-3hg3-c2mh</id>
        <link href="https://github.com/advisories/GHSA-xgp8-3hg3-c2mh"/>
        <updated>2026-04-16T21:17:13.000Z</updated>
        <content type="html"><![CDATA[<p>Permitted subtree name constraints for DNS names were accepted for certificates asserting a wildcard name.</p>
<p>This was incorrect because, given a name constraint of <code>accept.example.com</code>, <code>*.example.com</code> could feasibly allow a name of <code>reject.example.com</code> which is outside the constraint.
This is very similar to <a href="https://go.dev/issue/76442">CVE-2025-61727</a>.</p>
<p>Since name constraints are restrictions on otherwise properly-issued certificates, this bug is reachable only after signature verification and requires misissuance to exploit.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rustls/webpki/security/advisories/GHSA-xgp8-3hg3-c2mh">https://github.com/rustls/webpki/security/advisories/GHSA-xgp8-3hg3-c2mh</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0099.html">https://rustsec.org/advisories/RUSTSEC-2026-0099.html</a></li>
<li><a href="https://github.com/advisories/GHSA-xgp8-3hg3-c2mh">https://github.com/advisories/GHSA-xgp8-3hg3-c2mh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xgp8-3hg3-c2mh</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-16T21:17:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rustls-webpki] webpki: Name constraints for URI names were incorrectly accepted]]></title>
        <id>https://github.com/advisories/GHSA-965h-392x-2mh5</id>
        <link href="https://github.com/advisories/GHSA-965h-392x-2mh5"/>
        <updated>2026-04-16T21:16:23.000Z</updated>
        <content type="html"><![CDATA[<p>Name constraints for URI names were ignored and therefore accepted.</p>
<p>Note this library does not provide an API for asserting URI names, and URI name constraints are otherwise not implemented.  URI name constraints are now rejected unconditionally.</p>
<p>Since name constraints are restrictions on otherwise properly-issued certificates, this bug is reachable only after signature verification and requires misissuance to exploit.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rustls/webpki/security/advisories/GHSA-965h-392x-2mh5">https://github.com/rustls/webpki/security/advisories/GHSA-965h-392x-2mh5</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0098.html">https://rustsec.org/advisories/RUSTSEC-2026-0098.html</a></li>
<li><a href="https://github.com/advisories/GHSA-965h-392x-2mh5">https://github.com/advisories/GHSA-965h-392x-2mh5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-965h-392x-2mh5</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-16T21:16:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rustls-webpki] webpki: Name constraints for URI names were incorrectly accepted]]></title>
        <id>https://github.com/advisories/GHSA-965h-392x-2mh5</id>
        <link href="https://github.com/advisories/GHSA-965h-392x-2mh5"/>
        <updated>2026-04-16T21:16:23.000Z</updated>
        <content type="html"><![CDATA[<p>Name constraints for URI names were ignored and therefore accepted.</p>
<p>Note this library does not provide an API for asserting URI names, and URI name constraints are otherwise not implemented.  URI name constraints are now rejected unconditionally.</p>
<p>Since name constraints are restrictions on otherwise properly-issued certificates, this bug is reachable only after signature verification and requires misissuance to exploit.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rustls/webpki/security/advisories/GHSA-965h-392x-2mh5">https://github.com/rustls/webpki/security/advisories/GHSA-965h-392x-2mh5</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0098.html">https://rustsec.org/advisories/RUSTSEC-2026-0098.html</a></li>
<li><a href="https://github.com/advisories/GHSA-965h-392x-2mh5">https://github.com/advisories/GHSA-965h-392x-2mh5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-965h-392x-2mh5</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-16T21:16:22.000Z</published>
    </entry>
</feed>