<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-04-29T11:28:59.586Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[ml-dsa] RustCrypto: Signatures has timing side-channel in ML-DSA decomposition]]></title>
        <id>https://github.com/advisories/GHSA-hcp2-x6j4-29j7</id>
        <link href="https://github.com/advisories/GHSA-hcp2-x6j4-29j7"/>
        <updated>2026-04-24T19:58:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A timing side-channel was discovered in the Decompose algorithm which is used during ML-DSA signing to generate hints for the signature.</p>
<h3 id="details">Details</h3>
<p>The analysis was performed using a constant-time analyzer that examines compiled assembly code for instructions with data-dependent timing behavior. The analyzer flags:</p>
<ul>
<li><strong>UDIV/SDIV instructions</strong>: Hardware division instructions have early termination optimizations where execution time depends on operand values.</li>
</ul>
<p>The <code>decompose</code> function used a hardware division instruction to compute <code>r1.0 / TwoGamma2::U32</code>. This function is called during signing through <code>high_bits()</code> and <code>low_bits()</code>, which process values derived from secret key components:</p>
<ul>
<li><code>(&amp;w - &amp;cs2).low_bits()</code> where <code>cs2</code> is derived from secret key component <code>s2</code></li>
<li><code>Hint::new()</code> calls <code>high_bits()</code> on values derived from secret key component <code>t0</code></li>
</ul>
<p><strong>Original Code</strong>:</p>
<pre><code class="language-rust">fn decompose&lt;TwoGamma2: Unsigned&gt;(self) -&gt; (Elem, Elem) {
    // ...
    let mut r1 = r_plus - r0;
    r1.0 /= TwoGamma2::U32;  // Variable-time division on secret-derived data
    (r1, r0)
}
</code></pre>
<h3 id="poc">PoC</h3>
<p>I do not have an exploit written for this, currently.</p>
<h3 id="impact">Impact</h3>
<p>The dividend (<code>r1.0</code>) is derived from secret key material. An attacker with precise timing measurements could extract information about the signing key by observing timing variations in the division operation.</p>
<h3 id="mitigation">Mitigation</h3>
<p>Replacing division with constant-time Barrett reduction mitigates this risk. Since <code>TwoGamma2</code> is a compile-time constant, we precompute the multiplicative inverse:</p>
<pre><code class="language-patch">diff --git a/ml-dsa/src/algebra.rs b/ml-dsa/src/algebra.rs
index 559b68a..bb126ce 100644
--- a/ml-dsa/src/algebra.rs
+++ b/ml-dsa/src/algebra.rs
@@ -54,8 +54,50 @@ pub(crate) trait Decompose {
     fn decompose&lt;TwoGamma2: Unsigned&gt;(self) -&gt; (Elem, Elem);
 }
 
+/// Constant-time division by a compile-time constant divisor.
+///
+/// This trait provides a constant-time alternative to the hardware division
+/// instruction, which has variable timing based on operand values.
+/// Uses Barrett reduction to compute `x / M` where M is a compile-time constant.
+pub(crate) trait ConstantTimeDiv: Unsigned {
+    /// Bit shift for Barrett reduction, chosen to provide sufficient precision
+    const CT_DIV_SHIFT: usize;
+    /// Precomputed multiplier: ceil(2^SHIFT / M)
+    const CT_DIV_MULTIPLIER: u64;
+
+    /// Perform constant-time division of x by Self::U32
+    /// Requires: x &lt; Q (the field modulus, ~2^23)
+    #[inline(always)]
+    fn ct_div(x: u32) -&gt; u32 {
+        // Barrett reduction: q = (x * MULTIPLIER) &gt;&gt; SHIFT
+        // This gives us floor(x / M) for x &lt; 2^SHIFT / MULTIPLIER * M
+        let x64 = u64::from(x);
+        let quotient = (x64 * Self::CT_DIV_MULTIPLIER) &gt;&gt; Self::CT_DIV_SHIFT;
+        quotient as u32
+    }
+}
+
+impl&lt;M&gt; ConstantTimeDiv for M
+where
+    M: Unsigned,
+{
+    // Use a shift that provides enough precision for the ML-DSA field (Q ~ 2^23)
+    // We need SHIFT &gt; log2(Q) + log2(M) to ensure accuracy
+    // With Q &lt; 2^24 and M &lt; 2^20, SHIFT = 48 is sufficient
+    const CT_DIV_SHIFT: usize = 48;
+
+    // Precompute the multiplier at compile time
+    // We add (M-1) before dividing to get ceiling division, ensuring we never underestimate
+    #[allow(clippy::integer_division_remainder_used)]
+    const CT_DIV_MULTIPLIER: u64 = ((1u64 &lt;&lt; Self::CT_DIV_SHIFT) + M::U64 - 1) / M::U64;
+}
+
 impl Decompose for Elem {
     // Algorithm 36 Decompose
+    //
+    // This implementation uses constant-time division to avoid timing side-channels.
+    // The original algorithm used hardware division which has variable timing based
+    // on operand values, potentially leaking secret information during signing.
     fn decompose&lt;TwoGamma2: Unsigned&gt;(self) -&gt; (Elem, Elem) {
         let r_plus = self.clone();
         let r0 = r_plus.mod_plus_minus::&lt;TwoGamma2&gt;();
@@ -63,8 +105,9 @@ impl Decompose for Elem {
         if r_plus - r0 == Elem::new(BaseField::Q - 1) {
             (Elem::new(0), r0 - Elem::new(1))
         } else {
-            let mut r1 = r_plus - r0;
-            r1.0 /= TwoGamma2::U32;
+            let diff = r_plus - r0;
+            // Use constant-time division instead of hardware division
+            let r1 = Elem::new(TwoGamma2::ct_div(diff.0));
             (r1, r0)
         }
     }
</code></pre>
<p>See our blog post on <a href="https://blog.trailofbits.com/2025/11/14/how-we-avoided-side-channels-in-our-new-post-quantum-go-cryptography-libraries/">how we avoided side-channels in our Go implementation of ML-DSA</a> for more information.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/RustCrypto/signatures/security/advisories/GHSA-hcp2-x6j4-29j7">https://github.com/RustCrypto/signatures/security/advisories/GHSA-hcp2-x6j4-29j7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-22705">https://nvd.nist.gov/vuln/detail/CVE-2026-22705</a></li>
<li><a href="https://github.com/RustCrypto/signatures/pull/1144">https://github.com/RustCrypto/signatures/pull/1144</a></li>
<li><a href="https://github.com/RustCrypto/signatures/commit/035d9eef98486ecd00a8bf418c7817eb14dd6558">https://github.com/RustCrypto/signatures/commit/035d9eef98486ecd00a8bf418c7817eb14dd6558</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2025-0144.html">https://rustsec.org/advisories/RUSTSEC-2025-0144.html</a></li>
<li><a href="https://github.com/advisories/GHSA-hcp2-x6j4-29j7">https://github.com/advisories/GHSA-hcp2-x6j4-29j7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hcp2-x6j4-29j7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-13T15:10:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rustls-webpki] rustls-webpki: Denial of service via panic on malformed CRL BIT STRING]]></title>
        <id>https://github.com/advisories/GHSA-82j2-j2ch-gfr8</id>
        <link href="https://github.com/advisories/GHSA-82j2-j2ch-gfr8"/>
        <updated>2026-04-24T16:20:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>bit_string_flags()</code> in <code>src/der.rs</code> panics with an index-out-of-bounds when given a BIT STRING whose content is exactly <code>[0x00]</code> (one byte: zero padding bits, zero data bytes). This is reachable through the public API <code>BorrowedCertRevocationList::from_der()</code> via the <code>issuingDistributionPoint</code> CRL extension.</p>
<p><strong>Precondition</strong>: CRL checking is opt-in in rustls-webpki. This vulnerability affects only applications that explicitly pass <code>RevocationOptions</code> to <code>verify_for_usage()</code> and load CRL bytes from a source the attacker can influence. The default rustls configuration (no <code>RevocationOptions</code>) is not affected.</p>
<blockquote>
<p><strong>AI disclosure</strong>: This report was prepared with AI assistance (Claude). The vulnerability was discovered by differential fuzzing against a formally-verified Rust oracle. All technical claims have been independently verified against the live source code before submission.</p>
</blockquote>
<h3 id="details">Details</h3>
<p><code>bit_string_flags()</code> in <code>src/der.rs</code> reads the content of named-bit BIT
STRINGs (KeyUsage, ReasonFlags, etc.). Its input guard:</p>
<pre><code class="language-rust">if padding_bits &gt; 7 || (raw_bits.is_empty() &amp;&amp; padding_bits != 0) {
    return Err(Error::BadDer);
}
let last_byte = raw_bits[raw_bits.len() - 1];  // ← crash
</code></pre>
<p>misses the case <code>padding_bits == 0 &amp;&amp; raw_bits.is_empty()</code>.
When a BIT STRING has content <code>[0x00]</code>  (one padding-bits byte set to zero, no data bytes):</p>
<ul>
<li>padding_bits = 0x00 — passes the &gt; 7 check ✓</li>
<li>raw_bits = [] — passes is_empty() &amp;&amp; != 0 check ✓ (because 0 != 0 is false)</li>
<li>raw_bits.len() - 1 = 0usize - 1 = underflow → usize::MAX</li>
<li>raw_bits[usize::MAX] → panic</li>
</ul>
<p>Debug:   thread 'main' panicked: attempt to subtract with overflow
Release: thread 'main' panicked: index out of bounds: the len is 0
         but the index is 18446744073709551615</p>
<h3 id="poc">PoC</h3>
<p>Cargo.toml:</p>
<pre><code>[dependencies]
rustls-webpki = "0.102.8"   # also reproduces on 0.103.12
</code></pre>
<p>src/main.rs:</p>
<pre><code>fn main() {
    let crl: &amp;[u8] = &amp;[
        0x30, 0x65, 0x30, 0x50, 0x02, 0x01, 0x01, 0x30, 0x0d, 0x06, 0x09,
        0x2a, 0x86, 0x48, 0x86, 0xf7, 0x0d, 0x01, 0x01, 0x0b, 0x05, 0x00,
        0x30, 0x0c, 0x31, 0x0a, 0x30, 0x08, 0x06, 0x03, 0x55, 0x04, 0x03,
        0x13, 0x01, 0x41, 0x17, 0x0d, 0x32, 0x30, 0x30, 0x31, 0x30, 0x31,
        0x30, 0x30, 0x30, 0x30, 0x30, 0x30, 0x5a, 0x17, 0x0d, 0x32, 0x31,
        0x30, 0x31, 0x30, 0x31, 0x30, 0x30, 0x30, 0x30, 0x30, 0x30, 0x5a,
        0xa0, 0x10, 0x30, 0x0e, 0x30, 0x0c, 0x06, 0x03, 0x55, 0x1d, 0x1c,
        0x04, 0x05, 0x30, 0x03, 0x83, 0x01, 0x00, 0x30, 0x0d, 0x06, 0x09,
        0x2a, 0x86, 0x48, 0x86, 0xf7, 0x0d, 0x01, 0x01, 0x0b, 0x05, 0x00,
        0x03, 0x02, 0x00, 0x00,
    ];
    // Panics — never returns
    let _ = webpki::BorrowedCertRevocationList::from_der(crl);
}
</code></pre>
<p>output:</p>
<pre><code>thread 'main' panicked at src/der.rs:...
index out of bounds: the len is 0 but the index is 18446744073709551615
</code></pre>
<h4 id="trigger">Trigger</h4>
<pre><code>a0 10            -- cRLExtensions [0] EXPLICIT
  30 0e          -- SEQUENCE OF Extension
    30 0c        -- Extension SEQUENCE
      06 03 55 1d 1c   -- OID 2.5.29.28 (id-ce-issuingDistributionPoint)
      04 05            -- OCTET STRING (extnValue)
        30 03          -- IssuingDistributionPoint SEQUENCE
          83 01 00     -- [3] onlySomeReasons: BIT STRING, len=1, content=0x00
                       --   padding_bits=0, data=[]  ← TRIGGER
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Who is affected: 
Applications that (1) use rustls-webpki with CRL
revocation checking explicitly enabled via RevocationOptions, and (2)
load CRL bytes from a source an attacker can influence.</li>
<li>Attack paths:<ul>
<li>mTLS server (most realistic): An attacker obtains any certificate from a CA that permits custom CDP URLs — common in enterprise PKI. They set the CDP to a server they control, serve the 103-byte crafted CRL, and connect to the target. The server fetches the attacker's CRL during the handshake and panics. No MITM required.</li>
<li>TLS client with server-cert CRL checking: An attacker who can MITM an HTTP CRL distribution point (ARP/DNS poisoning on a local network) serves the crafted CRL in place of the legitimate one.</li>
</ul>
</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rustls/webpki/security/advisories/GHSA-82j2-j2ch-gfr8">https://github.com/rustls/webpki/security/advisories/GHSA-82j2-j2ch-gfr8</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0104.html">https://rustsec.org/advisories/RUSTSEC-2026-0104.html</a></li>
<li><a href="https://github.com/advisories/GHSA-82j2-j2ch-gfr8">https://github.com/advisories/GHSA-82j2-j2ch-gfr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-82j2-j2ch-gfr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-24T16:20:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rustls-webpki] rustls-webpki: Denial of service via panic on malformed CRL BIT STRING]]></title>
        <id>https://github.com/advisories/GHSA-82j2-j2ch-gfr8</id>
        <link href="https://github.com/advisories/GHSA-82j2-j2ch-gfr8"/>
        <updated>2026-04-24T16:20:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>bit_string_flags()</code> in <code>src/der.rs</code> panics with an index-out-of-bounds when given a BIT STRING whose content is exactly <code>[0x00]</code> (one byte: zero padding bits, zero data bytes). This is reachable through the public API <code>BorrowedCertRevocationList::from_der()</code> via the <code>issuingDistributionPoint</code> CRL extension.</p>
<p><strong>Precondition</strong>: CRL checking is opt-in in rustls-webpki. This vulnerability affects only applications that explicitly pass <code>RevocationOptions</code> to <code>verify_for_usage()</code> and load CRL bytes from a source the attacker can influence. The default rustls configuration (no <code>RevocationOptions</code>) is not affected.</p>
<blockquote>
<p><strong>AI disclosure</strong>: This report was prepared with AI assistance (Claude). The vulnerability was discovered by differential fuzzing against a formally-verified Rust oracle. All technical claims have been independently verified against the live source code before submission.</p>
</blockquote>
<h3 id="details">Details</h3>
<p><code>bit_string_flags()</code> in <code>src/der.rs</code> reads the content of named-bit BIT
STRINGs (KeyUsage, ReasonFlags, etc.). Its input guard:</p>
<pre><code class="language-rust">if padding_bits &gt; 7 || (raw_bits.is_empty() &amp;&amp; padding_bits != 0) {
    return Err(Error::BadDer);
}
let last_byte = raw_bits[raw_bits.len() - 1];  // ← crash
</code></pre>
<p>misses the case <code>padding_bits == 0 &amp;&amp; raw_bits.is_empty()</code>.
When a BIT STRING has content <code>[0x00]</code>  (one padding-bits byte set to zero, no data bytes):</p>
<ul>
<li>padding_bits = 0x00 — passes the &gt; 7 check ✓</li>
<li>raw_bits = [] — passes is_empty() &amp;&amp; != 0 check ✓ (because 0 != 0 is false)</li>
<li>raw_bits.len() - 1 = 0usize - 1 = underflow → usize::MAX</li>
<li>raw_bits[usize::MAX] → panic</li>
</ul>
<p>Debug:   thread 'main' panicked: attempt to subtract with overflow
Release: thread 'main' panicked: index out of bounds: the len is 0
         but the index is 18446744073709551615</p>
<h3 id="poc">PoC</h3>
<p>Cargo.toml:</p>
<pre><code>[dependencies]
rustls-webpki = "0.102.8"   # also reproduces on 0.103.12
</code></pre>
<p>src/main.rs:</p>
<pre><code>fn main() {
    let crl: &amp;[u8] = &amp;[
        0x30, 0x65, 0x30, 0x50, 0x02, 0x01, 0x01, 0x30, 0x0d, 0x06, 0x09,
        0x2a, 0x86, 0x48, 0x86, 0xf7, 0x0d, 0x01, 0x01, 0x0b, 0x05, 0x00,
        0x30, 0x0c, 0x31, 0x0a, 0x30, 0x08, 0x06, 0x03, 0x55, 0x04, 0x03,
        0x13, 0x01, 0x41, 0x17, 0x0d, 0x32, 0x30, 0x30, 0x31, 0x30, 0x31,
        0x30, 0x30, 0x30, 0x30, 0x30, 0x30, 0x5a, 0x17, 0x0d, 0x32, 0x31,
        0x30, 0x31, 0x30, 0x31, 0x30, 0x30, 0x30, 0x30, 0x30, 0x30, 0x5a,
        0xa0, 0x10, 0x30, 0x0e, 0x30, 0x0c, 0x06, 0x03, 0x55, 0x1d, 0x1c,
        0x04, 0x05, 0x30, 0x03, 0x83, 0x01, 0x00, 0x30, 0x0d, 0x06, 0x09,
        0x2a, 0x86, 0x48, 0x86, 0xf7, 0x0d, 0x01, 0x01, 0x0b, 0x05, 0x00,
        0x03, 0x02, 0x00, 0x00,
    ];
    // Panics — never returns
    let _ = webpki::BorrowedCertRevocationList::from_der(crl);
}
</code></pre>
<p>output:</p>
<pre><code>thread 'main' panicked at src/der.rs:...
index out of bounds: the len is 0 but the index is 18446744073709551615
</code></pre>
<h4 id="trigger">Trigger</h4>
<pre><code>a0 10            -- cRLExtensions [0] EXPLICIT
  30 0e          -- SEQUENCE OF Extension
    30 0c        -- Extension SEQUENCE
      06 03 55 1d 1c   -- OID 2.5.29.28 (id-ce-issuingDistributionPoint)
      04 05            -- OCTET STRING (extnValue)
        30 03          -- IssuingDistributionPoint SEQUENCE
          83 01 00     -- [3] onlySomeReasons: BIT STRING, len=1, content=0x00
                       --   padding_bits=0, data=[]  ← TRIGGER
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Who is affected: 
Applications that (1) use rustls-webpki with CRL
revocation checking explicitly enabled via RevocationOptions, and (2)
load CRL bytes from a source an attacker can influence.</li>
<li>Attack paths:<ul>
<li>mTLS server (most realistic): An attacker obtains any certificate from a CA that permits custom CDP URLs — common in enterprise PKI. They set the CDP to a server they control, serve the 103-byte crafted CRL, and connect to the target. The server fetches the attacker's CRL during the handshake and panics. No MITM required.</li>
<li>TLS client with server-cert CRL checking: An attacker who can MITM an HTTP CRL distribution point (ARP/DNS poisoning on a local network) serves the crafted CRL in place of the legitimate one.</li>
</ul>
</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rustls/webpki/security/advisories/GHSA-82j2-j2ch-gfr8">https://github.com/rustls/webpki/security/advisories/GHSA-82j2-j2ch-gfr8</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0104.html">https://rustsec.org/advisories/RUSTSEC-2026-0104.html</a></li>
<li><a href="https://github.com/advisories/GHSA-82j2-j2ch-gfr8">https://github.com/advisories/GHSA-82j2-j2ch-gfr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-82j2-j2ch-gfr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-24T16:20:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[grid] Grid: Integer Overflow in Grid::expand_rows Leads to Safe-API Undefined Behavior]]></title>
        <id>https://github.com/advisories/GHSA-38c5-483c-4qqp</id>
        <link href="https://github.com/advisories/GHSA-38c5-483c-4qqp"/>
        <updated>2026-04-24T15:57:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An integer overflow in <code>Grid::expand_rows()</code> can corrupt the relationship between the grid’s logical dimensions and its backing storage. After the internal invariant is broken, the safe API get() may invoke get_unchecked() with an invalid index, resulting in Undefined Behavior.</p>
<h3 id="details">Details</h3>
<p>Tested Version: grid = "1.0.0"</p>
<p>expand_rows() computes the new backing length using unchecked arithmetic:</p>
<p><code>self.data.len() + rows * self.cols </code></p>
<p>If rows * self.cols or the subsequent addition overflows usize, the result wraps in release builds and self.data may be resized to a length much smaller than logically required.</p>
<p>After that, if the grid is in ColumnMajor order, the function performs in-place rotation using indices derived from:</p>
<pre><code>let total_rows = self.rows + row_added;
let col_idx = i * total_rows;
self.data[col_idx..col_idx + total_rows + i].rotate_right(i);
</code></pre>
<p>These computations also rely on the assumption that the backing storage has been resized to the correct length. Once the earlier length computation has wrapped, this assumption no longer holds, so the function may operate on invalid ranges or otherwise enter an inconsistent state.</p>
<p>Finally, the function updates logical metadata with:</p>
<p><code>self.rows += rows; </code></p>
<p>As a result, the grid can end up with logical dimensions that no longer match the actual backing storage. Subsequent safe API calls such as get() may then rely on corrupted metadata and reach unsafe internal accesses, resulting in invalid unchecked access and Undefined Behavior.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-rust">#![forbid(unsafe_code)]

use grid::Grid;

fn main() {
    let mut g = Grid::from_vec(vec![1u8, 2u8], 2);

    g.expand_rows(usize::MAX / 2);

    g.get(0, 0); // triggers UB in get_unchecked
}
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Invalid unchecked access (<code>get_unchecked</code>) reached via safe API</li>
<li>Confirmed by Miri (release-mode):</li>
</ul>
<pre><code>error: Undefined Behavior: `assume` called with `false`
   --&gt; ..../grid-1.0.0/src/lib.rs:527:9
    |
527 |         self.data.get_unchecked(index)
    |         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Undefined Behavior occurred here
</code></pre>
<ul>
<li>Potential crash / denial of service in release-builds (e.g., SIGSEGV, Illegal instruction)</li>
<li>Violates Rust’s safety guarantees despite using only safe code</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/becheran/grid/security/advisories/GHSA-38c5-483c-4qqp">https://github.com/becheran/grid/security/advisories/GHSA-38c5-483c-4qqp</a></li>
<li><a href="https://github.com/becheran/grid/commit/be213bd3528727148bef2d523c89e95d1fd9c072">https://github.com/becheran/grid/commit/be213bd3528727148bef2d523c89e95d1fd9c072</a></li>
<li><a href="https://github.com/advisories/GHSA-38c5-483c-4qqp">https://github.com/advisories/GHSA-38c5-483c-4qqp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-38c5-483c-4qqp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-24T15:57:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[russh] russh has pre-auth DoS via unbounded allocation in its keyboard-interactive auth handler]]></title>
        <id>https://github.com/advisories/GHSA-f5v4-2wr6-hqmg</id>
        <link href="https://github.com/advisories/GHSA-f5v4-2wr6-hqmg"/>
        <updated>2026-04-24T15:39:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A pre-authentication denial-of-service vulnerability exists in the server's keyboard-interactive authentication handler. A malicious client can crash any russh-based server that implements keyboard-interactive auth (e.g., for 2FA/TOTP) with a single malformed packet, requiring no credentials.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>In <code>russh/src/server/encrypted.rs</code>, the function <code>read_userauth_info_response</code> decodes a <code>u32</code> count from the client's <code>SSH_MSG_USERAUTH_INFO_RESPONSE</code> and passes it directly to <code>Vec::with_capacity()</code>:</p>
<pre><code class="language-rust">let n = map_err!(u32::decode(r))?;

// Bound both allocation and iteration by remaining packet data to
// prevent a malicious client from causing a multi-GB allocation or
// billions of loop iterations with a crafted count.
// Each response needs at least 4 bytes (length prefix).
let max_responses = r.remaining_len().saturating_add(3) / 4;
let n = (n as usize).min(max_responses);
let mut responses = Vec::with_capacity(n);
for _ in 0..n {
    responses.push(Bytes::decode(r).ok())
}
</code></pre>
<p>An attacker can send <code>n = 0x10000000</code> (268M) or larger in a minimal packet (~50 bytes after encryption). The server attempts to allocate <code>n * ~24 bytes</code> (size of <code>Option&lt;Bytes&gt;</code>) = ~6.4GB, causing an OOM crash.</p>
<h2 id="attack-flow">Attack Flow</h2>
<ol>
<li>Attacker connects via TCP, completes key exchange (no credentials needed -- this is the anonymous DH handshake, not authentication)</li>
<li>Sends <code>USERAUTH_REQUEST</code> with method <code>keyboard-interactive</code></li>
<li>Server handler returns <code>Auth::Partial</code> with prompts (standard for 2FA/TOTP)</li>
<li>Attacker sends <code>USERAUTH_INFO_RESPONSE</code> with <code>n = 0x10000000</code> and no response data</li>
<li>Server calls <code>Vec::with_capacity(268_435_456)</code>, OOM killed</li>
</ol>
<p>No authentication is required. The allocation occurs before the handler validates any credentials. The attack is repeatable faster than the server can restart.</p>
<h2 id="affected-configurations">Affected Configurations</h2>
<p>Any russh-based server where the <code>Handler::auth_keyboard_interactive</code> implementation returns <code>Auth::Partial</code> (i.e., sends prompts to the client). The default handler returns <code>Auth::reject()</code> and is not affected.</p>
<p>Source code review suggests that downstream projects using keyboard-interactive for multi-step auth (e.g., TOTP/2FA) follow the affected pattern, since returning <code>Auth::Partial</code> before credential verification is the intended API usage for prompting.</p>
<h2 id="confirmed-end-to-end-poc">Confirmed End-to-End PoC</h2>
<p>There is a complete Docker-contained PoC confirming the OOM kill:</p>
<ul>
<li>Minimal russh server returning <code>Auth::Partial</code> for keyboard-interactive</li>
<li>Python client (paramiko for key exchange) sends malformed <code>USERAUTH_INFO_RESPONSE</code></li>
<li>Container with 512MB memory limit; server is OOM-killed (exit code 137)</li>
</ul>
<p>Available on request.</p>
<h2 id="proposed-fix">Proposed Fix</h2>
<p>Cap the <code>Vec::with_capacity</code> allocation to what the remaining packet data can actually contain. Each response requires at least 4 bytes (length prefix), so:</p>
<pre><code class="language-rust">let n = map_err!(u32::decode(r))?;

// Bound both allocation and iteration by remaining packet data to
// prevent a malicious client from causing a multi-GB allocation or
// billions of loop iterations with a crafted count.
// Each response needs at least 4 bytes (length prefix).
let max_responses = r.remaining_len().saturating_add(3) / 4;
let n = (n as usize).min(max_responses);
let mut responses = Vec::with_capacity(n);
for _ in 0..n {
    responses.push(Bytes::decode(r).ok())
}
</code></pre>
<p>This bounds the allocation to at most the packet size (~256KB), while preserving the existing behavior for well-formed packets. This fix has been implemented, tested, and contributed via the temporary private fork.</p>
<h2 id="severity">Severity</h2>
<p>Pre-auth, remote, no credentials required, crashes the server process affecting all active sessions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Eugeny/russh/security/advisories/GHSA-f5v4-2wr6-hqmg">https://github.com/Eugeny/russh/security/advisories/GHSA-f5v4-2wr6-hqmg</a></li>
<li><a href="https://github.com/Eugeny/russh/commit/6c3c80a9b6d60763d6227d60fa8310e57172a4d1">https://github.com/Eugeny/russh/commit/6c3c80a9b6d60763d6227d60fa8310e57172a4d1</a></li>
<li><a href="https://github.com/Eugeny/russh/releases/tag/v0.60.1">https://github.com/Eugeny/russh/releases/tag/v0.60.1</a></li>
<li><a href="https://github.com/advisories/GHSA-f5v4-2wr6-hqmg">https://github.com/advisories/GHSA-f5v4-2wr6-hqmg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f5v4-2wr6-hqmg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-24T15:39:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lemmy_api_common] Lemmy has SSRF in /api/v3/post via Webmention dispatch]]></title>
        <id>https://github.com/advisories/GHSA-3jvj-v6w2-h948</id>
        <link href="https://github.com/advisories/GHSA-3jvj-v6w2-h948"/>
        <updated>2026-04-24T15:22:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Lemmy allows an authenticated low-privileged user to create a link post through <code>POST /api/v3/post</code>. When a post is created in a public community, the backend asynchronously sends a Webmention to the attacker-controlled link target.</p>
<p>The submitted URL is checked for syntax and scheme, but the audited code path does not reject loopback, private, or link-local destinations before the Webmention request is issued. This lets a normal user trigger server-side HTTP requests toward internal services.</p>
<h3 id="details">Details</h3>
<p>The entry point is the normal post creation API. The user-controlled <code>url</code> field is accepted, normalized with <code>diesel_url_create()</code>, and only validated with <code>is_valid_url()</code>. That validation allows <code>http</code> and <code>https</code> but does not implement internal address rejection.</p>
<p>The post creation flow then schedules Webmention delivery for public communities. This creates a direct source-to-sink path from an externally supplied post URL to a server-side outbound HTTP request.</p>
<p>Core vulnerable code path:</p>
<pre><code class="language-rust">// crates/api_crud/src/post/create.rs
let url = diesel_url_create(data.url.as_deref())?;
if let Some(url) = &amp;url {
  is_url_blocked(url, &amp;url_blocklist)?;
  is_valid_url(url)?;
}
</code></pre>
<pre><code class="language-rust">// crates/utils/src/utils/validation.rs
pub fn is_valid_url(url: &amp;Url) -&gt; LemmyResult&lt;()&gt; {
  let is_valid = ["http", "https", "magnet"].contains(&amp;url.scheme());
  if !is_valid {
    Err(LemmyErrorType::InvalidUrl)?
  }
  Ok(())
}
</code></pre>
<pre><code class="language-rust">// crates/api_crud/src/post/create.rs
if community.visibility == CommunityVisibility::Public {
  let post = inserted_post.clone();
  let url = url.clone();
  spawn_try_task(async move {
    if let Some(url) = url {
      Webmention::new(post.ap_id.clone().into(), url.into()).send().await?;
    }
    Ok(())
  });
}
</code></pre>
<p>These snippets matter because they show that the attacker controls <code>CreatePost.url</code>, the only validation is scheme-level, and the resulting URL is later used for server-side Webmention delivery.</p>
<h3 id="poc">PoC</h3>
<p>_Complete instructions, including specific configuration details, to reproduce the vulnerability._Prerequisites:</p>
<ul>
<li>The attacker has a valid low-privileged account.</li>
<li>The attacker can post to a public community.</li>
</ul>
<p>Practical reproduction flow:</p>
<ol>
<li>Run an HTTP listener on an internal or loopback-reachable address from the Lemmy server's perspective, such as <code>127.0.0.1:8081</code>.</li>
<li>Authenticate as a normal user.</li>
<li>Submit a post to a public community with <code>url</code> set to the internal target.</li>
<li>Observe the Lemmy API return a normal post creation response.</li>
<li>Observe the internal HTTP listener receive a request from the Lemmy server shortly afterwards.</li>
</ol>
<p>Complete PoC:</p>
<pre><code class="language-http">POST /api/v3/post HTTP/1.1
Host: victim.example
Authorization: Bearer &lt;low-priv-jwt&gt;
Content-Type: application/json

{
  "name": "wm-ssrf",
  "community_id": 1,
  "url": "http://127.0.0.1:8081/",
  "body": null,
  "alt_text": null,
  "honeypot": null,
  "nsfw": false,
  "language_id": null,
  "custom_thumbnail": null
}
</code></pre>
<p>Outcome:</p>
<ul>
<li>The API returns a successful <code>post_view</code> response.</li>
<li>The Lemmy server later issues an outbound request toward <code>http://127.0.0.1:8081/</code> as part of Webmention processing.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>An authenticated user can use the application server as a blind SSRF primitive against internal HTTP services. This can expose internal network reachability, trigger internal webhooks or administrative endpoints, and expand the attack surface beyond the public deployment boundary.</p>
<p>Because the sink is reached after ordinary user content submission, the issue is practical to exploit in real deployments where normal users can post to public communities.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/LemmyNet/lemmy/security/advisories/GHSA-3jvj-v6w2-h948">https://github.com/LemmyNet/lemmy/security/advisories/GHSA-3jvj-v6w2-h948</a></li>
<li><a href="https://github.com/LemmyNet/lemmy/commit/1f06693b708020c5c3a3752bd2f1c6006a75e9bc">https://github.com/LemmyNet/lemmy/commit/1f06693b708020c5c3a3752bd2f1c6006a75e9bc</a></li>
<li><a href="https://github.com/advisories/GHSA-3jvj-v6w2-h948">https://github.com/advisories/GHSA-3jvj-v6w2-h948</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3jvj-v6w2-h948</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-24T15:22:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lemmy_api_common] Lemmy has SSRF and internal image disclosure in post link metadata via unvalidated og:image]]></title>
        <id>https://github.com/advisories/GHSA-h6hf-9846-xwrq</id>
        <link href="https://github.com/advisories/GHSA-h6hf-9846-xwrq"/>
        <updated>2026-04-24T15:21:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Lemmy fetches metadata for user-supplied post URLs and, under the default <code>StoreLinkPreviews</code> image mode, downloads the preview image through local pict-rs. While the top-level page URL is checked against internal IP ranges, the extracted <code>og:image</code> URL is not subject to the same restriction.</p>
<p>As a result, an authenticated low-privileged user can submit an attacker-controlled public page whose Open Graph image points to an internal image endpoint. Lemmy will fetch that internal image server-side and store a local thumbnail that can then be served back to users.</p>
<h3 id="details">Details</h3>
<p>The metadata fetch logic applies an internal-address check only to the initial post URL. After HTML parsing, <code>extract_opengraph_data()</code> accepts absolute <code>og:image</code> values and returns them as-is. Later, <code>generate_post_link_metadata()</code> passes that second-hop image URL into <code>generate_pictrs_thumbnail()</code>, which instructs local pict-rs to fetch it through <code>image/download?url=...</code>.</p>
<p>This creates a two-stage source-to-sink chain where the first URL is constrained, but the security boundary is bypassed through an unvalidated secondary resource.</p>
<p>Core vulnerable code path:</p>
<pre><code class="language-rust">// crates/api_common/src/request.rs
let metadata = match &amp;post.url {
  Some(url) =&gt; fetch_link_metadata(url, &amp;context, false).await.unwrap_or_default(),
  _ =&gt; Default::default(),
};
</code></pre>
<pre><code class="language-rust">// crates/api_common/src/request.rs
let og_image = page
  .opengraph
  .images
  .first()
  .and_then(|ogo| url.join(&amp;ogo.url).ok());
</code></pre>
<pre><code class="language-rust">// crates/api_common/src/request.rs
let thumbnail_url = if let (true, Some(url)) = (allow_generate_thumbnail, image_url.clone()) {
  generate_pictrs_thumbnail(&amp;url, &amp;context).await.ok().map(Into::into).or(image_url)
} else {
  image_url.clone()
};
</code></pre>
<pre><code class="language-rust">// crates/api_common/src/request.rs
let fetch_url = format!(
  "{}image/download?url={}&amp;resize={}",
  pictrs_config.url,
  encode(image_url.as_str()),
  context.settings().pictrs_config()?.max_thumbnail_size
);
</code></pre>
<p>These snippets show that only the outer page URL is checked, while the extracted <code>og:image</code> value becomes a server-side fetch target without an equivalent internal-address guard.</p>
<h3 id="poc">PoC</h3>
<p>Prerequisites:</p>
<ul>
<li>The attacker has a valid low-privileged account.</li>
<li>The instance uses the default link preview storage mode.</li>
<li>The attacker can post a link to a community they can access.</li>
</ul>
<p>Practical reproduction flow:</p>
<ol>
<li>Host a public HTML page under attacker control.</li>
<li>Add an Open Graph image tag whose value points to an internal image URL reachable from the Lemmy host, such as <code>http://127.0.0.1:8081/internal.png</code>.</li>
<li>Create a Lemmy post whose <code>url</code> is the attacker-controlled page.</li>
<li>Observe Lemmy fetch the public page, extract <code>og:image</code>, and then fetch the internal image through pict-rs.</li>
<li>Observe the created post receive a local thumbnail URL, demonstrating that the internal image was retrieved and cached.</li>
</ol>
<p>Complete PoC attacker page:</p>
<pre><code class="language-html">&lt;html&gt;&lt;head&gt;
&lt;meta property="og:image" content="http://127.0.0.1:8081/internal.png"&gt;
&lt;/head&gt;&lt;body&gt;x&lt;/body&gt;&lt;/html&gt;
</code></pre>
<p>Complete PoC request:</p>
<pre><code class="language-http">POST /api/v3/post HTTP/1.1
Host: victim.example
Authorization: Bearer &lt;low-priv-jwt&gt;
Content-Type: application/json

{
  "name": "thumb-ssrf",
  "community_id": 1,
  "url": "https://attacker.example/og.html",
  "body": null,
  "alt_text": null,
  "honeypot": null,
  "nsfw": false,
  "language_id": null,
  "custom_thumbnail": null
}
</code></pre>
<p>Outcome:</p>
<ul>
<li>The post creation request succeeds.</li>
<li>The internal image endpoint receives a request from the Lemmy server.</li>
<li>The created post is updated with a local <code>thumbnail_url</code>, indicating that the internal image was fetched and cached.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>This issue upgrades an attacker-controlled external page into an internal image fetch primitive. It can be used to retrieve internal image resources, expose content that is otherwise reachable only from the application host, and publish those internal resources through Lemmy's own thumbnail serving path.</p>
<p>Because the vulnerable mode is the documented default behavior for link previews, the issue is relevant even without non-default privacy settings.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/LemmyNet/lemmy/security/advisories/GHSA-h6hf-9846-xwrq">https://github.com/LemmyNet/lemmy/security/advisories/GHSA-h6hf-9846-xwrq</a></li>
<li><a href="https://github.com/LemmyNet/lemmy/commit/9ffe586dafac1a46acf17edf90e0165e5503b2f1">https://github.com/LemmyNet/lemmy/commit/9ffe586dafac1a46acf17edf90e0165e5503b2f1</a></li>
<li><a href="https://join-lemmy.org/news/2026-04-20_-_Lemmy_Release_v0.19.18">https://join-lemmy.org/news/2026-04-20_-_Lemmy_Release_v0.19.18</a></li>
<li><a href="https://github.com/advisories/GHSA-h6hf-9846-xwrq">https://github.com/advisories/GHSA-h6hf-9846-xwrq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h6hf-9846-xwrq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-24T15:21:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openssl] rust-openssl: Deriver::derive and PkeyCtxRef::derive can overflow short buffers on OpenSSL 1.1.1]]></title>
        <id>https://github.com/advisories/GHSA-pqf5-4pqq-29f5</id>
        <link href="https://github.com/advisories/GHSA-pqf5-4pqq-29f5"/>
        <updated>2026-04-27T16:42:06.000Z</updated>
        <content type="html"><![CDATA[<p><code>Deriver::derive</code> (and <code>PkeyCtxRef::derive</code>) sets <code>len = buf.len()</code> and passes it as the in/out length to <code>EVP_PKEY_derive</code>, relying on OpenSSL to honor it. On OpenSSL 1.1.x, X25519, X448, DH and HKDF-extract  ignore the incoming <code>*keylen</code>, unconditionally writing the full shared secret (32/56/prime-size bytes). A caller passing a short slice gets a heap/stack overflow from safe code. OpenSSL 3.x providers do check, so this only impacts older OpenSSL.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-pqf5-4pqq-29f5">https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-pqf5-4pqq-29f5</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/pull/2606">https://github.com/rust-openssl/rust-openssl/pull/2606</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/commit/09b425e5f59a2466d806e71a83a9a449c914c596">https://github.com/rust-openssl/rust-openssl/commit/09b425e5f59a2466d806e71a83a9a449c914c596</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/releases/tag/openssl-v0.10.78">https://github.com/rust-openssl/rust-openssl/releases/tag/openssl-v0.10.78</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41676">https://nvd.nist.gov/vuln/detail/CVE-2026-41676</a></li>
<li><a href="https://github.com/advisories/GHSA-pqf5-4pqq-29f5">https://github.com/advisories/GHSA-pqf5-4pqq-29f5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pqf5-4pqq-29f5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T21:22:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openssl] rust-opennssl has an Out-of-bounds read in PEM password callback when returning an oversized length]]></title>
        <id>https://github.com/advisories/GHSA-xmgf-hq76-4vx2</id>
        <link href="https://github.com/advisories/GHSA-xmgf-hq76-4vx2"/>
        <updated>2026-04-27T16:42:01.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>*_from_pem_callback</code> APIs did not validate the length returned by the user's callback. A password callback that returns a value larger than the buffer it was given can cause some versions of OpenSSL to over-read this buffer. OpenSSL 3.x is not affected by this.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-xmgf-hq76-4vx2">https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-xmgf-hq76-4vx2</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/pull/2605">https://github.com/rust-openssl/rust-openssl/pull/2605</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/commit/5af6895c907773699f37f583f409b862284062b1">https://github.com/rust-openssl/rust-openssl/commit/5af6895c907773699f37f583f409b862284062b1</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/releases/tag/openssl-v0.10.78">https://github.com/rust-openssl/rust-openssl/releases/tag/openssl-v0.10.78</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41677">https://nvd.nist.gov/vuln/detail/CVE-2026-41677</a></li>
<li><a href="https://github.com/advisories/GHSA-xmgf-hq76-4vx2">https://github.com/advisories/GHSA-xmgf-hq76-4vx2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xmgf-hq76-4vx2</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-22T21:20:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openssl] rust-openssl has incorrect bounds assertion in aes key wrap]]></title>
        <id>https://github.com/advisories/GHSA-8c75-8mhr-p7r9</id>
        <link href="https://github.com/advisories/GHSA-8c75-8mhr-p7r9"/>
        <updated>2026-04-27T16:41:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>aes::unwrap_key()</code> has an incorrect bounds assertion on the out buffer size, which can lead to out-of-bounds write.</p>
<h3 id="details">Details</h3>
<p><code>aes::unwrap_key()</code> contains an incorrect assertion: it checks that <code>out.len() + 8 &lt;= in_.len()</code>, but this condition is reversed. The intended invariant is <code>out.len() &gt;= in_.len() - 8</code>, ensuring the output buffer is large enough.</p>
<p>Because of the inverted check, the function only accepts buffers at or below the minimum required size and rejects larger ones. If a smaller buffer is provided the function will write past the end of <code>out</code> by <code>in_.len() - 8 - out.len()</code> bytes, causing an out-of-bounds write from a safe public function.</p>
<h3 id="impact">Impact</h3>
<p>Vulnerable applications using AES keywrap and allowing attacker controlled buffer sizes could have an attacker trigger an out-of-bounds write.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-8c75-8mhr-p7r9">https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-8c75-8mhr-p7r9</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/pull/2604">https://github.com/rust-openssl/rust-openssl/pull/2604</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/commit/718d07ff8ff7be417d5b7a6a0047f1607520b3b6">https://github.com/rust-openssl/rust-openssl/commit/718d07ff8ff7be417d5b7a6a0047f1607520b3b6</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/releases/tag/openssl-v0.10.78">https://github.com/rust-openssl/rust-openssl/releases/tag/openssl-v0.10.78</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41678">https://nvd.nist.gov/vuln/detail/CVE-2026-41678</a></li>
<li><a href="https://github.com/advisories/GHSA-8c75-8mhr-p7r9">https://github.com/advisories/GHSA-8c75-8mhr-p7r9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8c75-8mhr-p7r9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T21:17:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openssl] rust-openssl: rustMdCtxRef::digest_final() writes past caller buffer with no length check]]></title>
        <id>https://github.com/advisories/GHSA-ghm9-cr32-g9qj</id>
        <link href="https://github.com/advisories/GHSA-ghm9-cr32-g9qj"/>
        <updated>2026-04-27T16:41:51.000Z</updated>
        <content type="html"><![CDATA[<p><code>EVP_DigestFinal()</code> always writes <code>EVP_MD_CTX_size(ctx)</code> to the <code>out</code> buffer. If <code>out</code> is smaller than that, <code>MdCtxRef::digest_final()</code> writes past its end, usually corrupting the stack. This is reachable from safe Rust.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-ghm9-cr32-g9qj">https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-ghm9-cr32-g9qj</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/pull/2608">https://github.com/rust-openssl/rust-openssl/pull/2608</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/commit/826c3888b77add418b394770e2b2e3a72d9f92fe">https://github.com/rust-openssl/rust-openssl/commit/826c3888b77add418b394770e2b2e3a72d9f92fe</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/releases/tag/openssl-v0.10.78">https://github.com/rust-openssl/rust-openssl/releases/tag/openssl-v0.10.78</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41681">https://nvd.nist.gov/vuln/detail/CVE-2026-41681</a></li>
<li><a href="https://github.com/advisories/GHSA-ghm9-cr32-g9qj">https://github.com/advisories/GHSA-ghm9-cr32-g9qj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ghm9-cr32-g9qj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T21:05:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openssl] rust-openssl: Unchecked callback length in PSK/cookie trampolines leaks adjacent memory to peer]]></title>
        <id>https://github.com/advisories/GHSA-hppc-g8h3-xhp3</id>
        <link href="https://github.com/advisories/GHSA-hppc-g8h3-xhp3"/>
        <updated>2026-04-27T16:29:44.000Z</updated>
        <content type="html"><![CDATA[<p>The FFI trampolines behind <code>SslContextBuilder::set_psk_client_callback</code>, <code>set_psk_server_callback</code>, <code>set_cookie_generate_cb</code>,  and <code>set_stateless_cookie_generate_cb</code> forwarded the user closure's returned usize directly to OpenSSL without checking it against the <code>&amp;mut [u8]</code> that was handed to the closure. This can lead to buffer overflows and other unintended consequences.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-hppc-g8h3-xhp3">https://github.com/rust-openssl/rust-openssl/security/advisories/GHSA-hppc-g8h3-xhp3</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/pull/2607">https://github.com/rust-openssl/rust-openssl/pull/2607</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/commit/1d109020d98fff2fb2e45c39a373af3dff99b24c">https://github.com/rust-openssl/rust-openssl/commit/1d109020d98fff2fb2e45c39a373af3dff99b24c</a></li>
<li><a href="https://github.com/rust-openssl/rust-openssl/releases/tag/openssl-v0.10.78">https://github.com/rust-openssl/rust-openssl/releases/tag/openssl-v0.10.78</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41898">https://nvd.nist.gov/vuln/detail/CVE-2026-41898</a></li>
<li><a href="https://github.com/advisories/GHSA-hppc-g8h3-xhp3">https://github.com/advisories/GHSA-hppc-g8h3-xhp3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hppc-g8h3-xhp3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T21:00:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rand] Rand is unsound with a custom logger using rand::rng()]]></title>
        <id>https://github.com/advisories/GHSA-cq8v-f236-94qc</id>
        <link href="https://github.com/advisories/GHSA-cq8v-f236-94qc"/>
        <updated>2026-04-22T20:13:41.000Z</updated>
        <content type="html"><![CDATA[<p>It has been reported (by @lopopolo) that the <code>rand</code> library is <a href="https://rust-lang.github.io/unsafe-code-guidelines/glossary.html#soundness-of-code--of-a-library">unsound</a> (i.e. that safe code using the public API can cause Undefined Behaviour) when all the following conditions are met:</p>
<ul>
<li>The <code>log</code> and <code>thread_rng</code> features are enabled</li>
<li>A <a href="https://docs.rs/log/latest/log/#implementing-a-logger">custom logger</a> is defined</li>
<li>The custom logger accesses <code>rand::rng()</code> (previously <code>rand::thread_rng()</code>) and calls any <code>TryRng</code> (previously <code>RngCore</code>) methods on <code>ThreadRng</code></li>
<li>The <code>ThreadRng</code> (attempts to) reseed while called from the custom logger (this happens every 64 kB of generated data)</li>
<li>Trace-level logging is enabled or warn-level logging is enabled and the random source (the <code>getrandom</code> crate) is unable to provide a new seed</li>
</ul>
<p><code>TryRng</code> (previously <code>RngCore</code>) methods for <code>ThreadRng</code> use <code>unsafe</code> code to cast <code>*mut BlockRng&lt;ReseedingCore&gt;</code> to <code>&amp;mut BlockRng&lt;ReseedingCore&gt;</code>. When all the above conditions are met this results in an aliased mutable reference, violating the Stacked Borrows rules. Miri is able to detect this violation in sample code. Since construction of <a href="https://doc.rust-lang.org/stable/nomicon/references.html">aliased mutable references is Undefined Behaviour</a>, the behaviour of optimized builds is hard to predict.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-random/rand/pull/1763">https://github.com/rust-random/rand/pull/1763</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0097.html">https://rustsec.org/advisories/RUSTSEC-2026-0097.html</a></li>
<li><a href="https://github.com/advisories/GHSA-cq8v-f236-94qc">https://github.com/advisories/GHSA-cq8v-f236-94qc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cq8v-f236-94qc</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-14T01:03:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rand] Rand is unsound with a custom logger using rand::rng()]]></title>
        <id>https://github.com/advisories/GHSA-cq8v-f236-94qc</id>
        <link href="https://github.com/advisories/GHSA-cq8v-f236-94qc"/>
        <updated>2026-04-22T20:13:41.000Z</updated>
        <content type="html"><![CDATA[<p>It has been reported (by @lopopolo) that the <code>rand</code> library is <a href="https://rust-lang.github.io/unsafe-code-guidelines/glossary.html#soundness-of-code--of-a-library">unsound</a> (i.e. that safe code using the public API can cause Undefined Behaviour) when all the following conditions are met:</p>
<ul>
<li>The <code>log</code> and <code>thread_rng</code> features are enabled</li>
<li>A <a href="https://docs.rs/log/latest/log/#implementing-a-logger">custom logger</a> is defined</li>
<li>The custom logger accesses <code>rand::rng()</code> (previously <code>rand::thread_rng()</code>) and calls any <code>TryRng</code> (previously <code>RngCore</code>) methods on <code>ThreadRng</code></li>
<li>The <code>ThreadRng</code> (attempts to) reseed while called from the custom logger (this happens every 64 kB of generated data)</li>
<li>Trace-level logging is enabled or warn-level logging is enabled and the random source (the <code>getrandom</code> crate) is unable to provide a new seed</li>
</ul>
<p><code>TryRng</code> (previously <code>RngCore</code>) methods for <code>ThreadRng</code> use <code>unsafe</code> code to cast <code>*mut BlockRng&lt;ReseedingCore&gt;</code> to <code>&amp;mut BlockRng&lt;ReseedingCore&gt;</code>. When all the above conditions are met this results in an aliased mutable reference, violating the Stacked Borrows rules. Miri is able to detect this violation in sample code. Since construction of <a href="https://doc.rust-lang.org/stable/nomicon/references.html">aliased mutable references is Undefined Behaviour</a>, the behaviour of optimized builds is hard to predict.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-random/rand/pull/1763">https://github.com/rust-random/rand/pull/1763</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0097.html">https://rustsec.org/advisories/RUSTSEC-2026-0097.html</a></li>
<li><a href="https://github.com/advisories/GHSA-cq8v-f236-94qc">https://github.com/advisories/GHSA-cq8v-f236-94qc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cq8v-f236-94qc</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-14T01:03:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rustfs] RustFS: Missing admin authorization on notification target endpoints allows unauthenticated configuration of event webhooks]]></title>
        <id>https://github.com/advisories/GHSA-pfcq-4gjr-6gjm</id>
        <link href="https://github.com/advisories/GHSA-pfcq-4gjr-6gjm"/>
        <updated>2026-04-27T16:22:58.000Z</updated>
        <content type="html"><![CDATA[<h1 id="missing-admin-auth-on-notification-target-endpoints-in-rustfs">Missing Admin Auth on Notification Target Endpoints in RustFS</h1>
<h3 id="finding-summary">Finding Summary</h3>
<p>All four notification target admin API endpoints in <code>rustfs/src/admin/handlers/event.rs</code> use a <code>check_permissions</code> helper that validates authentication only (access key + session token), without performing any admin-action authorization via <code>validate_admin_request</code>. Every other admin handler in the codebase correctly calls <code>validate_admin_request</code> with a specific <code>AdminAction</code>. This is the only admin handler file that skips authorization.</p>
<p>A non-admin user can overwrite a shared admin-defined notification target by name, causing subsequent bucket events to be delivered to an attacker-controlled endpoint. This enables cross-user event interception and audit evasion.</p>
<h3 id="what-was-proven-live">What Was Proven Live</h3>
<ol>
<li><p><strong>Authorization bypass on all four endpoints</strong> (03_readonly_user_bypass.py)</p>
<ul>
<li>PUT, GET list, GET arns, DELETE all return 200 for readonly-user</li>
<li>Control routes (list-users, kms/status) correctly return 403</li>
<li>Unauthenticated requests correctly rejected (403 Signature required)</li>
</ul>
</li>
<li><p><strong>SSRF via health probe</strong> (04_ssrf_listener_landing.py)</p>
<ul>
<li>HEAD request from rustfs container to attacker-controlled listener</li>
<li>No host validation: only scheme check (http/https)</li>
</ul>
</li>
<li><p><strong>Target hijacking and event exfiltration</strong> (05_target_hijacking.py, 06_full_event_exfil.py)</p>
<ul>
<li>Readonly-user overwrites admin-configured target URL by name</li>
<li>Subsequent S3 events delivered to attacker-controlled endpoint</li>
<li>Captured event body includes object keys, bucket names, user identities, and request metadata</li>
</ul>
</li>
<li><p><strong>Audit evasion</strong> (05_target_hijacking.py)</p>
<ul>
<li>Readonly-user can delete unbound targets</li>
<li>Readonly-user can overwrite bound targets (silently redirecting events)</li>
</ul>
</li>
</ol>
<h3 id="escalation-vectors-tested-but-not-viable">Escalation Vectors Tested But Not Viable</h3>
<ol>
<li><p><strong>Self-referencing webhook to admin API</strong> (13_self_referencing_test.py)</p>
<ul>
<li>Webhook sends unsigned POST with event JSON body</li>
<li>Admin endpoints require SigV4 auth -- unsigned request rejected</li>
<li>"Confused deputy" via self-referencing does NOT work</li>
</ul>
</li>
<li><p><strong>Protocol smuggling via non-HTTP targets</strong></p>
<ul>
<li>Only 2 target types implemented: webhook and MQTT (<code>event.rs:613</code> enforces this)</li>
<li>No Redis, Kafka, AMQP, or other protocol targets exist</li>
<li>CRLF injection in webhook config fields sanitized by reqwest</li>
<li>MQTT uses rumqttc (pure Rust binary protocol client), no raw TCP injection</li>
</ul>
</li>
<li><p><strong>MQTT target for RCE</strong></p>
<ul>
<li>No unsafe code in MQTT handler</li>
<li>rumqttc 0.29.0 has no known public CVEs</li>
<li>No Command::new, template engines, or deserialization of broker responses</li>
</ul>
</li>
<li><p><strong>Unauth access</strong></p>
<ul>
<li>Endpoints correctly reject unauthenticated requests (403)</li>
<li>Endpoints correctly reject invalid credentials (403)</li>
</ul>
</li>
</ol>
<h3 id="prior-art">Prior Art</h3>
<p>No existing advisory covers notification target endpoints. 11 published GHSAs on rustfs/rustfs cover different handlers. Closest:</p>
<ul>
<li>CVE-2026-22042 (ImportIam wrong action constant) -- same bug class, different file</li>
<li>CVE-2026-22043 (deny_only short-circuit) -- different bug class</li>
</ul>
<h3 id="recommendation">Recommendation</h3>
<p>Submit via GitHub PVR. The finding is well-supported with live PoC, code references, and clear root cause. The fix is straightforward (add <code>validate_admin_request</code> calls to event.rs handlers). Core submission should reference 2-3 focused PoC scripts (readonly bypass, target hijack, event exfil), not the full set of 13 exploratory scripts.</p>
<p>Koda Reef</p>
<h3 id="patch">Patch</h3>
<p>This issue has been patched in version <a href="https://github.com/rustfs/rustfs/releases/tag/1.0.0-alpha.94">https://github.com/rustfs/rustfs/releases/tag/1.0.0-alpha.94</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rustfs/rustfs/security/advisories/GHSA-pfcq-4gjr-6gjm">https://github.com/rustfs/rustfs/security/advisories/GHSA-pfcq-4gjr-6gjm</a></li>
<li><a href="https://github.com/rustfs/rustfs/releases/tag/1.0.0-alpha.94">https://github.com/rustfs/rustfs/releases/tag/1.0.0-alpha.94</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-40937">https://nvd.nist.gov/vuln/detail/CVE-2026-40937</a></li>
<li><a href="https://github.com/advisories/GHSA-pfcq-4gjr-6gjm">https://github.com/advisories/GHSA-pfcq-4gjr-6gjm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pfcq-4gjr-6gjm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T19:24:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-blockchain] nimiq-blockchain: Peer-triggerable panic during history sync]]></title>
        <id>https://github.com/advisories/GHSA-j99g-7rqw-q9jg</id>
        <link href="https://github.com/advisories/GHSA-j99g-7rqw-q9jg"/>
        <updated>2026-04-27T16:22:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>HistoryStore::put_historic_txns</code> uses an <code>assert!</code> to enforce invariants about <code>HistoricTransaction.block_number</code> (must be within the macro block being pushed and within the same epoch). During history sync, a peer can influence the <code>history: &amp;[HistoricTransaction]</code> input passed into <code>Blockchain::push_history_sync</code>, and a malformed history list can violate these invariants and trigger a panic.</p>
<p><code>extend_history_sync</code> calls <code>this.history_store.add_to_history(..)</code> before comparing the computed history root against the macro block header (<code>block.history_root()</code>), so the panic can happen before later rejection checks run.</p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/nimiq/core-rs-albatross/commit/6f5511309c199d84b012fe6b9aba7e5582892c50">The patch for this vulnerability</a> is included as part of <a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">v1.3.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No known workarounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-j99g-7rqw-q9jg">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-j99g-7rqw-q9jg</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/pull/3656">https://github.com/nimiq/core-rs-albatross/pull/3656</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/commit/6f5511309c199d84b012fe6b9aba7e5582892c50">https://github.com/nimiq/core-rs-albatross/commit/6f5511309c199d84b012fe6b9aba7e5582892c50</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34066">https://nvd.nist.gov/vuln/detail/CVE-2026-34066</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-j99g-7rqw-q9jg">https://github.com/advisories/GHSA-j99g-7rqw-q9jg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j99g-7rqw-q9jg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T19:23:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-transaction] nimiq-transaction: UpdateValidator transactions allows voting key change without proof-of-knowledge]]></title>
        <id>https://github.com/advisories/GHSA-pf4j-pf3w-95f9</id>
        <link href="https://github.com/advisories/GHSA-pf4j-pf3w-95f9"/>
        <updated>2026-04-27T16:22:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The staking contract accepts <code>UpdateValidator</code> transactions that set <code>new_voting_key=Some(...)</code> while omitting <code>new_proof_of_knowledge</code>. this skips the proof-of-knowledge requirement that is needed to prevent BLS rogue-key attacks when public keys are aggregated.</p>
<p>Because tendermint macro block justification verification aggregates validator voting keys and verifies a single aggregated BLS signature against that aggregate public key, a rogue-key voting key in the validator set can allow an attacker to forge a quorum-looking justification while only producing a single signature.</p>
<p>While the impact is critical, the exploitability is low: The voting keys are fixed for the epoch, so the attacker would need to know the next epoch validator set (chosen through VRF), which is unlikely.</p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/nimiq/core-rs-albatross/commit/e7f0ab7d2115e17d6e5548ddc60f10df1a5d645f">The patch for this vulnerability</a> is included as part of <a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">v1.3.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No known workarounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-pf4j-pf3w-95f9">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-pf4j-pf3w-95f9</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/pull/3654">https://github.com/nimiq/core-rs-albatross/pull/3654</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/commit/e7f0ab7d2115e17d6e5548ddc60f10df1a5d645f">https://github.com/nimiq/core-rs-albatross/commit/e7f0ab7d2115e17d6e5548ddc60f10df1a5d645f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34068">https://nvd.nist.gov/vuln/detail/CVE-2026-34068</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-pf4j-pf3w-95f9">https://github.com/advisories/GHSA-pf4j-pf3w-95f9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pf4j-pf3w-95f9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T19:22:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-transaction] nimiq-transaction: Panic via `HistoryTreeProof` length mismatch]]></title>
        <id>https://github.com/advisories/GHSA-264v-m8fm-76jm</id>
        <link href="https://github.com/advisories/GHSA-264v-m8fm-76jm"/>
        <updated>2026-04-27T16:22:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>HistoryTreeProof::verify</code> panics on a malformed proof where <code>history.len() != positions.len()</code> due to <code>assert_eq!(history.len(), positions.len())</code>. </p>
<p>The proof object is derived from untrusted p2p responses (<code>ResponseTransactionsProof.proof</code>) and is therefore attacker-controlled at the network boundary until validated. A malicious peer could trigger a crash by returning a crafted inclusion proof with a length mismatch.</p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/nimiq/core-rs-albatross/commit/6ff0800e8e031363e787c827d8d033e5694e4e6a">The patch for this vulnerability</a> is included as part of <a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">v1.3.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No known workarounds know.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-264v-m8fm-76jm">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-264v-m8fm-76jm</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/pull/3659">https://github.com/nimiq/core-rs-albatross/pull/3659</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/commit/6ff0800e8e031363e787c827d8d033e5694e4e6a">https://github.com/nimiq/core-rs-albatross/commit/6ff0800e8e031363e787c827d8d033e5694e4e6a</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34067">https://nvd.nist.gov/vuln/detail/CVE-2026-34067</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-264v-m8fm-76jm">https://github.com/advisories/GHSA-264v-m8fm-76jm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-264v-m8fm-76jm</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-22T19:20:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-primitives] nimiq-primitives: Node crash due to missing interlink validation in election macro block proposals]]></title>
        <id>https://github.com/advisories/GHSA-7c4j-2m43-2mgh</id>
        <link href="https://github.com/advisories/GHSA-7c4j-2m43-2mgh"/>
        <updated>2026-04-27T16:22:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An untrusted p2p peer can cause a node to panic by announcing an election macro block whose <code>validators</code> set contains an invalid compressed BLS voting key.</p>
<p>Hashing an election macro header hashes <code>validators</code> and reaches <code>Validators::voting_keys()</code>, which calls <code>validator.voting_key.uncompress().unwrap()</code> and panics on invalid bytes.</p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/nimiq/core-rs-albatross/commit/e10eaebcd7774e5da6d0ff5e88ed13503474f0ff">The patch for this vulnerability</a> is included as part of <a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">v1.3.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No known workarounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-7c4j-2m43-2mgh">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-7c4j-2m43-2mgh</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/pull/3662">https://github.com/nimiq/core-rs-albatross/pull/3662</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/commit/e10eaebcd7774e5da6d0ff5e88ed13503474f0ff">https://github.com/nimiq/core-rs-albatross/commit/e10eaebcd7774e5da6d0ff5e88ed13503474f0ff</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34065">https://nvd.nist.gov/vuln/detail/CVE-2026-34065</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-7c4j-2m43-2mgh">https://github.com/advisories/GHSA-7c4j-2m43-2mgh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7c4j-2m43-2mgh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T19:19:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-account] nimiq-account: Vesting insufficient funds error can panic]]></title>
        <id>https://github.com/advisories/GHSA-vc34-39q2-m6q3</id>
        <link href="https://github.com/advisories/GHSA-vc34-39q2-m6q3"/>
        <updated>2026-04-27T16:22:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>VestingContract::can_change_balance</code> returns <code>AccountError::InsufficientFunds</code> when <code>new_balance &lt; min_cap</code>, but it constructs the error using <code>balance: self.balance - min_cap</code>. <code>Coin::sub</code> panics on underflow, so if an attacker can reach a state where <code>min_cap &gt; balance</code>, the node crashes while trying to return an error.</p>
<p>The <code>min_cap &gt; balance</code> precondition is attacker-reachable because the vesting contract creation data (32-byte format) allows encoding <code>total_amount</code> without validating <code>total_amount &lt;= transaction.value</code> (the real contract balance). After creating such a vesting contract, the attacker can broadcast an outgoing transaction to trigger the panic during mempool admission and block processing.</p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/nimiq/core-rs-albatross/commit/4d01946f0b3d6c6e31786f91cdfb3eb902908da0">The patch for this vulnerability</a> is included as part of <a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">v1.3.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No known workarounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-vc34-39q2-m6q3">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-vc34-39q2-m6q3</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/pull/3658">https://github.com/nimiq/core-rs-albatross/pull/3658</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/commit/4d01946f0b3d6c6e31786f91cdfb3eb902908da0">https://github.com/nimiq/core-rs-albatross/commit/4d01946f0b3d6c6e31786f91cdfb3eb902908da0</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34064">https://nvd.nist.gov/vuln/detail/CVE-2026-34064</a></li>
<li><a href="https://github.com/advisories/GHSA-vc34-39q2-m6q3">https://github.com/advisories/GHSA-vc34-39q2-m6q3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vc34-39q2-m6q3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T19:18:55.000Z</published>
    </entry>
</feed>